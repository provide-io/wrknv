{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"]},"docs":[{"location":"","title":"WrkNv Documentation","text":"<p>Welcome to WrkNv - Work environment management and toolchain automation for modern development workflows.</p>"},{"location":"#features","title":"Features","text":"<p>WrkNv provides:</p> <ul> <li>Environment Management: Automated setup and management of development environments</li> <li>Toolchain Automation: Orchestration of development tools and dependencies</li> <li>Container Integration: Seamless Docker and container workflow support</li> <li>Package Management: Unified package management across languages and platforms</li> <li>Configuration Management: Environment-specific configuration handling</li> <li>Git Integration: Advanced Git workflow automation and management</li> </ul>"},{"location":"#quick-start","title":"Quick Start","text":""},{"location":"#installation","title":"Installation","text":"<pre><code>$ pip install wrknv\n// Installing wrknv...\nSuccessfully installed wrknv\n\n$ wrknv --version\nwrknv, version 0.1.0\n</code></pre>"},{"location":"#initialize-a-new-work-environment","title":"Initialize a New Work Environment","text":"<pre><code>$ wrknv init my-project\n// Creating work environment...\n\u2713 Created workenv/my-project/\n\u2713 Initialized configuration\n\u2713 Generated env.sh\n\u2713 Generated env.ps1\n\nEnvironment initialized successfully!\n\n$ cd my-project\n$ source workenv/*/bin/activate\n// Activating work environment...\n(my-project) $\n</code></pre>"},{"location":"#check-environment-status","title":"Check Environment Status","text":"<pre><code>$ wrknv status\n// Checking environment status...\n\nEnvironment: my-project\nStatus: \u2713 active\nPython: 3.11.12\nUV: 0.5.1\nPlatform: darwin_arm64\n\nTools installed:\n  \u2713 uv\n  \u2713 terraform\n  \u2713 go\n</code></pre>"},{"location":"#manage-tools-and-dependencies","title":"Manage Tools and Dependencies","text":"<pre><code>$ wrknv tool install terraform --version 1.9.0\n// Downloading terraform 1.9.0...\n---&gt; 100%\n\u2713 Installed terraform 1.9.0\n\u2713 Added to PATH\n\n$ wrknv sync\n// Synchronizing dependencies...\nResolved 45 packages\n\u2713 Environment synchronized\n</code></pre>"},{"location":"#python-api-usage","title":"Python API Usage","text":"<pre><code>from wrknv import Environment, Container\n\n# Create and manage development environment\nenv = Environment(\"myproject\")\nenv.setup()\n\n# Work with containers\ncontainer = Container(\"python:3.11\")\ncontainer.run(\"pip install -r requirements.txt\")\n</code></pre>"},{"location":"#api-reference","title":"API Reference","text":"<p>For complete API documentation, see the API Reference.</p>"},{"location":"#core-components","title":"Core Components","text":"<ul> <li>Environment: Development environment orchestration</li> <li>Container: Container and Docker integration</li> <li>Package: Package management and dependency resolution</li> <li>Config: Configuration management utilities</li> <li>CLI: Command-line interface tools</li> </ul>"},{"location":"DESIGN_AND_STRATEGY/","title":"DESIGN_AND_STRATEGY.md - wrknv Architecture and Vision","text":"<p>This document outlines the strategic vision, architectural principles, and key design patterns for the <code>wrknv</code> project.</p>"},{"location":"DESIGN_AND_STRATEGY/#1-strategic-vision","title":"1. Strategic Vision","text":"<p><code>wrknv</code> is evolving from a tool-version manager into a comprehensive developer workflow orchestrator for the Provide.io ecosystem and beyond. Its primary purpose is to provide a reliable, reproducible, and efficient development experience for projects within the Terraform ecosystem.</p> <p>The strategy is to create a central CLI tool that handles: 1.  Environment Setup: Managing versions of tools like OpenTofu, Go, and <code>uv</code>. 2.  Development Lifecycle: Providing containerized development environments. 3.  Building &amp; Packaging: Integrating with <code>flavor</code> to build and distribute provider packages. 4.  Context Management: (Future) A generic system for bundling files and configurations for AI-assisted development.</p>"},{"location":"DESIGN_AND_STRATEGY/#2-core-architectural-principles","title":"2. Core Architectural Principles","text":"<ul> <li>Operational Excellence: All features must be robust, testable, and reliable. Critical infrastructure code requires high test coverage.</li> <li>Type-Safe Configuration: User-facing configuration should be validated against a defined schema to ensure correctness and provide clear, actionable error messages to the user.</li> <li>Extensibility: The system should be designed with clear interfaces (e.g., <code>BaseToolManager</code>, <code>Bundle</code> protocols) to allow for future expansion with new tools or bundle types.</li> <li>Standalone Runtime: The primary output for end-users (<code>env.sh</code>/<code>env.ps1</code>) should be self-contained and not require <code>wrknv</code> to be installed, ensuring maximum portability. <code>wrknv</code> is a development-time tool.</li> </ul>"},{"location":"DESIGN_AND_STRATEGY/#3-key-design-patterns","title":"3. Key Design Patterns","text":""},{"location":"DESIGN_AND_STRATEGY/#31-validated-configuration","title":"3.1. Validated Configuration","text":"<ul> <li>Current State: Basic TOML parsing.</li> <li>Target State: Configuration loaded from <code>wrknv.toml</code> will be validated against an internal schema. This provides:</li> <li>Early failure for invalid configuration.</li> <li>Rich, context-aware error messages.</li> <li>A structured, discoverable configuration API for programmatic use (e.g., by AI agents).</li> </ul>"},{"location":"DESIGN_AND_STRATEGY/#32-sibling-dependency-management","title":"3.2. Sibling Dependency Management","text":"<ul> <li>The <code>siblings</code> configuration in <code>wrknv.toml</code> allows projects to link to other local, editable packages.</li> <li>The generated scripts (<code>env.sh</code>) will attempt to install these siblings. A critical feature is the graceful fallback mechanism: if an installation with dependencies fails (often due to Python version conflicts between a local project and a published dependency), the script should retry with <code>--no-deps</code>. This prioritizes a working local environment.</li> </ul>"},{"location":"DESIGN_AND_STRATEGY/#33-containerized-development","title":"3.3. Containerized Development","text":"<ul> <li>The <code>wrknv container</code> command provides a disposable, consistent development environment.</li> <li>Implementation: The <code>ContainerManager</code> generates a <code>Dockerfile</code> dynamically.</li> <li>Target State: The Dockerfile's contents (base image, system packages) will be configurable in <code>wrknv.toml</code> to adapt to different project requirements.</li> </ul>"},{"location":"DESIGN_AND_STRATEGY/#34-bundler-system-future","title":"3.4. Bundler System (Future)","text":"<ul> <li>Concept: A generic framework for discovering, parsing, and compiling \"bundles\" of files. A bundle is a directory with a specific structure and suffix (e.g., <code>.agents</code>, <code>.garnish</code>).</li> <li>Use Cases:</li> <li>Consolidating AI agent configurations (<code>.agents</code>).</li> <li>Aggregating documentation (<code>.garnish</code>).</li> <li>Processing source code bundles for LLM context (<code>bfiles</code>).</li> <li>Architecture:</li> <li>A <code>Bundle</code> protocol defining the interface.</li> <li>A <code>BundleDiscovery</code> service to find bundles on the filesystem.</li> <li><code>BundleCompiler</code> classes to transform bundles into different outputs (e.g., a single markdown file).</li> </ul>"},{"location":"TODO/","title":"TODO.md - wrknv Development Tasks","text":"<p>This document tracks prioritized development tasks.</p>"},{"location":"TODO/#high-priority-reliability-correctness","title":"High Priority - Reliability &amp; Correctness","text":"<ul> <li>[ ] Increase Test Coverage on Critical Infrastructure</li> <li><code>container</code> module (0% coverage): Add comprehensive tests for Dockerfile generation, container lifecycle (build, start, stop), and command execution.</li> <li><code>operations.download</code> (11% coverage): Add tests for various download scenarios, error handling (404s, timeouts), and checksum verification.</li> <li><code>operations.install</code> (14% coverage): Test archive extraction for different formats and executable permission handling across platforms.</li> <li><code>env.managers</code> (18-24% coverage): Add tests for tool-specific installation logic for <code>ibmtf</code>, <code>tofu</code>, <code>go</code>, and <code>uv</code>.</li> </ul>"},{"location":"TODO/#medium-priority-feature-completeness-architecture","title":"Medium Priority - Feature Completeness &amp; Architecture","text":"<ul> <li>[ ] Implement Ecosystem Test Matrix Command</li> <li>Add <code>wrknv test-matrix</code> command to run tests across all sibling packages</li> <li>Generate comprehensive test report in markdown format</li> <li>Include pass/fail rates, coverage metrics, and identified issues</li> <li>Support parallel test execution for faster results</li> <li> <p>Integrate with CI/CD for automated ecosystem health checks</p> </li> <li> <p>[ ] Fix Test Environment Integration</p> </li> <li>Tests should use wrknv-managed <code>workenv/</code> directories (e.g., <code>workenv/wrknv_darwin_arm64</code>)</li> <li>Currently tests are creating <code>.venv</code> instead of using wrknv's workenv pattern</li> <li>Update test runners to properly activate and use wrknv environments</li> <li> <p>Ensure consistent environment usage across all packages</p> </li> <li> <p>[ ] Implement Type-Safe Configuration</p> </li> <li>Define a validation schema for the <code>wrknv.toml</code> structure.</li> <li> <p>Refactor <code>env.config</code> to validate loaded configuration against this schema, providing clear errors on invalid input. This will improve reliability and user experience.</p> </li> <li> <p>[ ] Complete <code>package</code> Command Implementation</p> </li> <li>Implement the actual logic for <code>sign_package</code> and <code>publish_package</code> by integrating with the <code>flavor</code> API.</li> <li>Remove mock data and <code>NotImplementedError</code> placeholders.</li> <li> <p>Mark commands as experimental in the CLI help text until fully functional.</p> </li> <li> <p>[ ] Make Container Configuration Dynamic</p> </li> <li> <p>Refactor <code>ContainerManager._generate_dockerfile</code> to source its base image and packages from <code>wrknv.toml</code> instead of using hardcoded values.</p> </li> <li> <p>[ ] Refactor Data Classes to Use <code>attrs</code></p> </li> <li>Systematically replace standard classes and dataclasses used for configuration and data modeling with <code>attrs</code> classes for improved validation and robustness.</li> </ul>"},{"location":"TODO/#low-priority-future-enhancements","title":"Low Priority - Future Enhancements","text":"<ul> <li>[ ] Implement Generic Bundler System</li> <li>Design and implement the extensible framework for managing directory-based bundles as proposed in <code>BUNDLER_PROPOSAL.md</code>.</li> <li> <p>This would provide a foundation for managing <code>.agents</code>, <code>.garnish</code>, and <code>bfiles</code> outputs.</p> </li> <li> <p>[ ] Finalize <code>bfiles</code> Integration</p> </li> <li>Implement <code>bfiles</code> as a managed tool and a bundle type within the new bundler system.</li> </ul>"},{"location":"api/","title":"Wrknv API","text":"<p>Auto-generated API documentation for <code>wrknv</code>.</p>"},{"location":"api/#wrknv--wrknv-work-environment-tool","title":"\ud83e\uddf0\ud83c\udf0d wrknv - Work Environment Tool","text":"<p>A flexible tool for managing development environments and tool versions. Supports Terraform, OpenTofu, Go, UV, and more.</p> <p>Can be used standalone or integrated with other tools.</p>"},{"location":"api/#wrknv-attributes","title":"Attributes","text":""},{"location":"api/#wrknv.__version__","title":"__version__  <code>module-attribute</code>","text":"<pre><code>__version__ = get_version('wrknv', caller_file=__file__)\n</code></pre>"},{"location":"api/#wrknv.__all__","title":"__all__  <code>module-attribute</code>","text":"<pre><code>__all__ = [\n    \"WorkenvConfig\",\n    \"WorkenvConfigError\",\n    \"__version__\",\n    \"container\",\n    \"errors\",\n    \"get_supported_tools\",\n    \"get_tool_manager\",\n    \"package\",\n    \"wenv\",\n]\n</code></pre>"},{"location":"api/#wrknv-classes","title":"Classes","text":""},{"location":"api/#wrknv.WorkenvConfig","title":"WorkenvConfig","text":"<p>               Bases: <code>RuntimeConfig</code></p> <p>Main workenv configuration with WRKNV_ environment variable support.</p>"},{"location":"api/#wrknv.WorkenvConfig-attributes","title":"Attributes","text":""},{"location":"api/#wrknv.WorkenvConfig.project_name","title":"project_name  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>project_name: str = field(\n    default=\"my-project\",\n    description=\"Project name\",\n    env_var=\"WRKNV_PROJECT_NAME\",\n)\n</code></pre>"},{"location":"api/#wrknv.WorkenvConfig.version","title":"version  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>version: str = field(\n    default=\"1.0.0\",\n    description=\"Project version\",\n    env_var=\"WRKNV_VERSION\",\n)\n</code></pre>"},{"location":"api/#wrknv.WorkenvConfig.description","title":"description  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>description: str = field(\n    default=\"\",\n    description=\"Project description\",\n    env_var=\"WRKNV_DESCRIPTION\",\n)\n</code></pre>"},{"location":"api/#wrknv.WorkenvConfig.tools","title":"tools  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>tools: dict[str, dict[str, Any]] = field(factory=dict)\n</code></pre>"},{"location":"api/#wrknv.WorkenvConfig.container","title":"container  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>container: ContainerConfig | None = field(default=None)\n</code></pre>"},{"location":"api/#wrknv.WorkenvConfig.profiles","title":"profiles  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>profiles: dict[str, dict[str, str]] = field(factory=dict)\n</code></pre>"},{"location":"api/#wrknv.WorkenvConfig.workenv","title":"workenv  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>workenv: WorkenvSettings = field(factory=WorkenvSettings)\n</code></pre>"},{"location":"api/#wrknv.WorkenvConfig.env","title":"env  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>env: dict[str, Any] = field(factory=dict)\n</code></pre>"},{"location":"api/#wrknv.WorkenvConfig.gitignore","title":"gitignore  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>gitignore: dict[str, Any] = field(factory=dict)\n</code></pre>"},{"location":"api/#wrknv.WorkenvConfig.config_path","title":"config_path  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>config_path: Path | None = field(\n    init=False, repr=False, default=None\n)\n</code></pre>"},{"location":"api/#wrknv.WorkenvConfig-functions","title":"Functions","text":""},{"location":"api/#wrknv.WorkenvConfig.__attrs_post_init__","title":"__attrs_post_init__","text":"<pre><code>__attrs_post_init__()\n</code></pre> <p>Initialize helper instances after attrs initialization.</p> Source code in <code>src/wrknv/config/core.py</code> <pre><code>def __attrs_post_init__(self):\n    \"\"\"Initialize helper instances after attrs initialization.\"\"\"\n    from wrknv.config.display import WorkenvConfigDisplay\n    from wrknv.config.persistence import WorkenvConfigPersistence\n    from wrknv.config.validation import WorkenvConfigValidator\n\n    self._validator = WorkenvConfigValidator(self)\n    self._persistence = WorkenvConfigPersistence(self)\n    self._display = WorkenvConfigDisplay(self)\n</code></pre>"},{"location":"api/#wrknv.WorkenvConfig.load","title":"load  <code>classmethod</code>","text":"<pre><code>load(config_file: Path | None = None) -&gt; WorkenvConfig\n</code></pre> <p>Load configuration from file and environment variables.</p> Source code in <code>src/wrknv/config/core.py</code> <pre><code>@classmethod\ndef load(cls, config_file: Path | None = None) -&gt; WorkenvConfig:\n    \"\"\"Load configuration from file and environment variables.\"\"\"\n    instance = cls()\n    instance.config_path = config_file or instance._find_config_file()\n    instance._manager = instance._create_manager()\n    instance._load_config()\n\n    # Also load from environment variables with WRKNV_ prefix\n    env_config = cls.from_env(prefix=\"WRKNV\")\n\n    # Merge environment config over file config\n    if env_config.project_name != \"my-project\":\n        instance.project_name = env_config.project_name\n    if env_config.version != \"1.0.0\":\n        instance.version = env_config.version\n\n    # Merge workenv settings from environment\n    env_settings = WorkenvSettings.from_env(prefix=\"WRKNV\")\n    if env_settings.log_level != \"WARNING\":\n        instance.workenv.log_level = env_settings.log_level\n    if not env_settings.auto_install:\n        instance.workenv.auto_install = env_settings.auto_install\n    if not env_settings.use_cache:\n        instance.workenv.use_cache = env_settings.use_cache\n    if env_settings.cache_ttl != \"7d\":\n        instance.workenv.cache_ttl = env_settings.cache_ttl\n    if env_settings.container_runtime != \"docker\":\n        instance.workenv.container_runtime = env_settings.container_runtime\n    if env_settings.container_registry != \"ghcr.io\":\n        instance.workenv.container_registry = env_settings.container_registry\n\n    return instance\n</code></pre>"},{"location":"api/#wrknv.WorkenvConfig.config_exists","title":"config_exists","text":"<pre><code>config_exists() -&gt; bool\n</code></pre> <p>Check if configuration file exists.</p> Source code in <code>src/wrknv/config/core.py</code> <pre><code>def config_exists(self) -&gt; bool:\n    \"\"\"Check if configuration file exists.\"\"\"\n    return self._persistence.config_exists()\n</code></pre>"},{"location":"api/#wrknv.WorkenvConfig.get_config_path","title":"get_config_path","text":"<pre><code>get_config_path() -&gt; Path\n</code></pre> <p>Get path to configuration file.</p> Source code in <code>src/wrknv/config/core.py</code> <pre><code>def get_config_path(self) -&gt; Path:\n    \"\"\"Get path to configuration file.\"\"\"\n    return self._persistence.get_config_path()\n</code></pre>"},{"location":"api/#wrknv.WorkenvConfig.get_tool_version","title":"get_tool_version","text":"<pre><code>get_tool_version(tool_name: str) -&gt; str | None\n</code></pre> <p>Get version for a specific tool.</p> Source code in <code>src/wrknv/config/core.py</code> <pre><code>def get_tool_version(self, tool_name: str) -&gt; str | None:\n    \"\"\"Get version for a specific tool.\"\"\"\n    tool_config = self.tools.get(tool_name, {})\n    if isinstance(tool_config, dict):\n        return tool_config.get(\"version\")\n    return tool_config if isinstance(tool_config, str) else None\n</code></pre>"},{"location":"api/#wrknv.WorkenvConfig.get_all_tools","title":"get_all_tools","text":"<pre><code>get_all_tools() -&gt; dict[str, str | list[str]]\n</code></pre> <p>Get all tool versions.</p> Source code in <code>src/wrknv/config/core.py</code> <pre><code>def get_all_tools(self) -&gt; dict[str, str | list[str]]:\n    \"\"\"Get all tool versions.\"\"\"\n    result = {}\n    for tool_name, config in self.tools.items():\n        version = config.get(\"version\") if isinstance(config, dict) else config\n        if version:\n            result[tool_name] = version\n    return result\n</code></pre>"},{"location":"api/#wrknv.WorkenvConfig.get_profile","title":"get_profile","text":"<pre><code>get_profile(profile_name: str) -&gt; dict[str, str] | None\n</code></pre> <p>Get a configuration profile.</p> Source code in <code>src/wrknv/config/core.py</code> <pre><code>def get_profile(self, profile_name: str) -&gt; dict[str, str] | None:\n    \"\"\"Get a configuration profile.\"\"\"\n    return self.profiles.get(profile_name)\n</code></pre>"},{"location":"api/#wrknv.WorkenvConfig.list_profiles","title":"list_profiles","text":"<pre><code>list_profiles() -&gt; list[str]\n</code></pre> <p>List all available profiles.</p> Source code in <code>src/wrknv/config/core.py</code> <pre><code>def list_profiles(self) -&gt; list[str]:\n    \"\"\"List all available profiles.\"\"\"\n    return list(self.profiles.keys())\n</code></pre>"},{"location":"api/#wrknv.WorkenvConfig.profile_exists","title":"profile_exists","text":"<pre><code>profile_exists(profile_name: str) -&gt; bool\n</code></pre> <p>Check if a profile exists.</p> Source code in <code>src/wrknv/config/core.py</code> <pre><code>def profile_exists(self, profile_name: str) -&gt; bool:\n    \"\"\"Check if a profile exists.\"\"\"\n    return profile_name in self.profiles\n</code></pre>"},{"location":"api/#wrknv.WorkenvConfig.save_profile","title":"save_profile","text":"<pre><code>save_profile(\n    profile_name: str, tools: dict[str, str]\n) -&gt; None\n</code></pre> <p>Save a profile.</p> Source code in <code>src/wrknv/config/core.py</code> <pre><code>def save_profile(self, profile_name: str, tools: dict[str, str]) -&gt; None:\n    \"\"\"Save a profile.\"\"\"\n    self.profiles[profile_name] = tools\n    self.save_config()\n</code></pre>"},{"location":"api/#wrknv.WorkenvConfig.delete_profile","title":"delete_profile","text":"<pre><code>delete_profile(profile_name: str) -&gt; bool\n</code></pre> <p>Delete a profile.</p> Source code in <code>src/wrknv/config/core.py</code> <pre><code>def delete_profile(self, profile_name: str) -&gt; bool:\n    \"\"\"Delete a profile.\"\"\"\n    if profile_name in self.profiles:\n        del self.profiles[profile_name]\n        self.save_config()\n        return True\n    return False\n</code></pre>"},{"location":"api/#wrknv.WorkenvConfig.get_current_profile","title":"get_current_profile","text":"<pre><code>get_current_profile() -&gt; str\n</code></pre> <p>Get the current active profile name.</p> Source code in <code>src/wrknv/config/core.py</code> <pre><code>def get_current_profile(self) -&gt; str:\n    \"\"\"Get the current active profile name.\"\"\"\n    # For now, always return \"default\"\n    # TODO: Add support for tracking active profile\n    return \"default\"\n</code></pre>"},{"location":"api/#wrknv.WorkenvConfig.get_setting","title":"get_setting","text":"<pre><code>get_setting(key: str, default: Any = None) -&gt; Any\n</code></pre> <p>Get a configuration setting using dot notation.</p> Source code in <code>src/wrknv/config/core.py</code> <pre><code>def get_setting(self, key: str, default: Any = None) -&gt; Any:\n    \"\"\"Get a configuration setting using dot notation.\"\"\"\n    parts = key.split(\".\")\n    current = self\n\n    for part in parts:\n        if hasattr(current, part):\n            current = getattr(current, part)\n        elif isinstance(current, dict) and part in current:\n            current = current[part]\n        else:\n            return default\n\n    return current\n</code></pre>"},{"location":"api/#wrknv.WorkenvConfig.get_env_config","title":"get_env_config","text":"<pre><code>get_env_config() -&gt; dict[str, Any]\n</code></pre> <p>Get environment configuration for env generation.</p> Source code in <code>src/wrknv/config/core.py</code> <pre><code>def get_env_config(self) -&gt; dict[str, Any]:\n    \"\"\"Get environment configuration for env generation.\"\"\"\n    return self.env\n</code></pre>"},{"location":"api/#wrknv.WorkenvConfig.set_setting","title":"set_setting","text":"<pre><code>set_setting(key: str, value: Any) -&gt; None\n</code></pre> <p>Set a configuration setting using dot notation.</p> Source code in <code>src/wrknv/config/core.py</code> <pre><code>def set_setting(self, key: str, value: Any) -&gt; None:\n    \"\"\"Set a configuration setting using dot notation.\"\"\"\n    parts = key.split(\".\")\n\n    # Navigate to the parent\n    current = self\n    for part in parts[:-1]:\n        if hasattr(current, part):\n            current = getattr(current, part)\n        elif isinstance(current, dict):\n            if part not in current:\n                current[part] = {}\n            current = current[part]\n        else:\n            raise WorkenvConfigError(f\"Cannot set {key}: parent doesn't exist\")\n\n    # Set the value\n    last_part = parts[-1]\n    if hasattr(current, last_part):\n        setattr(current, last_part, value)\n    elif isinstance(current, dict):\n        current[last_part] = value\n    else:\n        raise WorkenvConfigError(f\"Cannot set {key}: invalid target\")\n\n    # Save to file\n    self.save_config()\n</code></pre>"},{"location":"api/#wrknv.WorkenvConfig.save_config","title":"save_config","text":"<pre><code>save_config() -&gt; None\n</code></pre> <p>Save configuration to file.</p> Source code in <code>src/wrknv/config/core.py</code> <pre><code>def save_config(self) -&gt; None:\n    \"\"\"Save configuration to file.\"\"\"\n    self._persistence.save_config()\n</code></pre>"},{"location":"api/#wrknv.WorkenvConfig.to_dict","title":"to_dict","text":"<pre><code>to_dict() -&gt; dict[str, Any]\n</code></pre> <p>Convert configuration to dictionary.</p> Source code in <code>src/wrknv/config/core.py</code> <pre><code>def to_dict(self) -&gt; dict[str, Any]:\n    \"\"\"Convert configuration to dictionary.\"\"\"\n    return self._persistence.to_dict()\n</code></pre>"},{"location":"api/#wrknv.WorkenvConfig.write_config","title":"write_config","text":"<pre><code>write_config(config_data: dict[str, Any]) -&gt; None\n</code></pre> <p>Write configuration data to file.</p> Source code in <code>src/wrknv/config/core.py</code> <pre><code>def write_config(self, config_data: dict[str, Any]) -&gt; None:\n    \"\"\"Write configuration data to file.\"\"\"\n    self._persistence.write_config(config_data)\n</code></pre>"},{"location":"api/#wrknv.WorkenvConfig.edit_config","title":"edit_config","text":"<pre><code>edit_config() -&gt; None\n</code></pre> <p>Open configuration file in editor.</p> Source code in <code>src/wrknv/config/core.py</code> <pre><code>def edit_config(self) -&gt; None:\n    \"\"\"Open configuration file in editor.\"\"\"\n    self._persistence.edit_config()\n</code></pre>"},{"location":"api/#wrknv.WorkenvConfig.show_config","title":"show_config","text":"<pre><code>show_config() -&gt; None\n</code></pre> <p>Display configuration in a readable format.</p> Source code in <code>src/wrknv/config/core.py</code> <pre><code>def show_config(self) -&gt; None:\n    \"\"\"Display configuration in a readable format.\"\"\"\n    self._display.show_config()\n</code></pre>"},{"location":"api/#wrknv.WorkenvConfig.validate","title":"validate","text":"<pre><code>validate() -&gt; tuple[bool, list[str]]\n</code></pre> <p>Validate configuration comprehensively.</p> Source code in <code>src/wrknv/config/core.py</code> <pre><code>def validate(self) -&gt; tuple[bool, list[str]]:\n    \"\"\"Validate configuration comprehensively.\"\"\"\n    return self._validator.validate()\n</code></pre>"},{"location":"api/#wrknv.WorkenvConfig.validate_version","title":"validate_version","text":"<pre><code>validate_version(tool_name: str, version: str) -&gt; bool\n</code></pre> <p>Validate a tool version format.</p> <p>Parameters:</p> Name Type Description Default <code>tool_name</code> <code>str</code> <p>Name of the tool</p> required <code>version</code> <code>str</code> <p>Version string to validate</p> required <p>Returns:</p> Type Description <code>bool</code> <p>True if version is valid, False otherwise</p> Source code in <code>src/wrknv/config/core.py</code> <pre><code>def validate_version(self, tool_name: str, version: str) -&gt; bool:\n    \"\"\"Validate a tool version format.\n\n    Args:\n        tool_name: Name of the tool\n        version: Version string to validate\n\n    Returns:\n        True if version is valid, False otherwise\n    \"\"\"\n    import re\n\n    if not version:\n        return False\n\n    # Allow \"latest\" as a special case\n    if version.lower() in [\"latest\", \"stable\"]:\n        return True\n\n    # Check semantic versioning format (X.Y.Z with optional suffixes)\n    semver_pattern = r\"^\\d+\\.\\d+(\\.\\d+)?(-[a-zA-Z0-9\\.\\-]+)?(\\+[a-zA-Z0-9\\.\\-]+)?$\"\n    if re.match(semver_pattern, version):\n        return True\n\n    # Allow version patterns (for matrix testing)\n    if \"*\" in version or \"~\" in version or \"^\" in version:\n        return True\n\n    return False\n</code></pre>"},{"location":"api/#wrknv.WorkenvConfigError","title":"WorkenvConfigError","text":"<p>               Bases: <code>Exception</code></p> <p>Raised when there's an error in workenv configuration.</p>"},{"location":"api/#wrknv-functions","title":"Functions","text":""},{"location":"api/#wrknv.get_supported_tools","title":"get_supported_tools","text":"<pre><code>get_supported_tools() -&gt; list[str]\n</code></pre> <p>Get list of supported tools.</p> Source code in <code>src/wrknv/managers/factory.py</code> <pre><code>def get_supported_tools() -&gt; list[str]:\n    \"\"\"Get list of supported tools.\"\"\"\n    return [\"ibmtf\", \"tofu\", \"bao\", \"vault\", \"uv\", \"go\"]\n</code></pre>"},{"location":"api/#wrknv.get_tool_manager","title":"get_tool_manager","text":"<pre><code>get_tool_manager(\n    tool_name: str, config: WorkenvConfig | None = None\n) -&gt; BaseToolManager | None\n</code></pre> <p>Get appropriate tool manager for a tool.</p> Source code in <code>src/wrknv/managers/factory.py</code> <pre><code>def get_tool_manager(tool_name: str, config: WorkenvConfig | None = None) -&gt; BaseToolManager | None:\n    \"\"\"Get appropriate tool manager for a tool.\"\"\"\n\n    if config is None:\n        config = WorkenvConfig.load()\n\n    # Terraform ecosystem\n    if tool_name == \"ibmtf\":\n        from .tf.ibm import IbmTfVariant\n\n        return IbmTfVariant(config)\n\n    elif tool_name == \"tofu\":\n        from .tf.tofu import TofuTfVariant\n\n        return TofuTfVariant(config)\n\n    # Secret management (sub rosa)\n    elif tool_name == \"bao\":\n        from .subrosa.bao import BaoVariant\n\n        return BaoVariant(config)\n\n    elif tool_name == \"vault\":\n        from .subrosa.ibm import IbmVaultVariant\n\n        return IbmVaultVariant(config)\n\n    # Single-variant tools\n    elif tool_name == \"uv\":\n        from .uv import UvManager\n\n        return UvManager(config)\n\n    elif tool_name == \"go\":\n        from .go import GoManager\n\n        return GoManager(config)\n\n    # Add more tools as needed:\n    # elif tool_name == \"python\":\n    #     from .python import PythonManager\n    #     return PythonManager(config)\n    #\n    # elif tool_name == \"node\":\n    #     from .node import NodeManager\n    #     return NodeManager(config)\n\n    return None\n</code></pre>"},{"location":"api/config/","title":"Config API","text":"<p>Auto-generated API documentation for <code>wrknv.config</code>.</p>"},{"location":"api/config/#wrknv.config--wrknv-configuration-management","title":"wrknv Configuration Management","text":"<p>Configuration system for wrknv using provide.foundation.</p>"},{"location":"api/config/#wrknv.config-attributes","title":"Attributes","text":""},{"location":"api/config/#wrknv.config.__all__","title":"__all__  <code>module-attribute</code>","text":"<pre><code>__all__ = [\n    \"ConfigSource\",\n    \"EnvironmentConfigSource\",\n    \"FileConfigSource\",\n    \"WorkenvConfig\",\n    \"WorkenvConfigDisplay\",\n    \"WorkenvConfigError\",\n    \"WorkenvConfigPersistence\",\n    \"WorkenvConfigValidator\",\n    \"WorkenvSettings\",\n    \"WorkenvToolConfig\",\n]\n</code></pre>"},{"location":"api/config/#wrknv.config-classes","title":"Classes","text":""},{"location":"api/config/#wrknv.config.WorkenvConfig","title":"WorkenvConfig","text":"<p>               Bases: <code>RuntimeConfig</code></p> <p>Main workenv configuration with WRKNV_ environment variable support.</p>"},{"location":"api/config/#wrknv.config.WorkenvConfig-attributes","title":"Attributes","text":""},{"location":"api/config/#wrknv.config.WorkenvConfig.project_name","title":"project_name  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>project_name: str = field(\n    default=\"my-project\",\n    description=\"Project name\",\n    env_var=\"WRKNV_PROJECT_NAME\",\n)\n</code></pre>"},{"location":"api/config/#wrknv.config.WorkenvConfig.version","title":"version  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>version: str = field(\n    default=\"1.0.0\",\n    description=\"Project version\",\n    env_var=\"WRKNV_VERSION\",\n)\n</code></pre>"},{"location":"api/config/#wrknv.config.WorkenvConfig.description","title":"description  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>description: str = field(\n    default=\"\",\n    description=\"Project description\",\n    env_var=\"WRKNV_DESCRIPTION\",\n)\n</code></pre>"},{"location":"api/config/#wrknv.config.WorkenvConfig.tools","title":"tools  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>tools: dict[str, dict[str, Any]] = field(factory=dict)\n</code></pre>"},{"location":"api/config/#wrknv.config.WorkenvConfig.container","title":"container  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>container: ContainerConfig | None = field(default=None)\n</code></pre>"},{"location":"api/config/#wrknv.config.WorkenvConfig.profiles","title":"profiles  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>profiles: dict[str, dict[str, str]] = field(factory=dict)\n</code></pre>"},{"location":"api/config/#wrknv.config.WorkenvConfig.workenv","title":"workenv  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>workenv: WorkenvSettings = field(factory=WorkenvSettings)\n</code></pre>"},{"location":"api/config/#wrknv.config.WorkenvConfig.env","title":"env  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>env: dict[str, Any] = field(factory=dict)\n</code></pre>"},{"location":"api/config/#wrknv.config.WorkenvConfig.gitignore","title":"gitignore  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>gitignore: dict[str, Any] = field(factory=dict)\n</code></pre>"},{"location":"api/config/#wrknv.config.WorkenvConfig.config_path","title":"config_path  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>config_path: Path | None = field(\n    init=False, repr=False, default=None\n)\n</code></pre>"},{"location":"api/config/#wrknv.config.WorkenvConfig-functions","title":"Functions","text":""},{"location":"api/config/#wrknv.config.WorkenvConfig.__attrs_post_init__","title":"__attrs_post_init__","text":"<pre><code>__attrs_post_init__()\n</code></pre> <p>Initialize helper instances after attrs initialization.</p> Source code in <code>src/wrknv/config/core.py</code> <pre><code>def __attrs_post_init__(self):\n    \"\"\"Initialize helper instances after attrs initialization.\"\"\"\n    from wrknv.config.display import WorkenvConfigDisplay\n    from wrknv.config.persistence import WorkenvConfigPersistence\n    from wrknv.config.validation import WorkenvConfigValidator\n\n    self._validator = WorkenvConfigValidator(self)\n    self._persistence = WorkenvConfigPersistence(self)\n    self._display = WorkenvConfigDisplay(self)\n</code></pre>"},{"location":"api/config/#wrknv.config.WorkenvConfig.load","title":"load  <code>classmethod</code>","text":"<pre><code>load(config_file: Path | None = None) -&gt; WorkenvConfig\n</code></pre> <p>Load configuration from file and environment variables.</p> Source code in <code>src/wrknv/config/core.py</code> <pre><code>@classmethod\ndef load(cls, config_file: Path | None = None) -&gt; WorkenvConfig:\n    \"\"\"Load configuration from file and environment variables.\"\"\"\n    instance = cls()\n    instance.config_path = config_file or instance._find_config_file()\n    instance._manager = instance._create_manager()\n    instance._load_config()\n\n    # Also load from environment variables with WRKNV_ prefix\n    env_config = cls.from_env(prefix=\"WRKNV\")\n\n    # Merge environment config over file config\n    if env_config.project_name != \"my-project\":\n        instance.project_name = env_config.project_name\n    if env_config.version != \"1.0.0\":\n        instance.version = env_config.version\n\n    # Merge workenv settings from environment\n    env_settings = WorkenvSettings.from_env(prefix=\"WRKNV\")\n    if env_settings.log_level != \"WARNING\":\n        instance.workenv.log_level = env_settings.log_level\n    if not env_settings.auto_install:\n        instance.workenv.auto_install = env_settings.auto_install\n    if not env_settings.use_cache:\n        instance.workenv.use_cache = env_settings.use_cache\n    if env_settings.cache_ttl != \"7d\":\n        instance.workenv.cache_ttl = env_settings.cache_ttl\n    if env_settings.container_runtime != \"docker\":\n        instance.workenv.container_runtime = env_settings.container_runtime\n    if env_settings.container_registry != \"ghcr.io\":\n        instance.workenv.container_registry = env_settings.container_registry\n\n    return instance\n</code></pre>"},{"location":"api/config/#wrknv.config.WorkenvConfig.config_exists","title":"config_exists","text":"<pre><code>config_exists() -&gt; bool\n</code></pre> <p>Check if configuration file exists.</p> Source code in <code>src/wrknv/config/core.py</code> <pre><code>def config_exists(self) -&gt; bool:\n    \"\"\"Check if configuration file exists.\"\"\"\n    return self._persistence.config_exists()\n</code></pre>"},{"location":"api/config/#wrknv.config.WorkenvConfig.get_config_path","title":"get_config_path","text":"<pre><code>get_config_path() -&gt; Path\n</code></pre> <p>Get path to configuration file.</p> Source code in <code>src/wrknv/config/core.py</code> <pre><code>def get_config_path(self) -&gt; Path:\n    \"\"\"Get path to configuration file.\"\"\"\n    return self._persistence.get_config_path()\n</code></pre>"},{"location":"api/config/#wrknv.config.WorkenvConfig.get_tool_version","title":"get_tool_version","text":"<pre><code>get_tool_version(tool_name: str) -&gt; str | None\n</code></pre> <p>Get version for a specific tool.</p> Source code in <code>src/wrknv/config/core.py</code> <pre><code>def get_tool_version(self, tool_name: str) -&gt; str | None:\n    \"\"\"Get version for a specific tool.\"\"\"\n    tool_config = self.tools.get(tool_name, {})\n    if isinstance(tool_config, dict):\n        return tool_config.get(\"version\")\n    return tool_config if isinstance(tool_config, str) else None\n</code></pre>"},{"location":"api/config/#wrknv.config.WorkenvConfig.get_all_tools","title":"get_all_tools","text":"<pre><code>get_all_tools() -&gt; dict[str, str | list[str]]\n</code></pre> <p>Get all tool versions.</p> Source code in <code>src/wrknv/config/core.py</code> <pre><code>def get_all_tools(self) -&gt; dict[str, str | list[str]]:\n    \"\"\"Get all tool versions.\"\"\"\n    result = {}\n    for tool_name, config in self.tools.items():\n        version = config.get(\"version\") if isinstance(config, dict) else config\n        if version:\n            result[tool_name] = version\n    return result\n</code></pre>"},{"location":"api/config/#wrknv.config.WorkenvConfig.get_profile","title":"get_profile","text":"<pre><code>get_profile(profile_name: str) -&gt; dict[str, str] | None\n</code></pre> <p>Get a configuration profile.</p> Source code in <code>src/wrknv/config/core.py</code> <pre><code>def get_profile(self, profile_name: str) -&gt; dict[str, str] | None:\n    \"\"\"Get a configuration profile.\"\"\"\n    return self.profiles.get(profile_name)\n</code></pre>"},{"location":"api/config/#wrknv.config.WorkenvConfig.list_profiles","title":"list_profiles","text":"<pre><code>list_profiles() -&gt; list[str]\n</code></pre> <p>List all available profiles.</p> Source code in <code>src/wrknv/config/core.py</code> <pre><code>def list_profiles(self) -&gt; list[str]:\n    \"\"\"List all available profiles.\"\"\"\n    return list(self.profiles.keys())\n</code></pre>"},{"location":"api/config/#wrknv.config.WorkenvConfig.profile_exists","title":"profile_exists","text":"<pre><code>profile_exists(profile_name: str) -&gt; bool\n</code></pre> <p>Check if a profile exists.</p> Source code in <code>src/wrknv/config/core.py</code> <pre><code>def profile_exists(self, profile_name: str) -&gt; bool:\n    \"\"\"Check if a profile exists.\"\"\"\n    return profile_name in self.profiles\n</code></pre>"},{"location":"api/config/#wrknv.config.WorkenvConfig.save_profile","title":"save_profile","text":"<pre><code>save_profile(\n    profile_name: str, tools: dict[str, str]\n) -&gt; None\n</code></pre> <p>Save a profile.</p> Source code in <code>src/wrknv/config/core.py</code> <pre><code>def save_profile(self, profile_name: str, tools: dict[str, str]) -&gt; None:\n    \"\"\"Save a profile.\"\"\"\n    self.profiles[profile_name] = tools\n    self.save_config()\n</code></pre>"},{"location":"api/config/#wrknv.config.WorkenvConfig.delete_profile","title":"delete_profile","text":"<pre><code>delete_profile(profile_name: str) -&gt; bool\n</code></pre> <p>Delete a profile.</p> Source code in <code>src/wrknv/config/core.py</code> <pre><code>def delete_profile(self, profile_name: str) -&gt; bool:\n    \"\"\"Delete a profile.\"\"\"\n    if profile_name in self.profiles:\n        del self.profiles[profile_name]\n        self.save_config()\n        return True\n    return False\n</code></pre>"},{"location":"api/config/#wrknv.config.WorkenvConfig.get_current_profile","title":"get_current_profile","text":"<pre><code>get_current_profile() -&gt; str\n</code></pre> <p>Get the current active profile name.</p> Source code in <code>src/wrknv/config/core.py</code> <pre><code>def get_current_profile(self) -&gt; str:\n    \"\"\"Get the current active profile name.\"\"\"\n    # For now, always return \"default\"\n    # TODO: Add support for tracking active profile\n    return \"default\"\n</code></pre>"},{"location":"api/config/#wrknv.config.WorkenvConfig.get_setting","title":"get_setting","text":"<pre><code>get_setting(key: str, default: Any = None) -&gt; Any\n</code></pre> <p>Get a configuration setting using dot notation.</p> Source code in <code>src/wrknv/config/core.py</code> <pre><code>def get_setting(self, key: str, default: Any = None) -&gt; Any:\n    \"\"\"Get a configuration setting using dot notation.\"\"\"\n    parts = key.split(\".\")\n    current = self\n\n    for part in parts:\n        if hasattr(current, part):\n            current = getattr(current, part)\n        elif isinstance(current, dict) and part in current:\n            current = current[part]\n        else:\n            return default\n\n    return current\n</code></pre>"},{"location":"api/config/#wrknv.config.WorkenvConfig.get_env_config","title":"get_env_config","text":"<pre><code>get_env_config() -&gt; dict[str, Any]\n</code></pre> <p>Get environment configuration for env generation.</p> Source code in <code>src/wrknv/config/core.py</code> <pre><code>def get_env_config(self) -&gt; dict[str, Any]:\n    \"\"\"Get environment configuration for env generation.\"\"\"\n    return self.env\n</code></pre>"},{"location":"api/config/#wrknv.config.WorkenvConfig.set_setting","title":"set_setting","text":"<pre><code>set_setting(key: str, value: Any) -&gt; None\n</code></pre> <p>Set a configuration setting using dot notation.</p> Source code in <code>src/wrknv/config/core.py</code> <pre><code>def set_setting(self, key: str, value: Any) -&gt; None:\n    \"\"\"Set a configuration setting using dot notation.\"\"\"\n    parts = key.split(\".\")\n\n    # Navigate to the parent\n    current = self\n    for part in parts[:-1]:\n        if hasattr(current, part):\n            current = getattr(current, part)\n        elif isinstance(current, dict):\n            if part not in current:\n                current[part] = {}\n            current = current[part]\n        else:\n            raise WorkenvConfigError(f\"Cannot set {key}: parent doesn't exist\")\n\n    # Set the value\n    last_part = parts[-1]\n    if hasattr(current, last_part):\n        setattr(current, last_part, value)\n    elif isinstance(current, dict):\n        current[last_part] = value\n    else:\n        raise WorkenvConfigError(f\"Cannot set {key}: invalid target\")\n\n    # Save to file\n    self.save_config()\n</code></pre>"},{"location":"api/config/#wrknv.config.WorkenvConfig.save_config","title":"save_config","text":"<pre><code>save_config() -&gt; None\n</code></pre> <p>Save configuration to file.</p> Source code in <code>src/wrknv/config/core.py</code> <pre><code>def save_config(self) -&gt; None:\n    \"\"\"Save configuration to file.\"\"\"\n    self._persistence.save_config()\n</code></pre>"},{"location":"api/config/#wrknv.config.WorkenvConfig.to_dict","title":"to_dict","text":"<pre><code>to_dict() -&gt; dict[str, Any]\n</code></pre> <p>Convert configuration to dictionary.</p> Source code in <code>src/wrknv/config/core.py</code> <pre><code>def to_dict(self) -&gt; dict[str, Any]:\n    \"\"\"Convert configuration to dictionary.\"\"\"\n    return self._persistence.to_dict()\n</code></pre>"},{"location":"api/config/#wrknv.config.WorkenvConfig.write_config","title":"write_config","text":"<pre><code>write_config(config_data: dict[str, Any]) -&gt; None\n</code></pre> <p>Write configuration data to file.</p> Source code in <code>src/wrknv/config/core.py</code> <pre><code>def write_config(self, config_data: dict[str, Any]) -&gt; None:\n    \"\"\"Write configuration data to file.\"\"\"\n    self._persistence.write_config(config_data)\n</code></pre>"},{"location":"api/config/#wrknv.config.WorkenvConfig.edit_config","title":"edit_config","text":"<pre><code>edit_config() -&gt; None\n</code></pre> <p>Open configuration file in editor.</p> Source code in <code>src/wrknv/config/core.py</code> <pre><code>def edit_config(self) -&gt; None:\n    \"\"\"Open configuration file in editor.\"\"\"\n    self._persistence.edit_config()\n</code></pre>"},{"location":"api/config/#wrknv.config.WorkenvConfig.show_config","title":"show_config","text":"<pre><code>show_config() -&gt; None\n</code></pre> <p>Display configuration in a readable format.</p> Source code in <code>src/wrknv/config/core.py</code> <pre><code>def show_config(self) -&gt; None:\n    \"\"\"Display configuration in a readable format.\"\"\"\n    self._display.show_config()\n</code></pre>"},{"location":"api/config/#wrknv.config.WorkenvConfig.validate","title":"validate","text":"<pre><code>validate() -&gt; tuple[bool, list[str]]\n</code></pre> <p>Validate configuration comprehensively.</p> Source code in <code>src/wrknv/config/core.py</code> <pre><code>def validate(self) -&gt; tuple[bool, list[str]]:\n    \"\"\"Validate configuration comprehensively.\"\"\"\n    return self._validator.validate()\n</code></pre>"},{"location":"api/config/#wrknv.config.WorkenvConfig.validate_version","title":"validate_version","text":"<pre><code>validate_version(tool_name: str, version: str) -&gt; bool\n</code></pre> <p>Validate a tool version format.</p> <p>Parameters:</p> Name Type Description Default <code>tool_name</code> <code>str</code> <p>Name of the tool</p> required <code>version</code> <code>str</code> <p>Version string to validate</p> required <p>Returns:</p> Type Description <code>bool</code> <p>True if version is valid, False otherwise</p> Source code in <code>src/wrknv/config/core.py</code> <pre><code>def validate_version(self, tool_name: str, version: str) -&gt; bool:\n    \"\"\"Validate a tool version format.\n\n    Args:\n        tool_name: Name of the tool\n        version: Version string to validate\n\n    Returns:\n        True if version is valid, False otherwise\n    \"\"\"\n    import re\n\n    if not version:\n        return False\n\n    # Allow \"latest\" as a special case\n    if version.lower() in [\"latest\", \"stable\"]:\n        return True\n\n    # Check semantic versioning format (X.Y.Z with optional suffixes)\n    semver_pattern = r\"^\\d+\\.\\d+(\\.\\d+)?(-[a-zA-Z0-9\\.\\-]+)?(\\+[a-zA-Z0-9\\.\\-]+)?$\"\n    if re.match(semver_pattern, version):\n        return True\n\n    # Allow version patterns (for matrix testing)\n    if \"*\" in version or \"~\" in version or \"^\" in version:\n        return True\n\n    return False\n</code></pre>"},{"location":"api/config/#wrknv.config.WorkenvConfigError","title":"WorkenvConfigError","text":"<p>               Bases: <code>Exception</code></p> <p>Raised when there's an error in workenv configuration.</p>"},{"location":"api/config/#wrknv.config.WorkenvSettings","title":"WorkenvSettings","text":"<p>               Bases: <code>RuntimeConfig</code></p> <p>Workenv-specific settings with WRKNV_ environment variable support.</p>"},{"location":"api/config/#wrknv.config.WorkenvSettings-attributes","title":"Attributes","text":""},{"location":"api/config/#wrknv.config.WorkenvSettings.auto_install","title":"auto_install  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>auto_install: bool = field(\n    default=True,\n    description=\"Automatically install missing tools\",\n    env_var=\"WRKNV_AUTO_INSTALL\",\n)\n</code></pre>"},{"location":"api/config/#wrknv.config.WorkenvSettings.use_cache","title":"use_cache  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>use_cache: bool = field(\n    default=True,\n    description=\"Use cached tool installations\",\n    env_var=\"WRKNV_USE_CACHE\",\n)\n</code></pre>"},{"location":"api/config/#wrknv.config.WorkenvSettings.cache_ttl","title":"cache_ttl  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>cache_ttl: str = field(\n    default=\"7d\",\n    description=\"Cache time-to-live\",\n    env_var=\"WRKNV_CACHE_TTL\",\n)\n</code></pre>"},{"location":"api/config/#wrknv.config.WorkenvSettings.log_level","title":"log_level  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>log_level: str = field(\n    default=\"WARNING\",\n    description=\"Logging level\",\n    env_var=\"WRKNV_LOG_LEVEL\",\n)\n</code></pre>"},{"location":"api/config/#wrknv.config.WorkenvSettings.container_runtime","title":"container_runtime  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>container_runtime: str = field(\n    default=\"docker\",\n    description=\"Container runtime to use\",\n    env_var=\"WRKNV_CONTAINER_RUNTIME\",\n)\n</code></pre>"},{"location":"api/config/#wrknv.config.WorkenvSettings.container_registry","title":"container_registry  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>container_registry: str = field(\n    default=\"ghcr.io\",\n    description=\"Default container registry\",\n    env_var=\"WRKNV_CONTAINER_REGISTRY\",\n)\n</code></pre>"},{"location":"api/config/#wrknv.config.WorkenvToolConfig","title":"WorkenvToolConfig","text":"<p>Configuration for a single tool.</p>"},{"location":"api/config/#wrknv.config.WorkenvToolConfig-attributes","title":"Attributes","text":""},{"location":"api/config/#wrknv.config.WorkenvToolConfig.version","title":"version  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>version: str | None = None\n</code></pre>"},{"location":"api/config/#wrknv.config.WorkenvToolConfig.path","title":"path  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>path: str | None = None\n</code></pre>"},{"location":"api/config/#wrknv.config.WorkenvToolConfig.env","title":"env  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>env: dict[str, str] = field(factory=dict)\n</code></pre>"},{"location":"api/config/#wrknv.config.WorkenvConfigDisplay","title":"WorkenvConfigDisplay","text":"<pre><code>WorkenvConfigDisplay(config)\n</code></pre> <p>Handles displaying configuration information.</p> <p>Initialize display handler with config instance.</p> Source code in <code>src/wrknv/config/display.py</code> <pre><code>def __init__(self, config) -&gt; None:\n    \"\"\"Initialize display handler with config instance.\"\"\"\n    self.config = config\n</code></pre>"},{"location":"api/config/#wrknv.config.WorkenvConfigDisplay-attributes","title":"Attributes","text":""},{"location":"api/config/#wrknv.config.WorkenvConfigDisplay.config","title":"config  <code>instance-attribute</code>","text":"<pre><code>config = config\n</code></pre>"},{"location":"api/config/#wrknv.config.WorkenvConfigDisplay-functions","title":"Functions","text":""},{"location":"api/config/#wrknv.config.WorkenvConfigDisplay.show_config","title":"show_config","text":"<pre><code>show_config() -&gt; None\n</code></pre> <p>Display configuration in a readable format.</p> Source code in <code>src/wrknv/config/display.py</code> <pre><code>def show_config(self) -&gt; None:\n    \"\"\"Display configuration in a readable format.\"\"\"\n    from provide.foundation.cli import echo_info, echo_success\n\n    echo_success(f\"Configuration: {self.config.config_path}\")\n    echo_info(f\"  Project: {self.config.project_name} v{self.config.version}\")\n\n    if self.config.tools:\n        echo_info(\"\\n  Tools:\")\n        for tool, config in self.config.tools.items():\n            version = config.get(\"version\", \"latest\") if isinstance(config, dict) else config\n            echo_info(f\"    {tool}: {version}\")\n\n    if self.config.profiles:\n        echo_info(\"\\n  Profiles:\")\n        for profile in self.config.profiles:\n            echo_info(f\"    - {profile}\")\n\n    echo_info(\"\\n  Settings:\")\n    echo_info(f\"    auto_install: {self.config.workenv.auto_install}\")\n    echo_info(f\"    use_cache: {self.config.workenv.use_cache}\")\n    echo_info(f\"    cache_ttl: {self.config.workenv.cache_ttl}\")\n    echo_info(f\"    log_level: {self.config.workenv.log_level}\")\n</code></pre>"},{"location":"api/config/#wrknv.config.WorkenvConfigPersistence","title":"WorkenvConfigPersistence","text":"<pre><code>WorkenvConfigPersistence(config)\n</code></pre> <p>Handles saving and loading configuration files.</p> <p>Initialize persistence handler with config instance.</p> Source code in <code>src/wrknv/config/persistence.py</code> <pre><code>def __init__(self, config) -&gt; None:\n    \"\"\"Initialize persistence handler with config instance.\"\"\"\n    self.config = config\n</code></pre>"},{"location":"api/config/#wrknv.config.WorkenvConfigPersistence-attributes","title":"Attributes","text":""},{"location":"api/config/#wrknv.config.WorkenvConfigPersistence.config","title":"config  <code>instance-attribute</code>","text":"<pre><code>config = config\n</code></pre>"},{"location":"api/config/#wrknv.config.WorkenvConfigPersistence-functions","title":"Functions","text":""},{"location":"api/config/#wrknv.config.WorkenvConfigPersistence.load_config","title":"load_config","text":"<pre><code>load_config() -&gt; None\n</code></pre> <p>Load configuration from file.</p> Source code in <code>src/wrknv/config/persistence.py</code> <pre><code>def load_config(self) -&gt; None:\n    \"\"\"Load configuration from file.\"\"\"\n    if self.config.config_path and self.config.config_path.exists():\n        try:\n            # Load TOML using foundation\n            config_dict = read_toml(self.config.config_path, default={})\n\n            # Update attributes from loaded config\n            if \"project_name\" in config_dict:\n                self.config.project_name = config_dict[\"project_name\"]\n            if \"version\" in config_dict:\n                self.config.version = config_dict[\"version\"]\n            if \"tools\" in config_dict:\n                self.config.tools = config_dict[\"tools\"]\n            if \"profiles\" in config_dict:\n                self.config.profiles = config_dict[\"profiles\"]\n            if \"gitignore\" in config_dict:\n                self.config.gitignore = config_dict[\"gitignore\"]\n            if \"workenv\" in config_dict and isinstance(config_dict[\"workenv\"], dict):\n                workenv_data = config_dict[\"workenv\"]\n\n                # Handle nested env configuration (siblings, etc.)\n                if \"env\" in workenv_data:\n                    self.config.env = workenv_data[\"env\"]\n\n                # Set WorkenvSettings attributes\n                for key, value in workenv_data.items():\n                    if key != \"env\" and hasattr(self.config.workenv, key):\n                        setattr(self.config.workenv, key, value)\n\n        except Exception as e:\n            logger.warning(f\"Failed to load config from {self.config.config_path}: {e}\")\n</code></pre>"},{"location":"api/config/#wrknv.config.WorkenvConfigPersistence.save_config","title":"save_config","text":"<pre><code>save_config() -&gt; None\n</code></pre> <p>Save configuration to file.</p> Source code in <code>src/wrknv/config/persistence.py</code> <pre><code>def save_config(self) -&gt; None:\n    \"\"\"Save configuration to file.\"\"\"\n    # Convert to dict\n    config_dict = self.to_dict()\n\n    # Write to file using foundation's atomic TOML writer\n    write_toml(self.config.config_path, config_dict, atomic=True)\n\n    logger.info(f\"Saved configuration to {self.config.config_path}\")\n</code></pre>"},{"location":"api/config/#wrknv.config.WorkenvConfigPersistence.to_dict","title":"to_dict","text":"<pre><code>to_dict() -&gt; dict[str, Any]\n</code></pre> <p>Convert configuration to dictionary.</p> Source code in <code>src/wrknv/config/persistence.py</code> <pre><code>def to_dict(self) -&gt; dict[str, Any]:\n    \"\"\"Convert configuration to dictionary.\"\"\"\n    workenv_dict = {\n        \"auto_install\": self.config.workenv.auto_install,\n        \"use_cache\": self.config.workenv.use_cache,\n        \"cache_ttl\": self.config.workenv.cache_ttl,\n        \"log_level\": self.config.workenv.log_level,\n        \"container_runtime\": self.config.workenv.container_runtime,\n        \"container_registry\": self.config.workenv.container_registry,\n    }\n\n    # Add env configuration under workenv if it exists\n    if self.config.env:\n        workenv_dict[\"env\"] = self.config.env\n\n    config_dict = {\n        \"project_name\": self.config.project_name,\n        \"version\": self.config.version,\n        \"tools\": self.config.tools,\n        \"profiles\": self.config.profiles,\n        \"workenv\": workenv_dict,\n    }\n\n    # Add gitignore configuration if present\n    if self.config.gitignore:\n        config_dict[\"gitignore\"] = self.config.gitignore\n\n    return config_dict\n</code></pre>"},{"location":"api/config/#wrknv.config.WorkenvConfigPersistence.write_config","title":"write_config","text":"<pre><code>write_config(config_data: dict[str, Any]) -&gt; None\n</code></pre> <p>Write configuration data to file.</p> Source code in <code>src/wrknv/config/persistence.py</code> <pre><code>def write_config(self, config_data: dict[str, Any]) -&gt; None:\n    \"\"\"Write configuration data to file.\"\"\"\n    # Update current config\n    if \"project_name\" in config_data:\n        self.config.project_name = config_data[\"project_name\"]\n    if \"version\" in config_data:\n        self.config.version = config_data[\"version\"]\n    if \"tools\" in config_data:\n        self.config.tools = config_data[\"tools\"]\n    if \"profiles\" in config_data:\n        self.config.profiles = config_data[\"profiles\"]\n    if \"gitignore\" in config_data:\n        self.config.gitignore = config_data[\"gitignore\"]\n    if \"workenv\" in config_data and isinstance(config_data[\"workenv\"], dict):\n        workenv_data = config_data[\"workenv\"]\n\n        # Handle nested env configuration\n        if \"env\" in workenv_data:\n            self.config.env = workenv_data[\"env\"]\n\n        # Set WorkenvSettings attributes\n        for key, value in workenv_data.items():\n            if key != \"env\" and hasattr(self.config.workenv, key):\n                setattr(self.config.workenv, key, value)\n\n    # Save to file\n    self.save_config()\n</code></pre>"},{"location":"api/config/#wrknv.config.WorkenvConfigPersistence.edit_config","title":"edit_config","text":"<pre><code>edit_config() -&gt; None\n</code></pre> <p>Open configuration file in editor.</p> Source code in <code>src/wrknv/config/persistence.py</code> <pre><code>def edit_config(self) -&gt; None:\n    \"\"\"Open configuration file in editor.\"\"\"\n    # Ensure file exists\n    if not self.config.config_path.exists():\n        # Create with defaults\n        self.save_config()\n\n    # Get editor from environment\n    editor = os.environ.get(\"EDITOR\") or os.environ.get(\"VISUAL\")\n    if not editor:\n        raise RuntimeError(\"No editor configured. Set EDITOR or VISUAL environment variable.\")\n\n    # Open in editor\n    result = run([editor, str(self.config.config_path)])\n    if result.returncode != 0:\n        raise RuntimeError(f\"Editor exited with error code {result.returncode}\")\n\n    # Reload configuration\n    self.load_config()\n</code></pre>"},{"location":"api/config/#wrknv.config.WorkenvConfigPersistence.config_exists","title":"config_exists","text":"<pre><code>config_exists() -&gt; bool\n</code></pre> <p>Check if configuration file exists.</p> Source code in <code>src/wrknv/config/persistence.py</code> <pre><code>def config_exists(self) -&gt; bool:\n    \"\"\"Check if configuration file exists.\"\"\"\n    return self.config.config_path.exists() if self.config.config_path else False\n</code></pre>"},{"location":"api/config/#wrknv.config.WorkenvConfigPersistence.get_config_path","title":"get_config_path","text":"<pre><code>get_config_path() -&gt; Path\n</code></pre> <p>Get path to configuration file.</p> Source code in <code>src/wrknv/config/persistence.py</code> <pre><code>def get_config_path(self) -&gt; Path:\n    \"\"\"Get path to configuration file.\"\"\"\n    return self.config.config_path\n</code></pre>"},{"location":"api/config/#wrknv.config.ConfigSource","title":"ConfigSource","text":"<p>Base interface for configuration sources.</p>"},{"location":"api/config/#wrknv.config.ConfigSource-functions","title":"Functions","text":""},{"location":"api/config/#wrknv.config.ConfigSource.get_tool_version","title":"get_tool_version","text":"<pre><code>get_tool_version(tool_name: str) -&gt; str | None\n</code></pre> <p>Get version for a specific tool.</p> Source code in <code>src/wrknv/config/sources.py</code> <pre><code>def get_tool_version(self, tool_name: str) -&gt; str | None:\n    \"\"\"Get version for a specific tool.\"\"\"\n    return None\n</code></pre>"},{"location":"api/config/#wrknv.config.ConfigSource.get_all_tools","title":"get_all_tools","text":"<pre><code>get_all_tools() -&gt; dict[str, str]\n</code></pre> <p>Get all tool versions.</p> Source code in <code>src/wrknv/config/sources.py</code> <pre><code>def get_all_tools(self) -&gt; dict[str, str]:\n    \"\"\"Get all tool versions.\"\"\"\n    return {}\n</code></pre>"},{"location":"api/config/#wrknv.config.ConfigSource.get_profile","title":"get_profile","text":"<pre><code>get_profile(profile_name: str) -&gt; dict[str, str]\n</code></pre> <p>Get a configuration profile.</p> Source code in <code>src/wrknv/config/sources.py</code> <pre><code>def get_profile(self, profile_name: str) -&gt; dict[str, str]:\n    \"\"\"Get a configuration profile.\"\"\"\n    return {}\n</code></pre>"},{"location":"api/config/#wrknv.config.ConfigSource.get_setting","title":"get_setting","text":"<pre><code>get_setting(key: str, default: Any = None) -&gt; Any\n</code></pre> <p>Get a configuration setting.</p> Source code in <code>src/wrknv/config/sources.py</code> <pre><code>def get_setting(self, key: str, default: Any = None) -&gt; Any:\n    \"\"\"Get a configuration setting.\"\"\"\n    return default\n</code></pre>"},{"location":"api/config/#wrknv.config.EnvironmentConfigSource","title":"EnvironmentConfigSource","text":"<pre><code>EnvironmentConfigSource(prefix: str = 'WRKNV')\n</code></pre> <p>               Bases: <code>ConfigSource</code></p> <p>Configuration source that loads from environment variables.</p> <p>Initialize with environment variable prefix.</p> Source code in <code>src/wrknv/config/sources.py</code> <pre><code>def __init__(self, prefix: str = \"WRKNV\") -&gt; None:\n    \"\"\"Initialize with environment variable prefix.\"\"\"\n    self.prefix = prefix\n</code></pre>"},{"location":"api/config/#wrknv.config.EnvironmentConfigSource-attributes","title":"Attributes","text":""},{"location":"api/config/#wrknv.config.EnvironmentConfigSource.prefix","title":"prefix  <code>instance-attribute</code>","text":"<pre><code>prefix = prefix\n</code></pre>"},{"location":"api/config/#wrknv.config.EnvironmentConfigSource-functions","title":"Functions","text":""},{"location":"api/config/#wrknv.config.EnvironmentConfigSource.get_tool_version","title":"get_tool_version","text":"<pre><code>get_tool_version(tool_name: str) -&gt; str | None\n</code></pre> <p>Get version for a specific tool.</p> Source code in <code>src/wrknv/config/sources.py</code> <pre><code>def get_tool_version(self, tool_name: str) -&gt; str | None:\n    \"\"\"Get version for a specific tool.\"\"\"\n    # Try both formats for compatibility\n    env_var = f\"{self.prefix}_{tool_name.upper()}_VERSION\"\n    value = os.environ.get(env_var)\n    if value is None:\n        # Also try with TOOL_ prefix\n        env_var = f\"{self.prefix}_TOOL_{tool_name.upper()}_VERSION\"\n        value = os.environ.get(env_var)\n    return value\n</code></pre>"},{"location":"api/config/#wrknv.config.EnvironmentConfigSource.get_all_tools","title":"get_all_tools","text":"<pre><code>get_all_tools() -&gt; dict[str, str]\n</code></pre> <p>Get all tool versions from environment.</p> Source code in <code>src/wrknv/config/sources.py</code> <pre><code>def get_all_tools(self) -&gt; dict[str, str]:\n    \"\"\"Get all tool versions from environment.\"\"\"\n    tools = {}\n    # Check both formats\n    for key, value in os.environ.items():\n        if key.startswith(self.prefix) and key.endswith(\"_VERSION\"):\n            # Try to extract tool name from both formats\n            if f\"{self.prefix}_TOOL_\" in key:\n                # Format: PREFIX_TOOL_TOOLNAME_VERSION\n                prefix = f\"{self.prefix}_TOOL_\"\n                tool_name = key[len(prefix) : -8].lower()\n            else:\n                # Format: PREFIX_TOOLNAME_VERSION\n                prefix = f\"{self.prefix}_\"\n                tool_name = key[len(prefix) : -8].lower()\n                # Skip if it's not a tool (e.g., PROJECT_VERSION)\n                if tool_name in (\"project\", \"wrknv\"):\n                    continue\n            tools[tool_name] = value\n    return tools\n</code></pre>"},{"location":"api/config/#wrknv.config.EnvironmentConfigSource.get_profile","title":"get_profile","text":"<pre><code>get_profile(profile_name: str) -&gt; dict[str, str]\n</code></pre> <p>Get a configuration profile from environment.</p> Source code in <code>src/wrknv/config/sources.py</code> <pre><code>def get_profile(self, profile_name: str) -&gt; dict[str, str]:\n    \"\"\"Get a configuration profile from environment.\"\"\"\n    # Profiles from env vars are tool-specific overrides\n    profile = {}\n    prefix = f\"{self.prefix}_PROFILE_{profile_name.upper()}_\"\n    for key, value in os.environ.items():\n        if key.startswith(prefix):\n            tool_name = key[len(prefix) :].lower()\n            profile[tool_name] = value\n    return profile\n</code></pre>"},{"location":"api/config/#wrknv.config.EnvironmentConfigSource.get_setting","title":"get_setting","text":"<pre><code>get_setting(key: str, default: Any = None) -&gt; Any\n</code></pre> <p>Get a configuration setting from environment.</p> Source code in <code>src/wrknv/config/sources.py</code> <pre><code>def get_setting(self, key: str, default: Any = None) -&gt; Any:\n    \"\"\"Get a configuration setting from environment.\"\"\"\n    env_var = f\"{self.prefix}_{key.upper().replace('.', '_')}\"\n    value = os.environ.get(env_var)\n    if value is None:\n        return default\n    # Try to parse boolean values\n    if value.lower() in (\"true\", \"1\", \"yes\", \"on\"):\n        return True\n    elif value.lower() in (\"false\", \"0\", \"no\", \"off\"):\n        return False\n    return value\n</code></pre>"},{"location":"api/config/#wrknv.config.FileConfigSource","title":"FileConfigSource","text":"<pre><code>FileConfigSource(path: Path, section: str = 'workenv')\n</code></pre> <p>               Bases: <code>ConfigSource</code></p> <p>Configuration source that loads from TOML files.</p> <p>Initialize with file path and optional section.</p> Source code in <code>src/wrknv/config/sources.py</code> <pre><code>def __init__(self, path: Path, section: str = \"workenv\") -&gt; None:\n    \"\"\"Initialize with file path and optional section.\"\"\"\n    self.path = path\n    self.section = section\n    self._data = {}\n    self._load()\n</code></pre>"},{"location":"api/config/#wrknv.config.FileConfigSource-attributes","title":"Attributes","text":""},{"location":"api/config/#wrknv.config.FileConfigSource.path","title":"path  <code>instance-attribute</code>","text":"<pre><code>path = path\n</code></pre>"},{"location":"api/config/#wrknv.config.FileConfigSource.section","title":"section  <code>instance-attribute</code>","text":"<pre><code>section = section\n</code></pre>"},{"location":"api/config/#wrknv.config.FileConfigSource-functions","title":"Functions","text":""},{"location":"api/config/#wrknv.config.FileConfigSource.get_tool_version","title":"get_tool_version","text":"<pre><code>get_tool_version(tool_name: str) -&gt; str | None\n</code></pre> <p>Get version for a specific tool.</p> Source code in <code>src/wrknv/config/sources.py</code> <pre><code>def get_tool_version(self, tool_name: str) -&gt; str | None:\n    \"\"\"Get version for a specific tool.\"\"\"\n    tools = self._data.get(self.section, {}).get(\"tools\", {})\n    return tools.get(tool_name)\n</code></pre>"},{"location":"api/config/#wrknv.config.FileConfigSource.get_all_tools","title":"get_all_tools","text":"<pre><code>get_all_tools() -&gt; dict[str, str]\n</code></pre> <p>Get all tool versions.</p> Source code in <code>src/wrknv/config/sources.py</code> <pre><code>def get_all_tools(self) -&gt; dict[str, str]:\n    \"\"\"Get all tool versions.\"\"\"\n    return self._data.get(self.section, {}).get(\"tools\", {})\n</code></pre>"},{"location":"api/config/#wrknv.config.FileConfigSource.get_profile","title":"get_profile","text":"<pre><code>get_profile(profile_name: str) -&gt; dict[str, str]\n</code></pre> <p>Get a configuration profile.</p> Source code in <code>src/wrknv/config/sources.py</code> <pre><code>def get_profile(self, profile_name: str) -&gt; dict[str, str]:\n    \"\"\"Get a configuration profile.\"\"\"\n    profiles = self._data.get(self.section, {}).get(\"profiles\", {})\n    return profiles.get(profile_name, {})\n</code></pre>"},{"location":"api/config/#wrknv.config.FileConfigSource.get_setting","title":"get_setting","text":"<pre><code>get_setting(key: str, default: Any = None) -&gt; Any\n</code></pre> <p>Get a configuration setting.</p> Source code in <code>src/wrknv/config/sources.py</code> <pre><code>def get_setting(self, key: str, default: Any = None) -&gt; Any:\n    \"\"\"Get a configuration setting.\"\"\"\n    # First check in the section's settings\n    settings = self._data.get(self.section, {}).get(\"settings\", {})\n    if key in settings:\n        return settings[key]\n\n    # Then try navigating from root\n    parts = key.split(\".\")\n    current = self._data\n    for part in parts:\n        if isinstance(current, dict):\n            current = current.get(part)\n            if current is None:\n                return default\n        else:\n            return default\n    return current\n</code></pre>"},{"location":"api/config/#wrknv.config.WorkenvConfigValidator","title":"WorkenvConfigValidator","text":"<pre><code>WorkenvConfigValidator(config)\n</code></pre> <p>Validator for WorkenvConfig instances.</p> <p>Initialize validator with config instance.</p> Source code in <code>src/wrknv/config/validation.py</code> <pre><code>def __init__(self, config) -&gt; None:\n    \"\"\"Initialize validator with config instance.\"\"\"\n    self.config = config\n</code></pre>"},{"location":"api/config/#wrknv.config.WorkenvConfigValidator-attributes","title":"Attributes","text":""},{"location":"api/config/#wrknv.config.WorkenvConfigValidator.config","title":"config  <code>instance-attribute</code>","text":"<pre><code>config = config\n</code></pre>"},{"location":"api/config/#wrknv.config.WorkenvConfigValidator-functions","title":"Functions","text":""},{"location":"api/config/#wrknv.config.WorkenvConfigValidator.validate","title":"validate","text":"<pre><code>validate() -&gt; tuple[bool, list[str]]\n</code></pre> <p>Validate the entire configuration.</p> <p>Returns:</p> Type Description <code>tuple[bool, list[str]]</code> <p>Tuple of (is_valid, error_messages)</p> Source code in <code>src/wrknv/config/validation.py</code> <pre><code>def validate(self) -&gt; tuple[bool, list[str]]:\n    \"\"\"Validate the entire configuration.\n\n    Returns:\n        Tuple of (is_valid, error_messages)\n    \"\"\"\n    errors = []\n\n    # Basic validation\n    if not isinstance(self.config.project_name, str):\n        errors.append(f\"project_name must be string, got {type(self.config.project_name)}\")\n    elif not self.config.project_name.strip():\n        errors.append(\"project_name cannot be empty\")\n    elif not self._is_valid_project_name(self.config.project_name):\n        errors.append(f\"Invalid project_name: {self.config.project_name}\")\n\n    if not isinstance(self.config.version, str):\n        errors.append(f\"version must be string, got {type(self.config.version)}\")\n    elif not self._is_valid_version(self.config.version):\n        errors.append(f\"Invalid version: {self.config.version}\")\n\n    # Validate tools\n    if not isinstance(self.config.tools, dict):\n        errors.append(f\"tools must be dictionary, got {type(self.config.tools)}\")\n    else:\n        for tool_name, tool_config in self.config.tools.items():\n            tool_errors = self._validate_tool_config(tool_name, tool_config)\n            errors.extend(tool_errors)\n\n    # Validate profiles\n    if not isinstance(self.config.profiles, dict):\n        errors.append(f\"profiles must be dictionary, got {type(self.config.profiles)}\")\n    else:\n        for profile_name, profile_config in self.config.profiles.items():\n            profile_errors = self._validate_profile_config(profile_name, profile_config)\n            errors.extend(profile_errors)\n\n    # Validate workenv settings\n    workenv_errors = self._validate_workenv_settings()\n    errors.extend(workenv_errors)\n\n    # Validate env configuration\n    if not isinstance(self.config.env, dict):\n        errors.append(f\"env must be dictionary, got {type(self.config.env)}\")\n    else:\n        env_errors = self._validate_env_config()\n        errors.extend(env_errors)\n\n    return len(errors) == 0, errors\n</code></pre>"},{"location":"api/gitignore/","title":"Gitignore API","text":"<p>Auto-generated API documentation for <code>wrknv.gitignore</code>.</p>"},{"location":"api/gitignore/#wrknv.gitignore--gitignore-management-module","title":"Gitignore Management Module","text":"<p>Comprehensive gitignore file management for wrknv projects.</p>"},{"location":"api/gitignore/#wrknv.gitignore-attributes","title":"Attributes","text":""},{"location":"api/gitignore/#wrknv.gitignore.__all__","title":"__all__  <code>module-attribute</code>","text":"<pre><code>__all__ = [\n    \"GitignoreBuilder\",\n    \"GitignoreManager\",\n    \"ProjectDetector\",\n    \"TemplateHandler\",\n]\n</code></pre>"},{"location":"api/gitignore/#wrknv.gitignore-classes","title":"Classes","text":""},{"location":"api/gitignore/#wrknv.gitignore.GitignoreBuilder","title":"GitignoreBuilder","text":"<pre><code>GitignoreBuilder()\n</code></pre> <p>Builds gitignore files with proper sections and formatting.</p> <p>Initialize the builder.</p> Source code in <code>src/wrknv/gitignore/builder.py</code> <pre><code>def __init__(self):\n    \"\"\"Initialize the builder.\"\"\"\n    self.sections = []\n    self.custom_rules = []\n    logger.debug(\"GitignoreBuilder initialized\")\n</code></pre>"},{"location":"api/gitignore/#wrknv.gitignore.GitignoreBuilder-attributes","title":"Attributes","text":""},{"location":"api/gitignore/#wrknv.gitignore.GitignoreBuilder.sections","title":"sections  <code>instance-attribute</code>","text":"<pre><code>sections = []\n</code></pre>"},{"location":"api/gitignore/#wrknv.gitignore.GitignoreBuilder.custom_rules","title":"custom_rules  <code>instance-attribute</code>","text":"<pre><code>custom_rules = []\n</code></pre>"},{"location":"api/gitignore/#wrknv.gitignore.GitignoreBuilder-functions","title":"Functions","text":""},{"location":"api/gitignore/#wrknv.gitignore.GitignoreBuilder.add_header","title":"add_header","text":"<pre><code>add_header(project_name: str | None = None)\n</code></pre> <p>Add a header section to the gitignore.</p> <p>Parameters:</p> Name Type Description Default <code>project_name</code> <code>str | None</code> <p>Optional project name for the header</p> <code>None</code> Source code in <code>src/wrknv/gitignore/builder.py</code> <pre><code>def add_header(self, project_name: str | None = None):\n    \"\"\"\n    Add a header section to the gitignore.\n\n    Args:\n        project_name: Optional project name for the header\n    \"\"\"\n    header_lines = [\n        \"# ========================\",\n        \"# Generated by wrknv\",\n        f\"# Date: {provide_now().strftime('%Y-%m-%d %H:%M:%S')}\",\n    ]\n\n    if project_name:\n        header_lines.append(f\"# Project: {project_name}\")\n\n    header_lines.extend(\n        [\n            \"# ========================\",\n            \"\",\n            \"# This file is auto-generated. Custom rules should be added\",\n            \"# in the 'Custom Rules' section at the bottom to preserve them\",\n            \"# during regeneration.\",\n            \"\",\n        ]\n    )\n\n    self.sections.insert(0, (\"header\", \"\\n\".join(header_lines)))\n    logger.debug(f\"Added header for project: {project_name}\")\n</code></pre>"},{"location":"api/gitignore/#wrknv.gitignore.GitignoreBuilder.add_template_section","title":"add_template_section","text":"<pre><code>add_template_section(name: str, content: str)\n</code></pre> <p>Add a template section.</p> <p>Parameters:</p> Name Type Description Default <code>name</code> <code>str</code> <p>Template name</p> required <code>content</code> <code>str</code> <p>Template content</p> required Source code in <code>src/wrknv/gitignore/builder.py</code> <pre><code>def add_template_section(self, name: str, content: str):\n    \"\"\"\n    Add a template section.\n\n    Args:\n        name: Template name\n        content: Template content\n    \"\"\"\n    if not content:\n        logger.warning(f\"Empty content for template: {name}\")\n        return\n\n    section_header = f\"\\n# === {name} ===\\n\"\n    section_content = content.strip()\n\n    self.sections.append((name, section_header + section_content))\n    logger.debug(f\"Added template section: {name}\")\n</code></pre>"},{"location":"api/gitignore/#wrknv.gitignore.GitignoreBuilder.add_wrknv_section","title":"add_wrknv_section","text":"<pre><code>add_wrknv_section()\n</code></pre> <p>Add wrknv-specific ignore patterns.</p> Source code in <code>src/wrknv/gitignore/builder.py</code> <pre><code>def add_wrknv_section(self):\n    \"\"\"Add wrknv-specific ignore patterns.\"\"\"\n    wrknv_patterns = [\n        \"\",\n        \"# === wrknv ===\",\n        \"# Work environment directories\",\n        \"workenv/\",\n        \"wrknv_*/\",\n        \"\",\n        \"# wrknv configuration and cache\",\n        \".wrknv/\",\n        \"*.wrknv.bak\",\n        \"\",\n        \"# Container volumes\",\n        \".wrknv_*/\",\n    ]\n\n    self.sections.append((\"wrknv\", \"\\n\".join(wrknv_patterns)))\n    logger.debug(\"Added wrknv-specific patterns\")\n</code></pre>"},{"location":"api/gitignore/#wrknv.gitignore.GitignoreBuilder.add_custom_rules","title":"add_custom_rules","text":"<pre><code>add_custom_rules(rules: list[str])\n</code></pre> <p>Add custom rules.</p> <p>Parameters:</p> Name Type Description Default <code>rules</code> <code>list[str]</code> <p>List of custom ignore patterns</p> required Source code in <code>src/wrknv/gitignore/builder.py</code> <pre><code>def add_custom_rules(self, rules: list[str]):\n    \"\"\"\n    Add custom rules.\n\n    Args:\n        rules: List of custom ignore patterns\n    \"\"\"\n    if not rules:\n        return\n\n    self.custom_rules.extend(rules)\n    logger.debug(f\"Added {len(rules)} custom rules\")\n</code></pre>"},{"location":"api/gitignore/#wrknv.gitignore.GitignoreBuilder.build","title":"build","text":"<pre><code>build(merge_duplicates: bool = True) -&gt; str\n</code></pre> <p>Build the final gitignore content.</p> <p>Parameters:</p> Name Type Description Default <code>merge_duplicates</code> <code>bool</code> <p>Whether to remove duplicate patterns</p> <code>True</code> <p>Returns:</p> Type Description <code>str</code> <p>Complete gitignore file content</p> Source code in <code>src/wrknv/gitignore/builder.py</code> <pre><code>def build(self, merge_duplicates: bool = True) -&gt; str:\n    \"\"\"\n    Build the final gitignore content.\n\n    Args:\n        merge_duplicates: Whether to remove duplicate patterns\n\n    Returns:\n        Complete gitignore file content\n    \"\"\"\n    logger.info(\"Building gitignore file\")\n\n    all_lines = []\n    seen_patterns = set()\n\n    # Process all sections\n    for section_name, section_content in self.sections:\n        if merge_duplicates and section_name != \"header\":\n            # Filter out duplicate patterns\n            filtered_lines = []\n            for line in section_content.split(\"\\n\"):\n                # Skip empty lines and comments for deduplication\n                stripped = line.strip()\n                if stripped and not stripped.startswith(\"#\"):\n                    if stripped not in seen_patterns:\n                        seen_patterns.add(stripped)\n                        filtered_lines.append(line)\n                else:\n                    filtered_lines.append(line)\n\n            if filtered_lines:\n                all_lines.append(\"\\n\".join(filtered_lines))\n                logger.debug(f\"Added section {section_name} with {len(filtered_lines)} lines\")\n        else:\n            all_lines.append(section_content)\n            logger.debug(f\"Added section {section_name} without deduplication\")\n\n    # Add custom rules section if we have any\n    if self.custom_rules:\n        custom_section = [\n            \"\",\n            \"# === Custom Rules ===\",\n            \"# Add your project-specific patterns here\",\n        ]\n\n        for rule in self.custom_rules:\n            # Check for duplicates in custom rules too\n            if not merge_duplicates or rule.strip() not in seen_patterns:\n                custom_section.append(rule)\n                if merge_duplicates and rule.strip() and not rule.strip().startswith(\"#\"):\n                    seen_patterns.add(rule.strip())\n\n        all_lines.append(\"\\n\".join(custom_section))\n        logger.debug(f\"Added {len(self.custom_rules)} custom rules\")\n\n    # Join all sections with double newlines\n    result = \"\\n\\n\".join(all_lines)\n\n    # Ensure file ends with newline\n    if not result.endswith(\"\\n\"):\n        result += \"\\n\"\n\n    logger.info(f\"Built gitignore with {len(seen_patterns)} unique patterns\")\n    return result\n</code></pre>"},{"location":"api/gitignore/#wrknv.gitignore.GitignoreBuilder.merge_with_existing","title":"merge_with_existing","text":"<pre><code>merge_with_existing(existing_path: Path) -&gt; str\n</code></pre> <p>Merge with an existing gitignore file, preserving custom rules.</p> <p>Parameters:</p> Name Type Description Default <code>existing_path</code> <code>Path</code> <p>Path to existing gitignore file</p> required <p>Returns:</p> Type Description <code>str</code> <p>Merged gitignore content</p> Source code in <code>src/wrknv/gitignore/builder.py</code> <pre><code>def merge_with_existing(self, existing_path: Path) -&gt; str:\n    \"\"\"\n    Merge with an existing gitignore file, preserving custom rules.\n\n    Args:\n        existing_path: Path to existing gitignore file\n\n    Returns:\n        Merged gitignore content\n    \"\"\"\n    if not existing_path.exists():\n        logger.debug(f\"No existing file at {existing_path}\")\n        return self.build()\n\n    logger.info(f\"Merging with existing gitignore at {existing_path}\")\n\n    existing_content = existing_path.read_text()\n\n    # Try to extract custom rules from existing file\n    custom_rules = []\n    in_custom_section = False\n\n    for line in existing_content.split(\"\\n\"):\n        if \"=== Custom Rules ===\" in line or \"# Custom\" in line:\n            in_custom_section = True\n            continue\n        elif in_custom_section and line.strip().startswith(\"# ===\"):\n            # End of custom section\n            break\n        elif in_custom_section and line.strip():\n            custom_rules.append(line)\n\n    if custom_rules:\n        logger.debug(f\"Preserved {len(custom_rules)} custom rules from existing file\")\n        self.add_custom_rules(custom_rules)\n\n    return self.build()\n</code></pre>"},{"location":"api/gitignore/#wrknv.gitignore.ProjectDetector","title":"ProjectDetector","text":"<pre><code>ProjectDetector()\n</code></pre> <p>Detects project characteristics to suggest gitignore templates.</p> <p>Initialize the detector.</p> Source code in <code>src/wrknv/gitignore/detector.py</code> <pre><code>def __init__(self):\n    \"\"\"Initialize the detector.\"\"\"\n    self.detected_languages = set()\n    self.detected_frameworks = set()\n    self.detected_tools = set()\n    self.detected_os = set()\n    logger.debug(\"ProjectDetector initialized\")\n</code></pre>"},{"location":"api/gitignore/#wrknv.gitignore.ProjectDetector-attributes","title":"Attributes","text":""},{"location":"api/gitignore/#wrknv.gitignore.ProjectDetector.LANGUAGE_PATTERNS","title":"LANGUAGE_PATTERNS  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>LANGUAGE_PATTERNS = {\n    \"Python\": [\n        \"*.py\",\n        \"requirements.txt\",\n        \"setup.py\",\n        \"pyproject.toml\",\n    ],\n    \"Node\": [\"*.js\", \"*.mjs\", \"*.cjs\", \"package.json\"],\n    \"TypeScript\": [\"*.ts\", \"*.tsx\", \"tsconfig.json\"],\n    \"Go\": [\"*.go\", \"go.mod\", \"go.sum\"],\n    \"Rust\": [\"*.rs\", \"Cargo.toml\", \"Cargo.lock\"],\n    \"Java\": [\"*.java\", \"pom.xml\"],\n    \"Ruby\": [\"*.rb\", \"Gemfile\", \"Gemfile.lock\"],\n    \"PHP\": [\"*.php\", \"composer.json\", \"composer.lock\"],\n    \"C\": [\"*.c\", \"*.h\", \"Makefile\"],\n    \"C++\": [\n        \"*.cpp\",\n        \"*.hpp\",\n        \"*.cc\",\n        \"*.cxx\",\n        \"CMakeLists.txt\",\n    ],\n    \"Swift\": [\"*.swift\", \"Package.swift\"],\n    \"Kotlin\": [\"*.kt\", \"*.kts\"],\n    \"Scala\": [\"*.scala\", \"build.sbt\"],\n    \"Elixir\": [\"*.ex\", \"*.exs\", \"mix.exs\"],\n}\n</code></pre>"},{"location":"api/gitignore/#wrknv.gitignore.ProjectDetector.FRAMEWORK_PATTERNS","title":"FRAMEWORK_PATTERNS  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>FRAMEWORK_PATTERNS = {\n    \"Django\": [\"manage.py\", \"settings.py\", \"urls.py\"],\n    \"Flask\": [\"app.py\", \"flask_app.py\"],\n    \"React\": [\"react\", \"react-dom\"],\n    \"Vue\": [\"vue\"],\n    \"Angular\": [\"@angular/core\"],\n    \"NextJS\": [\"next\"],\n    \"Rails\": [\"Gemfile\", \"config.ru\", \"Rakefile\"],\n    \"Laravel\": [\"artisan\", \"composer.json\"],\n    \"Spring\": [\"pom.xml\", \"build.gradle\"],\n}\n</code></pre>"},{"location":"api/gitignore/#wrknv.gitignore.ProjectDetector.TOOL_PATTERNS","title":"TOOL_PATTERNS  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>TOOL_PATTERNS = {\n    \"Poetry\": [\"poetry.lock\", \"pyproject.toml\"],\n    \"PDM\": [\"pdm.lock\"],\n    \"Pipenv\": [\"Pipfile\", \"Pipfile.lock\"],\n    \"Ruff\": [\".ruff_cache\"],\n    \"Maven\": [\"pom.xml\"],\n    \"Gradle\": [\"build.gradle\", \"build.gradle.kts\"],\n    \"Docker\": [\n        \"Dockerfile\",\n        \"docker-compose.yml\",\n        \"docker-compose.yaml\",\n    ],\n    \"Terraform\": [\"*.tf\", \"*.tfvars\"],\n    \"Kubernetes\": [\"*.yaml\", \"*.yml\"],\n    \"VisualStudioCode\": [\".vscode\"],\n    \"JetBrains\": [\".idea\"],\n    \"Vim\": [\".vimrc\", \".vim\"],\n    \"Emacs\": [\".emacs\", \".emacs.d\"],\n    \"Git\": [\".git\"],\n}\n</code></pre>"},{"location":"api/gitignore/#wrknv.gitignore.ProjectDetector.OS_PATTERNS","title":"OS_PATTERNS  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>OS_PATTERNS = {\n    \"macOS\": [\".DS_Store\", \".AppleDouble\", \".LSOverride\"],\n    \"Windows\": [\"Thumbs.db\", \"Desktop.ini\", \"ehthumbs.db\"],\n    \"Linux\": [\".directory\", \".Trash-*\"],\n}\n</code></pre>"},{"location":"api/gitignore/#wrknv.gitignore.ProjectDetector.detected_languages","title":"detected_languages  <code>instance-attribute</code>","text":"<pre><code>detected_languages = set()\n</code></pre>"},{"location":"api/gitignore/#wrknv.gitignore.ProjectDetector.detected_frameworks","title":"detected_frameworks  <code>instance-attribute</code>","text":"<pre><code>detected_frameworks = set()\n</code></pre>"},{"location":"api/gitignore/#wrknv.gitignore.ProjectDetector.detected_tools","title":"detected_tools  <code>instance-attribute</code>","text":"<pre><code>detected_tools = set()\n</code></pre>"},{"location":"api/gitignore/#wrknv.gitignore.ProjectDetector.detected_os","title":"detected_os  <code>instance-attribute</code>","text":"<pre><code>detected_os = set()\n</code></pre>"},{"location":"api/gitignore/#wrknv.gitignore.ProjectDetector-functions","title":"Functions","text":""},{"location":"api/gitignore/#wrknv.gitignore.ProjectDetector.reset","title":"reset","text":"<pre><code>reset()\n</code></pre> <p>Reset detection state.</p> Source code in <code>src/wrknv/gitignore/detector.py</code> <pre><code>def reset(self):\n    \"\"\"Reset detection state.\"\"\"\n    self.detected_languages.clear()\n    self.detected_frameworks.clear()\n    self.detected_tools.clear()\n    self.detected_os.clear()\n    logger.debug(\"ProjectDetector reset\")\n</code></pre>"},{"location":"api/gitignore/#wrknv.gitignore.ProjectDetector.scan_directory","title":"scan_directory","text":"<pre><code>scan_directory(path: Path, max_depth: int = 5)\n</code></pre> <p>Scan a directory to detect project characteristics.</p> <p>Parameters:</p> Name Type Description Default <code>path</code> <code>Path</code> <p>Directory path to scan</p> required <code>max_depth</code> <code>int</code> <p>Maximum depth to recurse</p> <code>5</code> Source code in <code>src/wrknv/gitignore/detector.py</code> <pre><code>def scan_directory(self, path: Path, max_depth: int = 5):\n    \"\"\"\n    Scan a directory to detect project characteristics.\n\n    Args:\n        path: Directory path to scan\n        max_depth: Maximum depth to recurse\n    \"\"\"\n    path = Path(path)\n    if not path.exists():\n        logger.warning(f\"Path does not exist: {path}\")\n        return\n\n    logger.info(f\"Scanning directory: {path}\")\n\n    # Scan for files recursively\n    self._scan_recursive(path, current_depth=0, max_depth=max_depth)\n\n    # Check for framework-specific files\n    self._detect_frameworks_from_configs(path)\n\n    # Log results\n    logger.debug(f\"Detected languages: {self.detected_languages}\")\n    logger.debug(f\"Detected frameworks: {self.detected_frameworks}\")\n    logger.debug(f\"Detected tools: {self.detected_tools}\")\n    logger.debug(f\"Detected OS: {self.detected_os}\")\n</code></pre>"},{"location":"api/gitignore/#wrknv.gitignore.ProjectDetector.suggest_templates","title":"suggest_templates","text":"<pre><code>suggest_templates() -&gt; list[str]\n</code></pre> <p>Suggest gitignore templates based on detection.</p> <p>Returns:</p> Type Description <code>list[str]</code> <p>List of suggested template names, ordered by priority</p> Source code in <code>src/wrknv/gitignore/detector.py</code> <pre><code>def suggest_templates(self) -&gt; list[str]:\n    \"\"\"\n    Suggest gitignore templates based on detection.\n\n    Returns:\n        List of suggested template names, ordered by priority\n    \"\"\"\n    suggestions = []\n\n    # Add languages first (highest priority)\n    suggestions.extend(sorted(self.detected_languages))\n\n    # Add frameworks\n    suggestions.extend(sorted(self.detected_frameworks))\n\n    # Add tools\n    tool_priority = [\"Docker\", \"Terraform\", \"Poetry\", \"PDM\", \"Pipenv\"]\n    priority_tools = [t for t in tool_priority if t in self.detected_tools]\n    other_tools = sorted(t for t in self.detected_tools if t not in tool_priority)\n    suggestions.extend(priority_tools + other_tools)\n\n    # Add OS last\n    suggestions.extend(sorted(self.detected_os))\n\n    logger.info(f\"Suggested templates: {suggestions}\")\n    return suggestions\n</code></pre>"},{"location":"api/gitignore/#wrknv.gitignore.GitignoreManager","title":"GitignoreManager","text":"<pre><code>GitignoreManager(\n    project_dir: Path | None = None,\n    output_path: Path | None = None,\n    cache_dir: Path | None = None,\n)\n</code></pre> <p>Manages gitignore file generation and updates.</p> <p>Initialize the manager.</p> <p>Parameters:</p> Name Type Description Default <code>project_dir</code> <code>Path | None</code> <p>Project directory to work with</p> <code>None</code> <code>output_path</code> <code>Path | None</code> <p>Custom output path for gitignore file</p> <code>None</code> <code>cache_dir</code> <code>Path | None</code> <p>Custom cache directory for templates</p> <code>None</code> Source code in <code>src/wrknv/gitignore/manager.py</code> <pre><code>def __init__(\n    self,\n    project_dir: Path | None = None,\n    output_path: Path | None = None,\n    cache_dir: Path | None = None,\n):\n    \"\"\"\n    Initialize the manager.\n\n    Args:\n        project_dir: Project directory to work with\n        output_path: Custom output path for gitignore file\n        cache_dir: Custom cache directory for templates\n    \"\"\"\n    self.project_dir = Path(project_dir) if project_dir else Path.cwd()\n    self.gitignore_path = output_path or self.project_dir / \".gitignore\"\n    self.template_handler = TemplateHandler(cache_dir=cache_dir)\n    self.detector = ProjectDetector()\n\n    logger.debug(f\"GitignoreManager initialized for project: {self.project_dir}\")\n    logger.debug(f\"Gitignore path: {self.gitignore_path}\")\n</code></pre>"},{"location":"api/gitignore/#wrknv.gitignore.GitignoreManager-attributes","title":"Attributes","text":""},{"location":"api/gitignore/#wrknv.gitignore.GitignoreManager.project_dir","title":"project_dir  <code>instance-attribute</code>","text":"<pre><code>project_dir = Path(project_dir) if project_dir else cwd()\n</code></pre>"},{"location":"api/gitignore/#wrknv.gitignore.GitignoreManager.gitignore_path","title":"gitignore_path  <code>instance-attribute</code>","text":"<pre><code>gitignore_path = output_path or project_dir / '.gitignore'\n</code></pre>"},{"location":"api/gitignore/#wrknv.gitignore.GitignoreManager.template_handler","title":"template_handler  <code>instance-attribute</code>","text":"<pre><code>template_handler = TemplateHandler(cache_dir=cache_dir)\n</code></pre>"},{"location":"api/gitignore/#wrknv.gitignore.GitignoreManager.detector","title":"detector  <code>instance-attribute</code>","text":"<pre><code>detector = ProjectDetector()\n</code></pre>"},{"location":"api/gitignore/#wrknv.gitignore.GitignoreManager-functions","title":"Functions","text":""},{"location":"api/gitignore/#wrknv.gitignore.GitignoreManager.build_from_templates","title":"build_from_templates","text":"<pre><code>build_from_templates(\n    templates: list[str],\n    custom_rules: list[str] | None = None,\n    exclude_patterns: list[str] | None = None,\n    append: bool = False,\n    project_name: str | None = None,\n) -&gt; bool\n</code></pre> <p>Build gitignore from specified templates.</p> <p>Parameters:</p> Name Type Description Default <code>templates</code> <code>list[str]</code> <p>List of template names</p> required <code>custom_rules</code> <code>list[str] | None</code> <p>Custom rules to add</p> <code>None</code> <code>exclude_patterns</code> <code>list[str] | None</code> <p>Patterns to exclude from templates</p> <code>None</code> <code>append</code> <code>bool</code> <p>Whether to append to existing file</p> <code>False</code> <code>project_name</code> <code>str | None</code> <p>Project name for header</p> <code>None</code> <p>Returns:</p> Type Description <code>bool</code> <p>True if successful</p> Source code in <code>src/wrknv/gitignore/manager.py</code> <pre><code>def build_from_templates(\n    self,\n    templates: list[str],\n    custom_rules: list[str] | None = None,\n    exclude_patterns: list[str] | None = None,\n    append: bool = False,\n    project_name: str | None = None,\n) -&gt; bool:\n    \"\"\"\n    Build gitignore from specified templates.\n\n    Args:\n        templates: List of template names\n        custom_rules: Custom rules to add\n        exclude_patterns: Patterns to exclude from templates\n        append: Whether to append to existing file\n        project_name: Project name for header\n\n    Returns:\n        True if successful\n    \"\"\"\n    logger.info(f\"Building gitignore from templates: {templates}\")\n\n    builder = GitignoreBuilder()\n\n    # Add header unless appending\n    if not append:\n        builder.add_header(project_name=project_name)\n\n    # Add template sections\n    for template_name in templates:\n        content = self.template_handler.get_template(template_name)\n        if content:\n            # Filter excluded patterns\n            if exclude_patterns:\n                lines = content.split(\"\\n\")\n                filtered_lines = []\n                for line in lines:\n                    if not any(pattern in line for pattern in exclude_patterns):\n                        filtered_lines.append(line)\n                content = \"\\n\".join(filtered_lines)\n\n            builder.add_template_section(template_name, content)\n            logger.debug(f\"Added template: {template_name}\")\n        else:\n            logger.warning(f\"Template '{template_name}' not found\")\n\n    # Add wrknv section\n    builder.add_wrknv_section()\n\n    # Add custom rules\n    if custom_rules:\n        builder.add_custom_rules(custom_rules)\n\n    # Build or merge\n    if append and self.gitignore_path.exists():\n        # Append mode - merge with existing\n        existing_content = self.gitignore_path.read_text()\n        new_content = builder.build()\n        final_content = existing_content + \"\\n\\n\" + new_content\n    elif self.gitignore_path.exists():\n        # Overwrite mode but preserve custom rules\n        final_content = builder.merge_with_existing(self.gitignore_path)\n    else:\n        # New file\n        final_content = builder.build()\n\n    # Write the file\n    self.gitignore_path.write_text(final_content)\n    logger.info(f\"Successfully wrote gitignore to {self.gitignore_path}\")\n\n    return True\n</code></pre>"},{"location":"api/gitignore/#wrknv.gitignore.GitignoreManager.build_from_detection","title":"build_from_detection","text":"<pre><code>build_from_detection(\n    custom_rules: list[str] | None = None,\n    exclude_patterns: list[str] | None = None,\n    max_depth: int = 5,\n) -&gt; bool\n</code></pre> <p>Build gitignore from auto-detection.</p> <p>Parameters:</p> Name Type Description Default <code>custom_rules</code> <code>list[str] | None</code> <p>Custom rules to add</p> <code>None</code> <code>exclude_patterns</code> <code>list[str] | None</code> <p>Patterns to exclude</p> <code>None</code> <code>max_depth</code> <code>int</code> <p>Maximum scan depth</p> <code>5</code> <p>Returns:</p> Type Description <code>bool</code> <p>True if successful</p> Source code in <code>src/wrknv/gitignore/manager.py</code> <pre><code>def build_from_detection(\n    self,\n    custom_rules: list[str] | None = None,\n    exclude_patterns: list[str] | None = None,\n    max_depth: int = 5,\n) -&gt; bool:\n    \"\"\"\n    Build gitignore from auto-detection.\n\n    Args:\n        custom_rules: Custom rules to add\n        exclude_patterns: Patterns to exclude\n        max_depth: Maximum scan depth\n\n    Returns:\n        True if successful\n    \"\"\"\n    logger.info(\"Building gitignore from auto-detection\")\n\n    # Scan project\n    self.detector.reset()\n    self.detector.scan_directory(self.project_dir, max_depth=max_depth)\n\n    # Get suggested templates\n    templates = self.detector.suggest_templates()\n\n    if not templates:\n        logger.warning(\"No templates detected\")\n        return False\n\n    logger.info(f\"Auto-detected templates: {templates}\")\n\n    # Build from detected templates\n    return self.build_from_templates(\n        templates=templates,\n        custom_rules=custom_rules,\n        exclude_patterns=exclude_patterns,\n        project_name=self.project_dir.name,\n    )\n</code></pre>"},{"location":"api/gitignore/#wrknv.gitignore.GitignoreManager.build_from_config","title":"build_from_config","text":"<pre><code>build_from_config(config) -&gt; bool\n</code></pre> <p>Build gitignore from configuration object.</p> <p>Parameters:</p> Name Type Description Default <code>config</code> <p>GitignoreConfig object</p> required <p>Returns:</p> Type Description <code>bool</code> <p>True if successful</p> Source code in <code>src/wrknv/gitignore/manager.py</code> <pre><code>def build_from_config(self, config) -&gt; bool:\n    \"\"\"\n    Build gitignore from configuration object.\n\n    Args:\n        config: GitignoreConfig object\n\n    Returns:\n        True if successful\n    \"\"\"\n    logger.info(\"Building gitignore from configuration\")\n\n    return self.build_from_templates(\n        templates=config.templates,\n        custom_rules=config.custom_rules if hasattr(config, \"custom_rules\") else None,\n        exclude_patterns=config.exclude_patterns if hasattr(config, \"exclude_patterns\") else None,\n        append=False,\n    )\n</code></pre>"},{"location":"api/gitignore/#wrknv.gitignore.GitignoreManager.add_templates","title":"add_templates","text":"<pre><code>add_templates(\n    templates: list[str],\n    custom_rules: list[str] | None = None,\n) -&gt; bool\n</code></pre> <p>Add templates to existing gitignore.</p> <p>Parameters:</p> Name Type Description Default <code>templates</code> <code>list[str]</code> <p>Templates to add</p> required <code>custom_rules</code> <code>list[str] | None</code> <p>Additional custom rules</p> <code>None</code> <p>Returns:</p> Type Description <code>bool</code> <p>True if successful</p> Source code in <code>src/wrknv/gitignore/manager.py</code> <pre><code>def add_templates(\n    self,\n    templates: list[str],\n    custom_rules: list[str] | None = None,\n) -&gt; bool:\n    \"\"\"\n    Add templates to existing gitignore.\n\n    Args:\n        templates: Templates to add\n        custom_rules: Additional custom rules\n\n    Returns:\n        True if successful\n    \"\"\"\n    logger.info(f\"Adding templates to existing gitignore: {templates}\")\n\n    return self.build_from_templates(\n        templates=templates,\n        custom_rules=custom_rules,\n        append=True,\n    )\n</code></pre>"},{"location":"api/gitignore/#wrknv.gitignore.GitignoreManager.list_available_templates","title":"list_available_templates","text":"<pre><code>list_available_templates(\n    category: str | None = None,\n) -&gt; list[str]\n</code></pre> <p>List available templates.</p> <p>Parameters:</p> Name Type Description Default <code>category</code> <code>str | None</code> <p>Optional category filter</p> <code>None</code> <p>Returns:</p> Type Description <code>list[str]</code> <p>List of template names</p> Source code in <code>src/wrknv/gitignore/manager.py</code> <pre><code>def list_available_templates(self, category: str | None = None) -&gt; list[str]:\n    \"\"\"\n    List available templates.\n\n    Args:\n        category: Optional category filter\n\n    Returns:\n        List of template names\n    \"\"\"\n    return self.template_handler.list_templates(category=category)\n</code></pre>"},{"location":"api/gitignore/#wrknv.gitignore.GitignoreManager.search_templates","title":"search_templates","text":"<pre><code>search_templates(pattern: str) -&gt; list[str]\n</code></pre> <p>Search for templates.</p> <p>Parameters:</p> Name Type Description Default <code>pattern</code> <code>str</code> <p>Search pattern</p> required <p>Returns:</p> Type Description <code>list[str]</code> <p>List of matching template names</p> Source code in <code>src/wrknv/gitignore/manager.py</code> <pre><code>def search_templates(self, pattern: str) -&gt; list[str]:\n    \"\"\"\n    Search for templates.\n\n    Args:\n        pattern: Search pattern\n\n    Returns:\n        List of matching template names\n    \"\"\"\n    return self.template_handler.search_templates(pattern)\n</code></pre>"},{"location":"api/gitignore/#wrknv.gitignore.GitignoreManager.update_templates","title":"update_templates","text":"<pre><code>update_templates(force: bool = False) -&gt; bool\n</code></pre> <p>Update template cache from GitHub.</p> <p>Parameters:</p> Name Type Description Default <code>force</code> <code>bool</code> <p>Force update even if cache is valid</p> <code>False</code> <p>Returns:</p> Type Description <code>bool</code> <p>True if updated</p> Source code in <code>src/wrknv/gitignore/manager.py</code> <pre><code>def update_templates(self, force: bool = False) -&gt; bool:\n    \"\"\"\n    Update template cache from GitHub.\n\n    Args:\n        force: Force update even if cache is valid\n\n    Returns:\n        True if updated\n    \"\"\"\n    logger.info(\"Updating gitignore templates\")\n    return self.template_handler.update_cache(force=force)\n</code></pre>"},{"location":"api/gitignore/#wrknv.gitignore.GitignoreManager.preview","title":"preview","text":"<pre><code>preview(\n    templates: list[str],\n    custom_rules: list[str] | None = None,\n) -&gt; str\n</code></pre> <p>Preview what would be generated.</p> <p>Parameters:</p> Name Type Description Default <code>templates</code> <code>list[str]</code> <p>Templates to preview</p> required <code>custom_rules</code> <code>list[str] | None</code> <p>Custom rules to include</p> <code>None</code> <p>Returns:</p> Type Description <code>str</code> <p>Preview content</p> Source code in <code>src/wrknv/gitignore/manager.py</code> <pre><code>def preview(\n    self,\n    templates: list[str],\n    custom_rules: list[str] | None = None,\n) -&gt; str:\n    \"\"\"\n    Preview what would be generated.\n\n    Args:\n        templates: Templates to preview\n        custom_rules: Custom rules to include\n\n    Returns:\n        Preview content\n    \"\"\"\n    if not templates:\n        return \"No templates specified\"\n\n    builder = GitignoreBuilder()\n    builder.add_header()\n\n    for template_name in templates:\n        content = self.template_handler.get_template(template_name)\n        if content:\n            builder.add_template_section(template_name, content)\n\n    builder.add_wrknv_section()\n\n    if custom_rules:\n        builder.add_custom_rules(custom_rules)\n\n    return builder.build()\n</code></pre>"},{"location":"api/gitignore/#wrknv.gitignore.GitignoreManager.get_detection_report","title":"get_detection_report","text":"<pre><code>get_detection_report() -&gt; str\n</code></pre> <p>Get a report of detected project characteristics.</p> <p>Returns:</p> Type Description <code>str</code> <p>Detection report as string</p> Source code in <code>src/wrknv/gitignore/manager.py</code> <pre><code>def get_detection_report(self) -&gt; str:\n    \"\"\"\n    Get a report of detected project characteristics.\n\n    Returns:\n        Detection report as string\n    \"\"\"\n    self.detector.reset()\n    self.detector.scan_directory(self.project_dir)\n\n    report_lines = [\"Project Detection Report\", \"=\" * 24, \"\"]\n\n    if self.detector.detected_languages:\n        report_lines.append(f\"Languages: {', '.join(sorted(self.detector.detected_languages))}\")\n\n    if self.detector.detected_frameworks:\n        report_lines.append(f\"Frameworks: {', '.join(sorted(self.detector.detected_frameworks))}\")\n\n    if self.detector.detected_tools:\n        report_lines.append(f\"Tools: {', '.join(sorted(self.detector.detected_tools))}\")\n\n    if self.detector.detected_os:\n        report_lines.append(f\"OS: {', '.join(sorted(self.detector.detected_os))}\")\n\n    if not any(\n        [\n            self.detector.detected_languages,\n            self.detector.detected_frameworks,\n            self.detector.detected_tools,\n            self.detector.detected_os,\n        ]\n    ):\n        report_lines.append(\"No project characteristics detected\")\n\n    return \"\\n\".join(report_lines)\n</code></pre>"},{"location":"api/gitignore/#wrknv.gitignore.TemplateHandler","title":"TemplateHandler","text":"<pre><code>TemplateHandler(cache_dir: Path | None = None)\n</code></pre> <p>Handles gitignore template operations including caching and GitHub sync.</p> <p>Initialize template handler.</p> <p>Parameters:</p> Name Type Description Default <code>cache_dir</code> <code>Path | None</code> <p>Directory for caching templates (default: ~/.wrknv/gitignore-templates)</p> <code>None</code> Source code in <code>src/wrknv/gitignore/templates.py</code> <pre><code>def __init__(self, cache_dir: Path | None = None):\n    \"\"\"\n    Initialize template handler.\n\n    Args:\n        cache_dir: Directory for caching templates (default: ~/.wrknv/gitignore-templates)\n    \"\"\"\n    self.cache_dir = cache_dir or Path.home() / \".wrknv\" / \"gitignore-templates\"\n    self.cache_dir = self.cache_dir.expanduser()\n    logger.debug(f\"TemplateHandler initialized with cache_dir: {self.cache_dir}\")\n\n    # Create cache directory if it doesn't exist\n    if not self.cache_dir.exists():\n        logger.info(f\"Creating gitignore template cache directory: {self.cache_dir}\")\n        self.cache_dir.mkdir(parents=True, exist_ok=True)\n</code></pre>"},{"location":"api/gitignore/#wrknv.gitignore.TemplateHandler-attributes","title":"Attributes","text":""},{"location":"api/gitignore/#wrknv.gitignore.TemplateHandler.GITHUB_REPO","title":"GITHUB_REPO  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>GITHUB_REPO = 'https://github.com/github/gitignore'\n</code></pre>"},{"location":"api/gitignore/#wrknv.gitignore.TemplateHandler.GITHUB_ARCHIVE","title":"GITHUB_ARCHIVE  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>GITHUB_ARCHIVE = \"https://github.com/github/gitignore/archive/refs/heads/main.tar.gz\"\n</code></pre>"},{"location":"api/gitignore/#wrknv.gitignore.TemplateHandler.GITHUB_API","title":"GITHUB_API  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>GITHUB_API = \"https://api.github.com/repos/github/gitignore\"\n</code></pre>"},{"location":"api/gitignore/#wrknv.gitignore.TemplateHandler.cache_dir","title":"cache_dir  <code>instance-attribute</code>","text":"<pre><code>cache_dir = expanduser()\n</code></pre>"},{"location":"api/gitignore/#wrknv.gitignore.TemplateHandler-functions","title":"Functions","text":""},{"location":"api/gitignore/#wrknv.gitignore.TemplateHandler.update_cache","title":"update_cache","text":"<pre><code>update_cache(force: bool = False) -&gt; bool\n</code></pre> <p>Update the template cache from GitHub.</p> <p>Parameters:</p> Name Type Description Default <code>force</code> <code>bool</code> <p>Force update even if cache exists</p> <code>False</code> <p>Returns:</p> Type Description <code>bool</code> <p>True if cache was updated, False otherwise</p> Source code in <code>src/wrknv/gitignore/templates.py</code> <pre><code>def update_cache(self, force: bool = False) -&gt; bool:\n    \"\"\"\n    Update the template cache from GitHub.\n\n    Args:\n        force: Force update even if cache exists\n\n    Returns:\n        True if cache was updated, False otherwise\n    \"\"\"\n    version_file = self.cache_dir / \".version\"\n\n    # Check if update is needed\n    if not force and self._is_cache_valid():\n        logger.debug(\"Template cache is valid, skipping update\")\n        return False\n\n    logger.info(\"Updating gitignore templates from GitHub...\")\n\n    try:\n        # Download archive to temp file using foundation download\n        from wrknv.wenv.operations.download import download_file\n\n        # Use NamedTemporaryFile but don't delete automatically\n        with tempfile.NamedTemporaryFile(suffix=\".tar.gz\", delete=False) as tmp_file:\n            tmp_path = Path(tmp_file.name)\n\n        logger.debug(f\"Downloading templates archive to {tmp_path}\")\n        download_file(self.GITHUB_ARCHIVE, tmp_path, show_progress=False)\n\n        # Extract archive using foundation utilities\n        logger.debug(\"Extracting templates archive\")\n        with tempfile.TemporaryDirectory() as extract_dir:\n            # Extract with security validation (path traversal protection)\n            ArchiveOperations.extract_tar_gz(tmp_path, Path(extract_dir))\n\n            # Find root directory (usually gitignore-main)\n            extracted_items = list(Path(extract_dir).iterdir())\n            if not extracted_items:\n                raise ValueError(\"Archive is empty\")\n\n            source_dir = extracted_items[0] if len(extracted_items) == 1 else Path(extract_dir)\n\n            # Clear existing cache\n            if self.cache_dir.exists():\n                logger.debug(\"Clearing existing cache\")\n                safe_rmtree(self.cache_dir)\n\n            # Move extracted files to cache\n            safe_move(source_dir, self.cache_dir)\n\n        # Clean up temp file\n        tmp_path.unlink()\n\n        # Update version file with current commit\n        self._update_version_file()\n\n        logger.info(f\"Successfully updated templates in {self.cache_dir}\")\n        return True\n\n    except Exception as e:\n        logger.error(f\"Failed to update template cache: {e}\")\n        return False\n</code></pre>"},{"location":"api/gitignore/#wrknv.gitignore.TemplateHandler.get_template","title":"get_template","text":"<pre><code>get_template(name: str) -&gt; str | None\n</code></pre> <p>Get the content of a gitignore template.</p> <p>Parameters:</p> Name Type Description Default <code>name</code> <code>str</code> <p>Template name (e.g., \"Python\", \"Node\")</p> required <p>Returns:</p> Type Description <code>str | None</code> <p>Template content or None if not found</p> Source code in <code>src/wrknv/gitignore/templates.py</code> <pre><code>def get_template(self, name: str) -&gt; str | None:\n    \"\"\"\n    Get the content of a gitignore template.\n\n    Args:\n        name: Template name (e.g., \"Python\", \"Node\")\n\n    Returns:\n        Template content or None if not found\n    \"\"\"\n    # Ensure cache is populated\n    if not self._is_cache_valid():\n        logger.info(\"Cache invalid, updating templates...\")\n        self.update_cache()\n\n    # Try different locations for the template\n    possible_paths = [\n        self.cache_dir / f\"{name}.gitignore\",\n        self.cache_dir / \"Global\" / f\"{name}.gitignore\",\n        self.cache_dir / \"community\" / f\"{name}.gitignore\",\n    ]\n\n    for path in possible_paths:\n        if path.exists():\n            logger.debug(f\"Found template at {path}\")\n            return path.read_text()\n\n    logger.warning(f\"Template '{name}' not found in cache\")\n    return None\n</code></pre>"},{"location":"api/gitignore/#wrknv.gitignore.TemplateHandler.list_templates","title":"list_templates","text":"<pre><code>list_templates(category: str | None = None) -&gt; list[str]\n</code></pre> <p>List available templates.</p> <p>Parameters:</p> Name Type Description Default <code>category</code> <code>str | None</code> <p>Optional category filter (e.g., \"Global\", \"community\")</p> <code>None</code> <p>Returns:</p> Type Description <code>list[str]</code> <p>List of template names</p> Source code in <code>src/wrknv/gitignore/templates.py</code> <pre><code>def list_templates(self, category: str | None = None) -&gt; list[str]:\n    \"\"\"\n    List available templates.\n\n    Args:\n        category: Optional category filter (e.g., \"Global\", \"community\")\n\n    Returns:\n        List of template names\n    \"\"\"\n    # Ensure cache is populated\n    if not self._is_cache_valid():\n        logger.info(\"Cache invalid, updating templates...\")\n        self.update_cache()\n\n    templates = []\n\n    if category:\n        # List templates in specific category\n        category_dir = self.cache_dir / category\n        if category_dir.exists():\n            logger.debug(f\"Listing templates in category: {category}\")\n            for file in category_dir.glob(\"*.gitignore\"):\n                templates.append(file.stem)\n    else:\n        # List all templates\n        logger.debug(\"Listing all templates\")\n\n        # Root level templates\n        for file in self.cache_dir.glob(\"*.gitignore\"):\n            templates.append(file.stem)\n\n        # Global templates\n        global_dir = self.cache_dir / \"Global\"\n        if global_dir.exists():\n            for file in global_dir.glob(\"*.gitignore\"):\n                templates.append(f\"Global/{file.stem}\")\n\n        # Community templates\n        community_dir = self.cache_dir / \"community\"\n        if community_dir.exists():\n            for file in community_dir.rglob(\"*.gitignore\"):\n                relative = file.relative_to(community_dir)\n                templates.append(f\"community/{relative.with_suffix('').as_posix()}\")\n\n    logger.debug(f\"Found {len(templates)} templates\")\n    return sorted(templates)\n</code></pre>"},{"location":"api/gitignore/#wrknv.gitignore.TemplateHandler.search_templates","title":"search_templates","text":"<pre><code>search_templates(pattern: str) -&gt; list[str]\n</code></pre> <p>Search for templates matching a pattern.</p> <p>Parameters:</p> Name Type Description Default <code>pattern</code> <code>str</code> <p>Search pattern (case-insensitive)</p> required <p>Returns:</p> Type Description <code>list[str]</code> <p>List of matching template names</p> Source code in <code>src/wrknv/gitignore/templates.py</code> <pre><code>def search_templates(self, pattern: str) -&gt; list[str]:\n    \"\"\"\n    Search for templates matching a pattern.\n\n    Args:\n        pattern: Search pattern (case-insensitive)\n\n    Returns:\n        List of matching template names\n    \"\"\"\n    pattern_lower = pattern.lower()\n    all_templates = self.list_templates()\n\n    matches = [t for t in all_templates if pattern_lower in t.lower()]\n    logger.debug(f\"Found {len(matches)} templates matching '{pattern}'\")\n\n    return matches\n</code></pre>"},{"location":"api/cli/","title":"Cli API","text":"<p>Auto-generated API documentation for <code>wrknv.cli</code>.</p>"},{"location":"api/cli/#wrknv.cli--wrknv-cli-module","title":"wrknv CLI Module","text":"<p>Command-line interface for wrknv tool management.</p>"},{"location":"api/cli/#wrknv.cli-attributes","title":"Attributes","text":""},{"location":"api/cli/#wrknv.cli.entry_point","title":"entry_point  <code>module-attribute</code>","text":"<pre><code>entry_point = main\n</code></pre>"},{"location":"api/cli/#wrknv.cli.__all__","title":"__all__  <code>module-attribute</code>","text":"<pre><code>__all__ = ['create_cli', 'entry_point', 'main']\n</code></pre>"},{"location":"api/cli/#wrknv.cli-functions","title":"Functions","text":""},{"location":"api/cli/#wrknv.cli.create_cli","title":"create_cli","text":"<pre><code>create_cli()\n</code></pre> <p>Create the main CLI application using the hub.</p> Source code in <code>src/wrknv/cli/hub_cli.py</code> <pre><code>def create_cli():\n    \"\"\"Create the main CLI application using the hub.\"\"\"\n    # Get or create hub\n    hub = get_hub()\n\n    # Clear existing command registrations to allow re-creation in tests\n    # This prevents AlreadyExistsError when create_cli() is called multiple times\n    from provide.foundation.hub.categories import ComponentCategory\n\n    hub.clear(dimension=ComponentCategory.COMMAND.value)\n\n    # Reset config cache when creating CLI (for test isolation)\n    WrknvContext.reset()\n\n    # Load all commands (this will register them in the cleared registry)\n    load_commands()\n\n    # Create CLI with standard options\n    cli = hub.create_cli(\n        name=\"wrknv\",\n        version=\"0.1.0\",\n        help=\"\ud83e\uddf0\ud83c\udf0d Manage development environment tools and versions.\\n\\n\"\n        \"wrknv provides cross-platform tool installation and version management \"\n        \"for development environments, including Terraform, OpenTofu, Go, UV, and more.\",\n    )\n\n    return cli\n</code></pre>"},{"location":"api/cli/#wrknv.cli.main","title":"main","text":"<pre><code>main()\n</code></pre> <p>Main entry point for the CLI.</p> Source code in <code>src/wrknv/cli/hub_cli.py</code> <pre><code>def main():\n    \"\"\"Main entry point for the CLI.\"\"\"\n    # Note: Logging setup disabled for now due to missing dependencies\n    # from wrknv.logging.setup import setup_wrknv_logging\n    # setup_wrknv_logging()\n\n    cli = create_cli()\n    cli()\n</code></pre>"},{"location":"api/container/","title":"Container API","text":"<p>Auto-generated API documentation for <code>wrknv.container</code>.</p>"},{"location":"api/container/#wrknv.container--container-management-module","title":"Container Management Module","text":"<p>Docker container management for wrknv development environments.</p>"},{"location":"api/container/#wrknv.container-attributes","title":"Attributes","text":""},{"location":"api/container/#wrknv.container.__all__","title":"__all__  <code>module-attribute</code>","text":"<pre><code>__all__ = [\n    \"ContainerManager\",\n    \"build_container\",\n    \"clean_container\",\n    \"container_logs\",\n    \"container_status\",\n    \"enter_container\",\n    \"rebuild_container\",\n    \"restart_container\",\n    \"start_container\",\n    \"stop_container\",\n]\n</code></pre>"},{"location":"api/container/#wrknv.container-classes","title":"Classes","text":""},{"location":"api/container/#wrknv.container.ContainerManager","title":"ContainerManager","text":"<p>Orchestrates container operations through specialized components.</p>"},{"location":"api/container/#wrknv.container.ContainerManager-attributes","title":"Attributes","text":""},{"location":"api/container/#wrknv.container.ContainerManager.config","title":"config  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>config: WorkenvConfig = field()\n</code></pre>"},{"location":"api/container/#wrknv.container.ContainerManager.console","title":"console  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>console: Console = field(factory=Console)\n</code></pre>"},{"location":"api/container/#wrknv.container.ContainerManager.container_name","title":"container_name  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>container_name: str = field(init=False)\n</code></pre>"},{"location":"api/container/#wrknv.container.ContainerManager.image_name","title":"image_name  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>image_name: str = field(init=False)\n</code></pre>"},{"location":"api/container/#wrknv.container.ContainerManager.image_tag","title":"image_tag  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>image_tag: str = field(init=False, default='latest')\n</code></pre>"},{"location":"api/container/#wrknv.container.ContainerManager.full_image","title":"full_image  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>full_image: str = field(init=False)\n</code></pre>"},{"location":"api/container/#wrknv.container.ContainerManager.container_config","title":"container_config  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>container_config: ContainerConfig = field(init=False)\n</code></pre>"},{"location":"api/container/#wrknv.container.ContainerManager.storage","title":"storage  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>storage: ContainerStorage = field(init=False)\n</code></pre>"},{"location":"api/container/#wrknv.container.ContainerManager.runtime","title":"runtime  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>runtime: DockerRuntime = field(init=False)\n</code></pre>"},{"location":"api/container/#wrknv.container.ContainerManager.lifecycle","title":"lifecycle  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>lifecycle: ContainerLifecycle = field(init=False)\n</code></pre>"},{"location":"api/container/#wrknv.container.ContainerManager.exec","title":"exec  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>exec: ContainerExec = field(init=False)\n</code></pre>"},{"location":"api/container/#wrknv.container.ContainerManager.builder","title":"builder  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>builder: ContainerBuilder = field(init=False)\n</code></pre>"},{"location":"api/container/#wrknv.container.ContainerManager.logs","title":"logs  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>logs: ContainerLogs = field(init=False)\n</code></pre>"},{"location":"api/container/#wrknv.container.ContainerManager.volumes","title":"volumes  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>volumes: VolumeManager = field(init=False)\n</code></pre>"},{"location":"api/container/#wrknv.container.ContainerManager.metadata","title":"metadata  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>metadata: ContainerMetadata = field(init=False)\n</code></pre>"},{"location":"api/container/#wrknv.container.ContainerManager-functions","title":"Functions","text":""},{"location":"api/container/#wrknv.container.ContainerManager.__attrs_post_init__","title":"__attrs_post_init__","text":"<pre><code>__attrs_post_init__()\n</code></pre> <p>Initialize container components.</p> Source code in <code>src/wrknv/container/manager.py</code> <pre><code>def __attrs_post_init__(self):\n    \"\"\"Initialize container components.\"\"\"\n    # Get container configuration\n    self.container_config = self.config.container or ContainerConfig()\n\n    # Set container and image names\n    project_name = self.config.project_name.replace(\" \", \"-\").lower()\n    self.container_name = f\"{project_name}-dev\" if project_name != \"my-project\" else \"wrknv-dev\"\n    self.image_name = self.container_name\n    self.full_image = f\"{self.image_name}:{self.image_tag}\"\n\n    # Initialize storage\n    self.storage = ContainerStorage(\n        container_name=self.container_name,\n        container_config=self.container_config,\n    )\n    self.storage.setup_storage()\n\n    # Initialize runtime\n    self.runtime = DockerRuntime(runtime_name=\"docker\", runtime_command=\"docker\")\n\n    # Initialize operations\n    self.lifecycle = ContainerLifecycle(\n        runtime=self.runtime,\n        container_name=self.container_name,\n        console=self.console,\n        start_emoji=\"\ud83d\ude80\",\n        stop_emoji=\"\u23f9\ufe0f\",\n        restart_emoji=\"\ud83d\udd04\",\n        status_emoji=\"\ud83d\udcca\",\n    )\n\n    self.exec = ContainerExec(\n        runtime=self.runtime,\n        container_name=self.container_name,\n        console=self.console,\n        available_shells=[\"/bin/bash\", \"/bin/sh\"],\n        default_shell=\"/bin/bash\",\n    )\n\n    self.builder = ContainerBuilder(runtime=self.runtime, console=self.console)\n\n    self.logs = ContainerLogs(\n        runtime=self.runtime, container_name=self.container_name, console=self.console\n    )\n\n    self.volumes = VolumeManager(\n        runtime=self.runtime,\n        console=self.console,\n        backup_dir=self.storage.get_container_path(\"backups\"),\n    )\n\n    self.metadata = ContainerMetadata(\n        storage=self.storage,\n        container_name=self.container_name,\n        image_name=self.full_image,\n        config=self.config,\n    )\n</code></pre>"},{"location":"api/container/#wrknv.container.ContainerManager.check_docker","title":"check_docker","text":"<pre><code>check_docker() -&gt; bool\n</code></pre> <p>Check if Docker is available and running.</p> Source code in <code>src/wrknv/container/manager.py</code> <pre><code>def check_docker(self) -&gt; bool:\n    \"\"\"Check if Docker is available and running.\"\"\"\n    try:\n        return self.runtime.is_available()\n    except RuntimeError:\n        return False\n</code></pre>"},{"location":"api/container/#wrknv.container.ContainerManager.container_exists","title":"container_exists","text":"<pre><code>container_exists() -&gt; bool\n</code></pre> <p>Check if the container exists.</p> Source code in <code>src/wrknv/container/manager.py</code> <pre><code>def container_exists(self) -&gt; bool:\n    \"\"\"Check if the container exists.\"\"\"\n    return self.lifecycle.exists()\n</code></pre>"},{"location":"api/container/#wrknv.container.ContainerManager.container_running","title":"container_running","text":"<pre><code>container_running() -&gt; bool\n</code></pre> <p>Check if the container is currently running.</p> Source code in <code>src/wrknv/container/manager.py</code> <pre><code>def container_running(self) -&gt; bool:\n    \"\"\"Check if the container is currently running.\"\"\"\n    return self.lifecycle.is_running()\n</code></pre>"},{"location":"api/container/#wrknv.container.ContainerManager.image_exists","title":"image_exists","text":"<pre><code>image_exists() -&gt; bool\n</code></pre> <p>Check if the container image exists.</p> Source code in <code>src/wrknv/container/manager.py</code> <pre><code>def image_exists(self) -&gt; bool:\n    \"\"\"Check if the container image exists.\"\"\"\n    return self.builder.image_exists(self.full_image)\n</code></pre>"},{"location":"api/container/#wrknv.container.ContainerManager.build_image","title":"build_image","text":"<pre><code>build_image(rebuild: bool = False) -&gt; bool\n</code></pre> <p>Build the container image.</p> Source code in <code>src/wrknv/container/manager.py</code> <pre><code>def build_image(self, rebuild: bool = False) -&gt; bool:\n    \"\"\"Build the container image.\"\"\"\n    build_dir = self.storage.get_container_path(\"build\")\n    build_dir.mkdir(parents=True, exist_ok=True)\n\n    # Generate and save Dockerfile\n    dockerfile_path = build_dir / \"Dockerfile\"\n    dockerfile_content = self._generate_dockerfile()\n    dockerfile_path.write_text(dockerfile_content)\n\n    # Build arguments from config\n    build_args = {}\n    if self.container_config.environment:\n        build_args.update(self.container_config.environment)\n\n    # Build the image\n    return self.builder.build(\n        dockerfile=str(dockerfile_path),\n        tag=self.full_image,\n        context=str(build_dir),\n        build_args=build_args,\n        stream_output=False,\n    )\n</code></pre>"},{"location":"api/container/#wrknv.container.ContainerManager.start","title":"start","text":"<pre><code>start(force_rebuild: bool = False) -&gt; bool\n</code></pre> <p>Start the container, building if necessary.</p> Source code in <code>src/wrknv/container/manager.py</code> <pre><code>def start(self, force_rebuild: bool = False) -&gt; bool:\n    \"\"\"Start the container, building if necessary.\"\"\"\n    # Build image if needed\n    if (force_rebuild or not self.image_exists()) and not self.build_image(rebuild=force_rebuild):\n        return False\n\n    # Prepare run options\n    run_options = {\n        \"image\": self.full_image,\n        \"volumes\": self.storage.get_volume_mappings(),\n        \"environment\": self.container_config.environment or {},\n        \"ports\": self.container_config.ports or [],\n        \"workdir\": \"/workspace\",\n    }\n\n    return self.lifecycle.start(create_if_missing=True, **run_options)\n</code></pre>"},{"location":"api/container/#wrknv.container.ContainerManager.stop","title":"stop","text":"<pre><code>stop(timeout: int = 10) -&gt; bool\n</code></pre> <p>Stop the container.</p> Source code in <code>src/wrknv/container/manager.py</code> <pre><code>def stop(self, timeout: int = 10) -&gt; bool:\n    \"\"\"Stop the container.\"\"\"\n    return self.lifecycle.stop(timeout=timeout)\n</code></pre>"},{"location":"api/container/#wrknv.container.ContainerManager.restart","title":"restart","text":"<pre><code>restart(timeout: int = 10) -&gt; bool\n</code></pre> <p>Restart the container.</p> Source code in <code>src/wrknv/container/manager.py</code> <pre><code>def restart(self, timeout: int = 10) -&gt; bool:\n    \"\"\"Restart the container.\"\"\"\n    return self.lifecycle.restart(timeout=timeout)\n</code></pre>"},{"location":"api/container/#wrknv.container.ContainerManager.enter","title":"enter","text":"<pre><code>enter(\n    command: str | None = None,\n    user: str | None = None,\n    workdir: str | None = None,\n    environment: dict[str, str] | None = None,\n) -&gt; bool\n</code></pre> <p>Enter the container with an interactive shell or command.</p> Source code in <code>src/wrknv/container/manager.py</code> <pre><code>def enter(\n    self,\n    command: str | None = None,\n    user: str | None = None,\n    workdir: str | None = None,\n    environment: dict[str, str] | None = None,\n) -&gt; bool:\n    \"\"\"Enter the container with an interactive shell or command.\"\"\"\n    return self.exec.enter(command=command, user=user, workdir=workdir, environment=environment)\n</code></pre>"},{"location":"api/container/#wrknv.container.ContainerManager.status","title":"status","text":"<pre><code>status() -&gt; dict[str, Any]\n</code></pre> <p>Get container status information.</p> Source code in <code>src/wrknv/container/manager.py</code> <pre><code>def status(self) -&gt; dict[str, Any]:\n    \"\"\"Get container status information.\"\"\"\n    lifecycle_status = self.lifecycle.status()\n\n    try:\n        docker_available = self.runtime.is_available()\n    except RuntimeError:\n        docker_available = False\n\n    return {\n        \"docker_available\": docker_available,\n        \"container_exists\": lifecycle_status.get(\"exists\", False),\n        \"container_running\": lifecycle_status.get(\"running\", False),\n        \"container_info\": {\n            \"name\": lifecycle_status.get(\"name\"),\n            \"state\": lifecycle_status.get(\"status\"),\n            \"id\": lifecycle_status.get(\"id\"),\n            \"image\": lifecycle_status.get(\"image\"),\n        },\n    }\n</code></pre>"},{"location":"api/container/#wrknv.container.ContainerManager.backup_volumes","title":"backup_volumes","text":"<pre><code>backup_volumes(\n    backup_path: Path | None = None,\n    volumes: list[str] | None = None,\n    compress: bool = True,\n) -&gt; Path | None\n</code></pre> <p>Backup container volumes.</p> Source code in <code>src/wrknv/container/manager.py</code> <pre><code>def backup_volumes(\n    self,\n    backup_path: Path | None = None,\n    volumes: list[str] | None = None,\n    compress: bool = True,\n) -&gt; Path | None:\n    \"\"\"Backup container volumes.\"\"\"\n    import tarfile\n\n    from provide.foundation.time import provide_now\n\n    try:\n        if backup_path is None:\n            timestamp = provide_now().strftime(\"%Y%m%d_%H%M%S\")\n            backup_path = self.storage.get_backup_path(f\"volumes_backup_{timestamp}.tar.gz\")\n\n        backup_path.parent.mkdir(parents=True, exist_ok=True)\n        volumes_dir = self.storage.get_container_path(\"volumes\")\n\n        if not volumes_dir.exists():\n            logger.warning(\"No volumes directory to backup\", path=str(volumes_dir))\n            return None\n\n        with tarfile.open(backup_path, \"w:gz\" if compress else \"w\") as tar:\n            tar.add(volumes_dir, arcname=\"volumes\")\n\n        logger.info(\"\ud83d\udce6 Backed up volumes\", backup=str(backup_path))\n        return backup_path\n\n    except Exception as e:\n        logger.error(\"Failed to backup volumes\", error=str(e))\n        return None\n</code></pre>"},{"location":"api/container/#wrknv.container.ContainerManager.restore_volumes","title":"restore_volumes","text":"<pre><code>restore_volumes(\n    backup_path: Path, force: bool = False\n) -&gt; bool\n</code></pre> <p>Restore container volumes from backup.</p> Source code in <code>src/wrknv/container/manager.py</code> <pre><code>def restore_volumes(self, backup_path: Path, force: bool = False) -&gt; bool:\n    \"\"\"Restore container volumes from backup.\"\"\"\n    import shutil\n    import tarfile\n\n    try:\n        if not backup_path.exists():\n            logger.error(\"Backup file not found\", path=str(backup_path))\n            return False\n\n        container_dir = self.storage.get_container_path()\n        volumes_dir = container_dir / \"volumes\"\n\n        if volumes_dir.exists() and not force:\n            logger.warning(\"Volumes already exist, use force=True to overwrite\")\n            return False\n\n        if volumes_dir.exists() and force:\n            shutil.rmtree(volumes_dir)\n\n        with tarfile.open(backup_path, \"r:*\") as tar:\n            tar.extractall(container_dir)\n\n        logger.info(\"\ud83d\udce5 Restored volumes from backup\", backup=str(backup_path))\n        return True\n\n    except Exception as e:\n        logger.error(\"Failed to restore volumes\", error=str(e), backup=str(backup_path))\n        return False\n</code></pre>"},{"location":"api/container/#wrknv.container.ContainerManager.clean_volumes","title":"clean_volumes","text":"<pre><code>clean_volumes(preserve: list[str] | None = None) -&gt; bool\n</code></pre> <p>Clean up container volumes.</p> Source code in <code>src/wrknv/container/manager.py</code> <pre><code>def clean_volumes(self, preserve: list[str] | None = None) -&gt; bool:\n    \"\"\"Clean up container volumes.\"\"\"\n    import shutil\n\n    try:\n        volumes_dir = self.storage.get_container_path(\"volumes\")\n\n        if not volumes_dir.exists():\n            return True\n\n        for volume_path in volumes_dir.iterdir():\n            if volume_path.is_dir():\n                if preserve and volume_path.name in preserve:\n                    logger.debug(\"Preserving volume\", volume=volume_path.name)\n                    continue\n\n                shutil.rmtree(volume_path)\n                volume_path.mkdir(exist_ok=True)\n                logger.info(\"\ud83d\uddd1\ufe0f Cleaned volume\", volume=volume_path.name)\n\n        return True\n\n    except Exception as e:\n        logger.error(\"Failed to clean volumes\", error=str(e))\n        return False\n</code></pre>"},{"location":"api/container/#wrknv.container.ContainerManager.get_logs","title":"get_logs","text":"<pre><code>get_logs(\n    lines: int | None = None,\n    follow: bool = False,\n    since: str | None = None,\n) -&gt; str | None\n</code></pre> <p>Get container logs.</p> Source code in <code>src/wrknv/container/manager.py</code> <pre><code>def get_logs(\n    self,\n    lines: int | None = None,\n    follow: bool = False,\n    since: str | None = None,\n) -&gt; str | None:\n    \"\"\"Get container logs.\"\"\"\n    return self.logs.get_logs(tail=lines, follow=follow, since=since, timestamps=False)\n</code></pre>"},{"location":"api/container/#wrknv.container.ContainerManager.clean","title":"clean","text":"<pre><code>clean(preserve_volumes: bool = False) -&gt; bool\n</code></pre> <p>Clean up container and optionally volumes.</p> Source code in <code>src/wrknv/container/manager.py</code> <pre><code>def clean(self, preserve_volumes: bool = False) -&gt; bool:\n    \"\"\"Clean up container and optionally volumes.\"\"\"\n    success = True\n\n    if self.container_exists():\n        if self.container_running():\n            success = self.stop() and success\n        success = self.lifecycle.remove(force=False) and success\n\n    if not preserve_volumes:\n        success = self.clean_volumes() and success\n\n    success = self.storage.clean_storage(preserve_backups=True) and success\n\n    if success:\n        logger.info(\"\ud83e\uddf9 Container cleanup completed\", container=self.container_name)\n    else:\n        logger.warning(\"\u26a0\ufe0f Container cleanup had some issues\", container=self.container_name)\n\n    return success\n</code></pre>"},{"location":"api/container/#wrknv.container.ContainerManager.get_container_path","title":"get_container_path","text":"<pre><code>get_container_path(subpath: str = '') -&gt; Path\n</code></pre> <p>Get path to container-specific directory or file.</p> Source code in <code>src/wrknv/container/manager.py</code> <pre><code>def get_container_path(self, subpath: str = \"\") -&gt; Path:\n    \"\"\"Get path to container-specific directory or file.\"\"\"\n    return self.storage.get_container_path(subpath)\n</code></pre>"},{"location":"api/container/#wrknv.container.ContainerManager.get_volume_mappings","title":"get_volume_mappings","text":"<pre><code>get_volume_mappings() -&gt; dict[str, str]\n</code></pre> <p>Get volume mappings for the container.</p> Source code in <code>src/wrknv/container/manager.py</code> <pre><code>def get_volume_mappings(self) -&gt; dict[str, str]:\n    \"\"\"Get volume mappings for the container.\"\"\"\n    return self.storage.get_volume_mappings()\n</code></pre>"},{"location":"api/container/#wrknv.container.ContainerManager.save_metadata","title":"save_metadata","text":"<pre><code>save_metadata() -&gt; None\n</code></pre> <p>Save container metadata.</p> Source code in <code>src/wrknv/container/manager.py</code> <pre><code>def save_metadata(self) -&gt; None:\n    \"\"\"Save container metadata.\"\"\"\n    self.metadata.save()\n</code></pre>"},{"location":"api/container/#wrknv.container.ContainerManager.load_metadata","title":"load_metadata","text":"<pre><code>load_metadata() -&gt; dict[str, Any] | None\n</code></pre> <p>Load container metadata.</p> Source code in <code>src/wrknv/container/manager.py</code> <pre><code>def load_metadata(self) -&gt; dict[str, Any] | None:\n    \"\"\"Load container metadata.\"\"\"\n    return self.metadata.load()\n</code></pre>"},{"location":"api/container/#wrknv.container.ContainerManager.update_metadata","title":"update_metadata","text":"<pre><code>update_metadata(updates: dict[str, Any]) -&gt; None\n</code></pre> <p>Update container metadata.</p> Source code in <code>src/wrknv/container/manager.py</code> <pre><code>def update_metadata(self, updates: dict[str, Any]) -&gt; None:\n    \"\"\"Update container metadata.\"\"\"\n    self.metadata.update(updates)\n</code></pre>"},{"location":"api/container/#wrknv.container-functions","title":"Functions","text":""},{"location":"api/container/#wrknv.container.build_container","title":"build_container","text":"<pre><code>build_container(\n    config: WorkenvConfig | None = None,\n    rebuild: bool = False,\n) -&gt; bool\n</code></pre> <p>Build the development container image.</p> Source code in <code>src/wrknv/container/commands.py</code> <pre><code>def build_container(config: WorkenvConfig | None = None, rebuild: bool = False) -&gt; bool:\n    \"\"\"Build the development container image.\"\"\"\n    manager = ContainerManager(config)\n    return manager.build_image(rebuild=rebuild)\n</code></pre>"},{"location":"api/container/#wrknv.container.clean_container","title":"clean_container","text":"<pre><code>clean_container(\n    config: WorkenvConfig | None = None,\n) -&gt; bool\n</code></pre> <p>Clean up container and image.</p> Source code in <code>src/wrknv/container/commands.py</code> <pre><code>def clean_container(config: WorkenvConfig | None = None) -&gt; bool:\n    \"\"\"Clean up container and image.\"\"\"\n    manager = ContainerManager(config)\n    return manager.clean()\n</code></pre>"},{"location":"api/container/#wrknv.container.container_logs","title":"container_logs","text":"<pre><code>container_logs(\n    config: WorkenvConfig | None = None,\n    follow: bool = False,\n    tail: int = 100,\n    since: str | None = None,\n    timestamps: bool = False,\n    details: bool = False,\n) -&gt; None\n</code></pre> <p>Show container logs.</p> Source code in <code>src/wrknv/container/commands.py</code> <pre><code>def container_logs(\n    config: WorkenvConfig | None = None,\n    follow: bool = False,\n    tail: int = 100,\n    since: str | None = None,\n    timestamps: bool = False,\n    details: bool = False,\n) -&gt; None:\n    \"\"\"Show container logs.\"\"\"\n    manager = ContainerManager(config)\n    # Note: details parameter is ignored for now\n    manager.logs.get_logs(\n        follow=follow,\n        tail=tail,\n        since=since,\n        timestamps=timestamps,\n    )\n</code></pre>"},{"location":"api/container/#wrknv.container.container_status","title":"container_status","text":"<pre><code>container_status(\n    config: WorkenvConfig | None = None,\n) -&gt; None\n</code></pre> <p>Display container status information.</p> Source code in <code>src/wrknv/container/commands.py</code> <pre><code>def container_status(config: WorkenvConfig | None = None) -&gt; None:\n    \"\"\"Display container status information.\"\"\"\n    manager = ContainerManager(config)\n    console = Console()\n\n    status = manager.status()\n\n    # Create status table\n    table = Table(title=\"\ud83d\udcca Container Status\", show_header=True)\n    table.add_column(\"Property\", style=\"cyan\")\n    table.add_column(\"Status\", style=\"green\")\n\n    # Docker status\n    docker_status = \"\u2705 Available\" if status[\"docker_available\"] else \"\u274c Not Available\"\n    table.add_row(\"Docker\", docker_status)\n\n    # Image status\n    image_status = \"\u2705 Exists\" if status[\"image_exists\"] else \"\u274c Not Found\"\n    table.add_row(f\"Image ({manager.full_image})\", image_status)\n\n    # Container status\n    if status[\"container_exists\"]:\n        container_status = \"\\U0001f7e2 Running\" if status[\"container_running\"] else \"\\U0001f7e1 Stopped\"\n    else:\n        container_status = \"\u274c Not Created\"\n    table.add_row(f\"Container ({manager.container_name})\", container_status)\n\n    # Additional info if container exists\n    if status[\"container_info\"]:\n        info = status[\"container_info\"]\n        table.add_row(\"Container ID\", info.get(\"id\", \"N/A\"))\n        table.add_row(\"State\", info.get(\"state\", \"N/A\"))\n\n    console.print(table)\n\n    # Show helpful commands\n    if not status[\"docker_available\"]:\n        console.print(\"\\n[yellow]Please install Docker to use container features[/yellow]\")\n    elif not status[\"container_running\"]:\n        console.print(\"\\n[dim]Run 'wrknv container start' to start the container[/dim]\")\n    else:\n        console.print(\"\\n[dim]Run 'wrknv container enter' to access the container[/dim]\")\n</code></pre>"},{"location":"api/container/#wrknv.container.enter_container","title":"enter_container","text":"<pre><code>enter_container(\n    config: WorkenvConfig | None = None,\n    command: list[str] | None = None,\n    shell: str | None = None,\n    working_dir: str | None = None,\n    environment: dict[str, str] | None = None,\n    user: str | None = None,\n    auto_start: bool = False,\n) -&gt; None\n</code></pre> <p>Enter the running container.</p> Source code in <code>src/wrknv/container/commands.py</code> <pre><code>def enter_container(\n    config: WorkenvConfig | None = None,\n    command: list[str] | None = None,\n    shell: str | None = None,\n    working_dir: str | None = None,\n    environment: dict[str, str] | None = None,\n    user: str | None = None,\n    auto_start: bool = False,\n) -&gt; None:\n    \"\"\"Enter the running container.\"\"\"\n    manager = ContainerManager(config)\n    manager.enter(\n        command=command,\n        shell=shell,\n        working_dir=working_dir,\n        environment=environment,\n        user=user,\n        auto_start=auto_start,\n    )\n</code></pre>"},{"location":"api/container/#wrknv.container.rebuild_container","title":"rebuild_container","text":"<pre><code>rebuild_container(\n    config: WorkenvConfig | None = None,\n) -&gt; bool\n</code></pre> <p>Rebuild the container from scratch.</p> Source code in <code>src/wrknv/container/commands.py</code> <pre><code>def rebuild_container(config: WorkenvConfig | None = None) -&gt; bool:\n    \"\"\"Rebuild the container from scratch.\"\"\"\n    manager = ContainerManager(config)\n    console = Console()\n\n    console.print(\"\ud83d\udd28 Rebuilding container from scratch...\")\n\n    # Clean existing resources\n    if not manager.clean():\n        return False\n\n    # Build fresh image\n    if not manager.build_image(rebuild=True):\n        return False\n\n    # Start new container\n    return manager.start()\n</code></pre>"},{"location":"api/container/#wrknv.container.restart_container","title":"restart_container","text":"<pre><code>restart_container(\n    config: WorkenvConfig | None = None,\n) -&gt; bool\n</code></pre> <p>Restart the development container.</p> Source code in <code>src/wrknv/container/commands.py</code> <pre><code>def restart_container(config: WorkenvConfig | None = None) -&gt; bool:\n    \"\"\"Restart the development container.\"\"\"\n    manager = ContainerManager(config)\n    return manager.restart()\n</code></pre>"},{"location":"api/container/#wrknv.container.start_container","title":"start_container","text":"<pre><code>start_container(\n    config: WorkenvConfig | None = None,\n    rebuild: bool = False,\n) -&gt; bool\n</code></pre> <p>Start the development container.</p> Source code in <code>src/wrknv/container/commands.py</code> <pre><code>def start_container(config: WorkenvConfig | None = None, rebuild: bool = False) -&gt; bool:\n    \"\"\"Start the development container.\"\"\"\n    manager = ContainerManager(config)\n    return manager.start(force_rebuild=rebuild)\n</code></pre>"},{"location":"api/container/#wrknv.container.stop_container","title":"stop_container","text":"<pre><code>stop_container(config: WorkenvConfig | None = None) -&gt; bool\n</code></pre> <p>Stop the development container.</p> Source code in <code>src/wrknv/container/commands.py</code> <pre><code>def stop_container(config: WorkenvConfig | None = None) -&gt; bool:\n    \"\"\"Stop the development container.\"\"\"\n    manager = ContainerManager(config)\n    return manager.stop()\n</code></pre>"},{"location":"api/wenv/","title":"Wenv API","text":"<p>Auto-generated API documentation for <code>wrknv.wenv</code>.</p>"},{"location":"api/wenv/#wrknv.wenv--wrknvenv","title":"wrknv.env","text":"<p>Core environment management functionality for development tool management.</p> <p>Provides: - Cross-platform tool installation (Terraform, OpenTofu, Go, UV, etc.) - Version management and switching - Development environment profiles - Flexible configuration system</p> <p>This module can be used standalone or integrated with other tools.</p>"},{"location":"api/wenv/#wrknv.wenv-attributes","title":"Attributes","text":""},{"location":"api/wenv/#wrknv.wenv.__version__","title":"__version__  <code>module-attribute</code>","text":"<pre><code>__version__ = '0.1.0'\n</code></pre>"},{"location":"api/wenv/#wrknv.wenv.__all__","title":"__all__  <code>module-attribute</code>","text":"<pre><code>__all__ = ['config', 'managers', 'operations']\n</code></pre>"}]}