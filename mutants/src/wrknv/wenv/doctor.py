#
# SPDX-FileCopyrightText: Copyright (c) provide.io llc. All rights reserved.
# SPDX-License-Identifier: Apache-2.0
#

"""Doctor command for diagnosing wrknv environment issues.

This module provides diagnostic tools to help users identify and fix
common problems with their wrknv setup."""

from __future__ import annotations

import os
from pathlib import Path
import platform
import shutil
import sys

from provide.foundation.process import run
from rich import box
from rich.console import Console
from rich.panel import Panel
from rich.table import Table
from inspect import signature as _mutmut_signature
from typing import Annotated
from typing import Callable
from typing import ClassVar


MutantDict = Annotated[dict[str, Callable], "Mutant"]


def _mutmut_trampoline(orig, mutants, call_args, call_kwargs, self_arg = None):
    """Forward call to original or mutated function, depending on the environment"""
    import os
    mutant_under_test = os.environ['MUTANT_UNDER_TEST']
    if mutant_under_test == 'fail':
        from mutmut.__main__ import MutmutProgrammaticFailException
        raise MutmutProgrammaticFailException('Failed programmatically')      
    elif mutant_under_test == 'stats':
        from mutmut.__main__ import record_trampoline_hit
        record_trampoline_hit(orig.__module__ + '.' + orig.__name__)
        result = orig(*call_args, **call_kwargs)
        return result
    prefix = orig.__module__ + '.' + orig.__name__ + '__mutmut_'
    if not mutant_under_test.startswith(prefix):
        result = orig(*call_args, **call_kwargs)
        return result
    mutant_name = mutant_under_test.rpartition('.')[-1]
    if self_arg is not None:
        # call to a class method where self is not bound
        result = mutants[mutant_name](self_arg, *call_args, **call_kwargs)
    else:
        result = mutants[mutant_name](*call_args, **call_kwargs)
    return result


class WrknvDoctor:
    """Diagnose and report on wrknv environment health."""

    def xǁWrknvDoctorǁ__init____mutmut_orig(self) -> None:
        self.console = Console()
        self.checks_passed = []
        self.checks_failed = []
        self.checks_warning = []

    def xǁWrknvDoctorǁ__init____mutmut_1(self) -> None:
        self.console = None
        self.checks_passed = []
        self.checks_failed = []
        self.checks_warning = []

    def xǁWrknvDoctorǁ__init____mutmut_2(self) -> None:
        self.console = Console()
        self.checks_passed = None
        self.checks_failed = []
        self.checks_warning = []

    def xǁWrknvDoctorǁ__init____mutmut_3(self) -> None:
        self.console = Console()
        self.checks_passed = []
        self.checks_failed = None
        self.checks_warning = []

    def xǁWrknvDoctorǁ__init____mutmut_4(self) -> None:
        self.console = Console()
        self.checks_passed = []
        self.checks_failed = []
        self.checks_warning = None
    
    xǁWrknvDoctorǁ__init____mutmut_mutants : ClassVar[MutantDict] = {
    'xǁWrknvDoctorǁ__init____mutmut_1': xǁWrknvDoctorǁ__init____mutmut_1, 
        'xǁWrknvDoctorǁ__init____mutmut_2': xǁWrknvDoctorǁ__init____mutmut_2, 
        'xǁWrknvDoctorǁ__init____mutmut_3': xǁWrknvDoctorǁ__init____mutmut_3, 
        'xǁWrknvDoctorǁ__init____mutmut_4': xǁWrknvDoctorǁ__init____mutmut_4
    }
    
    def __init__(self, *args, **kwargs):
        result = _mutmut_trampoline(object.__getattribute__(self, "xǁWrknvDoctorǁ__init____mutmut_orig"), object.__getattribute__(self, "xǁWrknvDoctorǁ__init____mutmut_mutants"), args, kwargs, self)
        return result 
    
    __init__.__signature__ = _mutmut_signature(xǁWrknvDoctorǁ__init____mutmut_orig)
    xǁWrknvDoctorǁ__init____mutmut_orig.__name__ = 'xǁWrknvDoctorǁ__init__'

    def xǁWrknvDoctorǁrun__mutmut_orig(self, verbose: bool = False) -> int:
        """
        Run all diagnostic checks.

        Args:
            verbose: If True, show detailed output.

        Returns:
            Exit code (0 if all checks pass, 1 otherwise).
        """
        self.console.print("\n[bold cyan]🩺 Running wrknv doctor...[/bold cyan]\n")

        # Run all checks
        self._check_system_info()
        self._check_wrknv_installation()
        self._check_dependencies()
        self._check_workenv_structure()
        self._check_env_script()
        self._check_config_files()
        self._check_sibling_packages()
        self._check_common_issues()

        # Print summary
        self._print_summary(verbose)

        # Return appropriate exit code
        if self.checks_failed:
            return 1
        return 0

    def xǁWrknvDoctorǁrun__mutmut_1(self, verbose: bool = True) -> int:
        """
        Run all diagnostic checks.

        Args:
            verbose: If True, show detailed output.

        Returns:
            Exit code (0 if all checks pass, 1 otherwise).
        """
        self.console.print("\n[bold cyan]🩺 Running wrknv doctor...[/bold cyan]\n")

        # Run all checks
        self._check_system_info()
        self._check_wrknv_installation()
        self._check_dependencies()
        self._check_workenv_structure()
        self._check_env_script()
        self._check_config_files()
        self._check_sibling_packages()
        self._check_common_issues()

        # Print summary
        self._print_summary(verbose)

        # Return appropriate exit code
        if self.checks_failed:
            return 1
        return 0

    def xǁWrknvDoctorǁrun__mutmut_2(self, verbose: bool = False) -> int:
        """
        Run all diagnostic checks.

        Args:
            verbose: If True, show detailed output.

        Returns:
            Exit code (0 if all checks pass, 1 otherwise).
        """
        self.console.print(None)

        # Run all checks
        self._check_system_info()
        self._check_wrknv_installation()
        self._check_dependencies()
        self._check_workenv_structure()
        self._check_env_script()
        self._check_config_files()
        self._check_sibling_packages()
        self._check_common_issues()

        # Print summary
        self._print_summary(verbose)

        # Return appropriate exit code
        if self.checks_failed:
            return 1
        return 0

    def xǁWrknvDoctorǁrun__mutmut_3(self, verbose: bool = False) -> int:
        """
        Run all diagnostic checks.

        Args:
            verbose: If True, show detailed output.

        Returns:
            Exit code (0 if all checks pass, 1 otherwise).
        """
        self.console.print("XX\n[bold cyan]🩺 Running wrknv doctor...[/bold cyan]\nXX")

        # Run all checks
        self._check_system_info()
        self._check_wrknv_installation()
        self._check_dependencies()
        self._check_workenv_structure()
        self._check_env_script()
        self._check_config_files()
        self._check_sibling_packages()
        self._check_common_issues()

        # Print summary
        self._print_summary(verbose)

        # Return appropriate exit code
        if self.checks_failed:
            return 1
        return 0

    def xǁWrknvDoctorǁrun__mutmut_4(self, verbose: bool = False) -> int:
        """
        Run all diagnostic checks.

        Args:
            verbose: If True, show detailed output.

        Returns:
            Exit code (0 if all checks pass, 1 otherwise).
        """
        self.console.print("\n[bold cyan]🩺 running wrknv doctor...[/bold cyan]\n")

        # Run all checks
        self._check_system_info()
        self._check_wrknv_installation()
        self._check_dependencies()
        self._check_workenv_structure()
        self._check_env_script()
        self._check_config_files()
        self._check_sibling_packages()
        self._check_common_issues()

        # Print summary
        self._print_summary(verbose)

        # Return appropriate exit code
        if self.checks_failed:
            return 1
        return 0

    def xǁWrknvDoctorǁrun__mutmut_5(self, verbose: bool = False) -> int:
        """
        Run all diagnostic checks.

        Args:
            verbose: If True, show detailed output.

        Returns:
            Exit code (0 if all checks pass, 1 otherwise).
        """
        self.console.print("\n[BOLD CYAN]🩺 RUNNING WRKNV DOCTOR...[/BOLD CYAN]\n")

        # Run all checks
        self._check_system_info()
        self._check_wrknv_installation()
        self._check_dependencies()
        self._check_workenv_structure()
        self._check_env_script()
        self._check_config_files()
        self._check_sibling_packages()
        self._check_common_issues()

        # Print summary
        self._print_summary(verbose)

        # Return appropriate exit code
        if self.checks_failed:
            return 1
        return 0

    def xǁWrknvDoctorǁrun__mutmut_6(self, verbose: bool = False) -> int:
        """
        Run all diagnostic checks.

        Args:
            verbose: If True, show detailed output.

        Returns:
            Exit code (0 if all checks pass, 1 otherwise).
        """
        self.console.print("\n[bold cyan]🩺 Running wrknv doctor...[/bold cyan]\n")

        # Run all checks
        self._check_system_info()
        self._check_wrknv_installation()
        self._check_dependencies()
        self._check_workenv_structure()
        self._check_env_script()
        self._check_config_files()
        self._check_sibling_packages()
        self._check_common_issues()

        # Print summary
        self._print_summary(None)

        # Return appropriate exit code
        if self.checks_failed:
            return 1
        return 0

    def xǁWrknvDoctorǁrun__mutmut_7(self, verbose: bool = False) -> int:
        """
        Run all diagnostic checks.

        Args:
            verbose: If True, show detailed output.

        Returns:
            Exit code (0 if all checks pass, 1 otherwise).
        """
        self.console.print("\n[bold cyan]🩺 Running wrknv doctor...[/bold cyan]\n")

        # Run all checks
        self._check_system_info()
        self._check_wrknv_installation()
        self._check_dependencies()
        self._check_workenv_structure()
        self._check_env_script()
        self._check_config_files()
        self._check_sibling_packages()
        self._check_common_issues()

        # Print summary
        self._print_summary(verbose)

        # Return appropriate exit code
        if self.checks_failed:
            return 2
        return 0

    def xǁWrknvDoctorǁrun__mutmut_8(self, verbose: bool = False) -> int:
        """
        Run all diagnostic checks.

        Args:
            verbose: If True, show detailed output.

        Returns:
            Exit code (0 if all checks pass, 1 otherwise).
        """
        self.console.print("\n[bold cyan]🩺 Running wrknv doctor...[/bold cyan]\n")

        # Run all checks
        self._check_system_info()
        self._check_wrknv_installation()
        self._check_dependencies()
        self._check_workenv_structure()
        self._check_env_script()
        self._check_config_files()
        self._check_sibling_packages()
        self._check_common_issues()

        # Print summary
        self._print_summary(verbose)

        # Return appropriate exit code
        if self.checks_failed:
            return 1
        return 1
    
    xǁWrknvDoctorǁrun__mutmut_mutants : ClassVar[MutantDict] = {
    'xǁWrknvDoctorǁrun__mutmut_1': xǁWrknvDoctorǁrun__mutmut_1, 
        'xǁWrknvDoctorǁrun__mutmut_2': xǁWrknvDoctorǁrun__mutmut_2, 
        'xǁWrknvDoctorǁrun__mutmut_3': xǁWrknvDoctorǁrun__mutmut_3, 
        'xǁWrknvDoctorǁrun__mutmut_4': xǁWrknvDoctorǁrun__mutmut_4, 
        'xǁWrknvDoctorǁrun__mutmut_5': xǁWrknvDoctorǁrun__mutmut_5, 
        'xǁWrknvDoctorǁrun__mutmut_6': xǁWrknvDoctorǁrun__mutmut_6, 
        'xǁWrknvDoctorǁrun__mutmut_7': xǁWrknvDoctorǁrun__mutmut_7, 
        'xǁWrknvDoctorǁrun__mutmut_8': xǁWrknvDoctorǁrun__mutmut_8
    }
    
    def run(self, *args, **kwargs):
        result = _mutmut_trampoline(object.__getattribute__(self, "xǁWrknvDoctorǁrun__mutmut_orig"), object.__getattribute__(self, "xǁWrknvDoctorǁrun__mutmut_mutants"), args, kwargs, self)
        return result 
    
    run.__signature__ = _mutmut_signature(xǁWrknvDoctorǁrun__mutmut_orig)
    xǁWrknvDoctorǁrun__mutmut_orig.__name__ = 'xǁWrknvDoctorǁrun'

    def xǁWrknvDoctorǁ_check_system_info__mutmut_orig(self) -> None:
        """Check and display system information."""
        try:
            info = {
                "Platform": platform.platform(),
                "Python": f"{sys.version_info.major}.{sys.version_info.minor}.{sys.version_info.micro}",
                "Architecture": platform.machine(),
                "Working Directory": str(Path.cwd()),
            }

            self.console.print("[bold]System Information:[/bold]")
            for key, value in info.items():
                self.console.print(f"  {key}: {value}")
            self.console.print()

            self.checks_passed.append(("System Info", "Collected successfully"))
        except Exception as e:
            self.checks_failed.append(("System Info", str(e)))

    def xǁWrknvDoctorǁ_check_system_info__mutmut_1(self) -> None:
        """Check and display system information."""
        try:
            info = None

            self.console.print("[bold]System Information:[/bold]")
            for key, value in info.items():
                self.console.print(f"  {key}: {value}")
            self.console.print()

            self.checks_passed.append(("System Info", "Collected successfully"))
        except Exception as e:
            self.checks_failed.append(("System Info", str(e)))

    def xǁWrknvDoctorǁ_check_system_info__mutmut_2(self) -> None:
        """Check and display system information."""
        try:
            info = {
                "XXPlatformXX": platform.platform(),
                "Python": f"{sys.version_info.major}.{sys.version_info.minor}.{sys.version_info.micro}",
                "Architecture": platform.machine(),
                "Working Directory": str(Path.cwd()),
            }

            self.console.print("[bold]System Information:[/bold]")
            for key, value in info.items():
                self.console.print(f"  {key}: {value}")
            self.console.print()

            self.checks_passed.append(("System Info", "Collected successfully"))
        except Exception as e:
            self.checks_failed.append(("System Info", str(e)))

    def xǁWrknvDoctorǁ_check_system_info__mutmut_3(self) -> None:
        """Check and display system information."""
        try:
            info = {
                "platform": platform.platform(),
                "Python": f"{sys.version_info.major}.{sys.version_info.minor}.{sys.version_info.micro}",
                "Architecture": platform.machine(),
                "Working Directory": str(Path.cwd()),
            }

            self.console.print("[bold]System Information:[/bold]")
            for key, value in info.items():
                self.console.print(f"  {key}: {value}")
            self.console.print()

            self.checks_passed.append(("System Info", "Collected successfully"))
        except Exception as e:
            self.checks_failed.append(("System Info", str(e)))

    def xǁWrknvDoctorǁ_check_system_info__mutmut_4(self) -> None:
        """Check and display system information."""
        try:
            info = {
                "PLATFORM": platform.platform(),
                "Python": f"{sys.version_info.major}.{sys.version_info.minor}.{sys.version_info.micro}",
                "Architecture": platform.machine(),
                "Working Directory": str(Path.cwd()),
            }

            self.console.print("[bold]System Information:[/bold]")
            for key, value in info.items():
                self.console.print(f"  {key}: {value}")
            self.console.print()

            self.checks_passed.append(("System Info", "Collected successfully"))
        except Exception as e:
            self.checks_failed.append(("System Info", str(e)))

    def xǁWrknvDoctorǁ_check_system_info__mutmut_5(self) -> None:
        """Check and display system information."""
        try:
            info = {
                "Platform": platform.platform(),
                "XXPythonXX": f"{sys.version_info.major}.{sys.version_info.minor}.{sys.version_info.micro}",
                "Architecture": platform.machine(),
                "Working Directory": str(Path.cwd()),
            }

            self.console.print("[bold]System Information:[/bold]")
            for key, value in info.items():
                self.console.print(f"  {key}: {value}")
            self.console.print()

            self.checks_passed.append(("System Info", "Collected successfully"))
        except Exception as e:
            self.checks_failed.append(("System Info", str(e)))

    def xǁWrknvDoctorǁ_check_system_info__mutmut_6(self) -> None:
        """Check and display system information."""
        try:
            info = {
                "Platform": platform.platform(),
                "python": f"{sys.version_info.major}.{sys.version_info.minor}.{sys.version_info.micro}",
                "Architecture": platform.machine(),
                "Working Directory": str(Path.cwd()),
            }

            self.console.print("[bold]System Information:[/bold]")
            for key, value in info.items():
                self.console.print(f"  {key}: {value}")
            self.console.print()

            self.checks_passed.append(("System Info", "Collected successfully"))
        except Exception as e:
            self.checks_failed.append(("System Info", str(e)))

    def xǁWrknvDoctorǁ_check_system_info__mutmut_7(self) -> None:
        """Check and display system information."""
        try:
            info = {
                "Platform": platform.platform(),
                "PYTHON": f"{sys.version_info.major}.{sys.version_info.minor}.{sys.version_info.micro}",
                "Architecture": platform.machine(),
                "Working Directory": str(Path.cwd()),
            }

            self.console.print("[bold]System Information:[/bold]")
            for key, value in info.items():
                self.console.print(f"  {key}: {value}")
            self.console.print()

            self.checks_passed.append(("System Info", "Collected successfully"))
        except Exception as e:
            self.checks_failed.append(("System Info", str(e)))

    def xǁWrknvDoctorǁ_check_system_info__mutmut_8(self) -> None:
        """Check and display system information."""
        try:
            info = {
                "Platform": platform.platform(),
                "Python": f"{sys.version_info.major}.{sys.version_info.minor}.{sys.version_info.micro}",
                "XXArchitectureXX": platform.machine(),
                "Working Directory": str(Path.cwd()),
            }

            self.console.print("[bold]System Information:[/bold]")
            for key, value in info.items():
                self.console.print(f"  {key}: {value}")
            self.console.print()

            self.checks_passed.append(("System Info", "Collected successfully"))
        except Exception as e:
            self.checks_failed.append(("System Info", str(e)))

    def xǁWrknvDoctorǁ_check_system_info__mutmut_9(self) -> None:
        """Check and display system information."""
        try:
            info = {
                "Platform": platform.platform(),
                "Python": f"{sys.version_info.major}.{sys.version_info.minor}.{sys.version_info.micro}",
                "architecture": platform.machine(),
                "Working Directory": str(Path.cwd()),
            }

            self.console.print("[bold]System Information:[/bold]")
            for key, value in info.items():
                self.console.print(f"  {key}: {value}")
            self.console.print()

            self.checks_passed.append(("System Info", "Collected successfully"))
        except Exception as e:
            self.checks_failed.append(("System Info", str(e)))

    def xǁWrknvDoctorǁ_check_system_info__mutmut_10(self) -> None:
        """Check and display system information."""
        try:
            info = {
                "Platform": platform.platform(),
                "Python": f"{sys.version_info.major}.{sys.version_info.minor}.{sys.version_info.micro}",
                "ARCHITECTURE": platform.machine(),
                "Working Directory": str(Path.cwd()),
            }

            self.console.print("[bold]System Information:[/bold]")
            for key, value in info.items():
                self.console.print(f"  {key}: {value}")
            self.console.print()

            self.checks_passed.append(("System Info", "Collected successfully"))
        except Exception as e:
            self.checks_failed.append(("System Info", str(e)))

    def xǁWrknvDoctorǁ_check_system_info__mutmut_11(self) -> None:
        """Check and display system information."""
        try:
            info = {
                "Platform": platform.platform(),
                "Python": f"{sys.version_info.major}.{sys.version_info.minor}.{sys.version_info.micro}",
                "Architecture": platform.machine(),
                "XXWorking DirectoryXX": str(Path.cwd()),
            }

            self.console.print("[bold]System Information:[/bold]")
            for key, value in info.items():
                self.console.print(f"  {key}: {value}")
            self.console.print()

            self.checks_passed.append(("System Info", "Collected successfully"))
        except Exception as e:
            self.checks_failed.append(("System Info", str(e)))

    def xǁWrknvDoctorǁ_check_system_info__mutmut_12(self) -> None:
        """Check and display system information."""
        try:
            info = {
                "Platform": platform.platform(),
                "Python": f"{sys.version_info.major}.{sys.version_info.minor}.{sys.version_info.micro}",
                "Architecture": platform.machine(),
                "working directory": str(Path.cwd()),
            }

            self.console.print("[bold]System Information:[/bold]")
            for key, value in info.items():
                self.console.print(f"  {key}: {value}")
            self.console.print()

            self.checks_passed.append(("System Info", "Collected successfully"))
        except Exception as e:
            self.checks_failed.append(("System Info", str(e)))

    def xǁWrknvDoctorǁ_check_system_info__mutmut_13(self) -> None:
        """Check and display system information."""
        try:
            info = {
                "Platform": platform.platform(),
                "Python": f"{sys.version_info.major}.{sys.version_info.minor}.{sys.version_info.micro}",
                "Architecture": platform.machine(),
                "WORKING DIRECTORY": str(Path.cwd()),
            }

            self.console.print("[bold]System Information:[/bold]")
            for key, value in info.items():
                self.console.print(f"  {key}: {value}")
            self.console.print()

            self.checks_passed.append(("System Info", "Collected successfully"))
        except Exception as e:
            self.checks_failed.append(("System Info", str(e)))

    def xǁWrknvDoctorǁ_check_system_info__mutmut_14(self) -> None:
        """Check and display system information."""
        try:
            info = {
                "Platform": platform.platform(),
                "Python": f"{sys.version_info.major}.{sys.version_info.minor}.{sys.version_info.micro}",
                "Architecture": platform.machine(),
                "Working Directory": str(None),
            }

            self.console.print("[bold]System Information:[/bold]")
            for key, value in info.items():
                self.console.print(f"  {key}: {value}")
            self.console.print()

            self.checks_passed.append(("System Info", "Collected successfully"))
        except Exception as e:
            self.checks_failed.append(("System Info", str(e)))

    def xǁWrknvDoctorǁ_check_system_info__mutmut_15(self) -> None:
        """Check and display system information."""
        try:
            info = {
                "Platform": platform.platform(),
                "Python": f"{sys.version_info.major}.{sys.version_info.minor}.{sys.version_info.micro}",
                "Architecture": platform.machine(),
                "Working Directory": str(Path.cwd()),
            }

            self.console.print(None)
            for key, value in info.items():
                self.console.print(f"  {key}: {value}")
            self.console.print()

            self.checks_passed.append(("System Info", "Collected successfully"))
        except Exception as e:
            self.checks_failed.append(("System Info", str(e)))

    def xǁWrknvDoctorǁ_check_system_info__mutmut_16(self) -> None:
        """Check and display system information."""
        try:
            info = {
                "Platform": platform.platform(),
                "Python": f"{sys.version_info.major}.{sys.version_info.minor}.{sys.version_info.micro}",
                "Architecture": platform.machine(),
                "Working Directory": str(Path.cwd()),
            }

            self.console.print("XX[bold]System Information:[/bold]XX")
            for key, value in info.items():
                self.console.print(f"  {key}: {value}")
            self.console.print()

            self.checks_passed.append(("System Info", "Collected successfully"))
        except Exception as e:
            self.checks_failed.append(("System Info", str(e)))

    def xǁWrknvDoctorǁ_check_system_info__mutmut_17(self) -> None:
        """Check and display system information."""
        try:
            info = {
                "Platform": platform.platform(),
                "Python": f"{sys.version_info.major}.{sys.version_info.minor}.{sys.version_info.micro}",
                "Architecture": platform.machine(),
                "Working Directory": str(Path.cwd()),
            }

            self.console.print("[bold]system information:[/bold]")
            for key, value in info.items():
                self.console.print(f"  {key}: {value}")
            self.console.print()

            self.checks_passed.append(("System Info", "Collected successfully"))
        except Exception as e:
            self.checks_failed.append(("System Info", str(e)))

    def xǁWrknvDoctorǁ_check_system_info__mutmut_18(self) -> None:
        """Check and display system information."""
        try:
            info = {
                "Platform": platform.platform(),
                "Python": f"{sys.version_info.major}.{sys.version_info.minor}.{sys.version_info.micro}",
                "Architecture": platform.machine(),
                "Working Directory": str(Path.cwd()),
            }

            self.console.print("[BOLD]SYSTEM INFORMATION:[/BOLD]")
            for key, value in info.items():
                self.console.print(f"  {key}: {value}")
            self.console.print()

            self.checks_passed.append(("System Info", "Collected successfully"))
        except Exception as e:
            self.checks_failed.append(("System Info", str(e)))

    def xǁWrknvDoctorǁ_check_system_info__mutmut_19(self) -> None:
        """Check and display system information."""
        try:
            info = {
                "Platform": platform.platform(),
                "Python": f"{sys.version_info.major}.{sys.version_info.minor}.{sys.version_info.micro}",
                "Architecture": platform.machine(),
                "Working Directory": str(Path.cwd()),
            }

            self.console.print("[bold]System Information:[/bold]")
            for key, value in info.items():
                self.console.print(None)
            self.console.print()

            self.checks_passed.append(("System Info", "Collected successfully"))
        except Exception as e:
            self.checks_failed.append(("System Info", str(e)))

    def xǁWrknvDoctorǁ_check_system_info__mutmut_20(self) -> None:
        """Check and display system information."""
        try:
            info = {
                "Platform": platform.platform(),
                "Python": f"{sys.version_info.major}.{sys.version_info.minor}.{sys.version_info.micro}",
                "Architecture": platform.machine(),
                "Working Directory": str(Path.cwd()),
            }

            self.console.print("[bold]System Information:[/bold]")
            for key, value in info.items():
                self.console.print(f"  {key}: {value}")
            self.console.print()

            self.checks_passed.append(None)
        except Exception as e:
            self.checks_failed.append(("System Info", str(e)))

    def xǁWrknvDoctorǁ_check_system_info__mutmut_21(self) -> None:
        """Check and display system information."""
        try:
            info = {
                "Platform": platform.platform(),
                "Python": f"{sys.version_info.major}.{sys.version_info.minor}.{sys.version_info.micro}",
                "Architecture": platform.machine(),
                "Working Directory": str(Path.cwd()),
            }

            self.console.print("[bold]System Information:[/bold]")
            for key, value in info.items():
                self.console.print(f"  {key}: {value}")
            self.console.print()

            self.checks_passed.append(("XXSystem InfoXX", "Collected successfully"))
        except Exception as e:
            self.checks_failed.append(("System Info", str(e)))

    def xǁWrknvDoctorǁ_check_system_info__mutmut_22(self) -> None:
        """Check and display system information."""
        try:
            info = {
                "Platform": platform.platform(),
                "Python": f"{sys.version_info.major}.{sys.version_info.minor}.{sys.version_info.micro}",
                "Architecture": platform.machine(),
                "Working Directory": str(Path.cwd()),
            }

            self.console.print("[bold]System Information:[/bold]")
            for key, value in info.items():
                self.console.print(f"  {key}: {value}")
            self.console.print()

            self.checks_passed.append(("system info", "Collected successfully"))
        except Exception as e:
            self.checks_failed.append(("System Info", str(e)))

    def xǁWrknvDoctorǁ_check_system_info__mutmut_23(self) -> None:
        """Check and display system information."""
        try:
            info = {
                "Platform": platform.platform(),
                "Python": f"{sys.version_info.major}.{sys.version_info.minor}.{sys.version_info.micro}",
                "Architecture": platform.machine(),
                "Working Directory": str(Path.cwd()),
            }

            self.console.print("[bold]System Information:[/bold]")
            for key, value in info.items():
                self.console.print(f"  {key}: {value}")
            self.console.print()

            self.checks_passed.append(("SYSTEM INFO", "Collected successfully"))
        except Exception as e:
            self.checks_failed.append(("System Info", str(e)))

    def xǁWrknvDoctorǁ_check_system_info__mutmut_24(self) -> None:
        """Check and display system information."""
        try:
            info = {
                "Platform": platform.platform(),
                "Python": f"{sys.version_info.major}.{sys.version_info.minor}.{sys.version_info.micro}",
                "Architecture": platform.machine(),
                "Working Directory": str(Path.cwd()),
            }

            self.console.print("[bold]System Information:[/bold]")
            for key, value in info.items():
                self.console.print(f"  {key}: {value}")
            self.console.print()

            self.checks_passed.append(("System Info", "XXCollected successfullyXX"))
        except Exception as e:
            self.checks_failed.append(("System Info", str(e)))

    def xǁWrknvDoctorǁ_check_system_info__mutmut_25(self) -> None:
        """Check and display system information."""
        try:
            info = {
                "Platform": platform.platform(),
                "Python": f"{sys.version_info.major}.{sys.version_info.minor}.{sys.version_info.micro}",
                "Architecture": platform.machine(),
                "Working Directory": str(Path.cwd()),
            }

            self.console.print("[bold]System Information:[/bold]")
            for key, value in info.items():
                self.console.print(f"  {key}: {value}")
            self.console.print()

            self.checks_passed.append(("System Info", "collected successfully"))
        except Exception as e:
            self.checks_failed.append(("System Info", str(e)))

    def xǁWrknvDoctorǁ_check_system_info__mutmut_26(self) -> None:
        """Check and display system information."""
        try:
            info = {
                "Platform": platform.platform(),
                "Python": f"{sys.version_info.major}.{sys.version_info.minor}.{sys.version_info.micro}",
                "Architecture": platform.machine(),
                "Working Directory": str(Path.cwd()),
            }

            self.console.print("[bold]System Information:[/bold]")
            for key, value in info.items():
                self.console.print(f"  {key}: {value}")
            self.console.print()

            self.checks_passed.append(("System Info", "COLLECTED SUCCESSFULLY"))
        except Exception as e:
            self.checks_failed.append(("System Info", str(e)))

    def xǁWrknvDoctorǁ_check_system_info__mutmut_27(self) -> None:
        """Check and display system information."""
        try:
            info = {
                "Platform": platform.platform(),
                "Python": f"{sys.version_info.major}.{sys.version_info.minor}.{sys.version_info.micro}",
                "Architecture": platform.machine(),
                "Working Directory": str(Path.cwd()),
            }

            self.console.print("[bold]System Information:[/bold]")
            for key, value in info.items():
                self.console.print(f"  {key}: {value}")
            self.console.print()

            self.checks_passed.append(("System Info", "Collected successfully"))
        except Exception as e:
            self.checks_failed.append(None)

    def xǁWrknvDoctorǁ_check_system_info__mutmut_28(self) -> None:
        """Check and display system information."""
        try:
            info = {
                "Platform": platform.platform(),
                "Python": f"{sys.version_info.major}.{sys.version_info.minor}.{sys.version_info.micro}",
                "Architecture": platform.machine(),
                "Working Directory": str(Path.cwd()),
            }

            self.console.print("[bold]System Information:[/bold]")
            for key, value in info.items():
                self.console.print(f"  {key}: {value}")
            self.console.print()

            self.checks_passed.append(("System Info", "Collected successfully"))
        except Exception as e:
            self.checks_failed.append(("XXSystem InfoXX", str(e)))

    def xǁWrknvDoctorǁ_check_system_info__mutmut_29(self) -> None:
        """Check and display system information."""
        try:
            info = {
                "Platform": platform.platform(),
                "Python": f"{sys.version_info.major}.{sys.version_info.minor}.{sys.version_info.micro}",
                "Architecture": platform.machine(),
                "Working Directory": str(Path.cwd()),
            }

            self.console.print("[bold]System Information:[/bold]")
            for key, value in info.items():
                self.console.print(f"  {key}: {value}")
            self.console.print()

            self.checks_passed.append(("System Info", "Collected successfully"))
        except Exception as e:
            self.checks_failed.append(("system info", str(e)))

    def xǁWrknvDoctorǁ_check_system_info__mutmut_30(self) -> None:
        """Check and display system information."""
        try:
            info = {
                "Platform": platform.platform(),
                "Python": f"{sys.version_info.major}.{sys.version_info.minor}.{sys.version_info.micro}",
                "Architecture": platform.machine(),
                "Working Directory": str(Path.cwd()),
            }

            self.console.print("[bold]System Information:[/bold]")
            for key, value in info.items():
                self.console.print(f"  {key}: {value}")
            self.console.print()

            self.checks_passed.append(("System Info", "Collected successfully"))
        except Exception as e:
            self.checks_failed.append(("SYSTEM INFO", str(e)))

    def xǁWrknvDoctorǁ_check_system_info__mutmut_31(self) -> None:
        """Check and display system information."""
        try:
            info = {
                "Platform": platform.platform(),
                "Python": f"{sys.version_info.major}.{sys.version_info.minor}.{sys.version_info.micro}",
                "Architecture": platform.machine(),
                "Working Directory": str(Path.cwd()),
            }

            self.console.print("[bold]System Information:[/bold]")
            for key, value in info.items():
                self.console.print(f"  {key}: {value}")
            self.console.print()

            self.checks_passed.append(("System Info", "Collected successfully"))
        except Exception as e:
            self.checks_failed.append(("System Info", str(None)))
    
    xǁWrknvDoctorǁ_check_system_info__mutmut_mutants : ClassVar[MutantDict] = {
    'xǁWrknvDoctorǁ_check_system_info__mutmut_1': xǁWrknvDoctorǁ_check_system_info__mutmut_1, 
        'xǁWrknvDoctorǁ_check_system_info__mutmut_2': xǁWrknvDoctorǁ_check_system_info__mutmut_2, 
        'xǁWrknvDoctorǁ_check_system_info__mutmut_3': xǁWrknvDoctorǁ_check_system_info__mutmut_3, 
        'xǁWrknvDoctorǁ_check_system_info__mutmut_4': xǁWrknvDoctorǁ_check_system_info__mutmut_4, 
        'xǁWrknvDoctorǁ_check_system_info__mutmut_5': xǁWrknvDoctorǁ_check_system_info__mutmut_5, 
        'xǁWrknvDoctorǁ_check_system_info__mutmut_6': xǁWrknvDoctorǁ_check_system_info__mutmut_6, 
        'xǁWrknvDoctorǁ_check_system_info__mutmut_7': xǁWrknvDoctorǁ_check_system_info__mutmut_7, 
        'xǁWrknvDoctorǁ_check_system_info__mutmut_8': xǁWrknvDoctorǁ_check_system_info__mutmut_8, 
        'xǁWrknvDoctorǁ_check_system_info__mutmut_9': xǁWrknvDoctorǁ_check_system_info__mutmut_9, 
        'xǁWrknvDoctorǁ_check_system_info__mutmut_10': xǁWrknvDoctorǁ_check_system_info__mutmut_10, 
        'xǁWrknvDoctorǁ_check_system_info__mutmut_11': xǁWrknvDoctorǁ_check_system_info__mutmut_11, 
        'xǁWrknvDoctorǁ_check_system_info__mutmut_12': xǁWrknvDoctorǁ_check_system_info__mutmut_12, 
        'xǁWrknvDoctorǁ_check_system_info__mutmut_13': xǁWrknvDoctorǁ_check_system_info__mutmut_13, 
        'xǁWrknvDoctorǁ_check_system_info__mutmut_14': xǁWrknvDoctorǁ_check_system_info__mutmut_14, 
        'xǁWrknvDoctorǁ_check_system_info__mutmut_15': xǁWrknvDoctorǁ_check_system_info__mutmut_15, 
        'xǁWrknvDoctorǁ_check_system_info__mutmut_16': xǁWrknvDoctorǁ_check_system_info__mutmut_16, 
        'xǁWrknvDoctorǁ_check_system_info__mutmut_17': xǁWrknvDoctorǁ_check_system_info__mutmut_17, 
        'xǁWrknvDoctorǁ_check_system_info__mutmut_18': xǁWrknvDoctorǁ_check_system_info__mutmut_18, 
        'xǁWrknvDoctorǁ_check_system_info__mutmut_19': xǁWrknvDoctorǁ_check_system_info__mutmut_19, 
        'xǁWrknvDoctorǁ_check_system_info__mutmut_20': xǁWrknvDoctorǁ_check_system_info__mutmut_20, 
        'xǁWrknvDoctorǁ_check_system_info__mutmut_21': xǁWrknvDoctorǁ_check_system_info__mutmut_21, 
        'xǁWrknvDoctorǁ_check_system_info__mutmut_22': xǁWrknvDoctorǁ_check_system_info__mutmut_22, 
        'xǁWrknvDoctorǁ_check_system_info__mutmut_23': xǁWrknvDoctorǁ_check_system_info__mutmut_23, 
        'xǁWrknvDoctorǁ_check_system_info__mutmut_24': xǁWrknvDoctorǁ_check_system_info__mutmut_24, 
        'xǁWrknvDoctorǁ_check_system_info__mutmut_25': xǁWrknvDoctorǁ_check_system_info__mutmut_25, 
        'xǁWrknvDoctorǁ_check_system_info__mutmut_26': xǁWrknvDoctorǁ_check_system_info__mutmut_26, 
        'xǁWrknvDoctorǁ_check_system_info__mutmut_27': xǁWrknvDoctorǁ_check_system_info__mutmut_27, 
        'xǁWrknvDoctorǁ_check_system_info__mutmut_28': xǁWrknvDoctorǁ_check_system_info__mutmut_28, 
        'xǁWrknvDoctorǁ_check_system_info__mutmut_29': xǁWrknvDoctorǁ_check_system_info__mutmut_29, 
        'xǁWrknvDoctorǁ_check_system_info__mutmut_30': xǁWrknvDoctorǁ_check_system_info__mutmut_30, 
        'xǁWrknvDoctorǁ_check_system_info__mutmut_31': xǁWrknvDoctorǁ_check_system_info__mutmut_31
    }
    
    def _check_system_info(self, *args, **kwargs):
        result = _mutmut_trampoline(object.__getattribute__(self, "xǁWrknvDoctorǁ_check_system_info__mutmut_orig"), object.__getattribute__(self, "xǁWrknvDoctorǁ_check_system_info__mutmut_mutants"), args, kwargs, self)
        return result 
    
    _check_system_info.__signature__ = _mutmut_signature(xǁWrknvDoctorǁ_check_system_info__mutmut_orig)
    xǁWrknvDoctorǁ_check_system_info__mutmut_orig.__name__ = 'xǁWrknvDoctorǁ_check_system_info'

    def xǁWrknvDoctorǁ_check_wrknv_installation__mutmut_orig(self) -> None:
        """Check if wrknv is properly installed."""
        try:
            # Check if wrknv module is importable
            import wrknv

            version = getattr(wrknv, "__version__", "unknown")
            self.checks_passed.append(("wrknv Installation", f"Version {version}"))

            # Check if wrknv CLI is available
            result = run(["wrknv", "--version"], timeout=5)
            if result.returncode == 0:
                self.checks_passed.append(("wrknv CLI", "Available"))
            else:
                self.checks_warning.append(("wrknv CLI", "Not in PATH"))
        except ImportError:
            self.checks_failed.append(("wrknv Installation", "Module not found"))
        except FileNotFoundError:
            self.checks_warning.append(("wrknv CLI", "Not found in PATH"))
        except Exception as e:
            self.checks_failed.append(("wrknv Installation", str(e)))

    def xǁWrknvDoctorǁ_check_wrknv_installation__mutmut_1(self) -> None:
        """Check if wrknv is properly installed."""
        try:
            # Check if wrknv module is importable
            import wrknv

            version = None
            self.checks_passed.append(("wrknv Installation", f"Version {version}"))

            # Check if wrknv CLI is available
            result = run(["wrknv", "--version"], timeout=5)
            if result.returncode == 0:
                self.checks_passed.append(("wrknv CLI", "Available"))
            else:
                self.checks_warning.append(("wrknv CLI", "Not in PATH"))
        except ImportError:
            self.checks_failed.append(("wrknv Installation", "Module not found"))
        except FileNotFoundError:
            self.checks_warning.append(("wrknv CLI", "Not found in PATH"))
        except Exception as e:
            self.checks_failed.append(("wrknv Installation", str(e)))

    def xǁWrknvDoctorǁ_check_wrknv_installation__mutmut_2(self) -> None:
        """Check if wrknv is properly installed."""
        try:
            # Check if wrknv module is importable
            import wrknv

            version = getattr(None, "__version__", "unknown")
            self.checks_passed.append(("wrknv Installation", f"Version {version}"))

            # Check if wrknv CLI is available
            result = run(["wrknv", "--version"], timeout=5)
            if result.returncode == 0:
                self.checks_passed.append(("wrknv CLI", "Available"))
            else:
                self.checks_warning.append(("wrknv CLI", "Not in PATH"))
        except ImportError:
            self.checks_failed.append(("wrknv Installation", "Module not found"))
        except FileNotFoundError:
            self.checks_warning.append(("wrknv CLI", "Not found in PATH"))
        except Exception as e:
            self.checks_failed.append(("wrknv Installation", str(e)))

    def xǁWrknvDoctorǁ_check_wrknv_installation__mutmut_3(self) -> None:
        """Check if wrknv is properly installed."""
        try:
            # Check if wrknv module is importable
            import wrknv

            version = getattr(wrknv, None, "unknown")
            self.checks_passed.append(("wrknv Installation", f"Version {version}"))

            # Check if wrknv CLI is available
            result = run(["wrknv", "--version"], timeout=5)
            if result.returncode == 0:
                self.checks_passed.append(("wrknv CLI", "Available"))
            else:
                self.checks_warning.append(("wrknv CLI", "Not in PATH"))
        except ImportError:
            self.checks_failed.append(("wrknv Installation", "Module not found"))
        except FileNotFoundError:
            self.checks_warning.append(("wrknv CLI", "Not found in PATH"))
        except Exception as e:
            self.checks_failed.append(("wrknv Installation", str(e)))

    def xǁWrknvDoctorǁ_check_wrknv_installation__mutmut_4(self) -> None:
        """Check if wrknv is properly installed."""
        try:
            # Check if wrknv module is importable
            import wrknv

            version = getattr(wrknv, "__version__", None)
            self.checks_passed.append(("wrknv Installation", f"Version {version}"))

            # Check if wrknv CLI is available
            result = run(["wrknv", "--version"], timeout=5)
            if result.returncode == 0:
                self.checks_passed.append(("wrknv CLI", "Available"))
            else:
                self.checks_warning.append(("wrknv CLI", "Not in PATH"))
        except ImportError:
            self.checks_failed.append(("wrknv Installation", "Module not found"))
        except FileNotFoundError:
            self.checks_warning.append(("wrknv CLI", "Not found in PATH"))
        except Exception as e:
            self.checks_failed.append(("wrknv Installation", str(e)))

    def xǁWrknvDoctorǁ_check_wrknv_installation__mutmut_5(self) -> None:
        """Check if wrknv is properly installed."""
        try:
            # Check if wrknv module is importable
            import wrknv

            version = getattr("__version__", "unknown")
            self.checks_passed.append(("wrknv Installation", f"Version {version}"))

            # Check if wrknv CLI is available
            result = run(["wrknv", "--version"], timeout=5)
            if result.returncode == 0:
                self.checks_passed.append(("wrknv CLI", "Available"))
            else:
                self.checks_warning.append(("wrknv CLI", "Not in PATH"))
        except ImportError:
            self.checks_failed.append(("wrknv Installation", "Module not found"))
        except FileNotFoundError:
            self.checks_warning.append(("wrknv CLI", "Not found in PATH"))
        except Exception as e:
            self.checks_failed.append(("wrknv Installation", str(e)))

    def xǁWrknvDoctorǁ_check_wrknv_installation__mutmut_6(self) -> None:
        """Check if wrknv is properly installed."""
        try:
            # Check if wrknv module is importable
            import wrknv

            version = getattr(wrknv, "unknown")
            self.checks_passed.append(("wrknv Installation", f"Version {version}"))

            # Check if wrknv CLI is available
            result = run(["wrknv", "--version"], timeout=5)
            if result.returncode == 0:
                self.checks_passed.append(("wrknv CLI", "Available"))
            else:
                self.checks_warning.append(("wrknv CLI", "Not in PATH"))
        except ImportError:
            self.checks_failed.append(("wrknv Installation", "Module not found"))
        except FileNotFoundError:
            self.checks_warning.append(("wrknv CLI", "Not found in PATH"))
        except Exception as e:
            self.checks_failed.append(("wrknv Installation", str(e)))

    def xǁWrknvDoctorǁ_check_wrknv_installation__mutmut_7(self) -> None:
        """Check if wrknv is properly installed."""
        try:
            # Check if wrknv module is importable
            import wrknv

            version = getattr(wrknv, "__version__", )
            self.checks_passed.append(("wrknv Installation", f"Version {version}"))

            # Check if wrknv CLI is available
            result = run(["wrknv", "--version"], timeout=5)
            if result.returncode == 0:
                self.checks_passed.append(("wrknv CLI", "Available"))
            else:
                self.checks_warning.append(("wrknv CLI", "Not in PATH"))
        except ImportError:
            self.checks_failed.append(("wrknv Installation", "Module not found"))
        except FileNotFoundError:
            self.checks_warning.append(("wrknv CLI", "Not found in PATH"))
        except Exception as e:
            self.checks_failed.append(("wrknv Installation", str(e)))

    def xǁWrknvDoctorǁ_check_wrknv_installation__mutmut_8(self) -> None:
        """Check if wrknv is properly installed."""
        try:
            # Check if wrknv module is importable
            import wrknv

            version = getattr(wrknv, "XX__version__XX", "unknown")
            self.checks_passed.append(("wrknv Installation", f"Version {version}"))

            # Check if wrknv CLI is available
            result = run(["wrknv", "--version"], timeout=5)
            if result.returncode == 0:
                self.checks_passed.append(("wrknv CLI", "Available"))
            else:
                self.checks_warning.append(("wrknv CLI", "Not in PATH"))
        except ImportError:
            self.checks_failed.append(("wrknv Installation", "Module not found"))
        except FileNotFoundError:
            self.checks_warning.append(("wrknv CLI", "Not found in PATH"))
        except Exception as e:
            self.checks_failed.append(("wrknv Installation", str(e)))

    def xǁWrknvDoctorǁ_check_wrknv_installation__mutmut_9(self) -> None:
        """Check if wrknv is properly installed."""
        try:
            # Check if wrknv module is importable
            import wrknv

            version = getattr(wrknv, "__VERSION__", "unknown")
            self.checks_passed.append(("wrknv Installation", f"Version {version}"))

            # Check if wrknv CLI is available
            result = run(["wrknv", "--version"], timeout=5)
            if result.returncode == 0:
                self.checks_passed.append(("wrknv CLI", "Available"))
            else:
                self.checks_warning.append(("wrknv CLI", "Not in PATH"))
        except ImportError:
            self.checks_failed.append(("wrknv Installation", "Module not found"))
        except FileNotFoundError:
            self.checks_warning.append(("wrknv CLI", "Not found in PATH"))
        except Exception as e:
            self.checks_failed.append(("wrknv Installation", str(e)))

    def xǁWrknvDoctorǁ_check_wrknv_installation__mutmut_10(self) -> None:
        """Check if wrknv is properly installed."""
        try:
            # Check if wrknv module is importable
            import wrknv

            version = getattr(wrknv, "__version__", "XXunknownXX")
            self.checks_passed.append(("wrknv Installation", f"Version {version}"))

            # Check if wrknv CLI is available
            result = run(["wrknv", "--version"], timeout=5)
            if result.returncode == 0:
                self.checks_passed.append(("wrknv CLI", "Available"))
            else:
                self.checks_warning.append(("wrknv CLI", "Not in PATH"))
        except ImportError:
            self.checks_failed.append(("wrknv Installation", "Module not found"))
        except FileNotFoundError:
            self.checks_warning.append(("wrknv CLI", "Not found in PATH"))
        except Exception as e:
            self.checks_failed.append(("wrknv Installation", str(e)))

    def xǁWrknvDoctorǁ_check_wrknv_installation__mutmut_11(self) -> None:
        """Check if wrknv is properly installed."""
        try:
            # Check if wrknv module is importable
            import wrknv

            version = getattr(wrknv, "__version__", "UNKNOWN")
            self.checks_passed.append(("wrknv Installation", f"Version {version}"))

            # Check if wrknv CLI is available
            result = run(["wrknv", "--version"], timeout=5)
            if result.returncode == 0:
                self.checks_passed.append(("wrknv CLI", "Available"))
            else:
                self.checks_warning.append(("wrknv CLI", "Not in PATH"))
        except ImportError:
            self.checks_failed.append(("wrknv Installation", "Module not found"))
        except FileNotFoundError:
            self.checks_warning.append(("wrknv CLI", "Not found in PATH"))
        except Exception as e:
            self.checks_failed.append(("wrknv Installation", str(e)))

    def xǁWrknvDoctorǁ_check_wrknv_installation__mutmut_12(self) -> None:
        """Check if wrknv is properly installed."""
        try:
            # Check if wrknv module is importable
            import wrknv

            version = getattr(wrknv, "__version__", "unknown")
            self.checks_passed.append(None)

            # Check if wrknv CLI is available
            result = run(["wrknv", "--version"], timeout=5)
            if result.returncode == 0:
                self.checks_passed.append(("wrknv CLI", "Available"))
            else:
                self.checks_warning.append(("wrknv CLI", "Not in PATH"))
        except ImportError:
            self.checks_failed.append(("wrknv Installation", "Module not found"))
        except FileNotFoundError:
            self.checks_warning.append(("wrknv CLI", "Not found in PATH"))
        except Exception as e:
            self.checks_failed.append(("wrknv Installation", str(e)))

    def xǁWrknvDoctorǁ_check_wrknv_installation__mutmut_13(self) -> None:
        """Check if wrknv is properly installed."""
        try:
            # Check if wrknv module is importable
            import wrknv

            version = getattr(wrknv, "__version__", "unknown")
            self.checks_passed.append(("XXwrknv InstallationXX", f"Version {version}"))

            # Check if wrknv CLI is available
            result = run(["wrknv", "--version"], timeout=5)
            if result.returncode == 0:
                self.checks_passed.append(("wrknv CLI", "Available"))
            else:
                self.checks_warning.append(("wrknv CLI", "Not in PATH"))
        except ImportError:
            self.checks_failed.append(("wrknv Installation", "Module not found"))
        except FileNotFoundError:
            self.checks_warning.append(("wrknv CLI", "Not found in PATH"))
        except Exception as e:
            self.checks_failed.append(("wrknv Installation", str(e)))

    def xǁWrknvDoctorǁ_check_wrknv_installation__mutmut_14(self) -> None:
        """Check if wrknv is properly installed."""
        try:
            # Check if wrknv module is importable
            import wrknv

            version = getattr(wrknv, "__version__", "unknown")
            self.checks_passed.append(("wrknv installation", f"Version {version}"))

            # Check if wrknv CLI is available
            result = run(["wrknv", "--version"], timeout=5)
            if result.returncode == 0:
                self.checks_passed.append(("wrknv CLI", "Available"))
            else:
                self.checks_warning.append(("wrknv CLI", "Not in PATH"))
        except ImportError:
            self.checks_failed.append(("wrknv Installation", "Module not found"))
        except FileNotFoundError:
            self.checks_warning.append(("wrknv CLI", "Not found in PATH"))
        except Exception as e:
            self.checks_failed.append(("wrknv Installation", str(e)))

    def xǁWrknvDoctorǁ_check_wrknv_installation__mutmut_15(self) -> None:
        """Check if wrknv is properly installed."""
        try:
            # Check if wrknv module is importable
            import wrknv

            version = getattr(wrknv, "__version__", "unknown")
            self.checks_passed.append(("WRKNV INSTALLATION", f"Version {version}"))

            # Check if wrknv CLI is available
            result = run(["wrknv", "--version"], timeout=5)
            if result.returncode == 0:
                self.checks_passed.append(("wrknv CLI", "Available"))
            else:
                self.checks_warning.append(("wrknv CLI", "Not in PATH"))
        except ImportError:
            self.checks_failed.append(("wrknv Installation", "Module not found"))
        except FileNotFoundError:
            self.checks_warning.append(("wrknv CLI", "Not found in PATH"))
        except Exception as e:
            self.checks_failed.append(("wrknv Installation", str(e)))

    def xǁWrknvDoctorǁ_check_wrknv_installation__mutmut_16(self) -> None:
        """Check if wrknv is properly installed."""
        try:
            # Check if wrknv module is importable
            import wrknv

            version = getattr(wrknv, "__version__", "unknown")
            self.checks_passed.append(("wrknv Installation", f"Version {version}"))

            # Check if wrknv CLI is available
            result = None
            if result.returncode == 0:
                self.checks_passed.append(("wrknv CLI", "Available"))
            else:
                self.checks_warning.append(("wrknv CLI", "Not in PATH"))
        except ImportError:
            self.checks_failed.append(("wrknv Installation", "Module not found"))
        except FileNotFoundError:
            self.checks_warning.append(("wrknv CLI", "Not found in PATH"))
        except Exception as e:
            self.checks_failed.append(("wrknv Installation", str(e)))

    def xǁWrknvDoctorǁ_check_wrknv_installation__mutmut_17(self) -> None:
        """Check if wrknv is properly installed."""
        try:
            # Check if wrknv module is importable
            import wrknv

            version = getattr(wrknv, "__version__", "unknown")
            self.checks_passed.append(("wrknv Installation", f"Version {version}"))

            # Check if wrknv CLI is available
            result = run(None, timeout=5)
            if result.returncode == 0:
                self.checks_passed.append(("wrknv CLI", "Available"))
            else:
                self.checks_warning.append(("wrknv CLI", "Not in PATH"))
        except ImportError:
            self.checks_failed.append(("wrknv Installation", "Module not found"))
        except FileNotFoundError:
            self.checks_warning.append(("wrknv CLI", "Not found in PATH"))
        except Exception as e:
            self.checks_failed.append(("wrknv Installation", str(e)))

    def xǁWrknvDoctorǁ_check_wrknv_installation__mutmut_18(self) -> None:
        """Check if wrknv is properly installed."""
        try:
            # Check if wrknv module is importable
            import wrknv

            version = getattr(wrknv, "__version__", "unknown")
            self.checks_passed.append(("wrknv Installation", f"Version {version}"))

            # Check if wrknv CLI is available
            result = run(["wrknv", "--version"], timeout=None)
            if result.returncode == 0:
                self.checks_passed.append(("wrknv CLI", "Available"))
            else:
                self.checks_warning.append(("wrknv CLI", "Not in PATH"))
        except ImportError:
            self.checks_failed.append(("wrknv Installation", "Module not found"))
        except FileNotFoundError:
            self.checks_warning.append(("wrknv CLI", "Not found in PATH"))
        except Exception as e:
            self.checks_failed.append(("wrknv Installation", str(e)))

    def xǁWrknvDoctorǁ_check_wrknv_installation__mutmut_19(self) -> None:
        """Check if wrknv is properly installed."""
        try:
            # Check if wrknv module is importable
            import wrknv

            version = getattr(wrknv, "__version__", "unknown")
            self.checks_passed.append(("wrknv Installation", f"Version {version}"))

            # Check if wrknv CLI is available
            result = run(timeout=5)
            if result.returncode == 0:
                self.checks_passed.append(("wrknv CLI", "Available"))
            else:
                self.checks_warning.append(("wrknv CLI", "Not in PATH"))
        except ImportError:
            self.checks_failed.append(("wrknv Installation", "Module not found"))
        except FileNotFoundError:
            self.checks_warning.append(("wrknv CLI", "Not found in PATH"))
        except Exception as e:
            self.checks_failed.append(("wrknv Installation", str(e)))

    def xǁWrknvDoctorǁ_check_wrknv_installation__mutmut_20(self) -> None:
        """Check if wrknv is properly installed."""
        try:
            # Check if wrknv module is importable
            import wrknv

            version = getattr(wrknv, "__version__", "unknown")
            self.checks_passed.append(("wrknv Installation", f"Version {version}"))

            # Check if wrknv CLI is available
            result = run(["wrknv", "--version"], )
            if result.returncode == 0:
                self.checks_passed.append(("wrknv CLI", "Available"))
            else:
                self.checks_warning.append(("wrknv CLI", "Not in PATH"))
        except ImportError:
            self.checks_failed.append(("wrknv Installation", "Module not found"))
        except FileNotFoundError:
            self.checks_warning.append(("wrknv CLI", "Not found in PATH"))
        except Exception as e:
            self.checks_failed.append(("wrknv Installation", str(e)))

    def xǁWrknvDoctorǁ_check_wrknv_installation__mutmut_21(self) -> None:
        """Check if wrknv is properly installed."""
        try:
            # Check if wrknv module is importable
            import wrknv

            version = getattr(wrknv, "__version__", "unknown")
            self.checks_passed.append(("wrknv Installation", f"Version {version}"))

            # Check if wrknv CLI is available
            result = run(["XXwrknvXX", "--version"], timeout=5)
            if result.returncode == 0:
                self.checks_passed.append(("wrknv CLI", "Available"))
            else:
                self.checks_warning.append(("wrknv CLI", "Not in PATH"))
        except ImportError:
            self.checks_failed.append(("wrknv Installation", "Module not found"))
        except FileNotFoundError:
            self.checks_warning.append(("wrknv CLI", "Not found in PATH"))
        except Exception as e:
            self.checks_failed.append(("wrknv Installation", str(e)))

    def xǁWrknvDoctorǁ_check_wrknv_installation__mutmut_22(self) -> None:
        """Check if wrknv is properly installed."""
        try:
            # Check if wrknv module is importable
            import wrknv

            version = getattr(wrknv, "__version__", "unknown")
            self.checks_passed.append(("wrknv Installation", f"Version {version}"))

            # Check if wrknv CLI is available
            result = run(["WRKNV", "--version"], timeout=5)
            if result.returncode == 0:
                self.checks_passed.append(("wrknv CLI", "Available"))
            else:
                self.checks_warning.append(("wrknv CLI", "Not in PATH"))
        except ImportError:
            self.checks_failed.append(("wrknv Installation", "Module not found"))
        except FileNotFoundError:
            self.checks_warning.append(("wrknv CLI", "Not found in PATH"))
        except Exception as e:
            self.checks_failed.append(("wrknv Installation", str(e)))

    def xǁWrknvDoctorǁ_check_wrknv_installation__mutmut_23(self) -> None:
        """Check if wrknv is properly installed."""
        try:
            # Check if wrknv module is importable
            import wrknv

            version = getattr(wrknv, "__version__", "unknown")
            self.checks_passed.append(("wrknv Installation", f"Version {version}"))

            # Check if wrknv CLI is available
            result = run(["wrknv", "XX--versionXX"], timeout=5)
            if result.returncode == 0:
                self.checks_passed.append(("wrknv CLI", "Available"))
            else:
                self.checks_warning.append(("wrknv CLI", "Not in PATH"))
        except ImportError:
            self.checks_failed.append(("wrknv Installation", "Module not found"))
        except FileNotFoundError:
            self.checks_warning.append(("wrknv CLI", "Not found in PATH"))
        except Exception as e:
            self.checks_failed.append(("wrknv Installation", str(e)))

    def xǁWrknvDoctorǁ_check_wrknv_installation__mutmut_24(self) -> None:
        """Check if wrknv is properly installed."""
        try:
            # Check if wrknv module is importable
            import wrknv

            version = getattr(wrknv, "__version__", "unknown")
            self.checks_passed.append(("wrknv Installation", f"Version {version}"))

            # Check if wrknv CLI is available
            result = run(["wrknv", "--VERSION"], timeout=5)
            if result.returncode == 0:
                self.checks_passed.append(("wrknv CLI", "Available"))
            else:
                self.checks_warning.append(("wrknv CLI", "Not in PATH"))
        except ImportError:
            self.checks_failed.append(("wrknv Installation", "Module not found"))
        except FileNotFoundError:
            self.checks_warning.append(("wrknv CLI", "Not found in PATH"))
        except Exception as e:
            self.checks_failed.append(("wrknv Installation", str(e)))

    def xǁWrknvDoctorǁ_check_wrknv_installation__mutmut_25(self) -> None:
        """Check if wrknv is properly installed."""
        try:
            # Check if wrknv module is importable
            import wrknv

            version = getattr(wrknv, "__version__", "unknown")
            self.checks_passed.append(("wrknv Installation", f"Version {version}"))

            # Check if wrknv CLI is available
            result = run(["wrknv", "--version"], timeout=6)
            if result.returncode == 0:
                self.checks_passed.append(("wrknv CLI", "Available"))
            else:
                self.checks_warning.append(("wrknv CLI", "Not in PATH"))
        except ImportError:
            self.checks_failed.append(("wrknv Installation", "Module not found"))
        except FileNotFoundError:
            self.checks_warning.append(("wrknv CLI", "Not found in PATH"))
        except Exception as e:
            self.checks_failed.append(("wrknv Installation", str(e)))

    def xǁWrknvDoctorǁ_check_wrknv_installation__mutmut_26(self) -> None:
        """Check if wrknv is properly installed."""
        try:
            # Check if wrknv module is importable
            import wrknv

            version = getattr(wrknv, "__version__", "unknown")
            self.checks_passed.append(("wrknv Installation", f"Version {version}"))

            # Check if wrknv CLI is available
            result = run(["wrknv", "--version"], timeout=5)
            if result.returncode != 0:
                self.checks_passed.append(("wrknv CLI", "Available"))
            else:
                self.checks_warning.append(("wrknv CLI", "Not in PATH"))
        except ImportError:
            self.checks_failed.append(("wrknv Installation", "Module not found"))
        except FileNotFoundError:
            self.checks_warning.append(("wrknv CLI", "Not found in PATH"))
        except Exception as e:
            self.checks_failed.append(("wrknv Installation", str(e)))

    def xǁWrknvDoctorǁ_check_wrknv_installation__mutmut_27(self) -> None:
        """Check if wrknv is properly installed."""
        try:
            # Check if wrknv module is importable
            import wrknv

            version = getattr(wrknv, "__version__", "unknown")
            self.checks_passed.append(("wrknv Installation", f"Version {version}"))

            # Check if wrknv CLI is available
            result = run(["wrknv", "--version"], timeout=5)
            if result.returncode == 1:
                self.checks_passed.append(("wrknv CLI", "Available"))
            else:
                self.checks_warning.append(("wrknv CLI", "Not in PATH"))
        except ImportError:
            self.checks_failed.append(("wrknv Installation", "Module not found"))
        except FileNotFoundError:
            self.checks_warning.append(("wrknv CLI", "Not found in PATH"))
        except Exception as e:
            self.checks_failed.append(("wrknv Installation", str(e)))

    def xǁWrknvDoctorǁ_check_wrknv_installation__mutmut_28(self) -> None:
        """Check if wrknv is properly installed."""
        try:
            # Check if wrknv module is importable
            import wrknv

            version = getattr(wrknv, "__version__", "unknown")
            self.checks_passed.append(("wrknv Installation", f"Version {version}"))

            # Check if wrknv CLI is available
            result = run(["wrknv", "--version"], timeout=5)
            if result.returncode == 0:
                self.checks_passed.append(None)
            else:
                self.checks_warning.append(("wrknv CLI", "Not in PATH"))
        except ImportError:
            self.checks_failed.append(("wrknv Installation", "Module not found"))
        except FileNotFoundError:
            self.checks_warning.append(("wrknv CLI", "Not found in PATH"))
        except Exception as e:
            self.checks_failed.append(("wrknv Installation", str(e)))

    def xǁWrknvDoctorǁ_check_wrknv_installation__mutmut_29(self) -> None:
        """Check if wrknv is properly installed."""
        try:
            # Check if wrknv module is importable
            import wrknv

            version = getattr(wrknv, "__version__", "unknown")
            self.checks_passed.append(("wrknv Installation", f"Version {version}"))

            # Check if wrknv CLI is available
            result = run(["wrknv", "--version"], timeout=5)
            if result.returncode == 0:
                self.checks_passed.append(("XXwrknv CLIXX", "Available"))
            else:
                self.checks_warning.append(("wrknv CLI", "Not in PATH"))
        except ImportError:
            self.checks_failed.append(("wrknv Installation", "Module not found"))
        except FileNotFoundError:
            self.checks_warning.append(("wrknv CLI", "Not found in PATH"))
        except Exception as e:
            self.checks_failed.append(("wrknv Installation", str(e)))

    def xǁWrknvDoctorǁ_check_wrknv_installation__mutmut_30(self) -> None:
        """Check if wrknv is properly installed."""
        try:
            # Check if wrknv module is importable
            import wrknv

            version = getattr(wrknv, "__version__", "unknown")
            self.checks_passed.append(("wrknv Installation", f"Version {version}"))

            # Check if wrknv CLI is available
            result = run(["wrknv", "--version"], timeout=5)
            if result.returncode == 0:
                self.checks_passed.append(("wrknv cli", "Available"))
            else:
                self.checks_warning.append(("wrknv CLI", "Not in PATH"))
        except ImportError:
            self.checks_failed.append(("wrknv Installation", "Module not found"))
        except FileNotFoundError:
            self.checks_warning.append(("wrknv CLI", "Not found in PATH"))
        except Exception as e:
            self.checks_failed.append(("wrknv Installation", str(e)))

    def xǁWrknvDoctorǁ_check_wrknv_installation__mutmut_31(self) -> None:
        """Check if wrknv is properly installed."""
        try:
            # Check if wrknv module is importable
            import wrknv

            version = getattr(wrknv, "__version__", "unknown")
            self.checks_passed.append(("wrknv Installation", f"Version {version}"))

            # Check if wrknv CLI is available
            result = run(["wrknv", "--version"], timeout=5)
            if result.returncode == 0:
                self.checks_passed.append(("WRKNV CLI", "Available"))
            else:
                self.checks_warning.append(("wrknv CLI", "Not in PATH"))
        except ImportError:
            self.checks_failed.append(("wrknv Installation", "Module not found"))
        except FileNotFoundError:
            self.checks_warning.append(("wrknv CLI", "Not found in PATH"))
        except Exception as e:
            self.checks_failed.append(("wrknv Installation", str(e)))

    def xǁWrknvDoctorǁ_check_wrknv_installation__mutmut_32(self) -> None:
        """Check if wrknv is properly installed."""
        try:
            # Check if wrknv module is importable
            import wrknv

            version = getattr(wrknv, "__version__", "unknown")
            self.checks_passed.append(("wrknv Installation", f"Version {version}"))

            # Check if wrknv CLI is available
            result = run(["wrknv", "--version"], timeout=5)
            if result.returncode == 0:
                self.checks_passed.append(("wrknv CLI", "XXAvailableXX"))
            else:
                self.checks_warning.append(("wrknv CLI", "Not in PATH"))
        except ImportError:
            self.checks_failed.append(("wrknv Installation", "Module not found"))
        except FileNotFoundError:
            self.checks_warning.append(("wrknv CLI", "Not found in PATH"))
        except Exception as e:
            self.checks_failed.append(("wrknv Installation", str(e)))

    def xǁWrknvDoctorǁ_check_wrknv_installation__mutmut_33(self) -> None:
        """Check if wrknv is properly installed."""
        try:
            # Check if wrknv module is importable
            import wrknv

            version = getattr(wrknv, "__version__", "unknown")
            self.checks_passed.append(("wrknv Installation", f"Version {version}"))

            # Check if wrknv CLI is available
            result = run(["wrknv", "--version"], timeout=5)
            if result.returncode == 0:
                self.checks_passed.append(("wrknv CLI", "available"))
            else:
                self.checks_warning.append(("wrknv CLI", "Not in PATH"))
        except ImportError:
            self.checks_failed.append(("wrknv Installation", "Module not found"))
        except FileNotFoundError:
            self.checks_warning.append(("wrknv CLI", "Not found in PATH"))
        except Exception as e:
            self.checks_failed.append(("wrknv Installation", str(e)))

    def xǁWrknvDoctorǁ_check_wrknv_installation__mutmut_34(self) -> None:
        """Check if wrknv is properly installed."""
        try:
            # Check if wrknv module is importable
            import wrknv

            version = getattr(wrknv, "__version__", "unknown")
            self.checks_passed.append(("wrknv Installation", f"Version {version}"))

            # Check if wrknv CLI is available
            result = run(["wrknv", "--version"], timeout=5)
            if result.returncode == 0:
                self.checks_passed.append(("wrknv CLI", "AVAILABLE"))
            else:
                self.checks_warning.append(("wrknv CLI", "Not in PATH"))
        except ImportError:
            self.checks_failed.append(("wrknv Installation", "Module not found"))
        except FileNotFoundError:
            self.checks_warning.append(("wrknv CLI", "Not found in PATH"))
        except Exception as e:
            self.checks_failed.append(("wrknv Installation", str(e)))

    def xǁWrknvDoctorǁ_check_wrknv_installation__mutmut_35(self) -> None:
        """Check if wrknv is properly installed."""
        try:
            # Check if wrknv module is importable
            import wrknv

            version = getattr(wrknv, "__version__", "unknown")
            self.checks_passed.append(("wrknv Installation", f"Version {version}"))

            # Check if wrknv CLI is available
            result = run(["wrknv", "--version"], timeout=5)
            if result.returncode == 0:
                self.checks_passed.append(("wrknv CLI", "Available"))
            else:
                self.checks_warning.append(None)
        except ImportError:
            self.checks_failed.append(("wrknv Installation", "Module not found"))
        except FileNotFoundError:
            self.checks_warning.append(("wrknv CLI", "Not found in PATH"))
        except Exception as e:
            self.checks_failed.append(("wrknv Installation", str(e)))

    def xǁWrknvDoctorǁ_check_wrknv_installation__mutmut_36(self) -> None:
        """Check if wrknv is properly installed."""
        try:
            # Check if wrknv module is importable
            import wrknv

            version = getattr(wrknv, "__version__", "unknown")
            self.checks_passed.append(("wrknv Installation", f"Version {version}"))

            # Check if wrknv CLI is available
            result = run(["wrknv", "--version"], timeout=5)
            if result.returncode == 0:
                self.checks_passed.append(("wrknv CLI", "Available"))
            else:
                self.checks_warning.append(("XXwrknv CLIXX", "Not in PATH"))
        except ImportError:
            self.checks_failed.append(("wrknv Installation", "Module not found"))
        except FileNotFoundError:
            self.checks_warning.append(("wrknv CLI", "Not found in PATH"))
        except Exception as e:
            self.checks_failed.append(("wrknv Installation", str(e)))

    def xǁWrknvDoctorǁ_check_wrknv_installation__mutmut_37(self) -> None:
        """Check if wrknv is properly installed."""
        try:
            # Check if wrknv module is importable
            import wrknv

            version = getattr(wrknv, "__version__", "unknown")
            self.checks_passed.append(("wrknv Installation", f"Version {version}"))

            # Check if wrknv CLI is available
            result = run(["wrknv", "--version"], timeout=5)
            if result.returncode == 0:
                self.checks_passed.append(("wrknv CLI", "Available"))
            else:
                self.checks_warning.append(("wrknv cli", "Not in PATH"))
        except ImportError:
            self.checks_failed.append(("wrknv Installation", "Module not found"))
        except FileNotFoundError:
            self.checks_warning.append(("wrknv CLI", "Not found in PATH"))
        except Exception as e:
            self.checks_failed.append(("wrknv Installation", str(e)))

    def xǁWrknvDoctorǁ_check_wrknv_installation__mutmut_38(self) -> None:
        """Check if wrknv is properly installed."""
        try:
            # Check if wrknv module is importable
            import wrknv

            version = getattr(wrknv, "__version__", "unknown")
            self.checks_passed.append(("wrknv Installation", f"Version {version}"))

            # Check if wrknv CLI is available
            result = run(["wrknv", "--version"], timeout=5)
            if result.returncode == 0:
                self.checks_passed.append(("wrknv CLI", "Available"))
            else:
                self.checks_warning.append(("WRKNV CLI", "Not in PATH"))
        except ImportError:
            self.checks_failed.append(("wrknv Installation", "Module not found"))
        except FileNotFoundError:
            self.checks_warning.append(("wrknv CLI", "Not found in PATH"))
        except Exception as e:
            self.checks_failed.append(("wrknv Installation", str(e)))

    def xǁWrknvDoctorǁ_check_wrknv_installation__mutmut_39(self) -> None:
        """Check if wrknv is properly installed."""
        try:
            # Check if wrknv module is importable
            import wrknv

            version = getattr(wrknv, "__version__", "unknown")
            self.checks_passed.append(("wrknv Installation", f"Version {version}"))

            # Check if wrknv CLI is available
            result = run(["wrknv", "--version"], timeout=5)
            if result.returncode == 0:
                self.checks_passed.append(("wrknv CLI", "Available"))
            else:
                self.checks_warning.append(("wrknv CLI", "XXNot in PATHXX"))
        except ImportError:
            self.checks_failed.append(("wrknv Installation", "Module not found"))
        except FileNotFoundError:
            self.checks_warning.append(("wrknv CLI", "Not found in PATH"))
        except Exception as e:
            self.checks_failed.append(("wrknv Installation", str(e)))

    def xǁWrknvDoctorǁ_check_wrknv_installation__mutmut_40(self) -> None:
        """Check if wrknv is properly installed."""
        try:
            # Check if wrknv module is importable
            import wrknv

            version = getattr(wrknv, "__version__", "unknown")
            self.checks_passed.append(("wrknv Installation", f"Version {version}"))

            # Check if wrknv CLI is available
            result = run(["wrknv", "--version"], timeout=5)
            if result.returncode == 0:
                self.checks_passed.append(("wrknv CLI", "Available"))
            else:
                self.checks_warning.append(("wrknv CLI", "not in path"))
        except ImportError:
            self.checks_failed.append(("wrknv Installation", "Module not found"))
        except FileNotFoundError:
            self.checks_warning.append(("wrknv CLI", "Not found in PATH"))
        except Exception as e:
            self.checks_failed.append(("wrknv Installation", str(e)))

    def xǁWrknvDoctorǁ_check_wrknv_installation__mutmut_41(self) -> None:
        """Check if wrknv is properly installed."""
        try:
            # Check if wrknv module is importable
            import wrknv

            version = getattr(wrknv, "__version__", "unknown")
            self.checks_passed.append(("wrknv Installation", f"Version {version}"))

            # Check if wrknv CLI is available
            result = run(["wrknv", "--version"], timeout=5)
            if result.returncode == 0:
                self.checks_passed.append(("wrknv CLI", "Available"))
            else:
                self.checks_warning.append(("wrknv CLI", "NOT IN PATH"))
        except ImportError:
            self.checks_failed.append(("wrknv Installation", "Module not found"))
        except FileNotFoundError:
            self.checks_warning.append(("wrknv CLI", "Not found in PATH"))
        except Exception as e:
            self.checks_failed.append(("wrknv Installation", str(e)))

    def xǁWrknvDoctorǁ_check_wrknv_installation__mutmut_42(self) -> None:
        """Check if wrknv is properly installed."""
        try:
            # Check if wrknv module is importable
            import wrknv

            version = getattr(wrknv, "__version__", "unknown")
            self.checks_passed.append(("wrknv Installation", f"Version {version}"))

            # Check if wrknv CLI is available
            result = run(["wrknv", "--version"], timeout=5)
            if result.returncode == 0:
                self.checks_passed.append(("wrknv CLI", "Available"))
            else:
                self.checks_warning.append(("wrknv CLI", "Not in PATH"))
        except ImportError:
            self.checks_failed.append(None)
        except FileNotFoundError:
            self.checks_warning.append(("wrknv CLI", "Not found in PATH"))
        except Exception as e:
            self.checks_failed.append(("wrknv Installation", str(e)))

    def xǁWrknvDoctorǁ_check_wrknv_installation__mutmut_43(self) -> None:
        """Check if wrknv is properly installed."""
        try:
            # Check if wrknv module is importable
            import wrknv

            version = getattr(wrknv, "__version__", "unknown")
            self.checks_passed.append(("wrknv Installation", f"Version {version}"))

            # Check if wrknv CLI is available
            result = run(["wrknv", "--version"], timeout=5)
            if result.returncode == 0:
                self.checks_passed.append(("wrknv CLI", "Available"))
            else:
                self.checks_warning.append(("wrknv CLI", "Not in PATH"))
        except ImportError:
            self.checks_failed.append(("XXwrknv InstallationXX", "Module not found"))
        except FileNotFoundError:
            self.checks_warning.append(("wrknv CLI", "Not found in PATH"))
        except Exception as e:
            self.checks_failed.append(("wrknv Installation", str(e)))

    def xǁWrknvDoctorǁ_check_wrknv_installation__mutmut_44(self) -> None:
        """Check if wrknv is properly installed."""
        try:
            # Check if wrknv module is importable
            import wrknv

            version = getattr(wrknv, "__version__", "unknown")
            self.checks_passed.append(("wrknv Installation", f"Version {version}"))

            # Check if wrknv CLI is available
            result = run(["wrknv", "--version"], timeout=5)
            if result.returncode == 0:
                self.checks_passed.append(("wrknv CLI", "Available"))
            else:
                self.checks_warning.append(("wrknv CLI", "Not in PATH"))
        except ImportError:
            self.checks_failed.append(("wrknv installation", "Module not found"))
        except FileNotFoundError:
            self.checks_warning.append(("wrknv CLI", "Not found in PATH"))
        except Exception as e:
            self.checks_failed.append(("wrknv Installation", str(e)))

    def xǁWrknvDoctorǁ_check_wrknv_installation__mutmut_45(self) -> None:
        """Check if wrknv is properly installed."""
        try:
            # Check if wrknv module is importable
            import wrknv

            version = getattr(wrknv, "__version__", "unknown")
            self.checks_passed.append(("wrknv Installation", f"Version {version}"))

            # Check if wrknv CLI is available
            result = run(["wrknv", "--version"], timeout=5)
            if result.returncode == 0:
                self.checks_passed.append(("wrknv CLI", "Available"))
            else:
                self.checks_warning.append(("wrknv CLI", "Not in PATH"))
        except ImportError:
            self.checks_failed.append(("WRKNV INSTALLATION", "Module not found"))
        except FileNotFoundError:
            self.checks_warning.append(("wrknv CLI", "Not found in PATH"))
        except Exception as e:
            self.checks_failed.append(("wrknv Installation", str(e)))

    def xǁWrknvDoctorǁ_check_wrknv_installation__mutmut_46(self) -> None:
        """Check if wrknv is properly installed."""
        try:
            # Check if wrknv module is importable
            import wrknv

            version = getattr(wrknv, "__version__", "unknown")
            self.checks_passed.append(("wrknv Installation", f"Version {version}"))

            # Check if wrknv CLI is available
            result = run(["wrknv", "--version"], timeout=5)
            if result.returncode == 0:
                self.checks_passed.append(("wrknv CLI", "Available"))
            else:
                self.checks_warning.append(("wrknv CLI", "Not in PATH"))
        except ImportError:
            self.checks_failed.append(("wrknv Installation", "XXModule not foundXX"))
        except FileNotFoundError:
            self.checks_warning.append(("wrknv CLI", "Not found in PATH"))
        except Exception as e:
            self.checks_failed.append(("wrknv Installation", str(e)))

    def xǁWrknvDoctorǁ_check_wrknv_installation__mutmut_47(self) -> None:
        """Check if wrknv is properly installed."""
        try:
            # Check if wrknv module is importable
            import wrknv

            version = getattr(wrknv, "__version__", "unknown")
            self.checks_passed.append(("wrknv Installation", f"Version {version}"))

            # Check if wrknv CLI is available
            result = run(["wrknv", "--version"], timeout=5)
            if result.returncode == 0:
                self.checks_passed.append(("wrknv CLI", "Available"))
            else:
                self.checks_warning.append(("wrknv CLI", "Not in PATH"))
        except ImportError:
            self.checks_failed.append(("wrknv Installation", "module not found"))
        except FileNotFoundError:
            self.checks_warning.append(("wrknv CLI", "Not found in PATH"))
        except Exception as e:
            self.checks_failed.append(("wrknv Installation", str(e)))

    def xǁWrknvDoctorǁ_check_wrknv_installation__mutmut_48(self) -> None:
        """Check if wrknv is properly installed."""
        try:
            # Check if wrknv module is importable
            import wrknv

            version = getattr(wrknv, "__version__", "unknown")
            self.checks_passed.append(("wrknv Installation", f"Version {version}"))

            # Check if wrknv CLI is available
            result = run(["wrknv", "--version"], timeout=5)
            if result.returncode == 0:
                self.checks_passed.append(("wrknv CLI", "Available"))
            else:
                self.checks_warning.append(("wrknv CLI", "Not in PATH"))
        except ImportError:
            self.checks_failed.append(("wrknv Installation", "MODULE NOT FOUND"))
        except FileNotFoundError:
            self.checks_warning.append(("wrknv CLI", "Not found in PATH"))
        except Exception as e:
            self.checks_failed.append(("wrknv Installation", str(e)))

    def xǁWrknvDoctorǁ_check_wrknv_installation__mutmut_49(self) -> None:
        """Check if wrknv is properly installed."""
        try:
            # Check if wrknv module is importable
            import wrknv

            version = getattr(wrknv, "__version__", "unknown")
            self.checks_passed.append(("wrknv Installation", f"Version {version}"))

            # Check if wrknv CLI is available
            result = run(["wrknv", "--version"], timeout=5)
            if result.returncode == 0:
                self.checks_passed.append(("wrknv CLI", "Available"))
            else:
                self.checks_warning.append(("wrknv CLI", "Not in PATH"))
        except ImportError:
            self.checks_failed.append(("wrknv Installation", "Module not found"))
        except FileNotFoundError:
            self.checks_warning.append(None)
        except Exception as e:
            self.checks_failed.append(("wrknv Installation", str(e)))

    def xǁWrknvDoctorǁ_check_wrknv_installation__mutmut_50(self) -> None:
        """Check if wrknv is properly installed."""
        try:
            # Check if wrknv module is importable
            import wrknv

            version = getattr(wrknv, "__version__", "unknown")
            self.checks_passed.append(("wrknv Installation", f"Version {version}"))

            # Check if wrknv CLI is available
            result = run(["wrknv", "--version"], timeout=5)
            if result.returncode == 0:
                self.checks_passed.append(("wrknv CLI", "Available"))
            else:
                self.checks_warning.append(("wrknv CLI", "Not in PATH"))
        except ImportError:
            self.checks_failed.append(("wrknv Installation", "Module not found"))
        except FileNotFoundError:
            self.checks_warning.append(("XXwrknv CLIXX", "Not found in PATH"))
        except Exception as e:
            self.checks_failed.append(("wrknv Installation", str(e)))

    def xǁWrknvDoctorǁ_check_wrknv_installation__mutmut_51(self) -> None:
        """Check if wrknv is properly installed."""
        try:
            # Check if wrknv module is importable
            import wrknv

            version = getattr(wrknv, "__version__", "unknown")
            self.checks_passed.append(("wrknv Installation", f"Version {version}"))

            # Check if wrknv CLI is available
            result = run(["wrknv", "--version"], timeout=5)
            if result.returncode == 0:
                self.checks_passed.append(("wrknv CLI", "Available"))
            else:
                self.checks_warning.append(("wrknv CLI", "Not in PATH"))
        except ImportError:
            self.checks_failed.append(("wrknv Installation", "Module not found"))
        except FileNotFoundError:
            self.checks_warning.append(("wrknv cli", "Not found in PATH"))
        except Exception as e:
            self.checks_failed.append(("wrknv Installation", str(e)))

    def xǁWrknvDoctorǁ_check_wrknv_installation__mutmut_52(self) -> None:
        """Check if wrknv is properly installed."""
        try:
            # Check if wrknv module is importable
            import wrknv

            version = getattr(wrknv, "__version__", "unknown")
            self.checks_passed.append(("wrknv Installation", f"Version {version}"))

            # Check if wrknv CLI is available
            result = run(["wrknv", "--version"], timeout=5)
            if result.returncode == 0:
                self.checks_passed.append(("wrknv CLI", "Available"))
            else:
                self.checks_warning.append(("wrknv CLI", "Not in PATH"))
        except ImportError:
            self.checks_failed.append(("wrknv Installation", "Module not found"))
        except FileNotFoundError:
            self.checks_warning.append(("WRKNV CLI", "Not found in PATH"))
        except Exception as e:
            self.checks_failed.append(("wrknv Installation", str(e)))

    def xǁWrknvDoctorǁ_check_wrknv_installation__mutmut_53(self) -> None:
        """Check if wrknv is properly installed."""
        try:
            # Check if wrknv module is importable
            import wrknv

            version = getattr(wrknv, "__version__", "unknown")
            self.checks_passed.append(("wrknv Installation", f"Version {version}"))

            # Check if wrknv CLI is available
            result = run(["wrknv", "--version"], timeout=5)
            if result.returncode == 0:
                self.checks_passed.append(("wrknv CLI", "Available"))
            else:
                self.checks_warning.append(("wrknv CLI", "Not in PATH"))
        except ImportError:
            self.checks_failed.append(("wrknv Installation", "Module not found"))
        except FileNotFoundError:
            self.checks_warning.append(("wrknv CLI", "XXNot found in PATHXX"))
        except Exception as e:
            self.checks_failed.append(("wrknv Installation", str(e)))

    def xǁWrknvDoctorǁ_check_wrknv_installation__mutmut_54(self) -> None:
        """Check if wrknv is properly installed."""
        try:
            # Check if wrknv module is importable
            import wrknv

            version = getattr(wrknv, "__version__", "unknown")
            self.checks_passed.append(("wrknv Installation", f"Version {version}"))

            # Check if wrknv CLI is available
            result = run(["wrknv", "--version"], timeout=5)
            if result.returncode == 0:
                self.checks_passed.append(("wrknv CLI", "Available"))
            else:
                self.checks_warning.append(("wrknv CLI", "Not in PATH"))
        except ImportError:
            self.checks_failed.append(("wrknv Installation", "Module not found"))
        except FileNotFoundError:
            self.checks_warning.append(("wrknv CLI", "not found in path"))
        except Exception as e:
            self.checks_failed.append(("wrknv Installation", str(e)))

    def xǁWrknvDoctorǁ_check_wrknv_installation__mutmut_55(self) -> None:
        """Check if wrknv is properly installed."""
        try:
            # Check if wrknv module is importable
            import wrknv

            version = getattr(wrknv, "__version__", "unknown")
            self.checks_passed.append(("wrknv Installation", f"Version {version}"))

            # Check if wrknv CLI is available
            result = run(["wrknv", "--version"], timeout=5)
            if result.returncode == 0:
                self.checks_passed.append(("wrknv CLI", "Available"))
            else:
                self.checks_warning.append(("wrknv CLI", "Not in PATH"))
        except ImportError:
            self.checks_failed.append(("wrknv Installation", "Module not found"))
        except FileNotFoundError:
            self.checks_warning.append(("wrknv CLI", "NOT FOUND IN PATH"))
        except Exception as e:
            self.checks_failed.append(("wrknv Installation", str(e)))

    def xǁWrknvDoctorǁ_check_wrknv_installation__mutmut_56(self) -> None:
        """Check if wrknv is properly installed."""
        try:
            # Check if wrknv module is importable
            import wrknv

            version = getattr(wrknv, "__version__", "unknown")
            self.checks_passed.append(("wrknv Installation", f"Version {version}"))

            # Check if wrknv CLI is available
            result = run(["wrknv", "--version"], timeout=5)
            if result.returncode == 0:
                self.checks_passed.append(("wrknv CLI", "Available"))
            else:
                self.checks_warning.append(("wrknv CLI", "Not in PATH"))
        except ImportError:
            self.checks_failed.append(("wrknv Installation", "Module not found"))
        except FileNotFoundError:
            self.checks_warning.append(("wrknv CLI", "Not found in PATH"))
        except Exception as e:
            self.checks_failed.append(None)

    def xǁWrknvDoctorǁ_check_wrknv_installation__mutmut_57(self) -> None:
        """Check if wrknv is properly installed."""
        try:
            # Check if wrknv module is importable
            import wrknv

            version = getattr(wrknv, "__version__", "unknown")
            self.checks_passed.append(("wrknv Installation", f"Version {version}"))

            # Check if wrknv CLI is available
            result = run(["wrknv", "--version"], timeout=5)
            if result.returncode == 0:
                self.checks_passed.append(("wrknv CLI", "Available"))
            else:
                self.checks_warning.append(("wrknv CLI", "Not in PATH"))
        except ImportError:
            self.checks_failed.append(("wrknv Installation", "Module not found"))
        except FileNotFoundError:
            self.checks_warning.append(("wrknv CLI", "Not found in PATH"))
        except Exception as e:
            self.checks_failed.append(("XXwrknv InstallationXX", str(e)))

    def xǁWrknvDoctorǁ_check_wrknv_installation__mutmut_58(self) -> None:
        """Check if wrknv is properly installed."""
        try:
            # Check if wrknv module is importable
            import wrknv

            version = getattr(wrknv, "__version__", "unknown")
            self.checks_passed.append(("wrknv Installation", f"Version {version}"))

            # Check if wrknv CLI is available
            result = run(["wrknv", "--version"], timeout=5)
            if result.returncode == 0:
                self.checks_passed.append(("wrknv CLI", "Available"))
            else:
                self.checks_warning.append(("wrknv CLI", "Not in PATH"))
        except ImportError:
            self.checks_failed.append(("wrknv Installation", "Module not found"))
        except FileNotFoundError:
            self.checks_warning.append(("wrknv CLI", "Not found in PATH"))
        except Exception as e:
            self.checks_failed.append(("wrknv installation", str(e)))

    def xǁWrknvDoctorǁ_check_wrknv_installation__mutmut_59(self) -> None:
        """Check if wrknv is properly installed."""
        try:
            # Check if wrknv module is importable
            import wrknv

            version = getattr(wrknv, "__version__", "unknown")
            self.checks_passed.append(("wrknv Installation", f"Version {version}"))

            # Check if wrknv CLI is available
            result = run(["wrknv", "--version"], timeout=5)
            if result.returncode == 0:
                self.checks_passed.append(("wrknv CLI", "Available"))
            else:
                self.checks_warning.append(("wrknv CLI", "Not in PATH"))
        except ImportError:
            self.checks_failed.append(("wrknv Installation", "Module not found"))
        except FileNotFoundError:
            self.checks_warning.append(("wrknv CLI", "Not found in PATH"))
        except Exception as e:
            self.checks_failed.append(("WRKNV INSTALLATION", str(e)))

    def xǁWrknvDoctorǁ_check_wrknv_installation__mutmut_60(self) -> None:
        """Check if wrknv is properly installed."""
        try:
            # Check if wrknv module is importable
            import wrknv

            version = getattr(wrknv, "__version__", "unknown")
            self.checks_passed.append(("wrknv Installation", f"Version {version}"))

            # Check if wrknv CLI is available
            result = run(["wrknv", "--version"], timeout=5)
            if result.returncode == 0:
                self.checks_passed.append(("wrknv CLI", "Available"))
            else:
                self.checks_warning.append(("wrknv CLI", "Not in PATH"))
        except ImportError:
            self.checks_failed.append(("wrknv Installation", "Module not found"))
        except FileNotFoundError:
            self.checks_warning.append(("wrknv CLI", "Not found in PATH"))
        except Exception as e:
            self.checks_failed.append(("wrknv Installation", str(None)))
    
    xǁWrknvDoctorǁ_check_wrknv_installation__mutmut_mutants : ClassVar[MutantDict] = {
    'xǁWrknvDoctorǁ_check_wrknv_installation__mutmut_1': xǁWrknvDoctorǁ_check_wrknv_installation__mutmut_1, 
        'xǁWrknvDoctorǁ_check_wrknv_installation__mutmut_2': xǁWrknvDoctorǁ_check_wrknv_installation__mutmut_2, 
        'xǁWrknvDoctorǁ_check_wrknv_installation__mutmut_3': xǁWrknvDoctorǁ_check_wrknv_installation__mutmut_3, 
        'xǁWrknvDoctorǁ_check_wrknv_installation__mutmut_4': xǁWrknvDoctorǁ_check_wrknv_installation__mutmut_4, 
        'xǁWrknvDoctorǁ_check_wrknv_installation__mutmut_5': xǁWrknvDoctorǁ_check_wrknv_installation__mutmut_5, 
        'xǁWrknvDoctorǁ_check_wrknv_installation__mutmut_6': xǁWrknvDoctorǁ_check_wrknv_installation__mutmut_6, 
        'xǁWrknvDoctorǁ_check_wrknv_installation__mutmut_7': xǁWrknvDoctorǁ_check_wrknv_installation__mutmut_7, 
        'xǁWrknvDoctorǁ_check_wrknv_installation__mutmut_8': xǁWrknvDoctorǁ_check_wrknv_installation__mutmut_8, 
        'xǁWrknvDoctorǁ_check_wrknv_installation__mutmut_9': xǁWrknvDoctorǁ_check_wrknv_installation__mutmut_9, 
        'xǁWrknvDoctorǁ_check_wrknv_installation__mutmut_10': xǁWrknvDoctorǁ_check_wrknv_installation__mutmut_10, 
        'xǁWrknvDoctorǁ_check_wrknv_installation__mutmut_11': xǁWrknvDoctorǁ_check_wrknv_installation__mutmut_11, 
        'xǁWrknvDoctorǁ_check_wrknv_installation__mutmut_12': xǁWrknvDoctorǁ_check_wrknv_installation__mutmut_12, 
        'xǁWrknvDoctorǁ_check_wrknv_installation__mutmut_13': xǁWrknvDoctorǁ_check_wrknv_installation__mutmut_13, 
        'xǁWrknvDoctorǁ_check_wrknv_installation__mutmut_14': xǁWrknvDoctorǁ_check_wrknv_installation__mutmut_14, 
        'xǁWrknvDoctorǁ_check_wrknv_installation__mutmut_15': xǁWrknvDoctorǁ_check_wrknv_installation__mutmut_15, 
        'xǁWrknvDoctorǁ_check_wrknv_installation__mutmut_16': xǁWrknvDoctorǁ_check_wrknv_installation__mutmut_16, 
        'xǁWrknvDoctorǁ_check_wrknv_installation__mutmut_17': xǁWrknvDoctorǁ_check_wrknv_installation__mutmut_17, 
        'xǁWrknvDoctorǁ_check_wrknv_installation__mutmut_18': xǁWrknvDoctorǁ_check_wrknv_installation__mutmut_18, 
        'xǁWrknvDoctorǁ_check_wrknv_installation__mutmut_19': xǁWrknvDoctorǁ_check_wrknv_installation__mutmut_19, 
        'xǁWrknvDoctorǁ_check_wrknv_installation__mutmut_20': xǁWrknvDoctorǁ_check_wrknv_installation__mutmut_20, 
        'xǁWrknvDoctorǁ_check_wrknv_installation__mutmut_21': xǁWrknvDoctorǁ_check_wrknv_installation__mutmut_21, 
        'xǁWrknvDoctorǁ_check_wrknv_installation__mutmut_22': xǁWrknvDoctorǁ_check_wrknv_installation__mutmut_22, 
        'xǁWrknvDoctorǁ_check_wrknv_installation__mutmut_23': xǁWrknvDoctorǁ_check_wrknv_installation__mutmut_23, 
        'xǁWrknvDoctorǁ_check_wrknv_installation__mutmut_24': xǁWrknvDoctorǁ_check_wrknv_installation__mutmut_24, 
        'xǁWrknvDoctorǁ_check_wrknv_installation__mutmut_25': xǁWrknvDoctorǁ_check_wrknv_installation__mutmut_25, 
        'xǁWrknvDoctorǁ_check_wrknv_installation__mutmut_26': xǁWrknvDoctorǁ_check_wrknv_installation__mutmut_26, 
        'xǁWrknvDoctorǁ_check_wrknv_installation__mutmut_27': xǁWrknvDoctorǁ_check_wrknv_installation__mutmut_27, 
        'xǁWrknvDoctorǁ_check_wrknv_installation__mutmut_28': xǁWrknvDoctorǁ_check_wrknv_installation__mutmut_28, 
        'xǁWrknvDoctorǁ_check_wrknv_installation__mutmut_29': xǁWrknvDoctorǁ_check_wrknv_installation__mutmut_29, 
        'xǁWrknvDoctorǁ_check_wrknv_installation__mutmut_30': xǁWrknvDoctorǁ_check_wrknv_installation__mutmut_30, 
        'xǁWrknvDoctorǁ_check_wrknv_installation__mutmut_31': xǁWrknvDoctorǁ_check_wrknv_installation__mutmut_31, 
        'xǁWrknvDoctorǁ_check_wrknv_installation__mutmut_32': xǁWrknvDoctorǁ_check_wrknv_installation__mutmut_32, 
        'xǁWrknvDoctorǁ_check_wrknv_installation__mutmut_33': xǁWrknvDoctorǁ_check_wrknv_installation__mutmut_33, 
        'xǁWrknvDoctorǁ_check_wrknv_installation__mutmut_34': xǁWrknvDoctorǁ_check_wrknv_installation__mutmut_34, 
        'xǁWrknvDoctorǁ_check_wrknv_installation__mutmut_35': xǁWrknvDoctorǁ_check_wrknv_installation__mutmut_35, 
        'xǁWrknvDoctorǁ_check_wrknv_installation__mutmut_36': xǁWrknvDoctorǁ_check_wrknv_installation__mutmut_36, 
        'xǁWrknvDoctorǁ_check_wrknv_installation__mutmut_37': xǁWrknvDoctorǁ_check_wrknv_installation__mutmut_37, 
        'xǁWrknvDoctorǁ_check_wrknv_installation__mutmut_38': xǁWrknvDoctorǁ_check_wrknv_installation__mutmut_38, 
        'xǁWrknvDoctorǁ_check_wrknv_installation__mutmut_39': xǁWrknvDoctorǁ_check_wrknv_installation__mutmut_39, 
        'xǁWrknvDoctorǁ_check_wrknv_installation__mutmut_40': xǁWrknvDoctorǁ_check_wrknv_installation__mutmut_40, 
        'xǁWrknvDoctorǁ_check_wrknv_installation__mutmut_41': xǁWrknvDoctorǁ_check_wrknv_installation__mutmut_41, 
        'xǁWrknvDoctorǁ_check_wrknv_installation__mutmut_42': xǁWrknvDoctorǁ_check_wrknv_installation__mutmut_42, 
        'xǁWrknvDoctorǁ_check_wrknv_installation__mutmut_43': xǁWrknvDoctorǁ_check_wrknv_installation__mutmut_43, 
        'xǁWrknvDoctorǁ_check_wrknv_installation__mutmut_44': xǁWrknvDoctorǁ_check_wrknv_installation__mutmut_44, 
        'xǁWrknvDoctorǁ_check_wrknv_installation__mutmut_45': xǁWrknvDoctorǁ_check_wrknv_installation__mutmut_45, 
        'xǁWrknvDoctorǁ_check_wrknv_installation__mutmut_46': xǁWrknvDoctorǁ_check_wrknv_installation__mutmut_46, 
        'xǁWrknvDoctorǁ_check_wrknv_installation__mutmut_47': xǁWrknvDoctorǁ_check_wrknv_installation__mutmut_47, 
        'xǁWrknvDoctorǁ_check_wrknv_installation__mutmut_48': xǁWrknvDoctorǁ_check_wrknv_installation__mutmut_48, 
        'xǁWrknvDoctorǁ_check_wrknv_installation__mutmut_49': xǁWrknvDoctorǁ_check_wrknv_installation__mutmut_49, 
        'xǁWrknvDoctorǁ_check_wrknv_installation__mutmut_50': xǁWrknvDoctorǁ_check_wrknv_installation__mutmut_50, 
        'xǁWrknvDoctorǁ_check_wrknv_installation__mutmut_51': xǁWrknvDoctorǁ_check_wrknv_installation__mutmut_51, 
        'xǁWrknvDoctorǁ_check_wrknv_installation__mutmut_52': xǁWrknvDoctorǁ_check_wrknv_installation__mutmut_52, 
        'xǁWrknvDoctorǁ_check_wrknv_installation__mutmut_53': xǁWrknvDoctorǁ_check_wrknv_installation__mutmut_53, 
        'xǁWrknvDoctorǁ_check_wrknv_installation__mutmut_54': xǁWrknvDoctorǁ_check_wrknv_installation__mutmut_54, 
        'xǁWrknvDoctorǁ_check_wrknv_installation__mutmut_55': xǁWrknvDoctorǁ_check_wrknv_installation__mutmut_55, 
        'xǁWrknvDoctorǁ_check_wrknv_installation__mutmut_56': xǁWrknvDoctorǁ_check_wrknv_installation__mutmut_56, 
        'xǁWrknvDoctorǁ_check_wrknv_installation__mutmut_57': xǁWrknvDoctorǁ_check_wrknv_installation__mutmut_57, 
        'xǁWrknvDoctorǁ_check_wrknv_installation__mutmut_58': xǁWrknvDoctorǁ_check_wrknv_installation__mutmut_58, 
        'xǁWrknvDoctorǁ_check_wrknv_installation__mutmut_59': xǁWrknvDoctorǁ_check_wrknv_installation__mutmut_59, 
        'xǁWrknvDoctorǁ_check_wrknv_installation__mutmut_60': xǁWrknvDoctorǁ_check_wrknv_installation__mutmut_60
    }
    
    def _check_wrknv_installation(self, *args, **kwargs):
        result = _mutmut_trampoline(object.__getattribute__(self, "xǁWrknvDoctorǁ_check_wrknv_installation__mutmut_orig"), object.__getattribute__(self, "xǁWrknvDoctorǁ_check_wrknv_installation__mutmut_mutants"), args, kwargs, self)
        return result 
    
    _check_wrknv_installation.__signature__ = _mutmut_signature(xǁWrknvDoctorǁ_check_wrknv_installation__mutmut_orig)
    xǁWrknvDoctorǁ_check_wrknv_installation__mutmut_orig.__name__ = 'xǁWrknvDoctorǁ_check_wrknv_installation'

    def xǁWrknvDoctorǁ_check_dependencies__mutmut_orig(self) -> None:
        """Check for required dependencies."""
        dependencies = {
            "uv": "Package manager",
            "git": "Version control",
            "python3": "Python interpreter",
            "bash": "Shell for env.sh",
        }

        for cmd, description in dependencies.items():
            if shutil.which(cmd):
                self.checks_passed.append((f"{cmd}", f"{description} - Found"))
            elif cmd in ["uv"]:
                self.checks_failed.append((f"{cmd}", f"{description} - Required but not found"))
            else:
                self.checks_warning.append((f"{cmd}", f"{description} - Optional but recommended"))

    def xǁWrknvDoctorǁ_check_dependencies__mutmut_1(self) -> None:
        """Check for required dependencies."""
        dependencies = None

        for cmd, description in dependencies.items():
            if shutil.which(cmd):
                self.checks_passed.append((f"{cmd}", f"{description} - Found"))
            elif cmd in ["uv"]:
                self.checks_failed.append((f"{cmd}", f"{description} - Required but not found"))
            else:
                self.checks_warning.append((f"{cmd}", f"{description} - Optional but recommended"))

    def xǁWrknvDoctorǁ_check_dependencies__mutmut_2(self) -> None:
        """Check for required dependencies."""
        dependencies = {
            "XXuvXX": "Package manager",
            "git": "Version control",
            "python3": "Python interpreter",
            "bash": "Shell for env.sh",
        }

        for cmd, description in dependencies.items():
            if shutil.which(cmd):
                self.checks_passed.append((f"{cmd}", f"{description} - Found"))
            elif cmd in ["uv"]:
                self.checks_failed.append((f"{cmd}", f"{description} - Required but not found"))
            else:
                self.checks_warning.append((f"{cmd}", f"{description} - Optional but recommended"))

    def xǁWrknvDoctorǁ_check_dependencies__mutmut_3(self) -> None:
        """Check for required dependencies."""
        dependencies = {
            "UV": "Package manager",
            "git": "Version control",
            "python3": "Python interpreter",
            "bash": "Shell for env.sh",
        }

        for cmd, description in dependencies.items():
            if shutil.which(cmd):
                self.checks_passed.append((f"{cmd}", f"{description} - Found"))
            elif cmd in ["uv"]:
                self.checks_failed.append((f"{cmd}", f"{description} - Required but not found"))
            else:
                self.checks_warning.append((f"{cmd}", f"{description} - Optional but recommended"))

    def xǁWrknvDoctorǁ_check_dependencies__mutmut_4(self) -> None:
        """Check for required dependencies."""
        dependencies = {
            "uv": "XXPackage managerXX",
            "git": "Version control",
            "python3": "Python interpreter",
            "bash": "Shell for env.sh",
        }

        for cmd, description in dependencies.items():
            if shutil.which(cmd):
                self.checks_passed.append((f"{cmd}", f"{description} - Found"))
            elif cmd in ["uv"]:
                self.checks_failed.append((f"{cmd}", f"{description} - Required but not found"))
            else:
                self.checks_warning.append((f"{cmd}", f"{description} - Optional but recommended"))

    def xǁWrknvDoctorǁ_check_dependencies__mutmut_5(self) -> None:
        """Check for required dependencies."""
        dependencies = {
            "uv": "package manager",
            "git": "Version control",
            "python3": "Python interpreter",
            "bash": "Shell for env.sh",
        }

        for cmd, description in dependencies.items():
            if shutil.which(cmd):
                self.checks_passed.append((f"{cmd}", f"{description} - Found"))
            elif cmd in ["uv"]:
                self.checks_failed.append((f"{cmd}", f"{description} - Required but not found"))
            else:
                self.checks_warning.append((f"{cmd}", f"{description} - Optional but recommended"))

    def xǁWrknvDoctorǁ_check_dependencies__mutmut_6(self) -> None:
        """Check for required dependencies."""
        dependencies = {
            "uv": "PACKAGE MANAGER",
            "git": "Version control",
            "python3": "Python interpreter",
            "bash": "Shell for env.sh",
        }

        for cmd, description in dependencies.items():
            if shutil.which(cmd):
                self.checks_passed.append((f"{cmd}", f"{description} - Found"))
            elif cmd in ["uv"]:
                self.checks_failed.append((f"{cmd}", f"{description} - Required but not found"))
            else:
                self.checks_warning.append((f"{cmd}", f"{description} - Optional but recommended"))

    def xǁWrknvDoctorǁ_check_dependencies__mutmut_7(self) -> None:
        """Check for required dependencies."""
        dependencies = {
            "uv": "Package manager",
            "XXgitXX": "Version control",
            "python3": "Python interpreter",
            "bash": "Shell for env.sh",
        }

        for cmd, description in dependencies.items():
            if shutil.which(cmd):
                self.checks_passed.append((f"{cmd}", f"{description} - Found"))
            elif cmd in ["uv"]:
                self.checks_failed.append((f"{cmd}", f"{description} - Required but not found"))
            else:
                self.checks_warning.append((f"{cmd}", f"{description} - Optional but recommended"))

    def xǁWrknvDoctorǁ_check_dependencies__mutmut_8(self) -> None:
        """Check for required dependencies."""
        dependencies = {
            "uv": "Package manager",
            "GIT": "Version control",
            "python3": "Python interpreter",
            "bash": "Shell for env.sh",
        }

        for cmd, description in dependencies.items():
            if shutil.which(cmd):
                self.checks_passed.append((f"{cmd}", f"{description} - Found"))
            elif cmd in ["uv"]:
                self.checks_failed.append((f"{cmd}", f"{description} - Required but not found"))
            else:
                self.checks_warning.append((f"{cmd}", f"{description} - Optional but recommended"))

    def xǁWrknvDoctorǁ_check_dependencies__mutmut_9(self) -> None:
        """Check for required dependencies."""
        dependencies = {
            "uv": "Package manager",
            "git": "XXVersion controlXX",
            "python3": "Python interpreter",
            "bash": "Shell for env.sh",
        }

        for cmd, description in dependencies.items():
            if shutil.which(cmd):
                self.checks_passed.append((f"{cmd}", f"{description} - Found"))
            elif cmd in ["uv"]:
                self.checks_failed.append((f"{cmd}", f"{description} - Required but not found"))
            else:
                self.checks_warning.append((f"{cmd}", f"{description} - Optional but recommended"))

    def xǁWrknvDoctorǁ_check_dependencies__mutmut_10(self) -> None:
        """Check for required dependencies."""
        dependencies = {
            "uv": "Package manager",
            "git": "version control",
            "python3": "Python interpreter",
            "bash": "Shell for env.sh",
        }

        for cmd, description in dependencies.items():
            if shutil.which(cmd):
                self.checks_passed.append((f"{cmd}", f"{description} - Found"))
            elif cmd in ["uv"]:
                self.checks_failed.append((f"{cmd}", f"{description} - Required but not found"))
            else:
                self.checks_warning.append((f"{cmd}", f"{description} - Optional but recommended"))

    def xǁWrknvDoctorǁ_check_dependencies__mutmut_11(self) -> None:
        """Check for required dependencies."""
        dependencies = {
            "uv": "Package manager",
            "git": "VERSION CONTROL",
            "python3": "Python interpreter",
            "bash": "Shell for env.sh",
        }

        for cmd, description in dependencies.items():
            if shutil.which(cmd):
                self.checks_passed.append((f"{cmd}", f"{description} - Found"))
            elif cmd in ["uv"]:
                self.checks_failed.append((f"{cmd}", f"{description} - Required but not found"))
            else:
                self.checks_warning.append((f"{cmd}", f"{description} - Optional but recommended"))

    def xǁWrknvDoctorǁ_check_dependencies__mutmut_12(self) -> None:
        """Check for required dependencies."""
        dependencies = {
            "uv": "Package manager",
            "git": "Version control",
            "XXpython3XX": "Python interpreter",
            "bash": "Shell for env.sh",
        }

        for cmd, description in dependencies.items():
            if shutil.which(cmd):
                self.checks_passed.append((f"{cmd}", f"{description} - Found"))
            elif cmd in ["uv"]:
                self.checks_failed.append((f"{cmd}", f"{description} - Required but not found"))
            else:
                self.checks_warning.append((f"{cmd}", f"{description} - Optional but recommended"))

    def xǁWrknvDoctorǁ_check_dependencies__mutmut_13(self) -> None:
        """Check for required dependencies."""
        dependencies = {
            "uv": "Package manager",
            "git": "Version control",
            "PYTHON3": "Python interpreter",
            "bash": "Shell for env.sh",
        }

        for cmd, description in dependencies.items():
            if shutil.which(cmd):
                self.checks_passed.append((f"{cmd}", f"{description} - Found"))
            elif cmd in ["uv"]:
                self.checks_failed.append((f"{cmd}", f"{description} - Required but not found"))
            else:
                self.checks_warning.append((f"{cmd}", f"{description} - Optional but recommended"))

    def xǁWrknvDoctorǁ_check_dependencies__mutmut_14(self) -> None:
        """Check for required dependencies."""
        dependencies = {
            "uv": "Package manager",
            "git": "Version control",
            "python3": "XXPython interpreterXX",
            "bash": "Shell for env.sh",
        }

        for cmd, description in dependencies.items():
            if shutil.which(cmd):
                self.checks_passed.append((f"{cmd}", f"{description} - Found"))
            elif cmd in ["uv"]:
                self.checks_failed.append((f"{cmd}", f"{description} - Required but not found"))
            else:
                self.checks_warning.append((f"{cmd}", f"{description} - Optional but recommended"))

    def xǁWrknvDoctorǁ_check_dependencies__mutmut_15(self) -> None:
        """Check for required dependencies."""
        dependencies = {
            "uv": "Package manager",
            "git": "Version control",
            "python3": "python interpreter",
            "bash": "Shell for env.sh",
        }

        for cmd, description in dependencies.items():
            if shutil.which(cmd):
                self.checks_passed.append((f"{cmd}", f"{description} - Found"))
            elif cmd in ["uv"]:
                self.checks_failed.append((f"{cmd}", f"{description} - Required but not found"))
            else:
                self.checks_warning.append((f"{cmd}", f"{description} - Optional but recommended"))

    def xǁWrknvDoctorǁ_check_dependencies__mutmut_16(self) -> None:
        """Check for required dependencies."""
        dependencies = {
            "uv": "Package manager",
            "git": "Version control",
            "python3": "PYTHON INTERPRETER",
            "bash": "Shell for env.sh",
        }

        for cmd, description in dependencies.items():
            if shutil.which(cmd):
                self.checks_passed.append((f"{cmd}", f"{description} - Found"))
            elif cmd in ["uv"]:
                self.checks_failed.append((f"{cmd}", f"{description} - Required but not found"))
            else:
                self.checks_warning.append((f"{cmd}", f"{description} - Optional but recommended"))

    def xǁWrknvDoctorǁ_check_dependencies__mutmut_17(self) -> None:
        """Check for required dependencies."""
        dependencies = {
            "uv": "Package manager",
            "git": "Version control",
            "python3": "Python interpreter",
            "XXbashXX": "Shell for env.sh",
        }

        for cmd, description in dependencies.items():
            if shutil.which(cmd):
                self.checks_passed.append((f"{cmd}", f"{description} - Found"))
            elif cmd in ["uv"]:
                self.checks_failed.append((f"{cmd}", f"{description} - Required but not found"))
            else:
                self.checks_warning.append((f"{cmd}", f"{description} - Optional but recommended"))

    def xǁWrknvDoctorǁ_check_dependencies__mutmut_18(self) -> None:
        """Check for required dependencies."""
        dependencies = {
            "uv": "Package manager",
            "git": "Version control",
            "python3": "Python interpreter",
            "BASH": "Shell for env.sh",
        }

        for cmd, description in dependencies.items():
            if shutil.which(cmd):
                self.checks_passed.append((f"{cmd}", f"{description} - Found"))
            elif cmd in ["uv"]:
                self.checks_failed.append((f"{cmd}", f"{description} - Required but not found"))
            else:
                self.checks_warning.append((f"{cmd}", f"{description} - Optional but recommended"))

    def xǁWrknvDoctorǁ_check_dependencies__mutmut_19(self) -> None:
        """Check for required dependencies."""
        dependencies = {
            "uv": "Package manager",
            "git": "Version control",
            "python3": "Python interpreter",
            "bash": "XXShell for env.shXX",
        }

        for cmd, description in dependencies.items():
            if shutil.which(cmd):
                self.checks_passed.append((f"{cmd}", f"{description} - Found"))
            elif cmd in ["uv"]:
                self.checks_failed.append((f"{cmd}", f"{description} - Required but not found"))
            else:
                self.checks_warning.append((f"{cmd}", f"{description} - Optional but recommended"))

    def xǁWrknvDoctorǁ_check_dependencies__mutmut_20(self) -> None:
        """Check for required dependencies."""
        dependencies = {
            "uv": "Package manager",
            "git": "Version control",
            "python3": "Python interpreter",
            "bash": "shell for env.sh",
        }

        for cmd, description in dependencies.items():
            if shutil.which(cmd):
                self.checks_passed.append((f"{cmd}", f"{description} - Found"))
            elif cmd in ["uv"]:
                self.checks_failed.append((f"{cmd}", f"{description} - Required but not found"))
            else:
                self.checks_warning.append((f"{cmd}", f"{description} - Optional but recommended"))

    def xǁWrknvDoctorǁ_check_dependencies__mutmut_21(self) -> None:
        """Check for required dependencies."""
        dependencies = {
            "uv": "Package manager",
            "git": "Version control",
            "python3": "Python interpreter",
            "bash": "SHELL FOR ENV.SH",
        }

        for cmd, description in dependencies.items():
            if shutil.which(cmd):
                self.checks_passed.append((f"{cmd}", f"{description} - Found"))
            elif cmd in ["uv"]:
                self.checks_failed.append((f"{cmd}", f"{description} - Required but not found"))
            else:
                self.checks_warning.append((f"{cmd}", f"{description} - Optional but recommended"))

    def xǁWrknvDoctorǁ_check_dependencies__mutmut_22(self) -> None:
        """Check for required dependencies."""
        dependencies = {
            "uv": "Package manager",
            "git": "Version control",
            "python3": "Python interpreter",
            "bash": "Shell for env.sh",
        }

        for cmd, description in dependencies.items():
            if shutil.which(None):
                self.checks_passed.append((f"{cmd}", f"{description} - Found"))
            elif cmd in ["uv"]:
                self.checks_failed.append((f"{cmd}", f"{description} - Required but not found"))
            else:
                self.checks_warning.append((f"{cmd}", f"{description} - Optional but recommended"))

    def xǁWrknvDoctorǁ_check_dependencies__mutmut_23(self) -> None:
        """Check for required dependencies."""
        dependencies = {
            "uv": "Package manager",
            "git": "Version control",
            "python3": "Python interpreter",
            "bash": "Shell for env.sh",
        }

        for cmd, description in dependencies.items():
            if shutil.which(cmd):
                self.checks_passed.append(None)
            elif cmd in ["uv"]:
                self.checks_failed.append((f"{cmd}", f"{description} - Required but not found"))
            else:
                self.checks_warning.append((f"{cmd}", f"{description} - Optional but recommended"))

    def xǁWrknvDoctorǁ_check_dependencies__mutmut_24(self) -> None:
        """Check for required dependencies."""
        dependencies = {
            "uv": "Package manager",
            "git": "Version control",
            "python3": "Python interpreter",
            "bash": "Shell for env.sh",
        }

        for cmd, description in dependencies.items():
            if shutil.which(cmd):
                self.checks_passed.append((f"{cmd}", f"{description} - Found"))
            elif cmd not in ["uv"]:
                self.checks_failed.append((f"{cmd}", f"{description} - Required but not found"))
            else:
                self.checks_warning.append((f"{cmd}", f"{description} - Optional but recommended"))

    def xǁWrknvDoctorǁ_check_dependencies__mutmut_25(self) -> None:
        """Check for required dependencies."""
        dependencies = {
            "uv": "Package manager",
            "git": "Version control",
            "python3": "Python interpreter",
            "bash": "Shell for env.sh",
        }

        for cmd, description in dependencies.items():
            if shutil.which(cmd):
                self.checks_passed.append((f"{cmd}", f"{description} - Found"))
            elif cmd in ["XXuvXX"]:
                self.checks_failed.append((f"{cmd}", f"{description} - Required but not found"))
            else:
                self.checks_warning.append((f"{cmd}", f"{description} - Optional but recommended"))

    def xǁWrknvDoctorǁ_check_dependencies__mutmut_26(self) -> None:
        """Check for required dependencies."""
        dependencies = {
            "uv": "Package manager",
            "git": "Version control",
            "python3": "Python interpreter",
            "bash": "Shell for env.sh",
        }

        for cmd, description in dependencies.items():
            if shutil.which(cmd):
                self.checks_passed.append((f"{cmd}", f"{description} - Found"))
            elif cmd in ["UV"]:
                self.checks_failed.append((f"{cmd}", f"{description} - Required but not found"))
            else:
                self.checks_warning.append((f"{cmd}", f"{description} - Optional but recommended"))

    def xǁWrknvDoctorǁ_check_dependencies__mutmut_27(self) -> None:
        """Check for required dependencies."""
        dependencies = {
            "uv": "Package manager",
            "git": "Version control",
            "python3": "Python interpreter",
            "bash": "Shell for env.sh",
        }

        for cmd, description in dependencies.items():
            if shutil.which(cmd):
                self.checks_passed.append((f"{cmd}", f"{description} - Found"))
            elif cmd in ["uv"]:
                self.checks_failed.append(None)
            else:
                self.checks_warning.append((f"{cmd}", f"{description} - Optional but recommended"))

    def xǁWrknvDoctorǁ_check_dependencies__mutmut_28(self) -> None:
        """Check for required dependencies."""
        dependencies = {
            "uv": "Package manager",
            "git": "Version control",
            "python3": "Python interpreter",
            "bash": "Shell for env.sh",
        }

        for cmd, description in dependencies.items():
            if shutil.which(cmd):
                self.checks_passed.append((f"{cmd}", f"{description} - Found"))
            elif cmd in ["uv"]:
                self.checks_failed.append((f"{cmd}", f"{description} - Required but not found"))
            else:
                self.checks_warning.append(None)
    
    xǁWrknvDoctorǁ_check_dependencies__mutmut_mutants : ClassVar[MutantDict] = {
    'xǁWrknvDoctorǁ_check_dependencies__mutmut_1': xǁWrknvDoctorǁ_check_dependencies__mutmut_1, 
        'xǁWrknvDoctorǁ_check_dependencies__mutmut_2': xǁWrknvDoctorǁ_check_dependencies__mutmut_2, 
        'xǁWrknvDoctorǁ_check_dependencies__mutmut_3': xǁWrknvDoctorǁ_check_dependencies__mutmut_3, 
        'xǁWrknvDoctorǁ_check_dependencies__mutmut_4': xǁWrknvDoctorǁ_check_dependencies__mutmut_4, 
        'xǁWrknvDoctorǁ_check_dependencies__mutmut_5': xǁWrknvDoctorǁ_check_dependencies__mutmut_5, 
        'xǁWrknvDoctorǁ_check_dependencies__mutmut_6': xǁWrknvDoctorǁ_check_dependencies__mutmut_6, 
        'xǁWrknvDoctorǁ_check_dependencies__mutmut_7': xǁWrknvDoctorǁ_check_dependencies__mutmut_7, 
        'xǁWrknvDoctorǁ_check_dependencies__mutmut_8': xǁWrknvDoctorǁ_check_dependencies__mutmut_8, 
        'xǁWrknvDoctorǁ_check_dependencies__mutmut_9': xǁWrknvDoctorǁ_check_dependencies__mutmut_9, 
        'xǁWrknvDoctorǁ_check_dependencies__mutmut_10': xǁWrknvDoctorǁ_check_dependencies__mutmut_10, 
        'xǁWrknvDoctorǁ_check_dependencies__mutmut_11': xǁWrknvDoctorǁ_check_dependencies__mutmut_11, 
        'xǁWrknvDoctorǁ_check_dependencies__mutmut_12': xǁWrknvDoctorǁ_check_dependencies__mutmut_12, 
        'xǁWrknvDoctorǁ_check_dependencies__mutmut_13': xǁWrknvDoctorǁ_check_dependencies__mutmut_13, 
        'xǁWrknvDoctorǁ_check_dependencies__mutmut_14': xǁWrknvDoctorǁ_check_dependencies__mutmut_14, 
        'xǁWrknvDoctorǁ_check_dependencies__mutmut_15': xǁWrknvDoctorǁ_check_dependencies__mutmut_15, 
        'xǁWrknvDoctorǁ_check_dependencies__mutmut_16': xǁWrknvDoctorǁ_check_dependencies__mutmut_16, 
        'xǁWrknvDoctorǁ_check_dependencies__mutmut_17': xǁWrknvDoctorǁ_check_dependencies__mutmut_17, 
        'xǁWrknvDoctorǁ_check_dependencies__mutmut_18': xǁWrknvDoctorǁ_check_dependencies__mutmut_18, 
        'xǁWrknvDoctorǁ_check_dependencies__mutmut_19': xǁWrknvDoctorǁ_check_dependencies__mutmut_19, 
        'xǁWrknvDoctorǁ_check_dependencies__mutmut_20': xǁWrknvDoctorǁ_check_dependencies__mutmut_20, 
        'xǁWrknvDoctorǁ_check_dependencies__mutmut_21': xǁWrknvDoctorǁ_check_dependencies__mutmut_21, 
        'xǁWrknvDoctorǁ_check_dependencies__mutmut_22': xǁWrknvDoctorǁ_check_dependencies__mutmut_22, 
        'xǁWrknvDoctorǁ_check_dependencies__mutmut_23': xǁWrknvDoctorǁ_check_dependencies__mutmut_23, 
        'xǁWrknvDoctorǁ_check_dependencies__mutmut_24': xǁWrknvDoctorǁ_check_dependencies__mutmut_24, 
        'xǁWrknvDoctorǁ_check_dependencies__mutmut_25': xǁWrknvDoctorǁ_check_dependencies__mutmut_25, 
        'xǁWrknvDoctorǁ_check_dependencies__mutmut_26': xǁWrknvDoctorǁ_check_dependencies__mutmut_26, 
        'xǁWrknvDoctorǁ_check_dependencies__mutmut_27': xǁWrknvDoctorǁ_check_dependencies__mutmut_27, 
        'xǁWrknvDoctorǁ_check_dependencies__mutmut_28': xǁWrknvDoctorǁ_check_dependencies__mutmut_28
    }
    
    def _check_dependencies(self, *args, **kwargs):
        result = _mutmut_trampoline(object.__getattribute__(self, "xǁWrknvDoctorǁ_check_dependencies__mutmut_orig"), object.__getattribute__(self, "xǁWrknvDoctorǁ_check_dependencies__mutmut_mutants"), args, kwargs, self)
        return result 
    
    _check_dependencies.__signature__ = _mutmut_signature(xǁWrknvDoctorǁ_check_dependencies__mutmut_orig)
    xǁWrknvDoctorǁ_check_dependencies__mutmut_orig.__name__ = 'xǁWrknvDoctorǁ_check_dependencies'

    def xǁWrknvDoctorǁ_check_workenv_structure__mutmut_orig(self) -> None:
        """Check if workenv directory exists and has correct structure."""
        workenv_dir = Path.cwd() / "workenv"

        if not workenv_dir.exists():
            self.checks_warning.append(("workenv Directory", "Not found - run 'source env.sh' to create"))
            return

        # Check for platform-specific subdirectories
        subdirs = list(workenv_dir.iterdir())
        if not subdirs:
            self.checks_warning.append(("workenv Structure", "Empty - no environments created"))
            return

        # Check structure of first workenv
        for subdir in subdirs:
            if subdir.is_dir():
                expected_dirs = ["bin", "lib", "include"]
                missing = []
                for expected in expected_dirs:
                    if not (subdir / expected).exists():
                        missing.append(expected)

                if missing:
                    self.checks_warning.append(
                        (f"workenv/{subdir.name}", f"Missing directories: {', '.join(missing)}")
                    )
                else:
                    self.checks_passed.append((f"workenv/{subdir.name}", "Structure OK"))
                break

    def xǁWrknvDoctorǁ_check_workenv_structure__mutmut_1(self) -> None:
        """Check if workenv directory exists and has correct structure."""
        workenv_dir = None

        if not workenv_dir.exists():
            self.checks_warning.append(("workenv Directory", "Not found - run 'source env.sh' to create"))
            return

        # Check for platform-specific subdirectories
        subdirs = list(workenv_dir.iterdir())
        if not subdirs:
            self.checks_warning.append(("workenv Structure", "Empty - no environments created"))
            return

        # Check structure of first workenv
        for subdir in subdirs:
            if subdir.is_dir():
                expected_dirs = ["bin", "lib", "include"]
                missing = []
                for expected in expected_dirs:
                    if not (subdir / expected).exists():
                        missing.append(expected)

                if missing:
                    self.checks_warning.append(
                        (f"workenv/{subdir.name}", f"Missing directories: {', '.join(missing)}")
                    )
                else:
                    self.checks_passed.append((f"workenv/{subdir.name}", "Structure OK"))
                break

    def xǁWrknvDoctorǁ_check_workenv_structure__mutmut_2(self) -> None:
        """Check if workenv directory exists and has correct structure."""
        workenv_dir = Path.cwd() * "workenv"

        if not workenv_dir.exists():
            self.checks_warning.append(("workenv Directory", "Not found - run 'source env.sh' to create"))
            return

        # Check for platform-specific subdirectories
        subdirs = list(workenv_dir.iterdir())
        if not subdirs:
            self.checks_warning.append(("workenv Structure", "Empty - no environments created"))
            return

        # Check structure of first workenv
        for subdir in subdirs:
            if subdir.is_dir():
                expected_dirs = ["bin", "lib", "include"]
                missing = []
                for expected in expected_dirs:
                    if not (subdir / expected).exists():
                        missing.append(expected)

                if missing:
                    self.checks_warning.append(
                        (f"workenv/{subdir.name}", f"Missing directories: {', '.join(missing)}")
                    )
                else:
                    self.checks_passed.append((f"workenv/{subdir.name}", "Structure OK"))
                break

    def xǁWrknvDoctorǁ_check_workenv_structure__mutmut_3(self) -> None:
        """Check if workenv directory exists and has correct structure."""
        workenv_dir = Path.cwd() / "XXworkenvXX"

        if not workenv_dir.exists():
            self.checks_warning.append(("workenv Directory", "Not found - run 'source env.sh' to create"))
            return

        # Check for platform-specific subdirectories
        subdirs = list(workenv_dir.iterdir())
        if not subdirs:
            self.checks_warning.append(("workenv Structure", "Empty - no environments created"))
            return

        # Check structure of first workenv
        for subdir in subdirs:
            if subdir.is_dir():
                expected_dirs = ["bin", "lib", "include"]
                missing = []
                for expected in expected_dirs:
                    if not (subdir / expected).exists():
                        missing.append(expected)

                if missing:
                    self.checks_warning.append(
                        (f"workenv/{subdir.name}", f"Missing directories: {', '.join(missing)}")
                    )
                else:
                    self.checks_passed.append((f"workenv/{subdir.name}", "Structure OK"))
                break

    def xǁWrknvDoctorǁ_check_workenv_structure__mutmut_4(self) -> None:
        """Check if workenv directory exists and has correct structure."""
        workenv_dir = Path.cwd() / "WORKENV"

        if not workenv_dir.exists():
            self.checks_warning.append(("workenv Directory", "Not found - run 'source env.sh' to create"))
            return

        # Check for platform-specific subdirectories
        subdirs = list(workenv_dir.iterdir())
        if not subdirs:
            self.checks_warning.append(("workenv Structure", "Empty - no environments created"))
            return

        # Check structure of first workenv
        for subdir in subdirs:
            if subdir.is_dir():
                expected_dirs = ["bin", "lib", "include"]
                missing = []
                for expected in expected_dirs:
                    if not (subdir / expected).exists():
                        missing.append(expected)

                if missing:
                    self.checks_warning.append(
                        (f"workenv/{subdir.name}", f"Missing directories: {', '.join(missing)}")
                    )
                else:
                    self.checks_passed.append((f"workenv/{subdir.name}", "Structure OK"))
                break

    def xǁWrknvDoctorǁ_check_workenv_structure__mutmut_5(self) -> None:
        """Check if workenv directory exists and has correct structure."""
        workenv_dir = Path.cwd() / "workenv"

        if workenv_dir.exists():
            self.checks_warning.append(("workenv Directory", "Not found - run 'source env.sh' to create"))
            return

        # Check for platform-specific subdirectories
        subdirs = list(workenv_dir.iterdir())
        if not subdirs:
            self.checks_warning.append(("workenv Structure", "Empty - no environments created"))
            return

        # Check structure of first workenv
        for subdir in subdirs:
            if subdir.is_dir():
                expected_dirs = ["bin", "lib", "include"]
                missing = []
                for expected in expected_dirs:
                    if not (subdir / expected).exists():
                        missing.append(expected)

                if missing:
                    self.checks_warning.append(
                        (f"workenv/{subdir.name}", f"Missing directories: {', '.join(missing)}")
                    )
                else:
                    self.checks_passed.append((f"workenv/{subdir.name}", "Structure OK"))
                break

    def xǁWrknvDoctorǁ_check_workenv_structure__mutmut_6(self) -> None:
        """Check if workenv directory exists and has correct structure."""
        workenv_dir = Path.cwd() / "workenv"

        if not workenv_dir.exists():
            self.checks_warning.append(None)
            return

        # Check for platform-specific subdirectories
        subdirs = list(workenv_dir.iterdir())
        if not subdirs:
            self.checks_warning.append(("workenv Structure", "Empty - no environments created"))
            return

        # Check structure of first workenv
        for subdir in subdirs:
            if subdir.is_dir():
                expected_dirs = ["bin", "lib", "include"]
                missing = []
                for expected in expected_dirs:
                    if not (subdir / expected).exists():
                        missing.append(expected)

                if missing:
                    self.checks_warning.append(
                        (f"workenv/{subdir.name}", f"Missing directories: {', '.join(missing)}")
                    )
                else:
                    self.checks_passed.append((f"workenv/{subdir.name}", "Structure OK"))
                break

    def xǁWrknvDoctorǁ_check_workenv_structure__mutmut_7(self) -> None:
        """Check if workenv directory exists and has correct structure."""
        workenv_dir = Path.cwd() / "workenv"

        if not workenv_dir.exists():
            self.checks_warning.append(("XXworkenv DirectoryXX", "Not found - run 'source env.sh' to create"))
            return

        # Check for platform-specific subdirectories
        subdirs = list(workenv_dir.iterdir())
        if not subdirs:
            self.checks_warning.append(("workenv Structure", "Empty - no environments created"))
            return

        # Check structure of first workenv
        for subdir in subdirs:
            if subdir.is_dir():
                expected_dirs = ["bin", "lib", "include"]
                missing = []
                for expected in expected_dirs:
                    if not (subdir / expected).exists():
                        missing.append(expected)

                if missing:
                    self.checks_warning.append(
                        (f"workenv/{subdir.name}", f"Missing directories: {', '.join(missing)}")
                    )
                else:
                    self.checks_passed.append((f"workenv/{subdir.name}", "Structure OK"))
                break

    def xǁWrknvDoctorǁ_check_workenv_structure__mutmut_8(self) -> None:
        """Check if workenv directory exists and has correct structure."""
        workenv_dir = Path.cwd() / "workenv"

        if not workenv_dir.exists():
            self.checks_warning.append(("workenv directory", "Not found - run 'source env.sh' to create"))
            return

        # Check for platform-specific subdirectories
        subdirs = list(workenv_dir.iterdir())
        if not subdirs:
            self.checks_warning.append(("workenv Structure", "Empty - no environments created"))
            return

        # Check structure of first workenv
        for subdir in subdirs:
            if subdir.is_dir():
                expected_dirs = ["bin", "lib", "include"]
                missing = []
                for expected in expected_dirs:
                    if not (subdir / expected).exists():
                        missing.append(expected)

                if missing:
                    self.checks_warning.append(
                        (f"workenv/{subdir.name}", f"Missing directories: {', '.join(missing)}")
                    )
                else:
                    self.checks_passed.append((f"workenv/{subdir.name}", "Structure OK"))
                break

    def xǁWrknvDoctorǁ_check_workenv_structure__mutmut_9(self) -> None:
        """Check if workenv directory exists and has correct structure."""
        workenv_dir = Path.cwd() / "workenv"

        if not workenv_dir.exists():
            self.checks_warning.append(("WORKENV DIRECTORY", "Not found - run 'source env.sh' to create"))
            return

        # Check for platform-specific subdirectories
        subdirs = list(workenv_dir.iterdir())
        if not subdirs:
            self.checks_warning.append(("workenv Structure", "Empty - no environments created"))
            return

        # Check structure of first workenv
        for subdir in subdirs:
            if subdir.is_dir():
                expected_dirs = ["bin", "lib", "include"]
                missing = []
                for expected in expected_dirs:
                    if not (subdir / expected).exists():
                        missing.append(expected)

                if missing:
                    self.checks_warning.append(
                        (f"workenv/{subdir.name}", f"Missing directories: {', '.join(missing)}")
                    )
                else:
                    self.checks_passed.append((f"workenv/{subdir.name}", "Structure OK"))
                break

    def xǁWrknvDoctorǁ_check_workenv_structure__mutmut_10(self) -> None:
        """Check if workenv directory exists and has correct structure."""
        workenv_dir = Path.cwd() / "workenv"

        if not workenv_dir.exists():
            self.checks_warning.append(("workenv Directory", "XXNot found - run 'source env.sh' to createXX"))
            return

        # Check for platform-specific subdirectories
        subdirs = list(workenv_dir.iterdir())
        if not subdirs:
            self.checks_warning.append(("workenv Structure", "Empty - no environments created"))
            return

        # Check structure of first workenv
        for subdir in subdirs:
            if subdir.is_dir():
                expected_dirs = ["bin", "lib", "include"]
                missing = []
                for expected in expected_dirs:
                    if not (subdir / expected).exists():
                        missing.append(expected)

                if missing:
                    self.checks_warning.append(
                        (f"workenv/{subdir.name}", f"Missing directories: {', '.join(missing)}")
                    )
                else:
                    self.checks_passed.append((f"workenv/{subdir.name}", "Structure OK"))
                break

    def xǁWrknvDoctorǁ_check_workenv_structure__mutmut_11(self) -> None:
        """Check if workenv directory exists and has correct structure."""
        workenv_dir = Path.cwd() / "workenv"

        if not workenv_dir.exists():
            self.checks_warning.append(("workenv Directory", "not found - run 'source env.sh' to create"))
            return

        # Check for platform-specific subdirectories
        subdirs = list(workenv_dir.iterdir())
        if not subdirs:
            self.checks_warning.append(("workenv Structure", "Empty - no environments created"))
            return

        # Check structure of first workenv
        for subdir in subdirs:
            if subdir.is_dir():
                expected_dirs = ["bin", "lib", "include"]
                missing = []
                for expected in expected_dirs:
                    if not (subdir / expected).exists():
                        missing.append(expected)

                if missing:
                    self.checks_warning.append(
                        (f"workenv/{subdir.name}", f"Missing directories: {', '.join(missing)}")
                    )
                else:
                    self.checks_passed.append((f"workenv/{subdir.name}", "Structure OK"))
                break

    def xǁWrknvDoctorǁ_check_workenv_structure__mutmut_12(self) -> None:
        """Check if workenv directory exists and has correct structure."""
        workenv_dir = Path.cwd() / "workenv"

        if not workenv_dir.exists():
            self.checks_warning.append(("workenv Directory", "NOT FOUND - RUN 'SOURCE ENV.SH' TO CREATE"))
            return

        # Check for platform-specific subdirectories
        subdirs = list(workenv_dir.iterdir())
        if not subdirs:
            self.checks_warning.append(("workenv Structure", "Empty - no environments created"))
            return

        # Check structure of first workenv
        for subdir in subdirs:
            if subdir.is_dir():
                expected_dirs = ["bin", "lib", "include"]
                missing = []
                for expected in expected_dirs:
                    if not (subdir / expected).exists():
                        missing.append(expected)

                if missing:
                    self.checks_warning.append(
                        (f"workenv/{subdir.name}", f"Missing directories: {', '.join(missing)}")
                    )
                else:
                    self.checks_passed.append((f"workenv/{subdir.name}", "Structure OK"))
                break

    def xǁWrknvDoctorǁ_check_workenv_structure__mutmut_13(self) -> None:
        """Check if workenv directory exists and has correct structure."""
        workenv_dir = Path.cwd() / "workenv"

        if not workenv_dir.exists():
            self.checks_warning.append(("workenv Directory", "Not found - run 'source env.sh' to create"))
            return

        # Check for platform-specific subdirectories
        subdirs = None
        if not subdirs:
            self.checks_warning.append(("workenv Structure", "Empty - no environments created"))
            return

        # Check structure of first workenv
        for subdir in subdirs:
            if subdir.is_dir():
                expected_dirs = ["bin", "lib", "include"]
                missing = []
                for expected in expected_dirs:
                    if not (subdir / expected).exists():
                        missing.append(expected)

                if missing:
                    self.checks_warning.append(
                        (f"workenv/{subdir.name}", f"Missing directories: {', '.join(missing)}")
                    )
                else:
                    self.checks_passed.append((f"workenv/{subdir.name}", "Structure OK"))
                break

    def xǁWrknvDoctorǁ_check_workenv_structure__mutmut_14(self) -> None:
        """Check if workenv directory exists and has correct structure."""
        workenv_dir = Path.cwd() / "workenv"

        if not workenv_dir.exists():
            self.checks_warning.append(("workenv Directory", "Not found - run 'source env.sh' to create"))
            return

        # Check for platform-specific subdirectories
        subdirs = list(None)
        if not subdirs:
            self.checks_warning.append(("workenv Structure", "Empty - no environments created"))
            return

        # Check structure of first workenv
        for subdir in subdirs:
            if subdir.is_dir():
                expected_dirs = ["bin", "lib", "include"]
                missing = []
                for expected in expected_dirs:
                    if not (subdir / expected).exists():
                        missing.append(expected)

                if missing:
                    self.checks_warning.append(
                        (f"workenv/{subdir.name}", f"Missing directories: {', '.join(missing)}")
                    )
                else:
                    self.checks_passed.append((f"workenv/{subdir.name}", "Structure OK"))
                break

    def xǁWrknvDoctorǁ_check_workenv_structure__mutmut_15(self) -> None:
        """Check if workenv directory exists and has correct structure."""
        workenv_dir = Path.cwd() / "workenv"

        if not workenv_dir.exists():
            self.checks_warning.append(("workenv Directory", "Not found - run 'source env.sh' to create"))
            return

        # Check for platform-specific subdirectories
        subdirs = list(workenv_dir.iterdir())
        if subdirs:
            self.checks_warning.append(("workenv Structure", "Empty - no environments created"))
            return

        # Check structure of first workenv
        for subdir in subdirs:
            if subdir.is_dir():
                expected_dirs = ["bin", "lib", "include"]
                missing = []
                for expected in expected_dirs:
                    if not (subdir / expected).exists():
                        missing.append(expected)

                if missing:
                    self.checks_warning.append(
                        (f"workenv/{subdir.name}", f"Missing directories: {', '.join(missing)}")
                    )
                else:
                    self.checks_passed.append((f"workenv/{subdir.name}", "Structure OK"))
                break

    def xǁWrknvDoctorǁ_check_workenv_structure__mutmut_16(self) -> None:
        """Check if workenv directory exists and has correct structure."""
        workenv_dir = Path.cwd() / "workenv"

        if not workenv_dir.exists():
            self.checks_warning.append(("workenv Directory", "Not found - run 'source env.sh' to create"))
            return

        # Check for platform-specific subdirectories
        subdirs = list(workenv_dir.iterdir())
        if not subdirs:
            self.checks_warning.append(None)
            return

        # Check structure of first workenv
        for subdir in subdirs:
            if subdir.is_dir():
                expected_dirs = ["bin", "lib", "include"]
                missing = []
                for expected in expected_dirs:
                    if not (subdir / expected).exists():
                        missing.append(expected)

                if missing:
                    self.checks_warning.append(
                        (f"workenv/{subdir.name}", f"Missing directories: {', '.join(missing)}")
                    )
                else:
                    self.checks_passed.append((f"workenv/{subdir.name}", "Structure OK"))
                break

    def xǁWrknvDoctorǁ_check_workenv_structure__mutmut_17(self) -> None:
        """Check if workenv directory exists and has correct structure."""
        workenv_dir = Path.cwd() / "workenv"

        if not workenv_dir.exists():
            self.checks_warning.append(("workenv Directory", "Not found - run 'source env.sh' to create"))
            return

        # Check for platform-specific subdirectories
        subdirs = list(workenv_dir.iterdir())
        if not subdirs:
            self.checks_warning.append(("XXworkenv StructureXX", "Empty - no environments created"))
            return

        # Check structure of first workenv
        for subdir in subdirs:
            if subdir.is_dir():
                expected_dirs = ["bin", "lib", "include"]
                missing = []
                for expected in expected_dirs:
                    if not (subdir / expected).exists():
                        missing.append(expected)

                if missing:
                    self.checks_warning.append(
                        (f"workenv/{subdir.name}", f"Missing directories: {', '.join(missing)}")
                    )
                else:
                    self.checks_passed.append((f"workenv/{subdir.name}", "Structure OK"))
                break

    def xǁWrknvDoctorǁ_check_workenv_structure__mutmut_18(self) -> None:
        """Check if workenv directory exists and has correct structure."""
        workenv_dir = Path.cwd() / "workenv"

        if not workenv_dir.exists():
            self.checks_warning.append(("workenv Directory", "Not found - run 'source env.sh' to create"))
            return

        # Check for platform-specific subdirectories
        subdirs = list(workenv_dir.iterdir())
        if not subdirs:
            self.checks_warning.append(("workenv structure", "Empty - no environments created"))
            return

        # Check structure of first workenv
        for subdir in subdirs:
            if subdir.is_dir():
                expected_dirs = ["bin", "lib", "include"]
                missing = []
                for expected in expected_dirs:
                    if not (subdir / expected).exists():
                        missing.append(expected)

                if missing:
                    self.checks_warning.append(
                        (f"workenv/{subdir.name}", f"Missing directories: {', '.join(missing)}")
                    )
                else:
                    self.checks_passed.append((f"workenv/{subdir.name}", "Structure OK"))
                break

    def xǁWrknvDoctorǁ_check_workenv_structure__mutmut_19(self) -> None:
        """Check if workenv directory exists and has correct structure."""
        workenv_dir = Path.cwd() / "workenv"

        if not workenv_dir.exists():
            self.checks_warning.append(("workenv Directory", "Not found - run 'source env.sh' to create"))
            return

        # Check for platform-specific subdirectories
        subdirs = list(workenv_dir.iterdir())
        if not subdirs:
            self.checks_warning.append(("WORKENV STRUCTURE", "Empty - no environments created"))
            return

        # Check structure of first workenv
        for subdir in subdirs:
            if subdir.is_dir():
                expected_dirs = ["bin", "lib", "include"]
                missing = []
                for expected in expected_dirs:
                    if not (subdir / expected).exists():
                        missing.append(expected)

                if missing:
                    self.checks_warning.append(
                        (f"workenv/{subdir.name}", f"Missing directories: {', '.join(missing)}")
                    )
                else:
                    self.checks_passed.append((f"workenv/{subdir.name}", "Structure OK"))
                break

    def xǁWrknvDoctorǁ_check_workenv_structure__mutmut_20(self) -> None:
        """Check if workenv directory exists and has correct structure."""
        workenv_dir = Path.cwd() / "workenv"

        if not workenv_dir.exists():
            self.checks_warning.append(("workenv Directory", "Not found - run 'source env.sh' to create"))
            return

        # Check for platform-specific subdirectories
        subdirs = list(workenv_dir.iterdir())
        if not subdirs:
            self.checks_warning.append(("workenv Structure", "XXEmpty - no environments createdXX"))
            return

        # Check structure of first workenv
        for subdir in subdirs:
            if subdir.is_dir():
                expected_dirs = ["bin", "lib", "include"]
                missing = []
                for expected in expected_dirs:
                    if not (subdir / expected).exists():
                        missing.append(expected)

                if missing:
                    self.checks_warning.append(
                        (f"workenv/{subdir.name}", f"Missing directories: {', '.join(missing)}")
                    )
                else:
                    self.checks_passed.append((f"workenv/{subdir.name}", "Structure OK"))
                break

    def xǁWrknvDoctorǁ_check_workenv_structure__mutmut_21(self) -> None:
        """Check if workenv directory exists and has correct structure."""
        workenv_dir = Path.cwd() / "workenv"

        if not workenv_dir.exists():
            self.checks_warning.append(("workenv Directory", "Not found - run 'source env.sh' to create"))
            return

        # Check for platform-specific subdirectories
        subdirs = list(workenv_dir.iterdir())
        if not subdirs:
            self.checks_warning.append(("workenv Structure", "empty - no environments created"))
            return

        # Check structure of first workenv
        for subdir in subdirs:
            if subdir.is_dir():
                expected_dirs = ["bin", "lib", "include"]
                missing = []
                for expected in expected_dirs:
                    if not (subdir / expected).exists():
                        missing.append(expected)

                if missing:
                    self.checks_warning.append(
                        (f"workenv/{subdir.name}", f"Missing directories: {', '.join(missing)}")
                    )
                else:
                    self.checks_passed.append((f"workenv/{subdir.name}", "Structure OK"))
                break

    def xǁWrknvDoctorǁ_check_workenv_structure__mutmut_22(self) -> None:
        """Check if workenv directory exists and has correct structure."""
        workenv_dir = Path.cwd() / "workenv"

        if not workenv_dir.exists():
            self.checks_warning.append(("workenv Directory", "Not found - run 'source env.sh' to create"))
            return

        # Check for platform-specific subdirectories
        subdirs = list(workenv_dir.iterdir())
        if not subdirs:
            self.checks_warning.append(("workenv Structure", "EMPTY - NO ENVIRONMENTS CREATED"))
            return

        # Check structure of first workenv
        for subdir in subdirs:
            if subdir.is_dir():
                expected_dirs = ["bin", "lib", "include"]
                missing = []
                for expected in expected_dirs:
                    if not (subdir / expected).exists():
                        missing.append(expected)

                if missing:
                    self.checks_warning.append(
                        (f"workenv/{subdir.name}", f"Missing directories: {', '.join(missing)}")
                    )
                else:
                    self.checks_passed.append((f"workenv/{subdir.name}", "Structure OK"))
                break

    def xǁWrknvDoctorǁ_check_workenv_structure__mutmut_23(self) -> None:
        """Check if workenv directory exists and has correct structure."""
        workenv_dir = Path.cwd() / "workenv"

        if not workenv_dir.exists():
            self.checks_warning.append(("workenv Directory", "Not found - run 'source env.sh' to create"))
            return

        # Check for platform-specific subdirectories
        subdirs = list(workenv_dir.iterdir())
        if not subdirs:
            self.checks_warning.append(("workenv Structure", "Empty - no environments created"))
            return

        # Check structure of first workenv
        for subdir in subdirs:
            if subdir.is_dir():
                expected_dirs = None
                missing = []
                for expected in expected_dirs:
                    if not (subdir / expected).exists():
                        missing.append(expected)

                if missing:
                    self.checks_warning.append(
                        (f"workenv/{subdir.name}", f"Missing directories: {', '.join(missing)}")
                    )
                else:
                    self.checks_passed.append((f"workenv/{subdir.name}", "Structure OK"))
                break

    def xǁWrknvDoctorǁ_check_workenv_structure__mutmut_24(self) -> None:
        """Check if workenv directory exists and has correct structure."""
        workenv_dir = Path.cwd() / "workenv"

        if not workenv_dir.exists():
            self.checks_warning.append(("workenv Directory", "Not found - run 'source env.sh' to create"))
            return

        # Check for platform-specific subdirectories
        subdirs = list(workenv_dir.iterdir())
        if not subdirs:
            self.checks_warning.append(("workenv Structure", "Empty - no environments created"))
            return

        # Check structure of first workenv
        for subdir in subdirs:
            if subdir.is_dir():
                expected_dirs = ["XXbinXX", "lib", "include"]
                missing = []
                for expected in expected_dirs:
                    if not (subdir / expected).exists():
                        missing.append(expected)

                if missing:
                    self.checks_warning.append(
                        (f"workenv/{subdir.name}", f"Missing directories: {', '.join(missing)}")
                    )
                else:
                    self.checks_passed.append((f"workenv/{subdir.name}", "Structure OK"))
                break

    def xǁWrknvDoctorǁ_check_workenv_structure__mutmut_25(self) -> None:
        """Check if workenv directory exists and has correct structure."""
        workenv_dir = Path.cwd() / "workenv"

        if not workenv_dir.exists():
            self.checks_warning.append(("workenv Directory", "Not found - run 'source env.sh' to create"))
            return

        # Check for platform-specific subdirectories
        subdirs = list(workenv_dir.iterdir())
        if not subdirs:
            self.checks_warning.append(("workenv Structure", "Empty - no environments created"))
            return

        # Check structure of first workenv
        for subdir in subdirs:
            if subdir.is_dir():
                expected_dirs = ["BIN", "lib", "include"]
                missing = []
                for expected in expected_dirs:
                    if not (subdir / expected).exists():
                        missing.append(expected)

                if missing:
                    self.checks_warning.append(
                        (f"workenv/{subdir.name}", f"Missing directories: {', '.join(missing)}")
                    )
                else:
                    self.checks_passed.append((f"workenv/{subdir.name}", "Structure OK"))
                break

    def xǁWrknvDoctorǁ_check_workenv_structure__mutmut_26(self) -> None:
        """Check if workenv directory exists and has correct structure."""
        workenv_dir = Path.cwd() / "workenv"

        if not workenv_dir.exists():
            self.checks_warning.append(("workenv Directory", "Not found - run 'source env.sh' to create"))
            return

        # Check for platform-specific subdirectories
        subdirs = list(workenv_dir.iterdir())
        if not subdirs:
            self.checks_warning.append(("workenv Structure", "Empty - no environments created"))
            return

        # Check structure of first workenv
        for subdir in subdirs:
            if subdir.is_dir():
                expected_dirs = ["bin", "XXlibXX", "include"]
                missing = []
                for expected in expected_dirs:
                    if not (subdir / expected).exists():
                        missing.append(expected)

                if missing:
                    self.checks_warning.append(
                        (f"workenv/{subdir.name}", f"Missing directories: {', '.join(missing)}")
                    )
                else:
                    self.checks_passed.append((f"workenv/{subdir.name}", "Structure OK"))
                break

    def xǁWrknvDoctorǁ_check_workenv_structure__mutmut_27(self) -> None:
        """Check if workenv directory exists and has correct structure."""
        workenv_dir = Path.cwd() / "workenv"

        if not workenv_dir.exists():
            self.checks_warning.append(("workenv Directory", "Not found - run 'source env.sh' to create"))
            return

        # Check for platform-specific subdirectories
        subdirs = list(workenv_dir.iterdir())
        if not subdirs:
            self.checks_warning.append(("workenv Structure", "Empty - no environments created"))
            return

        # Check structure of first workenv
        for subdir in subdirs:
            if subdir.is_dir():
                expected_dirs = ["bin", "LIB", "include"]
                missing = []
                for expected in expected_dirs:
                    if not (subdir / expected).exists():
                        missing.append(expected)

                if missing:
                    self.checks_warning.append(
                        (f"workenv/{subdir.name}", f"Missing directories: {', '.join(missing)}")
                    )
                else:
                    self.checks_passed.append((f"workenv/{subdir.name}", "Structure OK"))
                break

    def xǁWrknvDoctorǁ_check_workenv_structure__mutmut_28(self) -> None:
        """Check if workenv directory exists and has correct structure."""
        workenv_dir = Path.cwd() / "workenv"

        if not workenv_dir.exists():
            self.checks_warning.append(("workenv Directory", "Not found - run 'source env.sh' to create"))
            return

        # Check for platform-specific subdirectories
        subdirs = list(workenv_dir.iterdir())
        if not subdirs:
            self.checks_warning.append(("workenv Structure", "Empty - no environments created"))
            return

        # Check structure of first workenv
        for subdir in subdirs:
            if subdir.is_dir():
                expected_dirs = ["bin", "lib", "XXincludeXX"]
                missing = []
                for expected in expected_dirs:
                    if not (subdir / expected).exists():
                        missing.append(expected)

                if missing:
                    self.checks_warning.append(
                        (f"workenv/{subdir.name}", f"Missing directories: {', '.join(missing)}")
                    )
                else:
                    self.checks_passed.append((f"workenv/{subdir.name}", "Structure OK"))
                break

    def xǁWrknvDoctorǁ_check_workenv_structure__mutmut_29(self) -> None:
        """Check if workenv directory exists and has correct structure."""
        workenv_dir = Path.cwd() / "workenv"

        if not workenv_dir.exists():
            self.checks_warning.append(("workenv Directory", "Not found - run 'source env.sh' to create"))
            return

        # Check for platform-specific subdirectories
        subdirs = list(workenv_dir.iterdir())
        if not subdirs:
            self.checks_warning.append(("workenv Structure", "Empty - no environments created"))
            return

        # Check structure of first workenv
        for subdir in subdirs:
            if subdir.is_dir():
                expected_dirs = ["bin", "lib", "INCLUDE"]
                missing = []
                for expected in expected_dirs:
                    if not (subdir / expected).exists():
                        missing.append(expected)

                if missing:
                    self.checks_warning.append(
                        (f"workenv/{subdir.name}", f"Missing directories: {', '.join(missing)}")
                    )
                else:
                    self.checks_passed.append((f"workenv/{subdir.name}", "Structure OK"))
                break

    def xǁWrknvDoctorǁ_check_workenv_structure__mutmut_30(self) -> None:
        """Check if workenv directory exists and has correct structure."""
        workenv_dir = Path.cwd() / "workenv"

        if not workenv_dir.exists():
            self.checks_warning.append(("workenv Directory", "Not found - run 'source env.sh' to create"))
            return

        # Check for platform-specific subdirectories
        subdirs = list(workenv_dir.iterdir())
        if not subdirs:
            self.checks_warning.append(("workenv Structure", "Empty - no environments created"))
            return

        # Check structure of first workenv
        for subdir in subdirs:
            if subdir.is_dir():
                expected_dirs = ["bin", "lib", "include"]
                missing = None
                for expected in expected_dirs:
                    if not (subdir / expected).exists():
                        missing.append(expected)

                if missing:
                    self.checks_warning.append(
                        (f"workenv/{subdir.name}", f"Missing directories: {', '.join(missing)}")
                    )
                else:
                    self.checks_passed.append((f"workenv/{subdir.name}", "Structure OK"))
                break

    def xǁWrknvDoctorǁ_check_workenv_structure__mutmut_31(self) -> None:
        """Check if workenv directory exists and has correct structure."""
        workenv_dir = Path.cwd() / "workenv"

        if not workenv_dir.exists():
            self.checks_warning.append(("workenv Directory", "Not found - run 'source env.sh' to create"))
            return

        # Check for platform-specific subdirectories
        subdirs = list(workenv_dir.iterdir())
        if not subdirs:
            self.checks_warning.append(("workenv Structure", "Empty - no environments created"))
            return

        # Check structure of first workenv
        for subdir in subdirs:
            if subdir.is_dir():
                expected_dirs = ["bin", "lib", "include"]
                missing = []
                for expected in expected_dirs:
                    if (subdir / expected).exists():
                        missing.append(expected)

                if missing:
                    self.checks_warning.append(
                        (f"workenv/{subdir.name}", f"Missing directories: {', '.join(missing)}")
                    )
                else:
                    self.checks_passed.append((f"workenv/{subdir.name}", "Structure OK"))
                break

    def xǁWrknvDoctorǁ_check_workenv_structure__mutmut_32(self) -> None:
        """Check if workenv directory exists and has correct structure."""
        workenv_dir = Path.cwd() / "workenv"

        if not workenv_dir.exists():
            self.checks_warning.append(("workenv Directory", "Not found - run 'source env.sh' to create"))
            return

        # Check for platform-specific subdirectories
        subdirs = list(workenv_dir.iterdir())
        if not subdirs:
            self.checks_warning.append(("workenv Structure", "Empty - no environments created"))
            return

        # Check structure of first workenv
        for subdir in subdirs:
            if subdir.is_dir():
                expected_dirs = ["bin", "lib", "include"]
                missing = []
                for expected in expected_dirs:
                    if not (subdir * expected).exists():
                        missing.append(expected)

                if missing:
                    self.checks_warning.append(
                        (f"workenv/{subdir.name}", f"Missing directories: {', '.join(missing)}")
                    )
                else:
                    self.checks_passed.append((f"workenv/{subdir.name}", "Structure OK"))
                break

    def xǁWrknvDoctorǁ_check_workenv_structure__mutmut_33(self) -> None:
        """Check if workenv directory exists and has correct structure."""
        workenv_dir = Path.cwd() / "workenv"

        if not workenv_dir.exists():
            self.checks_warning.append(("workenv Directory", "Not found - run 'source env.sh' to create"))
            return

        # Check for platform-specific subdirectories
        subdirs = list(workenv_dir.iterdir())
        if not subdirs:
            self.checks_warning.append(("workenv Structure", "Empty - no environments created"))
            return

        # Check structure of first workenv
        for subdir in subdirs:
            if subdir.is_dir():
                expected_dirs = ["bin", "lib", "include"]
                missing = []
                for expected in expected_dirs:
                    if not (subdir / expected).exists():
                        missing.append(None)

                if missing:
                    self.checks_warning.append(
                        (f"workenv/{subdir.name}", f"Missing directories: {', '.join(missing)}")
                    )
                else:
                    self.checks_passed.append((f"workenv/{subdir.name}", "Structure OK"))
                break

    def xǁWrknvDoctorǁ_check_workenv_structure__mutmut_34(self) -> None:
        """Check if workenv directory exists and has correct structure."""
        workenv_dir = Path.cwd() / "workenv"

        if not workenv_dir.exists():
            self.checks_warning.append(("workenv Directory", "Not found - run 'source env.sh' to create"))
            return

        # Check for platform-specific subdirectories
        subdirs = list(workenv_dir.iterdir())
        if not subdirs:
            self.checks_warning.append(("workenv Structure", "Empty - no environments created"))
            return

        # Check structure of first workenv
        for subdir in subdirs:
            if subdir.is_dir():
                expected_dirs = ["bin", "lib", "include"]
                missing = []
                for expected in expected_dirs:
                    if not (subdir / expected).exists():
                        missing.append(expected)

                if missing:
                    self.checks_warning.append(
                        None
                    )
                else:
                    self.checks_passed.append((f"workenv/{subdir.name}", "Structure OK"))
                break

    def xǁWrknvDoctorǁ_check_workenv_structure__mutmut_35(self) -> None:
        """Check if workenv directory exists and has correct structure."""
        workenv_dir = Path.cwd() / "workenv"

        if not workenv_dir.exists():
            self.checks_warning.append(("workenv Directory", "Not found - run 'source env.sh' to create"))
            return

        # Check for platform-specific subdirectories
        subdirs = list(workenv_dir.iterdir())
        if not subdirs:
            self.checks_warning.append(("workenv Structure", "Empty - no environments created"))
            return

        # Check structure of first workenv
        for subdir in subdirs:
            if subdir.is_dir():
                expected_dirs = ["bin", "lib", "include"]
                missing = []
                for expected in expected_dirs:
                    if not (subdir / expected).exists():
                        missing.append(expected)

                if missing:
                    self.checks_warning.append(
                        (f"workenv/{subdir.name}", f"Missing directories: {', '.join(None)}")
                    )
                else:
                    self.checks_passed.append((f"workenv/{subdir.name}", "Structure OK"))
                break

    def xǁWrknvDoctorǁ_check_workenv_structure__mutmut_36(self) -> None:
        """Check if workenv directory exists and has correct structure."""
        workenv_dir = Path.cwd() / "workenv"

        if not workenv_dir.exists():
            self.checks_warning.append(("workenv Directory", "Not found - run 'source env.sh' to create"))
            return

        # Check for platform-specific subdirectories
        subdirs = list(workenv_dir.iterdir())
        if not subdirs:
            self.checks_warning.append(("workenv Structure", "Empty - no environments created"))
            return

        # Check structure of first workenv
        for subdir in subdirs:
            if subdir.is_dir():
                expected_dirs = ["bin", "lib", "include"]
                missing = []
                for expected in expected_dirs:
                    if not (subdir / expected).exists():
                        missing.append(expected)

                if missing:
                    self.checks_warning.append(
                        (f"workenv/{subdir.name}", f"Missing directories: {'XX, XX'.join(missing)}")
                    )
                else:
                    self.checks_passed.append((f"workenv/{subdir.name}", "Structure OK"))
                break

    def xǁWrknvDoctorǁ_check_workenv_structure__mutmut_37(self) -> None:
        """Check if workenv directory exists and has correct structure."""
        workenv_dir = Path.cwd() / "workenv"

        if not workenv_dir.exists():
            self.checks_warning.append(("workenv Directory", "Not found - run 'source env.sh' to create"))
            return

        # Check for platform-specific subdirectories
        subdirs = list(workenv_dir.iterdir())
        if not subdirs:
            self.checks_warning.append(("workenv Structure", "Empty - no environments created"))
            return

        # Check structure of first workenv
        for subdir in subdirs:
            if subdir.is_dir():
                expected_dirs = ["bin", "lib", "include"]
                missing = []
                for expected in expected_dirs:
                    if not (subdir / expected).exists():
                        missing.append(expected)

                if missing:
                    self.checks_warning.append(
                        (f"workenv/{subdir.name}", f"Missing directories: {', '.join(missing)}")
                    )
                else:
                    self.checks_passed.append(None)
                break

    def xǁWrknvDoctorǁ_check_workenv_structure__mutmut_38(self) -> None:
        """Check if workenv directory exists and has correct structure."""
        workenv_dir = Path.cwd() / "workenv"

        if not workenv_dir.exists():
            self.checks_warning.append(("workenv Directory", "Not found - run 'source env.sh' to create"))
            return

        # Check for platform-specific subdirectories
        subdirs = list(workenv_dir.iterdir())
        if not subdirs:
            self.checks_warning.append(("workenv Structure", "Empty - no environments created"))
            return

        # Check structure of first workenv
        for subdir in subdirs:
            if subdir.is_dir():
                expected_dirs = ["bin", "lib", "include"]
                missing = []
                for expected in expected_dirs:
                    if not (subdir / expected).exists():
                        missing.append(expected)

                if missing:
                    self.checks_warning.append(
                        (f"workenv/{subdir.name}", f"Missing directories: {', '.join(missing)}")
                    )
                else:
                    self.checks_passed.append((f"workenv/{subdir.name}", "XXStructure OKXX"))
                break

    def xǁWrknvDoctorǁ_check_workenv_structure__mutmut_39(self) -> None:
        """Check if workenv directory exists and has correct structure."""
        workenv_dir = Path.cwd() / "workenv"

        if not workenv_dir.exists():
            self.checks_warning.append(("workenv Directory", "Not found - run 'source env.sh' to create"))
            return

        # Check for platform-specific subdirectories
        subdirs = list(workenv_dir.iterdir())
        if not subdirs:
            self.checks_warning.append(("workenv Structure", "Empty - no environments created"))
            return

        # Check structure of first workenv
        for subdir in subdirs:
            if subdir.is_dir():
                expected_dirs = ["bin", "lib", "include"]
                missing = []
                for expected in expected_dirs:
                    if not (subdir / expected).exists():
                        missing.append(expected)

                if missing:
                    self.checks_warning.append(
                        (f"workenv/{subdir.name}", f"Missing directories: {', '.join(missing)}")
                    )
                else:
                    self.checks_passed.append((f"workenv/{subdir.name}", "structure ok"))
                break

    def xǁWrknvDoctorǁ_check_workenv_structure__mutmut_40(self) -> None:
        """Check if workenv directory exists and has correct structure."""
        workenv_dir = Path.cwd() / "workenv"

        if not workenv_dir.exists():
            self.checks_warning.append(("workenv Directory", "Not found - run 'source env.sh' to create"))
            return

        # Check for platform-specific subdirectories
        subdirs = list(workenv_dir.iterdir())
        if not subdirs:
            self.checks_warning.append(("workenv Structure", "Empty - no environments created"))
            return

        # Check structure of first workenv
        for subdir in subdirs:
            if subdir.is_dir():
                expected_dirs = ["bin", "lib", "include"]
                missing = []
                for expected in expected_dirs:
                    if not (subdir / expected).exists():
                        missing.append(expected)

                if missing:
                    self.checks_warning.append(
                        (f"workenv/{subdir.name}", f"Missing directories: {', '.join(missing)}")
                    )
                else:
                    self.checks_passed.append((f"workenv/{subdir.name}", "STRUCTURE OK"))
                break

    def xǁWrknvDoctorǁ_check_workenv_structure__mutmut_41(self) -> None:
        """Check if workenv directory exists and has correct structure."""
        workenv_dir = Path.cwd() / "workenv"

        if not workenv_dir.exists():
            self.checks_warning.append(("workenv Directory", "Not found - run 'source env.sh' to create"))
            return

        # Check for platform-specific subdirectories
        subdirs = list(workenv_dir.iterdir())
        if not subdirs:
            self.checks_warning.append(("workenv Structure", "Empty - no environments created"))
            return

        # Check structure of first workenv
        for subdir in subdirs:
            if subdir.is_dir():
                expected_dirs = ["bin", "lib", "include"]
                missing = []
                for expected in expected_dirs:
                    if not (subdir / expected).exists():
                        missing.append(expected)

                if missing:
                    self.checks_warning.append(
                        (f"workenv/{subdir.name}", f"Missing directories: {', '.join(missing)}")
                    )
                else:
                    self.checks_passed.append((f"workenv/{subdir.name}", "Structure OK"))
                return
    
    xǁWrknvDoctorǁ_check_workenv_structure__mutmut_mutants : ClassVar[MutantDict] = {
    'xǁWrknvDoctorǁ_check_workenv_structure__mutmut_1': xǁWrknvDoctorǁ_check_workenv_structure__mutmut_1, 
        'xǁWrknvDoctorǁ_check_workenv_structure__mutmut_2': xǁWrknvDoctorǁ_check_workenv_structure__mutmut_2, 
        'xǁWrknvDoctorǁ_check_workenv_structure__mutmut_3': xǁWrknvDoctorǁ_check_workenv_structure__mutmut_3, 
        'xǁWrknvDoctorǁ_check_workenv_structure__mutmut_4': xǁWrknvDoctorǁ_check_workenv_structure__mutmut_4, 
        'xǁWrknvDoctorǁ_check_workenv_structure__mutmut_5': xǁWrknvDoctorǁ_check_workenv_structure__mutmut_5, 
        'xǁWrknvDoctorǁ_check_workenv_structure__mutmut_6': xǁWrknvDoctorǁ_check_workenv_structure__mutmut_6, 
        'xǁWrknvDoctorǁ_check_workenv_structure__mutmut_7': xǁWrknvDoctorǁ_check_workenv_structure__mutmut_7, 
        'xǁWrknvDoctorǁ_check_workenv_structure__mutmut_8': xǁWrknvDoctorǁ_check_workenv_structure__mutmut_8, 
        'xǁWrknvDoctorǁ_check_workenv_structure__mutmut_9': xǁWrknvDoctorǁ_check_workenv_structure__mutmut_9, 
        'xǁWrknvDoctorǁ_check_workenv_structure__mutmut_10': xǁWrknvDoctorǁ_check_workenv_structure__mutmut_10, 
        'xǁWrknvDoctorǁ_check_workenv_structure__mutmut_11': xǁWrknvDoctorǁ_check_workenv_structure__mutmut_11, 
        'xǁWrknvDoctorǁ_check_workenv_structure__mutmut_12': xǁWrknvDoctorǁ_check_workenv_structure__mutmut_12, 
        'xǁWrknvDoctorǁ_check_workenv_structure__mutmut_13': xǁWrknvDoctorǁ_check_workenv_structure__mutmut_13, 
        'xǁWrknvDoctorǁ_check_workenv_structure__mutmut_14': xǁWrknvDoctorǁ_check_workenv_structure__mutmut_14, 
        'xǁWrknvDoctorǁ_check_workenv_structure__mutmut_15': xǁWrknvDoctorǁ_check_workenv_structure__mutmut_15, 
        'xǁWrknvDoctorǁ_check_workenv_structure__mutmut_16': xǁWrknvDoctorǁ_check_workenv_structure__mutmut_16, 
        'xǁWrknvDoctorǁ_check_workenv_structure__mutmut_17': xǁWrknvDoctorǁ_check_workenv_structure__mutmut_17, 
        'xǁWrknvDoctorǁ_check_workenv_structure__mutmut_18': xǁWrknvDoctorǁ_check_workenv_structure__mutmut_18, 
        'xǁWrknvDoctorǁ_check_workenv_structure__mutmut_19': xǁWrknvDoctorǁ_check_workenv_structure__mutmut_19, 
        'xǁWrknvDoctorǁ_check_workenv_structure__mutmut_20': xǁWrknvDoctorǁ_check_workenv_structure__mutmut_20, 
        'xǁWrknvDoctorǁ_check_workenv_structure__mutmut_21': xǁWrknvDoctorǁ_check_workenv_structure__mutmut_21, 
        'xǁWrknvDoctorǁ_check_workenv_structure__mutmut_22': xǁWrknvDoctorǁ_check_workenv_structure__mutmut_22, 
        'xǁWrknvDoctorǁ_check_workenv_structure__mutmut_23': xǁWrknvDoctorǁ_check_workenv_structure__mutmut_23, 
        'xǁWrknvDoctorǁ_check_workenv_structure__mutmut_24': xǁWrknvDoctorǁ_check_workenv_structure__mutmut_24, 
        'xǁWrknvDoctorǁ_check_workenv_structure__mutmut_25': xǁWrknvDoctorǁ_check_workenv_structure__mutmut_25, 
        'xǁWrknvDoctorǁ_check_workenv_structure__mutmut_26': xǁWrknvDoctorǁ_check_workenv_structure__mutmut_26, 
        'xǁWrknvDoctorǁ_check_workenv_structure__mutmut_27': xǁWrknvDoctorǁ_check_workenv_structure__mutmut_27, 
        'xǁWrknvDoctorǁ_check_workenv_structure__mutmut_28': xǁWrknvDoctorǁ_check_workenv_structure__mutmut_28, 
        'xǁWrknvDoctorǁ_check_workenv_structure__mutmut_29': xǁWrknvDoctorǁ_check_workenv_structure__mutmut_29, 
        'xǁWrknvDoctorǁ_check_workenv_structure__mutmut_30': xǁWrknvDoctorǁ_check_workenv_structure__mutmut_30, 
        'xǁWrknvDoctorǁ_check_workenv_structure__mutmut_31': xǁWrknvDoctorǁ_check_workenv_structure__mutmut_31, 
        'xǁWrknvDoctorǁ_check_workenv_structure__mutmut_32': xǁWrknvDoctorǁ_check_workenv_structure__mutmut_32, 
        'xǁWrknvDoctorǁ_check_workenv_structure__mutmut_33': xǁWrknvDoctorǁ_check_workenv_structure__mutmut_33, 
        'xǁWrknvDoctorǁ_check_workenv_structure__mutmut_34': xǁWrknvDoctorǁ_check_workenv_structure__mutmut_34, 
        'xǁWrknvDoctorǁ_check_workenv_structure__mutmut_35': xǁWrknvDoctorǁ_check_workenv_structure__mutmut_35, 
        'xǁWrknvDoctorǁ_check_workenv_structure__mutmut_36': xǁWrknvDoctorǁ_check_workenv_structure__mutmut_36, 
        'xǁWrknvDoctorǁ_check_workenv_structure__mutmut_37': xǁWrknvDoctorǁ_check_workenv_structure__mutmut_37, 
        'xǁWrknvDoctorǁ_check_workenv_structure__mutmut_38': xǁWrknvDoctorǁ_check_workenv_structure__mutmut_38, 
        'xǁWrknvDoctorǁ_check_workenv_structure__mutmut_39': xǁWrknvDoctorǁ_check_workenv_structure__mutmut_39, 
        'xǁWrknvDoctorǁ_check_workenv_structure__mutmut_40': xǁWrknvDoctorǁ_check_workenv_structure__mutmut_40, 
        'xǁWrknvDoctorǁ_check_workenv_structure__mutmut_41': xǁWrknvDoctorǁ_check_workenv_structure__mutmut_41
    }
    
    def _check_workenv_structure(self, *args, **kwargs):
        result = _mutmut_trampoline(object.__getattribute__(self, "xǁWrknvDoctorǁ_check_workenv_structure__mutmut_orig"), object.__getattribute__(self, "xǁWrknvDoctorǁ_check_workenv_structure__mutmut_mutants"), args, kwargs, self)
        return result 
    
    _check_workenv_structure.__signature__ = _mutmut_signature(xǁWrknvDoctorǁ_check_workenv_structure__mutmut_orig)
    xǁWrknvDoctorǁ_check_workenv_structure__mutmut_orig.__name__ = 'xǁWrknvDoctorǁ_check_workenv_structure'

    def xǁWrknvDoctorǁ_check_env_script__mutmut_orig(self) -> None:
        """Check if env.sh exists and is valid."""
        env_script = Path.cwd() / "env.sh"

        if not env_script.exists():
            self.checks_failed.append(("env.sh", "Not found - run 'wrknv generate'"))
            return

        # Check if env.sh was generated by wrknv
        content = env_script.read_text()
        is_wrknv_generated = "Generated by wrknv" in content or "WORKENV_DIR=" in content

        if not is_wrknv_generated:
            self.checks_failed.append(
                ("env.sh", "Not generated by wrknv - run 'wrknv generate' to regenerate")
            )
            return

        # Check if it's executable
        if not os.access(env_script, os.X_OK):
            self.checks_warning.append(("env.sh", "Not executable - run 'chmod +x env.sh'"))

        # Check for correct workenv pattern
        system = platform.system().lower()
        machine = platform.machine().lower()
        if machine in ["x86_64", "amd64"]:
            arch = "amd64"
        elif machine in ["arm64", "aarch64"]:
            arch = "arm64"
        else:
            arch = machine

        package_name = Path.cwd().name
        expected_workenv = f"workenv/{package_name}_{system}_{arch}"

        if expected_workenv not in content:
            self.checks_failed.append(
                (
                    "env.sh Workenv Path",
                    f"Incorrect pattern - expected '{expected_workenv}'. Regenerate with 'wrknv generate'",
                )
            )
            return

        # Check required patterns for proper functioning
        required_patterns = [
            "WORKENV_DIR=",
            "UV_INSTALLER_URL=",
            "uv venv",
            "uv sync",
        ]

        missing = []
        for pattern in required_patterns:
            if pattern not in content:
                missing.append(pattern)

        if missing:
            self.checks_failed.append(
                (
                    "env.sh Content",
                    f"Missing required patterns: {', '.join(missing)}. Regenerate with 'wrknv generate'",
                )
            )
        else:
            # Test if env.sh actually works
            self._test_env_script_execution(env_script)

    def xǁWrknvDoctorǁ_check_env_script__mutmut_1(self) -> None:
        """Check if env.sh exists and is valid."""
        env_script = None

        if not env_script.exists():
            self.checks_failed.append(("env.sh", "Not found - run 'wrknv generate'"))
            return

        # Check if env.sh was generated by wrknv
        content = env_script.read_text()
        is_wrknv_generated = "Generated by wrknv" in content or "WORKENV_DIR=" in content

        if not is_wrknv_generated:
            self.checks_failed.append(
                ("env.sh", "Not generated by wrknv - run 'wrknv generate' to regenerate")
            )
            return

        # Check if it's executable
        if not os.access(env_script, os.X_OK):
            self.checks_warning.append(("env.sh", "Not executable - run 'chmod +x env.sh'"))

        # Check for correct workenv pattern
        system = platform.system().lower()
        machine = platform.machine().lower()
        if machine in ["x86_64", "amd64"]:
            arch = "amd64"
        elif machine in ["arm64", "aarch64"]:
            arch = "arm64"
        else:
            arch = machine

        package_name = Path.cwd().name
        expected_workenv = f"workenv/{package_name}_{system}_{arch}"

        if expected_workenv not in content:
            self.checks_failed.append(
                (
                    "env.sh Workenv Path",
                    f"Incorrect pattern - expected '{expected_workenv}'. Regenerate with 'wrknv generate'",
                )
            )
            return

        # Check required patterns for proper functioning
        required_patterns = [
            "WORKENV_DIR=",
            "UV_INSTALLER_URL=",
            "uv venv",
            "uv sync",
        ]

        missing = []
        for pattern in required_patterns:
            if pattern not in content:
                missing.append(pattern)

        if missing:
            self.checks_failed.append(
                (
                    "env.sh Content",
                    f"Missing required patterns: {', '.join(missing)}. Regenerate with 'wrknv generate'",
                )
            )
        else:
            # Test if env.sh actually works
            self._test_env_script_execution(env_script)

    def xǁWrknvDoctorǁ_check_env_script__mutmut_2(self) -> None:
        """Check if env.sh exists and is valid."""
        env_script = Path.cwd() * "env.sh"

        if not env_script.exists():
            self.checks_failed.append(("env.sh", "Not found - run 'wrknv generate'"))
            return

        # Check if env.sh was generated by wrknv
        content = env_script.read_text()
        is_wrknv_generated = "Generated by wrknv" in content or "WORKENV_DIR=" in content

        if not is_wrknv_generated:
            self.checks_failed.append(
                ("env.sh", "Not generated by wrknv - run 'wrknv generate' to regenerate")
            )
            return

        # Check if it's executable
        if not os.access(env_script, os.X_OK):
            self.checks_warning.append(("env.sh", "Not executable - run 'chmod +x env.sh'"))

        # Check for correct workenv pattern
        system = platform.system().lower()
        machine = platform.machine().lower()
        if machine in ["x86_64", "amd64"]:
            arch = "amd64"
        elif machine in ["arm64", "aarch64"]:
            arch = "arm64"
        else:
            arch = machine

        package_name = Path.cwd().name
        expected_workenv = f"workenv/{package_name}_{system}_{arch}"

        if expected_workenv not in content:
            self.checks_failed.append(
                (
                    "env.sh Workenv Path",
                    f"Incorrect pattern - expected '{expected_workenv}'. Regenerate with 'wrknv generate'",
                )
            )
            return

        # Check required patterns for proper functioning
        required_patterns = [
            "WORKENV_DIR=",
            "UV_INSTALLER_URL=",
            "uv venv",
            "uv sync",
        ]

        missing = []
        for pattern in required_patterns:
            if pattern not in content:
                missing.append(pattern)

        if missing:
            self.checks_failed.append(
                (
                    "env.sh Content",
                    f"Missing required patterns: {', '.join(missing)}. Regenerate with 'wrknv generate'",
                )
            )
        else:
            # Test if env.sh actually works
            self._test_env_script_execution(env_script)

    def xǁWrknvDoctorǁ_check_env_script__mutmut_3(self) -> None:
        """Check if env.sh exists and is valid."""
        env_script = Path.cwd() / "XXenv.shXX"

        if not env_script.exists():
            self.checks_failed.append(("env.sh", "Not found - run 'wrknv generate'"))
            return

        # Check if env.sh was generated by wrknv
        content = env_script.read_text()
        is_wrknv_generated = "Generated by wrknv" in content or "WORKENV_DIR=" in content

        if not is_wrknv_generated:
            self.checks_failed.append(
                ("env.sh", "Not generated by wrknv - run 'wrknv generate' to regenerate")
            )
            return

        # Check if it's executable
        if not os.access(env_script, os.X_OK):
            self.checks_warning.append(("env.sh", "Not executable - run 'chmod +x env.sh'"))

        # Check for correct workenv pattern
        system = platform.system().lower()
        machine = platform.machine().lower()
        if machine in ["x86_64", "amd64"]:
            arch = "amd64"
        elif machine in ["arm64", "aarch64"]:
            arch = "arm64"
        else:
            arch = machine

        package_name = Path.cwd().name
        expected_workenv = f"workenv/{package_name}_{system}_{arch}"

        if expected_workenv not in content:
            self.checks_failed.append(
                (
                    "env.sh Workenv Path",
                    f"Incorrect pattern - expected '{expected_workenv}'. Regenerate with 'wrknv generate'",
                )
            )
            return

        # Check required patterns for proper functioning
        required_patterns = [
            "WORKENV_DIR=",
            "UV_INSTALLER_URL=",
            "uv venv",
            "uv sync",
        ]

        missing = []
        for pattern in required_patterns:
            if pattern not in content:
                missing.append(pattern)

        if missing:
            self.checks_failed.append(
                (
                    "env.sh Content",
                    f"Missing required patterns: {', '.join(missing)}. Regenerate with 'wrknv generate'",
                )
            )
        else:
            # Test if env.sh actually works
            self._test_env_script_execution(env_script)

    def xǁWrknvDoctorǁ_check_env_script__mutmut_4(self) -> None:
        """Check if env.sh exists and is valid."""
        env_script = Path.cwd() / "ENV.SH"

        if not env_script.exists():
            self.checks_failed.append(("env.sh", "Not found - run 'wrknv generate'"))
            return

        # Check if env.sh was generated by wrknv
        content = env_script.read_text()
        is_wrknv_generated = "Generated by wrknv" in content or "WORKENV_DIR=" in content

        if not is_wrknv_generated:
            self.checks_failed.append(
                ("env.sh", "Not generated by wrknv - run 'wrknv generate' to regenerate")
            )
            return

        # Check if it's executable
        if not os.access(env_script, os.X_OK):
            self.checks_warning.append(("env.sh", "Not executable - run 'chmod +x env.sh'"))

        # Check for correct workenv pattern
        system = platform.system().lower()
        machine = platform.machine().lower()
        if machine in ["x86_64", "amd64"]:
            arch = "amd64"
        elif machine in ["arm64", "aarch64"]:
            arch = "arm64"
        else:
            arch = machine

        package_name = Path.cwd().name
        expected_workenv = f"workenv/{package_name}_{system}_{arch}"

        if expected_workenv not in content:
            self.checks_failed.append(
                (
                    "env.sh Workenv Path",
                    f"Incorrect pattern - expected '{expected_workenv}'. Regenerate with 'wrknv generate'",
                )
            )
            return

        # Check required patterns for proper functioning
        required_patterns = [
            "WORKENV_DIR=",
            "UV_INSTALLER_URL=",
            "uv venv",
            "uv sync",
        ]

        missing = []
        for pattern in required_patterns:
            if pattern not in content:
                missing.append(pattern)

        if missing:
            self.checks_failed.append(
                (
                    "env.sh Content",
                    f"Missing required patterns: {', '.join(missing)}. Regenerate with 'wrknv generate'",
                )
            )
        else:
            # Test if env.sh actually works
            self._test_env_script_execution(env_script)

    def xǁWrknvDoctorǁ_check_env_script__mutmut_5(self) -> None:
        """Check if env.sh exists and is valid."""
        env_script = Path.cwd() / "env.sh"

        if env_script.exists():
            self.checks_failed.append(("env.sh", "Not found - run 'wrknv generate'"))
            return

        # Check if env.sh was generated by wrknv
        content = env_script.read_text()
        is_wrknv_generated = "Generated by wrknv" in content or "WORKENV_DIR=" in content

        if not is_wrknv_generated:
            self.checks_failed.append(
                ("env.sh", "Not generated by wrknv - run 'wrknv generate' to regenerate")
            )
            return

        # Check if it's executable
        if not os.access(env_script, os.X_OK):
            self.checks_warning.append(("env.sh", "Not executable - run 'chmod +x env.sh'"))

        # Check for correct workenv pattern
        system = platform.system().lower()
        machine = platform.machine().lower()
        if machine in ["x86_64", "amd64"]:
            arch = "amd64"
        elif machine in ["arm64", "aarch64"]:
            arch = "arm64"
        else:
            arch = machine

        package_name = Path.cwd().name
        expected_workenv = f"workenv/{package_name}_{system}_{arch}"

        if expected_workenv not in content:
            self.checks_failed.append(
                (
                    "env.sh Workenv Path",
                    f"Incorrect pattern - expected '{expected_workenv}'. Regenerate with 'wrknv generate'",
                )
            )
            return

        # Check required patterns for proper functioning
        required_patterns = [
            "WORKENV_DIR=",
            "UV_INSTALLER_URL=",
            "uv venv",
            "uv sync",
        ]

        missing = []
        for pattern in required_patterns:
            if pattern not in content:
                missing.append(pattern)

        if missing:
            self.checks_failed.append(
                (
                    "env.sh Content",
                    f"Missing required patterns: {', '.join(missing)}. Regenerate with 'wrknv generate'",
                )
            )
        else:
            # Test if env.sh actually works
            self._test_env_script_execution(env_script)

    def xǁWrknvDoctorǁ_check_env_script__mutmut_6(self) -> None:
        """Check if env.sh exists and is valid."""
        env_script = Path.cwd() / "env.sh"

        if not env_script.exists():
            self.checks_failed.append(None)
            return

        # Check if env.sh was generated by wrknv
        content = env_script.read_text()
        is_wrknv_generated = "Generated by wrknv" in content or "WORKENV_DIR=" in content

        if not is_wrknv_generated:
            self.checks_failed.append(
                ("env.sh", "Not generated by wrknv - run 'wrknv generate' to regenerate")
            )
            return

        # Check if it's executable
        if not os.access(env_script, os.X_OK):
            self.checks_warning.append(("env.sh", "Not executable - run 'chmod +x env.sh'"))

        # Check for correct workenv pattern
        system = platform.system().lower()
        machine = platform.machine().lower()
        if machine in ["x86_64", "amd64"]:
            arch = "amd64"
        elif machine in ["arm64", "aarch64"]:
            arch = "arm64"
        else:
            arch = machine

        package_name = Path.cwd().name
        expected_workenv = f"workenv/{package_name}_{system}_{arch}"

        if expected_workenv not in content:
            self.checks_failed.append(
                (
                    "env.sh Workenv Path",
                    f"Incorrect pattern - expected '{expected_workenv}'. Regenerate with 'wrknv generate'",
                )
            )
            return

        # Check required patterns for proper functioning
        required_patterns = [
            "WORKENV_DIR=",
            "UV_INSTALLER_URL=",
            "uv venv",
            "uv sync",
        ]

        missing = []
        for pattern in required_patterns:
            if pattern not in content:
                missing.append(pattern)

        if missing:
            self.checks_failed.append(
                (
                    "env.sh Content",
                    f"Missing required patterns: {', '.join(missing)}. Regenerate with 'wrknv generate'",
                )
            )
        else:
            # Test if env.sh actually works
            self._test_env_script_execution(env_script)

    def xǁWrknvDoctorǁ_check_env_script__mutmut_7(self) -> None:
        """Check if env.sh exists and is valid."""
        env_script = Path.cwd() / "env.sh"

        if not env_script.exists():
            self.checks_failed.append(("XXenv.shXX", "Not found - run 'wrknv generate'"))
            return

        # Check if env.sh was generated by wrknv
        content = env_script.read_text()
        is_wrknv_generated = "Generated by wrknv" in content or "WORKENV_DIR=" in content

        if not is_wrknv_generated:
            self.checks_failed.append(
                ("env.sh", "Not generated by wrknv - run 'wrknv generate' to regenerate")
            )
            return

        # Check if it's executable
        if not os.access(env_script, os.X_OK):
            self.checks_warning.append(("env.sh", "Not executable - run 'chmod +x env.sh'"))

        # Check for correct workenv pattern
        system = platform.system().lower()
        machine = platform.machine().lower()
        if machine in ["x86_64", "amd64"]:
            arch = "amd64"
        elif machine in ["arm64", "aarch64"]:
            arch = "arm64"
        else:
            arch = machine

        package_name = Path.cwd().name
        expected_workenv = f"workenv/{package_name}_{system}_{arch}"

        if expected_workenv not in content:
            self.checks_failed.append(
                (
                    "env.sh Workenv Path",
                    f"Incorrect pattern - expected '{expected_workenv}'. Regenerate with 'wrknv generate'",
                )
            )
            return

        # Check required patterns for proper functioning
        required_patterns = [
            "WORKENV_DIR=",
            "UV_INSTALLER_URL=",
            "uv venv",
            "uv sync",
        ]

        missing = []
        for pattern in required_patterns:
            if pattern not in content:
                missing.append(pattern)

        if missing:
            self.checks_failed.append(
                (
                    "env.sh Content",
                    f"Missing required patterns: {', '.join(missing)}. Regenerate with 'wrknv generate'",
                )
            )
        else:
            # Test if env.sh actually works
            self._test_env_script_execution(env_script)

    def xǁWrknvDoctorǁ_check_env_script__mutmut_8(self) -> None:
        """Check if env.sh exists and is valid."""
        env_script = Path.cwd() / "env.sh"

        if not env_script.exists():
            self.checks_failed.append(("ENV.SH", "Not found - run 'wrknv generate'"))
            return

        # Check if env.sh was generated by wrknv
        content = env_script.read_text()
        is_wrknv_generated = "Generated by wrknv" in content or "WORKENV_DIR=" in content

        if not is_wrknv_generated:
            self.checks_failed.append(
                ("env.sh", "Not generated by wrknv - run 'wrknv generate' to regenerate")
            )
            return

        # Check if it's executable
        if not os.access(env_script, os.X_OK):
            self.checks_warning.append(("env.sh", "Not executable - run 'chmod +x env.sh'"))

        # Check for correct workenv pattern
        system = platform.system().lower()
        machine = platform.machine().lower()
        if machine in ["x86_64", "amd64"]:
            arch = "amd64"
        elif machine in ["arm64", "aarch64"]:
            arch = "arm64"
        else:
            arch = machine

        package_name = Path.cwd().name
        expected_workenv = f"workenv/{package_name}_{system}_{arch}"

        if expected_workenv not in content:
            self.checks_failed.append(
                (
                    "env.sh Workenv Path",
                    f"Incorrect pattern - expected '{expected_workenv}'. Regenerate with 'wrknv generate'",
                )
            )
            return

        # Check required patterns for proper functioning
        required_patterns = [
            "WORKENV_DIR=",
            "UV_INSTALLER_URL=",
            "uv venv",
            "uv sync",
        ]

        missing = []
        for pattern in required_patterns:
            if pattern not in content:
                missing.append(pattern)

        if missing:
            self.checks_failed.append(
                (
                    "env.sh Content",
                    f"Missing required patterns: {', '.join(missing)}. Regenerate with 'wrknv generate'",
                )
            )
        else:
            # Test if env.sh actually works
            self._test_env_script_execution(env_script)

    def xǁWrknvDoctorǁ_check_env_script__mutmut_9(self) -> None:
        """Check if env.sh exists and is valid."""
        env_script = Path.cwd() / "env.sh"

        if not env_script.exists():
            self.checks_failed.append(("env.sh", "XXNot found - run 'wrknv generate'XX"))
            return

        # Check if env.sh was generated by wrknv
        content = env_script.read_text()
        is_wrknv_generated = "Generated by wrknv" in content or "WORKENV_DIR=" in content

        if not is_wrknv_generated:
            self.checks_failed.append(
                ("env.sh", "Not generated by wrknv - run 'wrknv generate' to regenerate")
            )
            return

        # Check if it's executable
        if not os.access(env_script, os.X_OK):
            self.checks_warning.append(("env.sh", "Not executable - run 'chmod +x env.sh'"))

        # Check for correct workenv pattern
        system = platform.system().lower()
        machine = platform.machine().lower()
        if machine in ["x86_64", "amd64"]:
            arch = "amd64"
        elif machine in ["arm64", "aarch64"]:
            arch = "arm64"
        else:
            arch = machine

        package_name = Path.cwd().name
        expected_workenv = f"workenv/{package_name}_{system}_{arch}"

        if expected_workenv not in content:
            self.checks_failed.append(
                (
                    "env.sh Workenv Path",
                    f"Incorrect pattern - expected '{expected_workenv}'. Regenerate with 'wrknv generate'",
                )
            )
            return

        # Check required patterns for proper functioning
        required_patterns = [
            "WORKENV_DIR=",
            "UV_INSTALLER_URL=",
            "uv venv",
            "uv sync",
        ]

        missing = []
        for pattern in required_patterns:
            if pattern not in content:
                missing.append(pattern)

        if missing:
            self.checks_failed.append(
                (
                    "env.sh Content",
                    f"Missing required patterns: {', '.join(missing)}. Regenerate with 'wrknv generate'",
                )
            )
        else:
            # Test if env.sh actually works
            self._test_env_script_execution(env_script)

    def xǁWrknvDoctorǁ_check_env_script__mutmut_10(self) -> None:
        """Check if env.sh exists and is valid."""
        env_script = Path.cwd() / "env.sh"

        if not env_script.exists():
            self.checks_failed.append(("env.sh", "not found - run 'wrknv generate'"))
            return

        # Check if env.sh was generated by wrknv
        content = env_script.read_text()
        is_wrknv_generated = "Generated by wrknv" in content or "WORKENV_DIR=" in content

        if not is_wrknv_generated:
            self.checks_failed.append(
                ("env.sh", "Not generated by wrknv - run 'wrknv generate' to regenerate")
            )
            return

        # Check if it's executable
        if not os.access(env_script, os.X_OK):
            self.checks_warning.append(("env.sh", "Not executable - run 'chmod +x env.sh'"))

        # Check for correct workenv pattern
        system = platform.system().lower()
        machine = platform.machine().lower()
        if machine in ["x86_64", "amd64"]:
            arch = "amd64"
        elif machine in ["arm64", "aarch64"]:
            arch = "arm64"
        else:
            arch = machine

        package_name = Path.cwd().name
        expected_workenv = f"workenv/{package_name}_{system}_{arch}"

        if expected_workenv not in content:
            self.checks_failed.append(
                (
                    "env.sh Workenv Path",
                    f"Incorrect pattern - expected '{expected_workenv}'. Regenerate with 'wrknv generate'",
                )
            )
            return

        # Check required patterns for proper functioning
        required_patterns = [
            "WORKENV_DIR=",
            "UV_INSTALLER_URL=",
            "uv venv",
            "uv sync",
        ]

        missing = []
        for pattern in required_patterns:
            if pattern not in content:
                missing.append(pattern)

        if missing:
            self.checks_failed.append(
                (
                    "env.sh Content",
                    f"Missing required patterns: {', '.join(missing)}. Regenerate with 'wrknv generate'",
                )
            )
        else:
            # Test if env.sh actually works
            self._test_env_script_execution(env_script)

    def xǁWrknvDoctorǁ_check_env_script__mutmut_11(self) -> None:
        """Check if env.sh exists and is valid."""
        env_script = Path.cwd() / "env.sh"

        if not env_script.exists():
            self.checks_failed.append(("env.sh", "NOT FOUND - RUN 'WRKNV GENERATE'"))
            return

        # Check if env.sh was generated by wrknv
        content = env_script.read_text()
        is_wrknv_generated = "Generated by wrknv" in content or "WORKENV_DIR=" in content

        if not is_wrknv_generated:
            self.checks_failed.append(
                ("env.sh", "Not generated by wrknv - run 'wrknv generate' to regenerate")
            )
            return

        # Check if it's executable
        if not os.access(env_script, os.X_OK):
            self.checks_warning.append(("env.sh", "Not executable - run 'chmod +x env.sh'"))

        # Check for correct workenv pattern
        system = platform.system().lower()
        machine = platform.machine().lower()
        if machine in ["x86_64", "amd64"]:
            arch = "amd64"
        elif machine in ["arm64", "aarch64"]:
            arch = "arm64"
        else:
            arch = machine

        package_name = Path.cwd().name
        expected_workenv = f"workenv/{package_name}_{system}_{arch}"

        if expected_workenv not in content:
            self.checks_failed.append(
                (
                    "env.sh Workenv Path",
                    f"Incorrect pattern - expected '{expected_workenv}'. Regenerate with 'wrknv generate'",
                )
            )
            return

        # Check required patterns for proper functioning
        required_patterns = [
            "WORKENV_DIR=",
            "UV_INSTALLER_URL=",
            "uv venv",
            "uv sync",
        ]

        missing = []
        for pattern in required_patterns:
            if pattern not in content:
                missing.append(pattern)

        if missing:
            self.checks_failed.append(
                (
                    "env.sh Content",
                    f"Missing required patterns: {', '.join(missing)}. Regenerate with 'wrknv generate'",
                )
            )
        else:
            # Test if env.sh actually works
            self._test_env_script_execution(env_script)

    def xǁWrknvDoctorǁ_check_env_script__mutmut_12(self) -> None:
        """Check if env.sh exists and is valid."""
        env_script = Path.cwd() / "env.sh"

        if not env_script.exists():
            self.checks_failed.append(("env.sh", "Not found - run 'wrknv generate'"))
            return

        # Check if env.sh was generated by wrknv
        content = None
        is_wrknv_generated = "Generated by wrknv" in content or "WORKENV_DIR=" in content

        if not is_wrknv_generated:
            self.checks_failed.append(
                ("env.sh", "Not generated by wrknv - run 'wrknv generate' to regenerate")
            )
            return

        # Check if it's executable
        if not os.access(env_script, os.X_OK):
            self.checks_warning.append(("env.sh", "Not executable - run 'chmod +x env.sh'"))

        # Check for correct workenv pattern
        system = platform.system().lower()
        machine = platform.machine().lower()
        if machine in ["x86_64", "amd64"]:
            arch = "amd64"
        elif machine in ["arm64", "aarch64"]:
            arch = "arm64"
        else:
            arch = machine

        package_name = Path.cwd().name
        expected_workenv = f"workenv/{package_name}_{system}_{arch}"

        if expected_workenv not in content:
            self.checks_failed.append(
                (
                    "env.sh Workenv Path",
                    f"Incorrect pattern - expected '{expected_workenv}'. Regenerate with 'wrknv generate'",
                )
            )
            return

        # Check required patterns for proper functioning
        required_patterns = [
            "WORKENV_DIR=",
            "UV_INSTALLER_URL=",
            "uv venv",
            "uv sync",
        ]

        missing = []
        for pattern in required_patterns:
            if pattern not in content:
                missing.append(pattern)

        if missing:
            self.checks_failed.append(
                (
                    "env.sh Content",
                    f"Missing required patterns: {', '.join(missing)}. Regenerate with 'wrknv generate'",
                )
            )
        else:
            # Test if env.sh actually works
            self._test_env_script_execution(env_script)

    def xǁWrknvDoctorǁ_check_env_script__mutmut_13(self) -> None:
        """Check if env.sh exists and is valid."""
        env_script = Path.cwd() / "env.sh"

        if not env_script.exists():
            self.checks_failed.append(("env.sh", "Not found - run 'wrknv generate'"))
            return

        # Check if env.sh was generated by wrknv
        content = env_script.read_text()
        is_wrknv_generated = None

        if not is_wrknv_generated:
            self.checks_failed.append(
                ("env.sh", "Not generated by wrknv - run 'wrknv generate' to regenerate")
            )
            return

        # Check if it's executable
        if not os.access(env_script, os.X_OK):
            self.checks_warning.append(("env.sh", "Not executable - run 'chmod +x env.sh'"))

        # Check for correct workenv pattern
        system = platform.system().lower()
        machine = platform.machine().lower()
        if machine in ["x86_64", "amd64"]:
            arch = "amd64"
        elif machine in ["arm64", "aarch64"]:
            arch = "arm64"
        else:
            arch = machine

        package_name = Path.cwd().name
        expected_workenv = f"workenv/{package_name}_{system}_{arch}"

        if expected_workenv not in content:
            self.checks_failed.append(
                (
                    "env.sh Workenv Path",
                    f"Incorrect pattern - expected '{expected_workenv}'. Regenerate with 'wrknv generate'",
                )
            )
            return

        # Check required patterns for proper functioning
        required_patterns = [
            "WORKENV_DIR=",
            "UV_INSTALLER_URL=",
            "uv venv",
            "uv sync",
        ]

        missing = []
        for pattern in required_patterns:
            if pattern not in content:
                missing.append(pattern)

        if missing:
            self.checks_failed.append(
                (
                    "env.sh Content",
                    f"Missing required patterns: {', '.join(missing)}. Regenerate with 'wrknv generate'",
                )
            )
        else:
            # Test if env.sh actually works
            self._test_env_script_execution(env_script)

    def xǁWrknvDoctorǁ_check_env_script__mutmut_14(self) -> None:
        """Check if env.sh exists and is valid."""
        env_script = Path.cwd() / "env.sh"

        if not env_script.exists():
            self.checks_failed.append(("env.sh", "Not found - run 'wrknv generate'"))
            return

        # Check if env.sh was generated by wrknv
        content = env_script.read_text()
        is_wrknv_generated = "Generated by wrknv" in content and "WORKENV_DIR=" in content

        if not is_wrknv_generated:
            self.checks_failed.append(
                ("env.sh", "Not generated by wrknv - run 'wrknv generate' to regenerate")
            )
            return

        # Check if it's executable
        if not os.access(env_script, os.X_OK):
            self.checks_warning.append(("env.sh", "Not executable - run 'chmod +x env.sh'"))

        # Check for correct workenv pattern
        system = platform.system().lower()
        machine = platform.machine().lower()
        if machine in ["x86_64", "amd64"]:
            arch = "amd64"
        elif machine in ["arm64", "aarch64"]:
            arch = "arm64"
        else:
            arch = machine

        package_name = Path.cwd().name
        expected_workenv = f"workenv/{package_name}_{system}_{arch}"

        if expected_workenv not in content:
            self.checks_failed.append(
                (
                    "env.sh Workenv Path",
                    f"Incorrect pattern - expected '{expected_workenv}'. Regenerate with 'wrknv generate'",
                )
            )
            return

        # Check required patterns for proper functioning
        required_patterns = [
            "WORKENV_DIR=",
            "UV_INSTALLER_URL=",
            "uv venv",
            "uv sync",
        ]

        missing = []
        for pattern in required_patterns:
            if pattern not in content:
                missing.append(pattern)

        if missing:
            self.checks_failed.append(
                (
                    "env.sh Content",
                    f"Missing required patterns: {', '.join(missing)}. Regenerate with 'wrknv generate'",
                )
            )
        else:
            # Test if env.sh actually works
            self._test_env_script_execution(env_script)

    def xǁWrknvDoctorǁ_check_env_script__mutmut_15(self) -> None:
        """Check if env.sh exists and is valid."""
        env_script = Path.cwd() / "env.sh"

        if not env_script.exists():
            self.checks_failed.append(("env.sh", "Not found - run 'wrknv generate'"))
            return

        # Check if env.sh was generated by wrknv
        content = env_script.read_text()
        is_wrknv_generated = "XXGenerated by wrknvXX" in content or "WORKENV_DIR=" in content

        if not is_wrknv_generated:
            self.checks_failed.append(
                ("env.sh", "Not generated by wrknv - run 'wrknv generate' to regenerate")
            )
            return

        # Check if it's executable
        if not os.access(env_script, os.X_OK):
            self.checks_warning.append(("env.sh", "Not executable - run 'chmod +x env.sh'"))

        # Check for correct workenv pattern
        system = platform.system().lower()
        machine = platform.machine().lower()
        if machine in ["x86_64", "amd64"]:
            arch = "amd64"
        elif machine in ["arm64", "aarch64"]:
            arch = "arm64"
        else:
            arch = machine

        package_name = Path.cwd().name
        expected_workenv = f"workenv/{package_name}_{system}_{arch}"

        if expected_workenv not in content:
            self.checks_failed.append(
                (
                    "env.sh Workenv Path",
                    f"Incorrect pattern - expected '{expected_workenv}'. Regenerate with 'wrknv generate'",
                )
            )
            return

        # Check required patterns for proper functioning
        required_patterns = [
            "WORKENV_DIR=",
            "UV_INSTALLER_URL=",
            "uv venv",
            "uv sync",
        ]

        missing = []
        for pattern in required_patterns:
            if pattern not in content:
                missing.append(pattern)

        if missing:
            self.checks_failed.append(
                (
                    "env.sh Content",
                    f"Missing required patterns: {', '.join(missing)}. Regenerate with 'wrknv generate'",
                )
            )
        else:
            # Test if env.sh actually works
            self._test_env_script_execution(env_script)

    def xǁWrknvDoctorǁ_check_env_script__mutmut_16(self) -> None:
        """Check if env.sh exists and is valid."""
        env_script = Path.cwd() / "env.sh"

        if not env_script.exists():
            self.checks_failed.append(("env.sh", "Not found - run 'wrknv generate'"))
            return

        # Check if env.sh was generated by wrknv
        content = env_script.read_text()
        is_wrknv_generated = "generated by wrknv" in content or "WORKENV_DIR=" in content

        if not is_wrknv_generated:
            self.checks_failed.append(
                ("env.sh", "Not generated by wrknv - run 'wrknv generate' to regenerate")
            )
            return

        # Check if it's executable
        if not os.access(env_script, os.X_OK):
            self.checks_warning.append(("env.sh", "Not executable - run 'chmod +x env.sh'"))

        # Check for correct workenv pattern
        system = platform.system().lower()
        machine = platform.machine().lower()
        if machine in ["x86_64", "amd64"]:
            arch = "amd64"
        elif machine in ["arm64", "aarch64"]:
            arch = "arm64"
        else:
            arch = machine

        package_name = Path.cwd().name
        expected_workenv = f"workenv/{package_name}_{system}_{arch}"

        if expected_workenv not in content:
            self.checks_failed.append(
                (
                    "env.sh Workenv Path",
                    f"Incorrect pattern - expected '{expected_workenv}'. Regenerate with 'wrknv generate'",
                )
            )
            return

        # Check required patterns for proper functioning
        required_patterns = [
            "WORKENV_DIR=",
            "UV_INSTALLER_URL=",
            "uv venv",
            "uv sync",
        ]

        missing = []
        for pattern in required_patterns:
            if pattern not in content:
                missing.append(pattern)

        if missing:
            self.checks_failed.append(
                (
                    "env.sh Content",
                    f"Missing required patterns: {', '.join(missing)}. Regenerate with 'wrknv generate'",
                )
            )
        else:
            # Test if env.sh actually works
            self._test_env_script_execution(env_script)

    def xǁWrknvDoctorǁ_check_env_script__mutmut_17(self) -> None:
        """Check if env.sh exists and is valid."""
        env_script = Path.cwd() / "env.sh"

        if not env_script.exists():
            self.checks_failed.append(("env.sh", "Not found - run 'wrknv generate'"))
            return

        # Check if env.sh was generated by wrknv
        content = env_script.read_text()
        is_wrknv_generated = "GENERATED BY WRKNV" in content or "WORKENV_DIR=" in content

        if not is_wrknv_generated:
            self.checks_failed.append(
                ("env.sh", "Not generated by wrknv - run 'wrknv generate' to regenerate")
            )
            return

        # Check if it's executable
        if not os.access(env_script, os.X_OK):
            self.checks_warning.append(("env.sh", "Not executable - run 'chmod +x env.sh'"))

        # Check for correct workenv pattern
        system = platform.system().lower()
        machine = platform.machine().lower()
        if machine in ["x86_64", "amd64"]:
            arch = "amd64"
        elif machine in ["arm64", "aarch64"]:
            arch = "arm64"
        else:
            arch = machine

        package_name = Path.cwd().name
        expected_workenv = f"workenv/{package_name}_{system}_{arch}"

        if expected_workenv not in content:
            self.checks_failed.append(
                (
                    "env.sh Workenv Path",
                    f"Incorrect pattern - expected '{expected_workenv}'. Regenerate with 'wrknv generate'",
                )
            )
            return

        # Check required patterns for proper functioning
        required_patterns = [
            "WORKENV_DIR=",
            "UV_INSTALLER_URL=",
            "uv venv",
            "uv sync",
        ]

        missing = []
        for pattern in required_patterns:
            if pattern not in content:
                missing.append(pattern)

        if missing:
            self.checks_failed.append(
                (
                    "env.sh Content",
                    f"Missing required patterns: {', '.join(missing)}. Regenerate with 'wrknv generate'",
                )
            )
        else:
            # Test if env.sh actually works
            self._test_env_script_execution(env_script)

    def xǁWrknvDoctorǁ_check_env_script__mutmut_18(self) -> None:
        """Check if env.sh exists and is valid."""
        env_script = Path.cwd() / "env.sh"

        if not env_script.exists():
            self.checks_failed.append(("env.sh", "Not found - run 'wrknv generate'"))
            return

        # Check if env.sh was generated by wrknv
        content = env_script.read_text()
        is_wrknv_generated = "Generated by wrknv" not in content or "WORKENV_DIR=" in content

        if not is_wrknv_generated:
            self.checks_failed.append(
                ("env.sh", "Not generated by wrknv - run 'wrknv generate' to regenerate")
            )
            return

        # Check if it's executable
        if not os.access(env_script, os.X_OK):
            self.checks_warning.append(("env.sh", "Not executable - run 'chmod +x env.sh'"))

        # Check for correct workenv pattern
        system = platform.system().lower()
        machine = platform.machine().lower()
        if machine in ["x86_64", "amd64"]:
            arch = "amd64"
        elif machine in ["arm64", "aarch64"]:
            arch = "arm64"
        else:
            arch = machine

        package_name = Path.cwd().name
        expected_workenv = f"workenv/{package_name}_{system}_{arch}"

        if expected_workenv not in content:
            self.checks_failed.append(
                (
                    "env.sh Workenv Path",
                    f"Incorrect pattern - expected '{expected_workenv}'. Regenerate with 'wrknv generate'",
                )
            )
            return

        # Check required patterns for proper functioning
        required_patterns = [
            "WORKENV_DIR=",
            "UV_INSTALLER_URL=",
            "uv venv",
            "uv sync",
        ]

        missing = []
        for pattern in required_patterns:
            if pattern not in content:
                missing.append(pattern)

        if missing:
            self.checks_failed.append(
                (
                    "env.sh Content",
                    f"Missing required patterns: {', '.join(missing)}. Regenerate with 'wrknv generate'",
                )
            )
        else:
            # Test if env.sh actually works
            self._test_env_script_execution(env_script)

    def xǁWrknvDoctorǁ_check_env_script__mutmut_19(self) -> None:
        """Check if env.sh exists and is valid."""
        env_script = Path.cwd() / "env.sh"

        if not env_script.exists():
            self.checks_failed.append(("env.sh", "Not found - run 'wrknv generate'"))
            return

        # Check if env.sh was generated by wrknv
        content = env_script.read_text()
        is_wrknv_generated = "Generated by wrknv" in content or "XXWORKENV_DIR=XX" in content

        if not is_wrknv_generated:
            self.checks_failed.append(
                ("env.sh", "Not generated by wrknv - run 'wrknv generate' to regenerate")
            )
            return

        # Check if it's executable
        if not os.access(env_script, os.X_OK):
            self.checks_warning.append(("env.sh", "Not executable - run 'chmod +x env.sh'"))

        # Check for correct workenv pattern
        system = platform.system().lower()
        machine = platform.machine().lower()
        if machine in ["x86_64", "amd64"]:
            arch = "amd64"
        elif machine in ["arm64", "aarch64"]:
            arch = "arm64"
        else:
            arch = machine

        package_name = Path.cwd().name
        expected_workenv = f"workenv/{package_name}_{system}_{arch}"

        if expected_workenv not in content:
            self.checks_failed.append(
                (
                    "env.sh Workenv Path",
                    f"Incorrect pattern - expected '{expected_workenv}'. Regenerate with 'wrknv generate'",
                )
            )
            return

        # Check required patterns for proper functioning
        required_patterns = [
            "WORKENV_DIR=",
            "UV_INSTALLER_URL=",
            "uv venv",
            "uv sync",
        ]

        missing = []
        for pattern in required_patterns:
            if pattern not in content:
                missing.append(pattern)

        if missing:
            self.checks_failed.append(
                (
                    "env.sh Content",
                    f"Missing required patterns: {', '.join(missing)}. Regenerate with 'wrknv generate'",
                )
            )
        else:
            # Test if env.sh actually works
            self._test_env_script_execution(env_script)

    def xǁWrknvDoctorǁ_check_env_script__mutmut_20(self) -> None:
        """Check if env.sh exists and is valid."""
        env_script = Path.cwd() / "env.sh"

        if not env_script.exists():
            self.checks_failed.append(("env.sh", "Not found - run 'wrknv generate'"))
            return

        # Check if env.sh was generated by wrknv
        content = env_script.read_text()
        is_wrknv_generated = "Generated by wrknv" in content or "workenv_dir=" in content

        if not is_wrknv_generated:
            self.checks_failed.append(
                ("env.sh", "Not generated by wrknv - run 'wrknv generate' to regenerate")
            )
            return

        # Check if it's executable
        if not os.access(env_script, os.X_OK):
            self.checks_warning.append(("env.sh", "Not executable - run 'chmod +x env.sh'"))

        # Check for correct workenv pattern
        system = platform.system().lower()
        machine = platform.machine().lower()
        if machine in ["x86_64", "amd64"]:
            arch = "amd64"
        elif machine in ["arm64", "aarch64"]:
            arch = "arm64"
        else:
            arch = machine

        package_name = Path.cwd().name
        expected_workenv = f"workenv/{package_name}_{system}_{arch}"

        if expected_workenv not in content:
            self.checks_failed.append(
                (
                    "env.sh Workenv Path",
                    f"Incorrect pattern - expected '{expected_workenv}'. Regenerate with 'wrknv generate'",
                )
            )
            return

        # Check required patterns for proper functioning
        required_patterns = [
            "WORKENV_DIR=",
            "UV_INSTALLER_URL=",
            "uv venv",
            "uv sync",
        ]

        missing = []
        for pattern in required_patterns:
            if pattern not in content:
                missing.append(pattern)

        if missing:
            self.checks_failed.append(
                (
                    "env.sh Content",
                    f"Missing required patterns: {', '.join(missing)}. Regenerate with 'wrknv generate'",
                )
            )
        else:
            # Test if env.sh actually works
            self._test_env_script_execution(env_script)

    def xǁWrknvDoctorǁ_check_env_script__mutmut_21(self) -> None:
        """Check if env.sh exists and is valid."""
        env_script = Path.cwd() / "env.sh"

        if not env_script.exists():
            self.checks_failed.append(("env.sh", "Not found - run 'wrknv generate'"))
            return

        # Check if env.sh was generated by wrknv
        content = env_script.read_text()
        is_wrknv_generated = "Generated by wrknv" in content or "WORKENV_DIR=" not in content

        if not is_wrknv_generated:
            self.checks_failed.append(
                ("env.sh", "Not generated by wrknv - run 'wrknv generate' to regenerate")
            )
            return

        # Check if it's executable
        if not os.access(env_script, os.X_OK):
            self.checks_warning.append(("env.sh", "Not executable - run 'chmod +x env.sh'"))

        # Check for correct workenv pattern
        system = platform.system().lower()
        machine = platform.machine().lower()
        if machine in ["x86_64", "amd64"]:
            arch = "amd64"
        elif machine in ["arm64", "aarch64"]:
            arch = "arm64"
        else:
            arch = machine

        package_name = Path.cwd().name
        expected_workenv = f"workenv/{package_name}_{system}_{arch}"

        if expected_workenv not in content:
            self.checks_failed.append(
                (
                    "env.sh Workenv Path",
                    f"Incorrect pattern - expected '{expected_workenv}'. Regenerate with 'wrknv generate'",
                )
            )
            return

        # Check required patterns for proper functioning
        required_patterns = [
            "WORKENV_DIR=",
            "UV_INSTALLER_URL=",
            "uv venv",
            "uv sync",
        ]

        missing = []
        for pattern in required_patterns:
            if pattern not in content:
                missing.append(pattern)

        if missing:
            self.checks_failed.append(
                (
                    "env.sh Content",
                    f"Missing required patterns: {', '.join(missing)}. Regenerate with 'wrknv generate'",
                )
            )
        else:
            # Test if env.sh actually works
            self._test_env_script_execution(env_script)

    def xǁWrknvDoctorǁ_check_env_script__mutmut_22(self) -> None:
        """Check if env.sh exists and is valid."""
        env_script = Path.cwd() / "env.sh"

        if not env_script.exists():
            self.checks_failed.append(("env.sh", "Not found - run 'wrknv generate'"))
            return

        # Check if env.sh was generated by wrknv
        content = env_script.read_text()
        is_wrknv_generated = "Generated by wrknv" in content or "WORKENV_DIR=" in content

        if is_wrknv_generated:
            self.checks_failed.append(
                ("env.sh", "Not generated by wrknv - run 'wrknv generate' to regenerate")
            )
            return

        # Check if it's executable
        if not os.access(env_script, os.X_OK):
            self.checks_warning.append(("env.sh", "Not executable - run 'chmod +x env.sh'"))

        # Check for correct workenv pattern
        system = platform.system().lower()
        machine = platform.machine().lower()
        if machine in ["x86_64", "amd64"]:
            arch = "amd64"
        elif machine in ["arm64", "aarch64"]:
            arch = "arm64"
        else:
            arch = machine

        package_name = Path.cwd().name
        expected_workenv = f"workenv/{package_name}_{system}_{arch}"

        if expected_workenv not in content:
            self.checks_failed.append(
                (
                    "env.sh Workenv Path",
                    f"Incorrect pattern - expected '{expected_workenv}'. Regenerate with 'wrknv generate'",
                )
            )
            return

        # Check required patterns for proper functioning
        required_patterns = [
            "WORKENV_DIR=",
            "UV_INSTALLER_URL=",
            "uv venv",
            "uv sync",
        ]

        missing = []
        for pattern in required_patterns:
            if pattern not in content:
                missing.append(pattern)

        if missing:
            self.checks_failed.append(
                (
                    "env.sh Content",
                    f"Missing required patterns: {', '.join(missing)}. Regenerate with 'wrknv generate'",
                )
            )
        else:
            # Test if env.sh actually works
            self._test_env_script_execution(env_script)

    def xǁWrknvDoctorǁ_check_env_script__mutmut_23(self) -> None:
        """Check if env.sh exists and is valid."""
        env_script = Path.cwd() / "env.sh"

        if not env_script.exists():
            self.checks_failed.append(("env.sh", "Not found - run 'wrknv generate'"))
            return

        # Check if env.sh was generated by wrknv
        content = env_script.read_text()
        is_wrknv_generated = "Generated by wrknv" in content or "WORKENV_DIR=" in content

        if not is_wrknv_generated:
            self.checks_failed.append(
                None
            )
            return

        # Check if it's executable
        if not os.access(env_script, os.X_OK):
            self.checks_warning.append(("env.sh", "Not executable - run 'chmod +x env.sh'"))

        # Check for correct workenv pattern
        system = platform.system().lower()
        machine = platform.machine().lower()
        if machine in ["x86_64", "amd64"]:
            arch = "amd64"
        elif machine in ["arm64", "aarch64"]:
            arch = "arm64"
        else:
            arch = machine

        package_name = Path.cwd().name
        expected_workenv = f"workenv/{package_name}_{system}_{arch}"

        if expected_workenv not in content:
            self.checks_failed.append(
                (
                    "env.sh Workenv Path",
                    f"Incorrect pattern - expected '{expected_workenv}'. Regenerate with 'wrknv generate'",
                )
            )
            return

        # Check required patterns for proper functioning
        required_patterns = [
            "WORKENV_DIR=",
            "UV_INSTALLER_URL=",
            "uv venv",
            "uv sync",
        ]

        missing = []
        for pattern in required_patterns:
            if pattern not in content:
                missing.append(pattern)

        if missing:
            self.checks_failed.append(
                (
                    "env.sh Content",
                    f"Missing required patterns: {', '.join(missing)}. Regenerate with 'wrknv generate'",
                )
            )
        else:
            # Test if env.sh actually works
            self._test_env_script_execution(env_script)

    def xǁWrknvDoctorǁ_check_env_script__mutmut_24(self) -> None:
        """Check if env.sh exists and is valid."""
        env_script = Path.cwd() / "env.sh"

        if not env_script.exists():
            self.checks_failed.append(("env.sh", "Not found - run 'wrknv generate'"))
            return

        # Check if env.sh was generated by wrknv
        content = env_script.read_text()
        is_wrknv_generated = "Generated by wrknv" in content or "WORKENV_DIR=" in content

        if not is_wrknv_generated:
            self.checks_failed.append(
                ("XXenv.shXX", "Not generated by wrknv - run 'wrknv generate' to regenerate")
            )
            return

        # Check if it's executable
        if not os.access(env_script, os.X_OK):
            self.checks_warning.append(("env.sh", "Not executable - run 'chmod +x env.sh'"))

        # Check for correct workenv pattern
        system = platform.system().lower()
        machine = platform.machine().lower()
        if machine in ["x86_64", "amd64"]:
            arch = "amd64"
        elif machine in ["arm64", "aarch64"]:
            arch = "arm64"
        else:
            arch = machine

        package_name = Path.cwd().name
        expected_workenv = f"workenv/{package_name}_{system}_{arch}"

        if expected_workenv not in content:
            self.checks_failed.append(
                (
                    "env.sh Workenv Path",
                    f"Incorrect pattern - expected '{expected_workenv}'. Regenerate with 'wrknv generate'",
                )
            )
            return

        # Check required patterns for proper functioning
        required_patterns = [
            "WORKENV_DIR=",
            "UV_INSTALLER_URL=",
            "uv venv",
            "uv sync",
        ]

        missing = []
        for pattern in required_patterns:
            if pattern not in content:
                missing.append(pattern)

        if missing:
            self.checks_failed.append(
                (
                    "env.sh Content",
                    f"Missing required patterns: {', '.join(missing)}. Regenerate with 'wrknv generate'",
                )
            )
        else:
            # Test if env.sh actually works
            self._test_env_script_execution(env_script)

    def xǁWrknvDoctorǁ_check_env_script__mutmut_25(self) -> None:
        """Check if env.sh exists and is valid."""
        env_script = Path.cwd() / "env.sh"

        if not env_script.exists():
            self.checks_failed.append(("env.sh", "Not found - run 'wrknv generate'"))
            return

        # Check if env.sh was generated by wrknv
        content = env_script.read_text()
        is_wrknv_generated = "Generated by wrknv" in content or "WORKENV_DIR=" in content

        if not is_wrknv_generated:
            self.checks_failed.append(
                ("ENV.SH", "Not generated by wrknv - run 'wrknv generate' to regenerate")
            )
            return

        # Check if it's executable
        if not os.access(env_script, os.X_OK):
            self.checks_warning.append(("env.sh", "Not executable - run 'chmod +x env.sh'"))

        # Check for correct workenv pattern
        system = platform.system().lower()
        machine = platform.machine().lower()
        if machine in ["x86_64", "amd64"]:
            arch = "amd64"
        elif machine in ["arm64", "aarch64"]:
            arch = "arm64"
        else:
            arch = machine

        package_name = Path.cwd().name
        expected_workenv = f"workenv/{package_name}_{system}_{arch}"

        if expected_workenv not in content:
            self.checks_failed.append(
                (
                    "env.sh Workenv Path",
                    f"Incorrect pattern - expected '{expected_workenv}'. Regenerate with 'wrknv generate'",
                )
            )
            return

        # Check required patterns for proper functioning
        required_patterns = [
            "WORKENV_DIR=",
            "UV_INSTALLER_URL=",
            "uv venv",
            "uv sync",
        ]

        missing = []
        for pattern in required_patterns:
            if pattern not in content:
                missing.append(pattern)

        if missing:
            self.checks_failed.append(
                (
                    "env.sh Content",
                    f"Missing required patterns: {', '.join(missing)}. Regenerate with 'wrknv generate'",
                )
            )
        else:
            # Test if env.sh actually works
            self._test_env_script_execution(env_script)

    def xǁWrknvDoctorǁ_check_env_script__mutmut_26(self) -> None:
        """Check if env.sh exists and is valid."""
        env_script = Path.cwd() / "env.sh"

        if not env_script.exists():
            self.checks_failed.append(("env.sh", "Not found - run 'wrknv generate'"))
            return

        # Check if env.sh was generated by wrknv
        content = env_script.read_text()
        is_wrknv_generated = "Generated by wrknv" in content or "WORKENV_DIR=" in content

        if not is_wrknv_generated:
            self.checks_failed.append(
                ("env.sh", "XXNot generated by wrknv - run 'wrknv generate' to regenerateXX")
            )
            return

        # Check if it's executable
        if not os.access(env_script, os.X_OK):
            self.checks_warning.append(("env.sh", "Not executable - run 'chmod +x env.sh'"))

        # Check for correct workenv pattern
        system = platform.system().lower()
        machine = platform.machine().lower()
        if machine in ["x86_64", "amd64"]:
            arch = "amd64"
        elif machine in ["arm64", "aarch64"]:
            arch = "arm64"
        else:
            arch = machine

        package_name = Path.cwd().name
        expected_workenv = f"workenv/{package_name}_{system}_{arch}"

        if expected_workenv not in content:
            self.checks_failed.append(
                (
                    "env.sh Workenv Path",
                    f"Incorrect pattern - expected '{expected_workenv}'. Regenerate with 'wrknv generate'",
                )
            )
            return

        # Check required patterns for proper functioning
        required_patterns = [
            "WORKENV_DIR=",
            "UV_INSTALLER_URL=",
            "uv venv",
            "uv sync",
        ]

        missing = []
        for pattern in required_patterns:
            if pattern not in content:
                missing.append(pattern)

        if missing:
            self.checks_failed.append(
                (
                    "env.sh Content",
                    f"Missing required patterns: {', '.join(missing)}. Regenerate with 'wrknv generate'",
                )
            )
        else:
            # Test if env.sh actually works
            self._test_env_script_execution(env_script)

    def xǁWrknvDoctorǁ_check_env_script__mutmut_27(self) -> None:
        """Check if env.sh exists and is valid."""
        env_script = Path.cwd() / "env.sh"

        if not env_script.exists():
            self.checks_failed.append(("env.sh", "Not found - run 'wrknv generate'"))
            return

        # Check if env.sh was generated by wrknv
        content = env_script.read_text()
        is_wrknv_generated = "Generated by wrknv" in content or "WORKENV_DIR=" in content

        if not is_wrknv_generated:
            self.checks_failed.append(
                ("env.sh", "not generated by wrknv - run 'wrknv generate' to regenerate")
            )
            return

        # Check if it's executable
        if not os.access(env_script, os.X_OK):
            self.checks_warning.append(("env.sh", "Not executable - run 'chmod +x env.sh'"))

        # Check for correct workenv pattern
        system = platform.system().lower()
        machine = platform.machine().lower()
        if machine in ["x86_64", "amd64"]:
            arch = "amd64"
        elif machine in ["arm64", "aarch64"]:
            arch = "arm64"
        else:
            arch = machine

        package_name = Path.cwd().name
        expected_workenv = f"workenv/{package_name}_{system}_{arch}"

        if expected_workenv not in content:
            self.checks_failed.append(
                (
                    "env.sh Workenv Path",
                    f"Incorrect pattern - expected '{expected_workenv}'. Regenerate with 'wrknv generate'",
                )
            )
            return

        # Check required patterns for proper functioning
        required_patterns = [
            "WORKENV_DIR=",
            "UV_INSTALLER_URL=",
            "uv venv",
            "uv sync",
        ]

        missing = []
        for pattern in required_patterns:
            if pattern not in content:
                missing.append(pattern)

        if missing:
            self.checks_failed.append(
                (
                    "env.sh Content",
                    f"Missing required patterns: {', '.join(missing)}. Regenerate with 'wrknv generate'",
                )
            )
        else:
            # Test if env.sh actually works
            self._test_env_script_execution(env_script)

    def xǁWrknvDoctorǁ_check_env_script__mutmut_28(self) -> None:
        """Check if env.sh exists and is valid."""
        env_script = Path.cwd() / "env.sh"

        if not env_script.exists():
            self.checks_failed.append(("env.sh", "Not found - run 'wrknv generate'"))
            return

        # Check if env.sh was generated by wrknv
        content = env_script.read_text()
        is_wrknv_generated = "Generated by wrknv" in content or "WORKENV_DIR=" in content

        if not is_wrknv_generated:
            self.checks_failed.append(
                ("env.sh", "NOT GENERATED BY WRKNV - RUN 'WRKNV GENERATE' TO REGENERATE")
            )
            return

        # Check if it's executable
        if not os.access(env_script, os.X_OK):
            self.checks_warning.append(("env.sh", "Not executable - run 'chmod +x env.sh'"))

        # Check for correct workenv pattern
        system = platform.system().lower()
        machine = platform.machine().lower()
        if machine in ["x86_64", "amd64"]:
            arch = "amd64"
        elif machine in ["arm64", "aarch64"]:
            arch = "arm64"
        else:
            arch = machine

        package_name = Path.cwd().name
        expected_workenv = f"workenv/{package_name}_{system}_{arch}"

        if expected_workenv not in content:
            self.checks_failed.append(
                (
                    "env.sh Workenv Path",
                    f"Incorrect pattern - expected '{expected_workenv}'. Regenerate with 'wrknv generate'",
                )
            )
            return

        # Check required patterns for proper functioning
        required_patterns = [
            "WORKENV_DIR=",
            "UV_INSTALLER_URL=",
            "uv venv",
            "uv sync",
        ]

        missing = []
        for pattern in required_patterns:
            if pattern not in content:
                missing.append(pattern)

        if missing:
            self.checks_failed.append(
                (
                    "env.sh Content",
                    f"Missing required patterns: {', '.join(missing)}. Regenerate with 'wrknv generate'",
                )
            )
        else:
            # Test if env.sh actually works
            self._test_env_script_execution(env_script)

    def xǁWrknvDoctorǁ_check_env_script__mutmut_29(self) -> None:
        """Check if env.sh exists and is valid."""
        env_script = Path.cwd() / "env.sh"

        if not env_script.exists():
            self.checks_failed.append(("env.sh", "Not found - run 'wrknv generate'"))
            return

        # Check if env.sh was generated by wrknv
        content = env_script.read_text()
        is_wrknv_generated = "Generated by wrknv" in content or "WORKENV_DIR=" in content

        if not is_wrknv_generated:
            self.checks_failed.append(
                ("env.sh", "Not generated by wrknv - run 'wrknv generate' to regenerate")
            )
            return

        # Check if it's executable
        if os.access(env_script, os.X_OK):
            self.checks_warning.append(("env.sh", "Not executable - run 'chmod +x env.sh'"))

        # Check for correct workenv pattern
        system = platform.system().lower()
        machine = platform.machine().lower()
        if machine in ["x86_64", "amd64"]:
            arch = "amd64"
        elif machine in ["arm64", "aarch64"]:
            arch = "arm64"
        else:
            arch = machine

        package_name = Path.cwd().name
        expected_workenv = f"workenv/{package_name}_{system}_{arch}"

        if expected_workenv not in content:
            self.checks_failed.append(
                (
                    "env.sh Workenv Path",
                    f"Incorrect pattern - expected '{expected_workenv}'. Regenerate with 'wrknv generate'",
                )
            )
            return

        # Check required patterns for proper functioning
        required_patterns = [
            "WORKENV_DIR=",
            "UV_INSTALLER_URL=",
            "uv venv",
            "uv sync",
        ]

        missing = []
        for pattern in required_patterns:
            if pattern not in content:
                missing.append(pattern)

        if missing:
            self.checks_failed.append(
                (
                    "env.sh Content",
                    f"Missing required patterns: {', '.join(missing)}. Regenerate with 'wrknv generate'",
                )
            )
        else:
            # Test if env.sh actually works
            self._test_env_script_execution(env_script)

    def xǁWrknvDoctorǁ_check_env_script__mutmut_30(self) -> None:
        """Check if env.sh exists and is valid."""
        env_script = Path.cwd() / "env.sh"

        if not env_script.exists():
            self.checks_failed.append(("env.sh", "Not found - run 'wrknv generate'"))
            return

        # Check if env.sh was generated by wrknv
        content = env_script.read_text()
        is_wrknv_generated = "Generated by wrknv" in content or "WORKENV_DIR=" in content

        if not is_wrknv_generated:
            self.checks_failed.append(
                ("env.sh", "Not generated by wrknv - run 'wrknv generate' to regenerate")
            )
            return

        # Check if it's executable
        if not os.access(None, os.X_OK):
            self.checks_warning.append(("env.sh", "Not executable - run 'chmod +x env.sh'"))

        # Check for correct workenv pattern
        system = platform.system().lower()
        machine = platform.machine().lower()
        if machine in ["x86_64", "amd64"]:
            arch = "amd64"
        elif machine in ["arm64", "aarch64"]:
            arch = "arm64"
        else:
            arch = machine

        package_name = Path.cwd().name
        expected_workenv = f"workenv/{package_name}_{system}_{arch}"

        if expected_workenv not in content:
            self.checks_failed.append(
                (
                    "env.sh Workenv Path",
                    f"Incorrect pattern - expected '{expected_workenv}'. Regenerate with 'wrknv generate'",
                )
            )
            return

        # Check required patterns for proper functioning
        required_patterns = [
            "WORKENV_DIR=",
            "UV_INSTALLER_URL=",
            "uv venv",
            "uv sync",
        ]

        missing = []
        for pattern in required_patterns:
            if pattern not in content:
                missing.append(pattern)

        if missing:
            self.checks_failed.append(
                (
                    "env.sh Content",
                    f"Missing required patterns: {', '.join(missing)}. Regenerate with 'wrknv generate'",
                )
            )
        else:
            # Test if env.sh actually works
            self._test_env_script_execution(env_script)

    def xǁWrknvDoctorǁ_check_env_script__mutmut_31(self) -> None:
        """Check if env.sh exists and is valid."""
        env_script = Path.cwd() / "env.sh"

        if not env_script.exists():
            self.checks_failed.append(("env.sh", "Not found - run 'wrknv generate'"))
            return

        # Check if env.sh was generated by wrknv
        content = env_script.read_text()
        is_wrknv_generated = "Generated by wrknv" in content or "WORKENV_DIR=" in content

        if not is_wrknv_generated:
            self.checks_failed.append(
                ("env.sh", "Not generated by wrknv - run 'wrknv generate' to regenerate")
            )
            return

        # Check if it's executable
        if not os.access(env_script, None):
            self.checks_warning.append(("env.sh", "Not executable - run 'chmod +x env.sh'"))

        # Check for correct workenv pattern
        system = platform.system().lower()
        machine = platform.machine().lower()
        if machine in ["x86_64", "amd64"]:
            arch = "amd64"
        elif machine in ["arm64", "aarch64"]:
            arch = "arm64"
        else:
            arch = machine

        package_name = Path.cwd().name
        expected_workenv = f"workenv/{package_name}_{system}_{arch}"

        if expected_workenv not in content:
            self.checks_failed.append(
                (
                    "env.sh Workenv Path",
                    f"Incorrect pattern - expected '{expected_workenv}'. Regenerate with 'wrknv generate'",
                )
            )
            return

        # Check required patterns for proper functioning
        required_patterns = [
            "WORKENV_DIR=",
            "UV_INSTALLER_URL=",
            "uv venv",
            "uv sync",
        ]

        missing = []
        for pattern in required_patterns:
            if pattern not in content:
                missing.append(pattern)

        if missing:
            self.checks_failed.append(
                (
                    "env.sh Content",
                    f"Missing required patterns: {', '.join(missing)}. Regenerate with 'wrknv generate'",
                )
            )
        else:
            # Test if env.sh actually works
            self._test_env_script_execution(env_script)

    def xǁWrknvDoctorǁ_check_env_script__mutmut_32(self) -> None:
        """Check if env.sh exists and is valid."""
        env_script = Path.cwd() / "env.sh"

        if not env_script.exists():
            self.checks_failed.append(("env.sh", "Not found - run 'wrknv generate'"))
            return

        # Check if env.sh was generated by wrknv
        content = env_script.read_text()
        is_wrknv_generated = "Generated by wrknv" in content or "WORKENV_DIR=" in content

        if not is_wrknv_generated:
            self.checks_failed.append(
                ("env.sh", "Not generated by wrknv - run 'wrknv generate' to regenerate")
            )
            return

        # Check if it's executable
        if not os.access(os.X_OK):
            self.checks_warning.append(("env.sh", "Not executable - run 'chmod +x env.sh'"))

        # Check for correct workenv pattern
        system = platform.system().lower()
        machine = platform.machine().lower()
        if machine in ["x86_64", "amd64"]:
            arch = "amd64"
        elif machine in ["arm64", "aarch64"]:
            arch = "arm64"
        else:
            arch = machine

        package_name = Path.cwd().name
        expected_workenv = f"workenv/{package_name}_{system}_{arch}"

        if expected_workenv not in content:
            self.checks_failed.append(
                (
                    "env.sh Workenv Path",
                    f"Incorrect pattern - expected '{expected_workenv}'. Regenerate with 'wrknv generate'",
                )
            )
            return

        # Check required patterns for proper functioning
        required_patterns = [
            "WORKENV_DIR=",
            "UV_INSTALLER_URL=",
            "uv venv",
            "uv sync",
        ]

        missing = []
        for pattern in required_patterns:
            if pattern not in content:
                missing.append(pattern)

        if missing:
            self.checks_failed.append(
                (
                    "env.sh Content",
                    f"Missing required patterns: {', '.join(missing)}. Regenerate with 'wrknv generate'",
                )
            )
        else:
            # Test if env.sh actually works
            self._test_env_script_execution(env_script)

    def xǁWrknvDoctorǁ_check_env_script__mutmut_33(self) -> None:
        """Check if env.sh exists and is valid."""
        env_script = Path.cwd() / "env.sh"

        if not env_script.exists():
            self.checks_failed.append(("env.sh", "Not found - run 'wrknv generate'"))
            return

        # Check if env.sh was generated by wrknv
        content = env_script.read_text()
        is_wrknv_generated = "Generated by wrknv" in content or "WORKENV_DIR=" in content

        if not is_wrknv_generated:
            self.checks_failed.append(
                ("env.sh", "Not generated by wrknv - run 'wrknv generate' to regenerate")
            )
            return

        # Check if it's executable
        if not os.access(env_script, ):
            self.checks_warning.append(("env.sh", "Not executable - run 'chmod +x env.sh'"))

        # Check for correct workenv pattern
        system = platform.system().lower()
        machine = platform.machine().lower()
        if machine in ["x86_64", "amd64"]:
            arch = "amd64"
        elif machine in ["arm64", "aarch64"]:
            arch = "arm64"
        else:
            arch = machine

        package_name = Path.cwd().name
        expected_workenv = f"workenv/{package_name}_{system}_{arch}"

        if expected_workenv not in content:
            self.checks_failed.append(
                (
                    "env.sh Workenv Path",
                    f"Incorrect pattern - expected '{expected_workenv}'. Regenerate with 'wrknv generate'",
                )
            )
            return

        # Check required patterns for proper functioning
        required_patterns = [
            "WORKENV_DIR=",
            "UV_INSTALLER_URL=",
            "uv venv",
            "uv sync",
        ]

        missing = []
        for pattern in required_patterns:
            if pattern not in content:
                missing.append(pattern)

        if missing:
            self.checks_failed.append(
                (
                    "env.sh Content",
                    f"Missing required patterns: {', '.join(missing)}. Regenerate with 'wrknv generate'",
                )
            )
        else:
            # Test if env.sh actually works
            self._test_env_script_execution(env_script)

    def xǁWrknvDoctorǁ_check_env_script__mutmut_34(self) -> None:
        """Check if env.sh exists and is valid."""
        env_script = Path.cwd() / "env.sh"

        if not env_script.exists():
            self.checks_failed.append(("env.sh", "Not found - run 'wrknv generate'"))
            return

        # Check if env.sh was generated by wrknv
        content = env_script.read_text()
        is_wrknv_generated = "Generated by wrknv" in content or "WORKENV_DIR=" in content

        if not is_wrknv_generated:
            self.checks_failed.append(
                ("env.sh", "Not generated by wrknv - run 'wrknv generate' to regenerate")
            )
            return

        # Check if it's executable
        if not os.access(env_script, os.X_OK):
            self.checks_warning.append(None)

        # Check for correct workenv pattern
        system = platform.system().lower()
        machine = platform.machine().lower()
        if machine in ["x86_64", "amd64"]:
            arch = "amd64"
        elif machine in ["arm64", "aarch64"]:
            arch = "arm64"
        else:
            arch = machine

        package_name = Path.cwd().name
        expected_workenv = f"workenv/{package_name}_{system}_{arch}"

        if expected_workenv not in content:
            self.checks_failed.append(
                (
                    "env.sh Workenv Path",
                    f"Incorrect pattern - expected '{expected_workenv}'. Regenerate with 'wrknv generate'",
                )
            )
            return

        # Check required patterns for proper functioning
        required_patterns = [
            "WORKENV_DIR=",
            "UV_INSTALLER_URL=",
            "uv venv",
            "uv sync",
        ]

        missing = []
        for pattern in required_patterns:
            if pattern not in content:
                missing.append(pattern)

        if missing:
            self.checks_failed.append(
                (
                    "env.sh Content",
                    f"Missing required patterns: {', '.join(missing)}. Regenerate with 'wrknv generate'",
                )
            )
        else:
            # Test if env.sh actually works
            self._test_env_script_execution(env_script)

    def xǁWrknvDoctorǁ_check_env_script__mutmut_35(self) -> None:
        """Check if env.sh exists and is valid."""
        env_script = Path.cwd() / "env.sh"

        if not env_script.exists():
            self.checks_failed.append(("env.sh", "Not found - run 'wrknv generate'"))
            return

        # Check if env.sh was generated by wrknv
        content = env_script.read_text()
        is_wrknv_generated = "Generated by wrknv" in content or "WORKENV_DIR=" in content

        if not is_wrknv_generated:
            self.checks_failed.append(
                ("env.sh", "Not generated by wrknv - run 'wrknv generate' to regenerate")
            )
            return

        # Check if it's executable
        if not os.access(env_script, os.X_OK):
            self.checks_warning.append(("XXenv.shXX", "Not executable - run 'chmod +x env.sh'"))

        # Check for correct workenv pattern
        system = platform.system().lower()
        machine = platform.machine().lower()
        if machine in ["x86_64", "amd64"]:
            arch = "amd64"
        elif machine in ["arm64", "aarch64"]:
            arch = "arm64"
        else:
            arch = machine

        package_name = Path.cwd().name
        expected_workenv = f"workenv/{package_name}_{system}_{arch}"

        if expected_workenv not in content:
            self.checks_failed.append(
                (
                    "env.sh Workenv Path",
                    f"Incorrect pattern - expected '{expected_workenv}'. Regenerate with 'wrknv generate'",
                )
            )
            return

        # Check required patterns for proper functioning
        required_patterns = [
            "WORKENV_DIR=",
            "UV_INSTALLER_URL=",
            "uv venv",
            "uv sync",
        ]

        missing = []
        for pattern in required_patterns:
            if pattern not in content:
                missing.append(pattern)

        if missing:
            self.checks_failed.append(
                (
                    "env.sh Content",
                    f"Missing required patterns: {', '.join(missing)}. Regenerate with 'wrknv generate'",
                )
            )
        else:
            # Test if env.sh actually works
            self._test_env_script_execution(env_script)

    def xǁWrknvDoctorǁ_check_env_script__mutmut_36(self) -> None:
        """Check if env.sh exists and is valid."""
        env_script = Path.cwd() / "env.sh"

        if not env_script.exists():
            self.checks_failed.append(("env.sh", "Not found - run 'wrknv generate'"))
            return

        # Check if env.sh was generated by wrknv
        content = env_script.read_text()
        is_wrknv_generated = "Generated by wrknv" in content or "WORKENV_DIR=" in content

        if not is_wrknv_generated:
            self.checks_failed.append(
                ("env.sh", "Not generated by wrknv - run 'wrknv generate' to regenerate")
            )
            return

        # Check if it's executable
        if not os.access(env_script, os.X_OK):
            self.checks_warning.append(("ENV.SH", "Not executable - run 'chmod +x env.sh'"))

        # Check for correct workenv pattern
        system = platform.system().lower()
        machine = platform.machine().lower()
        if machine in ["x86_64", "amd64"]:
            arch = "amd64"
        elif machine in ["arm64", "aarch64"]:
            arch = "arm64"
        else:
            arch = machine

        package_name = Path.cwd().name
        expected_workenv = f"workenv/{package_name}_{system}_{arch}"

        if expected_workenv not in content:
            self.checks_failed.append(
                (
                    "env.sh Workenv Path",
                    f"Incorrect pattern - expected '{expected_workenv}'. Regenerate with 'wrknv generate'",
                )
            )
            return

        # Check required patterns for proper functioning
        required_patterns = [
            "WORKENV_DIR=",
            "UV_INSTALLER_URL=",
            "uv venv",
            "uv sync",
        ]

        missing = []
        for pattern in required_patterns:
            if pattern not in content:
                missing.append(pattern)

        if missing:
            self.checks_failed.append(
                (
                    "env.sh Content",
                    f"Missing required patterns: {', '.join(missing)}. Regenerate with 'wrknv generate'",
                )
            )
        else:
            # Test if env.sh actually works
            self._test_env_script_execution(env_script)

    def xǁWrknvDoctorǁ_check_env_script__mutmut_37(self) -> None:
        """Check if env.sh exists and is valid."""
        env_script = Path.cwd() / "env.sh"

        if not env_script.exists():
            self.checks_failed.append(("env.sh", "Not found - run 'wrknv generate'"))
            return

        # Check if env.sh was generated by wrknv
        content = env_script.read_text()
        is_wrknv_generated = "Generated by wrknv" in content or "WORKENV_DIR=" in content

        if not is_wrknv_generated:
            self.checks_failed.append(
                ("env.sh", "Not generated by wrknv - run 'wrknv generate' to regenerate")
            )
            return

        # Check if it's executable
        if not os.access(env_script, os.X_OK):
            self.checks_warning.append(("env.sh", "XXNot executable - run 'chmod +x env.sh'XX"))

        # Check for correct workenv pattern
        system = platform.system().lower()
        machine = platform.machine().lower()
        if machine in ["x86_64", "amd64"]:
            arch = "amd64"
        elif machine in ["arm64", "aarch64"]:
            arch = "arm64"
        else:
            arch = machine

        package_name = Path.cwd().name
        expected_workenv = f"workenv/{package_name}_{system}_{arch}"

        if expected_workenv not in content:
            self.checks_failed.append(
                (
                    "env.sh Workenv Path",
                    f"Incorrect pattern - expected '{expected_workenv}'. Regenerate with 'wrknv generate'",
                )
            )
            return

        # Check required patterns for proper functioning
        required_patterns = [
            "WORKENV_DIR=",
            "UV_INSTALLER_URL=",
            "uv venv",
            "uv sync",
        ]

        missing = []
        for pattern in required_patterns:
            if pattern not in content:
                missing.append(pattern)

        if missing:
            self.checks_failed.append(
                (
                    "env.sh Content",
                    f"Missing required patterns: {', '.join(missing)}. Regenerate with 'wrknv generate'",
                )
            )
        else:
            # Test if env.sh actually works
            self._test_env_script_execution(env_script)

    def xǁWrknvDoctorǁ_check_env_script__mutmut_38(self) -> None:
        """Check if env.sh exists and is valid."""
        env_script = Path.cwd() / "env.sh"

        if not env_script.exists():
            self.checks_failed.append(("env.sh", "Not found - run 'wrknv generate'"))
            return

        # Check if env.sh was generated by wrknv
        content = env_script.read_text()
        is_wrknv_generated = "Generated by wrknv" in content or "WORKENV_DIR=" in content

        if not is_wrknv_generated:
            self.checks_failed.append(
                ("env.sh", "Not generated by wrknv - run 'wrknv generate' to regenerate")
            )
            return

        # Check if it's executable
        if not os.access(env_script, os.X_OK):
            self.checks_warning.append(("env.sh", "not executable - run 'chmod +x env.sh'"))

        # Check for correct workenv pattern
        system = platform.system().lower()
        machine = platform.machine().lower()
        if machine in ["x86_64", "amd64"]:
            arch = "amd64"
        elif machine in ["arm64", "aarch64"]:
            arch = "arm64"
        else:
            arch = machine

        package_name = Path.cwd().name
        expected_workenv = f"workenv/{package_name}_{system}_{arch}"

        if expected_workenv not in content:
            self.checks_failed.append(
                (
                    "env.sh Workenv Path",
                    f"Incorrect pattern - expected '{expected_workenv}'. Regenerate with 'wrknv generate'",
                )
            )
            return

        # Check required patterns for proper functioning
        required_patterns = [
            "WORKENV_DIR=",
            "UV_INSTALLER_URL=",
            "uv venv",
            "uv sync",
        ]

        missing = []
        for pattern in required_patterns:
            if pattern not in content:
                missing.append(pattern)

        if missing:
            self.checks_failed.append(
                (
                    "env.sh Content",
                    f"Missing required patterns: {', '.join(missing)}. Regenerate with 'wrknv generate'",
                )
            )
        else:
            # Test if env.sh actually works
            self._test_env_script_execution(env_script)

    def xǁWrknvDoctorǁ_check_env_script__mutmut_39(self) -> None:
        """Check if env.sh exists and is valid."""
        env_script = Path.cwd() / "env.sh"

        if not env_script.exists():
            self.checks_failed.append(("env.sh", "Not found - run 'wrknv generate'"))
            return

        # Check if env.sh was generated by wrknv
        content = env_script.read_text()
        is_wrknv_generated = "Generated by wrknv" in content or "WORKENV_DIR=" in content

        if not is_wrknv_generated:
            self.checks_failed.append(
                ("env.sh", "Not generated by wrknv - run 'wrknv generate' to regenerate")
            )
            return

        # Check if it's executable
        if not os.access(env_script, os.X_OK):
            self.checks_warning.append(("env.sh", "NOT EXECUTABLE - RUN 'CHMOD +X ENV.SH'"))

        # Check for correct workenv pattern
        system = platform.system().lower()
        machine = platform.machine().lower()
        if machine in ["x86_64", "amd64"]:
            arch = "amd64"
        elif machine in ["arm64", "aarch64"]:
            arch = "arm64"
        else:
            arch = machine

        package_name = Path.cwd().name
        expected_workenv = f"workenv/{package_name}_{system}_{arch}"

        if expected_workenv not in content:
            self.checks_failed.append(
                (
                    "env.sh Workenv Path",
                    f"Incorrect pattern - expected '{expected_workenv}'. Regenerate with 'wrknv generate'",
                )
            )
            return

        # Check required patterns for proper functioning
        required_patterns = [
            "WORKENV_DIR=",
            "UV_INSTALLER_URL=",
            "uv venv",
            "uv sync",
        ]

        missing = []
        for pattern in required_patterns:
            if pattern not in content:
                missing.append(pattern)

        if missing:
            self.checks_failed.append(
                (
                    "env.sh Content",
                    f"Missing required patterns: {', '.join(missing)}. Regenerate with 'wrknv generate'",
                )
            )
        else:
            # Test if env.sh actually works
            self._test_env_script_execution(env_script)

    def xǁWrknvDoctorǁ_check_env_script__mutmut_40(self) -> None:
        """Check if env.sh exists and is valid."""
        env_script = Path.cwd() / "env.sh"

        if not env_script.exists():
            self.checks_failed.append(("env.sh", "Not found - run 'wrknv generate'"))
            return

        # Check if env.sh was generated by wrknv
        content = env_script.read_text()
        is_wrknv_generated = "Generated by wrknv" in content or "WORKENV_DIR=" in content

        if not is_wrknv_generated:
            self.checks_failed.append(
                ("env.sh", "Not generated by wrknv - run 'wrknv generate' to regenerate")
            )
            return

        # Check if it's executable
        if not os.access(env_script, os.X_OK):
            self.checks_warning.append(("env.sh", "Not executable - run 'chmod +x env.sh'"))

        # Check for correct workenv pattern
        system = None
        machine = platform.machine().lower()
        if machine in ["x86_64", "amd64"]:
            arch = "amd64"
        elif machine in ["arm64", "aarch64"]:
            arch = "arm64"
        else:
            arch = machine

        package_name = Path.cwd().name
        expected_workenv = f"workenv/{package_name}_{system}_{arch}"

        if expected_workenv not in content:
            self.checks_failed.append(
                (
                    "env.sh Workenv Path",
                    f"Incorrect pattern - expected '{expected_workenv}'. Regenerate with 'wrknv generate'",
                )
            )
            return

        # Check required patterns for proper functioning
        required_patterns = [
            "WORKENV_DIR=",
            "UV_INSTALLER_URL=",
            "uv venv",
            "uv sync",
        ]

        missing = []
        for pattern in required_patterns:
            if pattern not in content:
                missing.append(pattern)

        if missing:
            self.checks_failed.append(
                (
                    "env.sh Content",
                    f"Missing required patterns: {', '.join(missing)}. Regenerate with 'wrknv generate'",
                )
            )
        else:
            # Test if env.sh actually works
            self._test_env_script_execution(env_script)

    def xǁWrknvDoctorǁ_check_env_script__mutmut_41(self) -> None:
        """Check if env.sh exists and is valid."""
        env_script = Path.cwd() / "env.sh"

        if not env_script.exists():
            self.checks_failed.append(("env.sh", "Not found - run 'wrknv generate'"))
            return

        # Check if env.sh was generated by wrknv
        content = env_script.read_text()
        is_wrknv_generated = "Generated by wrknv" in content or "WORKENV_DIR=" in content

        if not is_wrknv_generated:
            self.checks_failed.append(
                ("env.sh", "Not generated by wrknv - run 'wrknv generate' to regenerate")
            )
            return

        # Check if it's executable
        if not os.access(env_script, os.X_OK):
            self.checks_warning.append(("env.sh", "Not executable - run 'chmod +x env.sh'"))

        # Check for correct workenv pattern
        system = platform.system().upper()
        machine = platform.machine().lower()
        if machine in ["x86_64", "amd64"]:
            arch = "amd64"
        elif machine in ["arm64", "aarch64"]:
            arch = "arm64"
        else:
            arch = machine

        package_name = Path.cwd().name
        expected_workenv = f"workenv/{package_name}_{system}_{arch}"

        if expected_workenv not in content:
            self.checks_failed.append(
                (
                    "env.sh Workenv Path",
                    f"Incorrect pattern - expected '{expected_workenv}'. Regenerate with 'wrknv generate'",
                )
            )
            return

        # Check required patterns for proper functioning
        required_patterns = [
            "WORKENV_DIR=",
            "UV_INSTALLER_URL=",
            "uv venv",
            "uv sync",
        ]

        missing = []
        for pattern in required_patterns:
            if pattern not in content:
                missing.append(pattern)

        if missing:
            self.checks_failed.append(
                (
                    "env.sh Content",
                    f"Missing required patterns: {', '.join(missing)}. Regenerate with 'wrknv generate'",
                )
            )
        else:
            # Test if env.sh actually works
            self._test_env_script_execution(env_script)

    def xǁWrknvDoctorǁ_check_env_script__mutmut_42(self) -> None:
        """Check if env.sh exists and is valid."""
        env_script = Path.cwd() / "env.sh"

        if not env_script.exists():
            self.checks_failed.append(("env.sh", "Not found - run 'wrknv generate'"))
            return

        # Check if env.sh was generated by wrknv
        content = env_script.read_text()
        is_wrknv_generated = "Generated by wrknv" in content or "WORKENV_DIR=" in content

        if not is_wrknv_generated:
            self.checks_failed.append(
                ("env.sh", "Not generated by wrknv - run 'wrknv generate' to regenerate")
            )
            return

        # Check if it's executable
        if not os.access(env_script, os.X_OK):
            self.checks_warning.append(("env.sh", "Not executable - run 'chmod +x env.sh'"))

        # Check for correct workenv pattern
        system = platform.system().lower()
        machine = None
        if machine in ["x86_64", "amd64"]:
            arch = "amd64"
        elif machine in ["arm64", "aarch64"]:
            arch = "arm64"
        else:
            arch = machine

        package_name = Path.cwd().name
        expected_workenv = f"workenv/{package_name}_{system}_{arch}"

        if expected_workenv not in content:
            self.checks_failed.append(
                (
                    "env.sh Workenv Path",
                    f"Incorrect pattern - expected '{expected_workenv}'. Regenerate with 'wrknv generate'",
                )
            )
            return

        # Check required patterns for proper functioning
        required_patterns = [
            "WORKENV_DIR=",
            "UV_INSTALLER_URL=",
            "uv venv",
            "uv sync",
        ]

        missing = []
        for pattern in required_patterns:
            if pattern not in content:
                missing.append(pattern)

        if missing:
            self.checks_failed.append(
                (
                    "env.sh Content",
                    f"Missing required patterns: {', '.join(missing)}. Regenerate with 'wrknv generate'",
                )
            )
        else:
            # Test if env.sh actually works
            self._test_env_script_execution(env_script)

    def xǁWrknvDoctorǁ_check_env_script__mutmut_43(self) -> None:
        """Check if env.sh exists and is valid."""
        env_script = Path.cwd() / "env.sh"

        if not env_script.exists():
            self.checks_failed.append(("env.sh", "Not found - run 'wrknv generate'"))
            return

        # Check if env.sh was generated by wrknv
        content = env_script.read_text()
        is_wrknv_generated = "Generated by wrknv" in content or "WORKENV_DIR=" in content

        if not is_wrknv_generated:
            self.checks_failed.append(
                ("env.sh", "Not generated by wrknv - run 'wrknv generate' to regenerate")
            )
            return

        # Check if it's executable
        if not os.access(env_script, os.X_OK):
            self.checks_warning.append(("env.sh", "Not executable - run 'chmod +x env.sh'"))

        # Check for correct workenv pattern
        system = platform.system().lower()
        machine = platform.machine().upper()
        if machine in ["x86_64", "amd64"]:
            arch = "amd64"
        elif machine in ["arm64", "aarch64"]:
            arch = "arm64"
        else:
            arch = machine

        package_name = Path.cwd().name
        expected_workenv = f"workenv/{package_name}_{system}_{arch}"

        if expected_workenv not in content:
            self.checks_failed.append(
                (
                    "env.sh Workenv Path",
                    f"Incorrect pattern - expected '{expected_workenv}'. Regenerate with 'wrknv generate'",
                )
            )
            return

        # Check required patterns for proper functioning
        required_patterns = [
            "WORKENV_DIR=",
            "UV_INSTALLER_URL=",
            "uv venv",
            "uv sync",
        ]

        missing = []
        for pattern in required_patterns:
            if pattern not in content:
                missing.append(pattern)

        if missing:
            self.checks_failed.append(
                (
                    "env.sh Content",
                    f"Missing required patterns: {', '.join(missing)}. Regenerate with 'wrknv generate'",
                )
            )
        else:
            # Test if env.sh actually works
            self._test_env_script_execution(env_script)

    def xǁWrknvDoctorǁ_check_env_script__mutmut_44(self) -> None:
        """Check if env.sh exists and is valid."""
        env_script = Path.cwd() / "env.sh"

        if not env_script.exists():
            self.checks_failed.append(("env.sh", "Not found - run 'wrknv generate'"))
            return

        # Check if env.sh was generated by wrknv
        content = env_script.read_text()
        is_wrknv_generated = "Generated by wrknv" in content or "WORKENV_DIR=" in content

        if not is_wrknv_generated:
            self.checks_failed.append(
                ("env.sh", "Not generated by wrknv - run 'wrknv generate' to regenerate")
            )
            return

        # Check if it's executable
        if not os.access(env_script, os.X_OK):
            self.checks_warning.append(("env.sh", "Not executable - run 'chmod +x env.sh'"))

        # Check for correct workenv pattern
        system = platform.system().lower()
        machine = platform.machine().lower()
        if machine not in ["x86_64", "amd64"]:
            arch = "amd64"
        elif machine in ["arm64", "aarch64"]:
            arch = "arm64"
        else:
            arch = machine

        package_name = Path.cwd().name
        expected_workenv = f"workenv/{package_name}_{system}_{arch}"

        if expected_workenv not in content:
            self.checks_failed.append(
                (
                    "env.sh Workenv Path",
                    f"Incorrect pattern - expected '{expected_workenv}'. Regenerate with 'wrknv generate'",
                )
            )
            return

        # Check required patterns for proper functioning
        required_patterns = [
            "WORKENV_DIR=",
            "UV_INSTALLER_URL=",
            "uv venv",
            "uv sync",
        ]

        missing = []
        for pattern in required_patterns:
            if pattern not in content:
                missing.append(pattern)

        if missing:
            self.checks_failed.append(
                (
                    "env.sh Content",
                    f"Missing required patterns: {', '.join(missing)}. Regenerate with 'wrknv generate'",
                )
            )
        else:
            # Test if env.sh actually works
            self._test_env_script_execution(env_script)

    def xǁWrknvDoctorǁ_check_env_script__mutmut_45(self) -> None:
        """Check if env.sh exists and is valid."""
        env_script = Path.cwd() / "env.sh"

        if not env_script.exists():
            self.checks_failed.append(("env.sh", "Not found - run 'wrknv generate'"))
            return

        # Check if env.sh was generated by wrknv
        content = env_script.read_text()
        is_wrknv_generated = "Generated by wrknv" in content or "WORKENV_DIR=" in content

        if not is_wrknv_generated:
            self.checks_failed.append(
                ("env.sh", "Not generated by wrknv - run 'wrknv generate' to regenerate")
            )
            return

        # Check if it's executable
        if not os.access(env_script, os.X_OK):
            self.checks_warning.append(("env.sh", "Not executable - run 'chmod +x env.sh'"))

        # Check for correct workenv pattern
        system = platform.system().lower()
        machine = platform.machine().lower()
        if machine in ["XXx86_64XX", "amd64"]:
            arch = "amd64"
        elif machine in ["arm64", "aarch64"]:
            arch = "arm64"
        else:
            arch = machine

        package_name = Path.cwd().name
        expected_workenv = f"workenv/{package_name}_{system}_{arch}"

        if expected_workenv not in content:
            self.checks_failed.append(
                (
                    "env.sh Workenv Path",
                    f"Incorrect pattern - expected '{expected_workenv}'. Regenerate with 'wrknv generate'",
                )
            )
            return

        # Check required patterns for proper functioning
        required_patterns = [
            "WORKENV_DIR=",
            "UV_INSTALLER_URL=",
            "uv venv",
            "uv sync",
        ]

        missing = []
        for pattern in required_patterns:
            if pattern not in content:
                missing.append(pattern)

        if missing:
            self.checks_failed.append(
                (
                    "env.sh Content",
                    f"Missing required patterns: {', '.join(missing)}. Regenerate with 'wrknv generate'",
                )
            )
        else:
            # Test if env.sh actually works
            self._test_env_script_execution(env_script)

    def xǁWrknvDoctorǁ_check_env_script__mutmut_46(self) -> None:
        """Check if env.sh exists and is valid."""
        env_script = Path.cwd() / "env.sh"

        if not env_script.exists():
            self.checks_failed.append(("env.sh", "Not found - run 'wrknv generate'"))
            return

        # Check if env.sh was generated by wrknv
        content = env_script.read_text()
        is_wrknv_generated = "Generated by wrknv" in content or "WORKENV_DIR=" in content

        if not is_wrknv_generated:
            self.checks_failed.append(
                ("env.sh", "Not generated by wrknv - run 'wrknv generate' to regenerate")
            )
            return

        # Check if it's executable
        if not os.access(env_script, os.X_OK):
            self.checks_warning.append(("env.sh", "Not executable - run 'chmod +x env.sh'"))

        # Check for correct workenv pattern
        system = platform.system().lower()
        machine = platform.machine().lower()
        if machine in ["X86_64", "amd64"]:
            arch = "amd64"
        elif machine in ["arm64", "aarch64"]:
            arch = "arm64"
        else:
            arch = machine

        package_name = Path.cwd().name
        expected_workenv = f"workenv/{package_name}_{system}_{arch}"

        if expected_workenv not in content:
            self.checks_failed.append(
                (
                    "env.sh Workenv Path",
                    f"Incorrect pattern - expected '{expected_workenv}'. Regenerate with 'wrknv generate'",
                )
            )
            return

        # Check required patterns for proper functioning
        required_patterns = [
            "WORKENV_DIR=",
            "UV_INSTALLER_URL=",
            "uv venv",
            "uv sync",
        ]

        missing = []
        for pattern in required_patterns:
            if pattern not in content:
                missing.append(pattern)

        if missing:
            self.checks_failed.append(
                (
                    "env.sh Content",
                    f"Missing required patterns: {', '.join(missing)}. Regenerate with 'wrknv generate'",
                )
            )
        else:
            # Test if env.sh actually works
            self._test_env_script_execution(env_script)

    def xǁWrknvDoctorǁ_check_env_script__mutmut_47(self) -> None:
        """Check if env.sh exists and is valid."""
        env_script = Path.cwd() / "env.sh"

        if not env_script.exists():
            self.checks_failed.append(("env.sh", "Not found - run 'wrknv generate'"))
            return

        # Check if env.sh was generated by wrknv
        content = env_script.read_text()
        is_wrknv_generated = "Generated by wrknv" in content or "WORKENV_DIR=" in content

        if not is_wrknv_generated:
            self.checks_failed.append(
                ("env.sh", "Not generated by wrknv - run 'wrknv generate' to regenerate")
            )
            return

        # Check if it's executable
        if not os.access(env_script, os.X_OK):
            self.checks_warning.append(("env.sh", "Not executable - run 'chmod +x env.sh'"))

        # Check for correct workenv pattern
        system = platform.system().lower()
        machine = platform.machine().lower()
        if machine in ["x86_64", "XXamd64XX"]:
            arch = "amd64"
        elif machine in ["arm64", "aarch64"]:
            arch = "arm64"
        else:
            arch = machine

        package_name = Path.cwd().name
        expected_workenv = f"workenv/{package_name}_{system}_{arch}"

        if expected_workenv not in content:
            self.checks_failed.append(
                (
                    "env.sh Workenv Path",
                    f"Incorrect pattern - expected '{expected_workenv}'. Regenerate with 'wrknv generate'",
                )
            )
            return

        # Check required patterns for proper functioning
        required_patterns = [
            "WORKENV_DIR=",
            "UV_INSTALLER_URL=",
            "uv venv",
            "uv sync",
        ]

        missing = []
        for pattern in required_patterns:
            if pattern not in content:
                missing.append(pattern)

        if missing:
            self.checks_failed.append(
                (
                    "env.sh Content",
                    f"Missing required patterns: {', '.join(missing)}. Regenerate with 'wrknv generate'",
                )
            )
        else:
            # Test if env.sh actually works
            self._test_env_script_execution(env_script)

    def xǁWrknvDoctorǁ_check_env_script__mutmut_48(self) -> None:
        """Check if env.sh exists and is valid."""
        env_script = Path.cwd() / "env.sh"

        if not env_script.exists():
            self.checks_failed.append(("env.sh", "Not found - run 'wrknv generate'"))
            return

        # Check if env.sh was generated by wrknv
        content = env_script.read_text()
        is_wrknv_generated = "Generated by wrknv" in content or "WORKENV_DIR=" in content

        if not is_wrknv_generated:
            self.checks_failed.append(
                ("env.sh", "Not generated by wrknv - run 'wrknv generate' to regenerate")
            )
            return

        # Check if it's executable
        if not os.access(env_script, os.X_OK):
            self.checks_warning.append(("env.sh", "Not executable - run 'chmod +x env.sh'"))

        # Check for correct workenv pattern
        system = platform.system().lower()
        machine = platform.machine().lower()
        if machine in ["x86_64", "AMD64"]:
            arch = "amd64"
        elif machine in ["arm64", "aarch64"]:
            arch = "arm64"
        else:
            arch = machine

        package_name = Path.cwd().name
        expected_workenv = f"workenv/{package_name}_{system}_{arch}"

        if expected_workenv not in content:
            self.checks_failed.append(
                (
                    "env.sh Workenv Path",
                    f"Incorrect pattern - expected '{expected_workenv}'. Regenerate with 'wrknv generate'",
                )
            )
            return

        # Check required patterns for proper functioning
        required_patterns = [
            "WORKENV_DIR=",
            "UV_INSTALLER_URL=",
            "uv venv",
            "uv sync",
        ]

        missing = []
        for pattern in required_patterns:
            if pattern not in content:
                missing.append(pattern)

        if missing:
            self.checks_failed.append(
                (
                    "env.sh Content",
                    f"Missing required patterns: {', '.join(missing)}. Regenerate with 'wrknv generate'",
                )
            )
        else:
            # Test if env.sh actually works
            self._test_env_script_execution(env_script)

    def xǁWrknvDoctorǁ_check_env_script__mutmut_49(self) -> None:
        """Check if env.sh exists and is valid."""
        env_script = Path.cwd() / "env.sh"

        if not env_script.exists():
            self.checks_failed.append(("env.sh", "Not found - run 'wrknv generate'"))
            return

        # Check if env.sh was generated by wrknv
        content = env_script.read_text()
        is_wrknv_generated = "Generated by wrknv" in content or "WORKENV_DIR=" in content

        if not is_wrknv_generated:
            self.checks_failed.append(
                ("env.sh", "Not generated by wrknv - run 'wrknv generate' to regenerate")
            )
            return

        # Check if it's executable
        if not os.access(env_script, os.X_OK):
            self.checks_warning.append(("env.sh", "Not executable - run 'chmod +x env.sh'"))

        # Check for correct workenv pattern
        system = platform.system().lower()
        machine = platform.machine().lower()
        if machine in ["x86_64", "amd64"]:
            arch = None
        elif machine in ["arm64", "aarch64"]:
            arch = "arm64"
        else:
            arch = machine

        package_name = Path.cwd().name
        expected_workenv = f"workenv/{package_name}_{system}_{arch}"

        if expected_workenv not in content:
            self.checks_failed.append(
                (
                    "env.sh Workenv Path",
                    f"Incorrect pattern - expected '{expected_workenv}'. Regenerate with 'wrknv generate'",
                )
            )
            return

        # Check required patterns for proper functioning
        required_patterns = [
            "WORKENV_DIR=",
            "UV_INSTALLER_URL=",
            "uv venv",
            "uv sync",
        ]

        missing = []
        for pattern in required_patterns:
            if pattern not in content:
                missing.append(pattern)

        if missing:
            self.checks_failed.append(
                (
                    "env.sh Content",
                    f"Missing required patterns: {', '.join(missing)}. Regenerate with 'wrknv generate'",
                )
            )
        else:
            # Test if env.sh actually works
            self._test_env_script_execution(env_script)

    def xǁWrknvDoctorǁ_check_env_script__mutmut_50(self) -> None:
        """Check if env.sh exists and is valid."""
        env_script = Path.cwd() / "env.sh"

        if not env_script.exists():
            self.checks_failed.append(("env.sh", "Not found - run 'wrknv generate'"))
            return

        # Check if env.sh was generated by wrknv
        content = env_script.read_text()
        is_wrknv_generated = "Generated by wrknv" in content or "WORKENV_DIR=" in content

        if not is_wrknv_generated:
            self.checks_failed.append(
                ("env.sh", "Not generated by wrknv - run 'wrknv generate' to regenerate")
            )
            return

        # Check if it's executable
        if not os.access(env_script, os.X_OK):
            self.checks_warning.append(("env.sh", "Not executable - run 'chmod +x env.sh'"))

        # Check for correct workenv pattern
        system = platform.system().lower()
        machine = platform.machine().lower()
        if machine in ["x86_64", "amd64"]:
            arch = "XXamd64XX"
        elif machine in ["arm64", "aarch64"]:
            arch = "arm64"
        else:
            arch = machine

        package_name = Path.cwd().name
        expected_workenv = f"workenv/{package_name}_{system}_{arch}"

        if expected_workenv not in content:
            self.checks_failed.append(
                (
                    "env.sh Workenv Path",
                    f"Incorrect pattern - expected '{expected_workenv}'. Regenerate with 'wrknv generate'",
                )
            )
            return

        # Check required patterns for proper functioning
        required_patterns = [
            "WORKENV_DIR=",
            "UV_INSTALLER_URL=",
            "uv venv",
            "uv sync",
        ]

        missing = []
        for pattern in required_patterns:
            if pattern not in content:
                missing.append(pattern)

        if missing:
            self.checks_failed.append(
                (
                    "env.sh Content",
                    f"Missing required patterns: {', '.join(missing)}. Regenerate with 'wrknv generate'",
                )
            )
        else:
            # Test if env.sh actually works
            self._test_env_script_execution(env_script)

    def xǁWrknvDoctorǁ_check_env_script__mutmut_51(self) -> None:
        """Check if env.sh exists and is valid."""
        env_script = Path.cwd() / "env.sh"

        if not env_script.exists():
            self.checks_failed.append(("env.sh", "Not found - run 'wrknv generate'"))
            return

        # Check if env.sh was generated by wrknv
        content = env_script.read_text()
        is_wrknv_generated = "Generated by wrknv" in content or "WORKENV_DIR=" in content

        if not is_wrknv_generated:
            self.checks_failed.append(
                ("env.sh", "Not generated by wrknv - run 'wrknv generate' to regenerate")
            )
            return

        # Check if it's executable
        if not os.access(env_script, os.X_OK):
            self.checks_warning.append(("env.sh", "Not executable - run 'chmod +x env.sh'"))

        # Check for correct workenv pattern
        system = platform.system().lower()
        machine = platform.machine().lower()
        if machine in ["x86_64", "amd64"]:
            arch = "AMD64"
        elif machine in ["arm64", "aarch64"]:
            arch = "arm64"
        else:
            arch = machine

        package_name = Path.cwd().name
        expected_workenv = f"workenv/{package_name}_{system}_{arch}"

        if expected_workenv not in content:
            self.checks_failed.append(
                (
                    "env.sh Workenv Path",
                    f"Incorrect pattern - expected '{expected_workenv}'. Regenerate with 'wrknv generate'",
                )
            )
            return

        # Check required patterns for proper functioning
        required_patterns = [
            "WORKENV_DIR=",
            "UV_INSTALLER_URL=",
            "uv venv",
            "uv sync",
        ]

        missing = []
        for pattern in required_patterns:
            if pattern not in content:
                missing.append(pattern)

        if missing:
            self.checks_failed.append(
                (
                    "env.sh Content",
                    f"Missing required patterns: {', '.join(missing)}. Regenerate with 'wrknv generate'",
                )
            )
        else:
            # Test if env.sh actually works
            self._test_env_script_execution(env_script)

    def xǁWrknvDoctorǁ_check_env_script__mutmut_52(self) -> None:
        """Check if env.sh exists and is valid."""
        env_script = Path.cwd() / "env.sh"

        if not env_script.exists():
            self.checks_failed.append(("env.sh", "Not found - run 'wrknv generate'"))
            return

        # Check if env.sh was generated by wrknv
        content = env_script.read_text()
        is_wrknv_generated = "Generated by wrknv" in content or "WORKENV_DIR=" in content

        if not is_wrknv_generated:
            self.checks_failed.append(
                ("env.sh", "Not generated by wrknv - run 'wrknv generate' to regenerate")
            )
            return

        # Check if it's executable
        if not os.access(env_script, os.X_OK):
            self.checks_warning.append(("env.sh", "Not executable - run 'chmod +x env.sh'"))

        # Check for correct workenv pattern
        system = platform.system().lower()
        machine = platform.machine().lower()
        if machine in ["x86_64", "amd64"]:
            arch = "amd64"
        elif machine not in ["arm64", "aarch64"]:
            arch = "arm64"
        else:
            arch = machine

        package_name = Path.cwd().name
        expected_workenv = f"workenv/{package_name}_{system}_{arch}"

        if expected_workenv not in content:
            self.checks_failed.append(
                (
                    "env.sh Workenv Path",
                    f"Incorrect pattern - expected '{expected_workenv}'. Regenerate with 'wrknv generate'",
                )
            )
            return

        # Check required patterns for proper functioning
        required_patterns = [
            "WORKENV_DIR=",
            "UV_INSTALLER_URL=",
            "uv venv",
            "uv sync",
        ]

        missing = []
        for pattern in required_patterns:
            if pattern not in content:
                missing.append(pattern)

        if missing:
            self.checks_failed.append(
                (
                    "env.sh Content",
                    f"Missing required patterns: {', '.join(missing)}. Regenerate with 'wrknv generate'",
                )
            )
        else:
            # Test if env.sh actually works
            self._test_env_script_execution(env_script)

    def xǁWrknvDoctorǁ_check_env_script__mutmut_53(self) -> None:
        """Check if env.sh exists and is valid."""
        env_script = Path.cwd() / "env.sh"

        if not env_script.exists():
            self.checks_failed.append(("env.sh", "Not found - run 'wrknv generate'"))
            return

        # Check if env.sh was generated by wrknv
        content = env_script.read_text()
        is_wrknv_generated = "Generated by wrknv" in content or "WORKENV_DIR=" in content

        if not is_wrknv_generated:
            self.checks_failed.append(
                ("env.sh", "Not generated by wrknv - run 'wrknv generate' to regenerate")
            )
            return

        # Check if it's executable
        if not os.access(env_script, os.X_OK):
            self.checks_warning.append(("env.sh", "Not executable - run 'chmod +x env.sh'"))

        # Check for correct workenv pattern
        system = platform.system().lower()
        machine = platform.machine().lower()
        if machine in ["x86_64", "amd64"]:
            arch = "amd64"
        elif machine in ["XXarm64XX", "aarch64"]:
            arch = "arm64"
        else:
            arch = machine

        package_name = Path.cwd().name
        expected_workenv = f"workenv/{package_name}_{system}_{arch}"

        if expected_workenv not in content:
            self.checks_failed.append(
                (
                    "env.sh Workenv Path",
                    f"Incorrect pattern - expected '{expected_workenv}'. Regenerate with 'wrknv generate'",
                )
            )
            return

        # Check required patterns for proper functioning
        required_patterns = [
            "WORKENV_DIR=",
            "UV_INSTALLER_URL=",
            "uv venv",
            "uv sync",
        ]

        missing = []
        for pattern in required_patterns:
            if pattern not in content:
                missing.append(pattern)

        if missing:
            self.checks_failed.append(
                (
                    "env.sh Content",
                    f"Missing required patterns: {', '.join(missing)}. Regenerate with 'wrknv generate'",
                )
            )
        else:
            # Test if env.sh actually works
            self._test_env_script_execution(env_script)

    def xǁWrknvDoctorǁ_check_env_script__mutmut_54(self) -> None:
        """Check if env.sh exists and is valid."""
        env_script = Path.cwd() / "env.sh"

        if not env_script.exists():
            self.checks_failed.append(("env.sh", "Not found - run 'wrknv generate'"))
            return

        # Check if env.sh was generated by wrknv
        content = env_script.read_text()
        is_wrknv_generated = "Generated by wrknv" in content or "WORKENV_DIR=" in content

        if not is_wrknv_generated:
            self.checks_failed.append(
                ("env.sh", "Not generated by wrknv - run 'wrknv generate' to regenerate")
            )
            return

        # Check if it's executable
        if not os.access(env_script, os.X_OK):
            self.checks_warning.append(("env.sh", "Not executable - run 'chmod +x env.sh'"))

        # Check for correct workenv pattern
        system = platform.system().lower()
        machine = platform.machine().lower()
        if machine in ["x86_64", "amd64"]:
            arch = "amd64"
        elif machine in ["ARM64", "aarch64"]:
            arch = "arm64"
        else:
            arch = machine

        package_name = Path.cwd().name
        expected_workenv = f"workenv/{package_name}_{system}_{arch}"

        if expected_workenv not in content:
            self.checks_failed.append(
                (
                    "env.sh Workenv Path",
                    f"Incorrect pattern - expected '{expected_workenv}'. Regenerate with 'wrknv generate'",
                )
            )
            return

        # Check required patterns for proper functioning
        required_patterns = [
            "WORKENV_DIR=",
            "UV_INSTALLER_URL=",
            "uv venv",
            "uv sync",
        ]

        missing = []
        for pattern in required_patterns:
            if pattern not in content:
                missing.append(pattern)

        if missing:
            self.checks_failed.append(
                (
                    "env.sh Content",
                    f"Missing required patterns: {', '.join(missing)}. Regenerate with 'wrknv generate'",
                )
            )
        else:
            # Test if env.sh actually works
            self._test_env_script_execution(env_script)

    def xǁWrknvDoctorǁ_check_env_script__mutmut_55(self) -> None:
        """Check if env.sh exists and is valid."""
        env_script = Path.cwd() / "env.sh"

        if not env_script.exists():
            self.checks_failed.append(("env.sh", "Not found - run 'wrknv generate'"))
            return

        # Check if env.sh was generated by wrknv
        content = env_script.read_text()
        is_wrknv_generated = "Generated by wrknv" in content or "WORKENV_DIR=" in content

        if not is_wrknv_generated:
            self.checks_failed.append(
                ("env.sh", "Not generated by wrknv - run 'wrknv generate' to regenerate")
            )
            return

        # Check if it's executable
        if not os.access(env_script, os.X_OK):
            self.checks_warning.append(("env.sh", "Not executable - run 'chmod +x env.sh'"))

        # Check for correct workenv pattern
        system = platform.system().lower()
        machine = platform.machine().lower()
        if machine in ["x86_64", "amd64"]:
            arch = "amd64"
        elif machine in ["arm64", "XXaarch64XX"]:
            arch = "arm64"
        else:
            arch = machine

        package_name = Path.cwd().name
        expected_workenv = f"workenv/{package_name}_{system}_{arch}"

        if expected_workenv not in content:
            self.checks_failed.append(
                (
                    "env.sh Workenv Path",
                    f"Incorrect pattern - expected '{expected_workenv}'. Regenerate with 'wrknv generate'",
                )
            )
            return

        # Check required patterns for proper functioning
        required_patterns = [
            "WORKENV_DIR=",
            "UV_INSTALLER_URL=",
            "uv venv",
            "uv sync",
        ]

        missing = []
        for pattern in required_patterns:
            if pattern not in content:
                missing.append(pattern)

        if missing:
            self.checks_failed.append(
                (
                    "env.sh Content",
                    f"Missing required patterns: {', '.join(missing)}. Regenerate with 'wrknv generate'",
                )
            )
        else:
            # Test if env.sh actually works
            self._test_env_script_execution(env_script)

    def xǁWrknvDoctorǁ_check_env_script__mutmut_56(self) -> None:
        """Check if env.sh exists and is valid."""
        env_script = Path.cwd() / "env.sh"

        if not env_script.exists():
            self.checks_failed.append(("env.sh", "Not found - run 'wrknv generate'"))
            return

        # Check if env.sh was generated by wrknv
        content = env_script.read_text()
        is_wrknv_generated = "Generated by wrknv" in content or "WORKENV_DIR=" in content

        if not is_wrknv_generated:
            self.checks_failed.append(
                ("env.sh", "Not generated by wrknv - run 'wrknv generate' to regenerate")
            )
            return

        # Check if it's executable
        if not os.access(env_script, os.X_OK):
            self.checks_warning.append(("env.sh", "Not executable - run 'chmod +x env.sh'"))

        # Check for correct workenv pattern
        system = platform.system().lower()
        machine = platform.machine().lower()
        if machine in ["x86_64", "amd64"]:
            arch = "amd64"
        elif machine in ["arm64", "AARCH64"]:
            arch = "arm64"
        else:
            arch = machine

        package_name = Path.cwd().name
        expected_workenv = f"workenv/{package_name}_{system}_{arch}"

        if expected_workenv not in content:
            self.checks_failed.append(
                (
                    "env.sh Workenv Path",
                    f"Incorrect pattern - expected '{expected_workenv}'. Regenerate with 'wrknv generate'",
                )
            )
            return

        # Check required patterns for proper functioning
        required_patterns = [
            "WORKENV_DIR=",
            "UV_INSTALLER_URL=",
            "uv venv",
            "uv sync",
        ]

        missing = []
        for pattern in required_patterns:
            if pattern not in content:
                missing.append(pattern)

        if missing:
            self.checks_failed.append(
                (
                    "env.sh Content",
                    f"Missing required patterns: {', '.join(missing)}. Regenerate with 'wrknv generate'",
                )
            )
        else:
            # Test if env.sh actually works
            self._test_env_script_execution(env_script)

    def xǁWrknvDoctorǁ_check_env_script__mutmut_57(self) -> None:
        """Check if env.sh exists and is valid."""
        env_script = Path.cwd() / "env.sh"

        if not env_script.exists():
            self.checks_failed.append(("env.sh", "Not found - run 'wrknv generate'"))
            return

        # Check if env.sh was generated by wrknv
        content = env_script.read_text()
        is_wrknv_generated = "Generated by wrknv" in content or "WORKENV_DIR=" in content

        if not is_wrknv_generated:
            self.checks_failed.append(
                ("env.sh", "Not generated by wrknv - run 'wrknv generate' to regenerate")
            )
            return

        # Check if it's executable
        if not os.access(env_script, os.X_OK):
            self.checks_warning.append(("env.sh", "Not executable - run 'chmod +x env.sh'"))

        # Check for correct workenv pattern
        system = platform.system().lower()
        machine = platform.machine().lower()
        if machine in ["x86_64", "amd64"]:
            arch = "amd64"
        elif machine in ["arm64", "aarch64"]:
            arch = None
        else:
            arch = machine

        package_name = Path.cwd().name
        expected_workenv = f"workenv/{package_name}_{system}_{arch}"

        if expected_workenv not in content:
            self.checks_failed.append(
                (
                    "env.sh Workenv Path",
                    f"Incorrect pattern - expected '{expected_workenv}'. Regenerate with 'wrknv generate'",
                )
            )
            return

        # Check required patterns for proper functioning
        required_patterns = [
            "WORKENV_DIR=",
            "UV_INSTALLER_URL=",
            "uv venv",
            "uv sync",
        ]

        missing = []
        for pattern in required_patterns:
            if pattern not in content:
                missing.append(pattern)

        if missing:
            self.checks_failed.append(
                (
                    "env.sh Content",
                    f"Missing required patterns: {', '.join(missing)}. Regenerate with 'wrknv generate'",
                )
            )
        else:
            # Test if env.sh actually works
            self._test_env_script_execution(env_script)

    def xǁWrknvDoctorǁ_check_env_script__mutmut_58(self) -> None:
        """Check if env.sh exists and is valid."""
        env_script = Path.cwd() / "env.sh"

        if not env_script.exists():
            self.checks_failed.append(("env.sh", "Not found - run 'wrknv generate'"))
            return

        # Check if env.sh was generated by wrknv
        content = env_script.read_text()
        is_wrknv_generated = "Generated by wrknv" in content or "WORKENV_DIR=" in content

        if not is_wrknv_generated:
            self.checks_failed.append(
                ("env.sh", "Not generated by wrknv - run 'wrknv generate' to regenerate")
            )
            return

        # Check if it's executable
        if not os.access(env_script, os.X_OK):
            self.checks_warning.append(("env.sh", "Not executable - run 'chmod +x env.sh'"))

        # Check for correct workenv pattern
        system = platform.system().lower()
        machine = platform.machine().lower()
        if machine in ["x86_64", "amd64"]:
            arch = "amd64"
        elif machine in ["arm64", "aarch64"]:
            arch = "XXarm64XX"
        else:
            arch = machine

        package_name = Path.cwd().name
        expected_workenv = f"workenv/{package_name}_{system}_{arch}"

        if expected_workenv not in content:
            self.checks_failed.append(
                (
                    "env.sh Workenv Path",
                    f"Incorrect pattern - expected '{expected_workenv}'. Regenerate with 'wrknv generate'",
                )
            )
            return

        # Check required patterns for proper functioning
        required_patterns = [
            "WORKENV_DIR=",
            "UV_INSTALLER_URL=",
            "uv venv",
            "uv sync",
        ]

        missing = []
        for pattern in required_patterns:
            if pattern not in content:
                missing.append(pattern)

        if missing:
            self.checks_failed.append(
                (
                    "env.sh Content",
                    f"Missing required patterns: {', '.join(missing)}. Regenerate with 'wrknv generate'",
                )
            )
        else:
            # Test if env.sh actually works
            self._test_env_script_execution(env_script)

    def xǁWrknvDoctorǁ_check_env_script__mutmut_59(self) -> None:
        """Check if env.sh exists and is valid."""
        env_script = Path.cwd() / "env.sh"

        if not env_script.exists():
            self.checks_failed.append(("env.sh", "Not found - run 'wrknv generate'"))
            return

        # Check if env.sh was generated by wrknv
        content = env_script.read_text()
        is_wrknv_generated = "Generated by wrknv" in content or "WORKENV_DIR=" in content

        if not is_wrknv_generated:
            self.checks_failed.append(
                ("env.sh", "Not generated by wrknv - run 'wrknv generate' to regenerate")
            )
            return

        # Check if it's executable
        if not os.access(env_script, os.X_OK):
            self.checks_warning.append(("env.sh", "Not executable - run 'chmod +x env.sh'"))

        # Check for correct workenv pattern
        system = platform.system().lower()
        machine = platform.machine().lower()
        if machine in ["x86_64", "amd64"]:
            arch = "amd64"
        elif machine in ["arm64", "aarch64"]:
            arch = "ARM64"
        else:
            arch = machine

        package_name = Path.cwd().name
        expected_workenv = f"workenv/{package_name}_{system}_{arch}"

        if expected_workenv not in content:
            self.checks_failed.append(
                (
                    "env.sh Workenv Path",
                    f"Incorrect pattern - expected '{expected_workenv}'. Regenerate with 'wrknv generate'",
                )
            )
            return

        # Check required patterns for proper functioning
        required_patterns = [
            "WORKENV_DIR=",
            "UV_INSTALLER_URL=",
            "uv venv",
            "uv sync",
        ]

        missing = []
        for pattern in required_patterns:
            if pattern not in content:
                missing.append(pattern)

        if missing:
            self.checks_failed.append(
                (
                    "env.sh Content",
                    f"Missing required patterns: {', '.join(missing)}. Regenerate with 'wrknv generate'",
                )
            )
        else:
            # Test if env.sh actually works
            self._test_env_script_execution(env_script)

    def xǁWrknvDoctorǁ_check_env_script__mutmut_60(self) -> None:
        """Check if env.sh exists and is valid."""
        env_script = Path.cwd() / "env.sh"

        if not env_script.exists():
            self.checks_failed.append(("env.sh", "Not found - run 'wrknv generate'"))
            return

        # Check if env.sh was generated by wrknv
        content = env_script.read_text()
        is_wrknv_generated = "Generated by wrknv" in content or "WORKENV_DIR=" in content

        if not is_wrknv_generated:
            self.checks_failed.append(
                ("env.sh", "Not generated by wrknv - run 'wrknv generate' to regenerate")
            )
            return

        # Check if it's executable
        if not os.access(env_script, os.X_OK):
            self.checks_warning.append(("env.sh", "Not executable - run 'chmod +x env.sh'"))

        # Check for correct workenv pattern
        system = platform.system().lower()
        machine = platform.machine().lower()
        if machine in ["x86_64", "amd64"]:
            arch = "amd64"
        elif machine in ["arm64", "aarch64"]:
            arch = "arm64"
        else:
            arch = None

        package_name = Path.cwd().name
        expected_workenv = f"workenv/{package_name}_{system}_{arch}"

        if expected_workenv not in content:
            self.checks_failed.append(
                (
                    "env.sh Workenv Path",
                    f"Incorrect pattern - expected '{expected_workenv}'. Regenerate with 'wrknv generate'",
                )
            )
            return

        # Check required patterns for proper functioning
        required_patterns = [
            "WORKENV_DIR=",
            "UV_INSTALLER_URL=",
            "uv venv",
            "uv sync",
        ]

        missing = []
        for pattern in required_patterns:
            if pattern not in content:
                missing.append(pattern)

        if missing:
            self.checks_failed.append(
                (
                    "env.sh Content",
                    f"Missing required patterns: {', '.join(missing)}. Regenerate with 'wrknv generate'",
                )
            )
        else:
            # Test if env.sh actually works
            self._test_env_script_execution(env_script)

    def xǁWrknvDoctorǁ_check_env_script__mutmut_61(self) -> None:
        """Check if env.sh exists and is valid."""
        env_script = Path.cwd() / "env.sh"

        if not env_script.exists():
            self.checks_failed.append(("env.sh", "Not found - run 'wrknv generate'"))
            return

        # Check if env.sh was generated by wrknv
        content = env_script.read_text()
        is_wrknv_generated = "Generated by wrknv" in content or "WORKENV_DIR=" in content

        if not is_wrknv_generated:
            self.checks_failed.append(
                ("env.sh", "Not generated by wrknv - run 'wrknv generate' to regenerate")
            )
            return

        # Check if it's executable
        if not os.access(env_script, os.X_OK):
            self.checks_warning.append(("env.sh", "Not executable - run 'chmod +x env.sh'"))

        # Check for correct workenv pattern
        system = platform.system().lower()
        machine = platform.machine().lower()
        if machine in ["x86_64", "amd64"]:
            arch = "amd64"
        elif machine in ["arm64", "aarch64"]:
            arch = "arm64"
        else:
            arch = machine

        package_name = None
        expected_workenv = f"workenv/{package_name}_{system}_{arch}"

        if expected_workenv not in content:
            self.checks_failed.append(
                (
                    "env.sh Workenv Path",
                    f"Incorrect pattern - expected '{expected_workenv}'. Regenerate with 'wrknv generate'",
                )
            )
            return

        # Check required patterns for proper functioning
        required_patterns = [
            "WORKENV_DIR=",
            "UV_INSTALLER_URL=",
            "uv venv",
            "uv sync",
        ]

        missing = []
        for pattern in required_patterns:
            if pattern not in content:
                missing.append(pattern)

        if missing:
            self.checks_failed.append(
                (
                    "env.sh Content",
                    f"Missing required patterns: {', '.join(missing)}. Regenerate with 'wrknv generate'",
                )
            )
        else:
            # Test if env.sh actually works
            self._test_env_script_execution(env_script)

    def xǁWrknvDoctorǁ_check_env_script__mutmut_62(self) -> None:
        """Check if env.sh exists and is valid."""
        env_script = Path.cwd() / "env.sh"

        if not env_script.exists():
            self.checks_failed.append(("env.sh", "Not found - run 'wrknv generate'"))
            return

        # Check if env.sh was generated by wrknv
        content = env_script.read_text()
        is_wrknv_generated = "Generated by wrknv" in content or "WORKENV_DIR=" in content

        if not is_wrknv_generated:
            self.checks_failed.append(
                ("env.sh", "Not generated by wrknv - run 'wrknv generate' to regenerate")
            )
            return

        # Check if it's executable
        if not os.access(env_script, os.X_OK):
            self.checks_warning.append(("env.sh", "Not executable - run 'chmod +x env.sh'"))

        # Check for correct workenv pattern
        system = platform.system().lower()
        machine = platform.machine().lower()
        if machine in ["x86_64", "amd64"]:
            arch = "amd64"
        elif machine in ["arm64", "aarch64"]:
            arch = "arm64"
        else:
            arch = machine

        package_name = Path.cwd().name
        expected_workenv = None

        if expected_workenv not in content:
            self.checks_failed.append(
                (
                    "env.sh Workenv Path",
                    f"Incorrect pattern - expected '{expected_workenv}'. Regenerate with 'wrknv generate'",
                )
            )
            return

        # Check required patterns for proper functioning
        required_patterns = [
            "WORKENV_DIR=",
            "UV_INSTALLER_URL=",
            "uv venv",
            "uv sync",
        ]

        missing = []
        for pattern in required_patterns:
            if pattern not in content:
                missing.append(pattern)

        if missing:
            self.checks_failed.append(
                (
                    "env.sh Content",
                    f"Missing required patterns: {', '.join(missing)}. Regenerate with 'wrknv generate'",
                )
            )
        else:
            # Test if env.sh actually works
            self._test_env_script_execution(env_script)

    def xǁWrknvDoctorǁ_check_env_script__mutmut_63(self) -> None:
        """Check if env.sh exists and is valid."""
        env_script = Path.cwd() / "env.sh"

        if not env_script.exists():
            self.checks_failed.append(("env.sh", "Not found - run 'wrknv generate'"))
            return

        # Check if env.sh was generated by wrknv
        content = env_script.read_text()
        is_wrknv_generated = "Generated by wrknv" in content or "WORKENV_DIR=" in content

        if not is_wrknv_generated:
            self.checks_failed.append(
                ("env.sh", "Not generated by wrknv - run 'wrknv generate' to regenerate")
            )
            return

        # Check if it's executable
        if not os.access(env_script, os.X_OK):
            self.checks_warning.append(("env.sh", "Not executable - run 'chmod +x env.sh'"))

        # Check for correct workenv pattern
        system = platform.system().lower()
        machine = platform.machine().lower()
        if machine in ["x86_64", "amd64"]:
            arch = "amd64"
        elif machine in ["arm64", "aarch64"]:
            arch = "arm64"
        else:
            arch = machine

        package_name = Path.cwd().name
        expected_workenv = f"workenv/{package_name}_{system}_{arch}"

        if expected_workenv in content:
            self.checks_failed.append(
                (
                    "env.sh Workenv Path",
                    f"Incorrect pattern - expected '{expected_workenv}'. Regenerate with 'wrknv generate'",
                )
            )
            return

        # Check required patterns for proper functioning
        required_patterns = [
            "WORKENV_DIR=",
            "UV_INSTALLER_URL=",
            "uv venv",
            "uv sync",
        ]

        missing = []
        for pattern in required_patterns:
            if pattern not in content:
                missing.append(pattern)

        if missing:
            self.checks_failed.append(
                (
                    "env.sh Content",
                    f"Missing required patterns: {', '.join(missing)}. Regenerate with 'wrknv generate'",
                )
            )
        else:
            # Test if env.sh actually works
            self._test_env_script_execution(env_script)

    def xǁWrknvDoctorǁ_check_env_script__mutmut_64(self) -> None:
        """Check if env.sh exists and is valid."""
        env_script = Path.cwd() / "env.sh"

        if not env_script.exists():
            self.checks_failed.append(("env.sh", "Not found - run 'wrknv generate'"))
            return

        # Check if env.sh was generated by wrknv
        content = env_script.read_text()
        is_wrknv_generated = "Generated by wrknv" in content or "WORKENV_DIR=" in content

        if not is_wrknv_generated:
            self.checks_failed.append(
                ("env.sh", "Not generated by wrknv - run 'wrknv generate' to regenerate")
            )
            return

        # Check if it's executable
        if not os.access(env_script, os.X_OK):
            self.checks_warning.append(("env.sh", "Not executable - run 'chmod +x env.sh'"))

        # Check for correct workenv pattern
        system = platform.system().lower()
        machine = platform.machine().lower()
        if machine in ["x86_64", "amd64"]:
            arch = "amd64"
        elif machine in ["arm64", "aarch64"]:
            arch = "arm64"
        else:
            arch = machine

        package_name = Path.cwd().name
        expected_workenv = f"workenv/{package_name}_{system}_{arch}"

        if expected_workenv not in content:
            self.checks_failed.append(
                None
            )
            return

        # Check required patterns for proper functioning
        required_patterns = [
            "WORKENV_DIR=",
            "UV_INSTALLER_URL=",
            "uv venv",
            "uv sync",
        ]

        missing = []
        for pattern in required_patterns:
            if pattern not in content:
                missing.append(pattern)

        if missing:
            self.checks_failed.append(
                (
                    "env.sh Content",
                    f"Missing required patterns: {', '.join(missing)}. Regenerate with 'wrknv generate'",
                )
            )
        else:
            # Test if env.sh actually works
            self._test_env_script_execution(env_script)

    def xǁWrknvDoctorǁ_check_env_script__mutmut_65(self) -> None:
        """Check if env.sh exists and is valid."""
        env_script = Path.cwd() / "env.sh"

        if not env_script.exists():
            self.checks_failed.append(("env.sh", "Not found - run 'wrknv generate'"))
            return

        # Check if env.sh was generated by wrknv
        content = env_script.read_text()
        is_wrknv_generated = "Generated by wrknv" in content or "WORKENV_DIR=" in content

        if not is_wrknv_generated:
            self.checks_failed.append(
                ("env.sh", "Not generated by wrknv - run 'wrknv generate' to regenerate")
            )
            return

        # Check if it's executable
        if not os.access(env_script, os.X_OK):
            self.checks_warning.append(("env.sh", "Not executable - run 'chmod +x env.sh'"))

        # Check for correct workenv pattern
        system = platform.system().lower()
        machine = platform.machine().lower()
        if machine in ["x86_64", "amd64"]:
            arch = "amd64"
        elif machine in ["arm64", "aarch64"]:
            arch = "arm64"
        else:
            arch = machine

        package_name = Path.cwd().name
        expected_workenv = f"workenv/{package_name}_{system}_{arch}"

        if expected_workenv not in content:
            self.checks_failed.append(
                (
                    "XXenv.sh Workenv PathXX",
                    f"Incorrect pattern - expected '{expected_workenv}'. Regenerate with 'wrknv generate'",
                )
            )
            return

        # Check required patterns for proper functioning
        required_patterns = [
            "WORKENV_DIR=",
            "UV_INSTALLER_URL=",
            "uv venv",
            "uv sync",
        ]

        missing = []
        for pattern in required_patterns:
            if pattern not in content:
                missing.append(pattern)

        if missing:
            self.checks_failed.append(
                (
                    "env.sh Content",
                    f"Missing required patterns: {', '.join(missing)}. Regenerate with 'wrknv generate'",
                )
            )
        else:
            # Test if env.sh actually works
            self._test_env_script_execution(env_script)

    def xǁWrknvDoctorǁ_check_env_script__mutmut_66(self) -> None:
        """Check if env.sh exists and is valid."""
        env_script = Path.cwd() / "env.sh"

        if not env_script.exists():
            self.checks_failed.append(("env.sh", "Not found - run 'wrknv generate'"))
            return

        # Check if env.sh was generated by wrknv
        content = env_script.read_text()
        is_wrknv_generated = "Generated by wrknv" in content or "WORKENV_DIR=" in content

        if not is_wrknv_generated:
            self.checks_failed.append(
                ("env.sh", "Not generated by wrknv - run 'wrknv generate' to regenerate")
            )
            return

        # Check if it's executable
        if not os.access(env_script, os.X_OK):
            self.checks_warning.append(("env.sh", "Not executable - run 'chmod +x env.sh'"))

        # Check for correct workenv pattern
        system = platform.system().lower()
        machine = platform.machine().lower()
        if machine in ["x86_64", "amd64"]:
            arch = "amd64"
        elif machine in ["arm64", "aarch64"]:
            arch = "arm64"
        else:
            arch = machine

        package_name = Path.cwd().name
        expected_workenv = f"workenv/{package_name}_{system}_{arch}"

        if expected_workenv not in content:
            self.checks_failed.append(
                (
                    "env.sh workenv path",
                    f"Incorrect pattern - expected '{expected_workenv}'. Regenerate with 'wrknv generate'",
                )
            )
            return

        # Check required patterns for proper functioning
        required_patterns = [
            "WORKENV_DIR=",
            "UV_INSTALLER_URL=",
            "uv venv",
            "uv sync",
        ]

        missing = []
        for pattern in required_patterns:
            if pattern not in content:
                missing.append(pattern)

        if missing:
            self.checks_failed.append(
                (
                    "env.sh Content",
                    f"Missing required patterns: {', '.join(missing)}. Regenerate with 'wrknv generate'",
                )
            )
        else:
            # Test if env.sh actually works
            self._test_env_script_execution(env_script)

    def xǁWrknvDoctorǁ_check_env_script__mutmut_67(self) -> None:
        """Check if env.sh exists and is valid."""
        env_script = Path.cwd() / "env.sh"

        if not env_script.exists():
            self.checks_failed.append(("env.sh", "Not found - run 'wrknv generate'"))
            return

        # Check if env.sh was generated by wrknv
        content = env_script.read_text()
        is_wrknv_generated = "Generated by wrknv" in content or "WORKENV_DIR=" in content

        if not is_wrknv_generated:
            self.checks_failed.append(
                ("env.sh", "Not generated by wrknv - run 'wrknv generate' to regenerate")
            )
            return

        # Check if it's executable
        if not os.access(env_script, os.X_OK):
            self.checks_warning.append(("env.sh", "Not executable - run 'chmod +x env.sh'"))

        # Check for correct workenv pattern
        system = platform.system().lower()
        machine = platform.machine().lower()
        if machine in ["x86_64", "amd64"]:
            arch = "amd64"
        elif machine in ["arm64", "aarch64"]:
            arch = "arm64"
        else:
            arch = machine

        package_name = Path.cwd().name
        expected_workenv = f"workenv/{package_name}_{system}_{arch}"

        if expected_workenv not in content:
            self.checks_failed.append(
                (
                    "ENV.SH WORKENV PATH",
                    f"Incorrect pattern - expected '{expected_workenv}'. Regenerate with 'wrknv generate'",
                )
            )
            return

        # Check required patterns for proper functioning
        required_patterns = [
            "WORKENV_DIR=",
            "UV_INSTALLER_URL=",
            "uv venv",
            "uv sync",
        ]

        missing = []
        for pattern in required_patterns:
            if pattern not in content:
                missing.append(pattern)

        if missing:
            self.checks_failed.append(
                (
                    "env.sh Content",
                    f"Missing required patterns: {', '.join(missing)}. Regenerate with 'wrknv generate'",
                )
            )
        else:
            # Test if env.sh actually works
            self._test_env_script_execution(env_script)

    def xǁWrknvDoctorǁ_check_env_script__mutmut_68(self) -> None:
        """Check if env.sh exists and is valid."""
        env_script = Path.cwd() / "env.sh"

        if not env_script.exists():
            self.checks_failed.append(("env.sh", "Not found - run 'wrknv generate'"))
            return

        # Check if env.sh was generated by wrknv
        content = env_script.read_text()
        is_wrknv_generated = "Generated by wrknv" in content or "WORKENV_DIR=" in content

        if not is_wrknv_generated:
            self.checks_failed.append(
                ("env.sh", "Not generated by wrknv - run 'wrknv generate' to regenerate")
            )
            return

        # Check if it's executable
        if not os.access(env_script, os.X_OK):
            self.checks_warning.append(("env.sh", "Not executable - run 'chmod +x env.sh'"))

        # Check for correct workenv pattern
        system = platform.system().lower()
        machine = platform.machine().lower()
        if machine in ["x86_64", "amd64"]:
            arch = "amd64"
        elif machine in ["arm64", "aarch64"]:
            arch = "arm64"
        else:
            arch = machine

        package_name = Path.cwd().name
        expected_workenv = f"workenv/{package_name}_{system}_{arch}"

        if expected_workenv not in content:
            self.checks_failed.append(
                (
                    "env.sh Workenv Path",
                    f"Incorrect pattern - expected '{expected_workenv}'. Regenerate with 'wrknv generate'",
                )
            )
            return

        # Check required patterns for proper functioning
        required_patterns = None

        missing = []
        for pattern in required_patterns:
            if pattern not in content:
                missing.append(pattern)

        if missing:
            self.checks_failed.append(
                (
                    "env.sh Content",
                    f"Missing required patterns: {', '.join(missing)}. Regenerate with 'wrknv generate'",
                )
            )
        else:
            # Test if env.sh actually works
            self._test_env_script_execution(env_script)

    def xǁWrknvDoctorǁ_check_env_script__mutmut_69(self) -> None:
        """Check if env.sh exists and is valid."""
        env_script = Path.cwd() / "env.sh"

        if not env_script.exists():
            self.checks_failed.append(("env.sh", "Not found - run 'wrknv generate'"))
            return

        # Check if env.sh was generated by wrknv
        content = env_script.read_text()
        is_wrknv_generated = "Generated by wrknv" in content or "WORKENV_DIR=" in content

        if not is_wrknv_generated:
            self.checks_failed.append(
                ("env.sh", "Not generated by wrknv - run 'wrknv generate' to regenerate")
            )
            return

        # Check if it's executable
        if not os.access(env_script, os.X_OK):
            self.checks_warning.append(("env.sh", "Not executable - run 'chmod +x env.sh'"))

        # Check for correct workenv pattern
        system = platform.system().lower()
        machine = platform.machine().lower()
        if machine in ["x86_64", "amd64"]:
            arch = "amd64"
        elif machine in ["arm64", "aarch64"]:
            arch = "arm64"
        else:
            arch = machine

        package_name = Path.cwd().name
        expected_workenv = f"workenv/{package_name}_{system}_{arch}"

        if expected_workenv not in content:
            self.checks_failed.append(
                (
                    "env.sh Workenv Path",
                    f"Incorrect pattern - expected '{expected_workenv}'. Regenerate with 'wrknv generate'",
                )
            )
            return

        # Check required patterns for proper functioning
        required_patterns = [
            "XXWORKENV_DIR=XX",
            "UV_INSTALLER_URL=",
            "uv venv",
            "uv sync",
        ]

        missing = []
        for pattern in required_patterns:
            if pattern not in content:
                missing.append(pattern)

        if missing:
            self.checks_failed.append(
                (
                    "env.sh Content",
                    f"Missing required patterns: {', '.join(missing)}. Regenerate with 'wrknv generate'",
                )
            )
        else:
            # Test if env.sh actually works
            self._test_env_script_execution(env_script)

    def xǁWrknvDoctorǁ_check_env_script__mutmut_70(self) -> None:
        """Check if env.sh exists and is valid."""
        env_script = Path.cwd() / "env.sh"

        if not env_script.exists():
            self.checks_failed.append(("env.sh", "Not found - run 'wrknv generate'"))
            return

        # Check if env.sh was generated by wrknv
        content = env_script.read_text()
        is_wrknv_generated = "Generated by wrknv" in content or "WORKENV_DIR=" in content

        if not is_wrknv_generated:
            self.checks_failed.append(
                ("env.sh", "Not generated by wrknv - run 'wrknv generate' to regenerate")
            )
            return

        # Check if it's executable
        if not os.access(env_script, os.X_OK):
            self.checks_warning.append(("env.sh", "Not executable - run 'chmod +x env.sh'"))

        # Check for correct workenv pattern
        system = platform.system().lower()
        machine = platform.machine().lower()
        if machine in ["x86_64", "amd64"]:
            arch = "amd64"
        elif machine in ["arm64", "aarch64"]:
            arch = "arm64"
        else:
            arch = machine

        package_name = Path.cwd().name
        expected_workenv = f"workenv/{package_name}_{system}_{arch}"

        if expected_workenv not in content:
            self.checks_failed.append(
                (
                    "env.sh Workenv Path",
                    f"Incorrect pattern - expected '{expected_workenv}'. Regenerate with 'wrknv generate'",
                )
            )
            return

        # Check required patterns for proper functioning
        required_patterns = [
            "workenv_dir=",
            "UV_INSTALLER_URL=",
            "uv venv",
            "uv sync",
        ]

        missing = []
        for pattern in required_patterns:
            if pattern not in content:
                missing.append(pattern)

        if missing:
            self.checks_failed.append(
                (
                    "env.sh Content",
                    f"Missing required patterns: {', '.join(missing)}. Regenerate with 'wrknv generate'",
                )
            )
        else:
            # Test if env.sh actually works
            self._test_env_script_execution(env_script)

    def xǁWrknvDoctorǁ_check_env_script__mutmut_71(self) -> None:
        """Check if env.sh exists and is valid."""
        env_script = Path.cwd() / "env.sh"

        if not env_script.exists():
            self.checks_failed.append(("env.sh", "Not found - run 'wrknv generate'"))
            return

        # Check if env.sh was generated by wrknv
        content = env_script.read_text()
        is_wrknv_generated = "Generated by wrknv" in content or "WORKENV_DIR=" in content

        if not is_wrknv_generated:
            self.checks_failed.append(
                ("env.sh", "Not generated by wrknv - run 'wrknv generate' to regenerate")
            )
            return

        # Check if it's executable
        if not os.access(env_script, os.X_OK):
            self.checks_warning.append(("env.sh", "Not executable - run 'chmod +x env.sh'"))

        # Check for correct workenv pattern
        system = platform.system().lower()
        machine = platform.machine().lower()
        if machine in ["x86_64", "amd64"]:
            arch = "amd64"
        elif machine in ["arm64", "aarch64"]:
            arch = "arm64"
        else:
            arch = machine

        package_name = Path.cwd().name
        expected_workenv = f"workenv/{package_name}_{system}_{arch}"

        if expected_workenv not in content:
            self.checks_failed.append(
                (
                    "env.sh Workenv Path",
                    f"Incorrect pattern - expected '{expected_workenv}'. Regenerate with 'wrknv generate'",
                )
            )
            return

        # Check required patterns for proper functioning
        required_patterns = [
            "WORKENV_DIR=",
            "XXUV_INSTALLER_URL=XX",
            "uv venv",
            "uv sync",
        ]

        missing = []
        for pattern in required_patterns:
            if pattern not in content:
                missing.append(pattern)

        if missing:
            self.checks_failed.append(
                (
                    "env.sh Content",
                    f"Missing required patterns: {', '.join(missing)}. Regenerate with 'wrknv generate'",
                )
            )
        else:
            # Test if env.sh actually works
            self._test_env_script_execution(env_script)

    def xǁWrknvDoctorǁ_check_env_script__mutmut_72(self) -> None:
        """Check if env.sh exists and is valid."""
        env_script = Path.cwd() / "env.sh"

        if not env_script.exists():
            self.checks_failed.append(("env.sh", "Not found - run 'wrknv generate'"))
            return

        # Check if env.sh was generated by wrknv
        content = env_script.read_text()
        is_wrknv_generated = "Generated by wrknv" in content or "WORKENV_DIR=" in content

        if not is_wrknv_generated:
            self.checks_failed.append(
                ("env.sh", "Not generated by wrknv - run 'wrknv generate' to regenerate")
            )
            return

        # Check if it's executable
        if not os.access(env_script, os.X_OK):
            self.checks_warning.append(("env.sh", "Not executable - run 'chmod +x env.sh'"))

        # Check for correct workenv pattern
        system = platform.system().lower()
        machine = platform.machine().lower()
        if machine in ["x86_64", "amd64"]:
            arch = "amd64"
        elif machine in ["arm64", "aarch64"]:
            arch = "arm64"
        else:
            arch = machine

        package_name = Path.cwd().name
        expected_workenv = f"workenv/{package_name}_{system}_{arch}"

        if expected_workenv not in content:
            self.checks_failed.append(
                (
                    "env.sh Workenv Path",
                    f"Incorrect pattern - expected '{expected_workenv}'. Regenerate with 'wrknv generate'",
                )
            )
            return

        # Check required patterns for proper functioning
        required_patterns = [
            "WORKENV_DIR=",
            "uv_installer_url=",
            "uv venv",
            "uv sync",
        ]

        missing = []
        for pattern in required_patterns:
            if pattern not in content:
                missing.append(pattern)

        if missing:
            self.checks_failed.append(
                (
                    "env.sh Content",
                    f"Missing required patterns: {', '.join(missing)}. Regenerate with 'wrknv generate'",
                )
            )
        else:
            # Test if env.sh actually works
            self._test_env_script_execution(env_script)

    def xǁWrknvDoctorǁ_check_env_script__mutmut_73(self) -> None:
        """Check if env.sh exists and is valid."""
        env_script = Path.cwd() / "env.sh"

        if not env_script.exists():
            self.checks_failed.append(("env.sh", "Not found - run 'wrknv generate'"))
            return

        # Check if env.sh was generated by wrknv
        content = env_script.read_text()
        is_wrknv_generated = "Generated by wrknv" in content or "WORKENV_DIR=" in content

        if not is_wrknv_generated:
            self.checks_failed.append(
                ("env.sh", "Not generated by wrknv - run 'wrknv generate' to regenerate")
            )
            return

        # Check if it's executable
        if not os.access(env_script, os.X_OK):
            self.checks_warning.append(("env.sh", "Not executable - run 'chmod +x env.sh'"))

        # Check for correct workenv pattern
        system = platform.system().lower()
        machine = platform.machine().lower()
        if machine in ["x86_64", "amd64"]:
            arch = "amd64"
        elif machine in ["arm64", "aarch64"]:
            arch = "arm64"
        else:
            arch = machine

        package_name = Path.cwd().name
        expected_workenv = f"workenv/{package_name}_{system}_{arch}"

        if expected_workenv not in content:
            self.checks_failed.append(
                (
                    "env.sh Workenv Path",
                    f"Incorrect pattern - expected '{expected_workenv}'. Regenerate with 'wrknv generate'",
                )
            )
            return

        # Check required patterns for proper functioning
        required_patterns = [
            "WORKENV_DIR=",
            "UV_INSTALLER_URL=",
            "XXuv venvXX",
            "uv sync",
        ]

        missing = []
        for pattern in required_patterns:
            if pattern not in content:
                missing.append(pattern)

        if missing:
            self.checks_failed.append(
                (
                    "env.sh Content",
                    f"Missing required patterns: {', '.join(missing)}. Regenerate with 'wrknv generate'",
                )
            )
        else:
            # Test if env.sh actually works
            self._test_env_script_execution(env_script)

    def xǁWrknvDoctorǁ_check_env_script__mutmut_74(self) -> None:
        """Check if env.sh exists and is valid."""
        env_script = Path.cwd() / "env.sh"

        if not env_script.exists():
            self.checks_failed.append(("env.sh", "Not found - run 'wrknv generate'"))
            return

        # Check if env.sh was generated by wrknv
        content = env_script.read_text()
        is_wrknv_generated = "Generated by wrknv" in content or "WORKENV_DIR=" in content

        if not is_wrknv_generated:
            self.checks_failed.append(
                ("env.sh", "Not generated by wrknv - run 'wrknv generate' to regenerate")
            )
            return

        # Check if it's executable
        if not os.access(env_script, os.X_OK):
            self.checks_warning.append(("env.sh", "Not executable - run 'chmod +x env.sh'"))

        # Check for correct workenv pattern
        system = platform.system().lower()
        machine = platform.machine().lower()
        if machine in ["x86_64", "amd64"]:
            arch = "amd64"
        elif machine in ["arm64", "aarch64"]:
            arch = "arm64"
        else:
            arch = machine

        package_name = Path.cwd().name
        expected_workenv = f"workenv/{package_name}_{system}_{arch}"

        if expected_workenv not in content:
            self.checks_failed.append(
                (
                    "env.sh Workenv Path",
                    f"Incorrect pattern - expected '{expected_workenv}'. Regenerate with 'wrknv generate'",
                )
            )
            return

        # Check required patterns for proper functioning
        required_patterns = [
            "WORKENV_DIR=",
            "UV_INSTALLER_URL=",
            "UV VENV",
            "uv sync",
        ]

        missing = []
        for pattern in required_patterns:
            if pattern not in content:
                missing.append(pattern)

        if missing:
            self.checks_failed.append(
                (
                    "env.sh Content",
                    f"Missing required patterns: {', '.join(missing)}. Regenerate with 'wrknv generate'",
                )
            )
        else:
            # Test if env.sh actually works
            self._test_env_script_execution(env_script)

    def xǁWrknvDoctorǁ_check_env_script__mutmut_75(self) -> None:
        """Check if env.sh exists and is valid."""
        env_script = Path.cwd() / "env.sh"

        if not env_script.exists():
            self.checks_failed.append(("env.sh", "Not found - run 'wrknv generate'"))
            return

        # Check if env.sh was generated by wrknv
        content = env_script.read_text()
        is_wrknv_generated = "Generated by wrknv" in content or "WORKENV_DIR=" in content

        if not is_wrknv_generated:
            self.checks_failed.append(
                ("env.sh", "Not generated by wrknv - run 'wrknv generate' to regenerate")
            )
            return

        # Check if it's executable
        if not os.access(env_script, os.X_OK):
            self.checks_warning.append(("env.sh", "Not executable - run 'chmod +x env.sh'"))

        # Check for correct workenv pattern
        system = platform.system().lower()
        machine = platform.machine().lower()
        if machine in ["x86_64", "amd64"]:
            arch = "amd64"
        elif machine in ["arm64", "aarch64"]:
            arch = "arm64"
        else:
            arch = machine

        package_name = Path.cwd().name
        expected_workenv = f"workenv/{package_name}_{system}_{arch}"

        if expected_workenv not in content:
            self.checks_failed.append(
                (
                    "env.sh Workenv Path",
                    f"Incorrect pattern - expected '{expected_workenv}'. Regenerate with 'wrknv generate'",
                )
            )
            return

        # Check required patterns for proper functioning
        required_patterns = [
            "WORKENV_DIR=",
            "UV_INSTALLER_URL=",
            "uv venv",
            "XXuv syncXX",
        ]

        missing = []
        for pattern in required_patterns:
            if pattern not in content:
                missing.append(pattern)

        if missing:
            self.checks_failed.append(
                (
                    "env.sh Content",
                    f"Missing required patterns: {', '.join(missing)}. Regenerate with 'wrknv generate'",
                )
            )
        else:
            # Test if env.sh actually works
            self._test_env_script_execution(env_script)

    def xǁWrknvDoctorǁ_check_env_script__mutmut_76(self) -> None:
        """Check if env.sh exists and is valid."""
        env_script = Path.cwd() / "env.sh"

        if not env_script.exists():
            self.checks_failed.append(("env.sh", "Not found - run 'wrknv generate'"))
            return

        # Check if env.sh was generated by wrknv
        content = env_script.read_text()
        is_wrknv_generated = "Generated by wrknv" in content or "WORKENV_DIR=" in content

        if not is_wrknv_generated:
            self.checks_failed.append(
                ("env.sh", "Not generated by wrknv - run 'wrknv generate' to regenerate")
            )
            return

        # Check if it's executable
        if not os.access(env_script, os.X_OK):
            self.checks_warning.append(("env.sh", "Not executable - run 'chmod +x env.sh'"))

        # Check for correct workenv pattern
        system = platform.system().lower()
        machine = platform.machine().lower()
        if machine in ["x86_64", "amd64"]:
            arch = "amd64"
        elif machine in ["arm64", "aarch64"]:
            arch = "arm64"
        else:
            arch = machine

        package_name = Path.cwd().name
        expected_workenv = f"workenv/{package_name}_{system}_{arch}"

        if expected_workenv not in content:
            self.checks_failed.append(
                (
                    "env.sh Workenv Path",
                    f"Incorrect pattern - expected '{expected_workenv}'. Regenerate with 'wrknv generate'",
                )
            )
            return

        # Check required patterns for proper functioning
        required_patterns = [
            "WORKENV_DIR=",
            "UV_INSTALLER_URL=",
            "uv venv",
            "UV SYNC",
        ]

        missing = []
        for pattern in required_patterns:
            if pattern not in content:
                missing.append(pattern)

        if missing:
            self.checks_failed.append(
                (
                    "env.sh Content",
                    f"Missing required patterns: {', '.join(missing)}. Regenerate with 'wrknv generate'",
                )
            )
        else:
            # Test if env.sh actually works
            self._test_env_script_execution(env_script)

    def xǁWrknvDoctorǁ_check_env_script__mutmut_77(self) -> None:
        """Check if env.sh exists and is valid."""
        env_script = Path.cwd() / "env.sh"

        if not env_script.exists():
            self.checks_failed.append(("env.sh", "Not found - run 'wrknv generate'"))
            return

        # Check if env.sh was generated by wrknv
        content = env_script.read_text()
        is_wrknv_generated = "Generated by wrknv" in content or "WORKENV_DIR=" in content

        if not is_wrknv_generated:
            self.checks_failed.append(
                ("env.sh", "Not generated by wrknv - run 'wrknv generate' to regenerate")
            )
            return

        # Check if it's executable
        if not os.access(env_script, os.X_OK):
            self.checks_warning.append(("env.sh", "Not executable - run 'chmod +x env.sh'"))

        # Check for correct workenv pattern
        system = platform.system().lower()
        machine = platform.machine().lower()
        if machine in ["x86_64", "amd64"]:
            arch = "amd64"
        elif machine in ["arm64", "aarch64"]:
            arch = "arm64"
        else:
            arch = machine

        package_name = Path.cwd().name
        expected_workenv = f"workenv/{package_name}_{system}_{arch}"

        if expected_workenv not in content:
            self.checks_failed.append(
                (
                    "env.sh Workenv Path",
                    f"Incorrect pattern - expected '{expected_workenv}'. Regenerate with 'wrknv generate'",
                )
            )
            return

        # Check required patterns for proper functioning
        required_patterns = [
            "WORKENV_DIR=",
            "UV_INSTALLER_URL=",
            "uv venv",
            "uv sync",
        ]

        missing = None
        for pattern in required_patterns:
            if pattern not in content:
                missing.append(pattern)

        if missing:
            self.checks_failed.append(
                (
                    "env.sh Content",
                    f"Missing required patterns: {', '.join(missing)}. Regenerate with 'wrknv generate'",
                )
            )
        else:
            # Test if env.sh actually works
            self._test_env_script_execution(env_script)

    def xǁWrknvDoctorǁ_check_env_script__mutmut_78(self) -> None:
        """Check if env.sh exists and is valid."""
        env_script = Path.cwd() / "env.sh"

        if not env_script.exists():
            self.checks_failed.append(("env.sh", "Not found - run 'wrknv generate'"))
            return

        # Check if env.sh was generated by wrknv
        content = env_script.read_text()
        is_wrknv_generated = "Generated by wrknv" in content or "WORKENV_DIR=" in content

        if not is_wrknv_generated:
            self.checks_failed.append(
                ("env.sh", "Not generated by wrknv - run 'wrknv generate' to regenerate")
            )
            return

        # Check if it's executable
        if not os.access(env_script, os.X_OK):
            self.checks_warning.append(("env.sh", "Not executable - run 'chmod +x env.sh'"))

        # Check for correct workenv pattern
        system = platform.system().lower()
        machine = platform.machine().lower()
        if machine in ["x86_64", "amd64"]:
            arch = "amd64"
        elif machine in ["arm64", "aarch64"]:
            arch = "arm64"
        else:
            arch = machine

        package_name = Path.cwd().name
        expected_workenv = f"workenv/{package_name}_{system}_{arch}"

        if expected_workenv not in content:
            self.checks_failed.append(
                (
                    "env.sh Workenv Path",
                    f"Incorrect pattern - expected '{expected_workenv}'. Regenerate with 'wrknv generate'",
                )
            )
            return

        # Check required patterns for proper functioning
        required_patterns = [
            "WORKENV_DIR=",
            "UV_INSTALLER_URL=",
            "uv venv",
            "uv sync",
        ]

        missing = []
        for pattern in required_patterns:
            if pattern in content:
                missing.append(pattern)

        if missing:
            self.checks_failed.append(
                (
                    "env.sh Content",
                    f"Missing required patterns: {', '.join(missing)}. Regenerate with 'wrknv generate'",
                )
            )
        else:
            # Test if env.sh actually works
            self._test_env_script_execution(env_script)

    def xǁWrknvDoctorǁ_check_env_script__mutmut_79(self) -> None:
        """Check if env.sh exists and is valid."""
        env_script = Path.cwd() / "env.sh"

        if not env_script.exists():
            self.checks_failed.append(("env.sh", "Not found - run 'wrknv generate'"))
            return

        # Check if env.sh was generated by wrknv
        content = env_script.read_text()
        is_wrknv_generated = "Generated by wrknv" in content or "WORKENV_DIR=" in content

        if not is_wrknv_generated:
            self.checks_failed.append(
                ("env.sh", "Not generated by wrknv - run 'wrknv generate' to regenerate")
            )
            return

        # Check if it's executable
        if not os.access(env_script, os.X_OK):
            self.checks_warning.append(("env.sh", "Not executable - run 'chmod +x env.sh'"))

        # Check for correct workenv pattern
        system = platform.system().lower()
        machine = platform.machine().lower()
        if machine in ["x86_64", "amd64"]:
            arch = "amd64"
        elif machine in ["arm64", "aarch64"]:
            arch = "arm64"
        else:
            arch = machine

        package_name = Path.cwd().name
        expected_workenv = f"workenv/{package_name}_{system}_{arch}"

        if expected_workenv not in content:
            self.checks_failed.append(
                (
                    "env.sh Workenv Path",
                    f"Incorrect pattern - expected '{expected_workenv}'. Regenerate with 'wrknv generate'",
                )
            )
            return

        # Check required patterns for proper functioning
        required_patterns = [
            "WORKENV_DIR=",
            "UV_INSTALLER_URL=",
            "uv venv",
            "uv sync",
        ]

        missing = []
        for pattern in required_patterns:
            if pattern not in content:
                missing.append(None)

        if missing:
            self.checks_failed.append(
                (
                    "env.sh Content",
                    f"Missing required patterns: {', '.join(missing)}. Regenerate with 'wrknv generate'",
                )
            )
        else:
            # Test if env.sh actually works
            self._test_env_script_execution(env_script)

    def xǁWrknvDoctorǁ_check_env_script__mutmut_80(self) -> None:
        """Check if env.sh exists and is valid."""
        env_script = Path.cwd() / "env.sh"

        if not env_script.exists():
            self.checks_failed.append(("env.sh", "Not found - run 'wrknv generate'"))
            return

        # Check if env.sh was generated by wrknv
        content = env_script.read_text()
        is_wrknv_generated = "Generated by wrknv" in content or "WORKENV_DIR=" in content

        if not is_wrknv_generated:
            self.checks_failed.append(
                ("env.sh", "Not generated by wrknv - run 'wrknv generate' to regenerate")
            )
            return

        # Check if it's executable
        if not os.access(env_script, os.X_OK):
            self.checks_warning.append(("env.sh", "Not executable - run 'chmod +x env.sh'"))

        # Check for correct workenv pattern
        system = platform.system().lower()
        machine = platform.machine().lower()
        if machine in ["x86_64", "amd64"]:
            arch = "amd64"
        elif machine in ["arm64", "aarch64"]:
            arch = "arm64"
        else:
            arch = machine

        package_name = Path.cwd().name
        expected_workenv = f"workenv/{package_name}_{system}_{arch}"

        if expected_workenv not in content:
            self.checks_failed.append(
                (
                    "env.sh Workenv Path",
                    f"Incorrect pattern - expected '{expected_workenv}'. Regenerate with 'wrknv generate'",
                )
            )
            return

        # Check required patterns for proper functioning
        required_patterns = [
            "WORKENV_DIR=",
            "UV_INSTALLER_URL=",
            "uv venv",
            "uv sync",
        ]

        missing = []
        for pattern in required_patterns:
            if pattern not in content:
                missing.append(pattern)

        if missing:
            self.checks_failed.append(
                None
            )
        else:
            # Test if env.sh actually works
            self._test_env_script_execution(env_script)

    def xǁWrknvDoctorǁ_check_env_script__mutmut_81(self) -> None:
        """Check if env.sh exists and is valid."""
        env_script = Path.cwd() / "env.sh"

        if not env_script.exists():
            self.checks_failed.append(("env.sh", "Not found - run 'wrknv generate'"))
            return

        # Check if env.sh was generated by wrknv
        content = env_script.read_text()
        is_wrknv_generated = "Generated by wrknv" in content or "WORKENV_DIR=" in content

        if not is_wrknv_generated:
            self.checks_failed.append(
                ("env.sh", "Not generated by wrknv - run 'wrknv generate' to regenerate")
            )
            return

        # Check if it's executable
        if not os.access(env_script, os.X_OK):
            self.checks_warning.append(("env.sh", "Not executable - run 'chmod +x env.sh'"))

        # Check for correct workenv pattern
        system = platform.system().lower()
        machine = platform.machine().lower()
        if machine in ["x86_64", "amd64"]:
            arch = "amd64"
        elif machine in ["arm64", "aarch64"]:
            arch = "arm64"
        else:
            arch = machine

        package_name = Path.cwd().name
        expected_workenv = f"workenv/{package_name}_{system}_{arch}"

        if expected_workenv not in content:
            self.checks_failed.append(
                (
                    "env.sh Workenv Path",
                    f"Incorrect pattern - expected '{expected_workenv}'. Regenerate with 'wrknv generate'",
                )
            )
            return

        # Check required patterns for proper functioning
        required_patterns = [
            "WORKENV_DIR=",
            "UV_INSTALLER_URL=",
            "uv venv",
            "uv sync",
        ]

        missing = []
        for pattern in required_patterns:
            if pattern not in content:
                missing.append(pattern)

        if missing:
            self.checks_failed.append(
                (
                    "XXenv.sh ContentXX",
                    f"Missing required patterns: {', '.join(missing)}. Regenerate with 'wrknv generate'",
                )
            )
        else:
            # Test if env.sh actually works
            self._test_env_script_execution(env_script)

    def xǁWrknvDoctorǁ_check_env_script__mutmut_82(self) -> None:
        """Check if env.sh exists and is valid."""
        env_script = Path.cwd() / "env.sh"

        if not env_script.exists():
            self.checks_failed.append(("env.sh", "Not found - run 'wrknv generate'"))
            return

        # Check if env.sh was generated by wrknv
        content = env_script.read_text()
        is_wrknv_generated = "Generated by wrknv" in content or "WORKENV_DIR=" in content

        if not is_wrknv_generated:
            self.checks_failed.append(
                ("env.sh", "Not generated by wrknv - run 'wrknv generate' to regenerate")
            )
            return

        # Check if it's executable
        if not os.access(env_script, os.X_OK):
            self.checks_warning.append(("env.sh", "Not executable - run 'chmod +x env.sh'"))

        # Check for correct workenv pattern
        system = platform.system().lower()
        machine = platform.machine().lower()
        if machine in ["x86_64", "amd64"]:
            arch = "amd64"
        elif machine in ["arm64", "aarch64"]:
            arch = "arm64"
        else:
            arch = machine

        package_name = Path.cwd().name
        expected_workenv = f"workenv/{package_name}_{system}_{arch}"

        if expected_workenv not in content:
            self.checks_failed.append(
                (
                    "env.sh Workenv Path",
                    f"Incorrect pattern - expected '{expected_workenv}'. Regenerate with 'wrknv generate'",
                )
            )
            return

        # Check required patterns for proper functioning
        required_patterns = [
            "WORKENV_DIR=",
            "UV_INSTALLER_URL=",
            "uv venv",
            "uv sync",
        ]

        missing = []
        for pattern in required_patterns:
            if pattern not in content:
                missing.append(pattern)

        if missing:
            self.checks_failed.append(
                (
                    "env.sh content",
                    f"Missing required patterns: {', '.join(missing)}. Regenerate with 'wrknv generate'",
                )
            )
        else:
            # Test if env.sh actually works
            self._test_env_script_execution(env_script)

    def xǁWrknvDoctorǁ_check_env_script__mutmut_83(self) -> None:
        """Check if env.sh exists and is valid."""
        env_script = Path.cwd() / "env.sh"

        if not env_script.exists():
            self.checks_failed.append(("env.sh", "Not found - run 'wrknv generate'"))
            return

        # Check if env.sh was generated by wrknv
        content = env_script.read_text()
        is_wrknv_generated = "Generated by wrknv" in content or "WORKENV_DIR=" in content

        if not is_wrknv_generated:
            self.checks_failed.append(
                ("env.sh", "Not generated by wrknv - run 'wrknv generate' to regenerate")
            )
            return

        # Check if it's executable
        if not os.access(env_script, os.X_OK):
            self.checks_warning.append(("env.sh", "Not executable - run 'chmod +x env.sh'"))

        # Check for correct workenv pattern
        system = platform.system().lower()
        machine = platform.machine().lower()
        if machine in ["x86_64", "amd64"]:
            arch = "amd64"
        elif machine in ["arm64", "aarch64"]:
            arch = "arm64"
        else:
            arch = machine

        package_name = Path.cwd().name
        expected_workenv = f"workenv/{package_name}_{system}_{arch}"

        if expected_workenv not in content:
            self.checks_failed.append(
                (
                    "env.sh Workenv Path",
                    f"Incorrect pattern - expected '{expected_workenv}'. Regenerate with 'wrknv generate'",
                )
            )
            return

        # Check required patterns for proper functioning
        required_patterns = [
            "WORKENV_DIR=",
            "UV_INSTALLER_URL=",
            "uv venv",
            "uv sync",
        ]

        missing = []
        for pattern in required_patterns:
            if pattern not in content:
                missing.append(pattern)

        if missing:
            self.checks_failed.append(
                (
                    "ENV.SH CONTENT",
                    f"Missing required patterns: {', '.join(missing)}. Regenerate with 'wrknv generate'",
                )
            )
        else:
            # Test if env.sh actually works
            self._test_env_script_execution(env_script)

    def xǁWrknvDoctorǁ_check_env_script__mutmut_84(self) -> None:
        """Check if env.sh exists and is valid."""
        env_script = Path.cwd() / "env.sh"

        if not env_script.exists():
            self.checks_failed.append(("env.sh", "Not found - run 'wrknv generate'"))
            return

        # Check if env.sh was generated by wrknv
        content = env_script.read_text()
        is_wrknv_generated = "Generated by wrknv" in content or "WORKENV_DIR=" in content

        if not is_wrknv_generated:
            self.checks_failed.append(
                ("env.sh", "Not generated by wrknv - run 'wrknv generate' to regenerate")
            )
            return

        # Check if it's executable
        if not os.access(env_script, os.X_OK):
            self.checks_warning.append(("env.sh", "Not executable - run 'chmod +x env.sh'"))

        # Check for correct workenv pattern
        system = platform.system().lower()
        machine = platform.machine().lower()
        if machine in ["x86_64", "amd64"]:
            arch = "amd64"
        elif machine in ["arm64", "aarch64"]:
            arch = "arm64"
        else:
            arch = machine

        package_name = Path.cwd().name
        expected_workenv = f"workenv/{package_name}_{system}_{arch}"

        if expected_workenv not in content:
            self.checks_failed.append(
                (
                    "env.sh Workenv Path",
                    f"Incorrect pattern - expected '{expected_workenv}'. Regenerate with 'wrknv generate'",
                )
            )
            return

        # Check required patterns for proper functioning
        required_patterns = [
            "WORKENV_DIR=",
            "UV_INSTALLER_URL=",
            "uv venv",
            "uv sync",
        ]

        missing = []
        for pattern in required_patterns:
            if pattern not in content:
                missing.append(pattern)

        if missing:
            self.checks_failed.append(
                (
                    "env.sh Content",
                    f"Missing required patterns: {', '.join(None)}. Regenerate with 'wrknv generate'",
                )
            )
        else:
            # Test if env.sh actually works
            self._test_env_script_execution(env_script)

    def xǁWrknvDoctorǁ_check_env_script__mutmut_85(self) -> None:
        """Check if env.sh exists and is valid."""
        env_script = Path.cwd() / "env.sh"

        if not env_script.exists():
            self.checks_failed.append(("env.sh", "Not found - run 'wrknv generate'"))
            return

        # Check if env.sh was generated by wrknv
        content = env_script.read_text()
        is_wrknv_generated = "Generated by wrknv" in content or "WORKENV_DIR=" in content

        if not is_wrknv_generated:
            self.checks_failed.append(
                ("env.sh", "Not generated by wrknv - run 'wrknv generate' to regenerate")
            )
            return

        # Check if it's executable
        if not os.access(env_script, os.X_OK):
            self.checks_warning.append(("env.sh", "Not executable - run 'chmod +x env.sh'"))

        # Check for correct workenv pattern
        system = platform.system().lower()
        machine = platform.machine().lower()
        if machine in ["x86_64", "amd64"]:
            arch = "amd64"
        elif machine in ["arm64", "aarch64"]:
            arch = "arm64"
        else:
            arch = machine

        package_name = Path.cwd().name
        expected_workenv = f"workenv/{package_name}_{system}_{arch}"

        if expected_workenv not in content:
            self.checks_failed.append(
                (
                    "env.sh Workenv Path",
                    f"Incorrect pattern - expected '{expected_workenv}'. Regenerate with 'wrknv generate'",
                )
            )
            return

        # Check required patterns for proper functioning
        required_patterns = [
            "WORKENV_DIR=",
            "UV_INSTALLER_URL=",
            "uv venv",
            "uv sync",
        ]

        missing = []
        for pattern in required_patterns:
            if pattern not in content:
                missing.append(pattern)

        if missing:
            self.checks_failed.append(
                (
                    "env.sh Content",
                    f"Missing required patterns: {'XX, XX'.join(missing)}. Regenerate with 'wrknv generate'",
                )
            )
        else:
            # Test if env.sh actually works
            self._test_env_script_execution(env_script)

    def xǁWrknvDoctorǁ_check_env_script__mutmut_86(self) -> None:
        """Check if env.sh exists and is valid."""
        env_script = Path.cwd() / "env.sh"

        if not env_script.exists():
            self.checks_failed.append(("env.sh", "Not found - run 'wrknv generate'"))
            return

        # Check if env.sh was generated by wrknv
        content = env_script.read_text()
        is_wrknv_generated = "Generated by wrknv" in content or "WORKENV_DIR=" in content

        if not is_wrknv_generated:
            self.checks_failed.append(
                ("env.sh", "Not generated by wrknv - run 'wrknv generate' to regenerate")
            )
            return

        # Check if it's executable
        if not os.access(env_script, os.X_OK):
            self.checks_warning.append(("env.sh", "Not executable - run 'chmod +x env.sh'"))

        # Check for correct workenv pattern
        system = platform.system().lower()
        machine = platform.machine().lower()
        if machine in ["x86_64", "amd64"]:
            arch = "amd64"
        elif machine in ["arm64", "aarch64"]:
            arch = "arm64"
        else:
            arch = machine

        package_name = Path.cwd().name
        expected_workenv = f"workenv/{package_name}_{system}_{arch}"

        if expected_workenv not in content:
            self.checks_failed.append(
                (
                    "env.sh Workenv Path",
                    f"Incorrect pattern - expected '{expected_workenv}'. Regenerate with 'wrknv generate'",
                )
            )
            return

        # Check required patterns for proper functioning
        required_patterns = [
            "WORKENV_DIR=",
            "UV_INSTALLER_URL=",
            "uv venv",
            "uv sync",
        ]

        missing = []
        for pattern in required_patterns:
            if pattern not in content:
                missing.append(pattern)

        if missing:
            self.checks_failed.append(
                (
                    "env.sh Content",
                    f"Missing required patterns: {', '.join(missing)}. Regenerate with 'wrknv generate'",
                )
            )
        else:
            # Test if env.sh actually works
            self._test_env_script_execution(None)
    
    xǁWrknvDoctorǁ_check_env_script__mutmut_mutants : ClassVar[MutantDict] = {
    'xǁWrknvDoctorǁ_check_env_script__mutmut_1': xǁWrknvDoctorǁ_check_env_script__mutmut_1, 
        'xǁWrknvDoctorǁ_check_env_script__mutmut_2': xǁWrknvDoctorǁ_check_env_script__mutmut_2, 
        'xǁWrknvDoctorǁ_check_env_script__mutmut_3': xǁWrknvDoctorǁ_check_env_script__mutmut_3, 
        'xǁWrknvDoctorǁ_check_env_script__mutmut_4': xǁWrknvDoctorǁ_check_env_script__mutmut_4, 
        'xǁWrknvDoctorǁ_check_env_script__mutmut_5': xǁWrknvDoctorǁ_check_env_script__mutmut_5, 
        'xǁWrknvDoctorǁ_check_env_script__mutmut_6': xǁWrknvDoctorǁ_check_env_script__mutmut_6, 
        'xǁWrknvDoctorǁ_check_env_script__mutmut_7': xǁWrknvDoctorǁ_check_env_script__mutmut_7, 
        'xǁWrknvDoctorǁ_check_env_script__mutmut_8': xǁWrknvDoctorǁ_check_env_script__mutmut_8, 
        'xǁWrknvDoctorǁ_check_env_script__mutmut_9': xǁWrknvDoctorǁ_check_env_script__mutmut_9, 
        'xǁWrknvDoctorǁ_check_env_script__mutmut_10': xǁWrknvDoctorǁ_check_env_script__mutmut_10, 
        'xǁWrknvDoctorǁ_check_env_script__mutmut_11': xǁWrknvDoctorǁ_check_env_script__mutmut_11, 
        'xǁWrknvDoctorǁ_check_env_script__mutmut_12': xǁWrknvDoctorǁ_check_env_script__mutmut_12, 
        'xǁWrknvDoctorǁ_check_env_script__mutmut_13': xǁWrknvDoctorǁ_check_env_script__mutmut_13, 
        'xǁWrknvDoctorǁ_check_env_script__mutmut_14': xǁWrknvDoctorǁ_check_env_script__mutmut_14, 
        'xǁWrknvDoctorǁ_check_env_script__mutmut_15': xǁWrknvDoctorǁ_check_env_script__mutmut_15, 
        'xǁWrknvDoctorǁ_check_env_script__mutmut_16': xǁWrknvDoctorǁ_check_env_script__mutmut_16, 
        'xǁWrknvDoctorǁ_check_env_script__mutmut_17': xǁWrknvDoctorǁ_check_env_script__mutmut_17, 
        'xǁWrknvDoctorǁ_check_env_script__mutmut_18': xǁWrknvDoctorǁ_check_env_script__mutmut_18, 
        'xǁWrknvDoctorǁ_check_env_script__mutmut_19': xǁWrknvDoctorǁ_check_env_script__mutmut_19, 
        'xǁWrknvDoctorǁ_check_env_script__mutmut_20': xǁWrknvDoctorǁ_check_env_script__mutmut_20, 
        'xǁWrknvDoctorǁ_check_env_script__mutmut_21': xǁWrknvDoctorǁ_check_env_script__mutmut_21, 
        'xǁWrknvDoctorǁ_check_env_script__mutmut_22': xǁWrknvDoctorǁ_check_env_script__mutmut_22, 
        'xǁWrknvDoctorǁ_check_env_script__mutmut_23': xǁWrknvDoctorǁ_check_env_script__mutmut_23, 
        'xǁWrknvDoctorǁ_check_env_script__mutmut_24': xǁWrknvDoctorǁ_check_env_script__mutmut_24, 
        'xǁWrknvDoctorǁ_check_env_script__mutmut_25': xǁWrknvDoctorǁ_check_env_script__mutmut_25, 
        'xǁWrknvDoctorǁ_check_env_script__mutmut_26': xǁWrknvDoctorǁ_check_env_script__mutmut_26, 
        'xǁWrknvDoctorǁ_check_env_script__mutmut_27': xǁWrknvDoctorǁ_check_env_script__mutmut_27, 
        'xǁWrknvDoctorǁ_check_env_script__mutmut_28': xǁWrknvDoctorǁ_check_env_script__mutmut_28, 
        'xǁWrknvDoctorǁ_check_env_script__mutmut_29': xǁWrknvDoctorǁ_check_env_script__mutmut_29, 
        'xǁWrknvDoctorǁ_check_env_script__mutmut_30': xǁWrknvDoctorǁ_check_env_script__mutmut_30, 
        'xǁWrknvDoctorǁ_check_env_script__mutmut_31': xǁWrknvDoctorǁ_check_env_script__mutmut_31, 
        'xǁWrknvDoctorǁ_check_env_script__mutmut_32': xǁWrknvDoctorǁ_check_env_script__mutmut_32, 
        'xǁWrknvDoctorǁ_check_env_script__mutmut_33': xǁWrknvDoctorǁ_check_env_script__mutmut_33, 
        'xǁWrknvDoctorǁ_check_env_script__mutmut_34': xǁWrknvDoctorǁ_check_env_script__mutmut_34, 
        'xǁWrknvDoctorǁ_check_env_script__mutmut_35': xǁWrknvDoctorǁ_check_env_script__mutmut_35, 
        'xǁWrknvDoctorǁ_check_env_script__mutmut_36': xǁWrknvDoctorǁ_check_env_script__mutmut_36, 
        'xǁWrknvDoctorǁ_check_env_script__mutmut_37': xǁWrknvDoctorǁ_check_env_script__mutmut_37, 
        'xǁWrknvDoctorǁ_check_env_script__mutmut_38': xǁWrknvDoctorǁ_check_env_script__mutmut_38, 
        'xǁWrknvDoctorǁ_check_env_script__mutmut_39': xǁWrknvDoctorǁ_check_env_script__mutmut_39, 
        'xǁWrknvDoctorǁ_check_env_script__mutmut_40': xǁWrknvDoctorǁ_check_env_script__mutmut_40, 
        'xǁWrknvDoctorǁ_check_env_script__mutmut_41': xǁWrknvDoctorǁ_check_env_script__mutmut_41, 
        'xǁWrknvDoctorǁ_check_env_script__mutmut_42': xǁWrknvDoctorǁ_check_env_script__mutmut_42, 
        'xǁWrknvDoctorǁ_check_env_script__mutmut_43': xǁWrknvDoctorǁ_check_env_script__mutmut_43, 
        'xǁWrknvDoctorǁ_check_env_script__mutmut_44': xǁWrknvDoctorǁ_check_env_script__mutmut_44, 
        'xǁWrknvDoctorǁ_check_env_script__mutmut_45': xǁWrknvDoctorǁ_check_env_script__mutmut_45, 
        'xǁWrknvDoctorǁ_check_env_script__mutmut_46': xǁWrknvDoctorǁ_check_env_script__mutmut_46, 
        'xǁWrknvDoctorǁ_check_env_script__mutmut_47': xǁWrknvDoctorǁ_check_env_script__mutmut_47, 
        'xǁWrknvDoctorǁ_check_env_script__mutmut_48': xǁWrknvDoctorǁ_check_env_script__mutmut_48, 
        'xǁWrknvDoctorǁ_check_env_script__mutmut_49': xǁWrknvDoctorǁ_check_env_script__mutmut_49, 
        'xǁWrknvDoctorǁ_check_env_script__mutmut_50': xǁWrknvDoctorǁ_check_env_script__mutmut_50, 
        'xǁWrknvDoctorǁ_check_env_script__mutmut_51': xǁWrknvDoctorǁ_check_env_script__mutmut_51, 
        'xǁWrknvDoctorǁ_check_env_script__mutmut_52': xǁWrknvDoctorǁ_check_env_script__mutmut_52, 
        'xǁWrknvDoctorǁ_check_env_script__mutmut_53': xǁWrknvDoctorǁ_check_env_script__mutmut_53, 
        'xǁWrknvDoctorǁ_check_env_script__mutmut_54': xǁWrknvDoctorǁ_check_env_script__mutmut_54, 
        'xǁWrknvDoctorǁ_check_env_script__mutmut_55': xǁWrknvDoctorǁ_check_env_script__mutmut_55, 
        'xǁWrknvDoctorǁ_check_env_script__mutmut_56': xǁWrknvDoctorǁ_check_env_script__mutmut_56, 
        'xǁWrknvDoctorǁ_check_env_script__mutmut_57': xǁWrknvDoctorǁ_check_env_script__mutmut_57, 
        'xǁWrknvDoctorǁ_check_env_script__mutmut_58': xǁWrknvDoctorǁ_check_env_script__mutmut_58, 
        'xǁWrknvDoctorǁ_check_env_script__mutmut_59': xǁWrknvDoctorǁ_check_env_script__mutmut_59, 
        'xǁWrknvDoctorǁ_check_env_script__mutmut_60': xǁWrknvDoctorǁ_check_env_script__mutmut_60, 
        'xǁWrknvDoctorǁ_check_env_script__mutmut_61': xǁWrknvDoctorǁ_check_env_script__mutmut_61, 
        'xǁWrknvDoctorǁ_check_env_script__mutmut_62': xǁWrknvDoctorǁ_check_env_script__mutmut_62, 
        'xǁWrknvDoctorǁ_check_env_script__mutmut_63': xǁWrknvDoctorǁ_check_env_script__mutmut_63, 
        'xǁWrknvDoctorǁ_check_env_script__mutmut_64': xǁWrknvDoctorǁ_check_env_script__mutmut_64, 
        'xǁWrknvDoctorǁ_check_env_script__mutmut_65': xǁWrknvDoctorǁ_check_env_script__mutmut_65, 
        'xǁWrknvDoctorǁ_check_env_script__mutmut_66': xǁWrknvDoctorǁ_check_env_script__mutmut_66, 
        'xǁWrknvDoctorǁ_check_env_script__mutmut_67': xǁWrknvDoctorǁ_check_env_script__mutmut_67, 
        'xǁWrknvDoctorǁ_check_env_script__mutmut_68': xǁWrknvDoctorǁ_check_env_script__mutmut_68, 
        'xǁWrknvDoctorǁ_check_env_script__mutmut_69': xǁWrknvDoctorǁ_check_env_script__mutmut_69, 
        'xǁWrknvDoctorǁ_check_env_script__mutmut_70': xǁWrknvDoctorǁ_check_env_script__mutmut_70, 
        'xǁWrknvDoctorǁ_check_env_script__mutmut_71': xǁWrknvDoctorǁ_check_env_script__mutmut_71, 
        'xǁWrknvDoctorǁ_check_env_script__mutmut_72': xǁWrknvDoctorǁ_check_env_script__mutmut_72, 
        'xǁWrknvDoctorǁ_check_env_script__mutmut_73': xǁWrknvDoctorǁ_check_env_script__mutmut_73, 
        'xǁWrknvDoctorǁ_check_env_script__mutmut_74': xǁWrknvDoctorǁ_check_env_script__mutmut_74, 
        'xǁWrknvDoctorǁ_check_env_script__mutmut_75': xǁWrknvDoctorǁ_check_env_script__mutmut_75, 
        'xǁWrknvDoctorǁ_check_env_script__mutmut_76': xǁWrknvDoctorǁ_check_env_script__mutmut_76, 
        'xǁWrknvDoctorǁ_check_env_script__mutmut_77': xǁWrknvDoctorǁ_check_env_script__mutmut_77, 
        'xǁWrknvDoctorǁ_check_env_script__mutmut_78': xǁWrknvDoctorǁ_check_env_script__mutmut_78, 
        'xǁWrknvDoctorǁ_check_env_script__mutmut_79': xǁWrknvDoctorǁ_check_env_script__mutmut_79, 
        'xǁWrknvDoctorǁ_check_env_script__mutmut_80': xǁWrknvDoctorǁ_check_env_script__mutmut_80, 
        'xǁWrknvDoctorǁ_check_env_script__mutmut_81': xǁWrknvDoctorǁ_check_env_script__mutmut_81, 
        'xǁWrknvDoctorǁ_check_env_script__mutmut_82': xǁWrknvDoctorǁ_check_env_script__mutmut_82, 
        'xǁWrknvDoctorǁ_check_env_script__mutmut_83': xǁWrknvDoctorǁ_check_env_script__mutmut_83, 
        'xǁWrknvDoctorǁ_check_env_script__mutmut_84': xǁWrknvDoctorǁ_check_env_script__mutmut_84, 
        'xǁWrknvDoctorǁ_check_env_script__mutmut_85': xǁWrknvDoctorǁ_check_env_script__mutmut_85, 
        'xǁWrknvDoctorǁ_check_env_script__mutmut_86': xǁWrknvDoctorǁ_check_env_script__mutmut_86
    }
    
    def _check_env_script(self, *args, **kwargs):
        result = _mutmut_trampoline(object.__getattribute__(self, "xǁWrknvDoctorǁ_check_env_script__mutmut_orig"), object.__getattribute__(self, "xǁWrknvDoctorǁ_check_env_script__mutmut_mutants"), args, kwargs, self)
        return result 
    
    _check_env_script.__signature__ = _mutmut_signature(xǁWrknvDoctorǁ_check_env_script__mutmut_orig)
    xǁWrknvDoctorǁ_check_env_script__mutmut_orig.__name__ = 'xǁWrknvDoctorǁ_check_env_script'

    def xǁWrknvDoctorǁ_test_env_script_execution__mutmut_orig(self, env_script: Path) -> None:
        """Test if env.sh can be executed successfully."""
        try:
            # Try to source env.sh and check if VIRTUAL_ENV is set
            result = run(["bash", "-c", f"source {env_script} && echo $VIRTUAL_ENV"], timeout=10)

            if result.returncode != 0:
                self.checks_failed.append(
                    (
                        "env.sh Execution",
                        f"Failed to execute: {result.stderr.strip()}. Regenerate with 'wrknv generate'",
                    )
                )
            elif not result.stdout.strip():
                self.checks_failed.append(
                    ("env.sh Execution", "VIRTUAL_ENV not set. Regenerate with 'wrknv generate'")
                )
            else:
                workenv_path = result.stdout.strip()
                if Path(workenv_path).exists():
                    self.checks_passed.append(("env.sh", f"Valid and working → {workenv_path}"))
                else:
                    self.checks_warning.append(
                        ("env.sh Execution", f"Points to non-existent workenv: {workenv_path}")
                    )
        except TimeoutError:
            self.checks_failed.append(
                ("env.sh Execution", "Timed out - possible infinite loop. Regenerate with 'wrknv generate'")
            )
        except Exception as e:
            self.checks_failed.append(
                ("env.sh Execution", f"Unexpected error: {e}. This may be a bug in wrknv")
            )

    def xǁWrknvDoctorǁ_test_env_script_execution__mutmut_1(self, env_script: Path) -> None:
        """Test if env.sh can be executed successfully."""
        try:
            # Try to source env.sh and check if VIRTUAL_ENV is set
            result = None

            if result.returncode != 0:
                self.checks_failed.append(
                    (
                        "env.sh Execution",
                        f"Failed to execute: {result.stderr.strip()}. Regenerate with 'wrknv generate'",
                    )
                )
            elif not result.stdout.strip():
                self.checks_failed.append(
                    ("env.sh Execution", "VIRTUAL_ENV not set. Regenerate with 'wrknv generate'")
                )
            else:
                workenv_path = result.stdout.strip()
                if Path(workenv_path).exists():
                    self.checks_passed.append(("env.sh", f"Valid and working → {workenv_path}"))
                else:
                    self.checks_warning.append(
                        ("env.sh Execution", f"Points to non-existent workenv: {workenv_path}")
                    )
        except TimeoutError:
            self.checks_failed.append(
                ("env.sh Execution", "Timed out - possible infinite loop. Regenerate with 'wrknv generate'")
            )
        except Exception as e:
            self.checks_failed.append(
                ("env.sh Execution", f"Unexpected error: {e}. This may be a bug in wrknv")
            )

    def xǁWrknvDoctorǁ_test_env_script_execution__mutmut_2(self, env_script: Path) -> None:
        """Test if env.sh can be executed successfully."""
        try:
            # Try to source env.sh and check if VIRTUAL_ENV is set
            result = run(None, timeout=10)

            if result.returncode != 0:
                self.checks_failed.append(
                    (
                        "env.sh Execution",
                        f"Failed to execute: {result.stderr.strip()}. Regenerate with 'wrknv generate'",
                    )
                )
            elif not result.stdout.strip():
                self.checks_failed.append(
                    ("env.sh Execution", "VIRTUAL_ENV not set. Regenerate with 'wrknv generate'")
                )
            else:
                workenv_path = result.stdout.strip()
                if Path(workenv_path).exists():
                    self.checks_passed.append(("env.sh", f"Valid and working → {workenv_path}"))
                else:
                    self.checks_warning.append(
                        ("env.sh Execution", f"Points to non-existent workenv: {workenv_path}")
                    )
        except TimeoutError:
            self.checks_failed.append(
                ("env.sh Execution", "Timed out - possible infinite loop. Regenerate with 'wrknv generate'")
            )
        except Exception as e:
            self.checks_failed.append(
                ("env.sh Execution", f"Unexpected error: {e}. This may be a bug in wrknv")
            )

    def xǁWrknvDoctorǁ_test_env_script_execution__mutmut_3(self, env_script: Path) -> None:
        """Test if env.sh can be executed successfully."""
        try:
            # Try to source env.sh and check if VIRTUAL_ENV is set
            result = run(["bash", "-c", f"source {env_script} && echo $VIRTUAL_ENV"], timeout=None)

            if result.returncode != 0:
                self.checks_failed.append(
                    (
                        "env.sh Execution",
                        f"Failed to execute: {result.stderr.strip()}. Regenerate with 'wrknv generate'",
                    )
                )
            elif not result.stdout.strip():
                self.checks_failed.append(
                    ("env.sh Execution", "VIRTUAL_ENV not set. Regenerate with 'wrknv generate'")
                )
            else:
                workenv_path = result.stdout.strip()
                if Path(workenv_path).exists():
                    self.checks_passed.append(("env.sh", f"Valid and working → {workenv_path}"))
                else:
                    self.checks_warning.append(
                        ("env.sh Execution", f"Points to non-existent workenv: {workenv_path}")
                    )
        except TimeoutError:
            self.checks_failed.append(
                ("env.sh Execution", "Timed out - possible infinite loop. Regenerate with 'wrknv generate'")
            )
        except Exception as e:
            self.checks_failed.append(
                ("env.sh Execution", f"Unexpected error: {e}. This may be a bug in wrknv")
            )

    def xǁWrknvDoctorǁ_test_env_script_execution__mutmut_4(self, env_script: Path) -> None:
        """Test if env.sh can be executed successfully."""
        try:
            # Try to source env.sh and check if VIRTUAL_ENV is set
            result = run(timeout=10)

            if result.returncode != 0:
                self.checks_failed.append(
                    (
                        "env.sh Execution",
                        f"Failed to execute: {result.stderr.strip()}. Regenerate with 'wrknv generate'",
                    )
                )
            elif not result.stdout.strip():
                self.checks_failed.append(
                    ("env.sh Execution", "VIRTUAL_ENV not set. Regenerate with 'wrknv generate'")
                )
            else:
                workenv_path = result.stdout.strip()
                if Path(workenv_path).exists():
                    self.checks_passed.append(("env.sh", f"Valid and working → {workenv_path}"))
                else:
                    self.checks_warning.append(
                        ("env.sh Execution", f"Points to non-existent workenv: {workenv_path}")
                    )
        except TimeoutError:
            self.checks_failed.append(
                ("env.sh Execution", "Timed out - possible infinite loop. Regenerate with 'wrknv generate'")
            )
        except Exception as e:
            self.checks_failed.append(
                ("env.sh Execution", f"Unexpected error: {e}. This may be a bug in wrknv")
            )

    def xǁWrknvDoctorǁ_test_env_script_execution__mutmut_5(self, env_script: Path) -> None:
        """Test if env.sh can be executed successfully."""
        try:
            # Try to source env.sh and check if VIRTUAL_ENV is set
            result = run(["bash", "-c", f"source {env_script} && echo $VIRTUAL_ENV"], )

            if result.returncode != 0:
                self.checks_failed.append(
                    (
                        "env.sh Execution",
                        f"Failed to execute: {result.stderr.strip()}. Regenerate with 'wrknv generate'",
                    )
                )
            elif not result.stdout.strip():
                self.checks_failed.append(
                    ("env.sh Execution", "VIRTUAL_ENV not set. Regenerate with 'wrknv generate'")
                )
            else:
                workenv_path = result.stdout.strip()
                if Path(workenv_path).exists():
                    self.checks_passed.append(("env.sh", f"Valid and working → {workenv_path}"))
                else:
                    self.checks_warning.append(
                        ("env.sh Execution", f"Points to non-existent workenv: {workenv_path}")
                    )
        except TimeoutError:
            self.checks_failed.append(
                ("env.sh Execution", "Timed out - possible infinite loop. Regenerate with 'wrknv generate'")
            )
        except Exception as e:
            self.checks_failed.append(
                ("env.sh Execution", f"Unexpected error: {e}. This may be a bug in wrknv")
            )

    def xǁWrknvDoctorǁ_test_env_script_execution__mutmut_6(self, env_script: Path) -> None:
        """Test if env.sh can be executed successfully."""
        try:
            # Try to source env.sh and check if VIRTUAL_ENV is set
            result = run(["XXbashXX", "-c", f"source {env_script} && echo $VIRTUAL_ENV"], timeout=10)

            if result.returncode != 0:
                self.checks_failed.append(
                    (
                        "env.sh Execution",
                        f"Failed to execute: {result.stderr.strip()}. Regenerate with 'wrknv generate'",
                    )
                )
            elif not result.stdout.strip():
                self.checks_failed.append(
                    ("env.sh Execution", "VIRTUAL_ENV not set. Regenerate with 'wrknv generate'")
                )
            else:
                workenv_path = result.stdout.strip()
                if Path(workenv_path).exists():
                    self.checks_passed.append(("env.sh", f"Valid and working → {workenv_path}"))
                else:
                    self.checks_warning.append(
                        ("env.sh Execution", f"Points to non-existent workenv: {workenv_path}")
                    )
        except TimeoutError:
            self.checks_failed.append(
                ("env.sh Execution", "Timed out - possible infinite loop. Regenerate with 'wrknv generate'")
            )
        except Exception as e:
            self.checks_failed.append(
                ("env.sh Execution", f"Unexpected error: {e}. This may be a bug in wrknv")
            )

    def xǁWrknvDoctorǁ_test_env_script_execution__mutmut_7(self, env_script: Path) -> None:
        """Test if env.sh can be executed successfully."""
        try:
            # Try to source env.sh and check if VIRTUAL_ENV is set
            result = run(["BASH", "-c", f"source {env_script} && echo $VIRTUAL_ENV"], timeout=10)

            if result.returncode != 0:
                self.checks_failed.append(
                    (
                        "env.sh Execution",
                        f"Failed to execute: {result.stderr.strip()}. Regenerate with 'wrknv generate'",
                    )
                )
            elif not result.stdout.strip():
                self.checks_failed.append(
                    ("env.sh Execution", "VIRTUAL_ENV not set. Regenerate with 'wrknv generate'")
                )
            else:
                workenv_path = result.stdout.strip()
                if Path(workenv_path).exists():
                    self.checks_passed.append(("env.sh", f"Valid and working → {workenv_path}"))
                else:
                    self.checks_warning.append(
                        ("env.sh Execution", f"Points to non-existent workenv: {workenv_path}")
                    )
        except TimeoutError:
            self.checks_failed.append(
                ("env.sh Execution", "Timed out - possible infinite loop. Regenerate with 'wrknv generate'")
            )
        except Exception as e:
            self.checks_failed.append(
                ("env.sh Execution", f"Unexpected error: {e}. This may be a bug in wrknv")
            )

    def xǁWrknvDoctorǁ_test_env_script_execution__mutmut_8(self, env_script: Path) -> None:
        """Test if env.sh can be executed successfully."""
        try:
            # Try to source env.sh and check if VIRTUAL_ENV is set
            result = run(["bash", "XX-cXX", f"source {env_script} && echo $VIRTUAL_ENV"], timeout=10)

            if result.returncode != 0:
                self.checks_failed.append(
                    (
                        "env.sh Execution",
                        f"Failed to execute: {result.stderr.strip()}. Regenerate with 'wrknv generate'",
                    )
                )
            elif not result.stdout.strip():
                self.checks_failed.append(
                    ("env.sh Execution", "VIRTUAL_ENV not set. Regenerate with 'wrknv generate'")
                )
            else:
                workenv_path = result.stdout.strip()
                if Path(workenv_path).exists():
                    self.checks_passed.append(("env.sh", f"Valid and working → {workenv_path}"))
                else:
                    self.checks_warning.append(
                        ("env.sh Execution", f"Points to non-existent workenv: {workenv_path}")
                    )
        except TimeoutError:
            self.checks_failed.append(
                ("env.sh Execution", "Timed out - possible infinite loop. Regenerate with 'wrknv generate'")
            )
        except Exception as e:
            self.checks_failed.append(
                ("env.sh Execution", f"Unexpected error: {e}. This may be a bug in wrknv")
            )

    def xǁWrknvDoctorǁ_test_env_script_execution__mutmut_9(self, env_script: Path) -> None:
        """Test if env.sh can be executed successfully."""
        try:
            # Try to source env.sh and check if VIRTUAL_ENV is set
            result = run(["bash", "-C", f"source {env_script} && echo $VIRTUAL_ENV"], timeout=10)

            if result.returncode != 0:
                self.checks_failed.append(
                    (
                        "env.sh Execution",
                        f"Failed to execute: {result.stderr.strip()}. Regenerate with 'wrknv generate'",
                    )
                )
            elif not result.stdout.strip():
                self.checks_failed.append(
                    ("env.sh Execution", "VIRTUAL_ENV not set. Regenerate with 'wrknv generate'")
                )
            else:
                workenv_path = result.stdout.strip()
                if Path(workenv_path).exists():
                    self.checks_passed.append(("env.sh", f"Valid and working → {workenv_path}"))
                else:
                    self.checks_warning.append(
                        ("env.sh Execution", f"Points to non-existent workenv: {workenv_path}")
                    )
        except TimeoutError:
            self.checks_failed.append(
                ("env.sh Execution", "Timed out - possible infinite loop. Regenerate with 'wrknv generate'")
            )
        except Exception as e:
            self.checks_failed.append(
                ("env.sh Execution", f"Unexpected error: {e}. This may be a bug in wrknv")
            )

    def xǁWrknvDoctorǁ_test_env_script_execution__mutmut_10(self, env_script: Path) -> None:
        """Test if env.sh can be executed successfully."""
        try:
            # Try to source env.sh and check if VIRTUAL_ENV is set
            result = run(["bash", "-c", f"source {env_script} && echo $VIRTUAL_ENV"], timeout=11)

            if result.returncode != 0:
                self.checks_failed.append(
                    (
                        "env.sh Execution",
                        f"Failed to execute: {result.stderr.strip()}. Regenerate with 'wrknv generate'",
                    )
                )
            elif not result.stdout.strip():
                self.checks_failed.append(
                    ("env.sh Execution", "VIRTUAL_ENV not set. Regenerate with 'wrknv generate'")
                )
            else:
                workenv_path = result.stdout.strip()
                if Path(workenv_path).exists():
                    self.checks_passed.append(("env.sh", f"Valid and working → {workenv_path}"))
                else:
                    self.checks_warning.append(
                        ("env.sh Execution", f"Points to non-existent workenv: {workenv_path}")
                    )
        except TimeoutError:
            self.checks_failed.append(
                ("env.sh Execution", "Timed out - possible infinite loop. Regenerate with 'wrknv generate'")
            )
        except Exception as e:
            self.checks_failed.append(
                ("env.sh Execution", f"Unexpected error: {e}. This may be a bug in wrknv")
            )

    def xǁWrknvDoctorǁ_test_env_script_execution__mutmut_11(self, env_script: Path) -> None:
        """Test if env.sh can be executed successfully."""
        try:
            # Try to source env.sh and check if VIRTUAL_ENV is set
            result = run(["bash", "-c", f"source {env_script} && echo $VIRTUAL_ENV"], timeout=10)

            if result.returncode == 0:
                self.checks_failed.append(
                    (
                        "env.sh Execution",
                        f"Failed to execute: {result.stderr.strip()}. Regenerate with 'wrknv generate'",
                    )
                )
            elif not result.stdout.strip():
                self.checks_failed.append(
                    ("env.sh Execution", "VIRTUAL_ENV not set. Regenerate with 'wrknv generate'")
                )
            else:
                workenv_path = result.stdout.strip()
                if Path(workenv_path).exists():
                    self.checks_passed.append(("env.sh", f"Valid and working → {workenv_path}"))
                else:
                    self.checks_warning.append(
                        ("env.sh Execution", f"Points to non-existent workenv: {workenv_path}")
                    )
        except TimeoutError:
            self.checks_failed.append(
                ("env.sh Execution", "Timed out - possible infinite loop. Regenerate with 'wrknv generate'")
            )
        except Exception as e:
            self.checks_failed.append(
                ("env.sh Execution", f"Unexpected error: {e}. This may be a bug in wrknv")
            )

    def xǁWrknvDoctorǁ_test_env_script_execution__mutmut_12(self, env_script: Path) -> None:
        """Test if env.sh can be executed successfully."""
        try:
            # Try to source env.sh and check if VIRTUAL_ENV is set
            result = run(["bash", "-c", f"source {env_script} && echo $VIRTUAL_ENV"], timeout=10)

            if result.returncode != 1:
                self.checks_failed.append(
                    (
                        "env.sh Execution",
                        f"Failed to execute: {result.stderr.strip()}. Regenerate with 'wrknv generate'",
                    )
                )
            elif not result.stdout.strip():
                self.checks_failed.append(
                    ("env.sh Execution", "VIRTUAL_ENV not set. Regenerate with 'wrknv generate'")
                )
            else:
                workenv_path = result.stdout.strip()
                if Path(workenv_path).exists():
                    self.checks_passed.append(("env.sh", f"Valid and working → {workenv_path}"))
                else:
                    self.checks_warning.append(
                        ("env.sh Execution", f"Points to non-existent workenv: {workenv_path}")
                    )
        except TimeoutError:
            self.checks_failed.append(
                ("env.sh Execution", "Timed out - possible infinite loop. Regenerate with 'wrknv generate'")
            )
        except Exception as e:
            self.checks_failed.append(
                ("env.sh Execution", f"Unexpected error: {e}. This may be a bug in wrknv")
            )

    def xǁWrknvDoctorǁ_test_env_script_execution__mutmut_13(self, env_script: Path) -> None:
        """Test if env.sh can be executed successfully."""
        try:
            # Try to source env.sh and check if VIRTUAL_ENV is set
            result = run(["bash", "-c", f"source {env_script} && echo $VIRTUAL_ENV"], timeout=10)

            if result.returncode != 0:
                self.checks_failed.append(
                    None
                )
            elif not result.stdout.strip():
                self.checks_failed.append(
                    ("env.sh Execution", "VIRTUAL_ENV not set. Regenerate with 'wrknv generate'")
                )
            else:
                workenv_path = result.stdout.strip()
                if Path(workenv_path).exists():
                    self.checks_passed.append(("env.sh", f"Valid and working → {workenv_path}"))
                else:
                    self.checks_warning.append(
                        ("env.sh Execution", f"Points to non-existent workenv: {workenv_path}")
                    )
        except TimeoutError:
            self.checks_failed.append(
                ("env.sh Execution", "Timed out - possible infinite loop. Regenerate with 'wrknv generate'")
            )
        except Exception as e:
            self.checks_failed.append(
                ("env.sh Execution", f"Unexpected error: {e}. This may be a bug in wrknv")
            )

    def xǁWrknvDoctorǁ_test_env_script_execution__mutmut_14(self, env_script: Path) -> None:
        """Test if env.sh can be executed successfully."""
        try:
            # Try to source env.sh and check if VIRTUAL_ENV is set
            result = run(["bash", "-c", f"source {env_script} && echo $VIRTUAL_ENV"], timeout=10)

            if result.returncode != 0:
                self.checks_failed.append(
                    (
                        "XXenv.sh ExecutionXX",
                        f"Failed to execute: {result.stderr.strip()}. Regenerate with 'wrknv generate'",
                    )
                )
            elif not result.stdout.strip():
                self.checks_failed.append(
                    ("env.sh Execution", "VIRTUAL_ENV not set. Regenerate with 'wrknv generate'")
                )
            else:
                workenv_path = result.stdout.strip()
                if Path(workenv_path).exists():
                    self.checks_passed.append(("env.sh", f"Valid and working → {workenv_path}"))
                else:
                    self.checks_warning.append(
                        ("env.sh Execution", f"Points to non-existent workenv: {workenv_path}")
                    )
        except TimeoutError:
            self.checks_failed.append(
                ("env.sh Execution", "Timed out - possible infinite loop. Regenerate with 'wrknv generate'")
            )
        except Exception as e:
            self.checks_failed.append(
                ("env.sh Execution", f"Unexpected error: {e}. This may be a bug in wrknv")
            )

    def xǁWrknvDoctorǁ_test_env_script_execution__mutmut_15(self, env_script: Path) -> None:
        """Test if env.sh can be executed successfully."""
        try:
            # Try to source env.sh and check if VIRTUAL_ENV is set
            result = run(["bash", "-c", f"source {env_script} && echo $VIRTUAL_ENV"], timeout=10)

            if result.returncode != 0:
                self.checks_failed.append(
                    (
                        "env.sh execution",
                        f"Failed to execute: {result.stderr.strip()}. Regenerate with 'wrknv generate'",
                    )
                )
            elif not result.stdout.strip():
                self.checks_failed.append(
                    ("env.sh Execution", "VIRTUAL_ENV not set. Regenerate with 'wrknv generate'")
                )
            else:
                workenv_path = result.stdout.strip()
                if Path(workenv_path).exists():
                    self.checks_passed.append(("env.sh", f"Valid and working → {workenv_path}"))
                else:
                    self.checks_warning.append(
                        ("env.sh Execution", f"Points to non-existent workenv: {workenv_path}")
                    )
        except TimeoutError:
            self.checks_failed.append(
                ("env.sh Execution", "Timed out - possible infinite loop. Regenerate with 'wrknv generate'")
            )
        except Exception as e:
            self.checks_failed.append(
                ("env.sh Execution", f"Unexpected error: {e}. This may be a bug in wrknv")
            )

    def xǁWrknvDoctorǁ_test_env_script_execution__mutmut_16(self, env_script: Path) -> None:
        """Test if env.sh can be executed successfully."""
        try:
            # Try to source env.sh and check if VIRTUAL_ENV is set
            result = run(["bash", "-c", f"source {env_script} && echo $VIRTUAL_ENV"], timeout=10)

            if result.returncode != 0:
                self.checks_failed.append(
                    (
                        "ENV.SH EXECUTION",
                        f"Failed to execute: {result.stderr.strip()}. Regenerate with 'wrknv generate'",
                    )
                )
            elif not result.stdout.strip():
                self.checks_failed.append(
                    ("env.sh Execution", "VIRTUAL_ENV not set. Regenerate with 'wrknv generate'")
                )
            else:
                workenv_path = result.stdout.strip()
                if Path(workenv_path).exists():
                    self.checks_passed.append(("env.sh", f"Valid and working → {workenv_path}"))
                else:
                    self.checks_warning.append(
                        ("env.sh Execution", f"Points to non-existent workenv: {workenv_path}")
                    )
        except TimeoutError:
            self.checks_failed.append(
                ("env.sh Execution", "Timed out - possible infinite loop. Regenerate with 'wrknv generate'")
            )
        except Exception as e:
            self.checks_failed.append(
                ("env.sh Execution", f"Unexpected error: {e}. This may be a bug in wrknv")
            )

    def xǁWrknvDoctorǁ_test_env_script_execution__mutmut_17(self, env_script: Path) -> None:
        """Test if env.sh can be executed successfully."""
        try:
            # Try to source env.sh and check if VIRTUAL_ENV is set
            result = run(["bash", "-c", f"source {env_script} && echo $VIRTUAL_ENV"], timeout=10)

            if result.returncode != 0:
                self.checks_failed.append(
                    (
                        "env.sh Execution",
                        f"Failed to execute: {result.stderr.strip()}. Regenerate with 'wrknv generate'",
                    )
                )
            elif result.stdout.strip():
                self.checks_failed.append(
                    ("env.sh Execution", "VIRTUAL_ENV not set. Regenerate with 'wrknv generate'")
                )
            else:
                workenv_path = result.stdout.strip()
                if Path(workenv_path).exists():
                    self.checks_passed.append(("env.sh", f"Valid and working → {workenv_path}"))
                else:
                    self.checks_warning.append(
                        ("env.sh Execution", f"Points to non-existent workenv: {workenv_path}")
                    )
        except TimeoutError:
            self.checks_failed.append(
                ("env.sh Execution", "Timed out - possible infinite loop. Regenerate with 'wrknv generate'")
            )
        except Exception as e:
            self.checks_failed.append(
                ("env.sh Execution", f"Unexpected error: {e}. This may be a bug in wrknv")
            )

    def xǁWrknvDoctorǁ_test_env_script_execution__mutmut_18(self, env_script: Path) -> None:
        """Test if env.sh can be executed successfully."""
        try:
            # Try to source env.sh and check if VIRTUAL_ENV is set
            result = run(["bash", "-c", f"source {env_script} && echo $VIRTUAL_ENV"], timeout=10)

            if result.returncode != 0:
                self.checks_failed.append(
                    (
                        "env.sh Execution",
                        f"Failed to execute: {result.stderr.strip()}. Regenerate with 'wrknv generate'",
                    )
                )
            elif not result.stdout.strip():
                self.checks_failed.append(
                    None
                )
            else:
                workenv_path = result.stdout.strip()
                if Path(workenv_path).exists():
                    self.checks_passed.append(("env.sh", f"Valid and working → {workenv_path}"))
                else:
                    self.checks_warning.append(
                        ("env.sh Execution", f"Points to non-existent workenv: {workenv_path}")
                    )
        except TimeoutError:
            self.checks_failed.append(
                ("env.sh Execution", "Timed out - possible infinite loop. Regenerate with 'wrknv generate'")
            )
        except Exception as e:
            self.checks_failed.append(
                ("env.sh Execution", f"Unexpected error: {e}. This may be a bug in wrknv")
            )

    def xǁWrknvDoctorǁ_test_env_script_execution__mutmut_19(self, env_script: Path) -> None:
        """Test if env.sh can be executed successfully."""
        try:
            # Try to source env.sh and check if VIRTUAL_ENV is set
            result = run(["bash", "-c", f"source {env_script} && echo $VIRTUAL_ENV"], timeout=10)

            if result.returncode != 0:
                self.checks_failed.append(
                    (
                        "env.sh Execution",
                        f"Failed to execute: {result.stderr.strip()}. Regenerate with 'wrknv generate'",
                    )
                )
            elif not result.stdout.strip():
                self.checks_failed.append(
                    ("XXenv.sh ExecutionXX", "VIRTUAL_ENV not set. Regenerate with 'wrknv generate'")
                )
            else:
                workenv_path = result.stdout.strip()
                if Path(workenv_path).exists():
                    self.checks_passed.append(("env.sh", f"Valid and working → {workenv_path}"))
                else:
                    self.checks_warning.append(
                        ("env.sh Execution", f"Points to non-existent workenv: {workenv_path}")
                    )
        except TimeoutError:
            self.checks_failed.append(
                ("env.sh Execution", "Timed out - possible infinite loop. Regenerate with 'wrknv generate'")
            )
        except Exception as e:
            self.checks_failed.append(
                ("env.sh Execution", f"Unexpected error: {e}. This may be a bug in wrknv")
            )

    def xǁWrknvDoctorǁ_test_env_script_execution__mutmut_20(self, env_script: Path) -> None:
        """Test if env.sh can be executed successfully."""
        try:
            # Try to source env.sh and check if VIRTUAL_ENV is set
            result = run(["bash", "-c", f"source {env_script} && echo $VIRTUAL_ENV"], timeout=10)

            if result.returncode != 0:
                self.checks_failed.append(
                    (
                        "env.sh Execution",
                        f"Failed to execute: {result.stderr.strip()}. Regenerate with 'wrknv generate'",
                    )
                )
            elif not result.stdout.strip():
                self.checks_failed.append(
                    ("env.sh execution", "VIRTUAL_ENV not set. Regenerate with 'wrknv generate'")
                )
            else:
                workenv_path = result.stdout.strip()
                if Path(workenv_path).exists():
                    self.checks_passed.append(("env.sh", f"Valid and working → {workenv_path}"))
                else:
                    self.checks_warning.append(
                        ("env.sh Execution", f"Points to non-existent workenv: {workenv_path}")
                    )
        except TimeoutError:
            self.checks_failed.append(
                ("env.sh Execution", "Timed out - possible infinite loop. Regenerate with 'wrknv generate'")
            )
        except Exception as e:
            self.checks_failed.append(
                ("env.sh Execution", f"Unexpected error: {e}. This may be a bug in wrknv")
            )

    def xǁWrknvDoctorǁ_test_env_script_execution__mutmut_21(self, env_script: Path) -> None:
        """Test if env.sh can be executed successfully."""
        try:
            # Try to source env.sh and check if VIRTUAL_ENV is set
            result = run(["bash", "-c", f"source {env_script} && echo $VIRTUAL_ENV"], timeout=10)

            if result.returncode != 0:
                self.checks_failed.append(
                    (
                        "env.sh Execution",
                        f"Failed to execute: {result.stderr.strip()}. Regenerate with 'wrknv generate'",
                    )
                )
            elif not result.stdout.strip():
                self.checks_failed.append(
                    ("ENV.SH EXECUTION", "VIRTUAL_ENV not set. Regenerate with 'wrknv generate'")
                )
            else:
                workenv_path = result.stdout.strip()
                if Path(workenv_path).exists():
                    self.checks_passed.append(("env.sh", f"Valid and working → {workenv_path}"))
                else:
                    self.checks_warning.append(
                        ("env.sh Execution", f"Points to non-existent workenv: {workenv_path}")
                    )
        except TimeoutError:
            self.checks_failed.append(
                ("env.sh Execution", "Timed out - possible infinite loop. Regenerate with 'wrknv generate'")
            )
        except Exception as e:
            self.checks_failed.append(
                ("env.sh Execution", f"Unexpected error: {e}. This may be a bug in wrknv")
            )

    def xǁWrknvDoctorǁ_test_env_script_execution__mutmut_22(self, env_script: Path) -> None:
        """Test if env.sh can be executed successfully."""
        try:
            # Try to source env.sh and check if VIRTUAL_ENV is set
            result = run(["bash", "-c", f"source {env_script} && echo $VIRTUAL_ENV"], timeout=10)

            if result.returncode != 0:
                self.checks_failed.append(
                    (
                        "env.sh Execution",
                        f"Failed to execute: {result.stderr.strip()}. Regenerate with 'wrknv generate'",
                    )
                )
            elif not result.stdout.strip():
                self.checks_failed.append(
                    ("env.sh Execution", "XXVIRTUAL_ENV not set. Regenerate with 'wrknv generate'XX")
                )
            else:
                workenv_path = result.stdout.strip()
                if Path(workenv_path).exists():
                    self.checks_passed.append(("env.sh", f"Valid and working → {workenv_path}"))
                else:
                    self.checks_warning.append(
                        ("env.sh Execution", f"Points to non-existent workenv: {workenv_path}")
                    )
        except TimeoutError:
            self.checks_failed.append(
                ("env.sh Execution", "Timed out - possible infinite loop. Regenerate with 'wrknv generate'")
            )
        except Exception as e:
            self.checks_failed.append(
                ("env.sh Execution", f"Unexpected error: {e}. This may be a bug in wrknv")
            )

    def xǁWrknvDoctorǁ_test_env_script_execution__mutmut_23(self, env_script: Path) -> None:
        """Test if env.sh can be executed successfully."""
        try:
            # Try to source env.sh and check if VIRTUAL_ENV is set
            result = run(["bash", "-c", f"source {env_script} && echo $VIRTUAL_ENV"], timeout=10)

            if result.returncode != 0:
                self.checks_failed.append(
                    (
                        "env.sh Execution",
                        f"Failed to execute: {result.stderr.strip()}. Regenerate with 'wrknv generate'",
                    )
                )
            elif not result.stdout.strip():
                self.checks_failed.append(
                    ("env.sh Execution", "virtual_env not set. regenerate with 'wrknv generate'")
                )
            else:
                workenv_path = result.stdout.strip()
                if Path(workenv_path).exists():
                    self.checks_passed.append(("env.sh", f"Valid and working → {workenv_path}"))
                else:
                    self.checks_warning.append(
                        ("env.sh Execution", f"Points to non-existent workenv: {workenv_path}")
                    )
        except TimeoutError:
            self.checks_failed.append(
                ("env.sh Execution", "Timed out - possible infinite loop. Regenerate with 'wrknv generate'")
            )
        except Exception as e:
            self.checks_failed.append(
                ("env.sh Execution", f"Unexpected error: {e}. This may be a bug in wrknv")
            )

    def xǁWrknvDoctorǁ_test_env_script_execution__mutmut_24(self, env_script: Path) -> None:
        """Test if env.sh can be executed successfully."""
        try:
            # Try to source env.sh and check if VIRTUAL_ENV is set
            result = run(["bash", "-c", f"source {env_script} && echo $VIRTUAL_ENV"], timeout=10)

            if result.returncode != 0:
                self.checks_failed.append(
                    (
                        "env.sh Execution",
                        f"Failed to execute: {result.stderr.strip()}. Regenerate with 'wrknv generate'",
                    )
                )
            elif not result.stdout.strip():
                self.checks_failed.append(
                    ("env.sh Execution", "VIRTUAL_ENV NOT SET. REGENERATE WITH 'WRKNV GENERATE'")
                )
            else:
                workenv_path = result.stdout.strip()
                if Path(workenv_path).exists():
                    self.checks_passed.append(("env.sh", f"Valid and working → {workenv_path}"))
                else:
                    self.checks_warning.append(
                        ("env.sh Execution", f"Points to non-existent workenv: {workenv_path}")
                    )
        except TimeoutError:
            self.checks_failed.append(
                ("env.sh Execution", "Timed out - possible infinite loop. Regenerate with 'wrknv generate'")
            )
        except Exception as e:
            self.checks_failed.append(
                ("env.sh Execution", f"Unexpected error: {e}. This may be a bug in wrknv")
            )

    def xǁWrknvDoctorǁ_test_env_script_execution__mutmut_25(self, env_script: Path) -> None:
        """Test if env.sh can be executed successfully."""
        try:
            # Try to source env.sh and check if VIRTUAL_ENV is set
            result = run(["bash", "-c", f"source {env_script} && echo $VIRTUAL_ENV"], timeout=10)

            if result.returncode != 0:
                self.checks_failed.append(
                    (
                        "env.sh Execution",
                        f"Failed to execute: {result.stderr.strip()}. Regenerate with 'wrknv generate'",
                    )
                )
            elif not result.stdout.strip():
                self.checks_failed.append(
                    ("env.sh Execution", "VIRTUAL_ENV not set. Regenerate with 'wrknv generate'")
                )
            else:
                workenv_path = None
                if Path(workenv_path).exists():
                    self.checks_passed.append(("env.sh", f"Valid and working → {workenv_path}"))
                else:
                    self.checks_warning.append(
                        ("env.sh Execution", f"Points to non-existent workenv: {workenv_path}")
                    )
        except TimeoutError:
            self.checks_failed.append(
                ("env.sh Execution", "Timed out - possible infinite loop. Regenerate with 'wrknv generate'")
            )
        except Exception as e:
            self.checks_failed.append(
                ("env.sh Execution", f"Unexpected error: {e}. This may be a bug in wrknv")
            )

    def xǁWrknvDoctorǁ_test_env_script_execution__mutmut_26(self, env_script: Path) -> None:
        """Test if env.sh can be executed successfully."""
        try:
            # Try to source env.sh and check if VIRTUAL_ENV is set
            result = run(["bash", "-c", f"source {env_script} && echo $VIRTUAL_ENV"], timeout=10)

            if result.returncode != 0:
                self.checks_failed.append(
                    (
                        "env.sh Execution",
                        f"Failed to execute: {result.stderr.strip()}. Regenerate with 'wrknv generate'",
                    )
                )
            elif not result.stdout.strip():
                self.checks_failed.append(
                    ("env.sh Execution", "VIRTUAL_ENV not set. Regenerate with 'wrknv generate'")
                )
            else:
                workenv_path = result.stdout.strip()
                if Path(None).exists():
                    self.checks_passed.append(("env.sh", f"Valid and working → {workenv_path}"))
                else:
                    self.checks_warning.append(
                        ("env.sh Execution", f"Points to non-existent workenv: {workenv_path}")
                    )
        except TimeoutError:
            self.checks_failed.append(
                ("env.sh Execution", "Timed out - possible infinite loop. Regenerate with 'wrknv generate'")
            )
        except Exception as e:
            self.checks_failed.append(
                ("env.sh Execution", f"Unexpected error: {e}. This may be a bug in wrknv")
            )

    def xǁWrknvDoctorǁ_test_env_script_execution__mutmut_27(self, env_script: Path) -> None:
        """Test if env.sh can be executed successfully."""
        try:
            # Try to source env.sh and check if VIRTUAL_ENV is set
            result = run(["bash", "-c", f"source {env_script} && echo $VIRTUAL_ENV"], timeout=10)

            if result.returncode != 0:
                self.checks_failed.append(
                    (
                        "env.sh Execution",
                        f"Failed to execute: {result.stderr.strip()}. Regenerate with 'wrknv generate'",
                    )
                )
            elif not result.stdout.strip():
                self.checks_failed.append(
                    ("env.sh Execution", "VIRTUAL_ENV not set. Regenerate with 'wrknv generate'")
                )
            else:
                workenv_path = result.stdout.strip()
                if Path(workenv_path).exists():
                    self.checks_passed.append(None)
                else:
                    self.checks_warning.append(
                        ("env.sh Execution", f"Points to non-existent workenv: {workenv_path}")
                    )
        except TimeoutError:
            self.checks_failed.append(
                ("env.sh Execution", "Timed out - possible infinite loop. Regenerate with 'wrknv generate'")
            )
        except Exception as e:
            self.checks_failed.append(
                ("env.sh Execution", f"Unexpected error: {e}. This may be a bug in wrknv")
            )

    def xǁWrknvDoctorǁ_test_env_script_execution__mutmut_28(self, env_script: Path) -> None:
        """Test if env.sh can be executed successfully."""
        try:
            # Try to source env.sh and check if VIRTUAL_ENV is set
            result = run(["bash", "-c", f"source {env_script} && echo $VIRTUAL_ENV"], timeout=10)

            if result.returncode != 0:
                self.checks_failed.append(
                    (
                        "env.sh Execution",
                        f"Failed to execute: {result.stderr.strip()}. Regenerate with 'wrknv generate'",
                    )
                )
            elif not result.stdout.strip():
                self.checks_failed.append(
                    ("env.sh Execution", "VIRTUAL_ENV not set. Regenerate with 'wrknv generate'")
                )
            else:
                workenv_path = result.stdout.strip()
                if Path(workenv_path).exists():
                    self.checks_passed.append(("XXenv.shXX", f"Valid and working → {workenv_path}"))
                else:
                    self.checks_warning.append(
                        ("env.sh Execution", f"Points to non-existent workenv: {workenv_path}")
                    )
        except TimeoutError:
            self.checks_failed.append(
                ("env.sh Execution", "Timed out - possible infinite loop. Regenerate with 'wrknv generate'")
            )
        except Exception as e:
            self.checks_failed.append(
                ("env.sh Execution", f"Unexpected error: {e}. This may be a bug in wrknv")
            )

    def xǁWrknvDoctorǁ_test_env_script_execution__mutmut_29(self, env_script: Path) -> None:
        """Test if env.sh can be executed successfully."""
        try:
            # Try to source env.sh and check if VIRTUAL_ENV is set
            result = run(["bash", "-c", f"source {env_script} && echo $VIRTUAL_ENV"], timeout=10)

            if result.returncode != 0:
                self.checks_failed.append(
                    (
                        "env.sh Execution",
                        f"Failed to execute: {result.stderr.strip()}. Regenerate with 'wrknv generate'",
                    )
                )
            elif not result.stdout.strip():
                self.checks_failed.append(
                    ("env.sh Execution", "VIRTUAL_ENV not set. Regenerate with 'wrknv generate'")
                )
            else:
                workenv_path = result.stdout.strip()
                if Path(workenv_path).exists():
                    self.checks_passed.append(("ENV.SH", f"Valid and working → {workenv_path}"))
                else:
                    self.checks_warning.append(
                        ("env.sh Execution", f"Points to non-existent workenv: {workenv_path}")
                    )
        except TimeoutError:
            self.checks_failed.append(
                ("env.sh Execution", "Timed out - possible infinite loop. Regenerate with 'wrknv generate'")
            )
        except Exception as e:
            self.checks_failed.append(
                ("env.sh Execution", f"Unexpected error: {e}. This may be a bug in wrknv")
            )

    def xǁWrknvDoctorǁ_test_env_script_execution__mutmut_30(self, env_script: Path) -> None:
        """Test if env.sh can be executed successfully."""
        try:
            # Try to source env.sh and check if VIRTUAL_ENV is set
            result = run(["bash", "-c", f"source {env_script} && echo $VIRTUAL_ENV"], timeout=10)

            if result.returncode != 0:
                self.checks_failed.append(
                    (
                        "env.sh Execution",
                        f"Failed to execute: {result.stderr.strip()}. Regenerate with 'wrknv generate'",
                    )
                )
            elif not result.stdout.strip():
                self.checks_failed.append(
                    ("env.sh Execution", "VIRTUAL_ENV not set. Regenerate with 'wrknv generate'")
                )
            else:
                workenv_path = result.stdout.strip()
                if Path(workenv_path).exists():
                    self.checks_passed.append(("env.sh", f"Valid and working → {workenv_path}"))
                else:
                    self.checks_warning.append(
                        None
                    )
        except TimeoutError:
            self.checks_failed.append(
                ("env.sh Execution", "Timed out - possible infinite loop. Regenerate with 'wrknv generate'")
            )
        except Exception as e:
            self.checks_failed.append(
                ("env.sh Execution", f"Unexpected error: {e}. This may be a bug in wrknv")
            )

    def xǁWrknvDoctorǁ_test_env_script_execution__mutmut_31(self, env_script: Path) -> None:
        """Test if env.sh can be executed successfully."""
        try:
            # Try to source env.sh and check if VIRTUAL_ENV is set
            result = run(["bash", "-c", f"source {env_script} && echo $VIRTUAL_ENV"], timeout=10)

            if result.returncode != 0:
                self.checks_failed.append(
                    (
                        "env.sh Execution",
                        f"Failed to execute: {result.stderr.strip()}. Regenerate with 'wrknv generate'",
                    )
                )
            elif not result.stdout.strip():
                self.checks_failed.append(
                    ("env.sh Execution", "VIRTUAL_ENV not set. Regenerate with 'wrknv generate'")
                )
            else:
                workenv_path = result.stdout.strip()
                if Path(workenv_path).exists():
                    self.checks_passed.append(("env.sh", f"Valid and working → {workenv_path}"))
                else:
                    self.checks_warning.append(
                        ("XXenv.sh ExecutionXX", f"Points to non-existent workenv: {workenv_path}")
                    )
        except TimeoutError:
            self.checks_failed.append(
                ("env.sh Execution", "Timed out - possible infinite loop. Regenerate with 'wrknv generate'")
            )
        except Exception as e:
            self.checks_failed.append(
                ("env.sh Execution", f"Unexpected error: {e}. This may be a bug in wrknv")
            )

    def xǁWrknvDoctorǁ_test_env_script_execution__mutmut_32(self, env_script: Path) -> None:
        """Test if env.sh can be executed successfully."""
        try:
            # Try to source env.sh and check if VIRTUAL_ENV is set
            result = run(["bash", "-c", f"source {env_script} && echo $VIRTUAL_ENV"], timeout=10)

            if result.returncode != 0:
                self.checks_failed.append(
                    (
                        "env.sh Execution",
                        f"Failed to execute: {result.stderr.strip()}. Regenerate with 'wrknv generate'",
                    )
                )
            elif not result.stdout.strip():
                self.checks_failed.append(
                    ("env.sh Execution", "VIRTUAL_ENV not set. Regenerate with 'wrknv generate'")
                )
            else:
                workenv_path = result.stdout.strip()
                if Path(workenv_path).exists():
                    self.checks_passed.append(("env.sh", f"Valid and working → {workenv_path}"))
                else:
                    self.checks_warning.append(
                        ("env.sh execution", f"Points to non-existent workenv: {workenv_path}")
                    )
        except TimeoutError:
            self.checks_failed.append(
                ("env.sh Execution", "Timed out - possible infinite loop. Regenerate with 'wrknv generate'")
            )
        except Exception as e:
            self.checks_failed.append(
                ("env.sh Execution", f"Unexpected error: {e}. This may be a bug in wrknv")
            )

    def xǁWrknvDoctorǁ_test_env_script_execution__mutmut_33(self, env_script: Path) -> None:
        """Test if env.sh can be executed successfully."""
        try:
            # Try to source env.sh and check if VIRTUAL_ENV is set
            result = run(["bash", "-c", f"source {env_script} && echo $VIRTUAL_ENV"], timeout=10)

            if result.returncode != 0:
                self.checks_failed.append(
                    (
                        "env.sh Execution",
                        f"Failed to execute: {result.stderr.strip()}. Regenerate with 'wrknv generate'",
                    )
                )
            elif not result.stdout.strip():
                self.checks_failed.append(
                    ("env.sh Execution", "VIRTUAL_ENV not set. Regenerate with 'wrknv generate'")
                )
            else:
                workenv_path = result.stdout.strip()
                if Path(workenv_path).exists():
                    self.checks_passed.append(("env.sh", f"Valid and working → {workenv_path}"))
                else:
                    self.checks_warning.append(
                        ("ENV.SH EXECUTION", f"Points to non-existent workenv: {workenv_path}")
                    )
        except TimeoutError:
            self.checks_failed.append(
                ("env.sh Execution", "Timed out - possible infinite loop. Regenerate with 'wrknv generate'")
            )
        except Exception as e:
            self.checks_failed.append(
                ("env.sh Execution", f"Unexpected error: {e}. This may be a bug in wrknv")
            )

    def xǁWrknvDoctorǁ_test_env_script_execution__mutmut_34(self, env_script: Path) -> None:
        """Test if env.sh can be executed successfully."""
        try:
            # Try to source env.sh and check if VIRTUAL_ENV is set
            result = run(["bash", "-c", f"source {env_script} && echo $VIRTUAL_ENV"], timeout=10)

            if result.returncode != 0:
                self.checks_failed.append(
                    (
                        "env.sh Execution",
                        f"Failed to execute: {result.stderr.strip()}. Regenerate with 'wrknv generate'",
                    )
                )
            elif not result.stdout.strip():
                self.checks_failed.append(
                    ("env.sh Execution", "VIRTUAL_ENV not set. Regenerate with 'wrknv generate'")
                )
            else:
                workenv_path = result.stdout.strip()
                if Path(workenv_path).exists():
                    self.checks_passed.append(("env.sh", f"Valid and working → {workenv_path}"))
                else:
                    self.checks_warning.append(
                        ("env.sh Execution", f"Points to non-existent workenv: {workenv_path}")
                    )
        except TimeoutError:
            self.checks_failed.append(
                None
            )
        except Exception as e:
            self.checks_failed.append(
                ("env.sh Execution", f"Unexpected error: {e}. This may be a bug in wrknv")
            )

    def xǁWrknvDoctorǁ_test_env_script_execution__mutmut_35(self, env_script: Path) -> None:
        """Test if env.sh can be executed successfully."""
        try:
            # Try to source env.sh and check if VIRTUAL_ENV is set
            result = run(["bash", "-c", f"source {env_script} && echo $VIRTUAL_ENV"], timeout=10)

            if result.returncode != 0:
                self.checks_failed.append(
                    (
                        "env.sh Execution",
                        f"Failed to execute: {result.stderr.strip()}. Regenerate with 'wrknv generate'",
                    )
                )
            elif not result.stdout.strip():
                self.checks_failed.append(
                    ("env.sh Execution", "VIRTUAL_ENV not set. Regenerate with 'wrknv generate'")
                )
            else:
                workenv_path = result.stdout.strip()
                if Path(workenv_path).exists():
                    self.checks_passed.append(("env.sh", f"Valid and working → {workenv_path}"))
                else:
                    self.checks_warning.append(
                        ("env.sh Execution", f"Points to non-existent workenv: {workenv_path}")
                    )
        except TimeoutError:
            self.checks_failed.append(
                ("XXenv.sh ExecutionXX", "Timed out - possible infinite loop. Regenerate with 'wrknv generate'")
            )
        except Exception as e:
            self.checks_failed.append(
                ("env.sh Execution", f"Unexpected error: {e}. This may be a bug in wrknv")
            )

    def xǁWrknvDoctorǁ_test_env_script_execution__mutmut_36(self, env_script: Path) -> None:
        """Test if env.sh can be executed successfully."""
        try:
            # Try to source env.sh and check if VIRTUAL_ENV is set
            result = run(["bash", "-c", f"source {env_script} && echo $VIRTUAL_ENV"], timeout=10)

            if result.returncode != 0:
                self.checks_failed.append(
                    (
                        "env.sh Execution",
                        f"Failed to execute: {result.stderr.strip()}. Regenerate with 'wrknv generate'",
                    )
                )
            elif not result.stdout.strip():
                self.checks_failed.append(
                    ("env.sh Execution", "VIRTUAL_ENV not set. Regenerate with 'wrknv generate'")
                )
            else:
                workenv_path = result.stdout.strip()
                if Path(workenv_path).exists():
                    self.checks_passed.append(("env.sh", f"Valid and working → {workenv_path}"))
                else:
                    self.checks_warning.append(
                        ("env.sh Execution", f"Points to non-existent workenv: {workenv_path}")
                    )
        except TimeoutError:
            self.checks_failed.append(
                ("env.sh execution", "Timed out - possible infinite loop. Regenerate with 'wrknv generate'")
            )
        except Exception as e:
            self.checks_failed.append(
                ("env.sh Execution", f"Unexpected error: {e}. This may be a bug in wrknv")
            )

    def xǁWrknvDoctorǁ_test_env_script_execution__mutmut_37(self, env_script: Path) -> None:
        """Test if env.sh can be executed successfully."""
        try:
            # Try to source env.sh and check if VIRTUAL_ENV is set
            result = run(["bash", "-c", f"source {env_script} && echo $VIRTUAL_ENV"], timeout=10)

            if result.returncode != 0:
                self.checks_failed.append(
                    (
                        "env.sh Execution",
                        f"Failed to execute: {result.stderr.strip()}. Regenerate with 'wrknv generate'",
                    )
                )
            elif not result.stdout.strip():
                self.checks_failed.append(
                    ("env.sh Execution", "VIRTUAL_ENV not set. Regenerate with 'wrknv generate'")
                )
            else:
                workenv_path = result.stdout.strip()
                if Path(workenv_path).exists():
                    self.checks_passed.append(("env.sh", f"Valid and working → {workenv_path}"))
                else:
                    self.checks_warning.append(
                        ("env.sh Execution", f"Points to non-existent workenv: {workenv_path}")
                    )
        except TimeoutError:
            self.checks_failed.append(
                ("ENV.SH EXECUTION", "Timed out - possible infinite loop. Regenerate with 'wrknv generate'")
            )
        except Exception as e:
            self.checks_failed.append(
                ("env.sh Execution", f"Unexpected error: {e}. This may be a bug in wrknv")
            )

    def xǁWrknvDoctorǁ_test_env_script_execution__mutmut_38(self, env_script: Path) -> None:
        """Test if env.sh can be executed successfully."""
        try:
            # Try to source env.sh and check if VIRTUAL_ENV is set
            result = run(["bash", "-c", f"source {env_script} && echo $VIRTUAL_ENV"], timeout=10)

            if result.returncode != 0:
                self.checks_failed.append(
                    (
                        "env.sh Execution",
                        f"Failed to execute: {result.stderr.strip()}. Regenerate with 'wrknv generate'",
                    )
                )
            elif not result.stdout.strip():
                self.checks_failed.append(
                    ("env.sh Execution", "VIRTUAL_ENV not set. Regenerate with 'wrknv generate'")
                )
            else:
                workenv_path = result.stdout.strip()
                if Path(workenv_path).exists():
                    self.checks_passed.append(("env.sh", f"Valid and working → {workenv_path}"))
                else:
                    self.checks_warning.append(
                        ("env.sh Execution", f"Points to non-existent workenv: {workenv_path}")
                    )
        except TimeoutError:
            self.checks_failed.append(
                ("env.sh Execution", "XXTimed out - possible infinite loop. Regenerate with 'wrknv generate'XX")
            )
        except Exception as e:
            self.checks_failed.append(
                ("env.sh Execution", f"Unexpected error: {e}. This may be a bug in wrknv")
            )

    def xǁWrknvDoctorǁ_test_env_script_execution__mutmut_39(self, env_script: Path) -> None:
        """Test if env.sh can be executed successfully."""
        try:
            # Try to source env.sh and check if VIRTUAL_ENV is set
            result = run(["bash", "-c", f"source {env_script} && echo $VIRTUAL_ENV"], timeout=10)

            if result.returncode != 0:
                self.checks_failed.append(
                    (
                        "env.sh Execution",
                        f"Failed to execute: {result.stderr.strip()}. Regenerate with 'wrknv generate'",
                    )
                )
            elif not result.stdout.strip():
                self.checks_failed.append(
                    ("env.sh Execution", "VIRTUAL_ENV not set. Regenerate with 'wrknv generate'")
                )
            else:
                workenv_path = result.stdout.strip()
                if Path(workenv_path).exists():
                    self.checks_passed.append(("env.sh", f"Valid and working → {workenv_path}"))
                else:
                    self.checks_warning.append(
                        ("env.sh Execution", f"Points to non-existent workenv: {workenv_path}")
                    )
        except TimeoutError:
            self.checks_failed.append(
                ("env.sh Execution", "timed out - possible infinite loop. regenerate with 'wrknv generate'")
            )
        except Exception as e:
            self.checks_failed.append(
                ("env.sh Execution", f"Unexpected error: {e}. This may be a bug in wrknv")
            )

    def xǁWrknvDoctorǁ_test_env_script_execution__mutmut_40(self, env_script: Path) -> None:
        """Test if env.sh can be executed successfully."""
        try:
            # Try to source env.sh and check if VIRTUAL_ENV is set
            result = run(["bash", "-c", f"source {env_script} && echo $VIRTUAL_ENV"], timeout=10)

            if result.returncode != 0:
                self.checks_failed.append(
                    (
                        "env.sh Execution",
                        f"Failed to execute: {result.stderr.strip()}. Regenerate with 'wrknv generate'",
                    )
                )
            elif not result.stdout.strip():
                self.checks_failed.append(
                    ("env.sh Execution", "VIRTUAL_ENV not set. Regenerate with 'wrknv generate'")
                )
            else:
                workenv_path = result.stdout.strip()
                if Path(workenv_path).exists():
                    self.checks_passed.append(("env.sh", f"Valid and working → {workenv_path}"))
                else:
                    self.checks_warning.append(
                        ("env.sh Execution", f"Points to non-existent workenv: {workenv_path}")
                    )
        except TimeoutError:
            self.checks_failed.append(
                ("env.sh Execution", "TIMED OUT - POSSIBLE INFINITE LOOP. REGENERATE WITH 'WRKNV GENERATE'")
            )
        except Exception as e:
            self.checks_failed.append(
                ("env.sh Execution", f"Unexpected error: {e}. This may be a bug in wrknv")
            )

    def xǁWrknvDoctorǁ_test_env_script_execution__mutmut_41(self, env_script: Path) -> None:
        """Test if env.sh can be executed successfully."""
        try:
            # Try to source env.sh and check if VIRTUAL_ENV is set
            result = run(["bash", "-c", f"source {env_script} && echo $VIRTUAL_ENV"], timeout=10)

            if result.returncode != 0:
                self.checks_failed.append(
                    (
                        "env.sh Execution",
                        f"Failed to execute: {result.stderr.strip()}. Regenerate with 'wrknv generate'",
                    )
                )
            elif not result.stdout.strip():
                self.checks_failed.append(
                    ("env.sh Execution", "VIRTUAL_ENV not set. Regenerate with 'wrknv generate'")
                )
            else:
                workenv_path = result.stdout.strip()
                if Path(workenv_path).exists():
                    self.checks_passed.append(("env.sh", f"Valid and working → {workenv_path}"))
                else:
                    self.checks_warning.append(
                        ("env.sh Execution", f"Points to non-existent workenv: {workenv_path}")
                    )
        except TimeoutError:
            self.checks_failed.append(
                ("env.sh Execution", "Timed out - possible infinite loop. Regenerate with 'wrknv generate'")
            )
        except Exception as e:
            self.checks_failed.append(
                None
            )

    def xǁWrknvDoctorǁ_test_env_script_execution__mutmut_42(self, env_script: Path) -> None:
        """Test if env.sh can be executed successfully."""
        try:
            # Try to source env.sh and check if VIRTUAL_ENV is set
            result = run(["bash", "-c", f"source {env_script} && echo $VIRTUAL_ENV"], timeout=10)

            if result.returncode != 0:
                self.checks_failed.append(
                    (
                        "env.sh Execution",
                        f"Failed to execute: {result.stderr.strip()}. Regenerate with 'wrknv generate'",
                    )
                )
            elif not result.stdout.strip():
                self.checks_failed.append(
                    ("env.sh Execution", "VIRTUAL_ENV not set. Regenerate with 'wrknv generate'")
                )
            else:
                workenv_path = result.stdout.strip()
                if Path(workenv_path).exists():
                    self.checks_passed.append(("env.sh", f"Valid and working → {workenv_path}"))
                else:
                    self.checks_warning.append(
                        ("env.sh Execution", f"Points to non-existent workenv: {workenv_path}")
                    )
        except TimeoutError:
            self.checks_failed.append(
                ("env.sh Execution", "Timed out - possible infinite loop. Regenerate with 'wrknv generate'")
            )
        except Exception as e:
            self.checks_failed.append(
                ("XXenv.sh ExecutionXX", f"Unexpected error: {e}. This may be a bug in wrknv")
            )

    def xǁWrknvDoctorǁ_test_env_script_execution__mutmut_43(self, env_script: Path) -> None:
        """Test if env.sh can be executed successfully."""
        try:
            # Try to source env.sh and check if VIRTUAL_ENV is set
            result = run(["bash", "-c", f"source {env_script} && echo $VIRTUAL_ENV"], timeout=10)

            if result.returncode != 0:
                self.checks_failed.append(
                    (
                        "env.sh Execution",
                        f"Failed to execute: {result.stderr.strip()}. Regenerate with 'wrknv generate'",
                    )
                )
            elif not result.stdout.strip():
                self.checks_failed.append(
                    ("env.sh Execution", "VIRTUAL_ENV not set. Regenerate with 'wrknv generate'")
                )
            else:
                workenv_path = result.stdout.strip()
                if Path(workenv_path).exists():
                    self.checks_passed.append(("env.sh", f"Valid and working → {workenv_path}"))
                else:
                    self.checks_warning.append(
                        ("env.sh Execution", f"Points to non-existent workenv: {workenv_path}")
                    )
        except TimeoutError:
            self.checks_failed.append(
                ("env.sh Execution", "Timed out - possible infinite loop. Regenerate with 'wrknv generate'")
            )
        except Exception as e:
            self.checks_failed.append(
                ("env.sh execution", f"Unexpected error: {e}. This may be a bug in wrknv")
            )

    def xǁWrknvDoctorǁ_test_env_script_execution__mutmut_44(self, env_script: Path) -> None:
        """Test if env.sh can be executed successfully."""
        try:
            # Try to source env.sh and check if VIRTUAL_ENV is set
            result = run(["bash", "-c", f"source {env_script} && echo $VIRTUAL_ENV"], timeout=10)

            if result.returncode != 0:
                self.checks_failed.append(
                    (
                        "env.sh Execution",
                        f"Failed to execute: {result.stderr.strip()}. Regenerate with 'wrknv generate'",
                    )
                )
            elif not result.stdout.strip():
                self.checks_failed.append(
                    ("env.sh Execution", "VIRTUAL_ENV not set. Regenerate with 'wrknv generate'")
                )
            else:
                workenv_path = result.stdout.strip()
                if Path(workenv_path).exists():
                    self.checks_passed.append(("env.sh", f"Valid and working → {workenv_path}"))
                else:
                    self.checks_warning.append(
                        ("env.sh Execution", f"Points to non-existent workenv: {workenv_path}")
                    )
        except TimeoutError:
            self.checks_failed.append(
                ("env.sh Execution", "Timed out - possible infinite loop. Regenerate with 'wrknv generate'")
            )
        except Exception as e:
            self.checks_failed.append(
                ("ENV.SH EXECUTION", f"Unexpected error: {e}. This may be a bug in wrknv")
            )
    
    xǁWrknvDoctorǁ_test_env_script_execution__mutmut_mutants : ClassVar[MutantDict] = {
    'xǁWrknvDoctorǁ_test_env_script_execution__mutmut_1': xǁWrknvDoctorǁ_test_env_script_execution__mutmut_1, 
        'xǁWrknvDoctorǁ_test_env_script_execution__mutmut_2': xǁWrknvDoctorǁ_test_env_script_execution__mutmut_2, 
        'xǁWrknvDoctorǁ_test_env_script_execution__mutmut_3': xǁWrknvDoctorǁ_test_env_script_execution__mutmut_3, 
        'xǁWrknvDoctorǁ_test_env_script_execution__mutmut_4': xǁWrknvDoctorǁ_test_env_script_execution__mutmut_4, 
        'xǁWrknvDoctorǁ_test_env_script_execution__mutmut_5': xǁWrknvDoctorǁ_test_env_script_execution__mutmut_5, 
        'xǁWrknvDoctorǁ_test_env_script_execution__mutmut_6': xǁWrknvDoctorǁ_test_env_script_execution__mutmut_6, 
        'xǁWrknvDoctorǁ_test_env_script_execution__mutmut_7': xǁWrknvDoctorǁ_test_env_script_execution__mutmut_7, 
        'xǁWrknvDoctorǁ_test_env_script_execution__mutmut_8': xǁWrknvDoctorǁ_test_env_script_execution__mutmut_8, 
        'xǁWrknvDoctorǁ_test_env_script_execution__mutmut_9': xǁWrknvDoctorǁ_test_env_script_execution__mutmut_9, 
        'xǁWrknvDoctorǁ_test_env_script_execution__mutmut_10': xǁWrknvDoctorǁ_test_env_script_execution__mutmut_10, 
        'xǁWrknvDoctorǁ_test_env_script_execution__mutmut_11': xǁWrknvDoctorǁ_test_env_script_execution__mutmut_11, 
        'xǁWrknvDoctorǁ_test_env_script_execution__mutmut_12': xǁWrknvDoctorǁ_test_env_script_execution__mutmut_12, 
        'xǁWrknvDoctorǁ_test_env_script_execution__mutmut_13': xǁWrknvDoctorǁ_test_env_script_execution__mutmut_13, 
        'xǁWrknvDoctorǁ_test_env_script_execution__mutmut_14': xǁWrknvDoctorǁ_test_env_script_execution__mutmut_14, 
        'xǁWrknvDoctorǁ_test_env_script_execution__mutmut_15': xǁWrknvDoctorǁ_test_env_script_execution__mutmut_15, 
        'xǁWrknvDoctorǁ_test_env_script_execution__mutmut_16': xǁWrknvDoctorǁ_test_env_script_execution__mutmut_16, 
        'xǁWrknvDoctorǁ_test_env_script_execution__mutmut_17': xǁWrknvDoctorǁ_test_env_script_execution__mutmut_17, 
        'xǁWrknvDoctorǁ_test_env_script_execution__mutmut_18': xǁWrknvDoctorǁ_test_env_script_execution__mutmut_18, 
        'xǁWrknvDoctorǁ_test_env_script_execution__mutmut_19': xǁWrknvDoctorǁ_test_env_script_execution__mutmut_19, 
        'xǁWrknvDoctorǁ_test_env_script_execution__mutmut_20': xǁWrknvDoctorǁ_test_env_script_execution__mutmut_20, 
        'xǁWrknvDoctorǁ_test_env_script_execution__mutmut_21': xǁWrknvDoctorǁ_test_env_script_execution__mutmut_21, 
        'xǁWrknvDoctorǁ_test_env_script_execution__mutmut_22': xǁWrknvDoctorǁ_test_env_script_execution__mutmut_22, 
        'xǁWrknvDoctorǁ_test_env_script_execution__mutmut_23': xǁWrknvDoctorǁ_test_env_script_execution__mutmut_23, 
        'xǁWrknvDoctorǁ_test_env_script_execution__mutmut_24': xǁWrknvDoctorǁ_test_env_script_execution__mutmut_24, 
        'xǁWrknvDoctorǁ_test_env_script_execution__mutmut_25': xǁWrknvDoctorǁ_test_env_script_execution__mutmut_25, 
        'xǁWrknvDoctorǁ_test_env_script_execution__mutmut_26': xǁWrknvDoctorǁ_test_env_script_execution__mutmut_26, 
        'xǁWrknvDoctorǁ_test_env_script_execution__mutmut_27': xǁWrknvDoctorǁ_test_env_script_execution__mutmut_27, 
        'xǁWrknvDoctorǁ_test_env_script_execution__mutmut_28': xǁWrknvDoctorǁ_test_env_script_execution__mutmut_28, 
        'xǁWrknvDoctorǁ_test_env_script_execution__mutmut_29': xǁWrknvDoctorǁ_test_env_script_execution__mutmut_29, 
        'xǁWrknvDoctorǁ_test_env_script_execution__mutmut_30': xǁWrknvDoctorǁ_test_env_script_execution__mutmut_30, 
        'xǁWrknvDoctorǁ_test_env_script_execution__mutmut_31': xǁWrknvDoctorǁ_test_env_script_execution__mutmut_31, 
        'xǁWrknvDoctorǁ_test_env_script_execution__mutmut_32': xǁWrknvDoctorǁ_test_env_script_execution__mutmut_32, 
        'xǁWrknvDoctorǁ_test_env_script_execution__mutmut_33': xǁWrknvDoctorǁ_test_env_script_execution__mutmut_33, 
        'xǁWrknvDoctorǁ_test_env_script_execution__mutmut_34': xǁWrknvDoctorǁ_test_env_script_execution__mutmut_34, 
        'xǁWrknvDoctorǁ_test_env_script_execution__mutmut_35': xǁWrknvDoctorǁ_test_env_script_execution__mutmut_35, 
        'xǁWrknvDoctorǁ_test_env_script_execution__mutmut_36': xǁWrknvDoctorǁ_test_env_script_execution__mutmut_36, 
        'xǁWrknvDoctorǁ_test_env_script_execution__mutmut_37': xǁWrknvDoctorǁ_test_env_script_execution__mutmut_37, 
        'xǁWrknvDoctorǁ_test_env_script_execution__mutmut_38': xǁWrknvDoctorǁ_test_env_script_execution__mutmut_38, 
        'xǁWrknvDoctorǁ_test_env_script_execution__mutmut_39': xǁWrknvDoctorǁ_test_env_script_execution__mutmut_39, 
        'xǁWrknvDoctorǁ_test_env_script_execution__mutmut_40': xǁWrknvDoctorǁ_test_env_script_execution__mutmut_40, 
        'xǁWrknvDoctorǁ_test_env_script_execution__mutmut_41': xǁWrknvDoctorǁ_test_env_script_execution__mutmut_41, 
        'xǁWrknvDoctorǁ_test_env_script_execution__mutmut_42': xǁWrknvDoctorǁ_test_env_script_execution__mutmut_42, 
        'xǁWrknvDoctorǁ_test_env_script_execution__mutmut_43': xǁWrknvDoctorǁ_test_env_script_execution__mutmut_43, 
        'xǁWrknvDoctorǁ_test_env_script_execution__mutmut_44': xǁWrknvDoctorǁ_test_env_script_execution__mutmut_44
    }
    
    def _test_env_script_execution(self, *args, **kwargs):
        result = _mutmut_trampoline(object.__getattribute__(self, "xǁWrknvDoctorǁ_test_env_script_execution__mutmut_orig"), object.__getattribute__(self, "xǁWrknvDoctorǁ_test_env_script_execution__mutmut_mutants"), args, kwargs, self)
        return result 
    
    _test_env_script_execution.__signature__ = _mutmut_signature(xǁWrknvDoctorǁ_test_env_script_execution__mutmut_orig)
    xǁWrknvDoctorǁ_test_env_script_execution__mutmut_orig.__name__ = 'xǁWrknvDoctorǁ_test_env_script_execution'

    def xǁWrknvDoctorǁ_check_config_files__mutmut_orig(self) -> None:
        """Check for wrknv.toml configuration."""
        config_file = Path.cwd() / "wrknv.toml"
        if not config_file.exists():
            config_file = Path.cwd() / ".wrknv.toml"  # Fallback for backwards compat

        if not config_file.exists():
            self.checks_warning.append(("wrknv.toml", "Not found - using defaults"))
            return

        try:
            import tomli

            with config_file.open("rb") as f:
                config = tomli.load(f)

            # Check for required sections
            if "project" in config:
                self.checks_passed.append(("wrknv.toml", "Valid project configuration"))
            else:
                self.checks_warning.append(("wrknv.toml", "Missing [project] section"))

            # Check for tools configuration
            if "tools" in config:
                tool_count = len(config["tools"])
                self.checks_passed.append(("Tools Configuration", f"{tool_count} tools configured"))

            # Check for siblings configuration
            if "siblings" in config:
                patterns = config["siblings"].get("patterns", [])
                if patterns:
                    self.checks_passed.append(("Sibling Packages", f"{len(patterns)} patterns configured"))

        except Exception as e:
            self.checks_failed.append(("wrknv.toml", f"Parse error: {e}"))

    def xǁWrknvDoctorǁ_check_config_files__mutmut_1(self) -> None:
        """Check for wrknv.toml configuration."""
        config_file = None
        if not config_file.exists():
            config_file = Path.cwd() / ".wrknv.toml"  # Fallback for backwards compat

        if not config_file.exists():
            self.checks_warning.append(("wrknv.toml", "Not found - using defaults"))
            return

        try:
            import tomli

            with config_file.open("rb") as f:
                config = tomli.load(f)

            # Check for required sections
            if "project" in config:
                self.checks_passed.append(("wrknv.toml", "Valid project configuration"))
            else:
                self.checks_warning.append(("wrknv.toml", "Missing [project] section"))

            # Check for tools configuration
            if "tools" in config:
                tool_count = len(config["tools"])
                self.checks_passed.append(("Tools Configuration", f"{tool_count} tools configured"))

            # Check for siblings configuration
            if "siblings" in config:
                patterns = config["siblings"].get("patterns", [])
                if patterns:
                    self.checks_passed.append(("Sibling Packages", f"{len(patterns)} patterns configured"))

        except Exception as e:
            self.checks_failed.append(("wrknv.toml", f"Parse error: {e}"))

    def xǁWrknvDoctorǁ_check_config_files__mutmut_2(self) -> None:
        """Check for wrknv.toml configuration."""
        config_file = Path.cwd() * "wrknv.toml"
        if not config_file.exists():
            config_file = Path.cwd() / ".wrknv.toml"  # Fallback for backwards compat

        if not config_file.exists():
            self.checks_warning.append(("wrknv.toml", "Not found - using defaults"))
            return

        try:
            import tomli

            with config_file.open("rb") as f:
                config = tomli.load(f)

            # Check for required sections
            if "project" in config:
                self.checks_passed.append(("wrknv.toml", "Valid project configuration"))
            else:
                self.checks_warning.append(("wrknv.toml", "Missing [project] section"))

            # Check for tools configuration
            if "tools" in config:
                tool_count = len(config["tools"])
                self.checks_passed.append(("Tools Configuration", f"{tool_count} tools configured"))

            # Check for siblings configuration
            if "siblings" in config:
                patterns = config["siblings"].get("patterns", [])
                if patterns:
                    self.checks_passed.append(("Sibling Packages", f"{len(patterns)} patterns configured"))

        except Exception as e:
            self.checks_failed.append(("wrknv.toml", f"Parse error: {e}"))

    def xǁWrknvDoctorǁ_check_config_files__mutmut_3(self) -> None:
        """Check for wrknv.toml configuration."""
        config_file = Path.cwd() / "XXwrknv.tomlXX"
        if not config_file.exists():
            config_file = Path.cwd() / ".wrknv.toml"  # Fallback for backwards compat

        if not config_file.exists():
            self.checks_warning.append(("wrknv.toml", "Not found - using defaults"))
            return

        try:
            import tomli

            with config_file.open("rb") as f:
                config = tomli.load(f)

            # Check for required sections
            if "project" in config:
                self.checks_passed.append(("wrknv.toml", "Valid project configuration"))
            else:
                self.checks_warning.append(("wrknv.toml", "Missing [project] section"))

            # Check for tools configuration
            if "tools" in config:
                tool_count = len(config["tools"])
                self.checks_passed.append(("Tools Configuration", f"{tool_count} tools configured"))

            # Check for siblings configuration
            if "siblings" in config:
                patterns = config["siblings"].get("patterns", [])
                if patterns:
                    self.checks_passed.append(("Sibling Packages", f"{len(patterns)} patterns configured"))

        except Exception as e:
            self.checks_failed.append(("wrknv.toml", f"Parse error: {e}"))

    def xǁWrknvDoctorǁ_check_config_files__mutmut_4(self) -> None:
        """Check for wrknv.toml configuration."""
        config_file = Path.cwd() / "WRKNV.TOML"
        if not config_file.exists():
            config_file = Path.cwd() / ".wrknv.toml"  # Fallback for backwards compat

        if not config_file.exists():
            self.checks_warning.append(("wrknv.toml", "Not found - using defaults"))
            return

        try:
            import tomli

            with config_file.open("rb") as f:
                config = tomli.load(f)

            # Check for required sections
            if "project" in config:
                self.checks_passed.append(("wrknv.toml", "Valid project configuration"))
            else:
                self.checks_warning.append(("wrknv.toml", "Missing [project] section"))

            # Check for tools configuration
            if "tools" in config:
                tool_count = len(config["tools"])
                self.checks_passed.append(("Tools Configuration", f"{tool_count} tools configured"))

            # Check for siblings configuration
            if "siblings" in config:
                patterns = config["siblings"].get("patterns", [])
                if patterns:
                    self.checks_passed.append(("Sibling Packages", f"{len(patterns)} patterns configured"))

        except Exception as e:
            self.checks_failed.append(("wrknv.toml", f"Parse error: {e}"))

    def xǁWrknvDoctorǁ_check_config_files__mutmut_5(self) -> None:
        """Check for wrknv.toml configuration."""
        config_file = Path.cwd() / "wrknv.toml"
        if config_file.exists():
            config_file = Path.cwd() / ".wrknv.toml"  # Fallback for backwards compat

        if not config_file.exists():
            self.checks_warning.append(("wrknv.toml", "Not found - using defaults"))
            return

        try:
            import tomli

            with config_file.open("rb") as f:
                config = tomli.load(f)

            # Check for required sections
            if "project" in config:
                self.checks_passed.append(("wrknv.toml", "Valid project configuration"))
            else:
                self.checks_warning.append(("wrknv.toml", "Missing [project] section"))

            # Check for tools configuration
            if "tools" in config:
                tool_count = len(config["tools"])
                self.checks_passed.append(("Tools Configuration", f"{tool_count} tools configured"))

            # Check for siblings configuration
            if "siblings" in config:
                patterns = config["siblings"].get("patterns", [])
                if patterns:
                    self.checks_passed.append(("Sibling Packages", f"{len(patterns)} patterns configured"))

        except Exception as e:
            self.checks_failed.append(("wrknv.toml", f"Parse error: {e}"))

    def xǁWrknvDoctorǁ_check_config_files__mutmut_6(self) -> None:
        """Check for wrknv.toml configuration."""
        config_file = Path.cwd() / "wrknv.toml"
        if not config_file.exists():
            config_file = None  # Fallback for backwards compat

        if not config_file.exists():
            self.checks_warning.append(("wrknv.toml", "Not found - using defaults"))
            return

        try:
            import tomli

            with config_file.open("rb") as f:
                config = tomli.load(f)

            # Check for required sections
            if "project" in config:
                self.checks_passed.append(("wrknv.toml", "Valid project configuration"))
            else:
                self.checks_warning.append(("wrknv.toml", "Missing [project] section"))

            # Check for tools configuration
            if "tools" in config:
                tool_count = len(config["tools"])
                self.checks_passed.append(("Tools Configuration", f"{tool_count} tools configured"))

            # Check for siblings configuration
            if "siblings" in config:
                patterns = config["siblings"].get("patterns", [])
                if patterns:
                    self.checks_passed.append(("Sibling Packages", f"{len(patterns)} patterns configured"))

        except Exception as e:
            self.checks_failed.append(("wrknv.toml", f"Parse error: {e}"))

    def xǁWrknvDoctorǁ_check_config_files__mutmut_7(self) -> None:
        """Check for wrknv.toml configuration."""
        config_file = Path.cwd() / "wrknv.toml"
        if not config_file.exists():
            config_file = Path.cwd() * ".wrknv.toml"  # Fallback for backwards compat

        if not config_file.exists():
            self.checks_warning.append(("wrknv.toml", "Not found - using defaults"))
            return

        try:
            import tomli

            with config_file.open("rb") as f:
                config = tomli.load(f)

            # Check for required sections
            if "project" in config:
                self.checks_passed.append(("wrknv.toml", "Valid project configuration"))
            else:
                self.checks_warning.append(("wrknv.toml", "Missing [project] section"))

            # Check for tools configuration
            if "tools" in config:
                tool_count = len(config["tools"])
                self.checks_passed.append(("Tools Configuration", f"{tool_count} tools configured"))

            # Check for siblings configuration
            if "siblings" in config:
                patterns = config["siblings"].get("patterns", [])
                if patterns:
                    self.checks_passed.append(("Sibling Packages", f"{len(patterns)} patterns configured"))

        except Exception as e:
            self.checks_failed.append(("wrknv.toml", f"Parse error: {e}"))

    def xǁWrknvDoctorǁ_check_config_files__mutmut_8(self) -> None:
        """Check for wrknv.toml configuration."""
        config_file = Path.cwd() / "wrknv.toml"
        if not config_file.exists():
            config_file = Path.cwd() / "XX.wrknv.tomlXX"  # Fallback for backwards compat

        if not config_file.exists():
            self.checks_warning.append(("wrknv.toml", "Not found - using defaults"))
            return

        try:
            import tomli

            with config_file.open("rb") as f:
                config = tomli.load(f)

            # Check for required sections
            if "project" in config:
                self.checks_passed.append(("wrknv.toml", "Valid project configuration"))
            else:
                self.checks_warning.append(("wrknv.toml", "Missing [project] section"))

            # Check for tools configuration
            if "tools" in config:
                tool_count = len(config["tools"])
                self.checks_passed.append(("Tools Configuration", f"{tool_count} tools configured"))

            # Check for siblings configuration
            if "siblings" in config:
                patterns = config["siblings"].get("patterns", [])
                if patterns:
                    self.checks_passed.append(("Sibling Packages", f"{len(patterns)} patterns configured"))

        except Exception as e:
            self.checks_failed.append(("wrknv.toml", f"Parse error: {e}"))

    def xǁWrknvDoctorǁ_check_config_files__mutmut_9(self) -> None:
        """Check for wrknv.toml configuration."""
        config_file = Path.cwd() / "wrknv.toml"
        if not config_file.exists():
            config_file = Path.cwd() / ".WRKNV.TOML"  # Fallback for backwards compat

        if not config_file.exists():
            self.checks_warning.append(("wrknv.toml", "Not found - using defaults"))
            return

        try:
            import tomli

            with config_file.open("rb") as f:
                config = tomli.load(f)

            # Check for required sections
            if "project" in config:
                self.checks_passed.append(("wrknv.toml", "Valid project configuration"))
            else:
                self.checks_warning.append(("wrknv.toml", "Missing [project] section"))

            # Check for tools configuration
            if "tools" in config:
                tool_count = len(config["tools"])
                self.checks_passed.append(("Tools Configuration", f"{tool_count} tools configured"))

            # Check for siblings configuration
            if "siblings" in config:
                patterns = config["siblings"].get("patterns", [])
                if patterns:
                    self.checks_passed.append(("Sibling Packages", f"{len(patterns)} patterns configured"))

        except Exception as e:
            self.checks_failed.append(("wrknv.toml", f"Parse error: {e}"))

    def xǁWrknvDoctorǁ_check_config_files__mutmut_10(self) -> None:
        """Check for wrknv.toml configuration."""
        config_file = Path.cwd() / "wrknv.toml"
        if not config_file.exists():
            config_file = Path.cwd() / ".wrknv.toml"  # Fallback for backwards compat

        if config_file.exists():
            self.checks_warning.append(("wrknv.toml", "Not found - using defaults"))
            return

        try:
            import tomli

            with config_file.open("rb") as f:
                config = tomli.load(f)

            # Check for required sections
            if "project" in config:
                self.checks_passed.append(("wrknv.toml", "Valid project configuration"))
            else:
                self.checks_warning.append(("wrknv.toml", "Missing [project] section"))

            # Check for tools configuration
            if "tools" in config:
                tool_count = len(config["tools"])
                self.checks_passed.append(("Tools Configuration", f"{tool_count} tools configured"))

            # Check for siblings configuration
            if "siblings" in config:
                patterns = config["siblings"].get("patterns", [])
                if patterns:
                    self.checks_passed.append(("Sibling Packages", f"{len(patterns)} patterns configured"))

        except Exception as e:
            self.checks_failed.append(("wrknv.toml", f"Parse error: {e}"))

    def xǁWrknvDoctorǁ_check_config_files__mutmut_11(self) -> None:
        """Check for wrknv.toml configuration."""
        config_file = Path.cwd() / "wrknv.toml"
        if not config_file.exists():
            config_file = Path.cwd() / ".wrknv.toml"  # Fallback for backwards compat

        if not config_file.exists():
            self.checks_warning.append(None)
            return

        try:
            import tomli

            with config_file.open("rb") as f:
                config = tomli.load(f)

            # Check for required sections
            if "project" in config:
                self.checks_passed.append(("wrknv.toml", "Valid project configuration"))
            else:
                self.checks_warning.append(("wrknv.toml", "Missing [project] section"))

            # Check for tools configuration
            if "tools" in config:
                tool_count = len(config["tools"])
                self.checks_passed.append(("Tools Configuration", f"{tool_count} tools configured"))

            # Check for siblings configuration
            if "siblings" in config:
                patterns = config["siblings"].get("patterns", [])
                if patterns:
                    self.checks_passed.append(("Sibling Packages", f"{len(patterns)} patterns configured"))

        except Exception as e:
            self.checks_failed.append(("wrknv.toml", f"Parse error: {e}"))

    def xǁWrknvDoctorǁ_check_config_files__mutmut_12(self) -> None:
        """Check for wrknv.toml configuration."""
        config_file = Path.cwd() / "wrknv.toml"
        if not config_file.exists():
            config_file = Path.cwd() / ".wrknv.toml"  # Fallback for backwards compat

        if not config_file.exists():
            self.checks_warning.append(("XXwrknv.tomlXX", "Not found - using defaults"))
            return

        try:
            import tomli

            with config_file.open("rb") as f:
                config = tomli.load(f)

            # Check for required sections
            if "project" in config:
                self.checks_passed.append(("wrknv.toml", "Valid project configuration"))
            else:
                self.checks_warning.append(("wrknv.toml", "Missing [project] section"))

            # Check for tools configuration
            if "tools" in config:
                tool_count = len(config["tools"])
                self.checks_passed.append(("Tools Configuration", f"{tool_count} tools configured"))

            # Check for siblings configuration
            if "siblings" in config:
                patterns = config["siblings"].get("patterns", [])
                if patterns:
                    self.checks_passed.append(("Sibling Packages", f"{len(patterns)} patterns configured"))

        except Exception as e:
            self.checks_failed.append(("wrknv.toml", f"Parse error: {e}"))

    def xǁWrknvDoctorǁ_check_config_files__mutmut_13(self) -> None:
        """Check for wrknv.toml configuration."""
        config_file = Path.cwd() / "wrknv.toml"
        if not config_file.exists():
            config_file = Path.cwd() / ".wrknv.toml"  # Fallback for backwards compat

        if not config_file.exists():
            self.checks_warning.append(("WRKNV.TOML", "Not found - using defaults"))
            return

        try:
            import tomli

            with config_file.open("rb") as f:
                config = tomli.load(f)

            # Check for required sections
            if "project" in config:
                self.checks_passed.append(("wrknv.toml", "Valid project configuration"))
            else:
                self.checks_warning.append(("wrknv.toml", "Missing [project] section"))

            # Check for tools configuration
            if "tools" in config:
                tool_count = len(config["tools"])
                self.checks_passed.append(("Tools Configuration", f"{tool_count} tools configured"))

            # Check for siblings configuration
            if "siblings" in config:
                patterns = config["siblings"].get("patterns", [])
                if patterns:
                    self.checks_passed.append(("Sibling Packages", f"{len(patterns)} patterns configured"))

        except Exception as e:
            self.checks_failed.append(("wrknv.toml", f"Parse error: {e}"))

    def xǁWrknvDoctorǁ_check_config_files__mutmut_14(self) -> None:
        """Check for wrknv.toml configuration."""
        config_file = Path.cwd() / "wrknv.toml"
        if not config_file.exists():
            config_file = Path.cwd() / ".wrknv.toml"  # Fallback for backwards compat

        if not config_file.exists():
            self.checks_warning.append(("wrknv.toml", "XXNot found - using defaultsXX"))
            return

        try:
            import tomli

            with config_file.open("rb") as f:
                config = tomli.load(f)

            # Check for required sections
            if "project" in config:
                self.checks_passed.append(("wrknv.toml", "Valid project configuration"))
            else:
                self.checks_warning.append(("wrknv.toml", "Missing [project] section"))

            # Check for tools configuration
            if "tools" in config:
                tool_count = len(config["tools"])
                self.checks_passed.append(("Tools Configuration", f"{tool_count} tools configured"))

            # Check for siblings configuration
            if "siblings" in config:
                patterns = config["siblings"].get("patterns", [])
                if patterns:
                    self.checks_passed.append(("Sibling Packages", f"{len(patterns)} patterns configured"))

        except Exception as e:
            self.checks_failed.append(("wrknv.toml", f"Parse error: {e}"))

    def xǁWrknvDoctorǁ_check_config_files__mutmut_15(self) -> None:
        """Check for wrknv.toml configuration."""
        config_file = Path.cwd() / "wrknv.toml"
        if not config_file.exists():
            config_file = Path.cwd() / ".wrknv.toml"  # Fallback for backwards compat

        if not config_file.exists():
            self.checks_warning.append(("wrknv.toml", "not found - using defaults"))
            return

        try:
            import tomli

            with config_file.open("rb") as f:
                config = tomli.load(f)

            # Check for required sections
            if "project" in config:
                self.checks_passed.append(("wrknv.toml", "Valid project configuration"))
            else:
                self.checks_warning.append(("wrknv.toml", "Missing [project] section"))

            # Check for tools configuration
            if "tools" in config:
                tool_count = len(config["tools"])
                self.checks_passed.append(("Tools Configuration", f"{tool_count} tools configured"))

            # Check for siblings configuration
            if "siblings" in config:
                patterns = config["siblings"].get("patterns", [])
                if patterns:
                    self.checks_passed.append(("Sibling Packages", f"{len(patterns)} patterns configured"))

        except Exception as e:
            self.checks_failed.append(("wrknv.toml", f"Parse error: {e}"))

    def xǁWrknvDoctorǁ_check_config_files__mutmut_16(self) -> None:
        """Check for wrknv.toml configuration."""
        config_file = Path.cwd() / "wrknv.toml"
        if not config_file.exists():
            config_file = Path.cwd() / ".wrknv.toml"  # Fallback for backwards compat

        if not config_file.exists():
            self.checks_warning.append(("wrknv.toml", "NOT FOUND - USING DEFAULTS"))
            return

        try:
            import tomli

            with config_file.open("rb") as f:
                config = tomli.load(f)

            # Check for required sections
            if "project" in config:
                self.checks_passed.append(("wrknv.toml", "Valid project configuration"))
            else:
                self.checks_warning.append(("wrknv.toml", "Missing [project] section"))

            # Check for tools configuration
            if "tools" in config:
                tool_count = len(config["tools"])
                self.checks_passed.append(("Tools Configuration", f"{tool_count} tools configured"))

            # Check for siblings configuration
            if "siblings" in config:
                patterns = config["siblings"].get("patterns", [])
                if patterns:
                    self.checks_passed.append(("Sibling Packages", f"{len(patterns)} patterns configured"))

        except Exception as e:
            self.checks_failed.append(("wrknv.toml", f"Parse error: {e}"))

    def xǁWrknvDoctorǁ_check_config_files__mutmut_17(self) -> None:
        """Check for wrknv.toml configuration."""
        config_file = Path.cwd() / "wrknv.toml"
        if not config_file.exists():
            config_file = Path.cwd() / ".wrknv.toml"  # Fallback for backwards compat

        if not config_file.exists():
            self.checks_warning.append(("wrknv.toml", "Not found - using defaults"))
            return

        try:
            import tomli

            with config_file.open(None) as f:
                config = tomli.load(f)

            # Check for required sections
            if "project" in config:
                self.checks_passed.append(("wrknv.toml", "Valid project configuration"))
            else:
                self.checks_warning.append(("wrknv.toml", "Missing [project] section"))

            # Check for tools configuration
            if "tools" in config:
                tool_count = len(config["tools"])
                self.checks_passed.append(("Tools Configuration", f"{tool_count} tools configured"))

            # Check for siblings configuration
            if "siblings" in config:
                patterns = config["siblings"].get("patterns", [])
                if patterns:
                    self.checks_passed.append(("Sibling Packages", f"{len(patterns)} patterns configured"))

        except Exception as e:
            self.checks_failed.append(("wrknv.toml", f"Parse error: {e}"))

    def xǁWrknvDoctorǁ_check_config_files__mutmut_18(self) -> None:
        """Check for wrknv.toml configuration."""
        config_file = Path.cwd() / "wrknv.toml"
        if not config_file.exists():
            config_file = Path.cwd() / ".wrknv.toml"  # Fallback for backwards compat

        if not config_file.exists():
            self.checks_warning.append(("wrknv.toml", "Not found - using defaults"))
            return

        try:
            import tomli

            with config_file.open("XXrbXX") as f:
                config = tomli.load(f)

            # Check for required sections
            if "project" in config:
                self.checks_passed.append(("wrknv.toml", "Valid project configuration"))
            else:
                self.checks_warning.append(("wrknv.toml", "Missing [project] section"))

            # Check for tools configuration
            if "tools" in config:
                tool_count = len(config["tools"])
                self.checks_passed.append(("Tools Configuration", f"{tool_count} tools configured"))

            # Check for siblings configuration
            if "siblings" in config:
                patterns = config["siblings"].get("patterns", [])
                if patterns:
                    self.checks_passed.append(("Sibling Packages", f"{len(patterns)} patterns configured"))

        except Exception as e:
            self.checks_failed.append(("wrknv.toml", f"Parse error: {e}"))

    def xǁWrknvDoctorǁ_check_config_files__mutmut_19(self) -> None:
        """Check for wrknv.toml configuration."""
        config_file = Path.cwd() / "wrknv.toml"
        if not config_file.exists():
            config_file = Path.cwd() / ".wrknv.toml"  # Fallback for backwards compat

        if not config_file.exists():
            self.checks_warning.append(("wrknv.toml", "Not found - using defaults"))
            return

        try:
            import tomli

            with config_file.open("RB") as f:
                config = tomli.load(f)

            # Check for required sections
            if "project" in config:
                self.checks_passed.append(("wrknv.toml", "Valid project configuration"))
            else:
                self.checks_warning.append(("wrknv.toml", "Missing [project] section"))

            # Check for tools configuration
            if "tools" in config:
                tool_count = len(config["tools"])
                self.checks_passed.append(("Tools Configuration", f"{tool_count} tools configured"))

            # Check for siblings configuration
            if "siblings" in config:
                patterns = config["siblings"].get("patterns", [])
                if patterns:
                    self.checks_passed.append(("Sibling Packages", f"{len(patterns)} patterns configured"))

        except Exception as e:
            self.checks_failed.append(("wrknv.toml", f"Parse error: {e}"))

    def xǁWrknvDoctorǁ_check_config_files__mutmut_20(self) -> None:
        """Check for wrknv.toml configuration."""
        config_file = Path.cwd() / "wrknv.toml"
        if not config_file.exists():
            config_file = Path.cwd() / ".wrknv.toml"  # Fallback for backwards compat

        if not config_file.exists():
            self.checks_warning.append(("wrknv.toml", "Not found - using defaults"))
            return

        try:
            import tomli

            with config_file.open("rb") as f:
                config = None

            # Check for required sections
            if "project" in config:
                self.checks_passed.append(("wrknv.toml", "Valid project configuration"))
            else:
                self.checks_warning.append(("wrknv.toml", "Missing [project] section"))

            # Check for tools configuration
            if "tools" in config:
                tool_count = len(config["tools"])
                self.checks_passed.append(("Tools Configuration", f"{tool_count} tools configured"))

            # Check for siblings configuration
            if "siblings" in config:
                patterns = config["siblings"].get("patterns", [])
                if patterns:
                    self.checks_passed.append(("Sibling Packages", f"{len(patterns)} patterns configured"))

        except Exception as e:
            self.checks_failed.append(("wrknv.toml", f"Parse error: {e}"))

    def xǁWrknvDoctorǁ_check_config_files__mutmut_21(self) -> None:
        """Check for wrknv.toml configuration."""
        config_file = Path.cwd() / "wrknv.toml"
        if not config_file.exists():
            config_file = Path.cwd() / ".wrknv.toml"  # Fallback for backwards compat

        if not config_file.exists():
            self.checks_warning.append(("wrknv.toml", "Not found - using defaults"))
            return

        try:
            import tomli

            with config_file.open("rb") as f:
                config = tomli.load(None)

            # Check for required sections
            if "project" in config:
                self.checks_passed.append(("wrknv.toml", "Valid project configuration"))
            else:
                self.checks_warning.append(("wrknv.toml", "Missing [project] section"))

            # Check for tools configuration
            if "tools" in config:
                tool_count = len(config["tools"])
                self.checks_passed.append(("Tools Configuration", f"{tool_count} tools configured"))

            # Check for siblings configuration
            if "siblings" in config:
                patterns = config["siblings"].get("patterns", [])
                if patterns:
                    self.checks_passed.append(("Sibling Packages", f"{len(patterns)} patterns configured"))

        except Exception as e:
            self.checks_failed.append(("wrknv.toml", f"Parse error: {e}"))

    def xǁWrknvDoctorǁ_check_config_files__mutmut_22(self) -> None:
        """Check for wrknv.toml configuration."""
        config_file = Path.cwd() / "wrknv.toml"
        if not config_file.exists():
            config_file = Path.cwd() / ".wrknv.toml"  # Fallback for backwards compat

        if not config_file.exists():
            self.checks_warning.append(("wrknv.toml", "Not found - using defaults"))
            return

        try:
            import tomli

            with config_file.open("rb") as f:
                config = tomli.load(f)

            # Check for required sections
            if "XXprojectXX" in config:
                self.checks_passed.append(("wrknv.toml", "Valid project configuration"))
            else:
                self.checks_warning.append(("wrknv.toml", "Missing [project] section"))

            # Check for tools configuration
            if "tools" in config:
                tool_count = len(config["tools"])
                self.checks_passed.append(("Tools Configuration", f"{tool_count} tools configured"))

            # Check for siblings configuration
            if "siblings" in config:
                patterns = config["siblings"].get("patterns", [])
                if patterns:
                    self.checks_passed.append(("Sibling Packages", f"{len(patterns)} patterns configured"))

        except Exception as e:
            self.checks_failed.append(("wrknv.toml", f"Parse error: {e}"))

    def xǁWrknvDoctorǁ_check_config_files__mutmut_23(self) -> None:
        """Check for wrknv.toml configuration."""
        config_file = Path.cwd() / "wrknv.toml"
        if not config_file.exists():
            config_file = Path.cwd() / ".wrknv.toml"  # Fallback for backwards compat

        if not config_file.exists():
            self.checks_warning.append(("wrknv.toml", "Not found - using defaults"))
            return

        try:
            import tomli

            with config_file.open("rb") as f:
                config = tomli.load(f)

            # Check for required sections
            if "PROJECT" in config:
                self.checks_passed.append(("wrknv.toml", "Valid project configuration"))
            else:
                self.checks_warning.append(("wrknv.toml", "Missing [project] section"))

            # Check for tools configuration
            if "tools" in config:
                tool_count = len(config["tools"])
                self.checks_passed.append(("Tools Configuration", f"{tool_count} tools configured"))

            # Check for siblings configuration
            if "siblings" in config:
                patterns = config["siblings"].get("patterns", [])
                if patterns:
                    self.checks_passed.append(("Sibling Packages", f"{len(patterns)} patterns configured"))

        except Exception as e:
            self.checks_failed.append(("wrknv.toml", f"Parse error: {e}"))

    def xǁWrknvDoctorǁ_check_config_files__mutmut_24(self) -> None:
        """Check for wrknv.toml configuration."""
        config_file = Path.cwd() / "wrknv.toml"
        if not config_file.exists():
            config_file = Path.cwd() / ".wrknv.toml"  # Fallback for backwards compat

        if not config_file.exists():
            self.checks_warning.append(("wrknv.toml", "Not found - using defaults"))
            return

        try:
            import tomli

            with config_file.open("rb") as f:
                config = tomli.load(f)

            # Check for required sections
            if "project" not in config:
                self.checks_passed.append(("wrknv.toml", "Valid project configuration"))
            else:
                self.checks_warning.append(("wrknv.toml", "Missing [project] section"))

            # Check for tools configuration
            if "tools" in config:
                tool_count = len(config["tools"])
                self.checks_passed.append(("Tools Configuration", f"{tool_count} tools configured"))

            # Check for siblings configuration
            if "siblings" in config:
                patterns = config["siblings"].get("patterns", [])
                if patterns:
                    self.checks_passed.append(("Sibling Packages", f"{len(patterns)} patterns configured"))

        except Exception as e:
            self.checks_failed.append(("wrknv.toml", f"Parse error: {e}"))

    def xǁWrknvDoctorǁ_check_config_files__mutmut_25(self) -> None:
        """Check for wrknv.toml configuration."""
        config_file = Path.cwd() / "wrknv.toml"
        if not config_file.exists():
            config_file = Path.cwd() / ".wrknv.toml"  # Fallback for backwards compat

        if not config_file.exists():
            self.checks_warning.append(("wrknv.toml", "Not found - using defaults"))
            return

        try:
            import tomli

            with config_file.open("rb") as f:
                config = tomli.load(f)

            # Check for required sections
            if "project" in config:
                self.checks_passed.append(None)
            else:
                self.checks_warning.append(("wrknv.toml", "Missing [project] section"))

            # Check for tools configuration
            if "tools" in config:
                tool_count = len(config["tools"])
                self.checks_passed.append(("Tools Configuration", f"{tool_count} tools configured"))

            # Check for siblings configuration
            if "siblings" in config:
                patterns = config["siblings"].get("patterns", [])
                if patterns:
                    self.checks_passed.append(("Sibling Packages", f"{len(patterns)} patterns configured"))

        except Exception as e:
            self.checks_failed.append(("wrknv.toml", f"Parse error: {e}"))

    def xǁWrknvDoctorǁ_check_config_files__mutmut_26(self) -> None:
        """Check for wrknv.toml configuration."""
        config_file = Path.cwd() / "wrknv.toml"
        if not config_file.exists():
            config_file = Path.cwd() / ".wrknv.toml"  # Fallback for backwards compat

        if not config_file.exists():
            self.checks_warning.append(("wrknv.toml", "Not found - using defaults"))
            return

        try:
            import tomli

            with config_file.open("rb") as f:
                config = tomli.load(f)

            # Check for required sections
            if "project" in config:
                self.checks_passed.append(("XXwrknv.tomlXX", "Valid project configuration"))
            else:
                self.checks_warning.append(("wrknv.toml", "Missing [project] section"))

            # Check for tools configuration
            if "tools" in config:
                tool_count = len(config["tools"])
                self.checks_passed.append(("Tools Configuration", f"{tool_count} tools configured"))

            # Check for siblings configuration
            if "siblings" in config:
                patterns = config["siblings"].get("patterns", [])
                if patterns:
                    self.checks_passed.append(("Sibling Packages", f"{len(patterns)} patterns configured"))

        except Exception as e:
            self.checks_failed.append(("wrknv.toml", f"Parse error: {e}"))

    def xǁWrknvDoctorǁ_check_config_files__mutmut_27(self) -> None:
        """Check for wrknv.toml configuration."""
        config_file = Path.cwd() / "wrknv.toml"
        if not config_file.exists():
            config_file = Path.cwd() / ".wrknv.toml"  # Fallback for backwards compat

        if not config_file.exists():
            self.checks_warning.append(("wrknv.toml", "Not found - using defaults"))
            return

        try:
            import tomli

            with config_file.open("rb") as f:
                config = tomli.load(f)

            # Check for required sections
            if "project" in config:
                self.checks_passed.append(("WRKNV.TOML", "Valid project configuration"))
            else:
                self.checks_warning.append(("wrknv.toml", "Missing [project] section"))

            # Check for tools configuration
            if "tools" in config:
                tool_count = len(config["tools"])
                self.checks_passed.append(("Tools Configuration", f"{tool_count} tools configured"))

            # Check for siblings configuration
            if "siblings" in config:
                patterns = config["siblings"].get("patterns", [])
                if patterns:
                    self.checks_passed.append(("Sibling Packages", f"{len(patterns)} patterns configured"))

        except Exception as e:
            self.checks_failed.append(("wrknv.toml", f"Parse error: {e}"))

    def xǁWrknvDoctorǁ_check_config_files__mutmut_28(self) -> None:
        """Check for wrknv.toml configuration."""
        config_file = Path.cwd() / "wrknv.toml"
        if not config_file.exists():
            config_file = Path.cwd() / ".wrknv.toml"  # Fallback for backwards compat

        if not config_file.exists():
            self.checks_warning.append(("wrknv.toml", "Not found - using defaults"))
            return

        try:
            import tomli

            with config_file.open("rb") as f:
                config = tomli.load(f)

            # Check for required sections
            if "project" in config:
                self.checks_passed.append(("wrknv.toml", "XXValid project configurationXX"))
            else:
                self.checks_warning.append(("wrknv.toml", "Missing [project] section"))

            # Check for tools configuration
            if "tools" in config:
                tool_count = len(config["tools"])
                self.checks_passed.append(("Tools Configuration", f"{tool_count} tools configured"))

            # Check for siblings configuration
            if "siblings" in config:
                patterns = config["siblings"].get("patterns", [])
                if patterns:
                    self.checks_passed.append(("Sibling Packages", f"{len(patterns)} patterns configured"))

        except Exception as e:
            self.checks_failed.append(("wrknv.toml", f"Parse error: {e}"))

    def xǁWrknvDoctorǁ_check_config_files__mutmut_29(self) -> None:
        """Check for wrknv.toml configuration."""
        config_file = Path.cwd() / "wrknv.toml"
        if not config_file.exists():
            config_file = Path.cwd() / ".wrknv.toml"  # Fallback for backwards compat

        if not config_file.exists():
            self.checks_warning.append(("wrknv.toml", "Not found - using defaults"))
            return

        try:
            import tomli

            with config_file.open("rb") as f:
                config = tomli.load(f)

            # Check for required sections
            if "project" in config:
                self.checks_passed.append(("wrknv.toml", "valid project configuration"))
            else:
                self.checks_warning.append(("wrknv.toml", "Missing [project] section"))

            # Check for tools configuration
            if "tools" in config:
                tool_count = len(config["tools"])
                self.checks_passed.append(("Tools Configuration", f"{tool_count} tools configured"))

            # Check for siblings configuration
            if "siblings" in config:
                patterns = config["siblings"].get("patterns", [])
                if patterns:
                    self.checks_passed.append(("Sibling Packages", f"{len(patterns)} patterns configured"))

        except Exception as e:
            self.checks_failed.append(("wrknv.toml", f"Parse error: {e}"))

    def xǁWrknvDoctorǁ_check_config_files__mutmut_30(self) -> None:
        """Check for wrknv.toml configuration."""
        config_file = Path.cwd() / "wrknv.toml"
        if not config_file.exists():
            config_file = Path.cwd() / ".wrknv.toml"  # Fallback for backwards compat

        if not config_file.exists():
            self.checks_warning.append(("wrknv.toml", "Not found - using defaults"))
            return

        try:
            import tomli

            with config_file.open("rb") as f:
                config = tomli.load(f)

            # Check for required sections
            if "project" in config:
                self.checks_passed.append(("wrknv.toml", "VALID PROJECT CONFIGURATION"))
            else:
                self.checks_warning.append(("wrknv.toml", "Missing [project] section"))

            # Check for tools configuration
            if "tools" in config:
                tool_count = len(config["tools"])
                self.checks_passed.append(("Tools Configuration", f"{tool_count} tools configured"))

            # Check for siblings configuration
            if "siblings" in config:
                patterns = config["siblings"].get("patterns", [])
                if patterns:
                    self.checks_passed.append(("Sibling Packages", f"{len(patterns)} patterns configured"))

        except Exception as e:
            self.checks_failed.append(("wrknv.toml", f"Parse error: {e}"))

    def xǁWrknvDoctorǁ_check_config_files__mutmut_31(self) -> None:
        """Check for wrknv.toml configuration."""
        config_file = Path.cwd() / "wrknv.toml"
        if not config_file.exists():
            config_file = Path.cwd() / ".wrknv.toml"  # Fallback for backwards compat

        if not config_file.exists():
            self.checks_warning.append(("wrknv.toml", "Not found - using defaults"))
            return

        try:
            import tomli

            with config_file.open("rb") as f:
                config = tomli.load(f)

            # Check for required sections
            if "project" in config:
                self.checks_passed.append(("wrknv.toml", "Valid project configuration"))
            else:
                self.checks_warning.append(None)

            # Check for tools configuration
            if "tools" in config:
                tool_count = len(config["tools"])
                self.checks_passed.append(("Tools Configuration", f"{tool_count} tools configured"))

            # Check for siblings configuration
            if "siblings" in config:
                patterns = config["siblings"].get("patterns", [])
                if patterns:
                    self.checks_passed.append(("Sibling Packages", f"{len(patterns)} patterns configured"))

        except Exception as e:
            self.checks_failed.append(("wrknv.toml", f"Parse error: {e}"))

    def xǁWrknvDoctorǁ_check_config_files__mutmut_32(self) -> None:
        """Check for wrknv.toml configuration."""
        config_file = Path.cwd() / "wrknv.toml"
        if not config_file.exists():
            config_file = Path.cwd() / ".wrknv.toml"  # Fallback for backwards compat

        if not config_file.exists():
            self.checks_warning.append(("wrknv.toml", "Not found - using defaults"))
            return

        try:
            import tomli

            with config_file.open("rb") as f:
                config = tomli.load(f)

            # Check for required sections
            if "project" in config:
                self.checks_passed.append(("wrknv.toml", "Valid project configuration"))
            else:
                self.checks_warning.append(("XXwrknv.tomlXX", "Missing [project] section"))

            # Check for tools configuration
            if "tools" in config:
                tool_count = len(config["tools"])
                self.checks_passed.append(("Tools Configuration", f"{tool_count} tools configured"))

            # Check for siblings configuration
            if "siblings" in config:
                patterns = config["siblings"].get("patterns", [])
                if patterns:
                    self.checks_passed.append(("Sibling Packages", f"{len(patterns)} patterns configured"))

        except Exception as e:
            self.checks_failed.append(("wrknv.toml", f"Parse error: {e}"))

    def xǁWrknvDoctorǁ_check_config_files__mutmut_33(self) -> None:
        """Check for wrknv.toml configuration."""
        config_file = Path.cwd() / "wrknv.toml"
        if not config_file.exists():
            config_file = Path.cwd() / ".wrknv.toml"  # Fallback for backwards compat

        if not config_file.exists():
            self.checks_warning.append(("wrknv.toml", "Not found - using defaults"))
            return

        try:
            import tomli

            with config_file.open("rb") as f:
                config = tomli.load(f)

            # Check for required sections
            if "project" in config:
                self.checks_passed.append(("wrknv.toml", "Valid project configuration"))
            else:
                self.checks_warning.append(("WRKNV.TOML", "Missing [project] section"))

            # Check for tools configuration
            if "tools" in config:
                tool_count = len(config["tools"])
                self.checks_passed.append(("Tools Configuration", f"{tool_count} tools configured"))

            # Check for siblings configuration
            if "siblings" in config:
                patterns = config["siblings"].get("patterns", [])
                if patterns:
                    self.checks_passed.append(("Sibling Packages", f"{len(patterns)} patterns configured"))

        except Exception as e:
            self.checks_failed.append(("wrknv.toml", f"Parse error: {e}"))

    def xǁWrknvDoctorǁ_check_config_files__mutmut_34(self) -> None:
        """Check for wrknv.toml configuration."""
        config_file = Path.cwd() / "wrknv.toml"
        if not config_file.exists():
            config_file = Path.cwd() / ".wrknv.toml"  # Fallback for backwards compat

        if not config_file.exists():
            self.checks_warning.append(("wrknv.toml", "Not found - using defaults"))
            return

        try:
            import tomli

            with config_file.open("rb") as f:
                config = tomli.load(f)

            # Check for required sections
            if "project" in config:
                self.checks_passed.append(("wrknv.toml", "Valid project configuration"))
            else:
                self.checks_warning.append(("wrknv.toml", "XXMissing [project] sectionXX"))

            # Check for tools configuration
            if "tools" in config:
                tool_count = len(config["tools"])
                self.checks_passed.append(("Tools Configuration", f"{tool_count} tools configured"))

            # Check for siblings configuration
            if "siblings" in config:
                patterns = config["siblings"].get("patterns", [])
                if patterns:
                    self.checks_passed.append(("Sibling Packages", f"{len(patterns)} patterns configured"))

        except Exception as e:
            self.checks_failed.append(("wrknv.toml", f"Parse error: {e}"))

    def xǁWrknvDoctorǁ_check_config_files__mutmut_35(self) -> None:
        """Check for wrknv.toml configuration."""
        config_file = Path.cwd() / "wrknv.toml"
        if not config_file.exists():
            config_file = Path.cwd() / ".wrknv.toml"  # Fallback for backwards compat

        if not config_file.exists():
            self.checks_warning.append(("wrknv.toml", "Not found - using defaults"))
            return

        try:
            import tomli

            with config_file.open("rb") as f:
                config = tomli.load(f)

            # Check for required sections
            if "project" in config:
                self.checks_passed.append(("wrknv.toml", "Valid project configuration"))
            else:
                self.checks_warning.append(("wrknv.toml", "missing [project] section"))

            # Check for tools configuration
            if "tools" in config:
                tool_count = len(config["tools"])
                self.checks_passed.append(("Tools Configuration", f"{tool_count} tools configured"))

            # Check for siblings configuration
            if "siblings" in config:
                patterns = config["siblings"].get("patterns", [])
                if patterns:
                    self.checks_passed.append(("Sibling Packages", f"{len(patterns)} patterns configured"))

        except Exception as e:
            self.checks_failed.append(("wrknv.toml", f"Parse error: {e}"))

    def xǁWrknvDoctorǁ_check_config_files__mutmut_36(self) -> None:
        """Check for wrknv.toml configuration."""
        config_file = Path.cwd() / "wrknv.toml"
        if not config_file.exists():
            config_file = Path.cwd() / ".wrknv.toml"  # Fallback for backwards compat

        if not config_file.exists():
            self.checks_warning.append(("wrknv.toml", "Not found - using defaults"))
            return

        try:
            import tomli

            with config_file.open("rb") as f:
                config = tomli.load(f)

            # Check for required sections
            if "project" in config:
                self.checks_passed.append(("wrknv.toml", "Valid project configuration"))
            else:
                self.checks_warning.append(("wrknv.toml", "MISSING [PROJECT] SECTION"))

            # Check for tools configuration
            if "tools" in config:
                tool_count = len(config["tools"])
                self.checks_passed.append(("Tools Configuration", f"{tool_count} tools configured"))

            # Check for siblings configuration
            if "siblings" in config:
                patterns = config["siblings"].get("patterns", [])
                if patterns:
                    self.checks_passed.append(("Sibling Packages", f"{len(patterns)} patterns configured"))

        except Exception as e:
            self.checks_failed.append(("wrknv.toml", f"Parse error: {e}"))

    def xǁWrknvDoctorǁ_check_config_files__mutmut_37(self) -> None:
        """Check for wrknv.toml configuration."""
        config_file = Path.cwd() / "wrknv.toml"
        if not config_file.exists():
            config_file = Path.cwd() / ".wrknv.toml"  # Fallback for backwards compat

        if not config_file.exists():
            self.checks_warning.append(("wrknv.toml", "Not found - using defaults"))
            return

        try:
            import tomli

            with config_file.open("rb") as f:
                config = tomli.load(f)

            # Check for required sections
            if "project" in config:
                self.checks_passed.append(("wrknv.toml", "Valid project configuration"))
            else:
                self.checks_warning.append(("wrknv.toml", "Missing [project] section"))

            # Check for tools configuration
            if "XXtoolsXX" in config:
                tool_count = len(config["tools"])
                self.checks_passed.append(("Tools Configuration", f"{tool_count} tools configured"))

            # Check for siblings configuration
            if "siblings" in config:
                patterns = config["siblings"].get("patterns", [])
                if patterns:
                    self.checks_passed.append(("Sibling Packages", f"{len(patterns)} patterns configured"))

        except Exception as e:
            self.checks_failed.append(("wrknv.toml", f"Parse error: {e}"))

    def xǁWrknvDoctorǁ_check_config_files__mutmut_38(self) -> None:
        """Check for wrknv.toml configuration."""
        config_file = Path.cwd() / "wrknv.toml"
        if not config_file.exists():
            config_file = Path.cwd() / ".wrknv.toml"  # Fallback for backwards compat

        if not config_file.exists():
            self.checks_warning.append(("wrknv.toml", "Not found - using defaults"))
            return

        try:
            import tomli

            with config_file.open("rb") as f:
                config = tomli.load(f)

            # Check for required sections
            if "project" in config:
                self.checks_passed.append(("wrknv.toml", "Valid project configuration"))
            else:
                self.checks_warning.append(("wrknv.toml", "Missing [project] section"))

            # Check for tools configuration
            if "TOOLS" in config:
                tool_count = len(config["tools"])
                self.checks_passed.append(("Tools Configuration", f"{tool_count} tools configured"))

            # Check for siblings configuration
            if "siblings" in config:
                patterns = config["siblings"].get("patterns", [])
                if patterns:
                    self.checks_passed.append(("Sibling Packages", f"{len(patterns)} patterns configured"))

        except Exception as e:
            self.checks_failed.append(("wrknv.toml", f"Parse error: {e}"))

    def xǁWrknvDoctorǁ_check_config_files__mutmut_39(self) -> None:
        """Check for wrknv.toml configuration."""
        config_file = Path.cwd() / "wrknv.toml"
        if not config_file.exists():
            config_file = Path.cwd() / ".wrknv.toml"  # Fallback for backwards compat

        if not config_file.exists():
            self.checks_warning.append(("wrknv.toml", "Not found - using defaults"))
            return

        try:
            import tomli

            with config_file.open("rb") as f:
                config = tomli.load(f)

            # Check for required sections
            if "project" in config:
                self.checks_passed.append(("wrknv.toml", "Valid project configuration"))
            else:
                self.checks_warning.append(("wrknv.toml", "Missing [project] section"))

            # Check for tools configuration
            if "tools" not in config:
                tool_count = len(config["tools"])
                self.checks_passed.append(("Tools Configuration", f"{tool_count} tools configured"))

            # Check for siblings configuration
            if "siblings" in config:
                patterns = config["siblings"].get("patterns", [])
                if patterns:
                    self.checks_passed.append(("Sibling Packages", f"{len(patterns)} patterns configured"))

        except Exception as e:
            self.checks_failed.append(("wrknv.toml", f"Parse error: {e}"))

    def xǁWrknvDoctorǁ_check_config_files__mutmut_40(self) -> None:
        """Check for wrknv.toml configuration."""
        config_file = Path.cwd() / "wrknv.toml"
        if not config_file.exists():
            config_file = Path.cwd() / ".wrknv.toml"  # Fallback for backwards compat

        if not config_file.exists():
            self.checks_warning.append(("wrknv.toml", "Not found - using defaults"))
            return

        try:
            import tomli

            with config_file.open("rb") as f:
                config = tomli.load(f)

            # Check for required sections
            if "project" in config:
                self.checks_passed.append(("wrknv.toml", "Valid project configuration"))
            else:
                self.checks_warning.append(("wrknv.toml", "Missing [project] section"))

            # Check for tools configuration
            if "tools" in config:
                tool_count = None
                self.checks_passed.append(("Tools Configuration", f"{tool_count} tools configured"))

            # Check for siblings configuration
            if "siblings" in config:
                patterns = config["siblings"].get("patterns", [])
                if patterns:
                    self.checks_passed.append(("Sibling Packages", f"{len(patterns)} patterns configured"))

        except Exception as e:
            self.checks_failed.append(("wrknv.toml", f"Parse error: {e}"))

    def xǁWrknvDoctorǁ_check_config_files__mutmut_41(self) -> None:
        """Check for wrknv.toml configuration."""
        config_file = Path.cwd() / "wrknv.toml"
        if not config_file.exists():
            config_file = Path.cwd() / ".wrknv.toml"  # Fallback for backwards compat

        if not config_file.exists():
            self.checks_warning.append(("wrknv.toml", "Not found - using defaults"))
            return

        try:
            import tomli

            with config_file.open("rb") as f:
                config = tomli.load(f)

            # Check for required sections
            if "project" in config:
                self.checks_passed.append(("wrknv.toml", "Valid project configuration"))
            else:
                self.checks_warning.append(("wrknv.toml", "Missing [project] section"))

            # Check for tools configuration
            if "tools" in config:
                tool_count = len(config["tools"])
                self.checks_passed.append(None)

            # Check for siblings configuration
            if "siblings" in config:
                patterns = config["siblings"].get("patterns", [])
                if patterns:
                    self.checks_passed.append(("Sibling Packages", f"{len(patterns)} patterns configured"))

        except Exception as e:
            self.checks_failed.append(("wrknv.toml", f"Parse error: {e}"))

    def xǁWrknvDoctorǁ_check_config_files__mutmut_42(self) -> None:
        """Check for wrknv.toml configuration."""
        config_file = Path.cwd() / "wrknv.toml"
        if not config_file.exists():
            config_file = Path.cwd() / ".wrknv.toml"  # Fallback for backwards compat

        if not config_file.exists():
            self.checks_warning.append(("wrknv.toml", "Not found - using defaults"))
            return

        try:
            import tomli

            with config_file.open("rb") as f:
                config = tomli.load(f)

            # Check for required sections
            if "project" in config:
                self.checks_passed.append(("wrknv.toml", "Valid project configuration"))
            else:
                self.checks_warning.append(("wrknv.toml", "Missing [project] section"))

            # Check for tools configuration
            if "tools" in config:
                tool_count = len(config["tools"])
                self.checks_passed.append(("XXTools ConfigurationXX", f"{tool_count} tools configured"))

            # Check for siblings configuration
            if "siblings" in config:
                patterns = config["siblings"].get("patterns", [])
                if patterns:
                    self.checks_passed.append(("Sibling Packages", f"{len(patterns)} patterns configured"))

        except Exception as e:
            self.checks_failed.append(("wrknv.toml", f"Parse error: {e}"))

    def xǁWrknvDoctorǁ_check_config_files__mutmut_43(self) -> None:
        """Check for wrknv.toml configuration."""
        config_file = Path.cwd() / "wrknv.toml"
        if not config_file.exists():
            config_file = Path.cwd() / ".wrknv.toml"  # Fallback for backwards compat

        if not config_file.exists():
            self.checks_warning.append(("wrknv.toml", "Not found - using defaults"))
            return

        try:
            import tomli

            with config_file.open("rb") as f:
                config = tomli.load(f)

            # Check for required sections
            if "project" in config:
                self.checks_passed.append(("wrknv.toml", "Valid project configuration"))
            else:
                self.checks_warning.append(("wrknv.toml", "Missing [project] section"))

            # Check for tools configuration
            if "tools" in config:
                tool_count = len(config["tools"])
                self.checks_passed.append(("tools configuration", f"{tool_count} tools configured"))

            # Check for siblings configuration
            if "siblings" in config:
                patterns = config["siblings"].get("patterns", [])
                if patterns:
                    self.checks_passed.append(("Sibling Packages", f"{len(patterns)} patterns configured"))

        except Exception as e:
            self.checks_failed.append(("wrknv.toml", f"Parse error: {e}"))

    def xǁWrknvDoctorǁ_check_config_files__mutmut_44(self) -> None:
        """Check for wrknv.toml configuration."""
        config_file = Path.cwd() / "wrknv.toml"
        if not config_file.exists():
            config_file = Path.cwd() / ".wrknv.toml"  # Fallback for backwards compat

        if not config_file.exists():
            self.checks_warning.append(("wrknv.toml", "Not found - using defaults"))
            return

        try:
            import tomli

            with config_file.open("rb") as f:
                config = tomli.load(f)

            # Check for required sections
            if "project" in config:
                self.checks_passed.append(("wrknv.toml", "Valid project configuration"))
            else:
                self.checks_warning.append(("wrknv.toml", "Missing [project] section"))

            # Check for tools configuration
            if "tools" in config:
                tool_count = len(config["tools"])
                self.checks_passed.append(("TOOLS CONFIGURATION", f"{tool_count} tools configured"))

            # Check for siblings configuration
            if "siblings" in config:
                patterns = config["siblings"].get("patterns", [])
                if patterns:
                    self.checks_passed.append(("Sibling Packages", f"{len(patterns)} patterns configured"))

        except Exception as e:
            self.checks_failed.append(("wrknv.toml", f"Parse error: {e}"))

    def xǁWrknvDoctorǁ_check_config_files__mutmut_45(self) -> None:
        """Check for wrknv.toml configuration."""
        config_file = Path.cwd() / "wrknv.toml"
        if not config_file.exists():
            config_file = Path.cwd() / ".wrknv.toml"  # Fallback for backwards compat

        if not config_file.exists():
            self.checks_warning.append(("wrknv.toml", "Not found - using defaults"))
            return

        try:
            import tomli

            with config_file.open("rb") as f:
                config = tomli.load(f)

            # Check for required sections
            if "project" in config:
                self.checks_passed.append(("wrknv.toml", "Valid project configuration"))
            else:
                self.checks_warning.append(("wrknv.toml", "Missing [project] section"))

            # Check for tools configuration
            if "tools" in config:
                tool_count = len(config["tools"])
                self.checks_passed.append(("Tools Configuration", f"{tool_count} tools configured"))

            # Check for siblings configuration
            if "XXsiblingsXX" in config:
                patterns = config["siblings"].get("patterns", [])
                if patterns:
                    self.checks_passed.append(("Sibling Packages", f"{len(patterns)} patterns configured"))

        except Exception as e:
            self.checks_failed.append(("wrknv.toml", f"Parse error: {e}"))

    def xǁWrknvDoctorǁ_check_config_files__mutmut_46(self) -> None:
        """Check for wrknv.toml configuration."""
        config_file = Path.cwd() / "wrknv.toml"
        if not config_file.exists():
            config_file = Path.cwd() / ".wrknv.toml"  # Fallback for backwards compat

        if not config_file.exists():
            self.checks_warning.append(("wrknv.toml", "Not found - using defaults"))
            return

        try:
            import tomli

            with config_file.open("rb") as f:
                config = tomli.load(f)

            # Check for required sections
            if "project" in config:
                self.checks_passed.append(("wrknv.toml", "Valid project configuration"))
            else:
                self.checks_warning.append(("wrknv.toml", "Missing [project] section"))

            # Check for tools configuration
            if "tools" in config:
                tool_count = len(config["tools"])
                self.checks_passed.append(("Tools Configuration", f"{tool_count} tools configured"))

            # Check for siblings configuration
            if "SIBLINGS" in config:
                patterns = config["siblings"].get("patterns", [])
                if patterns:
                    self.checks_passed.append(("Sibling Packages", f"{len(patterns)} patterns configured"))

        except Exception as e:
            self.checks_failed.append(("wrknv.toml", f"Parse error: {e}"))

    def xǁWrknvDoctorǁ_check_config_files__mutmut_47(self) -> None:
        """Check for wrknv.toml configuration."""
        config_file = Path.cwd() / "wrknv.toml"
        if not config_file.exists():
            config_file = Path.cwd() / ".wrknv.toml"  # Fallback for backwards compat

        if not config_file.exists():
            self.checks_warning.append(("wrknv.toml", "Not found - using defaults"))
            return

        try:
            import tomli

            with config_file.open("rb") as f:
                config = tomli.load(f)

            # Check for required sections
            if "project" in config:
                self.checks_passed.append(("wrknv.toml", "Valid project configuration"))
            else:
                self.checks_warning.append(("wrknv.toml", "Missing [project] section"))

            # Check for tools configuration
            if "tools" in config:
                tool_count = len(config["tools"])
                self.checks_passed.append(("Tools Configuration", f"{tool_count} tools configured"))

            # Check for siblings configuration
            if "siblings" not in config:
                patterns = config["siblings"].get("patterns", [])
                if patterns:
                    self.checks_passed.append(("Sibling Packages", f"{len(patterns)} patterns configured"))

        except Exception as e:
            self.checks_failed.append(("wrknv.toml", f"Parse error: {e}"))

    def xǁWrknvDoctorǁ_check_config_files__mutmut_48(self) -> None:
        """Check for wrknv.toml configuration."""
        config_file = Path.cwd() / "wrknv.toml"
        if not config_file.exists():
            config_file = Path.cwd() / ".wrknv.toml"  # Fallback for backwards compat

        if not config_file.exists():
            self.checks_warning.append(("wrknv.toml", "Not found - using defaults"))
            return

        try:
            import tomli

            with config_file.open("rb") as f:
                config = tomli.load(f)

            # Check for required sections
            if "project" in config:
                self.checks_passed.append(("wrknv.toml", "Valid project configuration"))
            else:
                self.checks_warning.append(("wrknv.toml", "Missing [project] section"))

            # Check for tools configuration
            if "tools" in config:
                tool_count = len(config["tools"])
                self.checks_passed.append(("Tools Configuration", f"{tool_count} tools configured"))

            # Check for siblings configuration
            if "siblings" in config:
                patterns = None
                if patterns:
                    self.checks_passed.append(("Sibling Packages", f"{len(patterns)} patterns configured"))

        except Exception as e:
            self.checks_failed.append(("wrknv.toml", f"Parse error: {e}"))

    def xǁWrknvDoctorǁ_check_config_files__mutmut_49(self) -> None:
        """Check for wrknv.toml configuration."""
        config_file = Path.cwd() / "wrknv.toml"
        if not config_file.exists():
            config_file = Path.cwd() / ".wrknv.toml"  # Fallback for backwards compat

        if not config_file.exists():
            self.checks_warning.append(("wrknv.toml", "Not found - using defaults"))
            return

        try:
            import tomli

            with config_file.open("rb") as f:
                config = tomli.load(f)

            # Check for required sections
            if "project" in config:
                self.checks_passed.append(("wrknv.toml", "Valid project configuration"))
            else:
                self.checks_warning.append(("wrknv.toml", "Missing [project] section"))

            # Check for tools configuration
            if "tools" in config:
                tool_count = len(config["tools"])
                self.checks_passed.append(("Tools Configuration", f"{tool_count} tools configured"))

            # Check for siblings configuration
            if "siblings" in config:
                patterns = config["siblings"].get(None, [])
                if patterns:
                    self.checks_passed.append(("Sibling Packages", f"{len(patterns)} patterns configured"))

        except Exception as e:
            self.checks_failed.append(("wrknv.toml", f"Parse error: {e}"))

    def xǁWrknvDoctorǁ_check_config_files__mutmut_50(self) -> None:
        """Check for wrknv.toml configuration."""
        config_file = Path.cwd() / "wrknv.toml"
        if not config_file.exists():
            config_file = Path.cwd() / ".wrknv.toml"  # Fallback for backwards compat

        if not config_file.exists():
            self.checks_warning.append(("wrknv.toml", "Not found - using defaults"))
            return

        try:
            import tomli

            with config_file.open("rb") as f:
                config = tomli.load(f)

            # Check for required sections
            if "project" in config:
                self.checks_passed.append(("wrknv.toml", "Valid project configuration"))
            else:
                self.checks_warning.append(("wrknv.toml", "Missing [project] section"))

            # Check for tools configuration
            if "tools" in config:
                tool_count = len(config["tools"])
                self.checks_passed.append(("Tools Configuration", f"{tool_count} tools configured"))

            # Check for siblings configuration
            if "siblings" in config:
                patterns = config["siblings"].get("patterns", None)
                if patterns:
                    self.checks_passed.append(("Sibling Packages", f"{len(patterns)} patterns configured"))

        except Exception as e:
            self.checks_failed.append(("wrknv.toml", f"Parse error: {e}"))

    def xǁWrknvDoctorǁ_check_config_files__mutmut_51(self) -> None:
        """Check for wrknv.toml configuration."""
        config_file = Path.cwd() / "wrknv.toml"
        if not config_file.exists():
            config_file = Path.cwd() / ".wrknv.toml"  # Fallback for backwards compat

        if not config_file.exists():
            self.checks_warning.append(("wrknv.toml", "Not found - using defaults"))
            return

        try:
            import tomli

            with config_file.open("rb") as f:
                config = tomli.load(f)

            # Check for required sections
            if "project" in config:
                self.checks_passed.append(("wrknv.toml", "Valid project configuration"))
            else:
                self.checks_warning.append(("wrknv.toml", "Missing [project] section"))

            # Check for tools configuration
            if "tools" in config:
                tool_count = len(config["tools"])
                self.checks_passed.append(("Tools Configuration", f"{tool_count} tools configured"))

            # Check for siblings configuration
            if "siblings" in config:
                patterns = config["siblings"].get([])
                if patterns:
                    self.checks_passed.append(("Sibling Packages", f"{len(patterns)} patterns configured"))

        except Exception as e:
            self.checks_failed.append(("wrknv.toml", f"Parse error: {e}"))

    def xǁWrknvDoctorǁ_check_config_files__mutmut_52(self) -> None:
        """Check for wrknv.toml configuration."""
        config_file = Path.cwd() / "wrknv.toml"
        if not config_file.exists():
            config_file = Path.cwd() / ".wrknv.toml"  # Fallback for backwards compat

        if not config_file.exists():
            self.checks_warning.append(("wrknv.toml", "Not found - using defaults"))
            return

        try:
            import tomli

            with config_file.open("rb") as f:
                config = tomli.load(f)

            # Check for required sections
            if "project" in config:
                self.checks_passed.append(("wrknv.toml", "Valid project configuration"))
            else:
                self.checks_warning.append(("wrknv.toml", "Missing [project] section"))

            # Check for tools configuration
            if "tools" in config:
                tool_count = len(config["tools"])
                self.checks_passed.append(("Tools Configuration", f"{tool_count} tools configured"))

            # Check for siblings configuration
            if "siblings" in config:
                patterns = config["siblings"].get("patterns", )
                if patterns:
                    self.checks_passed.append(("Sibling Packages", f"{len(patterns)} patterns configured"))

        except Exception as e:
            self.checks_failed.append(("wrknv.toml", f"Parse error: {e}"))

    def xǁWrknvDoctorǁ_check_config_files__mutmut_53(self) -> None:
        """Check for wrknv.toml configuration."""
        config_file = Path.cwd() / "wrknv.toml"
        if not config_file.exists():
            config_file = Path.cwd() / ".wrknv.toml"  # Fallback for backwards compat

        if not config_file.exists():
            self.checks_warning.append(("wrknv.toml", "Not found - using defaults"))
            return

        try:
            import tomli

            with config_file.open("rb") as f:
                config = tomli.load(f)

            # Check for required sections
            if "project" in config:
                self.checks_passed.append(("wrknv.toml", "Valid project configuration"))
            else:
                self.checks_warning.append(("wrknv.toml", "Missing [project] section"))

            # Check for tools configuration
            if "tools" in config:
                tool_count = len(config["tools"])
                self.checks_passed.append(("Tools Configuration", f"{tool_count} tools configured"))

            # Check for siblings configuration
            if "siblings" in config:
                patterns = config["XXsiblingsXX"].get("patterns", [])
                if patterns:
                    self.checks_passed.append(("Sibling Packages", f"{len(patterns)} patterns configured"))

        except Exception as e:
            self.checks_failed.append(("wrknv.toml", f"Parse error: {e}"))

    def xǁWrknvDoctorǁ_check_config_files__mutmut_54(self) -> None:
        """Check for wrknv.toml configuration."""
        config_file = Path.cwd() / "wrknv.toml"
        if not config_file.exists():
            config_file = Path.cwd() / ".wrknv.toml"  # Fallback for backwards compat

        if not config_file.exists():
            self.checks_warning.append(("wrknv.toml", "Not found - using defaults"))
            return

        try:
            import tomli

            with config_file.open("rb") as f:
                config = tomli.load(f)

            # Check for required sections
            if "project" in config:
                self.checks_passed.append(("wrknv.toml", "Valid project configuration"))
            else:
                self.checks_warning.append(("wrknv.toml", "Missing [project] section"))

            # Check for tools configuration
            if "tools" in config:
                tool_count = len(config["tools"])
                self.checks_passed.append(("Tools Configuration", f"{tool_count} tools configured"))

            # Check for siblings configuration
            if "siblings" in config:
                patterns = config["SIBLINGS"].get("patterns", [])
                if patterns:
                    self.checks_passed.append(("Sibling Packages", f"{len(patterns)} patterns configured"))

        except Exception as e:
            self.checks_failed.append(("wrknv.toml", f"Parse error: {e}"))

    def xǁWrknvDoctorǁ_check_config_files__mutmut_55(self) -> None:
        """Check for wrknv.toml configuration."""
        config_file = Path.cwd() / "wrknv.toml"
        if not config_file.exists():
            config_file = Path.cwd() / ".wrknv.toml"  # Fallback for backwards compat

        if not config_file.exists():
            self.checks_warning.append(("wrknv.toml", "Not found - using defaults"))
            return

        try:
            import tomli

            with config_file.open("rb") as f:
                config = tomli.load(f)

            # Check for required sections
            if "project" in config:
                self.checks_passed.append(("wrknv.toml", "Valid project configuration"))
            else:
                self.checks_warning.append(("wrknv.toml", "Missing [project] section"))

            # Check for tools configuration
            if "tools" in config:
                tool_count = len(config["tools"])
                self.checks_passed.append(("Tools Configuration", f"{tool_count} tools configured"))

            # Check for siblings configuration
            if "siblings" in config:
                patterns = config["siblings"].get("XXpatternsXX", [])
                if patterns:
                    self.checks_passed.append(("Sibling Packages", f"{len(patterns)} patterns configured"))

        except Exception as e:
            self.checks_failed.append(("wrknv.toml", f"Parse error: {e}"))

    def xǁWrknvDoctorǁ_check_config_files__mutmut_56(self) -> None:
        """Check for wrknv.toml configuration."""
        config_file = Path.cwd() / "wrknv.toml"
        if not config_file.exists():
            config_file = Path.cwd() / ".wrknv.toml"  # Fallback for backwards compat

        if not config_file.exists():
            self.checks_warning.append(("wrknv.toml", "Not found - using defaults"))
            return

        try:
            import tomli

            with config_file.open("rb") as f:
                config = tomli.load(f)

            # Check for required sections
            if "project" in config:
                self.checks_passed.append(("wrknv.toml", "Valid project configuration"))
            else:
                self.checks_warning.append(("wrknv.toml", "Missing [project] section"))

            # Check for tools configuration
            if "tools" in config:
                tool_count = len(config["tools"])
                self.checks_passed.append(("Tools Configuration", f"{tool_count} tools configured"))

            # Check for siblings configuration
            if "siblings" in config:
                patterns = config["siblings"].get("PATTERNS", [])
                if patterns:
                    self.checks_passed.append(("Sibling Packages", f"{len(patterns)} patterns configured"))

        except Exception as e:
            self.checks_failed.append(("wrknv.toml", f"Parse error: {e}"))

    def xǁWrknvDoctorǁ_check_config_files__mutmut_57(self) -> None:
        """Check for wrknv.toml configuration."""
        config_file = Path.cwd() / "wrknv.toml"
        if not config_file.exists():
            config_file = Path.cwd() / ".wrknv.toml"  # Fallback for backwards compat

        if not config_file.exists():
            self.checks_warning.append(("wrknv.toml", "Not found - using defaults"))
            return

        try:
            import tomli

            with config_file.open("rb") as f:
                config = tomli.load(f)

            # Check for required sections
            if "project" in config:
                self.checks_passed.append(("wrknv.toml", "Valid project configuration"))
            else:
                self.checks_warning.append(("wrknv.toml", "Missing [project] section"))

            # Check for tools configuration
            if "tools" in config:
                tool_count = len(config["tools"])
                self.checks_passed.append(("Tools Configuration", f"{tool_count} tools configured"))

            # Check for siblings configuration
            if "siblings" in config:
                patterns = config["siblings"].get("patterns", [])
                if patterns:
                    self.checks_passed.append(None)

        except Exception as e:
            self.checks_failed.append(("wrknv.toml", f"Parse error: {e}"))

    def xǁWrknvDoctorǁ_check_config_files__mutmut_58(self) -> None:
        """Check for wrknv.toml configuration."""
        config_file = Path.cwd() / "wrknv.toml"
        if not config_file.exists():
            config_file = Path.cwd() / ".wrknv.toml"  # Fallback for backwards compat

        if not config_file.exists():
            self.checks_warning.append(("wrknv.toml", "Not found - using defaults"))
            return

        try:
            import tomli

            with config_file.open("rb") as f:
                config = tomli.load(f)

            # Check for required sections
            if "project" in config:
                self.checks_passed.append(("wrknv.toml", "Valid project configuration"))
            else:
                self.checks_warning.append(("wrknv.toml", "Missing [project] section"))

            # Check for tools configuration
            if "tools" in config:
                tool_count = len(config["tools"])
                self.checks_passed.append(("Tools Configuration", f"{tool_count} tools configured"))

            # Check for siblings configuration
            if "siblings" in config:
                patterns = config["siblings"].get("patterns", [])
                if patterns:
                    self.checks_passed.append(("XXSibling PackagesXX", f"{len(patterns)} patterns configured"))

        except Exception as e:
            self.checks_failed.append(("wrknv.toml", f"Parse error: {e}"))

    def xǁWrknvDoctorǁ_check_config_files__mutmut_59(self) -> None:
        """Check for wrknv.toml configuration."""
        config_file = Path.cwd() / "wrknv.toml"
        if not config_file.exists():
            config_file = Path.cwd() / ".wrknv.toml"  # Fallback for backwards compat

        if not config_file.exists():
            self.checks_warning.append(("wrknv.toml", "Not found - using defaults"))
            return

        try:
            import tomli

            with config_file.open("rb") as f:
                config = tomli.load(f)

            # Check for required sections
            if "project" in config:
                self.checks_passed.append(("wrknv.toml", "Valid project configuration"))
            else:
                self.checks_warning.append(("wrknv.toml", "Missing [project] section"))

            # Check for tools configuration
            if "tools" in config:
                tool_count = len(config["tools"])
                self.checks_passed.append(("Tools Configuration", f"{tool_count} tools configured"))

            # Check for siblings configuration
            if "siblings" in config:
                patterns = config["siblings"].get("patterns", [])
                if patterns:
                    self.checks_passed.append(("sibling packages", f"{len(patterns)} patterns configured"))

        except Exception as e:
            self.checks_failed.append(("wrknv.toml", f"Parse error: {e}"))

    def xǁWrknvDoctorǁ_check_config_files__mutmut_60(self) -> None:
        """Check for wrknv.toml configuration."""
        config_file = Path.cwd() / "wrknv.toml"
        if not config_file.exists():
            config_file = Path.cwd() / ".wrknv.toml"  # Fallback for backwards compat

        if not config_file.exists():
            self.checks_warning.append(("wrknv.toml", "Not found - using defaults"))
            return

        try:
            import tomli

            with config_file.open("rb") as f:
                config = tomli.load(f)

            # Check for required sections
            if "project" in config:
                self.checks_passed.append(("wrknv.toml", "Valid project configuration"))
            else:
                self.checks_warning.append(("wrknv.toml", "Missing [project] section"))

            # Check for tools configuration
            if "tools" in config:
                tool_count = len(config["tools"])
                self.checks_passed.append(("Tools Configuration", f"{tool_count} tools configured"))

            # Check for siblings configuration
            if "siblings" in config:
                patterns = config["siblings"].get("patterns", [])
                if patterns:
                    self.checks_passed.append(("SIBLING PACKAGES", f"{len(patterns)} patterns configured"))

        except Exception as e:
            self.checks_failed.append(("wrknv.toml", f"Parse error: {e}"))

    def xǁWrknvDoctorǁ_check_config_files__mutmut_61(self) -> None:
        """Check for wrknv.toml configuration."""
        config_file = Path.cwd() / "wrknv.toml"
        if not config_file.exists():
            config_file = Path.cwd() / ".wrknv.toml"  # Fallback for backwards compat

        if not config_file.exists():
            self.checks_warning.append(("wrknv.toml", "Not found - using defaults"))
            return

        try:
            import tomli

            with config_file.open("rb") as f:
                config = tomli.load(f)

            # Check for required sections
            if "project" in config:
                self.checks_passed.append(("wrknv.toml", "Valid project configuration"))
            else:
                self.checks_warning.append(("wrknv.toml", "Missing [project] section"))

            # Check for tools configuration
            if "tools" in config:
                tool_count = len(config["tools"])
                self.checks_passed.append(("Tools Configuration", f"{tool_count} tools configured"))

            # Check for siblings configuration
            if "siblings" in config:
                patterns = config["siblings"].get("patterns", [])
                if patterns:
                    self.checks_passed.append(("Sibling Packages", f"{len(patterns)} patterns configured"))

        except Exception as e:
            self.checks_failed.append(None)

    def xǁWrknvDoctorǁ_check_config_files__mutmut_62(self) -> None:
        """Check for wrknv.toml configuration."""
        config_file = Path.cwd() / "wrknv.toml"
        if not config_file.exists():
            config_file = Path.cwd() / ".wrknv.toml"  # Fallback for backwards compat

        if not config_file.exists():
            self.checks_warning.append(("wrknv.toml", "Not found - using defaults"))
            return

        try:
            import tomli

            with config_file.open("rb") as f:
                config = tomli.load(f)

            # Check for required sections
            if "project" in config:
                self.checks_passed.append(("wrknv.toml", "Valid project configuration"))
            else:
                self.checks_warning.append(("wrknv.toml", "Missing [project] section"))

            # Check for tools configuration
            if "tools" in config:
                tool_count = len(config["tools"])
                self.checks_passed.append(("Tools Configuration", f"{tool_count} tools configured"))

            # Check for siblings configuration
            if "siblings" in config:
                patterns = config["siblings"].get("patterns", [])
                if patterns:
                    self.checks_passed.append(("Sibling Packages", f"{len(patterns)} patterns configured"))

        except Exception as e:
            self.checks_failed.append(("XXwrknv.tomlXX", f"Parse error: {e}"))

    def xǁWrknvDoctorǁ_check_config_files__mutmut_63(self) -> None:
        """Check for wrknv.toml configuration."""
        config_file = Path.cwd() / "wrknv.toml"
        if not config_file.exists():
            config_file = Path.cwd() / ".wrknv.toml"  # Fallback for backwards compat

        if not config_file.exists():
            self.checks_warning.append(("wrknv.toml", "Not found - using defaults"))
            return

        try:
            import tomli

            with config_file.open("rb") as f:
                config = tomli.load(f)

            # Check for required sections
            if "project" in config:
                self.checks_passed.append(("wrknv.toml", "Valid project configuration"))
            else:
                self.checks_warning.append(("wrknv.toml", "Missing [project] section"))

            # Check for tools configuration
            if "tools" in config:
                tool_count = len(config["tools"])
                self.checks_passed.append(("Tools Configuration", f"{tool_count} tools configured"))

            # Check for siblings configuration
            if "siblings" in config:
                patterns = config["siblings"].get("patterns", [])
                if patterns:
                    self.checks_passed.append(("Sibling Packages", f"{len(patterns)} patterns configured"))

        except Exception as e:
            self.checks_failed.append(("WRKNV.TOML", f"Parse error: {e}"))
    
    xǁWrknvDoctorǁ_check_config_files__mutmut_mutants : ClassVar[MutantDict] = {
    'xǁWrknvDoctorǁ_check_config_files__mutmut_1': xǁWrknvDoctorǁ_check_config_files__mutmut_1, 
        'xǁWrknvDoctorǁ_check_config_files__mutmut_2': xǁWrknvDoctorǁ_check_config_files__mutmut_2, 
        'xǁWrknvDoctorǁ_check_config_files__mutmut_3': xǁWrknvDoctorǁ_check_config_files__mutmut_3, 
        'xǁWrknvDoctorǁ_check_config_files__mutmut_4': xǁWrknvDoctorǁ_check_config_files__mutmut_4, 
        'xǁWrknvDoctorǁ_check_config_files__mutmut_5': xǁWrknvDoctorǁ_check_config_files__mutmut_5, 
        'xǁWrknvDoctorǁ_check_config_files__mutmut_6': xǁWrknvDoctorǁ_check_config_files__mutmut_6, 
        'xǁWrknvDoctorǁ_check_config_files__mutmut_7': xǁWrknvDoctorǁ_check_config_files__mutmut_7, 
        'xǁWrknvDoctorǁ_check_config_files__mutmut_8': xǁWrknvDoctorǁ_check_config_files__mutmut_8, 
        'xǁWrknvDoctorǁ_check_config_files__mutmut_9': xǁWrknvDoctorǁ_check_config_files__mutmut_9, 
        'xǁWrknvDoctorǁ_check_config_files__mutmut_10': xǁWrknvDoctorǁ_check_config_files__mutmut_10, 
        'xǁWrknvDoctorǁ_check_config_files__mutmut_11': xǁWrknvDoctorǁ_check_config_files__mutmut_11, 
        'xǁWrknvDoctorǁ_check_config_files__mutmut_12': xǁWrknvDoctorǁ_check_config_files__mutmut_12, 
        'xǁWrknvDoctorǁ_check_config_files__mutmut_13': xǁWrknvDoctorǁ_check_config_files__mutmut_13, 
        'xǁWrknvDoctorǁ_check_config_files__mutmut_14': xǁWrknvDoctorǁ_check_config_files__mutmut_14, 
        'xǁWrknvDoctorǁ_check_config_files__mutmut_15': xǁWrknvDoctorǁ_check_config_files__mutmut_15, 
        'xǁWrknvDoctorǁ_check_config_files__mutmut_16': xǁWrknvDoctorǁ_check_config_files__mutmut_16, 
        'xǁWrknvDoctorǁ_check_config_files__mutmut_17': xǁWrknvDoctorǁ_check_config_files__mutmut_17, 
        'xǁWrknvDoctorǁ_check_config_files__mutmut_18': xǁWrknvDoctorǁ_check_config_files__mutmut_18, 
        'xǁWrknvDoctorǁ_check_config_files__mutmut_19': xǁWrknvDoctorǁ_check_config_files__mutmut_19, 
        'xǁWrknvDoctorǁ_check_config_files__mutmut_20': xǁWrknvDoctorǁ_check_config_files__mutmut_20, 
        'xǁWrknvDoctorǁ_check_config_files__mutmut_21': xǁWrknvDoctorǁ_check_config_files__mutmut_21, 
        'xǁWrknvDoctorǁ_check_config_files__mutmut_22': xǁWrknvDoctorǁ_check_config_files__mutmut_22, 
        'xǁWrknvDoctorǁ_check_config_files__mutmut_23': xǁWrknvDoctorǁ_check_config_files__mutmut_23, 
        'xǁWrknvDoctorǁ_check_config_files__mutmut_24': xǁWrknvDoctorǁ_check_config_files__mutmut_24, 
        'xǁWrknvDoctorǁ_check_config_files__mutmut_25': xǁWrknvDoctorǁ_check_config_files__mutmut_25, 
        'xǁWrknvDoctorǁ_check_config_files__mutmut_26': xǁWrknvDoctorǁ_check_config_files__mutmut_26, 
        'xǁWrknvDoctorǁ_check_config_files__mutmut_27': xǁWrknvDoctorǁ_check_config_files__mutmut_27, 
        'xǁWrknvDoctorǁ_check_config_files__mutmut_28': xǁWrknvDoctorǁ_check_config_files__mutmut_28, 
        'xǁWrknvDoctorǁ_check_config_files__mutmut_29': xǁWrknvDoctorǁ_check_config_files__mutmut_29, 
        'xǁWrknvDoctorǁ_check_config_files__mutmut_30': xǁWrknvDoctorǁ_check_config_files__mutmut_30, 
        'xǁWrknvDoctorǁ_check_config_files__mutmut_31': xǁWrknvDoctorǁ_check_config_files__mutmut_31, 
        'xǁWrknvDoctorǁ_check_config_files__mutmut_32': xǁWrknvDoctorǁ_check_config_files__mutmut_32, 
        'xǁWrknvDoctorǁ_check_config_files__mutmut_33': xǁWrknvDoctorǁ_check_config_files__mutmut_33, 
        'xǁWrknvDoctorǁ_check_config_files__mutmut_34': xǁWrknvDoctorǁ_check_config_files__mutmut_34, 
        'xǁWrknvDoctorǁ_check_config_files__mutmut_35': xǁWrknvDoctorǁ_check_config_files__mutmut_35, 
        'xǁWrknvDoctorǁ_check_config_files__mutmut_36': xǁWrknvDoctorǁ_check_config_files__mutmut_36, 
        'xǁWrknvDoctorǁ_check_config_files__mutmut_37': xǁWrknvDoctorǁ_check_config_files__mutmut_37, 
        'xǁWrknvDoctorǁ_check_config_files__mutmut_38': xǁWrknvDoctorǁ_check_config_files__mutmut_38, 
        'xǁWrknvDoctorǁ_check_config_files__mutmut_39': xǁWrknvDoctorǁ_check_config_files__mutmut_39, 
        'xǁWrknvDoctorǁ_check_config_files__mutmut_40': xǁWrknvDoctorǁ_check_config_files__mutmut_40, 
        'xǁWrknvDoctorǁ_check_config_files__mutmut_41': xǁWrknvDoctorǁ_check_config_files__mutmut_41, 
        'xǁWrknvDoctorǁ_check_config_files__mutmut_42': xǁWrknvDoctorǁ_check_config_files__mutmut_42, 
        'xǁWrknvDoctorǁ_check_config_files__mutmut_43': xǁWrknvDoctorǁ_check_config_files__mutmut_43, 
        'xǁWrknvDoctorǁ_check_config_files__mutmut_44': xǁWrknvDoctorǁ_check_config_files__mutmut_44, 
        'xǁWrknvDoctorǁ_check_config_files__mutmut_45': xǁWrknvDoctorǁ_check_config_files__mutmut_45, 
        'xǁWrknvDoctorǁ_check_config_files__mutmut_46': xǁWrknvDoctorǁ_check_config_files__mutmut_46, 
        'xǁWrknvDoctorǁ_check_config_files__mutmut_47': xǁWrknvDoctorǁ_check_config_files__mutmut_47, 
        'xǁWrknvDoctorǁ_check_config_files__mutmut_48': xǁWrknvDoctorǁ_check_config_files__mutmut_48, 
        'xǁWrknvDoctorǁ_check_config_files__mutmut_49': xǁWrknvDoctorǁ_check_config_files__mutmut_49, 
        'xǁWrknvDoctorǁ_check_config_files__mutmut_50': xǁWrknvDoctorǁ_check_config_files__mutmut_50, 
        'xǁWrknvDoctorǁ_check_config_files__mutmut_51': xǁWrknvDoctorǁ_check_config_files__mutmut_51, 
        'xǁWrknvDoctorǁ_check_config_files__mutmut_52': xǁWrknvDoctorǁ_check_config_files__mutmut_52, 
        'xǁWrknvDoctorǁ_check_config_files__mutmut_53': xǁWrknvDoctorǁ_check_config_files__mutmut_53, 
        'xǁWrknvDoctorǁ_check_config_files__mutmut_54': xǁWrknvDoctorǁ_check_config_files__mutmut_54, 
        'xǁWrknvDoctorǁ_check_config_files__mutmut_55': xǁWrknvDoctorǁ_check_config_files__mutmut_55, 
        'xǁWrknvDoctorǁ_check_config_files__mutmut_56': xǁWrknvDoctorǁ_check_config_files__mutmut_56, 
        'xǁWrknvDoctorǁ_check_config_files__mutmut_57': xǁWrknvDoctorǁ_check_config_files__mutmut_57, 
        'xǁWrknvDoctorǁ_check_config_files__mutmut_58': xǁWrknvDoctorǁ_check_config_files__mutmut_58, 
        'xǁWrknvDoctorǁ_check_config_files__mutmut_59': xǁWrknvDoctorǁ_check_config_files__mutmut_59, 
        'xǁWrknvDoctorǁ_check_config_files__mutmut_60': xǁWrknvDoctorǁ_check_config_files__mutmut_60, 
        'xǁWrknvDoctorǁ_check_config_files__mutmut_61': xǁWrknvDoctorǁ_check_config_files__mutmut_61, 
        'xǁWrknvDoctorǁ_check_config_files__mutmut_62': xǁWrknvDoctorǁ_check_config_files__mutmut_62, 
        'xǁWrknvDoctorǁ_check_config_files__mutmut_63': xǁWrknvDoctorǁ_check_config_files__mutmut_63
    }
    
    def _check_config_files(self, *args, **kwargs):
        result = _mutmut_trampoline(object.__getattribute__(self, "xǁWrknvDoctorǁ_check_config_files__mutmut_orig"), object.__getattribute__(self, "xǁWrknvDoctorǁ_check_config_files__mutmut_mutants"), args, kwargs, self)
        return result 
    
    _check_config_files.__signature__ = _mutmut_signature(xǁWrknvDoctorǁ_check_config_files__mutmut_orig)
    xǁWrknvDoctorǁ_check_config_files__mutmut_orig.__name__ = 'xǁWrknvDoctorǁ_check_config_files'

    def xǁWrknvDoctorǁ_check_sibling_packages__mutmut_orig(self) -> None:
        """Check if configured sibling packages are accessible."""
        config_file = Path.cwd() / "wrknv.toml"
        if not config_file.exists():
            config_file = Path.cwd() / ".wrknv.toml"  # Fallback for backwards compat
        if not config_file.exists():
            return

        try:
            import tomli

            with config_file.open("rb") as f:
                config = tomli.load(f)

            siblings = config.get("siblings", {}).get("patterns", [])
            for sibling in siblings:
                # Check in parent directory
                sibling_path = Path.cwd().parent / sibling
                if sibling_path.exists():
                    self.checks_passed.append((f"Sibling: {sibling}", "Found"))
                else:
                    self.checks_warning.append((f"Sibling: {sibling}", "Not found in parent directory"))

        except Exception:
            pass  # nosec B110 - Already reported in config check

    def xǁWrknvDoctorǁ_check_sibling_packages__mutmut_1(self) -> None:
        """Check if configured sibling packages are accessible."""
        config_file = None
        if not config_file.exists():
            config_file = Path.cwd() / ".wrknv.toml"  # Fallback for backwards compat
        if not config_file.exists():
            return

        try:
            import tomli

            with config_file.open("rb") as f:
                config = tomli.load(f)

            siblings = config.get("siblings", {}).get("patterns", [])
            for sibling in siblings:
                # Check in parent directory
                sibling_path = Path.cwd().parent / sibling
                if sibling_path.exists():
                    self.checks_passed.append((f"Sibling: {sibling}", "Found"))
                else:
                    self.checks_warning.append((f"Sibling: {sibling}", "Not found in parent directory"))

        except Exception:
            pass  # nosec B110 - Already reported in config check

    def xǁWrknvDoctorǁ_check_sibling_packages__mutmut_2(self) -> None:
        """Check if configured sibling packages are accessible."""
        config_file = Path.cwd() * "wrknv.toml"
        if not config_file.exists():
            config_file = Path.cwd() / ".wrknv.toml"  # Fallback for backwards compat
        if not config_file.exists():
            return

        try:
            import tomli

            with config_file.open("rb") as f:
                config = tomli.load(f)

            siblings = config.get("siblings", {}).get("patterns", [])
            for sibling in siblings:
                # Check in parent directory
                sibling_path = Path.cwd().parent / sibling
                if sibling_path.exists():
                    self.checks_passed.append((f"Sibling: {sibling}", "Found"))
                else:
                    self.checks_warning.append((f"Sibling: {sibling}", "Not found in parent directory"))

        except Exception:
            pass  # nosec B110 - Already reported in config check

    def xǁWrknvDoctorǁ_check_sibling_packages__mutmut_3(self) -> None:
        """Check if configured sibling packages are accessible."""
        config_file = Path.cwd() / "XXwrknv.tomlXX"
        if not config_file.exists():
            config_file = Path.cwd() / ".wrknv.toml"  # Fallback for backwards compat
        if not config_file.exists():
            return

        try:
            import tomli

            with config_file.open("rb") as f:
                config = tomli.load(f)

            siblings = config.get("siblings", {}).get("patterns", [])
            for sibling in siblings:
                # Check in parent directory
                sibling_path = Path.cwd().parent / sibling
                if sibling_path.exists():
                    self.checks_passed.append((f"Sibling: {sibling}", "Found"))
                else:
                    self.checks_warning.append((f"Sibling: {sibling}", "Not found in parent directory"))

        except Exception:
            pass  # nosec B110 - Already reported in config check

    def xǁWrknvDoctorǁ_check_sibling_packages__mutmut_4(self) -> None:
        """Check if configured sibling packages are accessible."""
        config_file = Path.cwd() / "WRKNV.TOML"
        if not config_file.exists():
            config_file = Path.cwd() / ".wrknv.toml"  # Fallback for backwards compat
        if not config_file.exists():
            return

        try:
            import tomli

            with config_file.open("rb") as f:
                config = tomli.load(f)

            siblings = config.get("siblings", {}).get("patterns", [])
            for sibling in siblings:
                # Check in parent directory
                sibling_path = Path.cwd().parent / sibling
                if sibling_path.exists():
                    self.checks_passed.append((f"Sibling: {sibling}", "Found"))
                else:
                    self.checks_warning.append((f"Sibling: {sibling}", "Not found in parent directory"))

        except Exception:
            pass  # nosec B110 - Already reported in config check

    def xǁWrknvDoctorǁ_check_sibling_packages__mutmut_5(self) -> None:
        """Check if configured sibling packages are accessible."""
        config_file = Path.cwd() / "wrknv.toml"
        if config_file.exists():
            config_file = Path.cwd() / ".wrknv.toml"  # Fallback for backwards compat
        if not config_file.exists():
            return

        try:
            import tomli

            with config_file.open("rb") as f:
                config = tomli.load(f)

            siblings = config.get("siblings", {}).get("patterns", [])
            for sibling in siblings:
                # Check in parent directory
                sibling_path = Path.cwd().parent / sibling
                if sibling_path.exists():
                    self.checks_passed.append((f"Sibling: {sibling}", "Found"))
                else:
                    self.checks_warning.append((f"Sibling: {sibling}", "Not found in parent directory"))

        except Exception:
            pass  # nosec B110 - Already reported in config check

    def xǁWrknvDoctorǁ_check_sibling_packages__mutmut_6(self) -> None:
        """Check if configured sibling packages are accessible."""
        config_file = Path.cwd() / "wrknv.toml"
        if not config_file.exists():
            config_file = None  # Fallback for backwards compat
        if not config_file.exists():
            return

        try:
            import tomli

            with config_file.open("rb") as f:
                config = tomli.load(f)

            siblings = config.get("siblings", {}).get("patterns", [])
            for sibling in siblings:
                # Check in parent directory
                sibling_path = Path.cwd().parent / sibling
                if sibling_path.exists():
                    self.checks_passed.append((f"Sibling: {sibling}", "Found"))
                else:
                    self.checks_warning.append((f"Sibling: {sibling}", "Not found in parent directory"))

        except Exception:
            pass  # nosec B110 - Already reported in config check

    def xǁWrknvDoctorǁ_check_sibling_packages__mutmut_7(self) -> None:
        """Check if configured sibling packages are accessible."""
        config_file = Path.cwd() / "wrknv.toml"
        if not config_file.exists():
            config_file = Path.cwd() * ".wrknv.toml"  # Fallback for backwards compat
        if not config_file.exists():
            return

        try:
            import tomli

            with config_file.open("rb") as f:
                config = tomli.load(f)

            siblings = config.get("siblings", {}).get("patterns", [])
            for sibling in siblings:
                # Check in parent directory
                sibling_path = Path.cwd().parent / sibling
                if sibling_path.exists():
                    self.checks_passed.append((f"Sibling: {sibling}", "Found"))
                else:
                    self.checks_warning.append((f"Sibling: {sibling}", "Not found in parent directory"))

        except Exception:
            pass  # nosec B110 - Already reported in config check

    def xǁWrknvDoctorǁ_check_sibling_packages__mutmut_8(self) -> None:
        """Check if configured sibling packages are accessible."""
        config_file = Path.cwd() / "wrknv.toml"
        if not config_file.exists():
            config_file = Path.cwd() / "XX.wrknv.tomlXX"  # Fallback for backwards compat
        if not config_file.exists():
            return

        try:
            import tomli

            with config_file.open("rb") as f:
                config = tomli.load(f)

            siblings = config.get("siblings", {}).get("patterns", [])
            for sibling in siblings:
                # Check in parent directory
                sibling_path = Path.cwd().parent / sibling
                if sibling_path.exists():
                    self.checks_passed.append((f"Sibling: {sibling}", "Found"))
                else:
                    self.checks_warning.append((f"Sibling: {sibling}", "Not found in parent directory"))

        except Exception:
            pass  # nosec B110 - Already reported in config check

    def xǁWrknvDoctorǁ_check_sibling_packages__mutmut_9(self) -> None:
        """Check if configured sibling packages are accessible."""
        config_file = Path.cwd() / "wrknv.toml"
        if not config_file.exists():
            config_file = Path.cwd() / ".WRKNV.TOML"  # Fallback for backwards compat
        if not config_file.exists():
            return

        try:
            import tomli

            with config_file.open("rb") as f:
                config = tomli.load(f)

            siblings = config.get("siblings", {}).get("patterns", [])
            for sibling in siblings:
                # Check in parent directory
                sibling_path = Path.cwd().parent / sibling
                if sibling_path.exists():
                    self.checks_passed.append((f"Sibling: {sibling}", "Found"))
                else:
                    self.checks_warning.append((f"Sibling: {sibling}", "Not found in parent directory"))

        except Exception:
            pass  # nosec B110 - Already reported in config check

    def xǁWrknvDoctorǁ_check_sibling_packages__mutmut_10(self) -> None:
        """Check if configured sibling packages are accessible."""
        config_file = Path.cwd() / "wrknv.toml"
        if not config_file.exists():
            config_file = Path.cwd() / ".wrknv.toml"  # Fallback for backwards compat
        if config_file.exists():
            return

        try:
            import tomli

            with config_file.open("rb") as f:
                config = tomli.load(f)

            siblings = config.get("siblings", {}).get("patterns", [])
            for sibling in siblings:
                # Check in parent directory
                sibling_path = Path.cwd().parent / sibling
                if sibling_path.exists():
                    self.checks_passed.append((f"Sibling: {sibling}", "Found"))
                else:
                    self.checks_warning.append((f"Sibling: {sibling}", "Not found in parent directory"))

        except Exception:
            pass  # nosec B110 - Already reported in config check

    def xǁWrknvDoctorǁ_check_sibling_packages__mutmut_11(self) -> None:
        """Check if configured sibling packages are accessible."""
        config_file = Path.cwd() / "wrknv.toml"
        if not config_file.exists():
            config_file = Path.cwd() / ".wrknv.toml"  # Fallback for backwards compat
        if not config_file.exists():
            return

        try:
            import tomli

            with config_file.open(None) as f:
                config = tomli.load(f)

            siblings = config.get("siblings", {}).get("patterns", [])
            for sibling in siblings:
                # Check in parent directory
                sibling_path = Path.cwd().parent / sibling
                if sibling_path.exists():
                    self.checks_passed.append((f"Sibling: {sibling}", "Found"))
                else:
                    self.checks_warning.append((f"Sibling: {sibling}", "Not found in parent directory"))

        except Exception:
            pass  # nosec B110 - Already reported in config check

    def xǁWrknvDoctorǁ_check_sibling_packages__mutmut_12(self) -> None:
        """Check if configured sibling packages are accessible."""
        config_file = Path.cwd() / "wrknv.toml"
        if not config_file.exists():
            config_file = Path.cwd() / ".wrknv.toml"  # Fallback for backwards compat
        if not config_file.exists():
            return

        try:
            import tomli

            with config_file.open("XXrbXX") as f:
                config = tomli.load(f)

            siblings = config.get("siblings", {}).get("patterns", [])
            for sibling in siblings:
                # Check in parent directory
                sibling_path = Path.cwd().parent / sibling
                if sibling_path.exists():
                    self.checks_passed.append((f"Sibling: {sibling}", "Found"))
                else:
                    self.checks_warning.append((f"Sibling: {sibling}", "Not found in parent directory"))

        except Exception:
            pass  # nosec B110 - Already reported in config check

    def xǁWrknvDoctorǁ_check_sibling_packages__mutmut_13(self) -> None:
        """Check if configured sibling packages are accessible."""
        config_file = Path.cwd() / "wrknv.toml"
        if not config_file.exists():
            config_file = Path.cwd() / ".wrknv.toml"  # Fallback for backwards compat
        if not config_file.exists():
            return

        try:
            import tomli

            with config_file.open("RB") as f:
                config = tomli.load(f)

            siblings = config.get("siblings", {}).get("patterns", [])
            for sibling in siblings:
                # Check in parent directory
                sibling_path = Path.cwd().parent / sibling
                if sibling_path.exists():
                    self.checks_passed.append((f"Sibling: {sibling}", "Found"))
                else:
                    self.checks_warning.append((f"Sibling: {sibling}", "Not found in parent directory"))

        except Exception:
            pass  # nosec B110 - Already reported in config check

    def xǁWrknvDoctorǁ_check_sibling_packages__mutmut_14(self) -> None:
        """Check if configured sibling packages are accessible."""
        config_file = Path.cwd() / "wrknv.toml"
        if not config_file.exists():
            config_file = Path.cwd() / ".wrknv.toml"  # Fallback for backwards compat
        if not config_file.exists():
            return

        try:
            import tomli

            with config_file.open("rb") as f:
                config = None

            siblings = config.get("siblings", {}).get("patterns", [])
            for sibling in siblings:
                # Check in parent directory
                sibling_path = Path.cwd().parent / sibling
                if sibling_path.exists():
                    self.checks_passed.append((f"Sibling: {sibling}", "Found"))
                else:
                    self.checks_warning.append((f"Sibling: {sibling}", "Not found in parent directory"))

        except Exception:
            pass  # nosec B110 - Already reported in config check

    def xǁWrknvDoctorǁ_check_sibling_packages__mutmut_15(self) -> None:
        """Check if configured sibling packages are accessible."""
        config_file = Path.cwd() / "wrknv.toml"
        if not config_file.exists():
            config_file = Path.cwd() / ".wrknv.toml"  # Fallback for backwards compat
        if not config_file.exists():
            return

        try:
            import tomli

            with config_file.open("rb") as f:
                config = tomli.load(None)

            siblings = config.get("siblings", {}).get("patterns", [])
            for sibling in siblings:
                # Check in parent directory
                sibling_path = Path.cwd().parent / sibling
                if sibling_path.exists():
                    self.checks_passed.append((f"Sibling: {sibling}", "Found"))
                else:
                    self.checks_warning.append((f"Sibling: {sibling}", "Not found in parent directory"))

        except Exception:
            pass  # nosec B110 - Already reported in config check

    def xǁWrknvDoctorǁ_check_sibling_packages__mutmut_16(self) -> None:
        """Check if configured sibling packages are accessible."""
        config_file = Path.cwd() / "wrknv.toml"
        if not config_file.exists():
            config_file = Path.cwd() / ".wrknv.toml"  # Fallback for backwards compat
        if not config_file.exists():
            return

        try:
            import tomli

            with config_file.open("rb") as f:
                config = tomli.load(f)

            siblings = None
            for sibling in siblings:
                # Check in parent directory
                sibling_path = Path.cwd().parent / sibling
                if sibling_path.exists():
                    self.checks_passed.append((f"Sibling: {sibling}", "Found"))
                else:
                    self.checks_warning.append((f"Sibling: {sibling}", "Not found in parent directory"))

        except Exception:
            pass  # nosec B110 - Already reported in config check

    def xǁWrknvDoctorǁ_check_sibling_packages__mutmut_17(self) -> None:
        """Check if configured sibling packages are accessible."""
        config_file = Path.cwd() / "wrknv.toml"
        if not config_file.exists():
            config_file = Path.cwd() / ".wrknv.toml"  # Fallback for backwards compat
        if not config_file.exists():
            return

        try:
            import tomli

            with config_file.open("rb") as f:
                config = tomli.load(f)

            siblings = config.get("siblings", {}).get(None, [])
            for sibling in siblings:
                # Check in parent directory
                sibling_path = Path.cwd().parent / sibling
                if sibling_path.exists():
                    self.checks_passed.append((f"Sibling: {sibling}", "Found"))
                else:
                    self.checks_warning.append((f"Sibling: {sibling}", "Not found in parent directory"))

        except Exception:
            pass  # nosec B110 - Already reported in config check

    def xǁWrknvDoctorǁ_check_sibling_packages__mutmut_18(self) -> None:
        """Check if configured sibling packages are accessible."""
        config_file = Path.cwd() / "wrknv.toml"
        if not config_file.exists():
            config_file = Path.cwd() / ".wrknv.toml"  # Fallback for backwards compat
        if not config_file.exists():
            return

        try:
            import tomli

            with config_file.open("rb") as f:
                config = tomli.load(f)

            siblings = config.get("siblings", {}).get("patterns", None)
            for sibling in siblings:
                # Check in parent directory
                sibling_path = Path.cwd().parent / sibling
                if sibling_path.exists():
                    self.checks_passed.append((f"Sibling: {sibling}", "Found"))
                else:
                    self.checks_warning.append((f"Sibling: {sibling}", "Not found in parent directory"))

        except Exception:
            pass  # nosec B110 - Already reported in config check

    def xǁWrknvDoctorǁ_check_sibling_packages__mutmut_19(self) -> None:
        """Check if configured sibling packages are accessible."""
        config_file = Path.cwd() / "wrknv.toml"
        if not config_file.exists():
            config_file = Path.cwd() / ".wrknv.toml"  # Fallback for backwards compat
        if not config_file.exists():
            return

        try:
            import tomli

            with config_file.open("rb") as f:
                config = tomli.load(f)

            siblings = config.get("siblings", {}).get([])
            for sibling in siblings:
                # Check in parent directory
                sibling_path = Path.cwd().parent / sibling
                if sibling_path.exists():
                    self.checks_passed.append((f"Sibling: {sibling}", "Found"))
                else:
                    self.checks_warning.append((f"Sibling: {sibling}", "Not found in parent directory"))

        except Exception:
            pass  # nosec B110 - Already reported in config check

    def xǁWrknvDoctorǁ_check_sibling_packages__mutmut_20(self) -> None:
        """Check if configured sibling packages are accessible."""
        config_file = Path.cwd() / "wrknv.toml"
        if not config_file.exists():
            config_file = Path.cwd() / ".wrknv.toml"  # Fallback for backwards compat
        if not config_file.exists():
            return

        try:
            import tomli

            with config_file.open("rb") as f:
                config = tomli.load(f)

            siblings = config.get("siblings", {}).get("patterns", )
            for sibling in siblings:
                # Check in parent directory
                sibling_path = Path.cwd().parent / sibling
                if sibling_path.exists():
                    self.checks_passed.append((f"Sibling: {sibling}", "Found"))
                else:
                    self.checks_warning.append((f"Sibling: {sibling}", "Not found in parent directory"))

        except Exception:
            pass  # nosec B110 - Already reported in config check

    def xǁWrknvDoctorǁ_check_sibling_packages__mutmut_21(self) -> None:
        """Check if configured sibling packages are accessible."""
        config_file = Path.cwd() / "wrknv.toml"
        if not config_file.exists():
            config_file = Path.cwd() / ".wrknv.toml"  # Fallback for backwards compat
        if not config_file.exists():
            return

        try:
            import tomli

            with config_file.open("rb") as f:
                config = tomli.load(f)

            siblings = config.get(None, {}).get("patterns", [])
            for sibling in siblings:
                # Check in parent directory
                sibling_path = Path.cwd().parent / sibling
                if sibling_path.exists():
                    self.checks_passed.append((f"Sibling: {sibling}", "Found"))
                else:
                    self.checks_warning.append((f"Sibling: {sibling}", "Not found in parent directory"))

        except Exception:
            pass  # nosec B110 - Already reported in config check

    def xǁWrknvDoctorǁ_check_sibling_packages__mutmut_22(self) -> None:
        """Check if configured sibling packages are accessible."""
        config_file = Path.cwd() / "wrknv.toml"
        if not config_file.exists():
            config_file = Path.cwd() / ".wrknv.toml"  # Fallback for backwards compat
        if not config_file.exists():
            return

        try:
            import tomli

            with config_file.open("rb") as f:
                config = tomli.load(f)

            siblings = config.get("siblings", None).get("patterns", [])
            for sibling in siblings:
                # Check in parent directory
                sibling_path = Path.cwd().parent / sibling
                if sibling_path.exists():
                    self.checks_passed.append((f"Sibling: {sibling}", "Found"))
                else:
                    self.checks_warning.append((f"Sibling: {sibling}", "Not found in parent directory"))

        except Exception:
            pass  # nosec B110 - Already reported in config check

    def xǁWrknvDoctorǁ_check_sibling_packages__mutmut_23(self) -> None:
        """Check if configured sibling packages are accessible."""
        config_file = Path.cwd() / "wrknv.toml"
        if not config_file.exists():
            config_file = Path.cwd() / ".wrknv.toml"  # Fallback for backwards compat
        if not config_file.exists():
            return

        try:
            import tomli

            with config_file.open("rb") as f:
                config = tomli.load(f)

            siblings = config.get({}).get("patterns", [])
            for sibling in siblings:
                # Check in parent directory
                sibling_path = Path.cwd().parent / sibling
                if sibling_path.exists():
                    self.checks_passed.append((f"Sibling: {sibling}", "Found"))
                else:
                    self.checks_warning.append((f"Sibling: {sibling}", "Not found in parent directory"))

        except Exception:
            pass  # nosec B110 - Already reported in config check

    def xǁWrknvDoctorǁ_check_sibling_packages__mutmut_24(self) -> None:
        """Check if configured sibling packages are accessible."""
        config_file = Path.cwd() / "wrknv.toml"
        if not config_file.exists():
            config_file = Path.cwd() / ".wrknv.toml"  # Fallback for backwards compat
        if not config_file.exists():
            return

        try:
            import tomli

            with config_file.open("rb") as f:
                config = tomli.load(f)

            siblings = config.get("siblings", ).get("patterns", [])
            for sibling in siblings:
                # Check in parent directory
                sibling_path = Path.cwd().parent / sibling
                if sibling_path.exists():
                    self.checks_passed.append((f"Sibling: {sibling}", "Found"))
                else:
                    self.checks_warning.append((f"Sibling: {sibling}", "Not found in parent directory"))

        except Exception:
            pass  # nosec B110 - Already reported in config check

    def xǁWrknvDoctorǁ_check_sibling_packages__mutmut_25(self) -> None:
        """Check if configured sibling packages are accessible."""
        config_file = Path.cwd() / "wrknv.toml"
        if not config_file.exists():
            config_file = Path.cwd() / ".wrknv.toml"  # Fallback for backwards compat
        if not config_file.exists():
            return

        try:
            import tomli

            with config_file.open("rb") as f:
                config = tomli.load(f)

            siblings = config.get("XXsiblingsXX", {}).get("patterns", [])
            for sibling in siblings:
                # Check in parent directory
                sibling_path = Path.cwd().parent / sibling
                if sibling_path.exists():
                    self.checks_passed.append((f"Sibling: {sibling}", "Found"))
                else:
                    self.checks_warning.append((f"Sibling: {sibling}", "Not found in parent directory"))

        except Exception:
            pass  # nosec B110 - Already reported in config check

    def xǁWrknvDoctorǁ_check_sibling_packages__mutmut_26(self) -> None:
        """Check if configured sibling packages are accessible."""
        config_file = Path.cwd() / "wrknv.toml"
        if not config_file.exists():
            config_file = Path.cwd() / ".wrknv.toml"  # Fallback for backwards compat
        if not config_file.exists():
            return

        try:
            import tomli

            with config_file.open("rb") as f:
                config = tomli.load(f)

            siblings = config.get("SIBLINGS", {}).get("patterns", [])
            for sibling in siblings:
                # Check in parent directory
                sibling_path = Path.cwd().parent / sibling
                if sibling_path.exists():
                    self.checks_passed.append((f"Sibling: {sibling}", "Found"))
                else:
                    self.checks_warning.append((f"Sibling: {sibling}", "Not found in parent directory"))

        except Exception:
            pass  # nosec B110 - Already reported in config check

    def xǁWrknvDoctorǁ_check_sibling_packages__mutmut_27(self) -> None:
        """Check if configured sibling packages are accessible."""
        config_file = Path.cwd() / "wrknv.toml"
        if not config_file.exists():
            config_file = Path.cwd() / ".wrknv.toml"  # Fallback for backwards compat
        if not config_file.exists():
            return

        try:
            import tomli

            with config_file.open("rb") as f:
                config = tomli.load(f)

            siblings = config.get("siblings", {}).get("XXpatternsXX", [])
            for sibling in siblings:
                # Check in parent directory
                sibling_path = Path.cwd().parent / sibling
                if sibling_path.exists():
                    self.checks_passed.append((f"Sibling: {sibling}", "Found"))
                else:
                    self.checks_warning.append((f"Sibling: {sibling}", "Not found in parent directory"))

        except Exception:
            pass  # nosec B110 - Already reported in config check

    def xǁWrknvDoctorǁ_check_sibling_packages__mutmut_28(self) -> None:
        """Check if configured sibling packages are accessible."""
        config_file = Path.cwd() / "wrknv.toml"
        if not config_file.exists():
            config_file = Path.cwd() / ".wrknv.toml"  # Fallback for backwards compat
        if not config_file.exists():
            return

        try:
            import tomli

            with config_file.open("rb") as f:
                config = tomli.load(f)

            siblings = config.get("siblings", {}).get("PATTERNS", [])
            for sibling in siblings:
                # Check in parent directory
                sibling_path = Path.cwd().parent / sibling
                if sibling_path.exists():
                    self.checks_passed.append((f"Sibling: {sibling}", "Found"))
                else:
                    self.checks_warning.append((f"Sibling: {sibling}", "Not found in parent directory"))

        except Exception:
            pass  # nosec B110 - Already reported in config check

    def xǁWrknvDoctorǁ_check_sibling_packages__mutmut_29(self) -> None:
        """Check if configured sibling packages are accessible."""
        config_file = Path.cwd() / "wrknv.toml"
        if not config_file.exists():
            config_file = Path.cwd() / ".wrknv.toml"  # Fallback for backwards compat
        if not config_file.exists():
            return

        try:
            import tomli

            with config_file.open("rb") as f:
                config = tomli.load(f)

            siblings = config.get("siblings", {}).get("patterns", [])
            for sibling in siblings:
                # Check in parent directory
                sibling_path = None
                if sibling_path.exists():
                    self.checks_passed.append((f"Sibling: {sibling}", "Found"))
                else:
                    self.checks_warning.append((f"Sibling: {sibling}", "Not found in parent directory"))

        except Exception:
            pass  # nosec B110 - Already reported in config check

    def xǁWrknvDoctorǁ_check_sibling_packages__mutmut_30(self) -> None:
        """Check if configured sibling packages are accessible."""
        config_file = Path.cwd() / "wrknv.toml"
        if not config_file.exists():
            config_file = Path.cwd() / ".wrknv.toml"  # Fallback for backwards compat
        if not config_file.exists():
            return

        try:
            import tomli

            with config_file.open("rb") as f:
                config = tomli.load(f)

            siblings = config.get("siblings", {}).get("patterns", [])
            for sibling in siblings:
                # Check in parent directory
                sibling_path = Path.cwd().parent * sibling
                if sibling_path.exists():
                    self.checks_passed.append((f"Sibling: {sibling}", "Found"))
                else:
                    self.checks_warning.append((f"Sibling: {sibling}", "Not found in parent directory"))

        except Exception:
            pass  # nosec B110 - Already reported in config check

    def xǁWrknvDoctorǁ_check_sibling_packages__mutmut_31(self) -> None:
        """Check if configured sibling packages are accessible."""
        config_file = Path.cwd() / "wrknv.toml"
        if not config_file.exists():
            config_file = Path.cwd() / ".wrknv.toml"  # Fallback for backwards compat
        if not config_file.exists():
            return

        try:
            import tomli

            with config_file.open("rb") as f:
                config = tomli.load(f)

            siblings = config.get("siblings", {}).get("patterns", [])
            for sibling in siblings:
                # Check in parent directory
                sibling_path = Path.cwd().parent / sibling
                if sibling_path.exists():
                    self.checks_passed.append(None)
                else:
                    self.checks_warning.append((f"Sibling: {sibling}", "Not found in parent directory"))

        except Exception:
            pass  # nosec B110 - Already reported in config check

    def xǁWrknvDoctorǁ_check_sibling_packages__mutmut_32(self) -> None:
        """Check if configured sibling packages are accessible."""
        config_file = Path.cwd() / "wrknv.toml"
        if not config_file.exists():
            config_file = Path.cwd() / ".wrknv.toml"  # Fallback for backwards compat
        if not config_file.exists():
            return

        try:
            import tomli

            with config_file.open("rb") as f:
                config = tomli.load(f)

            siblings = config.get("siblings", {}).get("patterns", [])
            for sibling in siblings:
                # Check in parent directory
                sibling_path = Path.cwd().parent / sibling
                if sibling_path.exists():
                    self.checks_passed.append((f"Sibling: {sibling}", "XXFoundXX"))
                else:
                    self.checks_warning.append((f"Sibling: {sibling}", "Not found in parent directory"))

        except Exception:
            pass  # nosec B110 - Already reported in config check

    def xǁWrknvDoctorǁ_check_sibling_packages__mutmut_33(self) -> None:
        """Check if configured sibling packages are accessible."""
        config_file = Path.cwd() / "wrknv.toml"
        if not config_file.exists():
            config_file = Path.cwd() / ".wrknv.toml"  # Fallback for backwards compat
        if not config_file.exists():
            return

        try:
            import tomli

            with config_file.open("rb") as f:
                config = tomli.load(f)

            siblings = config.get("siblings", {}).get("patterns", [])
            for sibling in siblings:
                # Check in parent directory
                sibling_path = Path.cwd().parent / sibling
                if sibling_path.exists():
                    self.checks_passed.append((f"Sibling: {sibling}", "found"))
                else:
                    self.checks_warning.append((f"Sibling: {sibling}", "Not found in parent directory"))

        except Exception:
            pass  # nosec B110 - Already reported in config check

    def xǁWrknvDoctorǁ_check_sibling_packages__mutmut_34(self) -> None:
        """Check if configured sibling packages are accessible."""
        config_file = Path.cwd() / "wrknv.toml"
        if not config_file.exists():
            config_file = Path.cwd() / ".wrknv.toml"  # Fallback for backwards compat
        if not config_file.exists():
            return

        try:
            import tomli

            with config_file.open("rb") as f:
                config = tomli.load(f)

            siblings = config.get("siblings", {}).get("patterns", [])
            for sibling in siblings:
                # Check in parent directory
                sibling_path = Path.cwd().parent / sibling
                if sibling_path.exists():
                    self.checks_passed.append((f"Sibling: {sibling}", "FOUND"))
                else:
                    self.checks_warning.append((f"Sibling: {sibling}", "Not found in parent directory"))

        except Exception:
            pass  # nosec B110 - Already reported in config check

    def xǁWrknvDoctorǁ_check_sibling_packages__mutmut_35(self) -> None:
        """Check if configured sibling packages are accessible."""
        config_file = Path.cwd() / "wrknv.toml"
        if not config_file.exists():
            config_file = Path.cwd() / ".wrknv.toml"  # Fallback for backwards compat
        if not config_file.exists():
            return

        try:
            import tomli

            with config_file.open("rb") as f:
                config = tomli.load(f)

            siblings = config.get("siblings", {}).get("patterns", [])
            for sibling in siblings:
                # Check in parent directory
                sibling_path = Path.cwd().parent / sibling
                if sibling_path.exists():
                    self.checks_passed.append((f"Sibling: {sibling}", "Found"))
                else:
                    self.checks_warning.append(None)

        except Exception:
            pass  # nosec B110 - Already reported in config check

    def xǁWrknvDoctorǁ_check_sibling_packages__mutmut_36(self) -> None:
        """Check if configured sibling packages are accessible."""
        config_file = Path.cwd() / "wrknv.toml"
        if not config_file.exists():
            config_file = Path.cwd() / ".wrknv.toml"  # Fallback for backwards compat
        if not config_file.exists():
            return

        try:
            import tomli

            with config_file.open("rb") as f:
                config = tomli.load(f)

            siblings = config.get("siblings", {}).get("patterns", [])
            for sibling in siblings:
                # Check in parent directory
                sibling_path = Path.cwd().parent / sibling
                if sibling_path.exists():
                    self.checks_passed.append((f"Sibling: {sibling}", "Found"))
                else:
                    self.checks_warning.append((f"Sibling: {sibling}", "XXNot found in parent directoryXX"))

        except Exception:
            pass  # nosec B110 - Already reported in config check

    def xǁWrknvDoctorǁ_check_sibling_packages__mutmut_37(self) -> None:
        """Check if configured sibling packages are accessible."""
        config_file = Path.cwd() / "wrknv.toml"
        if not config_file.exists():
            config_file = Path.cwd() / ".wrknv.toml"  # Fallback for backwards compat
        if not config_file.exists():
            return

        try:
            import tomli

            with config_file.open("rb") as f:
                config = tomli.load(f)

            siblings = config.get("siblings", {}).get("patterns", [])
            for sibling in siblings:
                # Check in parent directory
                sibling_path = Path.cwd().parent / sibling
                if sibling_path.exists():
                    self.checks_passed.append((f"Sibling: {sibling}", "Found"))
                else:
                    self.checks_warning.append((f"Sibling: {sibling}", "not found in parent directory"))

        except Exception:
            pass  # nosec B110 - Already reported in config check

    def xǁWrknvDoctorǁ_check_sibling_packages__mutmut_38(self) -> None:
        """Check if configured sibling packages are accessible."""
        config_file = Path.cwd() / "wrknv.toml"
        if not config_file.exists():
            config_file = Path.cwd() / ".wrknv.toml"  # Fallback for backwards compat
        if not config_file.exists():
            return

        try:
            import tomli

            with config_file.open("rb") as f:
                config = tomli.load(f)

            siblings = config.get("siblings", {}).get("patterns", [])
            for sibling in siblings:
                # Check in parent directory
                sibling_path = Path.cwd().parent / sibling
                if sibling_path.exists():
                    self.checks_passed.append((f"Sibling: {sibling}", "Found"))
                else:
                    self.checks_warning.append((f"Sibling: {sibling}", "NOT FOUND IN PARENT DIRECTORY"))

        except Exception:
            pass  # nosec B110 - Already reported in config check
    
    xǁWrknvDoctorǁ_check_sibling_packages__mutmut_mutants : ClassVar[MutantDict] = {
    'xǁWrknvDoctorǁ_check_sibling_packages__mutmut_1': xǁWrknvDoctorǁ_check_sibling_packages__mutmut_1, 
        'xǁWrknvDoctorǁ_check_sibling_packages__mutmut_2': xǁWrknvDoctorǁ_check_sibling_packages__mutmut_2, 
        'xǁWrknvDoctorǁ_check_sibling_packages__mutmut_3': xǁWrknvDoctorǁ_check_sibling_packages__mutmut_3, 
        'xǁWrknvDoctorǁ_check_sibling_packages__mutmut_4': xǁWrknvDoctorǁ_check_sibling_packages__mutmut_4, 
        'xǁWrknvDoctorǁ_check_sibling_packages__mutmut_5': xǁWrknvDoctorǁ_check_sibling_packages__mutmut_5, 
        'xǁWrknvDoctorǁ_check_sibling_packages__mutmut_6': xǁWrknvDoctorǁ_check_sibling_packages__mutmut_6, 
        'xǁWrknvDoctorǁ_check_sibling_packages__mutmut_7': xǁWrknvDoctorǁ_check_sibling_packages__mutmut_7, 
        'xǁWrknvDoctorǁ_check_sibling_packages__mutmut_8': xǁWrknvDoctorǁ_check_sibling_packages__mutmut_8, 
        'xǁWrknvDoctorǁ_check_sibling_packages__mutmut_9': xǁWrknvDoctorǁ_check_sibling_packages__mutmut_9, 
        'xǁWrknvDoctorǁ_check_sibling_packages__mutmut_10': xǁWrknvDoctorǁ_check_sibling_packages__mutmut_10, 
        'xǁWrknvDoctorǁ_check_sibling_packages__mutmut_11': xǁWrknvDoctorǁ_check_sibling_packages__mutmut_11, 
        'xǁWrknvDoctorǁ_check_sibling_packages__mutmut_12': xǁWrknvDoctorǁ_check_sibling_packages__mutmut_12, 
        'xǁWrknvDoctorǁ_check_sibling_packages__mutmut_13': xǁWrknvDoctorǁ_check_sibling_packages__mutmut_13, 
        'xǁWrknvDoctorǁ_check_sibling_packages__mutmut_14': xǁWrknvDoctorǁ_check_sibling_packages__mutmut_14, 
        'xǁWrknvDoctorǁ_check_sibling_packages__mutmut_15': xǁWrknvDoctorǁ_check_sibling_packages__mutmut_15, 
        'xǁWrknvDoctorǁ_check_sibling_packages__mutmut_16': xǁWrknvDoctorǁ_check_sibling_packages__mutmut_16, 
        'xǁWrknvDoctorǁ_check_sibling_packages__mutmut_17': xǁWrknvDoctorǁ_check_sibling_packages__mutmut_17, 
        'xǁWrknvDoctorǁ_check_sibling_packages__mutmut_18': xǁWrknvDoctorǁ_check_sibling_packages__mutmut_18, 
        'xǁWrknvDoctorǁ_check_sibling_packages__mutmut_19': xǁWrknvDoctorǁ_check_sibling_packages__mutmut_19, 
        'xǁWrknvDoctorǁ_check_sibling_packages__mutmut_20': xǁWrknvDoctorǁ_check_sibling_packages__mutmut_20, 
        'xǁWrknvDoctorǁ_check_sibling_packages__mutmut_21': xǁWrknvDoctorǁ_check_sibling_packages__mutmut_21, 
        'xǁWrknvDoctorǁ_check_sibling_packages__mutmut_22': xǁWrknvDoctorǁ_check_sibling_packages__mutmut_22, 
        'xǁWrknvDoctorǁ_check_sibling_packages__mutmut_23': xǁWrknvDoctorǁ_check_sibling_packages__mutmut_23, 
        'xǁWrknvDoctorǁ_check_sibling_packages__mutmut_24': xǁWrknvDoctorǁ_check_sibling_packages__mutmut_24, 
        'xǁWrknvDoctorǁ_check_sibling_packages__mutmut_25': xǁWrknvDoctorǁ_check_sibling_packages__mutmut_25, 
        'xǁWrknvDoctorǁ_check_sibling_packages__mutmut_26': xǁWrknvDoctorǁ_check_sibling_packages__mutmut_26, 
        'xǁWrknvDoctorǁ_check_sibling_packages__mutmut_27': xǁWrknvDoctorǁ_check_sibling_packages__mutmut_27, 
        'xǁWrknvDoctorǁ_check_sibling_packages__mutmut_28': xǁWrknvDoctorǁ_check_sibling_packages__mutmut_28, 
        'xǁWrknvDoctorǁ_check_sibling_packages__mutmut_29': xǁWrknvDoctorǁ_check_sibling_packages__mutmut_29, 
        'xǁWrknvDoctorǁ_check_sibling_packages__mutmut_30': xǁWrknvDoctorǁ_check_sibling_packages__mutmut_30, 
        'xǁWrknvDoctorǁ_check_sibling_packages__mutmut_31': xǁWrknvDoctorǁ_check_sibling_packages__mutmut_31, 
        'xǁWrknvDoctorǁ_check_sibling_packages__mutmut_32': xǁWrknvDoctorǁ_check_sibling_packages__mutmut_32, 
        'xǁWrknvDoctorǁ_check_sibling_packages__mutmut_33': xǁWrknvDoctorǁ_check_sibling_packages__mutmut_33, 
        'xǁWrknvDoctorǁ_check_sibling_packages__mutmut_34': xǁWrknvDoctorǁ_check_sibling_packages__mutmut_34, 
        'xǁWrknvDoctorǁ_check_sibling_packages__mutmut_35': xǁWrknvDoctorǁ_check_sibling_packages__mutmut_35, 
        'xǁWrknvDoctorǁ_check_sibling_packages__mutmut_36': xǁWrknvDoctorǁ_check_sibling_packages__mutmut_36, 
        'xǁWrknvDoctorǁ_check_sibling_packages__mutmut_37': xǁWrknvDoctorǁ_check_sibling_packages__mutmut_37, 
        'xǁWrknvDoctorǁ_check_sibling_packages__mutmut_38': xǁWrknvDoctorǁ_check_sibling_packages__mutmut_38
    }
    
    def _check_sibling_packages(self, *args, **kwargs):
        result = _mutmut_trampoline(object.__getattribute__(self, "xǁWrknvDoctorǁ_check_sibling_packages__mutmut_orig"), object.__getattribute__(self, "xǁWrknvDoctorǁ_check_sibling_packages__mutmut_mutants"), args, kwargs, self)
        return result 
    
    _check_sibling_packages.__signature__ = _mutmut_signature(xǁWrknvDoctorǁ_check_sibling_packages__mutmut_orig)
    xǁWrknvDoctorǁ_check_sibling_packages__mutmut_orig.__name__ = 'xǁWrknvDoctorǁ_check_sibling_packages'

    def xǁWrknvDoctorǁ_check_common_issues__mutmut_orig(self) -> None:
        """Check for common issues that users encounter."""
        # Check if .venv exists (shouldn't use this with wrknv)
        if (Path.cwd() / ".venv").exists():
            self.checks_warning.append((".venv Directory", "Found - should use workenv/ instead with wrknv"))

        # Check for conflicting environment variables
        problematic_vars = ["VIRTUAL_ENV", "PYTHONPATH", "PYTHONHOME"]
        active_vars = []
        for var in problematic_vars:
            if os.environ.get(var):
                active_vars.append(var)

        if active_vars:
            self.checks_warning.append(
                ("Environment Variables", f"Potentially conflicting: {', '.join(active_vars)}")
            )

        # Check if we're inside another virtual environment
        if sys.prefix != sys.base_prefix:
            self.checks_warning.append(("Virtual Environment", "Already activated - may conflict with wrknv"))

    def xǁWrknvDoctorǁ_check_common_issues__mutmut_1(self) -> None:
        """Check for common issues that users encounter."""
        # Check if .venv exists (shouldn't use this with wrknv)
        if (Path.cwd() * ".venv").exists():
            self.checks_warning.append((".venv Directory", "Found - should use workenv/ instead with wrknv"))

        # Check for conflicting environment variables
        problematic_vars = ["VIRTUAL_ENV", "PYTHONPATH", "PYTHONHOME"]
        active_vars = []
        for var in problematic_vars:
            if os.environ.get(var):
                active_vars.append(var)

        if active_vars:
            self.checks_warning.append(
                ("Environment Variables", f"Potentially conflicting: {', '.join(active_vars)}")
            )

        # Check if we're inside another virtual environment
        if sys.prefix != sys.base_prefix:
            self.checks_warning.append(("Virtual Environment", "Already activated - may conflict with wrknv"))

    def xǁWrknvDoctorǁ_check_common_issues__mutmut_2(self) -> None:
        """Check for common issues that users encounter."""
        # Check if .venv exists (shouldn't use this with wrknv)
        if (Path.cwd() / "XX.venvXX").exists():
            self.checks_warning.append((".venv Directory", "Found - should use workenv/ instead with wrknv"))

        # Check for conflicting environment variables
        problematic_vars = ["VIRTUAL_ENV", "PYTHONPATH", "PYTHONHOME"]
        active_vars = []
        for var in problematic_vars:
            if os.environ.get(var):
                active_vars.append(var)

        if active_vars:
            self.checks_warning.append(
                ("Environment Variables", f"Potentially conflicting: {', '.join(active_vars)}")
            )

        # Check if we're inside another virtual environment
        if sys.prefix != sys.base_prefix:
            self.checks_warning.append(("Virtual Environment", "Already activated - may conflict with wrknv"))

    def xǁWrknvDoctorǁ_check_common_issues__mutmut_3(self) -> None:
        """Check for common issues that users encounter."""
        # Check if .venv exists (shouldn't use this with wrknv)
        if (Path.cwd() / ".VENV").exists():
            self.checks_warning.append((".venv Directory", "Found - should use workenv/ instead with wrknv"))

        # Check for conflicting environment variables
        problematic_vars = ["VIRTUAL_ENV", "PYTHONPATH", "PYTHONHOME"]
        active_vars = []
        for var in problematic_vars:
            if os.environ.get(var):
                active_vars.append(var)

        if active_vars:
            self.checks_warning.append(
                ("Environment Variables", f"Potentially conflicting: {', '.join(active_vars)}")
            )

        # Check if we're inside another virtual environment
        if sys.prefix != sys.base_prefix:
            self.checks_warning.append(("Virtual Environment", "Already activated - may conflict with wrknv"))

    def xǁWrknvDoctorǁ_check_common_issues__mutmut_4(self) -> None:
        """Check for common issues that users encounter."""
        # Check if .venv exists (shouldn't use this with wrknv)
        if (Path.cwd() / ".venv").exists():
            self.checks_warning.append(None)

        # Check for conflicting environment variables
        problematic_vars = ["VIRTUAL_ENV", "PYTHONPATH", "PYTHONHOME"]
        active_vars = []
        for var in problematic_vars:
            if os.environ.get(var):
                active_vars.append(var)

        if active_vars:
            self.checks_warning.append(
                ("Environment Variables", f"Potentially conflicting: {', '.join(active_vars)}")
            )

        # Check if we're inside another virtual environment
        if sys.prefix != sys.base_prefix:
            self.checks_warning.append(("Virtual Environment", "Already activated - may conflict with wrknv"))

    def xǁWrknvDoctorǁ_check_common_issues__mutmut_5(self) -> None:
        """Check for common issues that users encounter."""
        # Check if .venv exists (shouldn't use this with wrknv)
        if (Path.cwd() / ".venv").exists():
            self.checks_warning.append(("XX.venv DirectoryXX", "Found - should use workenv/ instead with wrknv"))

        # Check for conflicting environment variables
        problematic_vars = ["VIRTUAL_ENV", "PYTHONPATH", "PYTHONHOME"]
        active_vars = []
        for var in problematic_vars:
            if os.environ.get(var):
                active_vars.append(var)

        if active_vars:
            self.checks_warning.append(
                ("Environment Variables", f"Potentially conflicting: {', '.join(active_vars)}")
            )

        # Check if we're inside another virtual environment
        if sys.prefix != sys.base_prefix:
            self.checks_warning.append(("Virtual Environment", "Already activated - may conflict with wrknv"))

    def xǁWrknvDoctorǁ_check_common_issues__mutmut_6(self) -> None:
        """Check for common issues that users encounter."""
        # Check if .venv exists (shouldn't use this with wrknv)
        if (Path.cwd() / ".venv").exists():
            self.checks_warning.append((".venv directory", "Found - should use workenv/ instead with wrknv"))

        # Check for conflicting environment variables
        problematic_vars = ["VIRTUAL_ENV", "PYTHONPATH", "PYTHONHOME"]
        active_vars = []
        for var in problematic_vars:
            if os.environ.get(var):
                active_vars.append(var)

        if active_vars:
            self.checks_warning.append(
                ("Environment Variables", f"Potentially conflicting: {', '.join(active_vars)}")
            )

        # Check if we're inside another virtual environment
        if sys.prefix != sys.base_prefix:
            self.checks_warning.append(("Virtual Environment", "Already activated - may conflict with wrknv"))

    def xǁWrknvDoctorǁ_check_common_issues__mutmut_7(self) -> None:
        """Check for common issues that users encounter."""
        # Check if .venv exists (shouldn't use this with wrknv)
        if (Path.cwd() / ".venv").exists():
            self.checks_warning.append((".VENV DIRECTORY", "Found - should use workenv/ instead with wrknv"))

        # Check for conflicting environment variables
        problematic_vars = ["VIRTUAL_ENV", "PYTHONPATH", "PYTHONHOME"]
        active_vars = []
        for var in problematic_vars:
            if os.environ.get(var):
                active_vars.append(var)

        if active_vars:
            self.checks_warning.append(
                ("Environment Variables", f"Potentially conflicting: {', '.join(active_vars)}")
            )

        # Check if we're inside another virtual environment
        if sys.prefix != sys.base_prefix:
            self.checks_warning.append(("Virtual Environment", "Already activated - may conflict with wrknv"))

    def xǁWrknvDoctorǁ_check_common_issues__mutmut_8(self) -> None:
        """Check for common issues that users encounter."""
        # Check if .venv exists (shouldn't use this with wrknv)
        if (Path.cwd() / ".venv").exists():
            self.checks_warning.append((".venv Directory", "XXFound - should use workenv/ instead with wrknvXX"))

        # Check for conflicting environment variables
        problematic_vars = ["VIRTUAL_ENV", "PYTHONPATH", "PYTHONHOME"]
        active_vars = []
        for var in problematic_vars:
            if os.environ.get(var):
                active_vars.append(var)

        if active_vars:
            self.checks_warning.append(
                ("Environment Variables", f"Potentially conflicting: {', '.join(active_vars)}")
            )

        # Check if we're inside another virtual environment
        if sys.prefix != sys.base_prefix:
            self.checks_warning.append(("Virtual Environment", "Already activated - may conflict with wrknv"))

    def xǁWrknvDoctorǁ_check_common_issues__mutmut_9(self) -> None:
        """Check for common issues that users encounter."""
        # Check if .venv exists (shouldn't use this with wrknv)
        if (Path.cwd() / ".venv").exists():
            self.checks_warning.append((".venv Directory", "found - should use workenv/ instead with wrknv"))

        # Check for conflicting environment variables
        problematic_vars = ["VIRTUAL_ENV", "PYTHONPATH", "PYTHONHOME"]
        active_vars = []
        for var in problematic_vars:
            if os.environ.get(var):
                active_vars.append(var)

        if active_vars:
            self.checks_warning.append(
                ("Environment Variables", f"Potentially conflicting: {', '.join(active_vars)}")
            )

        # Check if we're inside another virtual environment
        if sys.prefix != sys.base_prefix:
            self.checks_warning.append(("Virtual Environment", "Already activated - may conflict with wrknv"))

    def xǁWrknvDoctorǁ_check_common_issues__mutmut_10(self) -> None:
        """Check for common issues that users encounter."""
        # Check if .venv exists (shouldn't use this with wrknv)
        if (Path.cwd() / ".venv").exists():
            self.checks_warning.append((".venv Directory", "FOUND - SHOULD USE WORKENV/ INSTEAD WITH WRKNV"))

        # Check for conflicting environment variables
        problematic_vars = ["VIRTUAL_ENV", "PYTHONPATH", "PYTHONHOME"]
        active_vars = []
        for var in problematic_vars:
            if os.environ.get(var):
                active_vars.append(var)

        if active_vars:
            self.checks_warning.append(
                ("Environment Variables", f"Potentially conflicting: {', '.join(active_vars)}")
            )

        # Check if we're inside another virtual environment
        if sys.prefix != sys.base_prefix:
            self.checks_warning.append(("Virtual Environment", "Already activated - may conflict with wrknv"))

    def xǁWrknvDoctorǁ_check_common_issues__mutmut_11(self) -> None:
        """Check for common issues that users encounter."""
        # Check if .venv exists (shouldn't use this with wrknv)
        if (Path.cwd() / ".venv").exists():
            self.checks_warning.append((".venv Directory", "Found - should use workenv/ instead with wrknv"))

        # Check for conflicting environment variables
        problematic_vars = None
        active_vars = []
        for var in problematic_vars:
            if os.environ.get(var):
                active_vars.append(var)

        if active_vars:
            self.checks_warning.append(
                ("Environment Variables", f"Potentially conflicting: {', '.join(active_vars)}")
            )

        # Check if we're inside another virtual environment
        if sys.prefix != sys.base_prefix:
            self.checks_warning.append(("Virtual Environment", "Already activated - may conflict with wrknv"))

    def xǁWrknvDoctorǁ_check_common_issues__mutmut_12(self) -> None:
        """Check for common issues that users encounter."""
        # Check if .venv exists (shouldn't use this with wrknv)
        if (Path.cwd() / ".venv").exists():
            self.checks_warning.append((".venv Directory", "Found - should use workenv/ instead with wrknv"))

        # Check for conflicting environment variables
        problematic_vars = ["XXVIRTUAL_ENVXX", "PYTHONPATH", "PYTHONHOME"]
        active_vars = []
        for var in problematic_vars:
            if os.environ.get(var):
                active_vars.append(var)

        if active_vars:
            self.checks_warning.append(
                ("Environment Variables", f"Potentially conflicting: {', '.join(active_vars)}")
            )

        # Check if we're inside another virtual environment
        if sys.prefix != sys.base_prefix:
            self.checks_warning.append(("Virtual Environment", "Already activated - may conflict with wrknv"))

    def xǁWrknvDoctorǁ_check_common_issues__mutmut_13(self) -> None:
        """Check for common issues that users encounter."""
        # Check if .venv exists (shouldn't use this with wrknv)
        if (Path.cwd() / ".venv").exists():
            self.checks_warning.append((".venv Directory", "Found - should use workenv/ instead with wrknv"))

        # Check for conflicting environment variables
        problematic_vars = ["virtual_env", "PYTHONPATH", "PYTHONHOME"]
        active_vars = []
        for var in problematic_vars:
            if os.environ.get(var):
                active_vars.append(var)

        if active_vars:
            self.checks_warning.append(
                ("Environment Variables", f"Potentially conflicting: {', '.join(active_vars)}")
            )

        # Check if we're inside another virtual environment
        if sys.prefix != sys.base_prefix:
            self.checks_warning.append(("Virtual Environment", "Already activated - may conflict with wrknv"))

    def xǁWrknvDoctorǁ_check_common_issues__mutmut_14(self) -> None:
        """Check for common issues that users encounter."""
        # Check if .venv exists (shouldn't use this with wrknv)
        if (Path.cwd() / ".venv").exists():
            self.checks_warning.append((".venv Directory", "Found - should use workenv/ instead with wrknv"))

        # Check for conflicting environment variables
        problematic_vars = ["VIRTUAL_ENV", "XXPYTHONPATHXX", "PYTHONHOME"]
        active_vars = []
        for var in problematic_vars:
            if os.environ.get(var):
                active_vars.append(var)

        if active_vars:
            self.checks_warning.append(
                ("Environment Variables", f"Potentially conflicting: {', '.join(active_vars)}")
            )

        # Check if we're inside another virtual environment
        if sys.prefix != sys.base_prefix:
            self.checks_warning.append(("Virtual Environment", "Already activated - may conflict with wrknv"))

    def xǁWrknvDoctorǁ_check_common_issues__mutmut_15(self) -> None:
        """Check for common issues that users encounter."""
        # Check if .venv exists (shouldn't use this with wrknv)
        if (Path.cwd() / ".venv").exists():
            self.checks_warning.append((".venv Directory", "Found - should use workenv/ instead with wrknv"))

        # Check for conflicting environment variables
        problematic_vars = ["VIRTUAL_ENV", "pythonpath", "PYTHONHOME"]
        active_vars = []
        for var in problematic_vars:
            if os.environ.get(var):
                active_vars.append(var)

        if active_vars:
            self.checks_warning.append(
                ("Environment Variables", f"Potentially conflicting: {', '.join(active_vars)}")
            )

        # Check if we're inside another virtual environment
        if sys.prefix != sys.base_prefix:
            self.checks_warning.append(("Virtual Environment", "Already activated - may conflict with wrknv"))

    def xǁWrknvDoctorǁ_check_common_issues__mutmut_16(self) -> None:
        """Check for common issues that users encounter."""
        # Check if .venv exists (shouldn't use this with wrknv)
        if (Path.cwd() / ".venv").exists():
            self.checks_warning.append((".venv Directory", "Found - should use workenv/ instead with wrknv"))

        # Check for conflicting environment variables
        problematic_vars = ["VIRTUAL_ENV", "PYTHONPATH", "XXPYTHONHOMEXX"]
        active_vars = []
        for var in problematic_vars:
            if os.environ.get(var):
                active_vars.append(var)

        if active_vars:
            self.checks_warning.append(
                ("Environment Variables", f"Potentially conflicting: {', '.join(active_vars)}")
            )

        # Check if we're inside another virtual environment
        if sys.prefix != sys.base_prefix:
            self.checks_warning.append(("Virtual Environment", "Already activated - may conflict with wrknv"))

    def xǁWrknvDoctorǁ_check_common_issues__mutmut_17(self) -> None:
        """Check for common issues that users encounter."""
        # Check if .venv exists (shouldn't use this with wrknv)
        if (Path.cwd() / ".venv").exists():
            self.checks_warning.append((".venv Directory", "Found - should use workenv/ instead with wrknv"))

        # Check for conflicting environment variables
        problematic_vars = ["VIRTUAL_ENV", "PYTHONPATH", "pythonhome"]
        active_vars = []
        for var in problematic_vars:
            if os.environ.get(var):
                active_vars.append(var)

        if active_vars:
            self.checks_warning.append(
                ("Environment Variables", f"Potentially conflicting: {', '.join(active_vars)}")
            )

        # Check if we're inside another virtual environment
        if sys.prefix != sys.base_prefix:
            self.checks_warning.append(("Virtual Environment", "Already activated - may conflict with wrknv"))

    def xǁWrknvDoctorǁ_check_common_issues__mutmut_18(self) -> None:
        """Check for common issues that users encounter."""
        # Check if .venv exists (shouldn't use this with wrknv)
        if (Path.cwd() / ".venv").exists():
            self.checks_warning.append((".venv Directory", "Found - should use workenv/ instead with wrknv"))

        # Check for conflicting environment variables
        problematic_vars = ["VIRTUAL_ENV", "PYTHONPATH", "PYTHONHOME"]
        active_vars = None
        for var in problematic_vars:
            if os.environ.get(var):
                active_vars.append(var)

        if active_vars:
            self.checks_warning.append(
                ("Environment Variables", f"Potentially conflicting: {', '.join(active_vars)}")
            )

        # Check if we're inside another virtual environment
        if sys.prefix != sys.base_prefix:
            self.checks_warning.append(("Virtual Environment", "Already activated - may conflict with wrknv"))

    def xǁWrknvDoctorǁ_check_common_issues__mutmut_19(self) -> None:
        """Check for common issues that users encounter."""
        # Check if .venv exists (shouldn't use this with wrknv)
        if (Path.cwd() / ".venv").exists():
            self.checks_warning.append((".venv Directory", "Found - should use workenv/ instead with wrknv"))

        # Check for conflicting environment variables
        problematic_vars = ["VIRTUAL_ENV", "PYTHONPATH", "PYTHONHOME"]
        active_vars = []
        for var in problematic_vars:
            if os.environ.get(None):
                active_vars.append(var)

        if active_vars:
            self.checks_warning.append(
                ("Environment Variables", f"Potentially conflicting: {', '.join(active_vars)}")
            )

        # Check if we're inside another virtual environment
        if sys.prefix != sys.base_prefix:
            self.checks_warning.append(("Virtual Environment", "Already activated - may conflict with wrknv"))

    def xǁWrknvDoctorǁ_check_common_issues__mutmut_20(self) -> None:
        """Check for common issues that users encounter."""
        # Check if .venv exists (shouldn't use this with wrknv)
        if (Path.cwd() / ".venv").exists():
            self.checks_warning.append((".venv Directory", "Found - should use workenv/ instead with wrknv"))

        # Check for conflicting environment variables
        problematic_vars = ["VIRTUAL_ENV", "PYTHONPATH", "PYTHONHOME"]
        active_vars = []
        for var in problematic_vars:
            if os.environ.get(var):
                active_vars.append(None)

        if active_vars:
            self.checks_warning.append(
                ("Environment Variables", f"Potentially conflicting: {', '.join(active_vars)}")
            )

        # Check if we're inside another virtual environment
        if sys.prefix != sys.base_prefix:
            self.checks_warning.append(("Virtual Environment", "Already activated - may conflict with wrknv"))

    def xǁWrknvDoctorǁ_check_common_issues__mutmut_21(self) -> None:
        """Check for common issues that users encounter."""
        # Check if .venv exists (shouldn't use this with wrknv)
        if (Path.cwd() / ".venv").exists():
            self.checks_warning.append((".venv Directory", "Found - should use workenv/ instead with wrknv"))

        # Check for conflicting environment variables
        problematic_vars = ["VIRTUAL_ENV", "PYTHONPATH", "PYTHONHOME"]
        active_vars = []
        for var in problematic_vars:
            if os.environ.get(var):
                active_vars.append(var)

        if active_vars:
            self.checks_warning.append(
                None
            )

        # Check if we're inside another virtual environment
        if sys.prefix != sys.base_prefix:
            self.checks_warning.append(("Virtual Environment", "Already activated - may conflict with wrknv"))

    def xǁWrknvDoctorǁ_check_common_issues__mutmut_22(self) -> None:
        """Check for common issues that users encounter."""
        # Check if .venv exists (shouldn't use this with wrknv)
        if (Path.cwd() / ".venv").exists():
            self.checks_warning.append((".venv Directory", "Found - should use workenv/ instead with wrknv"))

        # Check for conflicting environment variables
        problematic_vars = ["VIRTUAL_ENV", "PYTHONPATH", "PYTHONHOME"]
        active_vars = []
        for var in problematic_vars:
            if os.environ.get(var):
                active_vars.append(var)

        if active_vars:
            self.checks_warning.append(
                ("XXEnvironment VariablesXX", f"Potentially conflicting: {', '.join(active_vars)}")
            )

        # Check if we're inside another virtual environment
        if sys.prefix != sys.base_prefix:
            self.checks_warning.append(("Virtual Environment", "Already activated - may conflict with wrknv"))

    def xǁWrknvDoctorǁ_check_common_issues__mutmut_23(self) -> None:
        """Check for common issues that users encounter."""
        # Check if .venv exists (shouldn't use this with wrknv)
        if (Path.cwd() / ".venv").exists():
            self.checks_warning.append((".venv Directory", "Found - should use workenv/ instead with wrknv"))

        # Check for conflicting environment variables
        problematic_vars = ["VIRTUAL_ENV", "PYTHONPATH", "PYTHONHOME"]
        active_vars = []
        for var in problematic_vars:
            if os.environ.get(var):
                active_vars.append(var)

        if active_vars:
            self.checks_warning.append(
                ("environment variables", f"Potentially conflicting: {', '.join(active_vars)}")
            )

        # Check if we're inside another virtual environment
        if sys.prefix != sys.base_prefix:
            self.checks_warning.append(("Virtual Environment", "Already activated - may conflict with wrknv"))

    def xǁWrknvDoctorǁ_check_common_issues__mutmut_24(self) -> None:
        """Check for common issues that users encounter."""
        # Check if .venv exists (shouldn't use this with wrknv)
        if (Path.cwd() / ".venv").exists():
            self.checks_warning.append((".venv Directory", "Found - should use workenv/ instead with wrknv"))

        # Check for conflicting environment variables
        problematic_vars = ["VIRTUAL_ENV", "PYTHONPATH", "PYTHONHOME"]
        active_vars = []
        for var in problematic_vars:
            if os.environ.get(var):
                active_vars.append(var)

        if active_vars:
            self.checks_warning.append(
                ("ENVIRONMENT VARIABLES", f"Potentially conflicting: {', '.join(active_vars)}")
            )

        # Check if we're inside another virtual environment
        if sys.prefix != sys.base_prefix:
            self.checks_warning.append(("Virtual Environment", "Already activated - may conflict with wrknv"))

    def xǁWrknvDoctorǁ_check_common_issues__mutmut_25(self) -> None:
        """Check for common issues that users encounter."""
        # Check if .venv exists (shouldn't use this with wrknv)
        if (Path.cwd() / ".venv").exists():
            self.checks_warning.append((".venv Directory", "Found - should use workenv/ instead with wrknv"))

        # Check for conflicting environment variables
        problematic_vars = ["VIRTUAL_ENV", "PYTHONPATH", "PYTHONHOME"]
        active_vars = []
        for var in problematic_vars:
            if os.environ.get(var):
                active_vars.append(var)

        if active_vars:
            self.checks_warning.append(
                ("Environment Variables", f"Potentially conflicting: {', '.join(None)}")
            )

        # Check if we're inside another virtual environment
        if sys.prefix != sys.base_prefix:
            self.checks_warning.append(("Virtual Environment", "Already activated - may conflict with wrknv"))

    def xǁWrknvDoctorǁ_check_common_issues__mutmut_26(self) -> None:
        """Check for common issues that users encounter."""
        # Check if .venv exists (shouldn't use this with wrknv)
        if (Path.cwd() / ".venv").exists():
            self.checks_warning.append((".venv Directory", "Found - should use workenv/ instead with wrknv"))

        # Check for conflicting environment variables
        problematic_vars = ["VIRTUAL_ENV", "PYTHONPATH", "PYTHONHOME"]
        active_vars = []
        for var in problematic_vars:
            if os.environ.get(var):
                active_vars.append(var)

        if active_vars:
            self.checks_warning.append(
                ("Environment Variables", f"Potentially conflicting: {'XX, XX'.join(active_vars)}")
            )

        # Check if we're inside another virtual environment
        if sys.prefix != sys.base_prefix:
            self.checks_warning.append(("Virtual Environment", "Already activated - may conflict with wrknv"))

    def xǁWrknvDoctorǁ_check_common_issues__mutmut_27(self) -> None:
        """Check for common issues that users encounter."""
        # Check if .venv exists (shouldn't use this with wrknv)
        if (Path.cwd() / ".venv").exists():
            self.checks_warning.append((".venv Directory", "Found - should use workenv/ instead with wrknv"))

        # Check for conflicting environment variables
        problematic_vars = ["VIRTUAL_ENV", "PYTHONPATH", "PYTHONHOME"]
        active_vars = []
        for var in problematic_vars:
            if os.environ.get(var):
                active_vars.append(var)

        if active_vars:
            self.checks_warning.append(
                ("Environment Variables", f"Potentially conflicting: {', '.join(active_vars)}")
            )

        # Check if we're inside another virtual environment
        if sys.prefix == sys.base_prefix:
            self.checks_warning.append(("Virtual Environment", "Already activated - may conflict with wrknv"))

    def xǁWrknvDoctorǁ_check_common_issues__mutmut_28(self) -> None:
        """Check for common issues that users encounter."""
        # Check if .venv exists (shouldn't use this with wrknv)
        if (Path.cwd() / ".venv").exists():
            self.checks_warning.append((".venv Directory", "Found - should use workenv/ instead with wrknv"))

        # Check for conflicting environment variables
        problematic_vars = ["VIRTUAL_ENV", "PYTHONPATH", "PYTHONHOME"]
        active_vars = []
        for var in problematic_vars:
            if os.environ.get(var):
                active_vars.append(var)

        if active_vars:
            self.checks_warning.append(
                ("Environment Variables", f"Potentially conflicting: {', '.join(active_vars)}")
            )

        # Check if we're inside another virtual environment
        if sys.prefix != sys.base_prefix:
            self.checks_warning.append(None)

    def xǁWrknvDoctorǁ_check_common_issues__mutmut_29(self) -> None:
        """Check for common issues that users encounter."""
        # Check if .venv exists (shouldn't use this with wrknv)
        if (Path.cwd() / ".venv").exists():
            self.checks_warning.append((".venv Directory", "Found - should use workenv/ instead with wrknv"))

        # Check for conflicting environment variables
        problematic_vars = ["VIRTUAL_ENV", "PYTHONPATH", "PYTHONHOME"]
        active_vars = []
        for var in problematic_vars:
            if os.environ.get(var):
                active_vars.append(var)

        if active_vars:
            self.checks_warning.append(
                ("Environment Variables", f"Potentially conflicting: {', '.join(active_vars)}")
            )

        # Check if we're inside another virtual environment
        if sys.prefix != sys.base_prefix:
            self.checks_warning.append(("XXVirtual EnvironmentXX", "Already activated - may conflict with wrknv"))

    def xǁWrknvDoctorǁ_check_common_issues__mutmut_30(self) -> None:
        """Check for common issues that users encounter."""
        # Check if .venv exists (shouldn't use this with wrknv)
        if (Path.cwd() / ".venv").exists():
            self.checks_warning.append((".venv Directory", "Found - should use workenv/ instead with wrknv"))

        # Check for conflicting environment variables
        problematic_vars = ["VIRTUAL_ENV", "PYTHONPATH", "PYTHONHOME"]
        active_vars = []
        for var in problematic_vars:
            if os.environ.get(var):
                active_vars.append(var)

        if active_vars:
            self.checks_warning.append(
                ("Environment Variables", f"Potentially conflicting: {', '.join(active_vars)}")
            )

        # Check if we're inside another virtual environment
        if sys.prefix != sys.base_prefix:
            self.checks_warning.append(("virtual environment", "Already activated - may conflict with wrknv"))

    def xǁWrknvDoctorǁ_check_common_issues__mutmut_31(self) -> None:
        """Check for common issues that users encounter."""
        # Check if .venv exists (shouldn't use this with wrknv)
        if (Path.cwd() / ".venv").exists():
            self.checks_warning.append((".venv Directory", "Found - should use workenv/ instead with wrknv"))

        # Check for conflicting environment variables
        problematic_vars = ["VIRTUAL_ENV", "PYTHONPATH", "PYTHONHOME"]
        active_vars = []
        for var in problematic_vars:
            if os.environ.get(var):
                active_vars.append(var)

        if active_vars:
            self.checks_warning.append(
                ("Environment Variables", f"Potentially conflicting: {', '.join(active_vars)}")
            )

        # Check if we're inside another virtual environment
        if sys.prefix != sys.base_prefix:
            self.checks_warning.append(("VIRTUAL ENVIRONMENT", "Already activated - may conflict with wrknv"))

    def xǁWrknvDoctorǁ_check_common_issues__mutmut_32(self) -> None:
        """Check for common issues that users encounter."""
        # Check if .venv exists (shouldn't use this with wrknv)
        if (Path.cwd() / ".venv").exists():
            self.checks_warning.append((".venv Directory", "Found - should use workenv/ instead with wrknv"))

        # Check for conflicting environment variables
        problematic_vars = ["VIRTUAL_ENV", "PYTHONPATH", "PYTHONHOME"]
        active_vars = []
        for var in problematic_vars:
            if os.environ.get(var):
                active_vars.append(var)

        if active_vars:
            self.checks_warning.append(
                ("Environment Variables", f"Potentially conflicting: {', '.join(active_vars)}")
            )

        # Check if we're inside another virtual environment
        if sys.prefix != sys.base_prefix:
            self.checks_warning.append(("Virtual Environment", "XXAlready activated - may conflict with wrknvXX"))

    def xǁWrknvDoctorǁ_check_common_issues__mutmut_33(self) -> None:
        """Check for common issues that users encounter."""
        # Check if .venv exists (shouldn't use this with wrknv)
        if (Path.cwd() / ".venv").exists():
            self.checks_warning.append((".venv Directory", "Found - should use workenv/ instead with wrknv"))

        # Check for conflicting environment variables
        problematic_vars = ["VIRTUAL_ENV", "PYTHONPATH", "PYTHONHOME"]
        active_vars = []
        for var in problematic_vars:
            if os.environ.get(var):
                active_vars.append(var)

        if active_vars:
            self.checks_warning.append(
                ("Environment Variables", f"Potentially conflicting: {', '.join(active_vars)}")
            )

        # Check if we're inside another virtual environment
        if sys.prefix != sys.base_prefix:
            self.checks_warning.append(("Virtual Environment", "already activated - may conflict with wrknv"))

    def xǁWrknvDoctorǁ_check_common_issues__mutmut_34(self) -> None:
        """Check for common issues that users encounter."""
        # Check if .venv exists (shouldn't use this with wrknv)
        if (Path.cwd() / ".venv").exists():
            self.checks_warning.append((".venv Directory", "Found - should use workenv/ instead with wrknv"))

        # Check for conflicting environment variables
        problematic_vars = ["VIRTUAL_ENV", "PYTHONPATH", "PYTHONHOME"]
        active_vars = []
        for var in problematic_vars:
            if os.environ.get(var):
                active_vars.append(var)

        if active_vars:
            self.checks_warning.append(
                ("Environment Variables", f"Potentially conflicting: {', '.join(active_vars)}")
            )

        # Check if we're inside another virtual environment
        if sys.prefix != sys.base_prefix:
            self.checks_warning.append(("Virtual Environment", "ALREADY ACTIVATED - MAY CONFLICT WITH WRKNV"))
    
    xǁWrknvDoctorǁ_check_common_issues__mutmut_mutants : ClassVar[MutantDict] = {
    'xǁWrknvDoctorǁ_check_common_issues__mutmut_1': xǁWrknvDoctorǁ_check_common_issues__mutmut_1, 
        'xǁWrknvDoctorǁ_check_common_issues__mutmut_2': xǁWrknvDoctorǁ_check_common_issues__mutmut_2, 
        'xǁWrknvDoctorǁ_check_common_issues__mutmut_3': xǁWrknvDoctorǁ_check_common_issues__mutmut_3, 
        'xǁWrknvDoctorǁ_check_common_issues__mutmut_4': xǁWrknvDoctorǁ_check_common_issues__mutmut_4, 
        'xǁWrknvDoctorǁ_check_common_issues__mutmut_5': xǁWrknvDoctorǁ_check_common_issues__mutmut_5, 
        'xǁWrknvDoctorǁ_check_common_issues__mutmut_6': xǁWrknvDoctorǁ_check_common_issues__mutmut_6, 
        'xǁWrknvDoctorǁ_check_common_issues__mutmut_7': xǁWrknvDoctorǁ_check_common_issues__mutmut_7, 
        'xǁWrknvDoctorǁ_check_common_issues__mutmut_8': xǁWrknvDoctorǁ_check_common_issues__mutmut_8, 
        'xǁWrknvDoctorǁ_check_common_issues__mutmut_9': xǁWrknvDoctorǁ_check_common_issues__mutmut_9, 
        'xǁWrknvDoctorǁ_check_common_issues__mutmut_10': xǁWrknvDoctorǁ_check_common_issues__mutmut_10, 
        'xǁWrknvDoctorǁ_check_common_issues__mutmut_11': xǁWrknvDoctorǁ_check_common_issues__mutmut_11, 
        'xǁWrknvDoctorǁ_check_common_issues__mutmut_12': xǁWrknvDoctorǁ_check_common_issues__mutmut_12, 
        'xǁWrknvDoctorǁ_check_common_issues__mutmut_13': xǁWrknvDoctorǁ_check_common_issues__mutmut_13, 
        'xǁWrknvDoctorǁ_check_common_issues__mutmut_14': xǁWrknvDoctorǁ_check_common_issues__mutmut_14, 
        'xǁWrknvDoctorǁ_check_common_issues__mutmut_15': xǁWrknvDoctorǁ_check_common_issues__mutmut_15, 
        'xǁWrknvDoctorǁ_check_common_issues__mutmut_16': xǁWrknvDoctorǁ_check_common_issues__mutmut_16, 
        'xǁWrknvDoctorǁ_check_common_issues__mutmut_17': xǁWrknvDoctorǁ_check_common_issues__mutmut_17, 
        'xǁWrknvDoctorǁ_check_common_issues__mutmut_18': xǁWrknvDoctorǁ_check_common_issues__mutmut_18, 
        'xǁWrknvDoctorǁ_check_common_issues__mutmut_19': xǁWrknvDoctorǁ_check_common_issues__mutmut_19, 
        'xǁWrknvDoctorǁ_check_common_issues__mutmut_20': xǁWrknvDoctorǁ_check_common_issues__mutmut_20, 
        'xǁWrknvDoctorǁ_check_common_issues__mutmut_21': xǁWrknvDoctorǁ_check_common_issues__mutmut_21, 
        'xǁWrknvDoctorǁ_check_common_issues__mutmut_22': xǁWrknvDoctorǁ_check_common_issues__mutmut_22, 
        'xǁWrknvDoctorǁ_check_common_issues__mutmut_23': xǁWrknvDoctorǁ_check_common_issues__mutmut_23, 
        'xǁWrknvDoctorǁ_check_common_issues__mutmut_24': xǁWrknvDoctorǁ_check_common_issues__mutmut_24, 
        'xǁWrknvDoctorǁ_check_common_issues__mutmut_25': xǁWrknvDoctorǁ_check_common_issues__mutmut_25, 
        'xǁWrknvDoctorǁ_check_common_issues__mutmut_26': xǁWrknvDoctorǁ_check_common_issues__mutmut_26, 
        'xǁWrknvDoctorǁ_check_common_issues__mutmut_27': xǁWrknvDoctorǁ_check_common_issues__mutmut_27, 
        'xǁWrknvDoctorǁ_check_common_issues__mutmut_28': xǁWrknvDoctorǁ_check_common_issues__mutmut_28, 
        'xǁWrknvDoctorǁ_check_common_issues__mutmut_29': xǁWrknvDoctorǁ_check_common_issues__mutmut_29, 
        'xǁWrknvDoctorǁ_check_common_issues__mutmut_30': xǁWrknvDoctorǁ_check_common_issues__mutmut_30, 
        'xǁWrknvDoctorǁ_check_common_issues__mutmut_31': xǁWrknvDoctorǁ_check_common_issues__mutmut_31, 
        'xǁWrknvDoctorǁ_check_common_issues__mutmut_32': xǁWrknvDoctorǁ_check_common_issues__mutmut_32, 
        'xǁWrknvDoctorǁ_check_common_issues__mutmut_33': xǁWrknvDoctorǁ_check_common_issues__mutmut_33, 
        'xǁWrknvDoctorǁ_check_common_issues__mutmut_34': xǁWrknvDoctorǁ_check_common_issues__mutmut_34
    }
    
    def _check_common_issues(self, *args, **kwargs):
        result = _mutmut_trampoline(object.__getattribute__(self, "xǁWrknvDoctorǁ_check_common_issues__mutmut_orig"), object.__getattribute__(self, "xǁWrknvDoctorǁ_check_common_issues__mutmut_mutants"), args, kwargs, self)
        return result 
    
    _check_common_issues.__signature__ = _mutmut_signature(xǁWrknvDoctorǁ_check_common_issues__mutmut_orig)
    xǁWrknvDoctorǁ_check_common_issues__mutmut_orig.__name__ = 'xǁWrknvDoctorǁ_check_common_issues'

    def xǁWrknvDoctorǁ_print_summary__mutmut_orig(self, verbose: bool) -> None:
        """Print summary of all checks."""
        # Create summary table
        table = Table(title="Diagnostic Results", box=box.ROUNDED)
        table.add_column("Check", style="cyan")
        table.add_column("Status", style="bold")
        table.add_column("Details")

        # Add passed checks
        for check, details in self.checks_passed:
            table.add_row(check, "[green]✅ PASS[/green]", details)

        # Add warnings
        for check, details in self.checks_warning:
            table.add_row(check, "[yellow]⚠️  WARN[/yellow]", details)

        # Add failed checks
        for check, details in self.checks_failed:
            table.add_row(check, "[red]❌ FAIL[/red]", details)

        self.console.print(table)

        # Print summary statistics
        total = len(self.checks_passed) + len(self.checks_warning) + len(self.checks_failed)

        summary = Panel(
            f"[green]Passed: {len(self.checks_passed)}[/green] | "
            f"[yellow]Warnings: {len(self.checks_warning)}[/yellow] | "
            f"[red]Failed: {len(self.checks_failed)}[/red] | "
            f"Total: {total}",
            title="Summary",
            box=box.DOUBLE,
        )
        self.console.print(summary)

        # Print recommendations if there are issues
        if self.checks_failed or self.checks_warning:
            self._print_recommendations()

    def xǁWrknvDoctorǁ_print_summary__mutmut_1(self, verbose: bool) -> None:
        """Print summary of all checks."""
        # Create summary table
        table = None
        table.add_column("Check", style="cyan")
        table.add_column("Status", style="bold")
        table.add_column("Details")

        # Add passed checks
        for check, details in self.checks_passed:
            table.add_row(check, "[green]✅ PASS[/green]", details)

        # Add warnings
        for check, details in self.checks_warning:
            table.add_row(check, "[yellow]⚠️  WARN[/yellow]", details)

        # Add failed checks
        for check, details in self.checks_failed:
            table.add_row(check, "[red]❌ FAIL[/red]", details)

        self.console.print(table)

        # Print summary statistics
        total = len(self.checks_passed) + len(self.checks_warning) + len(self.checks_failed)

        summary = Panel(
            f"[green]Passed: {len(self.checks_passed)}[/green] | "
            f"[yellow]Warnings: {len(self.checks_warning)}[/yellow] | "
            f"[red]Failed: {len(self.checks_failed)}[/red] | "
            f"Total: {total}",
            title="Summary",
            box=box.DOUBLE,
        )
        self.console.print(summary)

        # Print recommendations if there are issues
        if self.checks_failed or self.checks_warning:
            self._print_recommendations()

    def xǁWrknvDoctorǁ_print_summary__mutmut_2(self, verbose: bool) -> None:
        """Print summary of all checks."""
        # Create summary table
        table = Table(title=None, box=box.ROUNDED)
        table.add_column("Check", style="cyan")
        table.add_column("Status", style="bold")
        table.add_column("Details")

        # Add passed checks
        for check, details in self.checks_passed:
            table.add_row(check, "[green]✅ PASS[/green]", details)

        # Add warnings
        for check, details in self.checks_warning:
            table.add_row(check, "[yellow]⚠️  WARN[/yellow]", details)

        # Add failed checks
        for check, details in self.checks_failed:
            table.add_row(check, "[red]❌ FAIL[/red]", details)

        self.console.print(table)

        # Print summary statistics
        total = len(self.checks_passed) + len(self.checks_warning) + len(self.checks_failed)

        summary = Panel(
            f"[green]Passed: {len(self.checks_passed)}[/green] | "
            f"[yellow]Warnings: {len(self.checks_warning)}[/yellow] | "
            f"[red]Failed: {len(self.checks_failed)}[/red] | "
            f"Total: {total}",
            title="Summary",
            box=box.DOUBLE,
        )
        self.console.print(summary)

        # Print recommendations if there are issues
        if self.checks_failed or self.checks_warning:
            self._print_recommendations()

    def xǁWrknvDoctorǁ_print_summary__mutmut_3(self, verbose: bool) -> None:
        """Print summary of all checks."""
        # Create summary table
        table = Table(title="Diagnostic Results", box=None)
        table.add_column("Check", style="cyan")
        table.add_column("Status", style="bold")
        table.add_column("Details")

        # Add passed checks
        for check, details in self.checks_passed:
            table.add_row(check, "[green]✅ PASS[/green]", details)

        # Add warnings
        for check, details in self.checks_warning:
            table.add_row(check, "[yellow]⚠️  WARN[/yellow]", details)

        # Add failed checks
        for check, details in self.checks_failed:
            table.add_row(check, "[red]❌ FAIL[/red]", details)

        self.console.print(table)

        # Print summary statistics
        total = len(self.checks_passed) + len(self.checks_warning) + len(self.checks_failed)

        summary = Panel(
            f"[green]Passed: {len(self.checks_passed)}[/green] | "
            f"[yellow]Warnings: {len(self.checks_warning)}[/yellow] | "
            f"[red]Failed: {len(self.checks_failed)}[/red] | "
            f"Total: {total}",
            title="Summary",
            box=box.DOUBLE,
        )
        self.console.print(summary)

        # Print recommendations if there are issues
        if self.checks_failed or self.checks_warning:
            self._print_recommendations()

    def xǁWrknvDoctorǁ_print_summary__mutmut_4(self, verbose: bool) -> None:
        """Print summary of all checks."""
        # Create summary table
        table = Table(box=box.ROUNDED)
        table.add_column("Check", style="cyan")
        table.add_column("Status", style="bold")
        table.add_column("Details")

        # Add passed checks
        for check, details in self.checks_passed:
            table.add_row(check, "[green]✅ PASS[/green]", details)

        # Add warnings
        for check, details in self.checks_warning:
            table.add_row(check, "[yellow]⚠️  WARN[/yellow]", details)

        # Add failed checks
        for check, details in self.checks_failed:
            table.add_row(check, "[red]❌ FAIL[/red]", details)

        self.console.print(table)

        # Print summary statistics
        total = len(self.checks_passed) + len(self.checks_warning) + len(self.checks_failed)

        summary = Panel(
            f"[green]Passed: {len(self.checks_passed)}[/green] | "
            f"[yellow]Warnings: {len(self.checks_warning)}[/yellow] | "
            f"[red]Failed: {len(self.checks_failed)}[/red] | "
            f"Total: {total}",
            title="Summary",
            box=box.DOUBLE,
        )
        self.console.print(summary)

        # Print recommendations if there are issues
        if self.checks_failed or self.checks_warning:
            self._print_recommendations()

    def xǁWrknvDoctorǁ_print_summary__mutmut_5(self, verbose: bool) -> None:
        """Print summary of all checks."""
        # Create summary table
        table = Table(title="Diagnostic Results", )
        table.add_column("Check", style="cyan")
        table.add_column("Status", style="bold")
        table.add_column("Details")

        # Add passed checks
        for check, details in self.checks_passed:
            table.add_row(check, "[green]✅ PASS[/green]", details)

        # Add warnings
        for check, details in self.checks_warning:
            table.add_row(check, "[yellow]⚠️  WARN[/yellow]", details)

        # Add failed checks
        for check, details in self.checks_failed:
            table.add_row(check, "[red]❌ FAIL[/red]", details)

        self.console.print(table)

        # Print summary statistics
        total = len(self.checks_passed) + len(self.checks_warning) + len(self.checks_failed)

        summary = Panel(
            f"[green]Passed: {len(self.checks_passed)}[/green] | "
            f"[yellow]Warnings: {len(self.checks_warning)}[/yellow] | "
            f"[red]Failed: {len(self.checks_failed)}[/red] | "
            f"Total: {total}",
            title="Summary",
            box=box.DOUBLE,
        )
        self.console.print(summary)

        # Print recommendations if there are issues
        if self.checks_failed or self.checks_warning:
            self._print_recommendations()

    def xǁWrknvDoctorǁ_print_summary__mutmut_6(self, verbose: bool) -> None:
        """Print summary of all checks."""
        # Create summary table
        table = Table(title="XXDiagnostic ResultsXX", box=box.ROUNDED)
        table.add_column("Check", style="cyan")
        table.add_column("Status", style="bold")
        table.add_column("Details")

        # Add passed checks
        for check, details in self.checks_passed:
            table.add_row(check, "[green]✅ PASS[/green]", details)

        # Add warnings
        for check, details in self.checks_warning:
            table.add_row(check, "[yellow]⚠️  WARN[/yellow]", details)

        # Add failed checks
        for check, details in self.checks_failed:
            table.add_row(check, "[red]❌ FAIL[/red]", details)

        self.console.print(table)

        # Print summary statistics
        total = len(self.checks_passed) + len(self.checks_warning) + len(self.checks_failed)

        summary = Panel(
            f"[green]Passed: {len(self.checks_passed)}[/green] | "
            f"[yellow]Warnings: {len(self.checks_warning)}[/yellow] | "
            f"[red]Failed: {len(self.checks_failed)}[/red] | "
            f"Total: {total}",
            title="Summary",
            box=box.DOUBLE,
        )
        self.console.print(summary)

        # Print recommendations if there are issues
        if self.checks_failed or self.checks_warning:
            self._print_recommendations()

    def xǁWrknvDoctorǁ_print_summary__mutmut_7(self, verbose: bool) -> None:
        """Print summary of all checks."""
        # Create summary table
        table = Table(title="diagnostic results", box=box.ROUNDED)
        table.add_column("Check", style="cyan")
        table.add_column("Status", style="bold")
        table.add_column("Details")

        # Add passed checks
        for check, details in self.checks_passed:
            table.add_row(check, "[green]✅ PASS[/green]", details)

        # Add warnings
        for check, details in self.checks_warning:
            table.add_row(check, "[yellow]⚠️  WARN[/yellow]", details)

        # Add failed checks
        for check, details in self.checks_failed:
            table.add_row(check, "[red]❌ FAIL[/red]", details)

        self.console.print(table)

        # Print summary statistics
        total = len(self.checks_passed) + len(self.checks_warning) + len(self.checks_failed)

        summary = Panel(
            f"[green]Passed: {len(self.checks_passed)}[/green] | "
            f"[yellow]Warnings: {len(self.checks_warning)}[/yellow] | "
            f"[red]Failed: {len(self.checks_failed)}[/red] | "
            f"Total: {total}",
            title="Summary",
            box=box.DOUBLE,
        )
        self.console.print(summary)

        # Print recommendations if there are issues
        if self.checks_failed or self.checks_warning:
            self._print_recommendations()

    def xǁWrknvDoctorǁ_print_summary__mutmut_8(self, verbose: bool) -> None:
        """Print summary of all checks."""
        # Create summary table
        table = Table(title="DIAGNOSTIC RESULTS", box=box.ROUNDED)
        table.add_column("Check", style="cyan")
        table.add_column("Status", style="bold")
        table.add_column("Details")

        # Add passed checks
        for check, details in self.checks_passed:
            table.add_row(check, "[green]✅ PASS[/green]", details)

        # Add warnings
        for check, details in self.checks_warning:
            table.add_row(check, "[yellow]⚠️  WARN[/yellow]", details)

        # Add failed checks
        for check, details in self.checks_failed:
            table.add_row(check, "[red]❌ FAIL[/red]", details)

        self.console.print(table)

        # Print summary statistics
        total = len(self.checks_passed) + len(self.checks_warning) + len(self.checks_failed)

        summary = Panel(
            f"[green]Passed: {len(self.checks_passed)}[/green] | "
            f"[yellow]Warnings: {len(self.checks_warning)}[/yellow] | "
            f"[red]Failed: {len(self.checks_failed)}[/red] | "
            f"Total: {total}",
            title="Summary",
            box=box.DOUBLE,
        )
        self.console.print(summary)

        # Print recommendations if there are issues
        if self.checks_failed or self.checks_warning:
            self._print_recommendations()

    def xǁWrknvDoctorǁ_print_summary__mutmut_9(self, verbose: bool) -> None:
        """Print summary of all checks."""
        # Create summary table
        table = Table(title="Diagnostic Results", box=box.ROUNDED)
        table.add_column(None, style="cyan")
        table.add_column("Status", style="bold")
        table.add_column("Details")

        # Add passed checks
        for check, details in self.checks_passed:
            table.add_row(check, "[green]✅ PASS[/green]", details)

        # Add warnings
        for check, details in self.checks_warning:
            table.add_row(check, "[yellow]⚠️  WARN[/yellow]", details)

        # Add failed checks
        for check, details in self.checks_failed:
            table.add_row(check, "[red]❌ FAIL[/red]", details)

        self.console.print(table)

        # Print summary statistics
        total = len(self.checks_passed) + len(self.checks_warning) + len(self.checks_failed)

        summary = Panel(
            f"[green]Passed: {len(self.checks_passed)}[/green] | "
            f"[yellow]Warnings: {len(self.checks_warning)}[/yellow] | "
            f"[red]Failed: {len(self.checks_failed)}[/red] | "
            f"Total: {total}",
            title="Summary",
            box=box.DOUBLE,
        )
        self.console.print(summary)

        # Print recommendations if there are issues
        if self.checks_failed or self.checks_warning:
            self._print_recommendations()

    def xǁWrknvDoctorǁ_print_summary__mutmut_10(self, verbose: bool) -> None:
        """Print summary of all checks."""
        # Create summary table
        table = Table(title="Diagnostic Results", box=box.ROUNDED)
        table.add_column("Check", style=None)
        table.add_column("Status", style="bold")
        table.add_column("Details")

        # Add passed checks
        for check, details in self.checks_passed:
            table.add_row(check, "[green]✅ PASS[/green]", details)

        # Add warnings
        for check, details in self.checks_warning:
            table.add_row(check, "[yellow]⚠️  WARN[/yellow]", details)

        # Add failed checks
        for check, details in self.checks_failed:
            table.add_row(check, "[red]❌ FAIL[/red]", details)

        self.console.print(table)

        # Print summary statistics
        total = len(self.checks_passed) + len(self.checks_warning) + len(self.checks_failed)

        summary = Panel(
            f"[green]Passed: {len(self.checks_passed)}[/green] | "
            f"[yellow]Warnings: {len(self.checks_warning)}[/yellow] | "
            f"[red]Failed: {len(self.checks_failed)}[/red] | "
            f"Total: {total}",
            title="Summary",
            box=box.DOUBLE,
        )
        self.console.print(summary)

        # Print recommendations if there are issues
        if self.checks_failed or self.checks_warning:
            self._print_recommendations()

    def xǁWrknvDoctorǁ_print_summary__mutmut_11(self, verbose: bool) -> None:
        """Print summary of all checks."""
        # Create summary table
        table = Table(title="Diagnostic Results", box=box.ROUNDED)
        table.add_column(style="cyan")
        table.add_column("Status", style="bold")
        table.add_column("Details")

        # Add passed checks
        for check, details in self.checks_passed:
            table.add_row(check, "[green]✅ PASS[/green]", details)

        # Add warnings
        for check, details in self.checks_warning:
            table.add_row(check, "[yellow]⚠️  WARN[/yellow]", details)

        # Add failed checks
        for check, details in self.checks_failed:
            table.add_row(check, "[red]❌ FAIL[/red]", details)

        self.console.print(table)

        # Print summary statistics
        total = len(self.checks_passed) + len(self.checks_warning) + len(self.checks_failed)

        summary = Panel(
            f"[green]Passed: {len(self.checks_passed)}[/green] | "
            f"[yellow]Warnings: {len(self.checks_warning)}[/yellow] | "
            f"[red]Failed: {len(self.checks_failed)}[/red] | "
            f"Total: {total}",
            title="Summary",
            box=box.DOUBLE,
        )
        self.console.print(summary)

        # Print recommendations if there are issues
        if self.checks_failed or self.checks_warning:
            self._print_recommendations()

    def xǁWrknvDoctorǁ_print_summary__mutmut_12(self, verbose: bool) -> None:
        """Print summary of all checks."""
        # Create summary table
        table = Table(title="Diagnostic Results", box=box.ROUNDED)
        table.add_column("Check", )
        table.add_column("Status", style="bold")
        table.add_column("Details")

        # Add passed checks
        for check, details in self.checks_passed:
            table.add_row(check, "[green]✅ PASS[/green]", details)

        # Add warnings
        for check, details in self.checks_warning:
            table.add_row(check, "[yellow]⚠️  WARN[/yellow]", details)

        # Add failed checks
        for check, details in self.checks_failed:
            table.add_row(check, "[red]❌ FAIL[/red]", details)

        self.console.print(table)

        # Print summary statistics
        total = len(self.checks_passed) + len(self.checks_warning) + len(self.checks_failed)

        summary = Panel(
            f"[green]Passed: {len(self.checks_passed)}[/green] | "
            f"[yellow]Warnings: {len(self.checks_warning)}[/yellow] | "
            f"[red]Failed: {len(self.checks_failed)}[/red] | "
            f"Total: {total}",
            title="Summary",
            box=box.DOUBLE,
        )
        self.console.print(summary)

        # Print recommendations if there are issues
        if self.checks_failed or self.checks_warning:
            self._print_recommendations()

    def xǁWrknvDoctorǁ_print_summary__mutmut_13(self, verbose: bool) -> None:
        """Print summary of all checks."""
        # Create summary table
        table = Table(title="Diagnostic Results", box=box.ROUNDED)
        table.add_column("XXCheckXX", style="cyan")
        table.add_column("Status", style="bold")
        table.add_column("Details")

        # Add passed checks
        for check, details in self.checks_passed:
            table.add_row(check, "[green]✅ PASS[/green]", details)

        # Add warnings
        for check, details in self.checks_warning:
            table.add_row(check, "[yellow]⚠️  WARN[/yellow]", details)

        # Add failed checks
        for check, details in self.checks_failed:
            table.add_row(check, "[red]❌ FAIL[/red]", details)

        self.console.print(table)

        # Print summary statistics
        total = len(self.checks_passed) + len(self.checks_warning) + len(self.checks_failed)

        summary = Panel(
            f"[green]Passed: {len(self.checks_passed)}[/green] | "
            f"[yellow]Warnings: {len(self.checks_warning)}[/yellow] | "
            f"[red]Failed: {len(self.checks_failed)}[/red] | "
            f"Total: {total}",
            title="Summary",
            box=box.DOUBLE,
        )
        self.console.print(summary)

        # Print recommendations if there are issues
        if self.checks_failed or self.checks_warning:
            self._print_recommendations()

    def xǁWrknvDoctorǁ_print_summary__mutmut_14(self, verbose: bool) -> None:
        """Print summary of all checks."""
        # Create summary table
        table = Table(title="Diagnostic Results", box=box.ROUNDED)
        table.add_column("check", style="cyan")
        table.add_column("Status", style="bold")
        table.add_column("Details")

        # Add passed checks
        for check, details in self.checks_passed:
            table.add_row(check, "[green]✅ PASS[/green]", details)

        # Add warnings
        for check, details in self.checks_warning:
            table.add_row(check, "[yellow]⚠️  WARN[/yellow]", details)

        # Add failed checks
        for check, details in self.checks_failed:
            table.add_row(check, "[red]❌ FAIL[/red]", details)

        self.console.print(table)

        # Print summary statistics
        total = len(self.checks_passed) + len(self.checks_warning) + len(self.checks_failed)

        summary = Panel(
            f"[green]Passed: {len(self.checks_passed)}[/green] | "
            f"[yellow]Warnings: {len(self.checks_warning)}[/yellow] | "
            f"[red]Failed: {len(self.checks_failed)}[/red] | "
            f"Total: {total}",
            title="Summary",
            box=box.DOUBLE,
        )
        self.console.print(summary)

        # Print recommendations if there are issues
        if self.checks_failed or self.checks_warning:
            self._print_recommendations()

    def xǁWrknvDoctorǁ_print_summary__mutmut_15(self, verbose: bool) -> None:
        """Print summary of all checks."""
        # Create summary table
        table = Table(title="Diagnostic Results", box=box.ROUNDED)
        table.add_column("CHECK", style="cyan")
        table.add_column("Status", style="bold")
        table.add_column("Details")

        # Add passed checks
        for check, details in self.checks_passed:
            table.add_row(check, "[green]✅ PASS[/green]", details)

        # Add warnings
        for check, details in self.checks_warning:
            table.add_row(check, "[yellow]⚠️  WARN[/yellow]", details)

        # Add failed checks
        for check, details in self.checks_failed:
            table.add_row(check, "[red]❌ FAIL[/red]", details)

        self.console.print(table)

        # Print summary statistics
        total = len(self.checks_passed) + len(self.checks_warning) + len(self.checks_failed)

        summary = Panel(
            f"[green]Passed: {len(self.checks_passed)}[/green] | "
            f"[yellow]Warnings: {len(self.checks_warning)}[/yellow] | "
            f"[red]Failed: {len(self.checks_failed)}[/red] | "
            f"Total: {total}",
            title="Summary",
            box=box.DOUBLE,
        )
        self.console.print(summary)

        # Print recommendations if there are issues
        if self.checks_failed or self.checks_warning:
            self._print_recommendations()

    def xǁWrknvDoctorǁ_print_summary__mutmut_16(self, verbose: bool) -> None:
        """Print summary of all checks."""
        # Create summary table
        table = Table(title="Diagnostic Results", box=box.ROUNDED)
        table.add_column("Check", style="XXcyanXX")
        table.add_column("Status", style="bold")
        table.add_column("Details")

        # Add passed checks
        for check, details in self.checks_passed:
            table.add_row(check, "[green]✅ PASS[/green]", details)

        # Add warnings
        for check, details in self.checks_warning:
            table.add_row(check, "[yellow]⚠️  WARN[/yellow]", details)

        # Add failed checks
        for check, details in self.checks_failed:
            table.add_row(check, "[red]❌ FAIL[/red]", details)

        self.console.print(table)

        # Print summary statistics
        total = len(self.checks_passed) + len(self.checks_warning) + len(self.checks_failed)

        summary = Panel(
            f"[green]Passed: {len(self.checks_passed)}[/green] | "
            f"[yellow]Warnings: {len(self.checks_warning)}[/yellow] | "
            f"[red]Failed: {len(self.checks_failed)}[/red] | "
            f"Total: {total}",
            title="Summary",
            box=box.DOUBLE,
        )
        self.console.print(summary)

        # Print recommendations if there are issues
        if self.checks_failed or self.checks_warning:
            self._print_recommendations()

    def xǁWrknvDoctorǁ_print_summary__mutmut_17(self, verbose: bool) -> None:
        """Print summary of all checks."""
        # Create summary table
        table = Table(title="Diagnostic Results", box=box.ROUNDED)
        table.add_column("Check", style="CYAN")
        table.add_column("Status", style="bold")
        table.add_column("Details")

        # Add passed checks
        for check, details in self.checks_passed:
            table.add_row(check, "[green]✅ PASS[/green]", details)

        # Add warnings
        for check, details in self.checks_warning:
            table.add_row(check, "[yellow]⚠️  WARN[/yellow]", details)

        # Add failed checks
        for check, details in self.checks_failed:
            table.add_row(check, "[red]❌ FAIL[/red]", details)

        self.console.print(table)

        # Print summary statistics
        total = len(self.checks_passed) + len(self.checks_warning) + len(self.checks_failed)

        summary = Panel(
            f"[green]Passed: {len(self.checks_passed)}[/green] | "
            f"[yellow]Warnings: {len(self.checks_warning)}[/yellow] | "
            f"[red]Failed: {len(self.checks_failed)}[/red] | "
            f"Total: {total}",
            title="Summary",
            box=box.DOUBLE,
        )
        self.console.print(summary)

        # Print recommendations if there are issues
        if self.checks_failed or self.checks_warning:
            self._print_recommendations()

    def xǁWrknvDoctorǁ_print_summary__mutmut_18(self, verbose: bool) -> None:
        """Print summary of all checks."""
        # Create summary table
        table = Table(title="Diagnostic Results", box=box.ROUNDED)
        table.add_column("Check", style="cyan")
        table.add_column(None, style="bold")
        table.add_column("Details")

        # Add passed checks
        for check, details in self.checks_passed:
            table.add_row(check, "[green]✅ PASS[/green]", details)

        # Add warnings
        for check, details in self.checks_warning:
            table.add_row(check, "[yellow]⚠️  WARN[/yellow]", details)

        # Add failed checks
        for check, details in self.checks_failed:
            table.add_row(check, "[red]❌ FAIL[/red]", details)

        self.console.print(table)

        # Print summary statistics
        total = len(self.checks_passed) + len(self.checks_warning) + len(self.checks_failed)

        summary = Panel(
            f"[green]Passed: {len(self.checks_passed)}[/green] | "
            f"[yellow]Warnings: {len(self.checks_warning)}[/yellow] | "
            f"[red]Failed: {len(self.checks_failed)}[/red] | "
            f"Total: {total}",
            title="Summary",
            box=box.DOUBLE,
        )
        self.console.print(summary)

        # Print recommendations if there are issues
        if self.checks_failed or self.checks_warning:
            self._print_recommendations()

    def xǁWrknvDoctorǁ_print_summary__mutmut_19(self, verbose: bool) -> None:
        """Print summary of all checks."""
        # Create summary table
        table = Table(title="Diagnostic Results", box=box.ROUNDED)
        table.add_column("Check", style="cyan")
        table.add_column("Status", style=None)
        table.add_column("Details")

        # Add passed checks
        for check, details in self.checks_passed:
            table.add_row(check, "[green]✅ PASS[/green]", details)

        # Add warnings
        for check, details in self.checks_warning:
            table.add_row(check, "[yellow]⚠️  WARN[/yellow]", details)

        # Add failed checks
        for check, details in self.checks_failed:
            table.add_row(check, "[red]❌ FAIL[/red]", details)

        self.console.print(table)

        # Print summary statistics
        total = len(self.checks_passed) + len(self.checks_warning) + len(self.checks_failed)

        summary = Panel(
            f"[green]Passed: {len(self.checks_passed)}[/green] | "
            f"[yellow]Warnings: {len(self.checks_warning)}[/yellow] | "
            f"[red]Failed: {len(self.checks_failed)}[/red] | "
            f"Total: {total}",
            title="Summary",
            box=box.DOUBLE,
        )
        self.console.print(summary)

        # Print recommendations if there are issues
        if self.checks_failed or self.checks_warning:
            self._print_recommendations()

    def xǁWrknvDoctorǁ_print_summary__mutmut_20(self, verbose: bool) -> None:
        """Print summary of all checks."""
        # Create summary table
        table = Table(title="Diagnostic Results", box=box.ROUNDED)
        table.add_column("Check", style="cyan")
        table.add_column(style="bold")
        table.add_column("Details")

        # Add passed checks
        for check, details in self.checks_passed:
            table.add_row(check, "[green]✅ PASS[/green]", details)

        # Add warnings
        for check, details in self.checks_warning:
            table.add_row(check, "[yellow]⚠️  WARN[/yellow]", details)

        # Add failed checks
        for check, details in self.checks_failed:
            table.add_row(check, "[red]❌ FAIL[/red]", details)

        self.console.print(table)

        # Print summary statistics
        total = len(self.checks_passed) + len(self.checks_warning) + len(self.checks_failed)

        summary = Panel(
            f"[green]Passed: {len(self.checks_passed)}[/green] | "
            f"[yellow]Warnings: {len(self.checks_warning)}[/yellow] | "
            f"[red]Failed: {len(self.checks_failed)}[/red] | "
            f"Total: {total}",
            title="Summary",
            box=box.DOUBLE,
        )
        self.console.print(summary)

        # Print recommendations if there are issues
        if self.checks_failed or self.checks_warning:
            self._print_recommendations()

    def xǁWrknvDoctorǁ_print_summary__mutmut_21(self, verbose: bool) -> None:
        """Print summary of all checks."""
        # Create summary table
        table = Table(title="Diagnostic Results", box=box.ROUNDED)
        table.add_column("Check", style="cyan")
        table.add_column("Status", )
        table.add_column("Details")

        # Add passed checks
        for check, details in self.checks_passed:
            table.add_row(check, "[green]✅ PASS[/green]", details)

        # Add warnings
        for check, details in self.checks_warning:
            table.add_row(check, "[yellow]⚠️  WARN[/yellow]", details)

        # Add failed checks
        for check, details in self.checks_failed:
            table.add_row(check, "[red]❌ FAIL[/red]", details)

        self.console.print(table)

        # Print summary statistics
        total = len(self.checks_passed) + len(self.checks_warning) + len(self.checks_failed)

        summary = Panel(
            f"[green]Passed: {len(self.checks_passed)}[/green] | "
            f"[yellow]Warnings: {len(self.checks_warning)}[/yellow] | "
            f"[red]Failed: {len(self.checks_failed)}[/red] | "
            f"Total: {total}",
            title="Summary",
            box=box.DOUBLE,
        )
        self.console.print(summary)

        # Print recommendations if there are issues
        if self.checks_failed or self.checks_warning:
            self._print_recommendations()

    def xǁWrknvDoctorǁ_print_summary__mutmut_22(self, verbose: bool) -> None:
        """Print summary of all checks."""
        # Create summary table
        table = Table(title="Diagnostic Results", box=box.ROUNDED)
        table.add_column("Check", style="cyan")
        table.add_column("XXStatusXX", style="bold")
        table.add_column("Details")

        # Add passed checks
        for check, details in self.checks_passed:
            table.add_row(check, "[green]✅ PASS[/green]", details)

        # Add warnings
        for check, details in self.checks_warning:
            table.add_row(check, "[yellow]⚠️  WARN[/yellow]", details)

        # Add failed checks
        for check, details in self.checks_failed:
            table.add_row(check, "[red]❌ FAIL[/red]", details)

        self.console.print(table)

        # Print summary statistics
        total = len(self.checks_passed) + len(self.checks_warning) + len(self.checks_failed)

        summary = Panel(
            f"[green]Passed: {len(self.checks_passed)}[/green] | "
            f"[yellow]Warnings: {len(self.checks_warning)}[/yellow] | "
            f"[red]Failed: {len(self.checks_failed)}[/red] | "
            f"Total: {total}",
            title="Summary",
            box=box.DOUBLE,
        )
        self.console.print(summary)

        # Print recommendations if there are issues
        if self.checks_failed or self.checks_warning:
            self._print_recommendations()

    def xǁWrknvDoctorǁ_print_summary__mutmut_23(self, verbose: bool) -> None:
        """Print summary of all checks."""
        # Create summary table
        table = Table(title="Diagnostic Results", box=box.ROUNDED)
        table.add_column("Check", style="cyan")
        table.add_column("status", style="bold")
        table.add_column("Details")

        # Add passed checks
        for check, details in self.checks_passed:
            table.add_row(check, "[green]✅ PASS[/green]", details)

        # Add warnings
        for check, details in self.checks_warning:
            table.add_row(check, "[yellow]⚠️  WARN[/yellow]", details)

        # Add failed checks
        for check, details in self.checks_failed:
            table.add_row(check, "[red]❌ FAIL[/red]", details)

        self.console.print(table)

        # Print summary statistics
        total = len(self.checks_passed) + len(self.checks_warning) + len(self.checks_failed)

        summary = Panel(
            f"[green]Passed: {len(self.checks_passed)}[/green] | "
            f"[yellow]Warnings: {len(self.checks_warning)}[/yellow] | "
            f"[red]Failed: {len(self.checks_failed)}[/red] | "
            f"Total: {total}",
            title="Summary",
            box=box.DOUBLE,
        )
        self.console.print(summary)

        # Print recommendations if there are issues
        if self.checks_failed or self.checks_warning:
            self._print_recommendations()

    def xǁWrknvDoctorǁ_print_summary__mutmut_24(self, verbose: bool) -> None:
        """Print summary of all checks."""
        # Create summary table
        table = Table(title="Diagnostic Results", box=box.ROUNDED)
        table.add_column("Check", style="cyan")
        table.add_column("STATUS", style="bold")
        table.add_column("Details")

        # Add passed checks
        for check, details in self.checks_passed:
            table.add_row(check, "[green]✅ PASS[/green]", details)

        # Add warnings
        for check, details in self.checks_warning:
            table.add_row(check, "[yellow]⚠️  WARN[/yellow]", details)

        # Add failed checks
        for check, details in self.checks_failed:
            table.add_row(check, "[red]❌ FAIL[/red]", details)

        self.console.print(table)

        # Print summary statistics
        total = len(self.checks_passed) + len(self.checks_warning) + len(self.checks_failed)

        summary = Panel(
            f"[green]Passed: {len(self.checks_passed)}[/green] | "
            f"[yellow]Warnings: {len(self.checks_warning)}[/yellow] | "
            f"[red]Failed: {len(self.checks_failed)}[/red] | "
            f"Total: {total}",
            title="Summary",
            box=box.DOUBLE,
        )
        self.console.print(summary)

        # Print recommendations if there are issues
        if self.checks_failed or self.checks_warning:
            self._print_recommendations()

    def xǁWrknvDoctorǁ_print_summary__mutmut_25(self, verbose: bool) -> None:
        """Print summary of all checks."""
        # Create summary table
        table = Table(title="Diagnostic Results", box=box.ROUNDED)
        table.add_column("Check", style="cyan")
        table.add_column("Status", style="XXboldXX")
        table.add_column("Details")

        # Add passed checks
        for check, details in self.checks_passed:
            table.add_row(check, "[green]✅ PASS[/green]", details)

        # Add warnings
        for check, details in self.checks_warning:
            table.add_row(check, "[yellow]⚠️  WARN[/yellow]", details)

        # Add failed checks
        for check, details in self.checks_failed:
            table.add_row(check, "[red]❌ FAIL[/red]", details)

        self.console.print(table)

        # Print summary statistics
        total = len(self.checks_passed) + len(self.checks_warning) + len(self.checks_failed)

        summary = Panel(
            f"[green]Passed: {len(self.checks_passed)}[/green] | "
            f"[yellow]Warnings: {len(self.checks_warning)}[/yellow] | "
            f"[red]Failed: {len(self.checks_failed)}[/red] | "
            f"Total: {total}",
            title="Summary",
            box=box.DOUBLE,
        )
        self.console.print(summary)

        # Print recommendations if there are issues
        if self.checks_failed or self.checks_warning:
            self._print_recommendations()

    def xǁWrknvDoctorǁ_print_summary__mutmut_26(self, verbose: bool) -> None:
        """Print summary of all checks."""
        # Create summary table
        table = Table(title="Diagnostic Results", box=box.ROUNDED)
        table.add_column("Check", style="cyan")
        table.add_column("Status", style="BOLD")
        table.add_column("Details")

        # Add passed checks
        for check, details in self.checks_passed:
            table.add_row(check, "[green]✅ PASS[/green]", details)

        # Add warnings
        for check, details in self.checks_warning:
            table.add_row(check, "[yellow]⚠️  WARN[/yellow]", details)

        # Add failed checks
        for check, details in self.checks_failed:
            table.add_row(check, "[red]❌ FAIL[/red]", details)

        self.console.print(table)

        # Print summary statistics
        total = len(self.checks_passed) + len(self.checks_warning) + len(self.checks_failed)

        summary = Panel(
            f"[green]Passed: {len(self.checks_passed)}[/green] | "
            f"[yellow]Warnings: {len(self.checks_warning)}[/yellow] | "
            f"[red]Failed: {len(self.checks_failed)}[/red] | "
            f"Total: {total}",
            title="Summary",
            box=box.DOUBLE,
        )
        self.console.print(summary)

        # Print recommendations if there are issues
        if self.checks_failed or self.checks_warning:
            self._print_recommendations()

    def xǁWrknvDoctorǁ_print_summary__mutmut_27(self, verbose: bool) -> None:
        """Print summary of all checks."""
        # Create summary table
        table = Table(title="Diagnostic Results", box=box.ROUNDED)
        table.add_column("Check", style="cyan")
        table.add_column("Status", style="bold")
        table.add_column(None)

        # Add passed checks
        for check, details in self.checks_passed:
            table.add_row(check, "[green]✅ PASS[/green]", details)

        # Add warnings
        for check, details in self.checks_warning:
            table.add_row(check, "[yellow]⚠️  WARN[/yellow]", details)

        # Add failed checks
        for check, details in self.checks_failed:
            table.add_row(check, "[red]❌ FAIL[/red]", details)

        self.console.print(table)

        # Print summary statistics
        total = len(self.checks_passed) + len(self.checks_warning) + len(self.checks_failed)

        summary = Panel(
            f"[green]Passed: {len(self.checks_passed)}[/green] | "
            f"[yellow]Warnings: {len(self.checks_warning)}[/yellow] | "
            f"[red]Failed: {len(self.checks_failed)}[/red] | "
            f"Total: {total}",
            title="Summary",
            box=box.DOUBLE,
        )
        self.console.print(summary)

        # Print recommendations if there are issues
        if self.checks_failed or self.checks_warning:
            self._print_recommendations()

    def xǁWrknvDoctorǁ_print_summary__mutmut_28(self, verbose: bool) -> None:
        """Print summary of all checks."""
        # Create summary table
        table = Table(title="Diagnostic Results", box=box.ROUNDED)
        table.add_column("Check", style="cyan")
        table.add_column("Status", style="bold")
        table.add_column("XXDetailsXX")

        # Add passed checks
        for check, details in self.checks_passed:
            table.add_row(check, "[green]✅ PASS[/green]", details)

        # Add warnings
        for check, details in self.checks_warning:
            table.add_row(check, "[yellow]⚠️  WARN[/yellow]", details)

        # Add failed checks
        for check, details in self.checks_failed:
            table.add_row(check, "[red]❌ FAIL[/red]", details)

        self.console.print(table)

        # Print summary statistics
        total = len(self.checks_passed) + len(self.checks_warning) + len(self.checks_failed)

        summary = Panel(
            f"[green]Passed: {len(self.checks_passed)}[/green] | "
            f"[yellow]Warnings: {len(self.checks_warning)}[/yellow] | "
            f"[red]Failed: {len(self.checks_failed)}[/red] | "
            f"Total: {total}",
            title="Summary",
            box=box.DOUBLE,
        )
        self.console.print(summary)

        # Print recommendations if there are issues
        if self.checks_failed or self.checks_warning:
            self._print_recommendations()

    def xǁWrknvDoctorǁ_print_summary__mutmut_29(self, verbose: bool) -> None:
        """Print summary of all checks."""
        # Create summary table
        table = Table(title="Diagnostic Results", box=box.ROUNDED)
        table.add_column("Check", style="cyan")
        table.add_column("Status", style="bold")
        table.add_column("details")

        # Add passed checks
        for check, details in self.checks_passed:
            table.add_row(check, "[green]✅ PASS[/green]", details)

        # Add warnings
        for check, details in self.checks_warning:
            table.add_row(check, "[yellow]⚠️  WARN[/yellow]", details)

        # Add failed checks
        for check, details in self.checks_failed:
            table.add_row(check, "[red]❌ FAIL[/red]", details)

        self.console.print(table)

        # Print summary statistics
        total = len(self.checks_passed) + len(self.checks_warning) + len(self.checks_failed)

        summary = Panel(
            f"[green]Passed: {len(self.checks_passed)}[/green] | "
            f"[yellow]Warnings: {len(self.checks_warning)}[/yellow] | "
            f"[red]Failed: {len(self.checks_failed)}[/red] | "
            f"Total: {total}",
            title="Summary",
            box=box.DOUBLE,
        )
        self.console.print(summary)

        # Print recommendations if there are issues
        if self.checks_failed or self.checks_warning:
            self._print_recommendations()

    def xǁWrknvDoctorǁ_print_summary__mutmut_30(self, verbose: bool) -> None:
        """Print summary of all checks."""
        # Create summary table
        table = Table(title="Diagnostic Results", box=box.ROUNDED)
        table.add_column("Check", style="cyan")
        table.add_column("Status", style="bold")
        table.add_column("DETAILS")

        # Add passed checks
        for check, details in self.checks_passed:
            table.add_row(check, "[green]✅ PASS[/green]", details)

        # Add warnings
        for check, details in self.checks_warning:
            table.add_row(check, "[yellow]⚠️  WARN[/yellow]", details)

        # Add failed checks
        for check, details in self.checks_failed:
            table.add_row(check, "[red]❌ FAIL[/red]", details)

        self.console.print(table)

        # Print summary statistics
        total = len(self.checks_passed) + len(self.checks_warning) + len(self.checks_failed)

        summary = Panel(
            f"[green]Passed: {len(self.checks_passed)}[/green] | "
            f"[yellow]Warnings: {len(self.checks_warning)}[/yellow] | "
            f"[red]Failed: {len(self.checks_failed)}[/red] | "
            f"Total: {total}",
            title="Summary",
            box=box.DOUBLE,
        )
        self.console.print(summary)

        # Print recommendations if there are issues
        if self.checks_failed or self.checks_warning:
            self._print_recommendations()

    def xǁWrknvDoctorǁ_print_summary__mutmut_31(self, verbose: bool) -> None:
        """Print summary of all checks."""
        # Create summary table
        table = Table(title="Diagnostic Results", box=box.ROUNDED)
        table.add_column("Check", style="cyan")
        table.add_column("Status", style="bold")
        table.add_column("Details")

        # Add passed checks
        for check, details in self.checks_passed:
            table.add_row(None, "[green]✅ PASS[/green]", details)

        # Add warnings
        for check, details in self.checks_warning:
            table.add_row(check, "[yellow]⚠️  WARN[/yellow]", details)

        # Add failed checks
        for check, details in self.checks_failed:
            table.add_row(check, "[red]❌ FAIL[/red]", details)

        self.console.print(table)

        # Print summary statistics
        total = len(self.checks_passed) + len(self.checks_warning) + len(self.checks_failed)

        summary = Panel(
            f"[green]Passed: {len(self.checks_passed)}[/green] | "
            f"[yellow]Warnings: {len(self.checks_warning)}[/yellow] | "
            f"[red]Failed: {len(self.checks_failed)}[/red] | "
            f"Total: {total}",
            title="Summary",
            box=box.DOUBLE,
        )
        self.console.print(summary)

        # Print recommendations if there are issues
        if self.checks_failed or self.checks_warning:
            self._print_recommendations()

    def xǁWrknvDoctorǁ_print_summary__mutmut_32(self, verbose: bool) -> None:
        """Print summary of all checks."""
        # Create summary table
        table = Table(title="Diagnostic Results", box=box.ROUNDED)
        table.add_column("Check", style="cyan")
        table.add_column("Status", style="bold")
        table.add_column("Details")

        # Add passed checks
        for check, details in self.checks_passed:
            table.add_row(check, None, details)

        # Add warnings
        for check, details in self.checks_warning:
            table.add_row(check, "[yellow]⚠️  WARN[/yellow]", details)

        # Add failed checks
        for check, details in self.checks_failed:
            table.add_row(check, "[red]❌ FAIL[/red]", details)

        self.console.print(table)

        # Print summary statistics
        total = len(self.checks_passed) + len(self.checks_warning) + len(self.checks_failed)

        summary = Panel(
            f"[green]Passed: {len(self.checks_passed)}[/green] | "
            f"[yellow]Warnings: {len(self.checks_warning)}[/yellow] | "
            f"[red]Failed: {len(self.checks_failed)}[/red] | "
            f"Total: {total}",
            title="Summary",
            box=box.DOUBLE,
        )
        self.console.print(summary)

        # Print recommendations if there are issues
        if self.checks_failed or self.checks_warning:
            self._print_recommendations()

    def xǁWrknvDoctorǁ_print_summary__mutmut_33(self, verbose: bool) -> None:
        """Print summary of all checks."""
        # Create summary table
        table = Table(title="Diagnostic Results", box=box.ROUNDED)
        table.add_column("Check", style="cyan")
        table.add_column("Status", style="bold")
        table.add_column("Details")

        # Add passed checks
        for check, details in self.checks_passed:
            table.add_row(check, "[green]✅ PASS[/green]", None)

        # Add warnings
        for check, details in self.checks_warning:
            table.add_row(check, "[yellow]⚠️  WARN[/yellow]", details)

        # Add failed checks
        for check, details in self.checks_failed:
            table.add_row(check, "[red]❌ FAIL[/red]", details)

        self.console.print(table)

        # Print summary statistics
        total = len(self.checks_passed) + len(self.checks_warning) + len(self.checks_failed)

        summary = Panel(
            f"[green]Passed: {len(self.checks_passed)}[/green] | "
            f"[yellow]Warnings: {len(self.checks_warning)}[/yellow] | "
            f"[red]Failed: {len(self.checks_failed)}[/red] | "
            f"Total: {total}",
            title="Summary",
            box=box.DOUBLE,
        )
        self.console.print(summary)

        # Print recommendations if there are issues
        if self.checks_failed or self.checks_warning:
            self._print_recommendations()

    def xǁWrknvDoctorǁ_print_summary__mutmut_34(self, verbose: bool) -> None:
        """Print summary of all checks."""
        # Create summary table
        table = Table(title="Diagnostic Results", box=box.ROUNDED)
        table.add_column("Check", style="cyan")
        table.add_column("Status", style="bold")
        table.add_column("Details")

        # Add passed checks
        for check, details in self.checks_passed:
            table.add_row("[green]✅ PASS[/green]", details)

        # Add warnings
        for check, details in self.checks_warning:
            table.add_row(check, "[yellow]⚠️  WARN[/yellow]", details)

        # Add failed checks
        for check, details in self.checks_failed:
            table.add_row(check, "[red]❌ FAIL[/red]", details)

        self.console.print(table)

        # Print summary statistics
        total = len(self.checks_passed) + len(self.checks_warning) + len(self.checks_failed)

        summary = Panel(
            f"[green]Passed: {len(self.checks_passed)}[/green] | "
            f"[yellow]Warnings: {len(self.checks_warning)}[/yellow] | "
            f"[red]Failed: {len(self.checks_failed)}[/red] | "
            f"Total: {total}",
            title="Summary",
            box=box.DOUBLE,
        )
        self.console.print(summary)

        # Print recommendations if there are issues
        if self.checks_failed or self.checks_warning:
            self._print_recommendations()

    def xǁWrknvDoctorǁ_print_summary__mutmut_35(self, verbose: bool) -> None:
        """Print summary of all checks."""
        # Create summary table
        table = Table(title="Diagnostic Results", box=box.ROUNDED)
        table.add_column("Check", style="cyan")
        table.add_column("Status", style="bold")
        table.add_column("Details")

        # Add passed checks
        for check, details in self.checks_passed:
            table.add_row(check, details)

        # Add warnings
        for check, details in self.checks_warning:
            table.add_row(check, "[yellow]⚠️  WARN[/yellow]", details)

        # Add failed checks
        for check, details in self.checks_failed:
            table.add_row(check, "[red]❌ FAIL[/red]", details)

        self.console.print(table)

        # Print summary statistics
        total = len(self.checks_passed) + len(self.checks_warning) + len(self.checks_failed)

        summary = Panel(
            f"[green]Passed: {len(self.checks_passed)}[/green] | "
            f"[yellow]Warnings: {len(self.checks_warning)}[/yellow] | "
            f"[red]Failed: {len(self.checks_failed)}[/red] | "
            f"Total: {total}",
            title="Summary",
            box=box.DOUBLE,
        )
        self.console.print(summary)

        # Print recommendations if there are issues
        if self.checks_failed or self.checks_warning:
            self._print_recommendations()

    def xǁWrknvDoctorǁ_print_summary__mutmut_36(self, verbose: bool) -> None:
        """Print summary of all checks."""
        # Create summary table
        table = Table(title="Diagnostic Results", box=box.ROUNDED)
        table.add_column("Check", style="cyan")
        table.add_column("Status", style="bold")
        table.add_column("Details")

        # Add passed checks
        for check, details in self.checks_passed:
            table.add_row(check, "[green]✅ PASS[/green]", )

        # Add warnings
        for check, details in self.checks_warning:
            table.add_row(check, "[yellow]⚠️  WARN[/yellow]", details)

        # Add failed checks
        for check, details in self.checks_failed:
            table.add_row(check, "[red]❌ FAIL[/red]", details)

        self.console.print(table)

        # Print summary statistics
        total = len(self.checks_passed) + len(self.checks_warning) + len(self.checks_failed)

        summary = Panel(
            f"[green]Passed: {len(self.checks_passed)}[/green] | "
            f"[yellow]Warnings: {len(self.checks_warning)}[/yellow] | "
            f"[red]Failed: {len(self.checks_failed)}[/red] | "
            f"Total: {total}",
            title="Summary",
            box=box.DOUBLE,
        )
        self.console.print(summary)

        # Print recommendations if there are issues
        if self.checks_failed or self.checks_warning:
            self._print_recommendations()

    def xǁWrknvDoctorǁ_print_summary__mutmut_37(self, verbose: bool) -> None:
        """Print summary of all checks."""
        # Create summary table
        table = Table(title="Diagnostic Results", box=box.ROUNDED)
        table.add_column("Check", style="cyan")
        table.add_column("Status", style="bold")
        table.add_column("Details")

        # Add passed checks
        for check, details in self.checks_passed:
            table.add_row(check, "XX[green]✅ PASS[/green]XX", details)

        # Add warnings
        for check, details in self.checks_warning:
            table.add_row(check, "[yellow]⚠️  WARN[/yellow]", details)

        # Add failed checks
        for check, details in self.checks_failed:
            table.add_row(check, "[red]❌ FAIL[/red]", details)

        self.console.print(table)

        # Print summary statistics
        total = len(self.checks_passed) + len(self.checks_warning) + len(self.checks_failed)

        summary = Panel(
            f"[green]Passed: {len(self.checks_passed)}[/green] | "
            f"[yellow]Warnings: {len(self.checks_warning)}[/yellow] | "
            f"[red]Failed: {len(self.checks_failed)}[/red] | "
            f"Total: {total}",
            title="Summary",
            box=box.DOUBLE,
        )
        self.console.print(summary)

        # Print recommendations if there are issues
        if self.checks_failed or self.checks_warning:
            self._print_recommendations()

    def xǁWrknvDoctorǁ_print_summary__mutmut_38(self, verbose: bool) -> None:
        """Print summary of all checks."""
        # Create summary table
        table = Table(title="Diagnostic Results", box=box.ROUNDED)
        table.add_column("Check", style="cyan")
        table.add_column("Status", style="bold")
        table.add_column("Details")

        # Add passed checks
        for check, details in self.checks_passed:
            table.add_row(check, "[green]✅ pass[/green]", details)

        # Add warnings
        for check, details in self.checks_warning:
            table.add_row(check, "[yellow]⚠️  WARN[/yellow]", details)

        # Add failed checks
        for check, details in self.checks_failed:
            table.add_row(check, "[red]❌ FAIL[/red]", details)

        self.console.print(table)

        # Print summary statistics
        total = len(self.checks_passed) + len(self.checks_warning) + len(self.checks_failed)

        summary = Panel(
            f"[green]Passed: {len(self.checks_passed)}[/green] | "
            f"[yellow]Warnings: {len(self.checks_warning)}[/yellow] | "
            f"[red]Failed: {len(self.checks_failed)}[/red] | "
            f"Total: {total}",
            title="Summary",
            box=box.DOUBLE,
        )
        self.console.print(summary)

        # Print recommendations if there are issues
        if self.checks_failed or self.checks_warning:
            self._print_recommendations()

    def xǁWrknvDoctorǁ_print_summary__mutmut_39(self, verbose: bool) -> None:
        """Print summary of all checks."""
        # Create summary table
        table = Table(title="Diagnostic Results", box=box.ROUNDED)
        table.add_column("Check", style="cyan")
        table.add_column("Status", style="bold")
        table.add_column("Details")

        # Add passed checks
        for check, details in self.checks_passed:
            table.add_row(check, "[GREEN]✅ PASS[/GREEN]", details)

        # Add warnings
        for check, details in self.checks_warning:
            table.add_row(check, "[yellow]⚠️  WARN[/yellow]", details)

        # Add failed checks
        for check, details in self.checks_failed:
            table.add_row(check, "[red]❌ FAIL[/red]", details)

        self.console.print(table)

        # Print summary statistics
        total = len(self.checks_passed) + len(self.checks_warning) + len(self.checks_failed)

        summary = Panel(
            f"[green]Passed: {len(self.checks_passed)}[/green] | "
            f"[yellow]Warnings: {len(self.checks_warning)}[/yellow] | "
            f"[red]Failed: {len(self.checks_failed)}[/red] | "
            f"Total: {total}",
            title="Summary",
            box=box.DOUBLE,
        )
        self.console.print(summary)

        # Print recommendations if there are issues
        if self.checks_failed or self.checks_warning:
            self._print_recommendations()

    def xǁWrknvDoctorǁ_print_summary__mutmut_40(self, verbose: bool) -> None:
        """Print summary of all checks."""
        # Create summary table
        table = Table(title="Diagnostic Results", box=box.ROUNDED)
        table.add_column("Check", style="cyan")
        table.add_column("Status", style="bold")
        table.add_column("Details")

        # Add passed checks
        for check, details in self.checks_passed:
            table.add_row(check, "[green]✅ PASS[/green]", details)

        # Add warnings
        for check, details in self.checks_warning:
            table.add_row(None, "[yellow]⚠️  WARN[/yellow]", details)

        # Add failed checks
        for check, details in self.checks_failed:
            table.add_row(check, "[red]❌ FAIL[/red]", details)

        self.console.print(table)

        # Print summary statistics
        total = len(self.checks_passed) + len(self.checks_warning) + len(self.checks_failed)

        summary = Panel(
            f"[green]Passed: {len(self.checks_passed)}[/green] | "
            f"[yellow]Warnings: {len(self.checks_warning)}[/yellow] | "
            f"[red]Failed: {len(self.checks_failed)}[/red] | "
            f"Total: {total}",
            title="Summary",
            box=box.DOUBLE,
        )
        self.console.print(summary)

        # Print recommendations if there are issues
        if self.checks_failed or self.checks_warning:
            self._print_recommendations()

    def xǁWrknvDoctorǁ_print_summary__mutmut_41(self, verbose: bool) -> None:
        """Print summary of all checks."""
        # Create summary table
        table = Table(title="Diagnostic Results", box=box.ROUNDED)
        table.add_column("Check", style="cyan")
        table.add_column("Status", style="bold")
        table.add_column("Details")

        # Add passed checks
        for check, details in self.checks_passed:
            table.add_row(check, "[green]✅ PASS[/green]", details)

        # Add warnings
        for check, details in self.checks_warning:
            table.add_row(check, None, details)

        # Add failed checks
        for check, details in self.checks_failed:
            table.add_row(check, "[red]❌ FAIL[/red]", details)

        self.console.print(table)

        # Print summary statistics
        total = len(self.checks_passed) + len(self.checks_warning) + len(self.checks_failed)

        summary = Panel(
            f"[green]Passed: {len(self.checks_passed)}[/green] | "
            f"[yellow]Warnings: {len(self.checks_warning)}[/yellow] | "
            f"[red]Failed: {len(self.checks_failed)}[/red] | "
            f"Total: {total}",
            title="Summary",
            box=box.DOUBLE,
        )
        self.console.print(summary)

        # Print recommendations if there are issues
        if self.checks_failed or self.checks_warning:
            self._print_recommendations()

    def xǁWrknvDoctorǁ_print_summary__mutmut_42(self, verbose: bool) -> None:
        """Print summary of all checks."""
        # Create summary table
        table = Table(title="Diagnostic Results", box=box.ROUNDED)
        table.add_column("Check", style="cyan")
        table.add_column("Status", style="bold")
        table.add_column("Details")

        # Add passed checks
        for check, details in self.checks_passed:
            table.add_row(check, "[green]✅ PASS[/green]", details)

        # Add warnings
        for check, details in self.checks_warning:
            table.add_row(check, "[yellow]⚠️  WARN[/yellow]", None)

        # Add failed checks
        for check, details in self.checks_failed:
            table.add_row(check, "[red]❌ FAIL[/red]", details)

        self.console.print(table)

        # Print summary statistics
        total = len(self.checks_passed) + len(self.checks_warning) + len(self.checks_failed)

        summary = Panel(
            f"[green]Passed: {len(self.checks_passed)}[/green] | "
            f"[yellow]Warnings: {len(self.checks_warning)}[/yellow] | "
            f"[red]Failed: {len(self.checks_failed)}[/red] | "
            f"Total: {total}",
            title="Summary",
            box=box.DOUBLE,
        )
        self.console.print(summary)

        # Print recommendations if there are issues
        if self.checks_failed or self.checks_warning:
            self._print_recommendations()

    def xǁWrknvDoctorǁ_print_summary__mutmut_43(self, verbose: bool) -> None:
        """Print summary of all checks."""
        # Create summary table
        table = Table(title="Diagnostic Results", box=box.ROUNDED)
        table.add_column("Check", style="cyan")
        table.add_column("Status", style="bold")
        table.add_column("Details")

        # Add passed checks
        for check, details in self.checks_passed:
            table.add_row(check, "[green]✅ PASS[/green]", details)

        # Add warnings
        for check, details in self.checks_warning:
            table.add_row("[yellow]⚠️  WARN[/yellow]", details)

        # Add failed checks
        for check, details in self.checks_failed:
            table.add_row(check, "[red]❌ FAIL[/red]", details)

        self.console.print(table)

        # Print summary statistics
        total = len(self.checks_passed) + len(self.checks_warning) + len(self.checks_failed)

        summary = Panel(
            f"[green]Passed: {len(self.checks_passed)}[/green] | "
            f"[yellow]Warnings: {len(self.checks_warning)}[/yellow] | "
            f"[red]Failed: {len(self.checks_failed)}[/red] | "
            f"Total: {total}",
            title="Summary",
            box=box.DOUBLE,
        )
        self.console.print(summary)

        # Print recommendations if there are issues
        if self.checks_failed or self.checks_warning:
            self._print_recommendations()

    def xǁWrknvDoctorǁ_print_summary__mutmut_44(self, verbose: bool) -> None:
        """Print summary of all checks."""
        # Create summary table
        table = Table(title="Diagnostic Results", box=box.ROUNDED)
        table.add_column("Check", style="cyan")
        table.add_column("Status", style="bold")
        table.add_column("Details")

        # Add passed checks
        for check, details in self.checks_passed:
            table.add_row(check, "[green]✅ PASS[/green]", details)

        # Add warnings
        for check, details in self.checks_warning:
            table.add_row(check, details)

        # Add failed checks
        for check, details in self.checks_failed:
            table.add_row(check, "[red]❌ FAIL[/red]", details)

        self.console.print(table)

        # Print summary statistics
        total = len(self.checks_passed) + len(self.checks_warning) + len(self.checks_failed)

        summary = Panel(
            f"[green]Passed: {len(self.checks_passed)}[/green] | "
            f"[yellow]Warnings: {len(self.checks_warning)}[/yellow] | "
            f"[red]Failed: {len(self.checks_failed)}[/red] | "
            f"Total: {total}",
            title="Summary",
            box=box.DOUBLE,
        )
        self.console.print(summary)

        # Print recommendations if there are issues
        if self.checks_failed or self.checks_warning:
            self._print_recommendations()

    def xǁWrknvDoctorǁ_print_summary__mutmut_45(self, verbose: bool) -> None:
        """Print summary of all checks."""
        # Create summary table
        table = Table(title="Diagnostic Results", box=box.ROUNDED)
        table.add_column("Check", style="cyan")
        table.add_column("Status", style="bold")
        table.add_column("Details")

        # Add passed checks
        for check, details in self.checks_passed:
            table.add_row(check, "[green]✅ PASS[/green]", details)

        # Add warnings
        for check, details in self.checks_warning:
            table.add_row(check, "[yellow]⚠️  WARN[/yellow]", )

        # Add failed checks
        for check, details in self.checks_failed:
            table.add_row(check, "[red]❌ FAIL[/red]", details)

        self.console.print(table)

        # Print summary statistics
        total = len(self.checks_passed) + len(self.checks_warning) + len(self.checks_failed)

        summary = Panel(
            f"[green]Passed: {len(self.checks_passed)}[/green] | "
            f"[yellow]Warnings: {len(self.checks_warning)}[/yellow] | "
            f"[red]Failed: {len(self.checks_failed)}[/red] | "
            f"Total: {total}",
            title="Summary",
            box=box.DOUBLE,
        )
        self.console.print(summary)

        # Print recommendations if there are issues
        if self.checks_failed or self.checks_warning:
            self._print_recommendations()

    def xǁWrknvDoctorǁ_print_summary__mutmut_46(self, verbose: bool) -> None:
        """Print summary of all checks."""
        # Create summary table
        table = Table(title="Diagnostic Results", box=box.ROUNDED)
        table.add_column("Check", style="cyan")
        table.add_column("Status", style="bold")
        table.add_column("Details")

        # Add passed checks
        for check, details in self.checks_passed:
            table.add_row(check, "[green]✅ PASS[/green]", details)

        # Add warnings
        for check, details in self.checks_warning:
            table.add_row(check, "XX[yellow]⚠️  WARN[/yellow]XX", details)

        # Add failed checks
        for check, details in self.checks_failed:
            table.add_row(check, "[red]❌ FAIL[/red]", details)

        self.console.print(table)

        # Print summary statistics
        total = len(self.checks_passed) + len(self.checks_warning) + len(self.checks_failed)

        summary = Panel(
            f"[green]Passed: {len(self.checks_passed)}[/green] | "
            f"[yellow]Warnings: {len(self.checks_warning)}[/yellow] | "
            f"[red]Failed: {len(self.checks_failed)}[/red] | "
            f"Total: {total}",
            title="Summary",
            box=box.DOUBLE,
        )
        self.console.print(summary)

        # Print recommendations if there are issues
        if self.checks_failed or self.checks_warning:
            self._print_recommendations()

    def xǁWrknvDoctorǁ_print_summary__mutmut_47(self, verbose: bool) -> None:
        """Print summary of all checks."""
        # Create summary table
        table = Table(title="Diagnostic Results", box=box.ROUNDED)
        table.add_column("Check", style="cyan")
        table.add_column("Status", style="bold")
        table.add_column("Details")

        # Add passed checks
        for check, details in self.checks_passed:
            table.add_row(check, "[green]✅ PASS[/green]", details)

        # Add warnings
        for check, details in self.checks_warning:
            table.add_row(check, "[yellow]⚠️  warn[/yellow]", details)

        # Add failed checks
        for check, details in self.checks_failed:
            table.add_row(check, "[red]❌ FAIL[/red]", details)

        self.console.print(table)

        # Print summary statistics
        total = len(self.checks_passed) + len(self.checks_warning) + len(self.checks_failed)

        summary = Panel(
            f"[green]Passed: {len(self.checks_passed)}[/green] | "
            f"[yellow]Warnings: {len(self.checks_warning)}[/yellow] | "
            f"[red]Failed: {len(self.checks_failed)}[/red] | "
            f"Total: {total}",
            title="Summary",
            box=box.DOUBLE,
        )
        self.console.print(summary)

        # Print recommendations if there are issues
        if self.checks_failed or self.checks_warning:
            self._print_recommendations()

    def xǁWrknvDoctorǁ_print_summary__mutmut_48(self, verbose: bool) -> None:
        """Print summary of all checks."""
        # Create summary table
        table = Table(title="Diagnostic Results", box=box.ROUNDED)
        table.add_column("Check", style="cyan")
        table.add_column("Status", style="bold")
        table.add_column("Details")

        # Add passed checks
        for check, details in self.checks_passed:
            table.add_row(check, "[green]✅ PASS[/green]", details)

        # Add warnings
        for check, details in self.checks_warning:
            table.add_row(check, "[YELLOW]⚠️  WARN[/YELLOW]", details)

        # Add failed checks
        for check, details in self.checks_failed:
            table.add_row(check, "[red]❌ FAIL[/red]", details)

        self.console.print(table)

        # Print summary statistics
        total = len(self.checks_passed) + len(self.checks_warning) + len(self.checks_failed)

        summary = Panel(
            f"[green]Passed: {len(self.checks_passed)}[/green] | "
            f"[yellow]Warnings: {len(self.checks_warning)}[/yellow] | "
            f"[red]Failed: {len(self.checks_failed)}[/red] | "
            f"Total: {total}",
            title="Summary",
            box=box.DOUBLE,
        )
        self.console.print(summary)

        # Print recommendations if there are issues
        if self.checks_failed or self.checks_warning:
            self._print_recommendations()

    def xǁWrknvDoctorǁ_print_summary__mutmut_49(self, verbose: bool) -> None:
        """Print summary of all checks."""
        # Create summary table
        table = Table(title="Diagnostic Results", box=box.ROUNDED)
        table.add_column("Check", style="cyan")
        table.add_column("Status", style="bold")
        table.add_column("Details")

        # Add passed checks
        for check, details in self.checks_passed:
            table.add_row(check, "[green]✅ PASS[/green]", details)

        # Add warnings
        for check, details in self.checks_warning:
            table.add_row(check, "[yellow]⚠️  WARN[/yellow]", details)

        # Add failed checks
        for check, details in self.checks_failed:
            table.add_row(None, "[red]❌ FAIL[/red]", details)

        self.console.print(table)

        # Print summary statistics
        total = len(self.checks_passed) + len(self.checks_warning) + len(self.checks_failed)

        summary = Panel(
            f"[green]Passed: {len(self.checks_passed)}[/green] | "
            f"[yellow]Warnings: {len(self.checks_warning)}[/yellow] | "
            f"[red]Failed: {len(self.checks_failed)}[/red] | "
            f"Total: {total}",
            title="Summary",
            box=box.DOUBLE,
        )
        self.console.print(summary)

        # Print recommendations if there are issues
        if self.checks_failed or self.checks_warning:
            self._print_recommendations()

    def xǁWrknvDoctorǁ_print_summary__mutmut_50(self, verbose: bool) -> None:
        """Print summary of all checks."""
        # Create summary table
        table = Table(title="Diagnostic Results", box=box.ROUNDED)
        table.add_column("Check", style="cyan")
        table.add_column("Status", style="bold")
        table.add_column("Details")

        # Add passed checks
        for check, details in self.checks_passed:
            table.add_row(check, "[green]✅ PASS[/green]", details)

        # Add warnings
        for check, details in self.checks_warning:
            table.add_row(check, "[yellow]⚠️  WARN[/yellow]", details)

        # Add failed checks
        for check, details in self.checks_failed:
            table.add_row(check, None, details)

        self.console.print(table)

        # Print summary statistics
        total = len(self.checks_passed) + len(self.checks_warning) + len(self.checks_failed)

        summary = Panel(
            f"[green]Passed: {len(self.checks_passed)}[/green] | "
            f"[yellow]Warnings: {len(self.checks_warning)}[/yellow] | "
            f"[red]Failed: {len(self.checks_failed)}[/red] | "
            f"Total: {total}",
            title="Summary",
            box=box.DOUBLE,
        )
        self.console.print(summary)

        # Print recommendations if there are issues
        if self.checks_failed or self.checks_warning:
            self._print_recommendations()

    def xǁWrknvDoctorǁ_print_summary__mutmut_51(self, verbose: bool) -> None:
        """Print summary of all checks."""
        # Create summary table
        table = Table(title="Diagnostic Results", box=box.ROUNDED)
        table.add_column("Check", style="cyan")
        table.add_column("Status", style="bold")
        table.add_column("Details")

        # Add passed checks
        for check, details in self.checks_passed:
            table.add_row(check, "[green]✅ PASS[/green]", details)

        # Add warnings
        for check, details in self.checks_warning:
            table.add_row(check, "[yellow]⚠️  WARN[/yellow]", details)

        # Add failed checks
        for check, details in self.checks_failed:
            table.add_row(check, "[red]❌ FAIL[/red]", None)

        self.console.print(table)

        # Print summary statistics
        total = len(self.checks_passed) + len(self.checks_warning) + len(self.checks_failed)

        summary = Panel(
            f"[green]Passed: {len(self.checks_passed)}[/green] | "
            f"[yellow]Warnings: {len(self.checks_warning)}[/yellow] | "
            f"[red]Failed: {len(self.checks_failed)}[/red] | "
            f"Total: {total}",
            title="Summary",
            box=box.DOUBLE,
        )
        self.console.print(summary)

        # Print recommendations if there are issues
        if self.checks_failed or self.checks_warning:
            self._print_recommendations()

    def xǁWrknvDoctorǁ_print_summary__mutmut_52(self, verbose: bool) -> None:
        """Print summary of all checks."""
        # Create summary table
        table = Table(title="Diagnostic Results", box=box.ROUNDED)
        table.add_column("Check", style="cyan")
        table.add_column("Status", style="bold")
        table.add_column("Details")

        # Add passed checks
        for check, details in self.checks_passed:
            table.add_row(check, "[green]✅ PASS[/green]", details)

        # Add warnings
        for check, details in self.checks_warning:
            table.add_row(check, "[yellow]⚠️  WARN[/yellow]", details)

        # Add failed checks
        for check, details in self.checks_failed:
            table.add_row("[red]❌ FAIL[/red]", details)

        self.console.print(table)

        # Print summary statistics
        total = len(self.checks_passed) + len(self.checks_warning) + len(self.checks_failed)

        summary = Panel(
            f"[green]Passed: {len(self.checks_passed)}[/green] | "
            f"[yellow]Warnings: {len(self.checks_warning)}[/yellow] | "
            f"[red]Failed: {len(self.checks_failed)}[/red] | "
            f"Total: {total}",
            title="Summary",
            box=box.DOUBLE,
        )
        self.console.print(summary)

        # Print recommendations if there are issues
        if self.checks_failed or self.checks_warning:
            self._print_recommendations()

    def xǁWrknvDoctorǁ_print_summary__mutmut_53(self, verbose: bool) -> None:
        """Print summary of all checks."""
        # Create summary table
        table = Table(title="Diagnostic Results", box=box.ROUNDED)
        table.add_column("Check", style="cyan")
        table.add_column("Status", style="bold")
        table.add_column("Details")

        # Add passed checks
        for check, details in self.checks_passed:
            table.add_row(check, "[green]✅ PASS[/green]", details)

        # Add warnings
        for check, details in self.checks_warning:
            table.add_row(check, "[yellow]⚠️  WARN[/yellow]", details)

        # Add failed checks
        for check, details in self.checks_failed:
            table.add_row(check, details)

        self.console.print(table)

        # Print summary statistics
        total = len(self.checks_passed) + len(self.checks_warning) + len(self.checks_failed)

        summary = Panel(
            f"[green]Passed: {len(self.checks_passed)}[/green] | "
            f"[yellow]Warnings: {len(self.checks_warning)}[/yellow] | "
            f"[red]Failed: {len(self.checks_failed)}[/red] | "
            f"Total: {total}",
            title="Summary",
            box=box.DOUBLE,
        )
        self.console.print(summary)

        # Print recommendations if there are issues
        if self.checks_failed or self.checks_warning:
            self._print_recommendations()

    def xǁWrknvDoctorǁ_print_summary__mutmut_54(self, verbose: bool) -> None:
        """Print summary of all checks."""
        # Create summary table
        table = Table(title="Diagnostic Results", box=box.ROUNDED)
        table.add_column("Check", style="cyan")
        table.add_column("Status", style="bold")
        table.add_column("Details")

        # Add passed checks
        for check, details in self.checks_passed:
            table.add_row(check, "[green]✅ PASS[/green]", details)

        # Add warnings
        for check, details in self.checks_warning:
            table.add_row(check, "[yellow]⚠️  WARN[/yellow]", details)

        # Add failed checks
        for check, details in self.checks_failed:
            table.add_row(check, "[red]❌ FAIL[/red]", )

        self.console.print(table)

        # Print summary statistics
        total = len(self.checks_passed) + len(self.checks_warning) + len(self.checks_failed)

        summary = Panel(
            f"[green]Passed: {len(self.checks_passed)}[/green] | "
            f"[yellow]Warnings: {len(self.checks_warning)}[/yellow] | "
            f"[red]Failed: {len(self.checks_failed)}[/red] | "
            f"Total: {total}",
            title="Summary",
            box=box.DOUBLE,
        )
        self.console.print(summary)

        # Print recommendations if there are issues
        if self.checks_failed or self.checks_warning:
            self._print_recommendations()

    def xǁWrknvDoctorǁ_print_summary__mutmut_55(self, verbose: bool) -> None:
        """Print summary of all checks."""
        # Create summary table
        table = Table(title="Diagnostic Results", box=box.ROUNDED)
        table.add_column("Check", style="cyan")
        table.add_column("Status", style="bold")
        table.add_column("Details")

        # Add passed checks
        for check, details in self.checks_passed:
            table.add_row(check, "[green]✅ PASS[/green]", details)

        # Add warnings
        for check, details in self.checks_warning:
            table.add_row(check, "[yellow]⚠️  WARN[/yellow]", details)

        # Add failed checks
        for check, details in self.checks_failed:
            table.add_row(check, "XX[red]❌ FAIL[/red]XX", details)

        self.console.print(table)

        # Print summary statistics
        total = len(self.checks_passed) + len(self.checks_warning) + len(self.checks_failed)

        summary = Panel(
            f"[green]Passed: {len(self.checks_passed)}[/green] | "
            f"[yellow]Warnings: {len(self.checks_warning)}[/yellow] | "
            f"[red]Failed: {len(self.checks_failed)}[/red] | "
            f"Total: {total}",
            title="Summary",
            box=box.DOUBLE,
        )
        self.console.print(summary)

        # Print recommendations if there are issues
        if self.checks_failed or self.checks_warning:
            self._print_recommendations()

    def xǁWrknvDoctorǁ_print_summary__mutmut_56(self, verbose: bool) -> None:
        """Print summary of all checks."""
        # Create summary table
        table = Table(title="Diagnostic Results", box=box.ROUNDED)
        table.add_column("Check", style="cyan")
        table.add_column("Status", style="bold")
        table.add_column("Details")

        # Add passed checks
        for check, details in self.checks_passed:
            table.add_row(check, "[green]✅ PASS[/green]", details)

        # Add warnings
        for check, details in self.checks_warning:
            table.add_row(check, "[yellow]⚠️  WARN[/yellow]", details)

        # Add failed checks
        for check, details in self.checks_failed:
            table.add_row(check, "[red]❌ fail[/red]", details)

        self.console.print(table)

        # Print summary statistics
        total = len(self.checks_passed) + len(self.checks_warning) + len(self.checks_failed)

        summary = Panel(
            f"[green]Passed: {len(self.checks_passed)}[/green] | "
            f"[yellow]Warnings: {len(self.checks_warning)}[/yellow] | "
            f"[red]Failed: {len(self.checks_failed)}[/red] | "
            f"Total: {total}",
            title="Summary",
            box=box.DOUBLE,
        )
        self.console.print(summary)

        # Print recommendations if there are issues
        if self.checks_failed or self.checks_warning:
            self._print_recommendations()

    def xǁWrknvDoctorǁ_print_summary__mutmut_57(self, verbose: bool) -> None:
        """Print summary of all checks."""
        # Create summary table
        table = Table(title="Diagnostic Results", box=box.ROUNDED)
        table.add_column("Check", style="cyan")
        table.add_column("Status", style="bold")
        table.add_column("Details")

        # Add passed checks
        for check, details in self.checks_passed:
            table.add_row(check, "[green]✅ PASS[/green]", details)

        # Add warnings
        for check, details in self.checks_warning:
            table.add_row(check, "[yellow]⚠️  WARN[/yellow]", details)

        # Add failed checks
        for check, details in self.checks_failed:
            table.add_row(check, "[RED]❌ FAIL[/RED]", details)

        self.console.print(table)

        # Print summary statistics
        total = len(self.checks_passed) + len(self.checks_warning) + len(self.checks_failed)

        summary = Panel(
            f"[green]Passed: {len(self.checks_passed)}[/green] | "
            f"[yellow]Warnings: {len(self.checks_warning)}[/yellow] | "
            f"[red]Failed: {len(self.checks_failed)}[/red] | "
            f"Total: {total}",
            title="Summary",
            box=box.DOUBLE,
        )
        self.console.print(summary)

        # Print recommendations if there are issues
        if self.checks_failed or self.checks_warning:
            self._print_recommendations()

    def xǁWrknvDoctorǁ_print_summary__mutmut_58(self, verbose: bool) -> None:
        """Print summary of all checks."""
        # Create summary table
        table = Table(title="Diagnostic Results", box=box.ROUNDED)
        table.add_column("Check", style="cyan")
        table.add_column("Status", style="bold")
        table.add_column("Details")

        # Add passed checks
        for check, details in self.checks_passed:
            table.add_row(check, "[green]✅ PASS[/green]", details)

        # Add warnings
        for check, details in self.checks_warning:
            table.add_row(check, "[yellow]⚠️  WARN[/yellow]", details)

        # Add failed checks
        for check, details in self.checks_failed:
            table.add_row(check, "[red]❌ FAIL[/red]", details)

        self.console.print(None)

        # Print summary statistics
        total = len(self.checks_passed) + len(self.checks_warning) + len(self.checks_failed)

        summary = Panel(
            f"[green]Passed: {len(self.checks_passed)}[/green] | "
            f"[yellow]Warnings: {len(self.checks_warning)}[/yellow] | "
            f"[red]Failed: {len(self.checks_failed)}[/red] | "
            f"Total: {total}",
            title="Summary",
            box=box.DOUBLE,
        )
        self.console.print(summary)

        # Print recommendations if there are issues
        if self.checks_failed or self.checks_warning:
            self._print_recommendations()

    def xǁWrknvDoctorǁ_print_summary__mutmut_59(self, verbose: bool) -> None:
        """Print summary of all checks."""
        # Create summary table
        table = Table(title="Diagnostic Results", box=box.ROUNDED)
        table.add_column("Check", style="cyan")
        table.add_column("Status", style="bold")
        table.add_column("Details")

        # Add passed checks
        for check, details in self.checks_passed:
            table.add_row(check, "[green]✅ PASS[/green]", details)

        # Add warnings
        for check, details in self.checks_warning:
            table.add_row(check, "[yellow]⚠️  WARN[/yellow]", details)

        # Add failed checks
        for check, details in self.checks_failed:
            table.add_row(check, "[red]❌ FAIL[/red]", details)

        self.console.print(table)

        # Print summary statistics
        total = None

        summary = Panel(
            f"[green]Passed: {len(self.checks_passed)}[/green] | "
            f"[yellow]Warnings: {len(self.checks_warning)}[/yellow] | "
            f"[red]Failed: {len(self.checks_failed)}[/red] | "
            f"Total: {total}",
            title="Summary",
            box=box.DOUBLE,
        )
        self.console.print(summary)

        # Print recommendations if there are issues
        if self.checks_failed or self.checks_warning:
            self._print_recommendations()

    def xǁWrknvDoctorǁ_print_summary__mutmut_60(self, verbose: bool) -> None:
        """Print summary of all checks."""
        # Create summary table
        table = Table(title="Diagnostic Results", box=box.ROUNDED)
        table.add_column("Check", style="cyan")
        table.add_column("Status", style="bold")
        table.add_column("Details")

        # Add passed checks
        for check, details in self.checks_passed:
            table.add_row(check, "[green]✅ PASS[/green]", details)

        # Add warnings
        for check, details in self.checks_warning:
            table.add_row(check, "[yellow]⚠️  WARN[/yellow]", details)

        # Add failed checks
        for check, details in self.checks_failed:
            table.add_row(check, "[red]❌ FAIL[/red]", details)

        self.console.print(table)

        # Print summary statistics
        total = len(self.checks_passed) + len(self.checks_warning) - len(self.checks_failed)

        summary = Panel(
            f"[green]Passed: {len(self.checks_passed)}[/green] | "
            f"[yellow]Warnings: {len(self.checks_warning)}[/yellow] | "
            f"[red]Failed: {len(self.checks_failed)}[/red] | "
            f"Total: {total}",
            title="Summary",
            box=box.DOUBLE,
        )
        self.console.print(summary)

        # Print recommendations if there are issues
        if self.checks_failed or self.checks_warning:
            self._print_recommendations()

    def xǁWrknvDoctorǁ_print_summary__mutmut_61(self, verbose: bool) -> None:
        """Print summary of all checks."""
        # Create summary table
        table = Table(title="Diagnostic Results", box=box.ROUNDED)
        table.add_column("Check", style="cyan")
        table.add_column("Status", style="bold")
        table.add_column("Details")

        # Add passed checks
        for check, details in self.checks_passed:
            table.add_row(check, "[green]✅ PASS[/green]", details)

        # Add warnings
        for check, details in self.checks_warning:
            table.add_row(check, "[yellow]⚠️  WARN[/yellow]", details)

        # Add failed checks
        for check, details in self.checks_failed:
            table.add_row(check, "[red]❌ FAIL[/red]", details)

        self.console.print(table)

        # Print summary statistics
        total = len(self.checks_passed) - len(self.checks_warning) + len(self.checks_failed)

        summary = Panel(
            f"[green]Passed: {len(self.checks_passed)}[/green] | "
            f"[yellow]Warnings: {len(self.checks_warning)}[/yellow] | "
            f"[red]Failed: {len(self.checks_failed)}[/red] | "
            f"Total: {total}",
            title="Summary",
            box=box.DOUBLE,
        )
        self.console.print(summary)

        # Print recommendations if there are issues
        if self.checks_failed or self.checks_warning:
            self._print_recommendations()

    def xǁWrknvDoctorǁ_print_summary__mutmut_62(self, verbose: bool) -> None:
        """Print summary of all checks."""
        # Create summary table
        table = Table(title="Diagnostic Results", box=box.ROUNDED)
        table.add_column("Check", style="cyan")
        table.add_column("Status", style="bold")
        table.add_column("Details")

        # Add passed checks
        for check, details in self.checks_passed:
            table.add_row(check, "[green]✅ PASS[/green]", details)

        # Add warnings
        for check, details in self.checks_warning:
            table.add_row(check, "[yellow]⚠️  WARN[/yellow]", details)

        # Add failed checks
        for check, details in self.checks_failed:
            table.add_row(check, "[red]❌ FAIL[/red]", details)

        self.console.print(table)

        # Print summary statistics
        total = len(self.checks_passed) + len(self.checks_warning) + len(self.checks_failed)

        summary = None
        self.console.print(summary)

        # Print recommendations if there are issues
        if self.checks_failed or self.checks_warning:
            self._print_recommendations()

    def xǁWrknvDoctorǁ_print_summary__mutmut_63(self, verbose: bool) -> None:
        """Print summary of all checks."""
        # Create summary table
        table = Table(title="Diagnostic Results", box=box.ROUNDED)
        table.add_column("Check", style="cyan")
        table.add_column("Status", style="bold")
        table.add_column("Details")

        # Add passed checks
        for check, details in self.checks_passed:
            table.add_row(check, "[green]✅ PASS[/green]", details)

        # Add warnings
        for check, details in self.checks_warning:
            table.add_row(check, "[yellow]⚠️  WARN[/yellow]", details)

        # Add failed checks
        for check, details in self.checks_failed:
            table.add_row(check, "[red]❌ FAIL[/red]", details)

        self.console.print(table)

        # Print summary statistics
        total = len(self.checks_passed) + len(self.checks_warning) + len(self.checks_failed)

        summary = Panel(
            None,
            title="Summary",
            box=box.DOUBLE,
        )
        self.console.print(summary)

        # Print recommendations if there are issues
        if self.checks_failed or self.checks_warning:
            self._print_recommendations()

    def xǁWrknvDoctorǁ_print_summary__mutmut_64(self, verbose: bool) -> None:
        """Print summary of all checks."""
        # Create summary table
        table = Table(title="Diagnostic Results", box=box.ROUNDED)
        table.add_column("Check", style="cyan")
        table.add_column("Status", style="bold")
        table.add_column("Details")

        # Add passed checks
        for check, details in self.checks_passed:
            table.add_row(check, "[green]✅ PASS[/green]", details)

        # Add warnings
        for check, details in self.checks_warning:
            table.add_row(check, "[yellow]⚠️  WARN[/yellow]", details)

        # Add failed checks
        for check, details in self.checks_failed:
            table.add_row(check, "[red]❌ FAIL[/red]", details)

        self.console.print(table)

        # Print summary statistics
        total = len(self.checks_passed) + len(self.checks_warning) + len(self.checks_failed)

        summary = Panel(
            f"[green]Passed: {len(self.checks_passed)}[/green] | "
            f"[yellow]Warnings: {len(self.checks_warning)}[/yellow] | "
            f"[red]Failed: {len(self.checks_failed)}[/red] | "
            f"Total: {total}",
            title=None,
            box=box.DOUBLE,
        )
        self.console.print(summary)

        # Print recommendations if there are issues
        if self.checks_failed or self.checks_warning:
            self._print_recommendations()

    def xǁWrknvDoctorǁ_print_summary__mutmut_65(self, verbose: bool) -> None:
        """Print summary of all checks."""
        # Create summary table
        table = Table(title="Diagnostic Results", box=box.ROUNDED)
        table.add_column("Check", style="cyan")
        table.add_column("Status", style="bold")
        table.add_column("Details")

        # Add passed checks
        for check, details in self.checks_passed:
            table.add_row(check, "[green]✅ PASS[/green]", details)

        # Add warnings
        for check, details in self.checks_warning:
            table.add_row(check, "[yellow]⚠️  WARN[/yellow]", details)

        # Add failed checks
        for check, details in self.checks_failed:
            table.add_row(check, "[red]❌ FAIL[/red]", details)

        self.console.print(table)

        # Print summary statistics
        total = len(self.checks_passed) + len(self.checks_warning) + len(self.checks_failed)

        summary = Panel(
            f"[green]Passed: {len(self.checks_passed)}[/green] | "
            f"[yellow]Warnings: {len(self.checks_warning)}[/yellow] | "
            f"[red]Failed: {len(self.checks_failed)}[/red] | "
            f"Total: {total}",
            title="Summary",
            box=None,
        )
        self.console.print(summary)

        # Print recommendations if there are issues
        if self.checks_failed or self.checks_warning:
            self._print_recommendations()

    def xǁWrknvDoctorǁ_print_summary__mutmut_66(self, verbose: bool) -> None:
        """Print summary of all checks."""
        # Create summary table
        table = Table(title="Diagnostic Results", box=box.ROUNDED)
        table.add_column("Check", style="cyan")
        table.add_column("Status", style="bold")
        table.add_column("Details")

        # Add passed checks
        for check, details in self.checks_passed:
            table.add_row(check, "[green]✅ PASS[/green]", details)

        # Add warnings
        for check, details in self.checks_warning:
            table.add_row(check, "[yellow]⚠️  WARN[/yellow]", details)

        # Add failed checks
        for check, details in self.checks_failed:
            table.add_row(check, "[red]❌ FAIL[/red]", details)

        self.console.print(table)

        # Print summary statistics
        total = len(self.checks_passed) + len(self.checks_warning) + len(self.checks_failed)

        summary = Panel(
            title="Summary",
            box=box.DOUBLE,
        )
        self.console.print(summary)

        # Print recommendations if there are issues
        if self.checks_failed or self.checks_warning:
            self._print_recommendations()

    def xǁWrknvDoctorǁ_print_summary__mutmut_67(self, verbose: bool) -> None:
        """Print summary of all checks."""
        # Create summary table
        table = Table(title="Diagnostic Results", box=box.ROUNDED)
        table.add_column("Check", style="cyan")
        table.add_column("Status", style="bold")
        table.add_column("Details")

        # Add passed checks
        for check, details in self.checks_passed:
            table.add_row(check, "[green]✅ PASS[/green]", details)

        # Add warnings
        for check, details in self.checks_warning:
            table.add_row(check, "[yellow]⚠️  WARN[/yellow]", details)

        # Add failed checks
        for check, details in self.checks_failed:
            table.add_row(check, "[red]❌ FAIL[/red]", details)

        self.console.print(table)

        # Print summary statistics
        total = len(self.checks_passed) + len(self.checks_warning) + len(self.checks_failed)

        summary = Panel(
            f"[green]Passed: {len(self.checks_passed)}[/green] | "
            f"[yellow]Warnings: {len(self.checks_warning)}[/yellow] | "
            f"[red]Failed: {len(self.checks_failed)}[/red] | "
            f"Total: {total}",
            box=box.DOUBLE,
        )
        self.console.print(summary)

        # Print recommendations if there are issues
        if self.checks_failed or self.checks_warning:
            self._print_recommendations()

    def xǁWrknvDoctorǁ_print_summary__mutmut_68(self, verbose: bool) -> None:
        """Print summary of all checks."""
        # Create summary table
        table = Table(title="Diagnostic Results", box=box.ROUNDED)
        table.add_column("Check", style="cyan")
        table.add_column("Status", style="bold")
        table.add_column("Details")

        # Add passed checks
        for check, details in self.checks_passed:
            table.add_row(check, "[green]✅ PASS[/green]", details)

        # Add warnings
        for check, details in self.checks_warning:
            table.add_row(check, "[yellow]⚠️  WARN[/yellow]", details)

        # Add failed checks
        for check, details in self.checks_failed:
            table.add_row(check, "[red]❌ FAIL[/red]", details)

        self.console.print(table)

        # Print summary statistics
        total = len(self.checks_passed) + len(self.checks_warning) + len(self.checks_failed)

        summary = Panel(
            f"[green]Passed: {len(self.checks_passed)}[/green] | "
            f"[yellow]Warnings: {len(self.checks_warning)}[/yellow] | "
            f"[red]Failed: {len(self.checks_failed)}[/red] | "
            f"Total: {total}",
            title="Summary",
            )
        self.console.print(summary)

        # Print recommendations if there are issues
        if self.checks_failed or self.checks_warning:
            self._print_recommendations()

    def xǁWrknvDoctorǁ_print_summary__mutmut_69(self, verbose: bool) -> None:
        """Print summary of all checks."""
        # Create summary table
        table = Table(title="Diagnostic Results", box=box.ROUNDED)
        table.add_column("Check", style="cyan")
        table.add_column("Status", style="bold")
        table.add_column("Details")

        # Add passed checks
        for check, details in self.checks_passed:
            table.add_row(check, "[green]✅ PASS[/green]", details)

        # Add warnings
        for check, details in self.checks_warning:
            table.add_row(check, "[yellow]⚠️  WARN[/yellow]", details)

        # Add failed checks
        for check, details in self.checks_failed:
            table.add_row(check, "[red]❌ FAIL[/red]", details)

        self.console.print(table)

        # Print summary statistics
        total = len(self.checks_passed) + len(self.checks_warning) + len(self.checks_failed)

        summary = Panel(
            f"[green]Passed: {len(self.checks_passed)}[/green] | "
            f"[yellow]Warnings: {len(self.checks_warning)}[/yellow] | "
            f"[red]Failed: {len(self.checks_failed)}[/red] | "
            f"Total: {total}",
            title="XXSummaryXX",
            box=box.DOUBLE,
        )
        self.console.print(summary)

        # Print recommendations if there are issues
        if self.checks_failed or self.checks_warning:
            self._print_recommendations()

    def xǁWrknvDoctorǁ_print_summary__mutmut_70(self, verbose: bool) -> None:
        """Print summary of all checks."""
        # Create summary table
        table = Table(title="Diagnostic Results", box=box.ROUNDED)
        table.add_column("Check", style="cyan")
        table.add_column("Status", style="bold")
        table.add_column("Details")

        # Add passed checks
        for check, details in self.checks_passed:
            table.add_row(check, "[green]✅ PASS[/green]", details)

        # Add warnings
        for check, details in self.checks_warning:
            table.add_row(check, "[yellow]⚠️  WARN[/yellow]", details)

        # Add failed checks
        for check, details in self.checks_failed:
            table.add_row(check, "[red]❌ FAIL[/red]", details)

        self.console.print(table)

        # Print summary statistics
        total = len(self.checks_passed) + len(self.checks_warning) + len(self.checks_failed)

        summary = Panel(
            f"[green]Passed: {len(self.checks_passed)}[/green] | "
            f"[yellow]Warnings: {len(self.checks_warning)}[/yellow] | "
            f"[red]Failed: {len(self.checks_failed)}[/red] | "
            f"Total: {total}",
            title="summary",
            box=box.DOUBLE,
        )
        self.console.print(summary)

        # Print recommendations if there are issues
        if self.checks_failed or self.checks_warning:
            self._print_recommendations()

    def xǁWrknvDoctorǁ_print_summary__mutmut_71(self, verbose: bool) -> None:
        """Print summary of all checks."""
        # Create summary table
        table = Table(title="Diagnostic Results", box=box.ROUNDED)
        table.add_column("Check", style="cyan")
        table.add_column("Status", style="bold")
        table.add_column("Details")

        # Add passed checks
        for check, details in self.checks_passed:
            table.add_row(check, "[green]✅ PASS[/green]", details)

        # Add warnings
        for check, details in self.checks_warning:
            table.add_row(check, "[yellow]⚠️  WARN[/yellow]", details)

        # Add failed checks
        for check, details in self.checks_failed:
            table.add_row(check, "[red]❌ FAIL[/red]", details)

        self.console.print(table)

        # Print summary statistics
        total = len(self.checks_passed) + len(self.checks_warning) + len(self.checks_failed)

        summary = Panel(
            f"[green]Passed: {len(self.checks_passed)}[/green] | "
            f"[yellow]Warnings: {len(self.checks_warning)}[/yellow] | "
            f"[red]Failed: {len(self.checks_failed)}[/red] | "
            f"Total: {total}",
            title="SUMMARY",
            box=box.DOUBLE,
        )
        self.console.print(summary)

        # Print recommendations if there are issues
        if self.checks_failed or self.checks_warning:
            self._print_recommendations()

    def xǁWrknvDoctorǁ_print_summary__mutmut_72(self, verbose: bool) -> None:
        """Print summary of all checks."""
        # Create summary table
        table = Table(title="Diagnostic Results", box=box.ROUNDED)
        table.add_column("Check", style="cyan")
        table.add_column("Status", style="bold")
        table.add_column("Details")

        # Add passed checks
        for check, details in self.checks_passed:
            table.add_row(check, "[green]✅ PASS[/green]", details)

        # Add warnings
        for check, details in self.checks_warning:
            table.add_row(check, "[yellow]⚠️  WARN[/yellow]", details)

        # Add failed checks
        for check, details in self.checks_failed:
            table.add_row(check, "[red]❌ FAIL[/red]", details)

        self.console.print(table)

        # Print summary statistics
        total = len(self.checks_passed) + len(self.checks_warning) + len(self.checks_failed)

        summary = Panel(
            f"[green]Passed: {len(self.checks_passed)}[/green] | "
            f"[yellow]Warnings: {len(self.checks_warning)}[/yellow] | "
            f"[red]Failed: {len(self.checks_failed)}[/red] | "
            f"Total: {total}",
            title="Summary",
            box=box.DOUBLE,
        )
        self.console.print(None)

        # Print recommendations if there are issues
        if self.checks_failed or self.checks_warning:
            self._print_recommendations()

    def xǁWrknvDoctorǁ_print_summary__mutmut_73(self, verbose: bool) -> None:
        """Print summary of all checks."""
        # Create summary table
        table = Table(title="Diagnostic Results", box=box.ROUNDED)
        table.add_column("Check", style="cyan")
        table.add_column("Status", style="bold")
        table.add_column("Details")

        # Add passed checks
        for check, details in self.checks_passed:
            table.add_row(check, "[green]✅ PASS[/green]", details)

        # Add warnings
        for check, details in self.checks_warning:
            table.add_row(check, "[yellow]⚠️  WARN[/yellow]", details)

        # Add failed checks
        for check, details in self.checks_failed:
            table.add_row(check, "[red]❌ FAIL[/red]", details)

        self.console.print(table)

        # Print summary statistics
        total = len(self.checks_passed) + len(self.checks_warning) + len(self.checks_failed)

        summary = Panel(
            f"[green]Passed: {len(self.checks_passed)}[/green] | "
            f"[yellow]Warnings: {len(self.checks_warning)}[/yellow] | "
            f"[red]Failed: {len(self.checks_failed)}[/red] | "
            f"Total: {total}",
            title="Summary",
            box=box.DOUBLE,
        )
        self.console.print(summary)

        # Print recommendations if there are issues
        if self.checks_failed and self.checks_warning:
            self._print_recommendations()
    
    xǁWrknvDoctorǁ_print_summary__mutmut_mutants : ClassVar[MutantDict] = {
    'xǁWrknvDoctorǁ_print_summary__mutmut_1': xǁWrknvDoctorǁ_print_summary__mutmut_1, 
        'xǁWrknvDoctorǁ_print_summary__mutmut_2': xǁWrknvDoctorǁ_print_summary__mutmut_2, 
        'xǁWrknvDoctorǁ_print_summary__mutmut_3': xǁWrknvDoctorǁ_print_summary__mutmut_3, 
        'xǁWrknvDoctorǁ_print_summary__mutmut_4': xǁWrknvDoctorǁ_print_summary__mutmut_4, 
        'xǁWrknvDoctorǁ_print_summary__mutmut_5': xǁWrknvDoctorǁ_print_summary__mutmut_5, 
        'xǁWrknvDoctorǁ_print_summary__mutmut_6': xǁWrknvDoctorǁ_print_summary__mutmut_6, 
        'xǁWrknvDoctorǁ_print_summary__mutmut_7': xǁWrknvDoctorǁ_print_summary__mutmut_7, 
        'xǁWrknvDoctorǁ_print_summary__mutmut_8': xǁWrknvDoctorǁ_print_summary__mutmut_8, 
        'xǁWrknvDoctorǁ_print_summary__mutmut_9': xǁWrknvDoctorǁ_print_summary__mutmut_9, 
        'xǁWrknvDoctorǁ_print_summary__mutmut_10': xǁWrknvDoctorǁ_print_summary__mutmut_10, 
        'xǁWrknvDoctorǁ_print_summary__mutmut_11': xǁWrknvDoctorǁ_print_summary__mutmut_11, 
        'xǁWrknvDoctorǁ_print_summary__mutmut_12': xǁWrknvDoctorǁ_print_summary__mutmut_12, 
        'xǁWrknvDoctorǁ_print_summary__mutmut_13': xǁWrknvDoctorǁ_print_summary__mutmut_13, 
        'xǁWrknvDoctorǁ_print_summary__mutmut_14': xǁWrknvDoctorǁ_print_summary__mutmut_14, 
        'xǁWrknvDoctorǁ_print_summary__mutmut_15': xǁWrknvDoctorǁ_print_summary__mutmut_15, 
        'xǁWrknvDoctorǁ_print_summary__mutmut_16': xǁWrknvDoctorǁ_print_summary__mutmut_16, 
        'xǁWrknvDoctorǁ_print_summary__mutmut_17': xǁWrknvDoctorǁ_print_summary__mutmut_17, 
        'xǁWrknvDoctorǁ_print_summary__mutmut_18': xǁWrknvDoctorǁ_print_summary__mutmut_18, 
        'xǁWrknvDoctorǁ_print_summary__mutmut_19': xǁWrknvDoctorǁ_print_summary__mutmut_19, 
        'xǁWrknvDoctorǁ_print_summary__mutmut_20': xǁWrknvDoctorǁ_print_summary__mutmut_20, 
        'xǁWrknvDoctorǁ_print_summary__mutmut_21': xǁWrknvDoctorǁ_print_summary__mutmut_21, 
        'xǁWrknvDoctorǁ_print_summary__mutmut_22': xǁWrknvDoctorǁ_print_summary__mutmut_22, 
        'xǁWrknvDoctorǁ_print_summary__mutmut_23': xǁWrknvDoctorǁ_print_summary__mutmut_23, 
        'xǁWrknvDoctorǁ_print_summary__mutmut_24': xǁWrknvDoctorǁ_print_summary__mutmut_24, 
        'xǁWrknvDoctorǁ_print_summary__mutmut_25': xǁWrknvDoctorǁ_print_summary__mutmut_25, 
        'xǁWrknvDoctorǁ_print_summary__mutmut_26': xǁWrknvDoctorǁ_print_summary__mutmut_26, 
        'xǁWrknvDoctorǁ_print_summary__mutmut_27': xǁWrknvDoctorǁ_print_summary__mutmut_27, 
        'xǁWrknvDoctorǁ_print_summary__mutmut_28': xǁWrknvDoctorǁ_print_summary__mutmut_28, 
        'xǁWrknvDoctorǁ_print_summary__mutmut_29': xǁWrknvDoctorǁ_print_summary__mutmut_29, 
        'xǁWrknvDoctorǁ_print_summary__mutmut_30': xǁWrknvDoctorǁ_print_summary__mutmut_30, 
        'xǁWrknvDoctorǁ_print_summary__mutmut_31': xǁWrknvDoctorǁ_print_summary__mutmut_31, 
        'xǁWrknvDoctorǁ_print_summary__mutmut_32': xǁWrknvDoctorǁ_print_summary__mutmut_32, 
        'xǁWrknvDoctorǁ_print_summary__mutmut_33': xǁWrknvDoctorǁ_print_summary__mutmut_33, 
        'xǁWrknvDoctorǁ_print_summary__mutmut_34': xǁWrknvDoctorǁ_print_summary__mutmut_34, 
        'xǁWrknvDoctorǁ_print_summary__mutmut_35': xǁWrknvDoctorǁ_print_summary__mutmut_35, 
        'xǁWrknvDoctorǁ_print_summary__mutmut_36': xǁWrknvDoctorǁ_print_summary__mutmut_36, 
        'xǁWrknvDoctorǁ_print_summary__mutmut_37': xǁWrknvDoctorǁ_print_summary__mutmut_37, 
        'xǁWrknvDoctorǁ_print_summary__mutmut_38': xǁWrknvDoctorǁ_print_summary__mutmut_38, 
        'xǁWrknvDoctorǁ_print_summary__mutmut_39': xǁWrknvDoctorǁ_print_summary__mutmut_39, 
        'xǁWrknvDoctorǁ_print_summary__mutmut_40': xǁWrknvDoctorǁ_print_summary__mutmut_40, 
        'xǁWrknvDoctorǁ_print_summary__mutmut_41': xǁWrknvDoctorǁ_print_summary__mutmut_41, 
        'xǁWrknvDoctorǁ_print_summary__mutmut_42': xǁWrknvDoctorǁ_print_summary__mutmut_42, 
        'xǁWrknvDoctorǁ_print_summary__mutmut_43': xǁWrknvDoctorǁ_print_summary__mutmut_43, 
        'xǁWrknvDoctorǁ_print_summary__mutmut_44': xǁWrknvDoctorǁ_print_summary__mutmut_44, 
        'xǁWrknvDoctorǁ_print_summary__mutmut_45': xǁWrknvDoctorǁ_print_summary__mutmut_45, 
        'xǁWrknvDoctorǁ_print_summary__mutmut_46': xǁWrknvDoctorǁ_print_summary__mutmut_46, 
        'xǁWrknvDoctorǁ_print_summary__mutmut_47': xǁWrknvDoctorǁ_print_summary__mutmut_47, 
        'xǁWrknvDoctorǁ_print_summary__mutmut_48': xǁWrknvDoctorǁ_print_summary__mutmut_48, 
        'xǁWrknvDoctorǁ_print_summary__mutmut_49': xǁWrknvDoctorǁ_print_summary__mutmut_49, 
        'xǁWrknvDoctorǁ_print_summary__mutmut_50': xǁWrknvDoctorǁ_print_summary__mutmut_50, 
        'xǁWrknvDoctorǁ_print_summary__mutmut_51': xǁWrknvDoctorǁ_print_summary__mutmut_51, 
        'xǁWrknvDoctorǁ_print_summary__mutmut_52': xǁWrknvDoctorǁ_print_summary__mutmut_52, 
        'xǁWrknvDoctorǁ_print_summary__mutmut_53': xǁWrknvDoctorǁ_print_summary__mutmut_53, 
        'xǁWrknvDoctorǁ_print_summary__mutmut_54': xǁWrknvDoctorǁ_print_summary__mutmut_54, 
        'xǁWrknvDoctorǁ_print_summary__mutmut_55': xǁWrknvDoctorǁ_print_summary__mutmut_55, 
        'xǁWrknvDoctorǁ_print_summary__mutmut_56': xǁWrknvDoctorǁ_print_summary__mutmut_56, 
        'xǁWrknvDoctorǁ_print_summary__mutmut_57': xǁWrknvDoctorǁ_print_summary__mutmut_57, 
        'xǁWrknvDoctorǁ_print_summary__mutmut_58': xǁWrknvDoctorǁ_print_summary__mutmut_58, 
        'xǁWrknvDoctorǁ_print_summary__mutmut_59': xǁWrknvDoctorǁ_print_summary__mutmut_59, 
        'xǁWrknvDoctorǁ_print_summary__mutmut_60': xǁWrknvDoctorǁ_print_summary__mutmut_60, 
        'xǁWrknvDoctorǁ_print_summary__mutmut_61': xǁWrknvDoctorǁ_print_summary__mutmut_61, 
        'xǁWrknvDoctorǁ_print_summary__mutmut_62': xǁWrknvDoctorǁ_print_summary__mutmut_62, 
        'xǁWrknvDoctorǁ_print_summary__mutmut_63': xǁWrknvDoctorǁ_print_summary__mutmut_63, 
        'xǁWrknvDoctorǁ_print_summary__mutmut_64': xǁWrknvDoctorǁ_print_summary__mutmut_64, 
        'xǁWrknvDoctorǁ_print_summary__mutmut_65': xǁWrknvDoctorǁ_print_summary__mutmut_65, 
        'xǁWrknvDoctorǁ_print_summary__mutmut_66': xǁWrknvDoctorǁ_print_summary__mutmut_66, 
        'xǁWrknvDoctorǁ_print_summary__mutmut_67': xǁWrknvDoctorǁ_print_summary__mutmut_67, 
        'xǁWrknvDoctorǁ_print_summary__mutmut_68': xǁWrknvDoctorǁ_print_summary__mutmut_68, 
        'xǁWrknvDoctorǁ_print_summary__mutmut_69': xǁWrknvDoctorǁ_print_summary__mutmut_69, 
        'xǁWrknvDoctorǁ_print_summary__mutmut_70': xǁWrknvDoctorǁ_print_summary__mutmut_70, 
        'xǁWrknvDoctorǁ_print_summary__mutmut_71': xǁWrknvDoctorǁ_print_summary__mutmut_71, 
        'xǁWrknvDoctorǁ_print_summary__mutmut_72': xǁWrknvDoctorǁ_print_summary__mutmut_72, 
        'xǁWrknvDoctorǁ_print_summary__mutmut_73': xǁWrknvDoctorǁ_print_summary__mutmut_73
    }
    
    def _print_summary(self, *args, **kwargs):
        result = _mutmut_trampoline(object.__getattribute__(self, "xǁWrknvDoctorǁ_print_summary__mutmut_orig"), object.__getattribute__(self, "xǁWrknvDoctorǁ_print_summary__mutmut_mutants"), args, kwargs, self)
        return result 
    
    _print_summary.__signature__ = _mutmut_signature(xǁWrknvDoctorǁ_print_summary__mutmut_orig)
    xǁWrknvDoctorǁ_print_summary__mutmut_orig.__name__ = 'xǁWrknvDoctorǁ_print_summary'

    def xǁWrknvDoctorǁ_print_recommendations__mutmut_orig(self) -> None:
        """Print recommendations based on failed checks."""
        self.console.print("\n[bold]Recommendations:[/bold]")

        recommendations = []

        # Priority 1: Fix critical failures
        has_env_issues = False
        for check, _details in self.checks_failed:
            if "env.sh" in check or "Workenv Path" in check:
                has_env_issues = True
            elif "uv" in check:
                recommendations.append(("🔧 Install uv", "curl -LsSf https://astral.sh/uv/install.sh | sh"))
            elif "wrknv Installation" in check:
                recommendations.append(("🔧 Reinstall wrknv", "See installation docs"))

        # If there are any env.sh issues, recommend regeneration
        if has_env_issues:
            recommendations.insert(0, ("🔄 Regenerate env.sh", "wrknv generate --force"))
            recommendations.append(("📝 After regenerating", "source env.sh"))

        # Priority 2: Address warnings
        for check, _details in self.checks_warning:
            if ".venv Directory" in check:
                recommendations.append(("🗑️  Remove .venv", "rm -rf .venv && source env.sh"))
            elif "workenv Directory" in check and not has_env_issues:
                recommendations.append(("🔧 Check workenv", "wrknv doctor"))
            elif "Virtual Environment" in check:
                recommendations.append(("🚪 Exit current venv", "deactivate && source env.sh"))

        # Remove duplicates and print
        seen = set()
        for title, command in recommendations:
            key = command
            if key not in seen:
                self.console.print(f"\n  {title}")
                self.console.print(f"  [dim]$ {command}[/dim]")
                seen.add(key)

        # If multiple env.sh failures, suggest bug report
        env_failure_count = sum(1 for check, _ in self.checks_failed if "env.sh" in check)
        if env_failure_count >= 3:
            self.console.print(
                "\n[yellow]⚠️  Multiple env.sh issues detected. "
                "If regenerating doesn't help, this may be a bug in wrknv.[/yellow]"
            )
            self.console.print("  [dim]Please report at: https://github.com/provide-io/wrknv/issues[/dim]")

    def xǁWrknvDoctorǁ_print_recommendations__mutmut_1(self) -> None:
        """Print recommendations based on failed checks."""
        self.console.print(None)

        recommendations = []

        # Priority 1: Fix critical failures
        has_env_issues = False
        for check, _details in self.checks_failed:
            if "env.sh" in check or "Workenv Path" in check:
                has_env_issues = True
            elif "uv" in check:
                recommendations.append(("🔧 Install uv", "curl -LsSf https://astral.sh/uv/install.sh | sh"))
            elif "wrknv Installation" in check:
                recommendations.append(("🔧 Reinstall wrknv", "See installation docs"))

        # If there are any env.sh issues, recommend regeneration
        if has_env_issues:
            recommendations.insert(0, ("🔄 Regenerate env.sh", "wrknv generate --force"))
            recommendations.append(("📝 After regenerating", "source env.sh"))

        # Priority 2: Address warnings
        for check, _details in self.checks_warning:
            if ".venv Directory" in check:
                recommendations.append(("🗑️  Remove .venv", "rm -rf .venv && source env.sh"))
            elif "workenv Directory" in check and not has_env_issues:
                recommendations.append(("🔧 Check workenv", "wrknv doctor"))
            elif "Virtual Environment" in check:
                recommendations.append(("🚪 Exit current venv", "deactivate && source env.sh"))

        # Remove duplicates and print
        seen = set()
        for title, command in recommendations:
            key = command
            if key not in seen:
                self.console.print(f"\n  {title}")
                self.console.print(f"  [dim]$ {command}[/dim]")
                seen.add(key)

        # If multiple env.sh failures, suggest bug report
        env_failure_count = sum(1 for check, _ in self.checks_failed if "env.sh" in check)
        if env_failure_count >= 3:
            self.console.print(
                "\n[yellow]⚠️  Multiple env.sh issues detected. "
                "If regenerating doesn't help, this may be a bug in wrknv.[/yellow]"
            )
            self.console.print("  [dim]Please report at: https://github.com/provide-io/wrknv/issues[/dim]")

    def xǁWrknvDoctorǁ_print_recommendations__mutmut_2(self) -> None:
        """Print recommendations based on failed checks."""
        self.console.print("XX\n[bold]Recommendations:[/bold]XX")

        recommendations = []

        # Priority 1: Fix critical failures
        has_env_issues = False
        for check, _details in self.checks_failed:
            if "env.sh" in check or "Workenv Path" in check:
                has_env_issues = True
            elif "uv" in check:
                recommendations.append(("🔧 Install uv", "curl -LsSf https://astral.sh/uv/install.sh | sh"))
            elif "wrknv Installation" in check:
                recommendations.append(("🔧 Reinstall wrknv", "See installation docs"))

        # If there are any env.sh issues, recommend regeneration
        if has_env_issues:
            recommendations.insert(0, ("🔄 Regenerate env.sh", "wrknv generate --force"))
            recommendations.append(("📝 After regenerating", "source env.sh"))

        # Priority 2: Address warnings
        for check, _details in self.checks_warning:
            if ".venv Directory" in check:
                recommendations.append(("🗑️  Remove .venv", "rm -rf .venv && source env.sh"))
            elif "workenv Directory" in check and not has_env_issues:
                recommendations.append(("🔧 Check workenv", "wrknv doctor"))
            elif "Virtual Environment" in check:
                recommendations.append(("🚪 Exit current venv", "deactivate && source env.sh"))

        # Remove duplicates and print
        seen = set()
        for title, command in recommendations:
            key = command
            if key not in seen:
                self.console.print(f"\n  {title}")
                self.console.print(f"  [dim]$ {command}[/dim]")
                seen.add(key)

        # If multiple env.sh failures, suggest bug report
        env_failure_count = sum(1 for check, _ in self.checks_failed if "env.sh" in check)
        if env_failure_count >= 3:
            self.console.print(
                "\n[yellow]⚠️  Multiple env.sh issues detected. "
                "If regenerating doesn't help, this may be a bug in wrknv.[/yellow]"
            )
            self.console.print("  [dim]Please report at: https://github.com/provide-io/wrknv/issues[/dim]")

    def xǁWrknvDoctorǁ_print_recommendations__mutmut_3(self) -> None:
        """Print recommendations based on failed checks."""
        self.console.print("\n[bold]recommendations:[/bold]")

        recommendations = []

        # Priority 1: Fix critical failures
        has_env_issues = False
        for check, _details in self.checks_failed:
            if "env.sh" in check or "Workenv Path" in check:
                has_env_issues = True
            elif "uv" in check:
                recommendations.append(("🔧 Install uv", "curl -LsSf https://astral.sh/uv/install.sh | sh"))
            elif "wrknv Installation" in check:
                recommendations.append(("🔧 Reinstall wrknv", "See installation docs"))

        # If there are any env.sh issues, recommend regeneration
        if has_env_issues:
            recommendations.insert(0, ("🔄 Regenerate env.sh", "wrknv generate --force"))
            recommendations.append(("📝 After regenerating", "source env.sh"))

        # Priority 2: Address warnings
        for check, _details in self.checks_warning:
            if ".venv Directory" in check:
                recommendations.append(("🗑️  Remove .venv", "rm -rf .venv && source env.sh"))
            elif "workenv Directory" in check and not has_env_issues:
                recommendations.append(("🔧 Check workenv", "wrknv doctor"))
            elif "Virtual Environment" in check:
                recommendations.append(("🚪 Exit current venv", "deactivate && source env.sh"))

        # Remove duplicates and print
        seen = set()
        for title, command in recommendations:
            key = command
            if key not in seen:
                self.console.print(f"\n  {title}")
                self.console.print(f"  [dim]$ {command}[/dim]")
                seen.add(key)

        # If multiple env.sh failures, suggest bug report
        env_failure_count = sum(1 for check, _ in self.checks_failed if "env.sh" in check)
        if env_failure_count >= 3:
            self.console.print(
                "\n[yellow]⚠️  Multiple env.sh issues detected. "
                "If regenerating doesn't help, this may be a bug in wrknv.[/yellow]"
            )
            self.console.print("  [dim]Please report at: https://github.com/provide-io/wrknv/issues[/dim]")

    def xǁWrknvDoctorǁ_print_recommendations__mutmut_4(self) -> None:
        """Print recommendations based on failed checks."""
        self.console.print("\n[BOLD]RECOMMENDATIONS:[/BOLD]")

        recommendations = []

        # Priority 1: Fix critical failures
        has_env_issues = False
        for check, _details in self.checks_failed:
            if "env.sh" in check or "Workenv Path" in check:
                has_env_issues = True
            elif "uv" in check:
                recommendations.append(("🔧 Install uv", "curl -LsSf https://astral.sh/uv/install.sh | sh"))
            elif "wrknv Installation" in check:
                recommendations.append(("🔧 Reinstall wrknv", "See installation docs"))

        # If there are any env.sh issues, recommend regeneration
        if has_env_issues:
            recommendations.insert(0, ("🔄 Regenerate env.sh", "wrknv generate --force"))
            recommendations.append(("📝 After regenerating", "source env.sh"))

        # Priority 2: Address warnings
        for check, _details in self.checks_warning:
            if ".venv Directory" in check:
                recommendations.append(("🗑️  Remove .venv", "rm -rf .venv && source env.sh"))
            elif "workenv Directory" in check and not has_env_issues:
                recommendations.append(("🔧 Check workenv", "wrknv doctor"))
            elif "Virtual Environment" in check:
                recommendations.append(("🚪 Exit current venv", "deactivate && source env.sh"))

        # Remove duplicates and print
        seen = set()
        for title, command in recommendations:
            key = command
            if key not in seen:
                self.console.print(f"\n  {title}")
                self.console.print(f"  [dim]$ {command}[/dim]")
                seen.add(key)

        # If multiple env.sh failures, suggest bug report
        env_failure_count = sum(1 for check, _ in self.checks_failed if "env.sh" in check)
        if env_failure_count >= 3:
            self.console.print(
                "\n[yellow]⚠️  Multiple env.sh issues detected. "
                "If regenerating doesn't help, this may be a bug in wrknv.[/yellow]"
            )
            self.console.print("  [dim]Please report at: https://github.com/provide-io/wrknv/issues[/dim]")

    def xǁWrknvDoctorǁ_print_recommendations__mutmut_5(self) -> None:
        """Print recommendations based on failed checks."""
        self.console.print("\n[bold]Recommendations:[/bold]")

        recommendations = None

        # Priority 1: Fix critical failures
        has_env_issues = False
        for check, _details in self.checks_failed:
            if "env.sh" in check or "Workenv Path" in check:
                has_env_issues = True
            elif "uv" in check:
                recommendations.append(("🔧 Install uv", "curl -LsSf https://astral.sh/uv/install.sh | sh"))
            elif "wrknv Installation" in check:
                recommendations.append(("🔧 Reinstall wrknv", "See installation docs"))

        # If there are any env.sh issues, recommend regeneration
        if has_env_issues:
            recommendations.insert(0, ("🔄 Regenerate env.sh", "wrknv generate --force"))
            recommendations.append(("📝 After regenerating", "source env.sh"))

        # Priority 2: Address warnings
        for check, _details in self.checks_warning:
            if ".venv Directory" in check:
                recommendations.append(("🗑️  Remove .venv", "rm -rf .venv && source env.sh"))
            elif "workenv Directory" in check and not has_env_issues:
                recommendations.append(("🔧 Check workenv", "wrknv doctor"))
            elif "Virtual Environment" in check:
                recommendations.append(("🚪 Exit current venv", "deactivate && source env.sh"))

        # Remove duplicates and print
        seen = set()
        for title, command in recommendations:
            key = command
            if key not in seen:
                self.console.print(f"\n  {title}")
                self.console.print(f"  [dim]$ {command}[/dim]")
                seen.add(key)

        # If multiple env.sh failures, suggest bug report
        env_failure_count = sum(1 for check, _ in self.checks_failed if "env.sh" in check)
        if env_failure_count >= 3:
            self.console.print(
                "\n[yellow]⚠️  Multiple env.sh issues detected. "
                "If regenerating doesn't help, this may be a bug in wrknv.[/yellow]"
            )
            self.console.print("  [dim]Please report at: https://github.com/provide-io/wrknv/issues[/dim]")

    def xǁWrknvDoctorǁ_print_recommendations__mutmut_6(self) -> None:
        """Print recommendations based on failed checks."""
        self.console.print("\n[bold]Recommendations:[/bold]")

        recommendations = []

        # Priority 1: Fix critical failures
        has_env_issues = None
        for check, _details in self.checks_failed:
            if "env.sh" in check or "Workenv Path" in check:
                has_env_issues = True
            elif "uv" in check:
                recommendations.append(("🔧 Install uv", "curl -LsSf https://astral.sh/uv/install.sh | sh"))
            elif "wrknv Installation" in check:
                recommendations.append(("🔧 Reinstall wrknv", "See installation docs"))

        # If there are any env.sh issues, recommend regeneration
        if has_env_issues:
            recommendations.insert(0, ("🔄 Regenerate env.sh", "wrknv generate --force"))
            recommendations.append(("📝 After regenerating", "source env.sh"))

        # Priority 2: Address warnings
        for check, _details in self.checks_warning:
            if ".venv Directory" in check:
                recommendations.append(("🗑️  Remove .venv", "rm -rf .venv && source env.sh"))
            elif "workenv Directory" in check and not has_env_issues:
                recommendations.append(("🔧 Check workenv", "wrknv doctor"))
            elif "Virtual Environment" in check:
                recommendations.append(("🚪 Exit current venv", "deactivate && source env.sh"))

        # Remove duplicates and print
        seen = set()
        for title, command in recommendations:
            key = command
            if key not in seen:
                self.console.print(f"\n  {title}")
                self.console.print(f"  [dim]$ {command}[/dim]")
                seen.add(key)

        # If multiple env.sh failures, suggest bug report
        env_failure_count = sum(1 for check, _ in self.checks_failed if "env.sh" in check)
        if env_failure_count >= 3:
            self.console.print(
                "\n[yellow]⚠️  Multiple env.sh issues detected. "
                "If regenerating doesn't help, this may be a bug in wrknv.[/yellow]"
            )
            self.console.print("  [dim]Please report at: https://github.com/provide-io/wrknv/issues[/dim]")

    def xǁWrknvDoctorǁ_print_recommendations__mutmut_7(self) -> None:
        """Print recommendations based on failed checks."""
        self.console.print("\n[bold]Recommendations:[/bold]")

        recommendations = []

        # Priority 1: Fix critical failures
        has_env_issues = True
        for check, _details in self.checks_failed:
            if "env.sh" in check or "Workenv Path" in check:
                has_env_issues = True
            elif "uv" in check:
                recommendations.append(("🔧 Install uv", "curl -LsSf https://astral.sh/uv/install.sh | sh"))
            elif "wrknv Installation" in check:
                recommendations.append(("🔧 Reinstall wrknv", "See installation docs"))

        # If there are any env.sh issues, recommend regeneration
        if has_env_issues:
            recommendations.insert(0, ("🔄 Regenerate env.sh", "wrknv generate --force"))
            recommendations.append(("📝 After regenerating", "source env.sh"))

        # Priority 2: Address warnings
        for check, _details in self.checks_warning:
            if ".venv Directory" in check:
                recommendations.append(("🗑️  Remove .venv", "rm -rf .venv && source env.sh"))
            elif "workenv Directory" in check and not has_env_issues:
                recommendations.append(("🔧 Check workenv", "wrknv doctor"))
            elif "Virtual Environment" in check:
                recommendations.append(("🚪 Exit current venv", "deactivate && source env.sh"))

        # Remove duplicates and print
        seen = set()
        for title, command in recommendations:
            key = command
            if key not in seen:
                self.console.print(f"\n  {title}")
                self.console.print(f"  [dim]$ {command}[/dim]")
                seen.add(key)

        # If multiple env.sh failures, suggest bug report
        env_failure_count = sum(1 for check, _ in self.checks_failed if "env.sh" in check)
        if env_failure_count >= 3:
            self.console.print(
                "\n[yellow]⚠️  Multiple env.sh issues detected. "
                "If regenerating doesn't help, this may be a bug in wrknv.[/yellow]"
            )
            self.console.print("  [dim]Please report at: https://github.com/provide-io/wrknv/issues[/dim]")

    def xǁWrknvDoctorǁ_print_recommendations__mutmut_8(self) -> None:
        """Print recommendations based on failed checks."""
        self.console.print("\n[bold]Recommendations:[/bold]")

        recommendations = []

        # Priority 1: Fix critical failures
        has_env_issues = False
        for check, _details in self.checks_failed:
            if "env.sh" in check and "Workenv Path" in check:
                has_env_issues = True
            elif "uv" in check:
                recommendations.append(("🔧 Install uv", "curl -LsSf https://astral.sh/uv/install.sh | sh"))
            elif "wrknv Installation" in check:
                recommendations.append(("🔧 Reinstall wrknv", "See installation docs"))

        # If there are any env.sh issues, recommend regeneration
        if has_env_issues:
            recommendations.insert(0, ("🔄 Regenerate env.sh", "wrknv generate --force"))
            recommendations.append(("📝 After regenerating", "source env.sh"))

        # Priority 2: Address warnings
        for check, _details in self.checks_warning:
            if ".venv Directory" in check:
                recommendations.append(("🗑️  Remove .venv", "rm -rf .venv && source env.sh"))
            elif "workenv Directory" in check and not has_env_issues:
                recommendations.append(("🔧 Check workenv", "wrknv doctor"))
            elif "Virtual Environment" in check:
                recommendations.append(("🚪 Exit current venv", "deactivate && source env.sh"))

        # Remove duplicates and print
        seen = set()
        for title, command in recommendations:
            key = command
            if key not in seen:
                self.console.print(f"\n  {title}")
                self.console.print(f"  [dim]$ {command}[/dim]")
                seen.add(key)

        # If multiple env.sh failures, suggest bug report
        env_failure_count = sum(1 for check, _ in self.checks_failed if "env.sh" in check)
        if env_failure_count >= 3:
            self.console.print(
                "\n[yellow]⚠️  Multiple env.sh issues detected. "
                "If regenerating doesn't help, this may be a bug in wrknv.[/yellow]"
            )
            self.console.print("  [dim]Please report at: https://github.com/provide-io/wrknv/issues[/dim]")

    def xǁWrknvDoctorǁ_print_recommendations__mutmut_9(self) -> None:
        """Print recommendations based on failed checks."""
        self.console.print("\n[bold]Recommendations:[/bold]")

        recommendations = []

        # Priority 1: Fix critical failures
        has_env_issues = False
        for check, _details in self.checks_failed:
            if "XXenv.shXX" in check or "Workenv Path" in check:
                has_env_issues = True
            elif "uv" in check:
                recommendations.append(("🔧 Install uv", "curl -LsSf https://astral.sh/uv/install.sh | sh"))
            elif "wrknv Installation" in check:
                recommendations.append(("🔧 Reinstall wrknv", "See installation docs"))

        # If there are any env.sh issues, recommend regeneration
        if has_env_issues:
            recommendations.insert(0, ("🔄 Regenerate env.sh", "wrknv generate --force"))
            recommendations.append(("📝 After regenerating", "source env.sh"))

        # Priority 2: Address warnings
        for check, _details in self.checks_warning:
            if ".venv Directory" in check:
                recommendations.append(("🗑️  Remove .venv", "rm -rf .venv && source env.sh"))
            elif "workenv Directory" in check and not has_env_issues:
                recommendations.append(("🔧 Check workenv", "wrknv doctor"))
            elif "Virtual Environment" in check:
                recommendations.append(("🚪 Exit current venv", "deactivate && source env.sh"))

        # Remove duplicates and print
        seen = set()
        for title, command in recommendations:
            key = command
            if key not in seen:
                self.console.print(f"\n  {title}")
                self.console.print(f"  [dim]$ {command}[/dim]")
                seen.add(key)

        # If multiple env.sh failures, suggest bug report
        env_failure_count = sum(1 for check, _ in self.checks_failed if "env.sh" in check)
        if env_failure_count >= 3:
            self.console.print(
                "\n[yellow]⚠️  Multiple env.sh issues detected. "
                "If regenerating doesn't help, this may be a bug in wrknv.[/yellow]"
            )
            self.console.print("  [dim]Please report at: https://github.com/provide-io/wrknv/issues[/dim]")

    def xǁWrknvDoctorǁ_print_recommendations__mutmut_10(self) -> None:
        """Print recommendations based on failed checks."""
        self.console.print("\n[bold]Recommendations:[/bold]")

        recommendations = []

        # Priority 1: Fix critical failures
        has_env_issues = False
        for check, _details in self.checks_failed:
            if "ENV.SH" in check or "Workenv Path" in check:
                has_env_issues = True
            elif "uv" in check:
                recommendations.append(("🔧 Install uv", "curl -LsSf https://astral.sh/uv/install.sh | sh"))
            elif "wrknv Installation" in check:
                recommendations.append(("🔧 Reinstall wrknv", "See installation docs"))

        # If there are any env.sh issues, recommend regeneration
        if has_env_issues:
            recommendations.insert(0, ("🔄 Regenerate env.sh", "wrknv generate --force"))
            recommendations.append(("📝 After regenerating", "source env.sh"))

        # Priority 2: Address warnings
        for check, _details in self.checks_warning:
            if ".venv Directory" in check:
                recommendations.append(("🗑️  Remove .venv", "rm -rf .venv && source env.sh"))
            elif "workenv Directory" in check and not has_env_issues:
                recommendations.append(("🔧 Check workenv", "wrknv doctor"))
            elif "Virtual Environment" in check:
                recommendations.append(("🚪 Exit current venv", "deactivate && source env.sh"))

        # Remove duplicates and print
        seen = set()
        for title, command in recommendations:
            key = command
            if key not in seen:
                self.console.print(f"\n  {title}")
                self.console.print(f"  [dim]$ {command}[/dim]")
                seen.add(key)

        # If multiple env.sh failures, suggest bug report
        env_failure_count = sum(1 for check, _ in self.checks_failed if "env.sh" in check)
        if env_failure_count >= 3:
            self.console.print(
                "\n[yellow]⚠️  Multiple env.sh issues detected. "
                "If regenerating doesn't help, this may be a bug in wrknv.[/yellow]"
            )
            self.console.print("  [dim]Please report at: https://github.com/provide-io/wrknv/issues[/dim]")

    def xǁWrknvDoctorǁ_print_recommendations__mutmut_11(self) -> None:
        """Print recommendations based on failed checks."""
        self.console.print("\n[bold]Recommendations:[/bold]")

        recommendations = []

        # Priority 1: Fix critical failures
        has_env_issues = False
        for check, _details in self.checks_failed:
            if "env.sh" not in check or "Workenv Path" in check:
                has_env_issues = True
            elif "uv" in check:
                recommendations.append(("🔧 Install uv", "curl -LsSf https://astral.sh/uv/install.sh | sh"))
            elif "wrknv Installation" in check:
                recommendations.append(("🔧 Reinstall wrknv", "See installation docs"))

        # If there are any env.sh issues, recommend regeneration
        if has_env_issues:
            recommendations.insert(0, ("🔄 Regenerate env.sh", "wrknv generate --force"))
            recommendations.append(("📝 After regenerating", "source env.sh"))

        # Priority 2: Address warnings
        for check, _details in self.checks_warning:
            if ".venv Directory" in check:
                recommendations.append(("🗑️  Remove .venv", "rm -rf .venv && source env.sh"))
            elif "workenv Directory" in check and not has_env_issues:
                recommendations.append(("🔧 Check workenv", "wrknv doctor"))
            elif "Virtual Environment" in check:
                recommendations.append(("🚪 Exit current venv", "deactivate && source env.sh"))

        # Remove duplicates and print
        seen = set()
        for title, command in recommendations:
            key = command
            if key not in seen:
                self.console.print(f"\n  {title}")
                self.console.print(f"  [dim]$ {command}[/dim]")
                seen.add(key)

        # If multiple env.sh failures, suggest bug report
        env_failure_count = sum(1 for check, _ in self.checks_failed if "env.sh" in check)
        if env_failure_count >= 3:
            self.console.print(
                "\n[yellow]⚠️  Multiple env.sh issues detected. "
                "If regenerating doesn't help, this may be a bug in wrknv.[/yellow]"
            )
            self.console.print("  [dim]Please report at: https://github.com/provide-io/wrknv/issues[/dim]")

    def xǁWrknvDoctorǁ_print_recommendations__mutmut_12(self) -> None:
        """Print recommendations based on failed checks."""
        self.console.print("\n[bold]Recommendations:[/bold]")

        recommendations = []

        # Priority 1: Fix critical failures
        has_env_issues = False
        for check, _details in self.checks_failed:
            if "env.sh" in check or "XXWorkenv PathXX" in check:
                has_env_issues = True
            elif "uv" in check:
                recommendations.append(("🔧 Install uv", "curl -LsSf https://astral.sh/uv/install.sh | sh"))
            elif "wrknv Installation" in check:
                recommendations.append(("🔧 Reinstall wrknv", "See installation docs"))

        # If there are any env.sh issues, recommend regeneration
        if has_env_issues:
            recommendations.insert(0, ("🔄 Regenerate env.sh", "wrknv generate --force"))
            recommendations.append(("📝 After regenerating", "source env.sh"))

        # Priority 2: Address warnings
        for check, _details in self.checks_warning:
            if ".venv Directory" in check:
                recommendations.append(("🗑️  Remove .venv", "rm -rf .venv && source env.sh"))
            elif "workenv Directory" in check and not has_env_issues:
                recommendations.append(("🔧 Check workenv", "wrknv doctor"))
            elif "Virtual Environment" in check:
                recommendations.append(("🚪 Exit current venv", "deactivate && source env.sh"))

        # Remove duplicates and print
        seen = set()
        for title, command in recommendations:
            key = command
            if key not in seen:
                self.console.print(f"\n  {title}")
                self.console.print(f"  [dim]$ {command}[/dim]")
                seen.add(key)

        # If multiple env.sh failures, suggest bug report
        env_failure_count = sum(1 for check, _ in self.checks_failed if "env.sh" in check)
        if env_failure_count >= 3:
            self.console.print(
                "\n[yellow]⚠️  Multiple env.sh issues detected. "
                "If regenerating doesn't help, this may be a bug in wrknv.[/yellow]"
            )
            self.console.print("  [dim]Please report at: https://github.com/provide-io/wrknv/issues[/dim]")

    def xǁWrknvDoctorǁ_print_recommendations__mutmut_13(self) -> None:
        """Print recommendations based on failed checks."""
        self.console.print("\n[bold]Recommendations:[/bold]")

        recommendations = []

        # Priority 1: Fix critical failures
        has_env_issues = False
        for check, _details in self.checks_failed:
            if "env.sh" in check or "workenv path" in check:
                has_env_issues = True
            elif "uv" in check:
                recommendations.append(("🔧 Install uv", "curl -LsSf https://astral.sh/uv/install.sh | sh"))
            elif "wrknv Installation" in check:
                recommendations.append(("🔧 Reinstall wrknv", "See installation docs"))

        # If there are any env.sh issues, recommend regeneration
        if has_env_issues:
            recommendations.insert(0, ("🔄 Regenerate env.sh", "wrknv generate --force"))
            recommendations.append(("📝 After regenerating", "source env.sh"))

        # Priority 2: Address warnings
        for check, _details in self.checks_warning:
            if ".venv Directory" in check:
                recommendations.append(("🗑️  Remove .venv", "rm -rf .venv && source env.sh"))
            elif "workenv Directory" in check and not has_env_issues:
                recommendations.append(("🔧 Check workenv", "wrknv doctor"))
            elif "Virtual Environment" in check:
                recommendations.append(("🚪 Exit current venv", "deactivate && source env.sh"))

        # Remove duplicates and print
        seen = set()
        for title, command in recommendations:
            key = command
            if key not in seen:
                self.console.print(f"\n  {title}")
                self.console.print(f"  [dim]$ {command}[/dim]")
                seen.add(key)

        # If multiple env.sh failures, suggest bug report
        env_failure_count = sum(1 for check, _ in self.checks_failed if "env.sh" in check)
        if env_failure_count >= 3:
            self.console.print(
                "\n[yellow]⚠️  Multiple env.sh issues detected. "
                "If regenerating doesn't help, this may be a bug in wrknv.[/yellow]"
            )
            self.console.print("  [dim]Please report at: https://github.com/provide-io/wrknv/issues[/dim]")

    def xǁWrknvDoctorǁ_print_recommendations__mutmut_14(self) -> None:
        """Print recommendations based on failed checks."""
        self.console.print("\n[bold]Recommendations:[/bold]")

        recommendations = []

        # Priority 1: Fix critical failures
        has_env_issues = False
        for check, _details in self.checks_failed:
            if "env.sh" in check or "WORKENV PATH" in check:
                has_env_issues = True
            elif "uv" in check:
                recommendations.append(("🔧 Install uv", "curl -LsSf https://astral.sh/uv/install.sh | sh"))
            elif "wrknv Installation" in check:
                recommendations.append(("🔧 Reinstall wrknv", "See installation docs"))

        # If there are any env.sh issues, recommend regeneration
        if has_env_issues:
            recommendations.insert(0, ("🔄 Regenerate env.sh", "wrknv generate --force"))
            recommendations.append(("📝 After regenerating", "source env.sh"))

        # Priority 2: Address warnings
        for check, _details in self.checks_warning:
            if ".venv Directory" in check:
                recommendations.append(("🗑️  Remove .venv", "rm -rf .venv && source env.sh"))
            elif "workenv Directory" in check and not has_env_issues:
                recommendations.append(("🔧 Check workenv", "wrknv doctor"))
            elif "Virtual Environment" in check:
                recommendations.append(("🚪 Exit current venv", "deactivate && source env.sh"))

        # Remove duplicates and print
        seen = set()
        for title, command in recommendations:
            key = command
            if key not in seen:
                self.console.print(f"\n  {title}")
                self.console.print(f"  [dim]$ {command}[/dim]")
                seen.add(key)

        # If multiple env.sh failures, suggest bug report
        env_failure_count = sum(1 for check, _ in self.checks_failed if "env.sh" in check)
        if env_failure_count >= 3:
            self.console.print(
                "\n[yellow]⚠️  Multiple env.sh issues detected. "
                "If regenerating doesn't help, this may be a bug in wrknv.[/yellow]"
            )
            self.console.print("  [dim]Please report at: https://github.com/provide-io/wrknv/issues[/dim]")

    def xǁWrknvDoctorǁ_print_recommendations__mutmut_15(self) -> None:
        """Print recommendations based on failed checks."""
        self.console.print("\n[bold]Recommendations:[/bold]")

        recommendations = []

        # Priority 1: Fix critical failures
        has_env_issues = False
        for check, _details in self.checks_failed:
            if "env.sh" in check or "Workenv Path" not in check:
                has_env_issues = True
            elif "uv" in check:
                recommendations.append(("🔧 Install uv", "curl -LsSf https://astral.sh/uv/install.sh | sh"))
            elif "wrknv Installation" in check:
                recommendations.append(("🔧 Reinstall wrknv", "See installation docs"))

        # If there are any env.sh issues, recommend regeneration
        if has_env_issues:
            recommendations.insert(0, ("🔄 Regenerate env.sh", "wrknv generate --force"))
            recommendations.append(("📝 After regenerating", "source env.sh"))

        # Priority 2: Address warnings
        for check, _details in self.checks_warning:
            if ".venv Directory" in check:
                recommendations.append(("🗑️  Remove .venv", "rm -rf .venv && source env.sh"))
            elif "workenv Directory" in check and not has_env_issues:
                recommendations.append(("🔧 Check workenv", "wrknv doctor"))
            elif "Virtual Environment" in check:
                recommendations.append(("🚪 Exit current venv", "deactivate && source env.sh"))

        # Remove duplicates and print
        seen = set()
        for title, command in recommendations:
            key = command
            if key not in seen:
                self.console.print(f"\n  {title}")
                self.console.print(f"  [dim]$ {command}[/dim]")
                seen.add(key)

        # If multiple env.sh failures, suggest bug report
        env_failure_count = sum(1 for check, _ in self.checks_failed if "env.sh" in check)
        if env_failure_count >= 3:
            self.console.print(
                "\n[yellow]⚠️  Multiple env.sh issues detected. "
                "If regenerating doesn't help, this may be a bug in wrknv.[/yellow]"
            )
            self.console.print("  [dim]Please report at: https://github.com/provide-io/wrknv/issues[/dim]")

    def xǁWrknvDoctorǁ_print_recommendations__mutmut_16(self) -> None:
        """Print recommendations based on failed checks."""
        self.console.print("\n[bold]Recommendations:[/bold]")

        recommendations = []

        # Priority 1: Fix critical failures
        has_env_issues = False
        for check, _details in self.checks_failed:
            if "env.sh" in check or "Workenv Path" in check:
                has_env_issues = None
            elif "uv" in check:
                recommendations.append(("🔧 Install uv", "curl -LsSf https://astral.sh/uv/install.sh | sh"))
            elif "wrknv Installation" in check:
                recommendations.append(("🔧 Reinstall wrknv", "See installation docs"))

        # If there are any env.sh issues, recommend regeneration
        if has_env_issues:
            recommendations.insert(0, ("🔄 Regenerate env.sh", "wrknv generate --force"))
            recommendations.append(("📝 After regenerating", "source env.sh"))

        # Priority 2: Address warnings
        for check, _details in self.checks_warning:
            if ".venv Directory" in check:
                recommendations.append(("🗑️  Remove .venv", "rm -rf .venv && source env.sh"))
            elif "workenv Directory" in check and not has_env_issues:
                recommendations.append(("🔧 Check workenv", "wrknv doctor"))
            elif "Virtual Environment" in check:
                recommendations.append(("🚪 Exit current venv", "deactivate && source env.sh"))

        # Remove duplicates and print
        seen = set()
        for title, command in recommendations:
            key = command
            if key not in seen:
                self.console.print(f"\n  {title}")
                self.console.print(f"  [dim]$ {command}[/dim]")
                seen.add(key)

        # If multiple env.sh failures, suggest bug report
        env_failure_count = sum(1 for check, _ in self.checks_failed if "env.sh" in check)
        if env_failure_count >= 3:
            self.console.print(
                "\n[yellow]⚠️  Multiple env.sh issues detected. "
                "If regenerating doesn't help, this may be a bug in wrknv.[/yellow]"
            )
            self.console.print("  [dim]Please report at: https://github.com/provide-io/wrknv/issues[/dim]")

    def xǁWrknvDoctorǁ_print_recommendations__mutmut_17(self) -> None:
        """Print recommendations based on failed checks."""
        self.console.print("\n[bold]Recommendations:[/bold]")

        recommendations = []

        # Priority 1: Fix critical failures
        has_env_issues = False
        for check, _details in self.checks_failed:
            if "env.sh" in check or "Workenv Path" in check:
                has_env_issues = False
            elif "uv" in check:
                recommendations.append(("🔧 Install uv", "curl -LsSf https://astral.sh/uv/install.sh | sh"))
            elif "wrknv Installation" in check:
                recommendations.append(("🔧 Reinstall wrknv", "See installation docs"))

        # If there are any env.sh issues, recommend regeneration
        if has_env_issues:
            recommendations.insert(0, ("🔄 Regenerate env.sh", "wrknv generate --force"))
            recommendations.append(("📝 After regenerating", "source env.sh"))

        # Priority 2: Address warnings
        for check, _details in self.checks_warning:
            if ".venv Directory" in check:
                recommendations.append(("🗑️  Remove .venv", "rm -rf .venv && source env.sh"))
            elif "workenv Directory" in check and not has_env_issues:
                recommendations.append(("🔧 Check workenv", "wrknv doctor"))
            elif "Virtual Environment" in check:
                recommendations.append(("🚪 Exit current venv", "deactivate && source env.sh"))

        # Remove duplicates and print
        seen = set()
        for title, command in recommendations:
            key = command
            if key not in seen:
                self.console.print(f"\n  {title}")
                self.console.print(f"  [dim]$ {command}[/dim]")
                seen.add(key)

        # If multiple env.sh failures, suggest bug report
        env_failure_count = sum(1 for check, _ in self.checks_failed if "env.sh" in check)
        if env_failure_count >= 3:
            self.console.print(
                "\n[yellow]⚠️  Multiple env.sh issues detected. "
                "If regenerating doesn't help, this may be a bug in wrknv.[/yellow]"
            )
            self.console.print("  [dim]Please report at: https://github.com/provide-io/wrknv/issues[/dim]")

    def xǁWrknvDoctorǁ_print_recommendations__mutmut_18(self) -> None:
        """Print recommendations based on failed checks."""
        self.console.print("\n[bold]Recommendations:[/bold]")

        recommendations = []

        # Priority 1: Fix critical failures
        has_env_issues = False
        for check, _details in self.checks_failed:
            if "env.sh" in check or "Workenv Path" in check:
                has_env_issues = True
            elif "XXuvXX" in check:
                recommendations.append(("🔧 Install uv", "curl -LsSf https://astral.sh/uv/install.sh | sh"))
            elif "wrknv Installation" in check:
                recommendations.append(("🔧 Reinstall wrknv", "See installation docs"))

        # If there are any env.sh issues, recommend regeneration
        if has_env_issues:
            recommendations.insert(0, ("🔄 Regenerate env.sh", "wrknv generate --force"))
            recommendations.append(("📝 After regenerating", "source env.sh"))

        # Priority 2: Address warnings
        for check, _details in self.checks_warning:
            if ".venv Directory" in check:
                recommendations.append(("🗑️  Remove .venv", "rm -rf .venv && source env.sh"))
            elif "workenv Directory" in check and not has_env_issues:
                recommendations.append(("🔧 Check workenv", "wrknv doctor"))
            elif "Virtual Environment" in check:
                recommendations.append(("🚪 Exit current venv", "deactivate && source env.sh"))

        # Remove duplicates and print
        seen = set()
        for title, command in recommendations:
            key = command
            if key not in seen:
                self.console.print(f"\n  {title}")
                self.console.print(f"  [dim]$ {command}[/dim]")
                seen.add(key)

        # If multiple env.sh failures, suggest bug report
        env_failure_count = sum(1 for check, _ in self.checks_failed if "env.sh" in check)
        if env_failure_count >= 3:
            self.console.print(
                "\n[yellow]⚠️  Multiple env.sh issues detected. "
                "If regenerating doesn't help, this may be a bug in wrknv.[/yellow]"
            )
            self.console.print("  [dim]Please report at: https://github.com/provide-io/wrknv/issues[/dim]")

    def xǁWrknvDoctorǁ_print_recommendations__mutmut_19(self) -> None:
        """Print recommendations based on failed checks."""
        self.console.print("\n[bold]Recommendations:[/bold]")

        recommendations = []

        # Priority 1: Fix critical failures
        has_env_issues = False
        for check, _details in self.checks_failed:
            if "env.sh" in check or "Workenv Path" in check:
                has_env_issues = True
            elif "UV" in check:
                recommendations.append(("🔧 Install uv", "curl -LsSf https://astral.sh/uv/install.sh | sh"))
            elif "wrknv Installation" in check:
                recommendations.append(("🔧 Reinstall wrknv", "See installation docs"))

        # If there are any env.sh issues, recommend regeneration
        if has_env_issues:
            recommendations.insert(0, ("🔄 Regenerate env.sh", "wrknv generate --force"))
            recommendations.append(("📝 After regenerating", "source env.sh"))

        # Priority 2: Address warnings
        for check, _details in self.checks_warning:
            if ".venv Directory" in check:
                recommendations.append(("🗑️  Remove .venv", "rm -rf .venv && source env.sh"))
            elif "workenv Directory" in check and not has_env_issues:
                recommendations.append(("🔧 Check workenv", "wrknv doctor"))
            elif "Virtual Environment" in check:
                recommendations.append(("🚪 Exit current venv", "deactivate && source env.sh"))

        # Remove duplicates and print
        seen = set()
        for title, command in recommendations:
            key = command
            if key not in seen:
                self.console.print(f"\n  {title}")
                self.console.print(f"  [dim]$ {command}[/dim]")
                seen.add(key)

        # If multiple env.sh failures, suggest bug report
        env_failure_count = sum(1 for check, _ in self.checks_failed if "env.sh" in check)
        if env_failure_count >= 3:
            self.console.print(
                "\n[yellow]⚠️  Multiple env.sh issues detected. "
                "If regenerating doesn't help, this may be a bug in wrknv.[/yellow]"
            )
            self.console.print("  [dim]Please report at: https://github.com/provide-io/wrknv/issues[/dim]")

    def xǁWrknvDoctorǁ_print_recommendations__mutmut_20(self) -> None:
        """Print recommendations based on failed checks."""
        self.console.print("\n[bold]Recommendations:[/bold]")

        recommendations = []

        # Priority 1: Fix critical failures
        has_env_issues = False
        for check, _details in self.checks_failed:
            if "env.sh" in check or "Workenv Path" in check:
                has_env_issues = True
            elif "uv" not in check:
                recommendations.append(("🔧 Install uv", "curl -LsSf https://astral.sh/uv/install.sh | sh"))
            elif "wrknv Installation" in check:
                recommendations.append(("🔧 Reinstall wrknv", "See installation docs"))

        # If there are any env.sh issues, recommend regeneration
        if has_env_issues:
            recommendations.insert(0, ("🔄 Regenerate env.sh", "wrknv generate --force"))
            recommendations.append(("📝 After regenerating", "source env.sh"))

        # Priority 2: Address warnings
        for check, _details in self.checks_warning:
            if ".venv Directory" in check:
                recommendations.append(("🗑️  Remove .venv", "rm -rf .venv && source env.sh"))
            elif "workenv Directory" in check and not has_env_issues:
                recommendations.append(("🔧 Check workenv", "wrknv doctor"))
            elif "Virtual Environment" in check:
                recommendations.append(("🚪 Exit current venv", "deactivate && source env.sh"))

        # Remove duplicates and print
        seen = set()
        for title, command in recommendations:
            key = command
            if key not in seen:
                self.console.print(f"\n  {title}")
                self.console.print(f"  [dim]$ {command}[/dim]")
                seen.add(key)

        # If multiple env.sh failures, suggest bug report
        env_failure_count = sum(1 for check, _ in self.checks_failed if "env.sh" in check)
        if env_failure_count >= 3:
            self.console.print(
                "\n[yellow]⚠️  Multiple env.sh issues detected. "
                "If regenerating doesn't help, this may be a bug in wrknv.[/yellow]"
            )
            self.console.print("  [dim]Please report at: https://github.com/provide-io/wrknv/issues[/dim]")

    def xǁWrknvDoctorǁ_print_recommendations__mutmut_21(self) -> None:
        """Print recommendations based on failed checks."""
        self.console.print("\n[bold]Recommendations:[/bold]")

        recommendations = []

        # Priority 1: Fix critical failures
        has_env_issues = False
        for check, _details in self.checks_failed:
            if "env.sh" in check or "Workenv Path" in check:
                has_env_issues = True
            elif "uv" in check:
                recommendations.append(None)
            elif "wrknv Installation" in check:
                recommendations.append(("🔧 Reinstall wrknv", "See installation docs"))

        # If there are any env.sh issues, recommend regeneration
        if has_env_issues:
            recommendations.insert(0, ("🔄 Regenerate env.sh", "wrknv generate --force"))
            recommendations.append(("📝 After regenerating", "source env.sh"))

        # Priority 2: Address warnings
        for check, _details in self.checks_warning:
            if ".venv Directory" in check:
                recommendations.append(("🗑️  Remove .venv", "rm -rf .venv && source env.sh"))
            elif "workenv Directory" in check and not has_env_issues:
                recommendations.append(("🔧 Check workenv", "wrknv doctor"))
            elif "Virtual Environment" in check:
                recommendations.append(("🚪 Exit current venv", "deactivate && source env.sh"))

        # Remove duplicates and print
        seen = set()
        for title, command in recommendations:
            key = command
            if key not in seen:
                self.console.print(f"\n  {title}")
                self.console.print(f"  [dim]$ {command}[/dim]")
                seen.add(key)

        # If multiple env.sh failures, suggest bug report
        env_failure_count = sum(1 for check, _ in self.checks_failed if "env.sh" in check)
        if env_failure_count >= 3:
            self.console.print(
                "\n[yellow]⚠️  Multiple env.sh issues detected. "
                "If regenerating doesn't help, this may be a bug in wrknv.[/yellow]"
            )
            self.console.print("  [dim]Please report at: https://github.com/provide-io/wrknv/issues[/dim]")

    def xǁWrknvDoctorǁ_print_recommendations__mutmut_22(self) -> None:
        """Print recommendations based on failed checks."""
        self.console.print("\n[bold]Recommendations:[/bold]")

        recommendations = []

        # Priority 1: Fix critical failures
        has_env_issues = False
        for check, _details in self.checks_failed:
            if "env.sh" in check or "Workenv Path" in check:
                has_env_issues = True
            elif "uv" in check:
                recommendations.append(("XX🔧 Install uvXX", "curl -LsSf https://astral.sh/uv/install.sh | sh"))
            elif "wrknv Installation" in check:
                recommendations.append(("🔧 Reinstall wrknv", "See installation docs"))

        # If there are any env.sh issues, recommend regeneration
        if has_env_issues:
            recommendations.insert(0, ("🔄 Regenerate env.sh", "wrknv generate --force"))
            recommendations.append(("📝 After regenerating", "source env.sh"))

        # Priority 2: Address warnings
        for check, _details in self.checks_warning:
            if ".venv Directory" in check:
                recommendations.append(("🗑️  Remove .venv", "rm -rf .venv && source env.sh"))
            elif "workenv Directory" in check and not has_env_issues:
                recommendations.append(("🔧 Check workenv", "wrknv doctor"))
            elif "Virtual Environment" in check:
                recommendations.append(("🚪 Exit current venv", "deactivate && source env.sh"))

        # Remove duplicates and print
        seen = set()
        for title, command in recommendations:
            key = command
            if key not in seen:
                self.console.print(f"\n  {title}")
                self.console.print(f"  [dim]$ {command}[/dim]")
                seen.add(key)

        # If multiple env.sh failures, suggest bug report
        env_failure_count = sum(1 for check, _ in self.checks_failed if "env.sh" in check)
        if env_failure_count >= 3:
            self.console.print(
                "\n[yellow]⚠️  Multiple env.sh issues detected. "
                "If regenerating doesn't help, this may be a bug in wrknv.[/yellow]"
            )
            self.console.print("  [dim]Please report at: https://github.com/provide-io/wrknv/issues[/dim]")

    def xǁWrknvDoctorǁ_print_recommendations__mutmut_23(self) -> None:
        """Print recommendations based on failed checks."""
        self.console.print("\n[bold]Recommendations:[/bold]")

        recommendations = []

        # Priority 1: Fix critical failures
        has_env_issues = False
        for check, _details in self.checks_failed:
            if "env.sh" in check or "Workenv Path" in check:
                has_env_issues = True
            elif "uv" in check:
                recommendations.append(("🔧 install uv", "curl -LsSf https://astral.sh/uv/install.sh | sh"))
            elif "wrknv Installation" in check:
                recommendations.append(("🔧 Reinstall wrknv", "See installation docs"))

        # If there are any env.sh issues, recommend regeneration
        if has_env_issues:
            recommendations.insert(0, ("🔄 Regenerate env.sh", "wrknv generate --force"))
            recommendations.append(("📝 After regenerating", "source env.sh"))

        # Priority 2: Address warnings
        for check, _details in self.checks_warning:
            if ".venv Directory" in check:
                recommendations.append(("🗑️  Remove .venv", "rm -rf .venv && source env.sh"))
            elif "workenv Directory" in check and not has_env_issues:
                recommendations.append(("🔧 Check workenv", "wrknv doctor"))
            elif "Virtual Environment" in check:
                recommendations.append(("🚪 Exit current venv", "deactivate && source env.sh"))

        # Remove duplicates and print
        seen = set()
        for title, command in recommendations:
            key = command
            if key not in seen:
                self.console.print(f"\n  {title}")
                self.console.print(f"  [dim]$ {command}[/dim]")
                seen.add(key)

        # If multiple env.sh failures, suggest bug report
        env_failure_count = sum(1 for check, _ in self.checks_failed if "env.sh" in check)
        if env_failure_count >= 3:
            self.console.print(
                "\n[yellow]⚠️  Multiple env.sh issues detected. "
                "If regenerating doesn't help, this may be a bug in wrknv.[/yellow]"
            )
            self.console.print("  [dim]Please report at: https://github.com/provide-io/wrknv/issues[/dim]")

    def xǁWrknvDoctorǁ_print_recommendations__mutmut_24(self) -> None:
        """Print recommendations based on failed checks."""
        self.console.print("\n[bold]Recommendations:[/bold]")

        recommendations = []

        # Priority 1: Fix critical failures
        has_env_issues = False
        for check, _details in self.checks_failed:
            if "env.sh" in check or "Workenv Path" in check:
                has_env_issues = True
            elif "uv" in check:
                recommendations.append(("🔧 INSTALL UV", "curl -LsSf https://astral.sh/uv/install.sh | sh"))
            elif "wrknv Installation" in check:
                recommendations.append(("🔧 Reinstall wrknv", "See installation docs"))

        # If there are any env.sh issues, recommend regeneration
        if has_env_issues:
            recommendations.insert(0, ("🔄 Regenerate env.sh", "wrknv generate --force"))
            recommendations.append(("📝 After regenerating", "source env.sh"))

        # Priority 2: Address warnings
        for check, _details in self.checks_warning:
            if ".venv Directory" in check:
                recommendations.append(("🗑️  Remove .venv", "rm -rf .venv && source env.sh"))
            elif "workenv Directory" in check and not has_env_issues:
                recommendations.append(("🔧 Check workenv", "wrknv doctor"))
            elif "Virtual Environment" in check:
                recommendations.append(("🚪 Exit current venv", "deactivate && source env.sh"))

        # Remove duplicates and print
        seen = set()
        for title, command in recommendations:
            key = command
            if key not in seen:
                self.console.print(f"\n  {title}")
                self.console.print(f"  [dim]$ {command}[/dim]")
                seen.add(key)

        # If multiple env.sh failures, suggest bug report
        env_failure_count = sum(1 for check, _ in self.checks_failed if "env.sh" in check)
        if env_failure_count >= 3:
            self.console.print(
                "\n[yellow]⚠️  Multiple env.sh issues detected. "
                "If regenerating doesn't help, this may be a bug in wrknv.[/yellow]"
            )
            self.console.print("  [dim]Please report at: https://github.com/provide-io/wrknv/issues[/dim]")

    def xǁWrknvDoctorǁ_print_recommendations__mutmut_25(self) -> None:
        """Print recommendations based on failed checks."""
        self.console.print("\n[bold]Recommendations:[/bold]")

        recommendations = []

        # Priority 1: Fix critical failures
        has_env_issues = False
        for check, _details in self.checks_failed:
            if "env.sh" in check or "Workenv Path" in check:
                has_env_issues = True
            elif "uv" in check:
                recommendations.append(("🔧 Install uv", "XXcurl -LsSf https://astral.sh/uv/install.sh | shXX"))
            elif "wrknv Installation" in check:
                recommendations.append(("🔧 Reinstall wrknv", "See installation docs"))

        # If there are any env.sh issues, recommend regeneration
        if has_env_issues:
            recommendations.insert(0, ("🔄 Regenerate env.sh", "wrknv generate --force"))
            recommendations.append(("📝 After regenerating", "source env.sh"))

        # Priority 2: Address warnings
        for check, _details in self.checks_warning:
            if ".venv Directory" in check:
                recommendations.append(("🗑️  Remove .venv", "rm -rf .venv && source env.sh"))
            elif "workenv Directory" in check and not has_env_issues:
                recommendations.append(("🔧 Check workenv", "wrknv doctor"))
            elif "Virtual Environment" in check:
                recommendations.append(("🚪 Exit current venv", "deactivate && source env.sh"))

        # Remove duplicates and print
        seen = set()
        for title, command in recommendations:
            key = command
            if key not in seen:
                self.console.print(f"\n  {title}")
                self.console.print(f"  [dim]$ {command}[/dim]")
                seen.add(key)

        # If multiple env.sh failures, suggest bug report
        env_failure_count = sum(1 for check, _ in self.checks_failed if "env.sh" in check)
        if env_failure_count >= 3:
            self.console.print(
                "\n[yellow]⚠️  Multiple env.sh issues detected. "
                "If regenerating doesn't help, this may be a bug in wrknv.[/yellow]"
            )
            self.console.print("  [dim]Please report at: https://github.com/provide-io/wrknv/issues[/dim]")

    def xǁWrknvDoctorǁ_print_recommendations__mutmut_26(self) -> None:
        """Print recommendations based on failed checks."""
        self.console.print("\n[bold]Recommendations:[/bold]")

        recommendations = []

        # Priority 1: Fix critical failures
        has_env_issues = False
        for check, _details in self.checks_failed:
            if "env.sh" in check or "Workenv Path" in check:
                has_env_issues = True
            elif "uv" in check:
                recommendations.append(("🔧 Install uv", "curl -lssf https://astral.sh/uv/install.sh | sh"))
            elif "wrknv Installation" in check:
                recommendations.append(("🔧 Reinstall wrknv", "See installation docs"))

        # If there are any env.sh issues, recommend regeneration
        if has_env_issues:
            recommendations.insert(0, ("🔄 Regenerate env.sh", "wrknv generate --force"))
            recommendations.append(("📝 After regenerating", "source env.sh"))

        # Priority 2: Address warnings
        for check, _details in self.checks_warning:
            if ".venv Directory" in check:
                recommendations.append(("🗑️  Remove .venv", "rm -rf .venv && source env.sh"))
            elif "workenv Directory" in check and not has_env_issues:
                recommendations.append(("🔧 Check workenv", "wrknv doctor"))
            elif "Virtual Environment" in check:
                recommendations.append(("🚪 Exit current venv", "deactivate && source env.sh"))

        # Remove duplicates and print
        seen = set()
        for title, command in recommendations:
            key = command
            if key not in seen:
                self.console.print(f"\n  {title}")
                self.console.print(f"  [dim]$ {command}[/dim]")
                seen.add(key)

        # If multiple env.sh failures, suggest bug report
        env_failure_count = sum(1 for check, _ in self.checks_failed if "env.sh" in check)
        if env_failure_count >= 3:
            self.console.print(
                "\n[yellow]⚠️  Multiple env.sh issues detected. "
                "If regenerating doesn't help, this may be a bug in wrknv.[/yellow]"
            )
            self.console.print("  [dim]Please report at: https://github.com/provide-io/wrknv/issues[/dim]")

    def xǁWrknvDoctorǁ_print_recommendations__mutmut_27(self) -> None:
        """Print recommendations based on failed checks."""
        self.console.print("\n[bold]Recommendations:[/bold]")

        recommendations = []

        # Priority 1: Fix critical failures
        has_env_issues = False
        for check, _details in self.checks_failed:
            if "env.sh" in check or "Workenv Path" in check:
                has_env_issues = True
            elif "uv" in check:
                recommendations.append(("🔧 Install uv", "CURL -LSSF HTTPS://ASTRAL.SH/UV/INSTALL.SH | SH"))
            elif "wrknv Installation" in check:
                recommendations.append(("🔧 Reinstall wrknv", "See installation docs"))

        # If there are any env.sh issues, recommend regeneration
        if has_env_issues:
            recommendations.insert(0, ("🔄 Regenerate env.sh", "wrknv generate --force"))
            recommendations.append(("📝 After regenerating", "source env.sh"))

        # Priority 2: Address warnings
        for check, _details in self.checks_warning:
            if ".venv Directory" in check:
                recommendations.append(("🗑️  Remove .venv", "rm -rf .venv && source env.sh"))
            elif "workenv Directory" in check and not has_env_issues:
                recommendations.append(("🔧 Check workenv", "wrknv doctor"))
            elif "Virtual Environment" in check:
                recommendations.append(("🚪 Exit current venv", "deactivate && source env.sh"))

        # Remove duplicates and print
        seen = set()
        for title, command in recommendations:
            key = command
            if key not in seen:
                self.console.print(f"\n  {title}")
                self.console.print(f"  [dim]$ {command}[/dim]")
                seen.add(key)

        # If multiple env.sh failures, suggest bug report
        env_failure_count = sum(1 for check, _ in self.checks_failed if "env.sh" in check)
        if env_failure_count >= 3:
            self.console.print(
                "\n[yellow]⚠️  Multiple env.sh issues detected. "
                "If regenerating doesn't help, this may be a bug in wrknv.[/yellow]"
            )
            self.console.print("  [dim]Please report at: https://github.com/provide-io/wrknv/issues[/dim]")

    def xǁWrknvDoctorǁ_print_recommendations__mutmut_28(self) -> None:
        """Print recommendations based on failed checks."""
        self.console.print("\n[bold]Recommendations:[/bold]")

        recommendations = []

        # Priority 1: Fix critical failures
        has_env_issues = False
        for check, _details in self.checks_failed:
            if "env.sh" in check or "Workenv Path" in check:
                has_env_issues = True
            elif "uv" in check:
                recommendations.append(("🔧 Install uv", "curl -LsSf https://astral.sh/uv/install.sh | sh"))
            elif "XXwrknv InstallationXX" in check:
                recommendations.append(("🔧 Reinstall wrknv", "See installation docs"))

        # If there are any env.sh issues, recommend regeneration
        if has_env_issues:
            recommendations.insert(0, ("🔄 Regenerate env.sh", "wrknv generate --force"))
            recommendations.append(("📝 After regenerating", "source env.sh"))

        # Priority 2: Address warnings
        for check, _details in self.checks_warning:
            if ".venv Directory" in check:
                recommendations.append(("🗑️  Remove .venv", "rm -rf .venv && source env.sh"))
            elif "workenv Directory" in check and not has_env_issues:
                recommendations.append(("🔧 Check workenv", "wrknv doctor"))
            elif "Virtual Environment" in check:
                recommendations.append(("🚪 Exit current venv", "deactivate && source env.sh"))

        # Remove duplicates and print
        seen = set()
        for title, command in recommendations:
            key = command
            if key not in seen:
                self.console.print(f"\n  {title}")
                self.console.print(f"  [dim]$ {command}[/dim]")
                seen.add(key)

        # If multiple env.sh failures, suggest bug report
        env_failure_count = sum(1 for check, _ in self.checks_failed if "env.sh" in check)
        if env_failure_count >= 3:
            self.console.print(
                "\n[yellow]⚠️  Multiple env.sh issues detected. "
                "If regenerating doesn't help, this may be a bug in wrknv.[/yellow]"
            )
            self.console.print("  [dim]Please report at: https://github.com/provide-io/wrknv/issues[/dim]")

    def xǁWrknvDoctorǁ_print_recommendations__mutmut_29(self) -> None:
        """Print recommendations based on failed checks."""
        self.console.print("\n[bold]Recommendations:[/bold]")

        recommendations = []

        # Priority 1: Fix critical failures
        has_env_issues = False
        for check, _details in self.checks_failed:
            if "env.sh" in check or "Workenv Path" in check:
                has_env_issues = True
            elif "uv" in check:
                recommendations.append(("🔧 Install uv", "curl -LsSf https://astral.sh/uv/install.sh | sh"))
            elif "wrknv installation" in check:
                recommendations.append(("🔧 Reinstall wrknv", "See installation docs"))

        # If there are any env.sh issues, recommend regeneration
        if has_env_issues:
            recommendations.insert(0, ("🔄 Regenerate env.sh", "wrknv generate --force"))
            recommendations.append(("📝 After regenerating", "source env.sh"))

        # Priority 2: Address warnings
        for check, _details in self.checks_warning:
            if ".venv Directory" in check:
                recommendations.append(("🗑️  Remove .venv", "rm -rf .venv && source env.sh"))
            elif "workenv Directory" in check and not has_env_issues:
                recommendations.append(("🔧 Check workenv", "wrknv doctor"))
            elif "Virtual Environment" in check:
                recommendations.append(("🚪 Exit current venv", "deactivate && source env.sh"))

        # Remove duplicates and print
        seen = set()
        for title, command in recommendations:
            key = command
            if key not in seen:
                self.console.print(f"\n  {title}")
                self.console.print(f"  [dim]$ {command}[/dim]")
                seen.add(key)

        # If multiple env.sh failures, suggest bug report
        env_failure_count = sum(1 for check, _ in self.checks_failed if "env.sh" in check)
        if env_failure_count >= 3:
            self.console.print(
                "\n[yellow]⚠️  Multiple env.sh issues detected. "
                "If regenerating doesn't help, this may be a bug in wrknv.[/yellow]"
            )
            self.console.print("  [dim]Please report at: https://github.com/provide-io/wrknv/issues[/dim]")

    def xǁWrknvDoctorǁ_print_recommendations__mutmut_30(self) -> None:
        """Print recommendations based on failed checks."""
        self.console.print("\n[bold]Recommendations:[/bold]")

        recommendations = []

        # Priority 1: Fix critical failures
        has_env_issues = False
        for check, _details in self.checks_failed:
            if "env.sh" in check or "Workenv Path" in check:
                has_env_issues = True
            elif "uv" in check:
                recommendations.append(("🔧 Install uv", "curl -LsSf https://astral.sh/uv/install.sh | sh"))
            elif "WRKNV INSTALLATION" in check:
                recommendations.append(("🔧 Reinstall wrknv", "See installation docs"))

        # If there are any env.sh issues, recommend regeneration
        if has_env_issues:
            recommendations.insert(0, ("🔄 Regenerate env.sh", "wrknv generate --force"))
            recommendations.append(("📝 After regenerating", "source env.sh"))

        # Priority 2: Address warnings
        for check, _details in self.checks_warning:
            if ".venv Directory" in check:
                recommendations.append(("🗑️  Remove .venv", "rm -rf .venv && source env.sh"))
            elif "workenv Directory" in check and not has_env_issues:
                recommendations.append(("🔧 Check workenv", "wrknv doctor"))
            elif "Virtual Environment" in check:
                recommendations.append(("🚪 Exit current venv", "deactivate && source env.sh"))

        # Remove duplicates and print
        seen = set()
        for title, command in recommendations:
            key = command
            if key not in seen:
                self.console.print(f"\n  {title}")
                self.console.print(f"  [dim]$ {command}[/dim]")
                seen.add(key)

        # If multiple env.sh failures, suggest bug report
        env_failure_count = sum(1 for check, _ in self.checks_failed if "env.sh" in check)
        if env_failure_count >= 3:
            self.console.print(
                "\n[yellow]⚠️  Multiple env.sh issues detected. "
                "If regenerating doesn't help, this may be a bug in wrknv.[/yellow]"
            )
            self.console.print("  [dim]Please report at: https://github.com/provide-io/wrknv/issues[/dim]")

    def xǁWrknvDoctorǁ_print_recommendations__mutmut_31(self) -> None:
        """Print recommendations based on failed checks."""
        self.console.print("\n[bold]Recommendations:[/bold]")

        recommendations = []

        # Priority 1: Fix critical failures
        has_env_issues = False
        for check, _details in self.checks_failed:
            if "env.sh" in check or "Workenv Path" in check:
                has_env_issues = True
            elif "uv" in check:
                recommendations.append(("🔧 Install uv", "curl -LsSf https://astral.sh/uv/install.sh | sh"))
            elif "wrknv Installation" not in check:
                recommendations.append(("🔧 Reinstall wrknv", "See installation docs"))

        # If there are any env.sh issues, recommend regeneration
        if has_env_issues:
            recommendations.insert(0, ("🔄 Regenerate env.sh", "wrknv generate --force"))
            recommendations.append(("📝 After regenerating", "source env.sh"))

        # Priority 2: Address warnings
        for check, _details in self.checks_warning:
            if ".venv Directory" in check:
                recommendations.append(("🗑️  Remove .venv", "rm -rf .venv && source env.sh"))
            elif "workenv Directory" in check and not has_env_issues:
                recommendations.append(("🔧 Check workenv", "wrknv doctor"))
            elif "Virtual Environment" in check:
                recommendations.append(("🚪 Exit current venv", "deactivate && source env.sh"))

        # Remove duplicates and print
        seen = set()
        for title, command in recommendations:
            key = command
            if key not in seen:
                self.console.print(f"\n  {title}")
                self.console.print(f"  [dim]$ {command}[/dim]")
                seen.add(key)

        # If multiple env.sh failures, suggest bug report
        env_failure_count = sum(1 for check, _ in self.checks_failed if "env.sh" in check)
        if env_failure_count >= 3:
            self.console.print(
                "\n[yellow]⚠️  Multiple env.sh issues detected. "
                "If regenerating doesn't help, this may be a bug in wrknv.[/yellow]"
            )
            self.console.print("  [dim]Please report at: https://github.com/provide-io/wrknv/issues[/dim]")

    def xǁWrknvDoctorǁ_print_recommendations__mutmut_32(self) -> None:
        """Print recommendations based on failed checks."""
        self.console.print("\n[bold]Recommendations:[/bold]")

        recommendations = []

        # Priority 1: Fix critical failures
        has_env_issues = False
        for check, _details in self.checks_failed:
            if "env.sh" in check or "Workenv Path" in check:
                has_env_issues = True
            elif "uv" in check:
                recommendations.append(("🔧 Install uv", "curl -LsSf https://astral.sh/uv/install.sh | sh"))
            elif "wrknv Installation" in check:
                recommendations.append(None)

        # If there are any env.sh issues, recommend regeneration
        if has_env_issues:
            recommendations.insert(0, ("🔄 Regenerate env.sh", "wrknv generate --force"))
            recommendations.append(("📝 After regenerating", "source env.sh"))

        # Priority 2: Address warnings
        for check, _details in self.checks_warning:
            if ".venv Directory" in check:
                recommendations.append(("🗑️  Remove .venv", "rm -rf .venv && source env.sh"))
            elif "workenv Directory" in check and not has_env_issues:
                recommendations.append(("🔧 Check workenv", "wrknv doctor"))
            elif "Virtual Environment" in check:
                recommendations.append(("🚪 Exit current venv", "deactivate && source env.sh"))

        # Remove duplicates and print
        seen = set()
        for title, command in recommendations:
            key = command
            if key not in seen:
                self.console.print(f"\n  {title}")
                self.console.print(f"  [dim]$ {command}[/dim]")
                seen.add(key)

        # If multiple env.sh failures, suggest bug report
        env_failure_count = sum(1 for check, _ in self.checks_failed if "env.sh" in check)
        if env_failure_count >= 3:
            self.console.print(
                "\n[yellow]⚠️  Multiple env.sh issues detected. "
                "If regenerating doesn't help, this may be a bug in wrknv.[/yellow]"
            )
            self.console.print("  [dim]Please report at: https://github.com/provide-io/wrknv/issues[/dim]")

    def xǁWrknvDoctorǁ_print_recommendations__mutmut_33(self) -> None:
        """Print recommendations based on failed checks."""
        self.console.print("\n[bold]Recommendations:[/bold]")

        recommendations = []

        # Priority 1: Fix critical failures
        has_env_issues = False
        for check, _details in self.checks_failed:
            if "env.sh" in check or "Workenv Path" in check:
                has_env_issues = True
            elif "uv" in check:
                recommendations.append(("🔧 Install uv", "curl -LsSf https://astral.sh/uv/install.sh | sh"))
            elif "wrknv Installation" in check:
                recommendations.append(("XX🔧 Reinstall wrknvXX", "See installation docs"))

        # If there are any env.sh issues, recommend regeneration
        if has_env_issues:
            recommendations.insert(0, ("🔄 Regenerate env.sh", "wrknv generate --force"))
            recommendations.append(("📝 After regenerating", "source env.sh"))

        # Priority 2: Address warnings
        for check, _details in self.checks_warning:
            if ".venv Directory" in check:
                recommendations.append(("🗑️  Remove .venv", "rm -rf .venv && source env.sh"))
            elif "workenv Directory" in check and not has_env_issues:
                recommendations.append(("🔧 Check workenv", "wrknv doctor"))
            elif "Virtual Environment" in check:
                recommendations.append(("🚪 Exit current venv", "deactivate && source env.sh"))

        # Remove duplicates and print
        seen = set()
        for title, command in recommendations:
            key = command
            if key not in seen:
                self.console.print(f"\n  {title}")
                self.console.print(f"  [dim]$ {command}[/dim]")
                seen.add(key)

        # If multiple env.sh failures, suggest bug report
        env_failure_count = sum(1 for check, _ in self.checks_failed if "env.sh" in check)
        if env_failure_count >= 3:
            self.console.print(
                "\n[yellow]⚠️  Multiple env.sh issues detected. "
                "If regenerating doesn't help, this may be a bug in wrknv.[/yellow]"
            )
            self.console.print("  [dim]Please report at: https://github.com/provide-io/wrknv/issues[/dim]")

    def xǁWrknvDoctorǁ_print_recommendations__mutmut_34(self) -> None:
        """Print recommendations based on failed checks."""
        self.console.print("\n[bold]Recommendations:[/bold]")

        recommendations = []

        # Priority 1: Fix critical failures
        has_env_issues = False
        for check, _details in self.checks_failed:
            if "env.sh" in check or "Workenv Path" in check:
                has_env_issues = True
            elif "uv" in check:
                recommendations.append(("🔧 Install uv", "curl -LsSf https://astral.sh/uv/install.sh | sh"))
            elif "wrknv Installation" in check:
                recommendations.append(("🔧 reinstall wrknv", "See installation docs"))

        # If there are any env.sh issues, recommend regeneration
        if has_env_issues:
            recommendations.insert(0, ("🔄 Regenerate env.sh", "wrknv generate --force"))
            recommendations.append(("📝 After regenerating", "source env.sh"))

        # Priority 2: Address warnings
        for check, _details in self.checks_warning:
            if ".venv Directory" in check:
                recommendations.append(("🗑️  Remove .venv", "rm -rf .venv && source env.sh"))
            elif "workenv Directory" in check and not has_env_issues:
                recommendations.append(("🔧 Check workenv", "wrknv doctor"))
            elif "Virtual Environment" in check:
                recommendations.append(("🚪 Exit current venv", "deactivate && source env.sh"))

        # Remove duplicates and print
        seen = set()
        for title, command in recommendations:
            key = command
            if key not in seen:
                self.console.print(f"\n  {title}")
                self.console.print(f"  [dim]$ {command}[/dim]")
                seen.add(key)

        # If multiple env.sh failures, suggest bug report
        env_failure_count = sum(1 for check, _ in self.checks_failed if "env.sh" in check)
        if env_failure_count >= 3:
            self.console.print(
                "\n[yellow]⚠️  Multiple env.sh issues detected. "
                "If regenerating doesn't help, this may be a bug in wrknv.[/yellow]"
            )
            self.console.print("  [dim]Please report at: https://github.com/provide-io/wrknv/issues[/dim]")

    def xǁWrknvDoctorǁ_print_recommendations__mutmut_35(self) -> None:
        """Print recommendations based on failed checks."""
        self.console.print("\n[bold]Recommendations:[/bold]")

        recommendations = []

        # Priority 1: Fix critical failures
        has_env_issues = False
        for check, _details in self.checks_failed:
            if "env.sh" in check or "Workenv Path" in check:
                has_env_issues = True
            elif "uv" in check:
                recommendations.append(("🔧 Install uv", "curl -LsSf https://astral.sh/uv/install.sh | sh"))
            elif "wrknv Installation" in check:
                recommendations.append(("🔧 REINSTALL WRKNV", "See installation docs"))

        # If there are any env.sh issues, recommend regeneration
        if has_env_issues:
            recommendations.insert(0, ("🔄 Regenerate env.sh", "wrknv generate --force"))
            recommendations.append(("📝 After regenerating", "source env.sh"))

        # Priority 2: Address warnings
        for check, _details in self.checks_warning:
            if ".venv Directory" in check:
                recommendations.append(("🗑️  Remove .venv", "rm -rf .venv && source env.sh"))
            elif "workenv Directory" in check and not has_env_issues:
                recommendations.append(("🔧 Check workenv", "wrknv doctor"))
            elif "Virtual Environment" in check:
                recommendations.append(("🚪 Exit current venv", "deactivate && source env.sh"))

        # Remove duplicates and print
        seen = set()
        for title, command in recommendations:
            key = command
            if key not in seen:
                self.console.print(f"\n  {title}")
                self.console.print(f"  [dim]$ {command}[/dim]")
                seen.add(key)

        # If multiple env.sh failures, suggest bug report
        env_failure_count = sum(1 for check, _ in self.checks_failed if "env.sh" in check)
        if env_failure_count >= 3:
            self.console.print(
                "\n[yellow]⚠️  Multiple env.sh issues detected. "
                "If regenerating doesn't help, this may be a bug in wrknv.[/yellow]"
            )
            self.console.print("  [dim]Please report at: https://github.com/provide-io/wrknv/issues[/dim]")

    def xǁWrknvDoctorǁ_print_recommendations__mutmut_36(self) -> None:
        """Print recommendations based on failed checks."""
        self.console.print("\n[bold]Recommendations:[/bold]")

        recommendations = []

        # Priority 1: Fix critical failures
        has_env_issues = False
        for check, _details in self.checks_failed:
            if "env.sh" in check or "Workenv Path" in check:
                has_env_issues = True
            elif "uv" in check:
                recommendations.append(("🔧 Install uv", "curl -LsSf https://astral.sh/uv/install.sh | sh"))
            elif "wrknv Installation" in check:
                recommendations.append(("🔧 Reinstall wrknv", "XXSee installation docsXX"))

        # If there are any env.sh issues, recommend regeneration
        if has_env_issues:
            recommendations.insert(0, ("🔄 Regenerate env.sh", "wrknv generate --force"))
            recommendations.append(("📝 After regenerating", "source env.sh"))

        # Priority 2: Address warnings
        for check, _details in self.checks_warning:
            if ".venv Directory" in check:
                recommendations.append(("🗑️  Remove .venv", "rm -rf .venv && source env.sh"))
            elif "workenv Directory" in check and not has_env_issues:
                recommendations.append(("🔧 Check workenv", "wrknv doctor"))
            elif "Virtual Environment" in check:
                recommendations.append(("🚪 Exit current venv", "deactivate && source env.sh"))

        # Remove duplicates and print
        seen = set()
        for title, command in recommendations:
            key = command
            if key not in seen:
                self.console.print(f"\n  {title}")
                self.console.print(f"  [dim]$ {command}[/dim]")
                seen.add(key)

        # If multiple env.sh failures, suggest bug report
        env_failure_count = sum(1 for check, _ in self.checks_failed if "env.sh" in check)
        if env_failure_count >= 3:
            self.console.print(
                "\n[yellow]⚠️  Multiple env.sh issues detected. "
                "If regenerating doesn't help, this may be a bug in wrknv.[/yellow]"
            )
            self.console.print("  [dim]Please report at: https://github.com/provide-io/wrknv/issues[/dim]")

    def xǁWrknvDoctorǁ_print_recommendations__mutmut_37(self) -> None:
        """Print recommendations based on failed checks."""
        self.console.print("\n[bold]Recommendations:[/bold]")

        recommendations = []

        # Priority 1: Fix critical failures
        has_env_issues = False
        for check, _details in self.checks_failed:
            if "env.sh" in check or "Workenv Path" in check:
                has_env_issues = True
            elif "uv" in check:
                recommendations.append(("🔧 Install uv", "curl -LsSf https://astral.sh/uv/install.sh | sh"))
            elif "wrknv Installation" in check:
                recommendations.append(("🔧 Reinstall wrknv", "see installation docs"))

        # If there are any env.sh issues, recommend regeneration
        if has_env_issues:
            recommendations.insert(0, ("🔄 Regenerate env.sh", "wrknv generate --force"))
            recommendations.append(("📝 After regenerating", "source env.sh"))

        # Priority 2: Address warnings
        for check, _details in self.checks_warning:
            if ".venv Directory" in check:
                recommendations.append(("🗑️  Remove .venv", "rm -rf .venv && source env.sh"))
            elif "workenv Directory" in check and not has_env_issues:
                recommendations.append(("🔧 Check workenv", "wrknv doctor"))
            elif "Virtual Environment" in check:
                recommendations.append(("🚪 Exit current venv", "deactivate && source env.sh"))

        # Remove duplicates and print
        seen = set()
        for title, command in recommendations:
            key = command
            if key not in seen:
                self.console.print(f"\n  {title}")
                self.console.print(f"  [dim]$ {command}[/dim]")
                seen.add(key)

        # If multiple env.sh failures, suggest bug report
        env_failure_count = sum(1 for check, _ in self.checks_failed if "env.sh" in check)
        if env_failure_count >= 3:
            self.console.print(
                "\n[yellow]⚠️  Multiple env.sh issues detected. "
                "If regenerating doesn't help, this may be a bug in wrknv.[/yellow]"
            )
            self.console.print("  [dim]Please report at: https://github.com/provide-io/wrknv/issues[/dim]")

    def xǁWrknvDoctorǁ_print_recommendations__mutmut_38(self) -> None:
        """Print recommendations based on failed checks."""
        self.console.print("\n[bold]Recommendations:[/bold]")

        recommendations = []

        # Priority 1: Fix critical failures
        has_env_issues = False
        for check, _details in self.checks_failed:
            if "env.sh" in check or "Workenv Path" in check:
                has_env_issues = True
            elif "uv" in check:
                recommendations.append(("🔧 Install uv", "curl -LsSf https://astral.sh/uv/install.sh | sh"))
            elif "wrknv Installation" in check:
                recommendations.append(("🔧 Reinstall wrknv", "SEE INSTALLATION DOCS"))

        # If there are any env.sh issues, recommend regeneration
        if has_env_issues:
            recommendations.insert(0, ("🔄 Regenerate env.sh", "wrknv generate --force"))
            recommendations.append(("📝 After regenerating", "source env.sh"))

        # Priority 2: Address warnings
        for check, _details in self.checks_warning:
            if ".venv Directory" in check:
                recommendations.append(("🗑️  Remove .venv", "rm -rf .venv && source env.sh"))
            elif "workenv Directory" in check and not has_env_issues:
                recommendations.append(("🔧 Check workenv", "wrknv doctor"))
            elif "Virtual Environment" in check:
                recommendations.append(("🚪 Exit current venv", "deactivate && source env.sh"))

        # Remove duplicates and print
        seen = set()
        for title, command in recommendations:
            key = command
            if key not in seen:
                self.console.print(f"\n  {title}")
                self.console.print(f"  [dim]$ {command}[/dim]")
                seen.add(key)

        # If multiple env.sh failures, suggest bug report
        env_failure_count = sum(1 for check, _ in self.checks_failed if "env.sh" in check)
        if env_failure_count >= 3:
            self.console.print(
                "\n[yellow]⚠️  Multiple env.sh issues detected. "
                "If regenerating doesn't help, this may be a bug in wrknv.[/yellow]"
            )
            self.console.print("  [dim]Please report at: https://github.com/provide-io/wrknv/issues[/dim]")

    def xǁWrknvDoctorǁ_print_recommendations__mutmut_39(self) -> None:
        """Print recommendations based on failed checks."""
        self.console.print("\n[bold]Recommendations:[/bold]")

        recommendations = []

        # Priority 1: Fix critical failures
        has_env_issues = False
        for check, _details in self.checks_failed:
            if "env.sh" in check or "Workenv Path" in check:
                has_env_issues = True
            elif "uv" in check:
                recommendations.append(("🔧 Install uv", "curl -LsSf https://astral.sh/uv/install.sh | sh"))
            elif "wrknv Installation" in check:
                recommendations.append(("🔧 Reinstall wrknv", "See installation docs"))

        # If there are any env.sh issues, recommend regeneration
        if has_env_issues:
            recommendations.insert(None, ("🔄 Regenerate env.sh", "wrknv generate --force"))
            recommendations.append(("📝 After regenerating", "source env.sh"))

        # Priority 2: Address warnings
        for check, _details in self.checks_warning:
            if ".venv Directory" in check:
                recommendations.append(("🗑️  Remove .venv", "rm -rf .venv && source env.sh"))
            elif "workenv Directory" in check and not has_env_issues:
                recommendations.append(("🔧 Check workenv", "wrknv doctor"))
            elif "Virtual Environment" in check:
                recommendations.append(("🚪 Exit current venv", "deactivate && source env.sh"))

        # Remove duplicates and print
        seen = set()
        for title, command in recommendations:
            key = command
            if key not in seen:
                self.console.print(f"\n  {title}")
                self.console.print(f"  [dim]$ {command}[/dim]")
                seen.add(key)

        # If multiple env.sh failures, suggest bug report
        env_failure_count = sum(1 for check, _ in self.checks_failed if "env.sh" in check)
        if env_failure_count >= 3:
            self.console.print(
                "\n[yellow]⚠️  Multiple env.sh issues detected. "
                "If regenerating doesn't help, this may be a bug in wrknv.[/yellow]"
            )
            self.console.print("  [dim]Please report at: https://github.com/provide-io/wrknv/issues[/dim]")

    def xǁWrknvDoctorǁ_print_recommendations__mutmut_40(self) -> None:
        """Print recommendations based on failed checks."""
        self.console.print("\n[bold]Recommendations:[/bold]")

        recommendations = []

        # Priority 1: Fix critical failures
        has_env_issues = False
        for check, _details in self.checks_failed:
            if "env.sh" in check or "Workenv Path" in check:
                has_env_issues = True
            elif "uv" in check:
                recommendations.append(("🔧 Install uv", "curl -LsSf https://astral.sh/uv/install.sh | sh"))
            elif "wrknv Installation" in check:
                recommendations.append(("🔧 Reinstall wrknv", "See installation docs"))

        # If there are any env.sh issues, recommend regeneration
        if has_env_issues:
            recommendations.insert(0, None)
            recommendations.append(("📝 After regenerating", "source env.sh"))

        # Priority 2: Address warnings
        for check, _details in self.checks_warning:
            if ".venv Directory" in check:
                recommendations.append(("🗑️  Remove .venv", "rm -rf .venv && source env.sh"))
            elif "workenv Directory" in check and not has_env_issues:
                recommendations.append(("🔧 Check workenv", "wrknv doctor"))
            elif "Virtual Environment" in check:
                recommendations.append(("🚪 Exit current venv", "deactivate && source env.sh"))

        # Remove duplicates and print
        seen = set()
        for title, command in recommendations:
            key = command
            if key not in seen:
                self.console.print(f"\n  {title}")
                self.console.print(f"  [dim]$ {command}[/dim]")
                seen.add(key)

        # If multiple env.sh failures, suggest bug report
        env_failure_count = sum(1 for check, _ in self.checks_failed if "env.sh" in check)
        if env_failure_count >= 3:
            self.console.print(
                "\n[yellow]⚠️  Multiple env.sh issues detected. "
                "If regenerating doesn't help, this may be a bug in wrknv.[/yellow]"
            )
            self.console.print("  [dim]Please report at: https://github.com/provide-io/wrknv/issues[/dim]")

    def xǁWrknvDoctorǁ_print_recommendations__mutmut_41(self) -> None:
        """Print recommendations based on failed checks."""
        self.console.print("\n[bold]Recommendations:[/bold]")

        recommendations = []

        # Priority 1: Fix critical failures
        has_env_issues = False
        for check, _details in self.checks_failed:
            if "env.sh" in check or "Workenv Path" in check:
                has_env_issues = True
            elif "uv" in check:
                recommendations.append(("🔧 Install uv", "curl -LsSf https://astral.sh/uv/install.sh | sh"))
            elif "wrknv Installation" in check:
                recommendations.append(("🔧 Reinstall wrknv", "See installation docs"))

        # If there are any env.sh issues, recommend regeneration
        if has_env_issues:
            recommendations.insert(("🔄 Regenerate env.sh", "wrknv generate --force"))
            recommendations.append(("📝 After regenerating", "source env.sh"))

        # Priority 2: Address warnings
        for check, _details in self.checks_warning:
            if ".venv Directory" in check:
                recommendations.append(("🗑️  Remove .venv", "rm -rf .venv && source env.sh"))
            elif "workenv Directory" in check and not has_env_issues:
                recommendations.append(("🔧 Check workenv", "wrknv doctor"))
            elif "Virtual Environment" in check:
                recommendations.append(("🚪 Exit current venv", "deactivate && source env.sh"))

        # Remove duplicates and print
        seen = set()
        for title, command in recommendations:
            key = command
            if key not in seen:
                self.console.print(f"\n  {title}")
                self.console.print(f"  [dim]$ {command}[/dim]")
                seen.add(key)

        # If multiple env.sh failures, suggest bug report
        env_failure_count = sum(1 for check, _ in self.checks_failed if "env.sh" in check)
        if env_failure_count >= 3:
            self.console.print(
                "\n[yellow]⚠️  Multiple env.sh issues detected. "
                "If regenerating doesn't help, this may be a bug in wrknv.[/yellow]"
            )
            self.console.print("  [dim]Please report at: https://github.com/provide-io/wrknv/issues[/dim]")

    def xǁWrknvDoctorǁ_print_recommendations__mutmut_42(self) -> None:
        """Print recommendations based on failed checks."""
        self.console.print("\n[bold]Recommendations:[/bold]")

        recommendations = []

        # Priority 1: Fix critical failures
        has_env_issues = False
        for check, _details in self.checks_failed:
            if "env.sh" in check or "Workenv Path" in check:
                has_env_issues = True
            elif "uv" in check:
                recommendations.append(("🔧 Install uv", "curl -LsSf https://astral.sh/uv/install.sh | sh"))
            elif "wrknv Installation" in check:
                recommendations.append(("🔧 Reinstall wrknv", "See installation docs"))

        # If there are any env.sh issues, recommend regeneration
        if has_env_issues:
            recommendations.insert(0, )
            recommendations.append(("📝 After regenerating", "source env.sh"))

        # Priority 2: Address warnings
        for check, _details in self.checks_warning:
            if ".venv Directory" in check:
                recommendations.append(("🗑️  Remove .venv", "rm -rf .venv && source env.sh"))
            elif "workenv Directory" in check and not has_env_issues:
                recommendations.append(("🔧 Check workenv", "wrknv doctor"))
            elif "Virtual Environment" in check:
                recommendations.append(("🚪 Exit current venv", "deactivate && source env.sh"))

        # Remove duplicates and print
        seen = set()
        for title, command in recommendations:
            key = command
            if key not in seen:
                self.console.print(f"\n  {title}")
                self.console.print(f"  [dim]$ {command}[/dim]")
                seen.add(key)

        # If multiple env.sh failures, suggest bug report
        env_failure_count = sum(1 for check, _ in self.checks_failed if "env.sh" in check)
        if env_failure_count >= 3:
            self.console.print(
                "\n[yellow]⚠️  Multiple env.sh issues detected. "
                "If regenerating doesn't help, this may be a bug in wrknv.[/yellow]"
            )
            self.console.print("  [dim]Please report at: https://github.com/provide-io/wrknv/issues[/dim]")

    def xǁWrknvDoctorǁ_print_recommendations__mutmut_43(self) -> None:
        """Print recommendations based on failed checks."""
        self.console.print("\n[bold]Recommendations:[/bold]")

        recommendations = []

        # Priority 1: Fix critical failures
        has_env_issues = False
        for check, _details in self.checks_failed:
            if "env.sh" in check or "Workenv Path" in check:
                has_env_issues = True
            elif "uv" in check:
                recommendations.append(("🔧 Install uv", "curl -LsSf https://astral.sh/uv/install.sh | sh"))
            elif "wrknv Installation" in check:
                recommendations.append(("🔧 Reinstall wrknv", "See installation docs"))

        # If there are any env.sh issues, recommend regeneration
        if has_env_issues:
            recommendations.insert(1, ("🔄 Regenerate env.sh", "wrknv generate --force"))
            recommendations.append(("📝 After regenerating", "source env.sh"))

        # Priority 2: Address warnings
        for check, _details in self.checks_warning:
            if ".venv Directory" in check:
                recommendations.append(("🗑️  Remove .venv", "rm -rf .venv && source env.sh"))
            elif "workenv Directory" in check and not has_env_issues:
                recommendations.append(("🔧 Check workenv", "wrknv doctor"))
            elif "Virtual Environment" in check:
                recommendations.append(("🚪 Exit current venv", "deactivate && source env.sh"))

        # Remove duplicates and print
        seen = set()
        for title, command in recommendations:
            key = command
            if key not in seen:
                self.console.print(f"\n  {title}")
                self.console.print(f"  [dim]$ {command}[/dim]")
                seen.add(key)

        # If multiple env.sh failures, suggest bug report
        env_failure_count = sum(1 for check, _ in self.checks_failed if "env.sh" in check)
        if env_failure_count >= 3:
            self.console.print(
                "\n[yellow]⚠️  Multiple env.sh issues detected. "
                "If regenerating doesn't help, this may be a bug in wrknv.[/yellow]"
            )
            self.console.print("  [dim]Please report at: https://github.com/provide-io/wrknv/issues[/dim]")

    def xǁWrknvDoctorǁ_print_recommendations__mutmut_44(self) -> None:
        """Print recommendations based on failed checks."""
        self.console.print("\n[bold]Recommendations:[/bold]")

        recommendations = []

        # Priority 1: Fix critical failures
        has_env_issues = False
        for check, _details in self.checks_failed:
            if "env.sh" in check or "Workenv Path" in check:
                has_env_issues = True
            elif "uv" in check:
                recommendations.append(("🔧 Install uv", "curl -LsSf https://astral.sh/uv/install.sh | sh"))
            elif "wrknv Installation" in check:
                recommendations.append(("🔧 Reinstall wrknv", "See installation docs"))

        # If there are any env.sh issues, recommend regeneration
        if has_env_issues:
            recommendations.insert(0, ("XX🔄 Regenerate env.shXX", "wrknv generate --force"))
            recommendations.append(("📝 After regenerating", "source env.sh"))

        # Priority 2: Address warnings
        for check, _details in self.checks_warning:
            if ".venv Directory" in check:
                recommendations.append(("🗑️  Remove .venv", "rm -rf .venv && source env.sh"))
            elif "workenv Directory" in check and not has_env_issues:
                recommendations.append(("🔧 Check workenv", "wrknv doctor"))
            elif "Virtual Environment" in check:
                recommendations.append(("🚪 Exit current venv", "deactivate && source env.sh"))

        # Remove duplicates and print
        seen = set()
        for title, command in recommendations:
            key = command
            if key not in seen:
                self.console.print(f"\n  {title}")
                self.console.print(f"  [dim]$ {command}[/dim]")
                seen.add(key)

        # If multiple env.sh failures, suggest bug report
        env_failure_count = sum(1 for check, _ in self.checks_failed if "env.sh" in check)
        if env_failure_count >= 3:
            self.console.print(
                "\n[yellow]⚠️  Multiple env.sh issues detected. "
                "If regenerating doesn't help, this may be a bug in wrknv.[/yellow]"
            )
            self.console.print("  [dim]Please report at: https://github.com/provide-io/wrknv/issues[/dim]")

    def xǁWrknvDoctorǁ_print_recommendations__mutmut_45(self) -> None:
        """Print recommendations based on failed checks."""
        self.console.print("\n[bold]Recommendations:[/bold]")

        recommendations = []

        # Priority 1: Fix critical failures
        has_env_issues = False
        for check, _details in self.checks_failed:
            if "env.sh" in check or "Workenv Path" in check:
                has_env_issues = True
            elif "uv" in check:
                recommendations.append(("🔧 Install uv", "curl -LsSf https://astral.sh/uv/install.sh | sh"))
            elif "wrknv Installation" in check:
                recommendations.append(("🔧 Reinstall wrknv", "See installation docs"))

        # If there are any env.sh issues, recommend regeneration
        if has_env_issues:
            recommendations.insert(0, ("🔄 regenerate env.sh", "wrknv generate --force"))
            recommendations.append(("📝 After regenerating", "source env.sh"))

        # Priority 2: Address warnings
        for check, _details in self.checks_warning:
            if ".venv Directory" in check:
                recommendations.append(("🗑️  Remove .venv", "rm -rf .venv && source env.sh"))
            elif "workenv Directory" in check and not has_env_issues:
                recommendations.append(("🔧 Check workenv", "wrknv doctor"))
            elif "Virtual Environment" in check:
                recommendations.append(("🚪 Exit current venv", "deactivate && source env.sh"))

        # Remove duplicates and print
        seen = set()
        for title, command in recommendations:
            key = command
            if key not in seen:
                self.console.print(f"\n  {title}")
                self.console.print(f"  [dim]$ {command}[/dim]")
                seen.add(key)

        # If multiple env.sh failures, suggest bug report
        env_failure_count = sum(1 for check, _ in self.checks_failed if "env.sh" in check)
        if env_failure_count >= 3:
            self.console.print(
                "\n[yellow]⚠️  Multiple env.sh issues detected. "
                "If regenerating doesn't help, this may be a bug in wrknv.[/yellow]"
            )
            self.console.print("  [dim]Please report at: https://github.com/provide-io/wrknv/issues[/dim]")

    def xǁWrknvDoctorǁ_print_recommendations__mutmut_46(self) -> None:
        """Print recommendations based on failed checks."""
        self.console.print("\n[bold]Recommendations:[/bold]")

        recommendations = []

        # Priority 1: Fix critical failures
        has_env_issues = False
        for check, _details in self.checks_failed:
            if "env.sh" in check or "Workenv Path" in check:
                has_env_issues = True
            elif "uv" in check:
                recommendations.append(("🔧 Install uv", "curl -LsSf https://astral.sh/uv/install.sh | sh"))
            elif "wrknv Installation" in check:
                recommendations.append(("🔧 Reinstall wrknv", "See installation docs"))

        # If there are any env.sh issues, recommend regeneration
        if has_env_issues:
            recommendations.insert(0, ("🔄 REGENERATE ENV.SH", "wrknv generate --force"))
            recommendations.append(("📝 After regenerating", "source env.sh"))

        # Priority 2: Address warnings
        for check, _details in self.checks_warning:
            if ".venv Directory" in check:
                recommendations.append(("🗑️  Remove .venv", "rm -rf .venv && source env.sh"))
            elif "workenv Directory" in check and not has_env_issues:
                recommendations.append(("🔧 Check workenv", "wrknv doctor"))
            elif "Virtual Environment" in check:
                recommendations.append(("🚪 Exit current venv", "deactivate && source env.sh"))

        # Remove duplicates and print
        seen = set()
        for title, command in recommendations:
            key = command
            if key not in seen:
                self.console.print(f"\n  {title}")
                self.console.print(f"  [dim]$ {command}[/dim]")
                seen.add(key)

        # If multiple env.sh failures, suggest bug report
        env_failure_count = sum(1 for check, _ in self.checks_failed if "env.sh" in check)
        if env_failure_count >= 3:
            self.console.print(
                "\n[yellow]⚠️  Multiple env.sh issues detected. "
                "If regenerating doesn't help, this may be a bug in wrknv.[/yellow]"
            )
            self.console.print("  [dim]Please report at: https://github.com/provide-io/wrknv/issues[/dim]")

    def xǁWrknvDoctorǁ_print_recommendations__mutmut_47(self) -> None:
        """Print recommendations based on failed checks."""
        self.console.print("\n[bold]Recommendations:[/bold]")

        recommendations = []

        # Priority 1: Fix critical failures
        has_env_issues = False
        for check, _details in self.checks_failed:
            if "env.sh" in check or "Workenv Path" in check:
                has_env_issues = True
            elif "uv" in check:
                recommendations.append(("🔧 Install uv", "curl -LsSf https://astral.sh/uv/install.sh | sh"))
            elif "wrknv Installation" in check:
                recommendations.append(("🔧 Reinstall wrknv", "See installation docs"))

        # If there are any env.sh issues, recommend regeneration
        if has_env_issues:
            recommendations.insert(0, ("🔄 Regenerate env.sh", "XXwrknv generate --forceXX"))
            recommendations.append(("📝 After regenerating", "source env.sh"))

        # Priority 2: Address warnings
        for check, _details in self.checks_warning:
            if ".venv Directory" in check:
                recommendations.append(("🗑️  Remove .venv", "rm -rf .venv && source env.sh"))
            elif "workenv Directory" in check and not has_env_issues:
                recommendations.append(("🔧 Check workenv", "wrknv doctor"))
            elif "Virtual Environment" in check:
                recommendations.append(("🚪 Exit current venv", "deactivate && source env.sh"))

        # Remove duplicates and print
        seen = set()
        for title, command in recommendations:
            key = command
            if key not in seen:
                self.console.print(f"\n  {title}")
                self.console.print(f"  [dim]$ {command}[/dim]")
                seen.add(key)

        # If multiple env.sh failures, suggest bug report
        env_failure_count = sum(1 for check, _ in self.checks_failed if "env.sh" in check)
        if env_failure_count >= 3:
            self.console.print(
                "\n[yellow]⚠️  Multiple env.sh issues detected. "
                "If regenerating doesn't help, this may be a bug in wrknv.[/yellow]"
            )
            self.console.print("  [dim]Please report at: https://github.com/provide-io/wrknv/issues[/dim]")

    def xǁWrknvDoctorǁ_print_recommendations__mutmut_48(self) -> None:
        """Print recommendations based on failed checks."""
        self.console.print("\n[bold]Recommendations:[/bold]")

        recommendations = []

        # Priority 1: Fix critical failures
        has_env_issues = False
        for check, _details in self.checks_failed:
            if "env.sh" in check or "Workenv Path" in check:
                has_env_issues = True
            elif "uv" in check:
                recommendations.append(("🔧 Install uv", "curl -LsSf https://astral.sh/uv/install.sh | sh"))
            elif "wrknv Installation" in check:
                recommendations.append(("🔧 Reinstall wrknv", "See installation docs"))

        # If there are any env.sh issues, recommend regeneration
        if has_env_issues:
            recommendations.insert(0, ("🔄 Regenerate env.sh", "WRKNV GENERATE --FORCE"))
            recommendations.append(("📝 After regenerating", "source env.sh"))

        # Priority 2: Address warnings
        for check, _details in self.checks_warning:
            if ".venv Directory" in check:
                recommendations.append(("🗑️  Remove .venv", "rm -rf .venv && source env.sh"))
            elif "workenv Directory" in check and not has_env_issues:
                recommendations.append(("🔧 Check workenv", "wrknv doctor"))
            elif "Virtual Environment" in check:
                recommendations.append(("🚪 Exit current venv", "deactivate && source env.sh"))

        # Remove duplicates and print
        seen = set()
        for title, command in recommendations:
            key = command
            if key not in seen:
                self.console.print(f"\n  {title}")
                self.console.print(f"  [dim]$ {command}[/dim]")
                seen.add(key)

        # If multiple env.sh failures, suggest bug report
        env_failure_count = sum(1 for check, _ in self.checks_failed if "env.sh" in check)
        if env_failure_count >= 3:
            self.console.print(
                "\n[yellow]⚠️  Multiple env.sh issues detected. "
                "If regenerating doesn't help, this may be a bug in wrknv.[/yellow]"
            )
            self.console.print("  [dim]Please report at: https://github.com/provide-io/wrknv/issues[/dim]")

    def xǁWrknvDoctorǁ_print_recommendations__mutmut_49(self) -> None:
        """Print recommendations based on failed checks."""
        self.console.print("\n[bold]Recommendations:[/bold]")

        recommendations = []

        # Priority 1: Fix critical failures
        has_env_issues = False
        for check, _details in self.checks_failed:
            if "env.sh" in check or "Workenv Path" in check:
                has_env_issues = True
            elif "uv" in check:
                recommendations.append(("🔧 Install uv", "curl -LsSf https://astral.sh/uv/install.sh | sh"))
            elif "wrknv Installation" in check:
                recommendations.append(("🔧 Reinstall wrknv", "See installation docs"))

        # If there are any env.sh issues, recommend regeneration
        if has_env_issues:
            recommendations.insert(0, ("🔄 Regenerate env.sh", "wrknv generate --force"))
            recommendations.append(None)

        # Priority 2: Address warnings
        for check, _details in self.checks_warning:
            if ".venv Directory" in check:
                recommendations.append(("🗑️  Remove .venv", "rm -rf .venv && source env.sh"))
            elif "workenv Directory" in check and not has_env_issues:
                recommendations.append(("🔧 Check workenv", "wrknv doctor"))
            elif "Virtual Environment" in check:
                recommendations.append(("🚪 Exit current venv", "deactivate && source env.sh"))

        # Remove duplicates and print
        seen = set()
        for title, command in recommendations:
            key = command
            if key not in seen:
                self.console.print(f"\n  {title}")
                self.console.print(f"  [dim]$ {command}[/dim]")
                seen.add(key)

        # If multiple env.sh failures, suggest bug report
        env_failure_count = sum(1 for check, _ in self.checks_failed if "env.sh" in check)
        if env_failure_count >= 3:
            self.console.print(
                "\n[yellow]⚠️  Multiple env.sh issues detected. "
                "If regenerating doesn't help, this may be a bug in wrknv.[/yellow]"
            )
            self.console.print("  [dim]Please report at: https://github.com/provide-io/wrknv/issues[/dim]")

    def xǁWrknvDoctorǁ_print_recommendations__mutmut_50(self) -> None:
        """Print recommendations based on failed checks."""
        self.console.print("\n[bold]Recommendations:[/bold]")

        recommendations = []

        # Priority 1: Fix critical failures
        has_env_issues = False
        for check, _details in self.checks_failed:
            if "env.sh" in check or "Workenv Path" in check:
                has_env_issues = True
            elif "uv" in check:
                recommendations.append(("🔧 Install uv", "curl -LsSf https://astral.sh/uv/install.sh | sh"))
            elif "wrknv Installation" in check:
                recommendations.append(("🔧 Reinstall wrknv", "See installation docs"))

        # If there are any env.sh issues, recommend regeneration
        if has_env_issues:
            recommendations.insert(0, ("🔄 Regenerate env.sh", "wrknv generate --force"))
            recommendations.append(("XX📝 After regeneratingXX", "source env.sh"))

        # Priority 2: Address warnings
        for check, _details in self.checks_warning:
            if ".venv Directory" in check:
                recommendations.append(("🗑️  Remove .venv", "rm -rf .venv && source env.sh"))
            elif "workenv Directory" in check and not has_env_issues:
                recommendations.append(("🔧 Check workenv", "wrknv doctor"))
            elif "Virtual Environment" in check:
                recommendations.append(("🚪 Exit current venv", "deactivate && source env.sh"))

        # Remove duplicates and print
        seen = set()
        for title, command in recommendations:
            key = command
            if key not in seen:
                self.console.print(f"\n  {title}")
                self.console.print(f"  [dim]$ {command}[/dim]")
                seen.add(key)

        # If multiple env.sh failures, suggest bug report
        env_failure_count = sum(1 for check, _ in self.checks_failed if "env.sh" in check)
        if env_failure_count >= 3:
            self.console.print(
                "\n[yellow]⚠️  Multiple env.sh issues detected. "
                "If regenerating doesn't help, this may be a bug in wrknv.[/yellow]"
            )
            self.console.print("  [dim]Please report at: https://github.com/provide-io/wrknv/issues[/dim]")

    def xǁWrknvDoctorǁ_print_recommendations__mutmut_51(self) -> None:
        """Print recommendations based on failed checks."""
        self.console.print("\n[bold]Recommendations:[/bold]")

        recommendations = []

        # Priority 1: Fix critical failures
        has_env_issues = False
        for check, _details in self.checks_failed:
            if "env.sh" in check or "Workenv Path" in check:
                has_env_issues = True
            elif "uv" in check:
                recommendations.append(("🔧 Install uv", "curl -LsSf https://astral.sh/uv/install.sh | sh"))
            elif "wrknv Installation" in check:
                recommendations.append(("🔧 Reinstall wrknv", "See installation docs"))

        # If there are any env.sh issues, recommend regeneration
        if has_env_issues:
            recommendations.insert(0, ("🔄 Regenerate env.sh", "wrknv generate --force"))
            recommendations.append(("📝 after regenerating", "source env.sh"))

        # Priority 2: Address warnings
        for check, _details in self.checks_warning:
            if ".venv Directory" in check:
                recommendations.append(("🗑️  Remove .venv", "rm -rf .venv && source env.sh"))
            elif "workenv Directory" in check and not has_env_issues:
                recommendations.append(("🔧 Check workenv", "wrknv doctor"))
            elif "Virtual Environment" in check:
                recommendations.append(("🚪 Exit current venv", "deactivate && source env.sh"))

        # Remove duplicates and print
        seen = set()
        for title, command in recommendations:
            key = command
            if key not in seen:
                self.console.print(f"\n  {title}")
                self.console.print(f"  [dim]$ {command}[/dim]")
                seen.add(key)

        # If multiple env.sh failures, suggest bug report
        env_failure_count = sum(1 for check, _ in self.checks_failed if "env.sh" in check)
        if env_failure_count >= 3:
            self.console.print(
                "\n[yellow]⚠️  Multiple env.sh issues detected. "
                "If regenerating doesn't help, this may be a bug in wrknv.[/yellow]"
            )
            self.console.print("  [dim]Please report at: https://github.com/provide-io/wrknv/issues[/dim]")

    def xǁWrknvDoctorǁ_print_recommendations__mutmut_52(self) -> None:
        """Print recommendations based on failed checks."""
        self.console.print("\n[bold]Recommendations:[/bold]")

        recommendations = []

        # Priority 1: Fix critical failures
        has_env_issues = False
        for check, _details in self.checks_failed:
            if "env.sh" in check or "Workenv Path" in check:
                has_env_issues = True
            elif "uv" in check:
                recommendations.append(("🔧 Install uv", "curl -LsSf https://astral.sh/uv/install.sh | sh"))
            elif "wrknv Installation" in check:
                recommendations.append(("🔧 Reinstall wrknv", "See installation docs"))

        # If there are any env.sh issues, recommend regeneration
        if has_env_issues:
            recommendations.insert(0, ("🔄 Regenerate env.sh", "wrknv generate --force"))
            recommendations.append(("📝 AFTER REGENERATING", "source env.sh"))

        # Priority 2: Address warnings
        for check, _details in self.checks_warning:
            if ".venv Directory" in check:
                recommendations.append(("🗑️  Remove .venv", "rm -rf .venv && source env.sh"))
            elif "workenv Directory" in check and not has_env_issues:
                recommendations.append(("🔧 Check workenv", "wrknv doctor"))
            elif "Virtual Environment" in check:
                recommendations.append(("🚪 Exit current venv", "deactivate && source env.sh"))

        # Remove duplicates and print
        seen = set()
        for title, command in recommendations:
            key = command
            if key not in seen:
                self.console.print(f"\n  {title}")
                self.console.print(f"  [dim]$ {command}[/dim]")
                seen.add(key)

        # If multiple env.sh failures, suggest bug report
        env_failure_count = sum(1 for check, _ in self.checks_failed if "env.sh" in check)
        if env_failure_count >= 3:
            self.console.print(
                "\n[yellow]⚠️  Multiple env.sh issues detected. "
                "If regenerating doesn't help, this may be a bug in wrknv.[/yellow]"
            )
            self.console.print("  [dim]Please report at: https://github.com/provide-io/wrknv/issues[/dim]")

    def xǁWrknvDoctorǁ_print_recommendations__mutmut_53(self) -> None:
        """Print recommendations based on failed checks."""
        self.console.print("\n[bold]Recommendations:[/bold]")

        recommendations = []

        # Priority 1: Fix critical failures
        has_env_issues = False
        for check, _details in self.checks_failed:
            if "env.sh" in check or "Workenv Path" in check:
                has_env_issues = True
            elif "uv" in check:
                recommendations.append(("🔧 Install uv", "curl -LsSf https://astral.sh/uv/install.sh | sh"))
            elif "wrknv Installation" in check:
                recommendations.append(("🔧 Reinstall wrknv", "See installation docs"))

        # If there are any env.sh issues, recommend regeneration
        if has_env_issues:
            recommendations.insert(0, ("🔄 Regenerate env.sh", "wrknv generate --force"))
            recommendations.append(("📝 After regenerating", "XXsource env.shXX"))

        # Priority 2: Address warnings
        for check, _details in self.checks_warning:
            if ".venv Directory" in check:
                recommendations.append(("🗑️  Remove .venv", "rm -rf .venv && source env.sh"))
            elif "workenv Directory" in check and not has_env_issues:
                recommendations.append(("🔧 Check workenv", "wrknv doctor"))
            elif "Virtual Environment" in check:
                recommendations.append(("🚪 Exit current venv", "deactivate && source env.sh"))

        # Remove duplicates and print
        seen = set()
        for title, command in recommendations:
            key = command
            if key not in seen:
                self.console.print(f"\n  {title}")
                self.console.print(f"  [dim]$ {command}[/dim]")
                seen.add(key)

        # If multiple env.sh failures, suggest bug report
        env_failure_count = sum(1 for check, _ in self.checks_failed if "env.sh" in check)
        if env_failure_count >= 3:
            self.console.print(
                "\n[yellow]⚠️  Multiple env.sh issues detected. "
                "If regenerating doesn't help, this may be a bug in wrknv.[/yellow]"
            )
            self.console.print("  [dim]Please report at: https://github.com/provide-io/wrknv/issues[/dim]")

    def xǁWrknvDoctorǁ_print_recommendations__mutmut_54(self) -> None:
        """Print recommendations based on failed checks."""
        self.console.print("\n[bold]Recommendations:[/bold]")

        recommendations = []

        # Priority 1: Fix critical failures
        has_env_issues = False
        for check, _details in self.checks_failed:
            if "env.sh" in check or "Workenv Path" in check:
                has_env_issues = True
            elif "uv" in check:
                recommendations.append(("🔧 Install uv", "curl -LsSf https://astral.sh/uv/install.sh | sh"))
            elif "wrknv Installation" in check:
                recommendations.append(("🔧 Reinstall wrknv", "See installation docs"))

        # If there are any env.sh issues, recommend regeneration
        if has_env_issues:
            recommendations.insert(0, ("🔄 Regenerate env.sh", "wrknv generate --force"))
            recommendations.append(("📝 After regenerating", "SOURCE ENV.SH"))

        # Priority 2: Address warnings
        for check, _details in self.checks_warning:
            if ".venv Directory" in check:
                recommendations.append(("🗑️  Remove .venv", "rm -rf .venv && source env.sh"))
            elif "workenv Directory" in check and not has_env_issues:
                recommendations.append(("🔧 Check workenv", "wrknv doctor"))
            elif "Virtual Environment" in check:
                recommendations.append(("🚪 Exit current venv", "deactivate && source env.sh"))

        # Remove duplicates and print
        seen = set()
        for title, command in recommendations:
            key = command
            if key not in seen:
                self.console.print(f"\n  {title}")
                self.console.print(f"  [dim]$ {command}[/dim]")
                seen.add(key)

        # If multiple env.sh failures, suggest bug report
        env_failure_count = sum(1 for check, _ in self.checks_failed if "env.sh" in check)
        if env_failure_count >= 3:
            self.console.print(
                "\n[yellow]⚠️  Multiple env.sh issues detected. "
                "If regenerating doesn't help, this may be a bug in wrknv.[/yellow]"
            )
            self.console.print("  [dim]Please report at: https://github.com/provide-io/wrknv/issues[/dim]")

    def xǁWrknvDoctorǁ_print_recommendations__mutmut_55(self) -> None:
        """Print recommendations based on failed checks."""
        self.console.print("\n[bold]Recommendations:[/bold]")

        recommendations = []

        # Priority 1: Fix critical failures
        has_env_issues = False
        for check, _details in self.checks_failed:
            if "env.sh" in check or "Workenv Path" in check:
                has_env_issues = True
            elif "uv" in check:
                recommendations.append(("🔧 Install uv", "curl -LsSf https://astral.sh/uv/install.sh | sh"))
            elif "wrknv Installation" in check:
                recommendations.append(("🔧 Reinstall wrknv", "See installation docs"))

        # If there are any env.sh issues, recommend regeneration
        if has_env_issues:
            recommendations.insert(0, ("🔄 Regenerate env.sh", "wrknv generate --force"))
            recommendations.append(("📝 After regenerating", "source env.sh"))

        # Priority 2: Address warnings
        for check, _details in self.checks_warning:
            if "XX.venv DirectoryXX" in check:
                recommendations.append(("🗑️  Remove .venv", "rm -rf .venv && source env.sh"))
            elif "workenv Directory" in check and not has_env_issues:
                recommendations.append(("🔧 Check workenv", "wrknv doctor"))
            elif "Virtual Environment" in check:
                recommendations.append(("🚪 Exit current venv", "deactivate && source env.sh"))

        # Remove duplicates and print
        seen = set()
        for title, command in recommendations:
            key = command
            if key not in seen:
                self.console.print(f"\n  {title}")
                self.console.print(f"  [dim]$ {command}[/dim]")
                seen.add(key)

        # If multiple env.sh failures, suggest bug report
        env_failure_count = sum(1 for check, _ in self.checks_failed if "env.sh" in check)
        if env_failure_count >= 3:
            self.console.print(
                "\n[yellow]⚠️  Multiple env.sh issues detected. "
                "If regenerating doesn't help, this may be a bug in wrknv.[/yellow]"
            )
            self.console.print("  [dim]Please report at: https://github.com/provide-io/wrknv/issues[/dim]")

    def xǁWrknvDoctorǁ_print_recommendations__mutmut_56(self) -> None:
        """Print recommendations based on failed checks."""
        self.console.print("\n[bold]Recommendations:[/bold]")

        recommendations = []

        # Priority 1: Fix critical failures
        has_env_issues = False
        for check, _details in self.checks_failed:
            if "env.sh" in check or "Workenv Path" in check:
                has_env_issues = True
            elif "uv" in check:
                recommendations.append(("🔧 Install uv", "curl -LsSf https://astral.sh/uv/install.sh | sh"))
            elif "wrknv Installation" in check:
                recommendations.append(("🔧 Reinstall wrknv", "See installation docs"))

        # If there are any env.sh issues, recommend regeneration
        if has_env_issues:
            recommendations.insert(0, ("🔄 Regenerate env.sh", "wrknv generate --force"))
            recommendations.append(("📝 After regenerating", "source env.sh"))

        # Priority 2: Address warnings
        for check, _details in self.checks_warning:
            if ".venv directory" in check:
                recommendations.append(("🗑️  Remove .venv", "rm -rf .venv && source env.sh"))
            elif "workenv Directory" in check and not has_env_issues:
                recommendations.append(("🔧 Check workenv", "wrknv doctor"))
            elif "Virtual Environment" in check:
                recommendations.append(("🚪 Exit current venv", "deactivate && source env.sh"))

        # Remove duplicates and print
        seen = set()
        for title, command in recommendations:
            key = command
            if key not in seen:
                self.console.print(f"\n  {title}")
                self.console.print(f"  [dim]$ {command}[/dim]")
                seen.add(key)

        # If multiple env.sh failures, suggest bug report
        env_failure_count = sum(1 for check, _ in self.checks_failed if "env.sh" in check)
        if env_failure_count >= 3:
            self.console.print(
                "\n[yellow]⚠️  Multiple env.sh issues detected. "
                "If regenerating doesn't help, this may be a bug in wrknv.[/yellow]"
            )
            self.console.print("  [dim]Please report at: https://github.com/provide-io/wrknv/issues[/dim]")

    def xǁWrknvDoctorǁ_print_recommendations__mutmut_57(self) -> None:
        """Print recommendations based on failed checks."""
        self.console.print("\n[bold]Recommendations:[/bold]")

        recommendations = []

        # Priority 1: Fix critical failures
        has_env_issues = False
        for check, _details in self.checks_failed:
            if "env.sh" in check or "Workenv Path" in check:
                has_env_issues = True
            elif "uv" in check:
                recommendations.append(("🔧 Install uv", "curl -LsSf https://astral.sh/uv/install.sh | sh"))
            elif "wrknv Installation" in check:
                recommendations.append(("🔧 Reinstall wrknv", "See installation docs"))

        # If there are any env.sh issues, recommend regeneration
        if has_env_issues:
            recommendations.insert(0, ("🔄 Regenerate env.sh", "wrknv generate --force"))
            recommendations.append(("📝 After regenerating", "source env.sh"))

        # Priority 2: Address warnings
        for check, _details in self.checks_warning:
            if ".VENV DIRECTORY" in check:
                recommendations.append(("🗑️  Remove .venv", "rm -rf .venv && source env.sh"))
            elif "workenv Directory" in check and not has_env_issues:
                recommendations.append(("🔧 Check workenv", "wrknv doctor"))
            elif "Virtual Environment" in check:
                recommendations.append(("🚪 Exit current venv", "deactivate && source env.sh"))

        # Remove duplicates and print
        seen = set()
        for title, command in recommendations:
            key = command
            if key not in seen:
                self.console.print(f"\n  {title}")
                self.console.print(f"  [dim]$ {command}[/dim]")
                seen.add(key)

        # If multiple env.sh failures, suggest bug report
        env_failure_count = sum(1 for check, _ in self.checks_failed if "env.sh" in check)
        if env_failure_count >= 3:
            self.console.print(
                "\n[yellow]⚠️  Multiple env.sh issues detected. "
                "If regenerating doesn't help, this may be a bug in wrknv.[/yellow]"
            )
            self.console.print("  [dim]Please report at: https://github.com/provide-io/wrknv/issues[/dim]")

    def xǁWrknvDoctorǁ_print_recommendations__mutmut_58(self) -> None:
        """Print recommendations based on failed checks."""
        self.console.print("\n[bold]Recommendations:[/bold]")

        recommendations = []

        # Priority 1: Fix critical failures
        has_env_issues = False
        for check, _details in self.checks_failed:
            if "env.sh" in check or "Workenv Path" in check:
                has_env_issues = True
            elif "uv" in check:
                recommendations.append(("🔧 Install uv", "curl -LsSf https://astral.sh/uv/install.sh | sh"))
            elif "wrknv Installation" in check:
                recommendations.append(("🔧 Reinstall wrknv", "See installation docs"))

        # If there are any env.sh issues, recommend regeneration
        if has_env_issues:
            recommendations.insert(0, ("🔄 Regenerate env.sh", "wrknv generate --force"))
            recommendations.append(("📝 After regenerating", "source env.sh"))

        # Priority 2: Address warnings
        for check, _details in self.checks_warning:
            if ".venv Directory" not in check:
                recommendations.append(("🗑️  Remove .venv", "rm -rf .venv && source env.sh"))
            elif "workenv Directory" in check and not has_env_issues:
                recommendations.append(("🔧 Check workenv", "wrknv doctor"))
            elif "Virtual Environment" in check:
                recommendations.append(("🚪 Exit current venv", "deactivate && source env.sh"))

        # Remove duplicates and print
        seen = set()
        for title, command in recommendations:
            key = command
            if key not in seen:
                self.console.print(f"\n  {title}")
                self.console.print(f"  [dim]$ {command}[/dim]")
                seen.add(key)

        # If multiple env.sh failures, suggest bug report
        env_failure_count = sum(1 for check, _ in self.checks_failed if "env.sh" in check)
        if env_failure_count >= 3:
            self.console.print(
                "\n[yellow]⚠️  Multiple env.sh issues detected. "
                "If regenerating doesn't help, this may be a bug in wrknv.[/yellow]"
            )
            self.console.print("  [dim]Please report at: https://github.com/provide-io/wrknv/issues[/dim]")

    def xǁWrknvDoctorǁ_print_recommendations__mutmut_59(self) -> None:
        """Print recommendations based on failed checks."""
        self.console.print("\n[bold]Recommendations:[/bold]")

        recommendations = []

        # Priority 1: Fix critical failures
        has_env_issues = False
        for check, _details in self.checks_failed:
            if "env.sh" in check or "Workenv Path" in check:
                has_env_issues = True
            elif "uv" in check:
                recommendations.append(("🔧 Install uv", "curl -LsSf https://astral.sh/uv/install.sh | sh"))
            elif "wrknv Installation" in check:
                recommendations.append(("🔧 Reinstall wrknv", "See installation docs"))

        # If there are any env.sh issues, recommend regeneration
        if has_env_issues:
            recommendations.insert(0, ("🔄 Regenerate env.sh", "wrknv generate --force"))
            recommendations.append(("📝 After regenerating", "source env.sh"))

        # Priority 2: Address warnings
        for check, _details in self.checks_warning:
            if ".venv Directory" in check:
                recommendations.append(None)
            elif "workenv Directory" in check and not has_env_issues:
                recommendations.append(("🔧 Check workenv", "wrknv doctor"))
            elif "Virtual Environment" in check:
                recommendations.append(("🚪 Exit current venv", "deactivate && source env.sh"))

        # Remove duplicates and print
        seen = set()
        for title, command in recommendations:
            key = command
            if key not in seen:
                self.console.print(f"\n  {title}")
                self.console.print(f"  [dim]$ {command}[/dim]")
                seen.add(key)

        # If multiple env.sh failures, suggest bug report
        env_failure_count = sum(1 for check, _ in self.checks_failed if "env.sh" in check)
        if env_failure_count >= 3:
            self.console.print(
                "\n[yellow]⚠️  Multiple env.sh issues detected. "
                "If regenerating doesn't help, this may be a bug in wrknv.[/yellow]"
            )
            self.console.print("  [dim]Please report at: https://github.com/provide-io/wrknv/issues[/dim]")

    def xǁWrknvDoctorǁ_print_recommendations__mutmut_60(self) -> None:
        """Print recommendations based on failed checks."""
        self.console.print("\n[bold]Recommendations:[/bold]")

        recommendations = []

        # Priority 1: Fix critical failures
        has_env_issues = False
        for check, _details in self.checks_failed:
            if "env.sh" in check or "Workenv Path" in check:
                has_env_issues = True
            elif "uv" in check:
                recommendations.append(("🔧 Install uv", "curl -LsSf https://astral.sh/uv/install.sh | sh"))
            elif "wrknv Installation" in check:
                recommendations.append(("🔧 Reinstall wrknv", "See installation docs"))

        # If there are any env.sh issues, recommend regeneration
        if has_env_issues:
            recommendations.insert(0, ("🔄 Regenerate env.sh", "wrknv generate --force"))
            recommendations.append(("📝 After regenerating", "source env.sh"))

        # Priority 2: Address warnings
        for check, _details in self.checks_warning:
            if ".venv Directory" in check:
                recommendations.append(("XX🗑️  Remove .venvXX", "rm -rf .venv && source env.sh"))
            elif "workenv Directory" in check and not has_env_issues:
                recommendations.append(("🔧 Check workenv", "wrknv doctor"))
            elif "Virtual Environment" in check:
                recommendations.append(("🚪 Exit current venv", "deactivate && source env.sh"))

        # Remove duplicates and print
        seen = set()
        for title, command in recommendations:
            key = command
            if key not in seen:
                self.console.print(f"\n  {title}")
                self.console.print(f"  [dim]$ {command}[/dim]")
                seen.add(key)

        # If multiple env.sh failures, suggest bug report
        env_failure_count = sum(1 for check, _ in self.checks_failed if "env.sh" in check)
        if env_failure_count >= 3:
            self.console.print(
                "\n[yellow]⚠️  Multiple env.sh issues detected. "
                "If regenerating doesn't help, this may be a bug in wrknv.[/yellow]"
            )
            self.console.print("  [dim]Please report at: https://github.com/provide-io/wrknv/issues[/dim]")

    def xǁWrknvDoctorǁ_print_recommendations__mutmut_61(self) -> None:
        """Print recommendations based on failed checks."""
        self.console.print("\n[bold]Recommendations:[/bold]")

        recommendations = []

        # Priority 1: Fix critical failures
        has_env_issues = False
        for check, _details in self.checks_failed:
            if "env.sh" in check or "Workenv Path" in check:
                has_env_issues = True
            elif "uv" in check:
                recommendations.append(("🔧 Install uv", "curl -LsSf https://astral.sh/uv/install.sh | sh"))
            elif "wrknv Installation" in check:
                recommendations.append(("🔧 Reinstall wrknv", "See installation docs"))

        # If there are any env.sh issues, recommend regeneration
        if has_env_issues:
            recommendations.insert(0, ("🔄 Regenerate env.sh", "wrknv generate --force"))
            recommendations.append(("📝 After regenerating", "source env.sh"))

        # Priority 2: Address warnings
        for check, _details in self.checks_warning:
            if ".venv Directory" in check:
                recommendations.append(("🗑️  remove .venv", "rm -rf .venv && source env.sh"))
            elif "workenv Directory" in check and not has_env_issues:
                recommendations.append(("🔧 Check workenv", "wrknv doctor"))
            elif "Virtual Environment" in check:
                recommendations.append(("🚪 Exit current venv", "deactivate && source env.sh"))

        # Remove duplicates and print
        seen = set()
        for title, command in recommendations:
            key = command
            if key not in seen:
                self.console.print(f"\n  {title}")
                self.console.print(f"  [dim]$ {command}[/dim]")
                seen.add(key)

        # If multiple env.sh failures, suggest bug report
        env_failure_count = sum(1 for check, _ in self.checks_failed if "env.sh" in check)
        if env_failure_count >= 3:
            self.console.print(
                "\n[yellow]⚠️  Multiple env.sh issues detected. "
                "If regenerating doesn't help, this may be a bug in wrknv.[/yellow]"
            )
            self.console.print("  [dim]Please report at: https://github.com/provide-io/wrknv/issues[/dim]")

    def xǁWrknvDoctorǁ_print_recommendations__mutmut_62(self) -> None:
        """Print recommendations based on failed checks."""
        self.console.print("\n[bold]Recommendations:[/bold]")

        recommendations = []

        # Priority 1: Fix critical failures
        has_env_issues = False
        for check, _details in self.checks_failed:
            if "env.sh" in check or "Workenv Path" in check:
                has_env_issues = True
            elif "uv" in check:
                recommendations.append(("🔧 Install uv", "curl -LsSf https://astral.sh/uv/install.sh | sh"))
            elif "wrknv Installation" in check:
                recommendations.append(("🔧 Reinstall wrknv", "See installation docs"))

        # If there are any env.sh issues, recommend regeneration
        if has_env_issues:
            recommendations.insert(0, ("🔄 Regenerate env.sh", "wrknv generate --force"))
            recommendations.append(("📝 After regenerating", "source env.sh"))

        # Priority 2: Address warnings
        for check, _details in self.checks_warning:
            if ".venv Directory" in check:
                recommendations.append(("🗑️  REMOVE .VENV", "rm -rf .venv && source env.sh"))
            elif "workenv Directory" in check and not has_env_issues:
                recommendations.append(("🔧 Check workenv", "wrknv doctor"))
            elif "Virtual Environment" in check:
                recommendations.append(("🚪 Exit current venv", "deactivate && source env.sh"))

        # Remove duplicates and print
        seen = set()
        for title, command in recommendations:
            key = command
            if key not in seen:
                self.console.print(f"\n  {title}")
                self.console.print(f"  [dim]$ {command}[/dim]")
                seen.add(key)

        # If multiple env.sh failures, suggest bug report
        env_failure_count = sum(1 for check, _ in self.checks_failed if "env.sh" in check)
        if env_failure_count >= 3:
            self.console.print(
                "\n[yellow]⚠️  Multiple env.sh issues detected. "
                "If regenerating doesn't help, this may be a bug in wrknv.[/yellow]"
            )
            self.console.print("  [dim]Please report at: https://github.com/provide-io/wrknv/issues[/dim]")

    def xǁWrknvDoctorǁ_print_recommendations__mutmut_63(self) -> None:
        """Print recommendations based on failed checks."""
        self.console.print("\n[bold]Recommendations:[/bold]")

        recommendations = []

        # Priority 1: Fix critical failures
        has_env_issues = False
        for check, _details in self.checks_failed:
            if "env.sh" in check or "Workenv Path" in check:
                has_env_issues = True
            elif "uv" in check:
                recommendations.append(("🔧 Install uv", "curl -LsSf https://astral.sh/uv/install.sh | sh"))
            elif "wrknv Installation" in check:
                recommendations.append(("🔧 Reinstall wrknv", "See installation docs"))

        # If there are any env.sh issues, recommend regeneration
        if has_env_issues:
            recommendations.insert(0, ("🔄 Regenerate env.sh", "wrknv generate --force"))
            recommendations.append(("📝 After regenerating", "source env.sh"))

        # Priority 2: Address warnings
        for check, _details in self.checks_warning:
            if ".venv Directory" in check:
                recommendations.append(("🗑️  Remove .venv", "XXrm -rf .venv && source env.shXX"))
            elif "workenv Directory" in check and not has_env_issues:
                recommendations.append(("🔧 Check workenv", "wrknv doctor"))
            elif "Virtual Environment" in check:
                recommendations.append(("🚪 Exit current venv", "deactivate && source env.sh"))

        # Remove duplicates and print
        seen = set()
        for title, command in recommendations:
            key = command
            if key not in seen:
                self.console.print(f"\n  {title}")
                self.console.print(f"  [dim]$ {command}[/dim]")
                seen.add(key)

        # If multiple env.sh failures, suggest bug report
        env_failure_count = sum(1 for check, _ in self.checks_failed if "env.sh" in check)
        if env_failure_count >= 3:
            self.console.print(
                "\n[yellow]⚠️  Multiple env.sh issues detected. "
                "If regenerating doesn't help, this may be a bug in wrknv.[/yellow]"
            )
            self.console.print("  [dim]Please report at: https://github.com/provide-io/wrknv/issues[/dim]")

    def xǁWrknvDoctorǁ_print_recommendations__mutmut_64(self) -> None:
        """Print recommendations based on failed checks."""
        self.console.print("\n[bold]Recommendations:[/bold]")

        recommendations = []

        # Priority 1: Fix critical failures
        has_env_issues = False
        for check, _details in self.checks_failed:
            if "env.sh" in check or "Workenv Path" in check:
                has_env_issues = True
            elif "uv" in check:
                recommendations.append(("🔧 Install uv", "curl -LsSf https://astral.sh/uv/install.sh | sh"))
            elif "wrknv Installation" in check:
                recommendations.append(("🔧 Reinstall wrknv", "See installation docs"))

        # If there are any env.sh issues, recommend regeneration
        if has_env_issues:
            recommendations.insert(0, ("🔄 Regenerate env.sh", "wrknv generate --force"))
            recommendations.append(("📝 After regenerating", "source env.sh"))

        # Priority 2: Address warnings
        for check, _details in self.checks_warning:
            if ".venv Directory" in check:
                recommendations.append(("🗑️  Remove .venv", "RM -RF .VENV && SOURCE ENV.SH"))
            elif "workenv Directory" in check and not has_env_issues:
                recommendations.append(("🔧 Check workenv", "wrknv doctor"))
            elif "Virtual Environment" in check:
                recommendations.append(("🚪 Exit current venv", "deactivate && source env.sh"))

        # Remove duplicates and print
        seen = set()
        for title, command in recommendations:
            key = command
            if key not in seen:
                self.console.print(f"\n  {title}")
                self.console.print(f"  [dim]$ {command}[/dim]")
                seen.add(key)

        # If multiple env.sh failures, suggest bug report
        env_failure_count = sum(1 for check, _ in self.checks_failed if "env.sh" in check)
        if env_failure_count >= 3:
            self.console.print(
                "\n[yellow]⚠️  Multiple env.sh issues detected. "
                "If regenerating doesn't help, this may be a bug in wrknv.[/yellow]"
            )
            self.console.print("  [dim]Please report at: https://github.com/provide-io/wrknv/issues[/dim]")

    def xǁWrknvDoctorǁ_print_recommendations__mutmut_65(self) -> None:
        """Print recommendations based on failed checks."""
        self.console.print("\n[bold]Recommendations:[/bold]")

        recommendations = []

        # Priority 1: Fix critical failures
        has_env_issues = False
        for check, _details in self.checks_failed:
            if "env.sh" in check or "Workenv Path" in check:
                has_env_issues = True
            elif "uv" in check:
                recommendations.append(("🔧 Install uv", "curl -LsSf https://astral.sh/uv/install.sh | sh"))
            elif "wrknv Installation" in check:
                recommendations.append(("🔧 Reinstall wrknv", "See installation docs"))

        # If there are any env.sh issues, recommend regeneration
        if has_env_issues:
            recommendations.insert(0, ("🔄 Regenerate env.sh", "wrknv generate --force"))
            recommendations.append(("📝 After regenerating", "source env.sh"))

        # Priority 2: Address warnings
        for check, _details in self.checks_warning:
            if ".venv Directory" in check:
                recommendations.append(("🗑️  Remove .venv", "rm -rf .venv && source env.sh"))
            elif "workenv Directory" in check or not has_env_issues:
                recommendations.append(("🔧 Check workenv", "wrknv doctor"))
            elif "Virtual Environment" in check:
                recommendations.append(("🚪 Exit current venv", "deactivate && source env.sh"))

        # Remove duplicates and print
        seen = set()
        for title, command in recommendations:
            key = command
            if key not in seen:
                self.console.print(f"\n  {title}")
                self.console.print(f"  [dim]$ {command}[/dim]")
                seen.add(key)

        # If multiple env.sh failures, suggest bug report
        env_failure_count = sum(1 for check, _ in self.checks_failed if "env.sh" in check)
        if env_failure_count >= 3:
            self.console.print(
                "\n[yellow]⚠️  Multiple env.sh issues detected. "
                "If regenerating doesn't help, this may be a bug in wrknv.[/yellow]"
            )
            self.console.print("  [dim]Please report at: https://github.com/provide-io/wrknv/issues[/dim]")

    def xǁWrknvDoctorǁ_print_recommendations__mutmut_66(self) -> None:
        """Print recommendations based on failed checks."""
        self.console.print("\n[bold]Recommendations:[/bold]")

        recommendations = []

        # Priority 1: Fix critical failures
        has_env_issues = False
        for check, _details in self.checks_failed:
            if "env.sh" in check or "Workenv Path" in check:
                has_env_issues = True
            elif "uv" in check:
                recommendations.append(("🔧 Install uv", "curl -LsSf https://astral.sh/uv/install.sh | sh"))
            elif "wrknv Installation" in check:
                recommendations.append(("🔧 Reinstall wrknv", "See installation docs"))

        # If there are any env.sh issues, recommend regeneration
        if has_env_issues:
            recommendations.insert(0, ("🔄 Regenerate env.sh", "wrknv generate --force"))
            recommendations.append(("📝 After regenerating", "source env.sh"))

        # Priority 2: Address warnings
        for check, _details in self.checks_warning:
            if ".venv Directory" in check:
                recommendations.append(("🗑️  Remove .venv", "rm -rf .venv && source env.sh"))
            elif "XXworkenv DirectoryXX" in check and not has_env_issues:
                recommendations.append(("🔧 Check workenv", "wrknv doctor"))
            elif "Virtual Environment" in check:
                recommendations.append(("🚪 Exit current venv", "deactivate && source env.sh"))

        # Remove duplicates and print
        seen = set()
        for title, command in recommendations:
            key = command
            if key not in seen:
                self.console.print(f"\n  {title}")
                self.console.print(f"  [dim]$ {command}[/dim]")
                seen.add(key)

        # If multiple env.sh failures, suggest bug report
        env_failure_count = sum(1 for check, _ in self.checks_failed if "env.sh" in check)
        if env_failure_count >= 3:
            self.console.print(
                "\n[yellow]⚠️  Multiple env.sh issues detected. "
                "If regenerating doesn't help, this may be a bug in wrknv.[/yellow]"
            )
            self.console.print("  [dim]Please report at: https://github.com/provide-io/wrknv/issues[/dim]")

    def xǁWrknvDoctorǁ_print_recommendations__mutmut_67(self) -> None:
        """Print recommendations based on failed checks."""
        self.console.print("\n[bold]Recommendations:[/bold]")

        recommendations = []

        # Priority 1: Fix critical failures
        has_env_issues = False
        for check, _details in self.checks_failed:
            if "env.sh" in check or "Workenv Path" in check:
                has_env_issues = True
            elif "uv" in check:
                recommendations.append(("🔧 Install uv", "curl -LsSf https://astral.sh/uv/install.sh | sh"))
            elif "wrknv Installation" in check:
                recommendations.append(("🔧 Reinstall wrknv", "See installation docs"))

        # If there are any env.sh issues, recommend regeneration
        if has_env_issues:
            recommendations.insert(0, ("🔄 Regenerate env.sh", "wrknv generate --force"))
            recommendations.append(("📝 After regenerating", "source env.sh"))

        # Priority 2: Address warnings
        for check, _details in self.checks_warning:
            if ".venv Directory" in check:
                recommendations.append(("🗑️  Remove .venv", "rm -rf .venv && source env.sh"))
            elif "workenv directory" in check and not has_env_issues:
                recommendations.append(("🔧 Check workenv", "wrknv doctor"))
            elif "Virtual Environment" in check:
                recommendations.append(("🚪 Exit current venv", "deactivate && source env.sh"))

        # Remove duplicates and print
        seen = set()
        for title, command in recommendations:
            key = command
            if key not in seen:
                self.console.print(f"\n  {title}")
                self.console.print(f"  [dim]$ {command}[/dim]")
                seen.add(key)

        # If multiple env.sh failures, suggest bug report
        env_failure_count = sum(1 for check, _ in self.checks_failed if "env.sh" in check)
        if env_failure_count >= 3:
            self.console.print(
                "\n[yellow]⚠️  Multiple env.sh issues detected. "
                "If regenerating doesn't help, this may be a bug in wrknv.[/yellow]"
            )
            self.console.print("  [dim]Please report at: https://github.com/provide-io/wrknv/issues[/dim]")

    def xǁWrknvDoctorǁ_print_recommendations__mutmut_68(self) -> None:
        """Print recommendations based on failed checks."""
        self.console.print("\n[bold]Recommendations:[/bold]")

        recommendations = []

        # Priority 1: Fix critical failures
        has_env_issues = False
        for check, _details in self.checks_failed:
            if "env.sh" in check or "Workenv Path" in check:
                has_env_issues = True
            elif "uv" in check:
                recommendations.append(("🔧 Install uv", "curl -LsSf https://astral.sh/uv/install.sh | sh"))
            elif "wrknv Installation" in check:
                recommendations.append(("🔧 Reinstall wrknv", "See installation docs"))

        # If there are any env.sh issues, recommend regeneration
        if has_env_issues:
            recommendations.insert(0, ("🔄 Regenerate env.sh", "wrknv generate --force"))
            recommendations.append(("📝 After regenerating", "source env.sh"))

        # Priority 2: Address warnings
        for check, _details in self.checks_warning:
            if ".venv Directory" in check:
                recommendations.append(("🗑️  Remove .venv", "rm -rf .venv && source env.sh"))
            elif "WORKENV DIRECTORY" in check and not has_env_issues:
                recommendations.append(("🔧 Check workenv", "wrknv doctor"))
            elif "Virtual Environment" in check:
                recommendations.append(("🚪 Exit current venv", "deactivate && source env.sh"))

        # Remove duplicates and print
        seen = set()
        for title, command in recommendations:
            key = command
            if key not in seen:
                self.console.print(f"\n  {title}")
                self.console.print(f"  [dim]$ {command}[/dim]")
                seen.add(key)

        # If multiple env.sh failures, suggest bug report
        env_failure_count = sum(1 for check, _ in self.checks_failed if "env.sh" in check)
        if env_failure_count >= 3:
            self.console.print(
                "\n[yellow]⚠️  Multiple env.sh issues detected. "
                "If regenerating doesn't help, this may be a bug in wrknv.[/yellow]"
            )
            self.console.print("  [dim]Please report at: https://github.com/provide-io/wrknv/issues[/dim]")

    def xǁWrknvDoctorǁ_print_recommendations__mutmut_69(self) -> None:
        """Print recommendations based on failed checks."""
        self.console.print("\n[bold]Recommendations:[/bold]")

        recommendations = []

        # Priority 1: Fix critical failures
        has_env_issues = False
        for check, _details in self.checks_failed:
            if "env.sh" in check or "Workenv Path" in check:
                has_env_issues = True
            elif "uv" in check:
                recommendations.append(("🔧 Install uv", "curl -LsSf https://astral.sh/uv/install.sh | sh"))
            elif "wrknv Installation" in check:
                recommendations.append(("🔧 Reinstall wrknv", "See installation docs"))

        # If there are any env.sh issues, recommend regeneration
        if has_env_issues:
            recommendations.insert(0, ("🔄 Regenerate env.sh", "wrknv generate --force"))
            recommendations.append(("📝 After regenerating", "source env.sh"))

        # Priority 2: Address warnings
        for check, _details in self.checks_warning:
            if ".venv Directory" in check:
                recommendations.append(("🗑️  Remove .venv", "rm -rf .venv && source env.sh"))
            elif "workenv Directory" not in check and not has_env_issues:
                recommendations.append(("🔧 Check workenv", "wrknv doctor"))
            elif "Virtual Environment" in check:
                recommendations.append(("🚪 Exit current venv", "deactivate && source env.sh"))

        # Remove duplicates and print
        seen = set()
        for title, command in recommendations:
            key = command
            if key not in seen:
                self.console.print(f"\n  {title}")
                self.console.print(f"  [dim]$ {command}[/dim]")
                seen.add(key)

        # If multiple env.sh failures, suggest bug report
        env_failure_count = sum(1 for check, _ in self.checks_failed if "env.sh" in check)
        if env_failure_count >= 3:
            self.console.print(
                "\n[yellow]⚠️  Multiple env.sh issues detected. "
                "If regenerating doesn't help, this may be a bug in wrknv.[/yellow]"
            )
            self.console.print("  [dim]Please report at: https://github.com/provide-io/wrknv/issues[/dim]")

    def xǁWrknvDoctorǁ_print_recommendations__mutmut_70(self) -> None:
        """Print recommendations based on failed checks."""
        self.console.print("\n[bold]Recommendations:[/bold]")

        recommendations = []

        # Priority 1: Fix critical failures
        has_env_issues = False
        for check, _details in self.checks_failed:
            if "env.sh" in check or "Workenv Path" in check:
                has_env_issues = True
            elif "uv" in check:
                recommendations.append(("🔧 Install uv", "curl -LsSf https://astral.sh/uv/install.sh | sh"))
            elif "wrknv Installation" in check:
                recommendations.append(("🔧 Reinstall wrknv", "See installation docs"))

        # If there are any env.sh issues, recommend regeneration
        if has_env_issues:
            recommendations.insert(0, ("🔄 Regenerate env.sh", "wrknv generate --force"))
            recommendations.append(("📝 After regenerating", "source env.sh"))

        # Priority 2: Address warnings
        for check, _details in self.checks_warning:
            if ".venv Directory" in check:
                recommendations.append(("🗑️  Remove .venv", "rm -rf .venv && source env.sh"))
            elif "workenv Directory" in check and has_env_issues:
                recommendations.append(("🔧 Check workenv", "wrknv doctor"))
            elif "Virtual Environment" in check:
                recommendations.append(("🚪 Exit current venv", "deactivate && source env.sh"))

        # Remove duplicates and print
        seen = set()
        for title, command in recommendations:
            key = command
            if key not in seen:
                self.console.print(f"\n  {title}")
                self.console.print(f"  [dim]$ {command}[/dim]")
                seen.add(key)

        # If multiple env.sh failures, suggest bug report
        env_failure_count = sum(1 for check, _ in self.checks_failed if "env.sh" in check)
        if env_failure_count >= 3:
            self.console.print(
                "\n[yellow]⚠️  Multiple env.sh issues detected. "
                "If regenerating doesn't help, this may be a bug in wrknv.[/yellow]"
            )
            self.console.print("  [dim]Please report at: https://github.com/provide-io/wrknv/issues[/dim]")

    def xǁWrknvDoctorǁ_print_recommendations__mutmut_71(self) -> None:
        """Print recommendations based on failed checks."""
        self.console.print("\n[bold]Recommendations:[/bold]")

        recommendations = []

        # Priority 1: Fix critical failures
        has_env_issues = False
        for check, _details in self.checks_failed:
            if "env.sh" in check or "Workenv Path" in check:
                has_env_issues = True
            elif "uv" in check:
                recommendations.append(("🔧 Install uv", "curl -LsSf https://astral.sh/uv/install.sh | sh"))
            elif "wrknv Installation" in check:
                recommendations.append(("🔧 Reinstall wrknv", "See installation docs"))

        # If there are any env.sh issues, recommend regeneration
        if has_env_issues:
            recommendations.insert(0, ("🔄 Regenerate env.sh", "wrknv generate --force"))
            recommendations.append(("📝 After regenerating", "source env.sh"))

        # Priority 2: Address warnings
        for check, _details in self.checks_warning:
            if ".venv Directory" in check:
                recommendations.append(("🗑️  Remove .venv", "rm -rf .venv && source env.sh"))
            elif "workenv Directory" in check and not has_env_issues:
                recommendations.append(None)
            elif "Virtual Environment" in check:
                recommendations.append(("🚪 Exit current venv", "deactivate && source env.sh"))

        # Remove duplicates and print
        seen = set()
        for title, command in recommendations:
            key = command
            if key not in seen:
                self.console.print(f"\n  {title}")
                self.console.print(f"  [dim]$ {command}[/dim]")
                seen.add(key)

        # If multiple env.sh failures, suggest bug report
        env_failure_count = sum(1 for check, _ in self.checks_failed if "env.sh" in check)
        if env_failure_count >= 3:
            self.console.print(
                "\n[yellow]⚠️  Multiple env.sh issues detected. "
                "If regenerating doesn't help, this may be a bug in wrknv.[/yellow]"
            )
            self.console.print("  [dim]Please report at: https://github.com/provide-io/wrknv/issues[/dim]")

    def xǁWrknvDoctorǁ_print_recommendations__mutmut_72(self) -> None:
        """Print recommendations based on failed checks."""
        self.console.print("\n[bold]Recommendations:[/bold]")

        recommendations = []

        # Priority 1: Fix critical failures
        has_env_issues = False
        for check, _details in self.checks_failed:
            if "env.sh" in check or "Workenv Path" in check:
                has_env_issues = True
            elif "uv" in check:
                recommendations.append(("🔧 Install uv", "curl -LsSf https://astral.sh/uv/install.sh | sh"))
            elif "wrknv Installation" in check:
                recommendations.append(("🔧 Reinstall wrknv", "See installation docs"))

        # If there are any env.sh issues, recommend regeneration
        if has_env_issues:
            recommendations.insert(0, ("🔄 Regenerate env.sh", "wrknv generate --force"))
            recommendations.append(("📝 After regenerating", "source env.sh"))

        # Priority 2: Address warnings
        for check, _details in self.checks_warning:
            if ".venv Directory" in check:
                recommendations.append(("🗑️  Remove .venv", "rm -rf .venv && source env.sh"))
            elif "workenv Directory" in check and not has_env_issues:
                recommendations.append(("XX🔧 Check workenvXX", "wrknv doctor"))
            elif "Virtual Environment" in check:
                recommendations.append(("🚪 Exit current venv", "deactivate && source env.sh"))

        # Remove duplicates and print
        seen = set()
        for title, command in recommendations:
            key = command
            if key not in seen:
                self.console.print(f"\n  {title}")
                self.console.print(f"  [dim]$ {command}[/dim]")
                seen.add(key)

        # If multiple env.sh failures, suggest bug report
        env_failure_count = sum(1 for check, _ in self.checks_failed if "env.sh" in check)
        if env_failure_count >= 3:
            self.console.print(
                "\n[yellow]⚠️  Multiple env.sh issues detected. "
                "If regenerating doesn't help, this may be a bug in wrknv.[/yellow]"
            )
            self.console.print("  [dim]Please report at: https://github.com/provide-io/wrknv/issues[/dim]")

    def xǁWrknvDoctorǁ_print_recommendations__mutmut_73(self) -> None:
        """Print recommendations based on failed checks."""
        self.console.print("\n[bold]Recommendations:[/bold]")

        recommendations = []

        # Priority 1: Fix critical failures
        has_env_issues = False
        for check, _details in self.checks_failed:
            if "env.sh" in check or "Workenv Path" in check:
                has_env_issues = True
            elif "uv" in check:
                recommendations.append(("🔧 Install uv", "curl -LsSf https://astral.sh/uv/install.sh | sh"))
            elif "wrknv Installation" in check:
                recommendations.append(("🔧 Reinstall wrknv", "See installation docs"))

        # If there are any env.sh issues, recommend regeneration
        if has_env_issues:
            recommendations.insert(0, ("🔄 Regenerate env.sh", "wrknv generate --force"))
            recommendations.append(("📝 After regenerating", "source env.sh"))

        # Priority 2: Address warnings
        for check, _details in self.checks_warning:
            if ".venv Directory" in check:
                recommendations.append(("🗑️  Remove .venv", "rm -rf .venv && source env.sh"))
            elif "workenv Directory" in check and not has_env_issues:
                recommendations.append(("🔧 check workenv", "wrknv doctor"))
            elif "Virtual Environment" in check:
                recommendations.append(("🚪 Exit current venv", "deactivate && source env.sh"))

        # Remove duplicates and print
        seen = set()
        for title, command in recommendations:
            key = command
            if key not in seen:
                self.console.print(f"\n  {title}")
                self.console.print(f"  [dim]$ {command}[/dim]")
                seen.add(key)

        # If multiple env.sh failures, suggest bug report
        env_failure_count = sum(1 for check, _ in self.checks_failed if "env.sh" in check)
        if env_failure_count >= 3:
            self.console.print(
                "\n[yellow]⚠️  Multiple env.sh issues detected. "
                "If regenerating doesn't help, this may be a bug in wrknv.[/yellow]"
            )
            self.console.print("  [dim]Please report at: https://github.com/provide-io/wrknv/issues[/dim]")

    def xǁWrknvDoctorǁ_print_recommendations__mutmut_74(self) -> None:
        """Print recommendations based on failed checks."""
        self.console.print("\n[bold]Recommendations:[/bold]")

        recommendations = []

        # Priority 1: Fix critical failures
        has_env_issues = False
        for check, _details in self.checks_failed:
            if "env.sh" in check or "Workenv Path" in check:
                has_env_issues = True
            elif "uv" in check:
                recommendations.append(("🔧 Install uv", "curl -LsSf https://astral.sh/uv/install.sh | sh"))
            elif "wrknv Installation" in check:
                recommendations.append(("🔧 Reinstall wrknv", "See installation docs"))

        # If there are any env.sh issues, recommend regeneration
        if has_env_issues:
            recommendations.insert(0, ("🔄 Regenerate env.sh", "wrknv generate --force"))
            recommendations.append(("📝 After regenerating", "source env.sh"))

        # Priority 2: Address warnings
        for check, _details in self.checks_warning:
            if ".venv Directory" in check:
                recommendations.append(("🗑️  Remove .venv", "rm -rf .venv && source env.sh"))
            elif "workenv Directory" in check and not has_env_issues:
                recommendations.append(("🔧 CHECK WORKENV", "wrknv doctor"))
            elif "Virtual Environment" in check:
                recommendations.append(("🚪 Exit current venv", "deactivate && source env.sh"))

        # Remove duplicates and print
        seen = set()
        for title, command in recommendations:
            key = command
            if key not in seen:
                self.console.print(f"\n  {title}")
                self.console.print(f"  [dim]$ {command}[/dim]")
                seen.add(key)

        # If multiple env.sh failures, suggest bug report
        env_failure_count = sum(1 for check, _ in self.checks_failed if "env.sh" in check)
        if env_failure_count >= 3:
            self.console.print(
                "\n[yellow]⚠️  Multiple env.sh issues detected. "
                "If regenerating doesn't help, this may be a bug in wrknv.[/yellow]"
            )
            self.console.print("  [dim]Please report at: https://github.com/provide-io/wrknv/issues[/dim]")

    def xǁWrknvDoctorǁ_print_recommendations__mutmut_75(self) -> None:
        """Print recommendations based on failed checks."""
        self.console.print("\n[bold]Recommendations:[/bold]")

        recommendations = []

        # Priority 1: Fix critical failures
        has_env_issues = False
        for check, _details in self.checks_failed:
            if "env.sh" in check or "Workenv Path" in check:
                has_env_issues = True
            elif "uv" in check:
                recommendations.append(("🔧 Install uv", "curl -LsSf https://astral.sh/uv/install.sh | sh"))
            elif "wrknv Installation" in check:
                recommendations.append(("🔧 Reinstall wrknv", "See installation docs"))

        # If there are any env.sh issues, recommend regeneration
        if has_env_issues:
            recommendations.insert(0, ("🔄 Regenerate env.sh", "wrknv generate --force"))
            recommendations.append(("📝 After regenerating", "source env.sh"))

        # Priority 2: Address warnings
        for check, _details in self.checks_warning:
            if ".venv Directory" in check:
                recommendations.append(("🗑️  Remove .venv", "rm -rf .venv && source env.sh"))
            elif "workenv Directory" in check and not has_env_issues:
                recommendations.append(("🔧 Check workenv", "XXwrknv doctorXX"))
            elif "Virtual Environment" in check:
                recommendations.append(("🚪 Exit current venv", "deactivate && source env.sh"))

        # Remove duplicates and print
        seen = set()
        for title, command in recommendations:
            key = command
            if key not in seen:
                self.console.print(f"\n  {title}")
                self.console.print(f"  [dim]$ {command}[/dim]")
                seen.add(key)

        # If multiple env.sh failures, suggest bug report
        env_failure_count = sum(1 for check, _ in self.checks_failed if "env.sh" in check)
        if env_failure_count >= 3:
            self.console.print(
                "\n[yellow]⚠️  Multiple env.sh issues detected. "
                "If regenerating doesn't help, this may be a bug in wrknv.[/yellow]"
            )
            self.console.print("  [dim]Please report at: https://github.com/provide-io/wrknv/issues[/dim]")

    def xǁWrknvDoctorǁ_print_recommendations__mutmut_76(self) -> None:
        """Print recommendations based on failed checks."""
        self.console.print("\n[bold]Recommendations:[/bold]")

        recommendations = []

        # Priority 1: Fix critical failures
        has_env_issues = False
        for check, _details in self.checks_failed:
            if "env.sh" in check or "Workenv Path" in check:
                has_env_issues = True
            elif "uv" in check:
                recommendations.append(("🔧 Install uv", "curl -LsSf https://astral.sh/uv/install.sh | sh"))
            elif "wrknv Installation" in check:
                recommendations.append(("🔧 Reinstall wrknv", "See installation docs"))

        # If there are any env.sh issues, recommend regeneration
        if has_env_issues:
            recommendations.insert(0, ("🔄 Regenerate env.sh", "wrknv generate --force"))
            recommendations.append(("📝 After regenerating", "source env.sh"))

        # Priority 2: Address warnings
        for check, _details in self.checks_warning:
            if ".venv Directory" in check:
                recommendations.append(("🗑️  Remove .venv", "rm -rf .venv && source env.sh"))
            elif "workenv Directory" in check and not has_env_issues:
                recommendations.append(("🔧 Check workenv", "WRKNV DOCTOR"))
            elif "Virtual Environment" in check:
                recommendations.append(("🚪 Exit current venv", "deactivate && source env.sh"))

        # Remove duplicates and print
        seen = set()
        for title, command in recommendations:
            key = command
            if key not in seen:
                self.console.print(f"\n  {title}")
                self.console.print(f"  [dim]$ {command}[/dim]")
                seen.add(key)

        # If multiple env.sh failures, suggest bug report
        env_failure_count = sum(1 for check, _ in self.checks_failed if "env.sh" in check)
        if env_failure_count >= 3:
            self.console.print(
                "\n[yellow]⚠️  Multiple env.sh issues detected. "
                "If regenerating doesn't help, this may be a bug in wrknv.[/yellow]"
            )
            self.console.print("  [dim]Please report at: https://github.com/provide-io/wrknv/issues[/dim]")

    def xǁWrknvDoctorǁ_print_recommendations__mutmut_77(self) -> None:
        """Print recommendations based on failed checks."""
        self.console.print("\n[bold]Recommendations:[/bold]")

        recommendations = []

        # Priority 1: Fix critical failures
        has_env_issues = False
        for check, _details in self.checks_failed:
            if "env.sh" in check or "Workenv Path" in check:
                has_env_issues = True
            elif "uv" in check:
                recommendations.append(("🔧 Install uv", "curl -LsSf https://astral.sh/uv/install.sh | sh"))
            elif "wrknv Installation" in check:
                recommendations.append(("🔧 Reinstall wrknv", "See installation docs"))

        # If there are any env.sh issues, recommend regeneration
        if has_env_issues:
            recommendations.insert(0, ("🔄 Regenerate env.sh", "wrknv generate --force"))
            recommendations.append(("📝 After regenerating", "source env.sh"))

        # Priority 2: Address warnings
        for check, _details in self.checks_warning:
            if ".venv Directory" in check:
                recommendations.append(("🗑️  Remove .venv", "rm -rf .venv && source env.sh"))
            elif "workenv Directory" in check and not has_env_issues:
                recommendations.append(("🔧 Check workenv", "wrknv doctor"))
            elif "XXVirtual EnvironmentXX" in check:
                recommendations.append(("🚪 Exit current venv", "deactivate && source env.sh"))

        # Remove duplicates and print
        seen = set()
        for title, command in recommendations:
            key = command
            if key not in seen:
                self.console.print(f"\n  {title}")
                self.console.print(f"  [dim]$ {command}[/dim]")
                seen.add(key)

        # If multiple env.sh failures, suggest bug report
        env_failure_count = sum(1 for check, _ in self.checks_failed if "env.sh" in check)
        if env_failure_count >= 3:
            self.console.print(
                "\n[yellow]⚠️  Multiple env.sh issues detected. "
                "If regenerating doesn't help, this may be a bug in wrknv.[/yellow]"
            )
            self.console.print("  [dim]Please report at: https://github.com/provide-io/wrknv/issues[/dim]")

    def xǁWrknvDoctorǁ_print_recommendations__mutmut_78(self) -> None:
        """Print recommendations based on failed checks."""
        self.console.print("\n[bold]Recommendations:[/bold]")

        recommendations = []

        # Priority 1: Fix critical failures
        has_env_issues = False
        for check, _details in self.checks_failed:
            if "env.sh" in check or "Workenv Path" in check:
                has_env_issues = True
            elif "uv" in check:
                recommendations.append(("🔧 Install uv", "curl -LsSf https://astral.sh/uv/install.sh | sh"))
            elif "wrknv Installation" in check:
                recommendations.append(("🔧 Reinstall wrknv", "See installation docs"))

        # If there are any env.sh issues, recommend regeneration
        if has_env_issues:
            recommendations.insert(0, ("🔄 Regenerate env.sh", "wrknv generate --force"))
            recommendations.append(("📝 After regenerating", "source env.sh"))

        # Priority 2: Address warnings
        for check, _details in self.checks_warning:
            if ".venv Directory" in check:
                recommendations.append(("🗑️  Remove .venv", "rm -rf .venv && source env.sh"))
            elif "workenv Directory" in check and not has_env_issues:
                recommendations.append(("🔧 Check workenv", "wrknv doctor"))
            elif "virtual environment" in check:
                recommendations.append(("🚪 Exit current venv", "deactivate && source env.sh"))

        # Remove duplicates and print
        seen = set()
        for title, command in recommendations:
            key = command
            if key not in seen:
                self.console.print(f"\n  {title}")
                self.console.print(f"  [dim]$ {command}[/dim]")
                seen.add(key)

        # If multiple env.sh failures, suggest bug report
        env_failure_count = sum(1 for check, _ in self.checks_failed if "env.sh" in check)
        if env_failure_count >= 3:
            self.console.print(
                "\n[yellow]⚠️  Multiple env.sh issues detected. "
                "If regenerating doesn't help, this may be a bug in wrknv.[/yellow]"
            )
            self.console.print("  [dim]Please report at: https://github.com/provide-io/wrknv/issues[/dim]")

    def xǁWrknvDoctorǁ_print_recommendations__mutmut_79(self) -> None:
        """Print recommendations based on failed checks."""
        self.console.print("\n[bold]Recommendations:[/bold]")

        recommendations = []

        # Priority 1: Fix critical failures
        has_env_issues = False
        for check, _details in self.checks_failed:
            if "env.sh" in check or "Workenv Path" in check:
                has_env_issues = True
            elif "uv" in check:
                recommendations.append(("🔧 Install uv", "curl -LsSf https://astral.sh/uv/install.sh | sh"))
            elif "wrknv Installation" in check:
                recommendations.append(("🔧 Reinstall wrknv", "See installation docs"))

        # If there are any env.sh issues, recommend regeneration
        if has_env_issues:
            recommendations.insert(0, ("🔄 Regenerate env.sh", "wrknv generate --force"))
            recommendations.append(("📝 After regenerating", "source env.sh"))

        # Priority 2: Address warnings
        for check, _details in self.checks_warning:
            if ".venv Directory" in check:
                recommendations.append(("🗑️  Remove .venv", "rm -rf .venv && source env.sh"))
            elif "workenv Directory" in check and not has_env_issues:
                recommendations.append(("🔧 Check workenv", "wrknv doctor"))
            elif "VIRTUAL ENVIRONMENT" in check:
                recommendations.append(("🚪 Exit current venv", "deactivate && source env.sh"))

        # Remove duplicates and print
        seen = set()
        for title, command in recommendations:
            key = command
            if key not in seen:
                self.console.print(f"\n  {title}")
                self.console.print(f"  [dim]$ {command}[/dim]")
                seen.add(key)

        # If multiple env.sh failures, suggest bug report
        env_failure_count = sum(1 for check, _ in self.checks_failed if "env.sh" in check)
        if env_failure_count >= 3:
            self.console.print(
                "\n[yellow]⚠️  Multiple env.sh issues detected. "
                "If regenerating doesn't help, this may be a bug in wrknv.[/yellow]"
            )
            self.console.print("  [dim]Please report at: https://github.com/provide-io/wrknv/issues[/dim]")

    def xǁWrknvDoctorǁ_print_recommendations__mutmut_80(self) -> None:
        """Print recommendations based on failed checks."""
        self.console.print("\n[bold]Recommendations:[/bold]")

        recommendations = []

        # Priority 1: Fix critical failures
        has_env_issues = False
        for check, _details in self.checks_failed:
            if "env.sh" in check or "Workenv Path" in check:
                has_env_issues = True
            elif "uv" in check:
                recommendations.append(("🔧 Install uv", "curl -LsSf https://astral.sh/uv/install.sh | sh"))
            elif "wrknv Installation" in check:
                recommendations.append(("🔧 Reinstall wrknv", "See installation docs"))

        # If there are any env.sh issues, recommend regeneration
        if has_env_issues:
            recommendations.insert(0, ("🔄 Regenerate env.sh", "wrknv generate --force"))
            recommendations.append(("📝 After regenerating", "source env.sh"))

        # Priority 2: Address warnings
        for check, _details in self.checks_warning:
            if ".venv Directory" in check:
                recommendations.append(("🗑️  Remove .venv", "rm -rf .venv && source env.sh"))
            elif "workenv Directory" in check and not has_env_issues:
                recommendations.append(("🔧 Check workenv", "wrknv doctor"))
            elif "Virtual Environment" not in check:
                recommendations.append(("🚪 Exit current venv", "deactivate && source env.sh"))

        # Remove duplicates and print
        seen = set()
        for title, command in recommendations:
            key = command
            if key not in seen:
                self.console.print(f"\n  {title}")
                self.console.print(f"  [dim]$ {command}[/dim]")
                seen.add(key)

        # If multiple env.sh failures, suggest bug report
        env_failure_count = sum(1 for check, _ in self.checks_failed if "env.sh" in check)
        if env_failure_count >= 3:
            self.console.print(
                "\n[yellow]⚠️  Multiple env.sh issues detected. "
                "If regenerating doesn't help, this may be a bug in wrknv.[/yellow]"
            )
            self.console.print("  [dim]Please report at: https://github.com/provide-io/wrknv/issues[/dim]")

    def xǁWrknvDoctorǁ_print_recommendations__mutmut_81(self) -> None:
        """Print recommendations based on failed checks."""
        self.console.print("\n[bold]Recommendations:[/bold]")

        recommendations = []

        # Priority 1: Fix critical failures
        has_env_issues = False
        for check, _details in self.checks_failed:
            if "env.sh" in check or "Workenv Path" in check:
                has_env_issues = True
            elif "uv" in check:
                recommendations.append(("🔧 Install uv", "curl -LsSf https://astral.sh/uv/install.sh | sh"))
            elif "wrknv Installation" in check:
                recommendations.append(("🔧 Reinstall wrknv", "See installation docs"))

        # If there are any env.sh issues, recommend regeneration
        if has_env_issues:
            recommendations.insert(0, ("🔄 Regenerate env.sh", "wrknv generate --force"))
            recommendations.append(("📝 After regenerating", "source env.sh"))

        # Priority 2: Address warnings
        for check, _details in self.checks_warning:
            if ".venv Directory" in check:
                recommendations.append(("🗑️  Remove .venv", "rm -rf .venv && source env.sh"))
            elif "workenv Directory" in check and not has_env_issues:
                recommendations.append(("🔧 Check workenv", "wrknv doctor"))
            elif "Virtual Environment" in check:
                recommendations.append(None)

        # Remove duplicates and print
        seen = set()
        for title, command in recommendations:
            key = command
            if key not in seen:
                self.console.print(f"\n  {title}")
                self.console.print(f"  [dim]$ {command}[/dim]")
                seen.add(key)

        # If multiple env.sh failures, suggest bug report
        env_failure_count = sum(1 for check, _ in self.checks_failed if "env.sh" in check)
        if env_failure_count >= 3:
            self.console.print(
                "\n[yellow]⚠️  Multiple env.sh issues detected. "
                "If regenerating doesn't help, this may be a bug in wrknv.[/yellow]"
            )
            self.console.print("  [dim]Please report at: https://github.com/provide-io/wrknv/issues[/dim]")

    def xǁWrknvDoctorǁ_print_recommendations__mutmut_82(self) -> None:
        """Print recommendations based on failed checks."""
        self.console.print("\n[bold]Recommendations:[/bold]")

        recommendations = []

        # Priority 1: Fix critical failures
        has_env_issues = False
        for check, _details in self.checks_failed:
            if "env.sh" in check or "Workenv Path" in check:
                has_env_issues = True
            elif "uv" in check:
                recommendations.append(("🔧 Install uv", "curl -LsSf https://astral.sh/uv/install.sh | sh"))
            elif "wrknv Installation" in check:
                recommendations.append(("🔧 Reinstall wrknv", "See installation docs"))

        # If there are any env.sh issues, recommend regeneration
        if has_env_issues:
            recommendations.insert(0, ("🔄 Regenerate env.sh", "wrknv generate --force"))
            recommendations.append(("📝 After regenerating", "source env.sh"))

        # Priority 2: Address warnings
        for check, _details in self.checks_warning:
            if ".venv Directory" in check:
                recommendations.append(("🗑️  Remove .venv", "rm -rf .venv && source env.sh"))
            elif "workenv Directory" in check and not has_env_issues:
                recommendations.append(("🔧 Check workenv", "wrknv doctor"))
            elif "Virtual Environment" in check:
                recommendations.append(("XX🚪 Exit current venvXX", "deactivate && source env.sh"))

        # Remove duplicates and print
        seen = set()
        for title, command in recommendations:
            key = command
            if key not in seen:
                self.console.print(f"\n  {title}")
                self.console.print(f"  [dim]$ {command}[/dim]")
                seen.add(key)

        # If multiple env.sh failures, suggest bug report
        env_failure_count = sum(1 for check, _ in self.checks_failed if "env.sh" in check)
        if env_failure_count >= 3:
            self.console.print(
                "\n[yellow]⚠️  Multiple env.sh issues detected. "
                "If regenerating doesn't help, this may be a bug in wrknv.[/yellow]"
            )
            self.console.print("  [dim]Please report at: https://github.com/provide-io/wrknv/issues[/dim]")

    def xǁWrknvDoctorǁ_print_recommendations__mutmut_83(self) -> None:
        """Print recommendations based on failed checks."""
        self.console.print("\n[bold]Recommendations:[/bold]")

        recommendations = []

        # Priority 1: Fix critical failures
        has_env_issues = False
        for check, _details in self.checks_failed:
            if "env.sh" in check or "Workenv Path" in check:
                has_env_issues = True
            elif "uv" in check:
                recommendations.append(("🔧 Install uv", "curl -LsSf https://astral.sh/uv/install.sh | sh"))
            elif "wrknv Installation" in check:
                recommendations.append(("🔧 Reinstall wrknv", "See installation docs"))

        # If there are any env.sh issues, recommend regeneration
        if has_env_issues:
            recommendations.insert(0, ("🔄 Regenerate env.sh", "wrknv generate --force"))
            recommendations.append(("📝 After regenerating", "source env.sh"))

        # Priority 2: Address warnings
        for check, _details in self.checks_warning:
            if ".venv Directory" in check:
                recommendations.append(("🗑️  Remove .venv", "rm -rf .venv && source env.sh"))
            elif "workenv Directory" in check and not has_env_issues:
                recommendations.append(("🔧 Check workenv", "wrknv doctor"))
            elif "Virtual Environment" in check:
                recommendations.append(("🚪 exit current venv", "deactivate && source env.sh"))

        # Remove duplicates and print
        seen = set()
        for title, command in recommendations:
            key = command
            if key not in seen:
                self.console.print(f"\n  {title}")
                self.console.print(f"  [dim]$ {command}[/dim]")
                seen.add(key)

        # If multiple env.sh failures, suggest bug report
        env_failure_count = sum(1 for check, _ in self.checks_failed if "env.sh" in check)
        if env_failure_count >= 3:
            self.console.print(
                "\n[yellow]⚠️  Multiple env.sh issues detected. "
                "If regenerating doesn't help, this may be a bug in wrknv.[/yellow]"
            )
            self.console.print("  [dim]Please report at: https://github.com/provide-io/wrknv/issues[/dim]")

    def xǁWrknvDoctorǁ_print_recommendations__mutmut_84(self) -> None:
        """Print recommendations based on failed checks."""
        self.console.print("\n[bold]Recommendations:[/bold]")

        recommendations = []

        # Priority 1: Fix critical failures
        has_env_issues = False
        for check, _details in self.checks_failed:
            if "env.sh" in check or "Workenv Path" in check:
                has_env_issues = True
            elif "uv" in check:
                recommendations.append(("🔧 Install uv", "curl -LsSf https://astral.sh/uv/install.sh | sh"))
            elif "wrknv Installation" in check:
                recommendations.append(("🔧 Reinstall wrknv", "See installation docs"))

        # If there are any env.sh issues, recommend regeneration
        if has_env_issues:
            recommendations.insert(0, ("🔄 Regenerate env.sh", "wrknv generate --force"))
            recommendations.append(("📝 After regenerating", "source env.sh"))

        # Priority 2: Address warnings
        for check, _details in self.checks_warning:
            if ".venv Directory" in check:
                recommendations.append(("🗑️  Remove .venv", "rm -rf .venv && source env.sh"))
            elif "workenv Directory" in check and not has_env_issues:
                recommendations.append(("🔧 Check workenv", "wrknv doctor"))
            elif "Virtual Environment" in check:
                recommendations.append(("🚪 EXIT CURRENT VENV", "deactivate && source env.sh"))

        # Remove duplicates and print
        seen = set()
        for title, command in recommendations:
            key = command
            if key not in seen:
                self.console.print(f"\n  {title}")
                self.console.print(f"  [dim]$ {command}[/dim]")
                seen.add(key)

        # If multiple env.sh failures, suggest bug report
        env_failure_count = sum(1 for check, _ in self.checks_failed if "env.sh" in check)
        if env_failure_count >= 3:
            self.console.print(
                "\n[yellow]⚠️  Multiple env.sh issues detected. "
                "If regenerating doesn't help, this may be a bug in wrknv.[/yellow]"
            )
            self.console.print("  [dim]Please report at: https://github.com/provide-io/wrknv/issues[/dim]")

    def xǁWrknvDoctorǁ_print_recommendations__mutmut_85(self) -> None:
        """Print recommendations based on failed checks."""
        self.console.print("\n[bold]Recommendations:[/bold]")

        recommendations = []

        # Priority 1: Fix critical failures
        has_env_issues = False
        for check, _details in self.checks_failed:
            if "env.sh" in check or "Workenv Path" in check:
                has_env_issues = True
            elif "uv" in check:
                recommendations.append(("🔧 Install uv", "curl -LsSf https://astral.sh/uv/install.sh | sh"))
            elif "wrknv Installation" in check:
                recommendations.append(("🔧 Reinstall wrknv", "See installation docs"))

        # If there are any env.sh issues, recommend regeneration
        if has_env_issues:
            recommendations.insert(0, ("🔄 Regenerate env.sh", "wrknv generate --force"))
            recommendations.append(("📝 After regenerating", "source env.sh"))

        # Priority 2: Address warnings
        for check, _details in self.checks_warning:
            if ".venv Directory" in check:
                recommendations.append(("🗑️  Remove .venv", "rm -rf .venv && source env.sh"))
            elif "workenv Directory" in check and not has_env_issues:
                recommendations.append(("🔧 Check workenv", "wrknv doctor"))
            elif "Virtual Environment" in check:
                recommendations.append(("🚪 Exit current venv", "XXdeactivate && source env.shXX"))

        # Remove duplicates and print
        seen = set()
        for title, command in recommendations:
            key = command
            if key not in seen:
                self.console.print(f"\n  {title}")
                self.console.print(f"  [dim]$ {command}[/dim]")
                seen.add(key)

        # If multiple env.sh failures, suggest bug report
        env_failure_count = sum(1 for check, _ in self.checks_failed if "env.sh" in check)
        if env_failure_count >= 3:
            self.console.print(
                "\n[yellow]⚠️  Multiple env.sh issues detected. "
                "If regenerating doesn't help, this may be a bug in wrknv.[/yellow]"
            )
            self.console.print("  [dim]Please report at: https://github.com/provide-io/wrknv/issues[/dim]")

    def xǁWrknvDoctorǁ_print_recommendations__mutmut_86(self) -> None:
        """Print recommendations based on failed checks."""
        self.console.print("\n[bold]Recommendations:[/bold]")

        recommendations = []

        # Priority 1: Fix critical failures
        has_env_issues = False
        for check, _details in self.checks_failed:
            if "env.sh" in check or "Workenv Path" in check:
                has_env_issues = True
            elif "uv" in check:
                recommendations.append(("🔧 Install uv", "curl -LsSf https://astral.sh/uv/install.sh | sh"))
            elif "wrknv Installation" in check:
                recommendations.append(("🔧 Reinstall wrknv", "See installation docs"))

        # If there are any env.sh issues, recommend regeneration
        if has_env_issues:
            recommendations.insert(0, ("🔄 Regenerate env.sh", "wrknv generate --force"))
            recommendations.append(("📝 After regenerating", "source env.sh"))

        # Priority 2: Address warnings
        for check, _details in self.checks_warning:
            if ".venv Directory" in check:
                recommendations.append(("🗑️  Remove .venv", "rm -rf .venv && source env.sh"))
            elif "workenv Directory" in check and not has_env_issues:
                recommendations.append(("🔧 Check workenv", "wrknv doctor"))
            elif "Virtual Environment" in check:
                recommendations.append(("🚪 Exit current venv", "DEACTIVATE && SOURCE ENV.SH"))

        # Remove duplicates and print
        seen = set()
        for title, command in recommendations:
            key = command
            if key not in seen:
                self.console.print(f"\n  {title}")
                self.console.print(f"  [dim]$ {command}[/dim]")
                seen.add(key)

        # If multiple env.sh failures, suggest bug report
        env_failure_count = sum(1 for check, _ in self.checks_failed if "env.sh" in check)
        if env_failure_count >= 3:
            self.console.print(
                "\n[yellow]⚠️  Multiple env.sh issues detected. "
                "If regenerating doesn't help, this may be a bug in wrknv.[/yellow]"
            )
            self.console.print("  [dim]Please report at: https://github.com/provide-io/wrknv/issues[/dim]")

    def xǁWrknvDoctorǁ_print_recommendations__mutmut_87(self) -> None:
        """Print recommendations based on failed checks."""
        self.console.print("\n[bold]Recommendations:[/bold]")

        recommendations = []

        # Priority 1: Fix critical failures
        has_env_issues = False
        for check, _details in self.checks_failed:
            if "env.sh" in check or "Workenv Path" in check:
                has_env_issues = True
            elif "uv" in check:
                recommendations.append(("🔧 Install uv", "curl -LsSf https://astral.sh/uv/install.sh | sh"))
            elif "wrknv Installation" in check:
                recommendations.append(("🔧 Reinstall wrknv", "See installation docs"))

        # If there are any env.sh issues, recommend regeneration
        if has_env_issues:
            recommendations.insert(0, ("🔄 Regenerate env.sh", "wrknv generate --force"))
            recommendations.append(("📝 After regenerating", "source env.sh"))

        # Priority 2: Address warnings
        for check, _details in self.checks_warning:
            if ".venv Directory" in check:
                recommendations.append(("🗑️  Remove .venv", "rm -rf .venv && source env.sh"))
            elif "workenv Directory" in check and not has_env_issues:
                recommendations.append(("🔧 Check workenv", "wrknv doctor"))
            elif "Virtual Environment" in check:
                recommendations.append(("🚪 Exit current venv", "deactivate && source env.sh"))

        # Remove duplicates and print
        seen = None
        for title, command in recommendations:
            key = command
            if key not in seen:
                self.console.print(f"\n  {title}")
                self.console.print(f"  [dim]$ {command}[/dim]")
                seen.add(key)

        # If multiple env.sh failures, suggest bug report
        env_failure_count = sum(1 for check, _ in self.checks_failed if "env.sh" in check)
        if env_failure_count >= 3:
            self.console.print(
                "\n[yellow]⚠️  Multiple env.sh issues detected. "
                "If regenerating doesn't help, this may be a bug in wrknv.[/yellow]"
            )
            self.console.print("  [dim]Please report at: https://github.com/provide-io/wrknv/issues[/dim]")

    def xǁWrknvDoctorǁ_print_recommendations__mutmut_88(self) -> None:
        """Print recommendations based on failed checks."""
        self.console.print("\n[bold]Recommendations:[/bold]")

        recommendations = []

        # Priority 1: Fix critical failures
        has_env_issues = False
        for check, _details in self.checks_failed:
            if "env.sh" in check or "Workenv Path" in check:
                has_env_issues = True
            elif "uv" in check:
                recommendations.append(("🔧 Install uv", "curl -LsSf https://astral.sh/uv/install.sh | sh"))
            elif "wrknv Installation" in check:
                recommendations.append(("🔧 Reinstall wrknv", "See installation docs"))

        # If there are any env.sh issues, recommend regeneration
        if has_env_issues:
            recommendations.insert(0, ("🔄 Regenerate env.sh", "wrknv generate --force"))
            recommendations.append(("📝 After regenerating", "source env.sh"))

        # Priority 2: Address warnings
        for check, _details in self.checks_warning:
            if ".venv Directory" in check:
                recommendations.append(("🗑️  Remove .venv", "rm -rf .venv && source env.sh"))
            elif "workenv Directory" in check and not has_env_issues:
                recommendations.append(("🔧 Check workenv", "wrknv doctor"))
            elif "Virtual Environment" in check:
                recommendations.append(("🚪 Exit current venv", "deactivate && source env.sh"))

        # Remove duplicates and print
        seen = set()
        for title, command in recommendations:
            key = None
            if key not in seen:
                self.console.print(f"\n  {title}")
                self.console.print(f"  [dim]$ {command}[/dim]")
                seen.add(key)

        # If multiple env.sh failures, suggest bug report
        env_failure_count = sum(1 for check, _ in self.checks_failed if "env.sh" in check)
        if env_failure_count >= 3:
            self.console.print(
                "\n[yellow]⚠️  Multiple env.sh issues detected. "
                "If regenerating doesn't help, this may be a bug in wrknv.[/yellow]"
            )
            self.console.print("  [dim]Please report at: https://github.com/provide-io/wrknv/issues[/dim]")

    def xǁWrknvDoctorǁ_print_recommendations__mutmut_89(self) -> None:
        """Print recommendations based on failed checks."""
        self.console.print("\n[bold]Recommendations:[/bold]")

        recommendations = []

        # Priority 1: Fix critical failures
        has_env_issues = False
        for check, _details in self.checks_failed:
            if "env.sh" in check or "Workenv Path" in check:
                has_env_issues = True
            elif "uv" in check:
                recommendations.append(("🔧 Install uv", "curl -LsSf https://astral.sh/uv/install.sh | sh"))
            elif "wrknv Installation" in check:
                recommendations.append(("🔧 Reinstall wrknv", "See installation docs"))

        # If there are any env.sh issues, recommend regeneration
        if has_env_issues:
            recommendations.insert(0, ("🔄 Regenerate env.sh", "wrknv generate --force"))
            recommendations.append(("📝 After regenerating", "source env.sh"))

        # Priority 2: Address warnings
        for check, _details in self.checks_warning:
            if ".venv Directory" in check:
                recommendations.append(("🗑️  Remove .venv", "rm -rf .venv && source env.sh"))
            elif "workenv Directory" in check and not has_env_issues:
                recommendations.append(("🔧 Check workenv", "wrknv doctor"))
            elif "Virtual Environment" in check:
                recommendations.append(("🚪 Exit current venv", "deactivate && source env.sh"))

        # Remove duplicates and print
        seen = set()
        for title, command in recommendations:
            key = command
            if key in seen:
                self.console.print(f"\n  {title}")
                self.console.print(f"  [dim]$ {command}[/dim]")
                seen.add(key)

        # If multiple env.sh failures, suggest bug report
        env_failure_count = sum(1 for check, _ in self.checks_failed if "env.sh" in check)
        if env_failure_count >= 3:
            self.console.print(
                "\n[yellow]⚠️  Multiple env.sh issues detected. "
                "If regenerating doesn't help, this may be a bug in wrknv.[/yellow]"
            )
            self.console.print("  [dim]Please report at: https://github.com/provide-io/wrknv/issues[/dim]")

    def xǁWrknvDoctorǁ_print_recommendations__mutmut_90(self) -> None:
        """Print recommendations based on failed checks."""
        self.console.print("\n[bold]Recommendations:[/bold]")

        recommendations = []

        # Priority 1: Fix critical failures
        has_env_issues = False
        for check, _details in self.checks_failed:
            if "env.sh" in check or "Workenv Path" in check:
                has_env_issues = True
            elif "uv" in check:
                recommendations.append(("🔧 Install uv", "curl -LsSf https://astral.sh/uv/install.sh | sh"))
            elif "wrknv Installation" in check:
                recommendations.append(("🔧 Reinstall wrknv", "See installation docs"))

        # If there are any env.sh issues, recommend regeneration
        if has_env_issues:
            recommendations.insert(0, ("🔄 Regenerate env.sh", "wrknv generate --force"))
            recommendations.append(("📝 After regenerating", "source env.sh"))

        # Priority 2: Address warnings
        for check, _details in self.checks_warning:
            if ".venv Directory" in check:
                recommendations.append(("🗑️  Remove .venv", "rm -rf .venv && source env.sh"))
            elif "workenv Directory" in check and not has_env_issues:
                recommendations.append(("🔧 Check workenv", "wrknv doctor"))
            elif "Virtual Environment" in check:
                recommendations.append(("🚪 Exit current venv", "deactivate && source env.sh"))

        # Remove duplicates and print
        seen = set()
        for title, command in recommendations:
            key = command
            if key not in seen:
                self.console.print(None)
                self.console.print(f"  [dim]$ {command}[/dim]")
                seen.add(key)

        # If multiple env.sh failures, suggest bug report
        env_failure_count = sum(1 for check, _ in self.checks_failed if "env.sh" in check)
        if env_failure_count >= 3:
            self.console.print(
                "\n[yellow]⚠️  Multiple env.sh issues detected. "
                "If regenerating doesn't help, this may be a bug in wrknv.[/yellow]"
            )
            self.console.print("  [dim]Please report at: https://github.com/provide-io/wrknv/issues[/dim]")

    def xǁWrknvDoctorǁ_print_recommendations__mutmut_91(self) -> None:
        """Print recommendations based on failed checks."""
        self.console.print("\n[bold]Recommendations:[/bold]")

        recommendations = []

        # Priority 1: Fix critical failures
        has_env_issues = False
        for check, _details in self.checks_failed:
            if "env.sh" in check or "Workenv Path" in check:
                has_env_issues = True
            elif "uv" in check:
                recommendations.append(("🔧 Install uv", "curl -LsSf https://astral.sh/uv/install.sh | sh"))
            elif "wrknv Installation" in check:
                recommendations.append(("🔧 Reinstall wrknv", "See installation docs"))

        # If there are any env.sh issues, recommend regeneration
        if has_env_issues:
            recommendations.insert(0, ("🔄 Regenerate env.sh", "wrknv generate --force"))
            recommendations.append(("📝 After regenerating", "source env.sh"))

        # Priority 2: Address warnings
        for check, _details in self.checks_warning:
            if ".venv Directory" in check:
                recommendations.append(("🗑️  Remove .venv", "rm -rf .venv && source env.sh"))
            elif "workenv Directory" in check and not has_env_issues:
                recommendations.append(("🔧 Check workenv", "wrknv doctor"))
            elif "Virtual Environment" in check:
                recommendations.append(("🚪 Exit current venv", "deactivate && source env.sh"))

        # Remove duplicates and print
        seen = set()
        for title, command in recommendations:
            key = command
            if key not in seen:
                self.console.print(f"\n  {title}")
                self.console.print(None)
                seen.add(key)

        # If multiple env.sh failures, suggest bug report
        env_failure_count = sum(1 for check, _ in self.checks_failed if "env.sh" in check)
        if env_failure_count >= 3:
            self.console.print(
                "\n[yellow]⚠️  Multiple env.sh issues detected. "
                "If regenerating doesn't help, this may be a bug in wrknv.[/yellow]"
            )
            self.console.print("  [dim]Please report at: https://github.com/provide-io/wrknv/issues[/dim]")

    def xǁWrknvDoctorǁ_print_recommendations__mutmut_92(self) -> None:
        """Print recommendations based on failed checks."""
        self.console.print("\n[bold]Recommendations:[/bold]")

        recommendations = []

        # Priority 1: Fix critical failures
        has_env_issues = False
        for check, _details in self.checks_failed:
            if "env.sh" in check or "Workenv Path" in check:
                has_env_issues = True
            elif "uv" in check:
                recommendations.append(("🔧 Install uv", "curl -LsSf https://astral.sh/uv/install.sh | sh"))
            elif "wrknv Installation" in check:
                recommendations.append(("🔧 Reinstall wrknv", "See installation docs"))

        # If there are any env.sh issues, recommend regeneration
        if has_env_issues:
            recommendations.insert(0, ("🔄 Regenerate env.sh", "wrknv generate --force"))
            recommendations.append(("📝 After regenerating", "source env.sh"))

        # Priority 2: Address warnings
        for check, _details in self.checks_warning:
            if ".venv Directory" in check:
                recommendations.append(("🗑️  Remove .venv", "rm -rf .venv && source env.sh"))
            elif "workenv Directory" in check and not has_env_issues:
                recommendations.append(("🔧 Check workenv", "wrknv doctor"))
            elif "Virtual Environment" in check:
                recommendations.append(("🚪 Exit current venv", "deactivate && source env.sh"))

        # Remove duplicates and print
        seen = set()
        for title, command in recommendations:
            key = command
            if key not in seen:
                self.console.print(f"\n  {title}")
                self.console.print(f"  [dim]$ {command}[/dim]")
                seen.add(None)

        # If multiple env.sh failures, suggest bug report
        env_failure_count = sum(1 for check, _ in self.checks_failed if "env.sh" in check)
        if env_failure_count >= 3:
            self.console.print(
                "\n[yellow]⚠️  Multiple env.sh issues detected. "
                "If regenerating doesn't help, this may be a bug in wrknv.[/yellow]"
            )
            self.console.print("  [dim]Please report at: https://github.com/provide-io/wrknv/issues[/dim]")

    def xǁWrknvDoctorǁ_print_recommendations__mutmut_93(self) -> None:
        """Print recommendations based on failed checks."""
        self.console.print("\n[bold]Recommendations:[/bold]")

        recommendations = []

        # Priority 1: Fix critical failures
        has_env_issues = False
        for check, _details in self.checks_failed:
            if "env.sh" in check or "Workenv Path" in check:
                has_env_issues = True
            elif "uv" in check:
                recommendations.append(("🔧 Install uv", "curl -LsSf https://astral.sh/uv/install.sh | sh"))
            elif "wrknv Installation" in check:
                recommendations.append(("🔧 Reinstall wrknv", "See installation docs"))

        # If there are any env.sh issues, recommend regeneration
        if has_env_issues:
            recommendations.insert(0, ("🔄 Regenerate env.sh", "wrknv generate --force"))
            recommendations.append(("📝 After regenerating", "source env.sh"))

        # Priority 2: Address warnings
        for check, _details in self.checks_warning:
            if ".venv Directory" in check:
                recommendations.append(("🗑️  Remove .venv", "rm -rf .venv && source env.sh"))
            elif "workenv Directory" in check and not has_env_issues:
                recommendations.append(("🔧 Check workenv", "wrknv doctor"))
            elif "Virtual Environment" in check:
                recommendations.append(("🚪 Exit current venv", "deactivate && source env.sh"))

        # Remove duplicates and print
        seen = set()
        for title, command in recommendations:
            key = command
            if key not in seen:
                self.console.print(f"\n  {title}")
                self.console.print(f"  [dim]$ {command}[/dim]")
                seen.add(key)

        # If multiple env.sh failures, suggest bug report
        env_failure_count = None
        if env_failure_count >= 3:
            self.console.print(
                "\n[yellow]⚠️  Multiple env.sh issues detected. "
                "If regenerating doesn't help, this may be a bug in wrknv.[/yellow]"
            )
            self.console.print("  [dim]Please report at: https://github.com/provide-io/wrknv/issues[/dim]")

    def xǁWrknvDoctorǁ_print_recommendations__mutmut_94(self) -> None:
        """Print recommendations based on failed checks."""
        self.console.print("\n[bold]Recommendations:[/bold]")

        recommendations = []

        # Priority 1: Fix critical failures
        has_env_issues = False
        for check, _details in self.checks_failed:
            if "env.sh" in check or "Workenv Path" in check:
                has_env_issues = True
            elif "uv" in check:
                recommendations.append(("🔧 Install uv", "curl -LsSf https://astral.sh/uv/install.sh | sh"))
            elif "wrknv Installation" in check:
                recommendations.append(("🔧 Reinstall wrknv", "See installation docs"))

        # If there are any env.sh issues, recommend regeneration
        if has_env_issues:
            recommendations.insert(0, ("🔄 Regenerate env.sh", "wrknv generate --force"))
            recommendations.append(("📝 After regenerating", "source env.sh"))

        # Priority 2: Address warnings
        for check, _details in self.checks_warning:
            if ".venv Directory" in check:
                recommendations.append(("🗑️  Remove .venv", "rm -rf .venv && source env.sh"))
            elif "workenv Directory" in check and not has_env_issues:
                recommendations.append(("🔧 Check workenv", "wrknv doctor"))
            elif "Virtual Environment" in check:
                recommendations.append(("🚪 Exit current venv", "deactivate && source env.sh"))

        # Remove duplicates and print
        seen = set()
        for title, command in recommendations:
            key = command
            if key not in seen:
                self.console.print(f"\n  {title}")
                self.console.print(f"  [dim]$ {command}[/dim]")
                seen.add(key)

        # If multiple env.sh failures, suggest bug report
        env_failure_count = sum(None)
        if env_failure_count >= 3:
            self.console.print(
                "\n[yellow]⚠️  Multiple env.sh issues detected. "
                "If regenerating doesn't help, this may be a bug in wrknv.[/yellow]"
            )
            self.console.print("  [dim]Please report at: https://github.com/provide-io/wrknv/issues[/dim]")

    def xǁWrknvDoctorǁ_print_recommendations__mutmut_95(self) -> None:
        """Print recommendations based on failed checks."""
        self.console.print("\n[bold]Recommendations:[/bold]")

        recommendations = []

        # Priority 1: Fix critical failures
        has_env_issues = False
        for check, _details in self.checks_failed:
            if "env.sh" in check or "Workenv Path" in check:
                has_env_issues = True
            elif "uv" in check:
                recommendations.append(("🔧 Install uv", "curl -LsSf https://astral.sh/uv/install.sh | sh"))
            elif "wrknv Installation" in check:
                recommendations.append(("🔧 Reinstall wrknv", "See installation docs"))

        # If there are any env.sh issues, recommend regeneration
        if has_env_issues:
            recommendations.insert(0, ("🔄 Regenerate env.sh", "wrknv generate --force"))
            recommendations.append(("📝 After regenerating", "source env.sh"))

        # Priority 2: Address warnings
        for check, _details in self.checks_warning:
            if ".venv Directory" in check:
                recommendations.append(("🗑️  Remove .venv", "rm -rf .venv && source env.sh"))
            elif "workenv Directory" in check and not has_env_issues:
                recommendations.append(("🔧 Check workenv", "wrknv doctor"))
            elif "Virtual Environment" in check:
                recommendations.append(("🚪 Exit current venv", "deactivate && source env.sh"))

        # Remove duplicates and print
        seen = set()
        for title, command in recommendations:
            key = command
            if key not in seen:
                self.console.print(f"\n  {title}")
                self.console.print(f"  [dim]$ {command}[/dim]")
                seen.add(key)

        # If multiple env.sh failures, suggest bug report
        env_failure_count = sum(2 for check, _ in self.checks_failed if "env.sh" in check)
        if env_failure_count >= 3:
            self.console.print(
                "\n[yellow]⚠️  Multiple env.sh issues detected. "
                "If regenerating doesn't help, this may be a bug in wrknv.[/yellow]"
            )
            self.console.print("  [dim]Please report at: https://github.com/provide-io/wrknv/issues[/dim]")

    def xǁWrknvDoctorǁ_print_recommendations__mutmut_96(self) -> None:
        """Print recommendations based on failed checks."""
        self.console.print("\n[bold]Recommendations:[/bold]")

        recommendations = []

        # Priority 1: Fix critical failures
        has_env_issues = False
        for check, _details in self.checks_failed:
            if "env.sh" in check or "Workenv Path" in check:
                has_env_issues = True
            elif "uv" in check:
                recommendations.append(("🔧 Install uv", "curl -LsSf https://astral.sh/uv/install.sh | sh"))
            elif "wrknv Installation" in check:
                recommendations.append(("🔧 Reinstall wrknv", "See installation docs"))

        # If there are any env.sh issues, recommend regeneration
        if has_env_issues:
            recommendations.insert(0, ("🔄 Regenerate env.sh", "wrknv generate --force"))
            recommendations.append(("📝 After regenerating", "source env.sh"))

        # Priority 2: Address warnings
        for check, _details in self.checks_warning:
            if ".venv Directory" in check:
                recommendations.append(("🗑️  Remove .venv", "rm -rf .venv && source env.sh"))
            elif "workenv Directory" in check and not has_env_issues:
                recommendations.append(("🔧 Check workenv", "wrknv doctor"))
            elif "Virtual Environment" in check:
                recommendations.append(("🚪 Exit current venv", "deactivate && source env.sh"))

        # Remove duplicates and print
        seen = set()
        for title, command in recommendations:
            key = command
            if key not in seen:
                self.console.print(f"\n  {title}")
                self.console.print(f"  [dim]$ {command}[/dim]")
                seen.add(key)

        # If multiple env.sh failures, suggest bug report
        env_failure_count = sum(1 for check, _ in self.checks_failed if "XXenv.shXX" in check)
        if env_failure_count >= 3:
            self.console.print(
                "\n[yellow]⚠️  Multiple env.sh issues detected. "
                "If regenerating doesn't help, this may be a bug in wrknv.[/yellow]"
            )
            self.console.print("  [dim]Please report at: https://github.com/provide-io/wrknv/issues[/dim]")

    def xǁWrknvDoctorǁ_print_recommendations__mutmut_97(self) -> None:
        """Print recommendations based on failed checks."""
        self.console.print("\n[bold]Recommendations:[/bold]")

        recommendations = []

        # Priority 1: Fix critical failures
        has_env_issues = False
        for check, _details in self.checks_failed:
            if "env.sh" in check or "Workenv Path" in check:
                has_env_issues = True
            elif "uv" in check:
                recommendations.append(("🔧 Install uv", "curl -LsSf https://astral.sh/uv/install.sh | sh"))
            elif "wrknv Installation" in check:
                recommendations.append(("🔧 Reinstall wrknv", "See installation docs"))

        # If there are any env.sh issues, recommend regeneration
        if has_env_issues:
            recommendations.insert(0, ("🔄 Regenerate env.sh", "wrknv generate --force"))
            recommendations.append(("📝 After regenerating", "source env.sh"))

        # Priority 2: Address warnings
        for check, _details in self.checks_warning:
            if ".venv Directory" in check:
                recommendations.append(("🗑️  Remove .venv", "rm -rf .venv && source env.sh"))
            elif "workenv Directory" in check and not has_env_issues:
                recommendations.append(("🔧 Check workenv", "wrknv doctor"))
            elif "Virtual Environment" in check:
                recommendations.append(("🚪 Exit current venv", "deactivate && source env.sh"))

        # Remove duplicates and print
        seen = set()
        for title, command in recommendations:
            key = command
            if key not in seen:
                self.console.print(f"\n  {title}")
                self.console.print(f"  [dim]$ {command}[/dim]")
                seen.add(key)

        # If multiple env.sh failures, suggest bug report
        env_failure_count = sum(1 for check, _ in self.checks_failed if "ENV.SH" in check)
        if env_failure_count >= 3:
            self.console.print(
                "\n[yellow]⚠️  Multiple env.sh issues detected. "
                "If regenerating doesn't help, this may be a bug in wrknv.[/yellow]"
            )
            self.console.print("  [dim]Please report at: https://github.com/provide-io/wrknv/issues[/dim]")

    def xǁWrknvDoctorǁ_print_recommendations__mutmut_98(self) -> None:
        """Print recommendations based on failed checks."""
        self.console.print("\n[bold]Recommendations:[/bold]")

        recommendations = []

        # Priority 1: Fix critical failures
        has_env_issues = False
        for check, _details in self.checks_failed:
            if "env.sh" in check or "Workenv Path" in check:
                has_env_issues = True
            elif "uv" in check:
                recommendations.append(("🔧 Install uv", "curl -LsSf https://astral.sh/uv/install.sh | sh"))
            elif "wrknv Installation" in check:
                recommendations.append(("🔧 Reinstall wrknv", "See installation docs"))

        # If there are any env.sh issues, recommend regeneration
        if has_env_issues:
            recommendations.insert(0, ("🔄 Regenerate env.sh", "wrknv generate --force"))
            recommendations.append(("📝 After regenerating", "source env.sh"))

        # Priority 2: Address warnings
        for check, _details in self.checks_warning:
            if ".venv Directory" in check:
                recommendations.append(("🗑️  Remove .venv", "rm -rf .venv && source env.sh"))
            elif "workenv Directory" in check and not has_env_issues:
                recommendations.append(("🔧 Check workenv", "wrknv doctor"))
            elif "Virtual Environment" in check:
                recommendations.append(("🚪 Exit current venv", "deactivate && source env.sh"))

        # Remove duplicates and print
        seen = set()
        for title, command in recommendations:
            key = command
            if key not in seen:
                self.console.print(f"\n  {title}")
                self.console.print(f"  [dim]$ {command}[/dim]")
                seen.add(key)

        # If multiple env.sh failures, suggest bug report
        env_failure_count = sum(1 for check, _ in self.checks_failed if "env.sh" not in check)
        if env_failure_count >= 3:
            self.console.print(
                "\n[yellow]⚠️  Multiple env.sh issues detected. "
                "If regenerating doesn't help, this may be a bug in wrknv.[/yellow]"
            )
            self.console.print("  [dim]Please report at: https://github.com/provide-io/wrknv/issues[/dim]")

    def xǁWrknvDoctorǁ_print_recommendations__mutmut_99(self) -> None:
        """Print recommendations based on failed checks."""
        self.console.print("\n[bold]Recommendations:[/bold]")

        recommendations = []

        # Priority 1: Fix critical failures
        has_env_issues = False
        for check, _details in self.checks_failed:
            if "env.sh" in check or "Workenv Path" in check:
                has_env_issues = True
            elif "uv" in check:
                recommendations.append(("🔧 Install uv", "curl -LsSf https://astral.sh/uv/install.sh | sh"))
            elif "wrknv Installation" in check:
                recommendations.append(("🔧 Reinstall wrknv", "See installation docs"))

        # If there are any env.sh issues, recommend regeneration
        if has_env_issues:
            recommendations.insert(0, ("🔄 Regenerate env.sh", "wrknv generate --force"))
            recommendations.append(("📝 After regenerating", "source env.sh"))

        # Priority 2: Address warnings
        for check, _details in self.checks_warning:
            if ".venv Directory" in check:
                recommendations.append(("🗑️  Remove .venv", "rm -rf .venv && source env.sh"))
            elif "workenv Directory" in check and not has_env_issues:
                recommendations.append(("🔧 Check workenv", "wrknv doctor"))
            elif "Virtual Environment" in check:
                recommendations.append(("🚪 Exit current venv", "deactivate && source env.sh"))

        # Remove duplicates and print
        seen = set()
        for title, command in recommendations:
            key = command
            if key not in seen:
                self.console.print(f"\n  {title}")
                self.console.print(f"  [dim]$ {command}[/dim]")
                seen.add(key)

        # If multiple env.sh failures, suggest bug report
        env_failure_count = sum(1 for check, _ in self.checks_failed if "env.sh" in check)
        if env_failure_count > 3:
            self.console.print(
                "\n[yellow]⚠️  Multiple env.sh issues detected. "
                "If regenerating doesn't help, this may be a bug in wrknv.[/yellow]"
            )
            self.console.print("  [dim]Please report at: https://github.com/provide-io/wrknv/issues[/dim]")

    def xǁWrknvDoctorǁ_print_recommendations__mutmut_100(self) -> None:
        """Print recommendations based on failed checks."""
        self.console.print("\n[bold]Recommendations:[/bold]")

        recommendations = []

        # Priority 1: Fix critical failures
        has_env_issues = False
        for check, _details in self.checks_failed:
            if "env.sh" in check or "Workenv Path" in check:
                has_env_issues = True
            elif "uv" in check:
                recommendations.append(("🔧 Install uv", "curl -LsSf https://astral.sh/uv/install.sh | sh"))
            elif "wrknv Installation" in check:
                recommendations.append(("🔧 Reinstall wrknv", "See installation docs"))

        # If there are any env.sh issues, recommend regeneration
        if has_env_issues:
            recommendations.insert(0, ("🔄 Regenerate env.sh", "wrknv generate --force"))
            recommendations.append(("📝 After regenerating", "source env.sh"))

        # Priority 2: Address warnings
        for check, _details in self.checks_warning:
            if ".venv Directory" in check:
                recommendations.append(("🗑️  Remove .venv", "rm -rf .venv && source env.sh"))
            elif "workenv Directory" in check and not has_env_issues:
                recommendations.append(("🔧 Check workenv", "wrknv doctor"))
            elif "Virtual Environment" in check:
                recommendations.append(("🚪 Exit current venv", "deactivate && source env.sh"))

        # Remove duplicates and print
        seen = set()
        for title, command in recommendations:
            key = command
            if key not in seen:
                self.console.print(f"\n  {title}")
                self.console.print(f"  [dim]$ {command}[/dim]")
                seen.add(key)

        # If multiple env.sh failures, suggest bug report
        env_failure_count = sum(1 for check, _ in self.checks_failed if "env.sh" in check)
        if env_failure_count >= 4:
            self.console.print(
                "\n[yellow]⚠️  Multiple env.sh issues detected. "
                "If regenerating doesn't help, this may be a bug in wrknv.[/yellow]"
            )
            self.console.print("  [dim]Please report at: https://github.com/provide-io/wrknv/issues[/dim]")

    def xǁWrknvDoctorǁ_print_recommendations__mutmut_101(self) -> None:
        """Print recommendations based on failed checks."""
        self.console.print("\n[bold]Recommendations:[/bold]")

        recommendations = []

        # Priority 1: Fix critical failures
        has_env_issues = False
        for check, _details in self.checks_failed:
            if "env.sh" in check or "Workenv Path" in check:
                has_env_issues = True
            elif "uv" in check:
                recommendations.append(("🔧 Install uv", "curl -LsSf https://astral.sh/uv/install.sh | sh"))
            elif "wrknv Installation" in check:
                recommendations.append(("🔧 Reinstall wrknv", "See installation docs"))

        # If there are any env.sh issues, recommend regeneration
        if has_env_issues:
            recommendations.insert(0, ("🔄 Regenerate env.sh", "wrknv generate --force"))
            recommendations.append(("📝 After regenerating", "source env.sh"))

        # Priority 2: Address warnings
        for check, _details in self.checks_warning:
            if ".venv Directory" in check:
                recommendations.append(("🗑️  Remove .venv", "rm -rf .venv && source env.sh"))
            elif "workenv Directory" in check and not has_env_issues:
                recommendations.append(("🔧 Check workenv", "wrknv doctor"))
            elif "Virtual Environment" in check:
                recommendations.append(("🚪 Exit current venv", "deactivate && source env.sh"))

        # Remove duplicates and print
        seen = set()
        for title, command in recommendations:
            key = command
            if key not in seen:
                self.console.print(f"\n  {title}")
                self.console.print(f"  [dim]$ {command}[/dim]")
                seen.add(key)

        # If multiple env.sh failures, suggest bug report
        env_failure_count = sum(1 for check, _ in self.checks_failed if "env.sh" in check)
        if env_failure_count >= 3:
            self.console.print(
                None
            )
            self.console.print("  [dim]Please report at: https://github.com/provide-io/wrknv/issues[/dim]")

    def xǁWrknvDoctorǁ_print_recommendations__mutmut_102(self) -> None:
        """Print recommendations based on failed checks."""
        self.console.print("\n[bold]Recommendations:[/bold]")

        recommendations = []

        # Priority 1: Fix critical failures
        has_env_issues = False
        for check, _details in self.checks_failed:
            if "env.sh" in check or "Workenv Path" in check:
                has_env_issues = True
            elif "uv" in check:
                recommendations.append(("🔧 Install uv", "curl -LsSf https://astral.sh/uv/install.sh | sh"))
            elif "wrknv Installation" in check:
                recommendations.append(("🔧 Reinstall wrknv", "See installation docs"))

        # If there are any env.sh issues, recommend regeneration
        if has_env_issues:
            recommendations.insert(0, ("🔄 Regenerate env.sh", "wrknv generate --force"))
            recommendations.append(("📝 After regenerating", "source env.sh"))

        # Priority 2: Address warnings
        for check, _details in self.checks_warning:
            if ".venv Directory" in check:
                recommendations.append(("🗑️  Remove .venv", "rm -rf .venv && source env.sh"))
            elif "workenv Directory" in check and not has_env_issues:
                recommendations.append(("🔧 Check workenv", "wrknv doctor"))
            elif "Virtual Environment" in check:
                recommendations.append(("🚪 Exit current venv", "deactivate && source env.sh"))

        # Remove duplicates and print
        seen = set()
        for title, command in recommendations:
            key = command
            if key not in seen:
                self.console.print(f"\n  {title}")
                self.console.print(f"  [dim]$ {command}[/dim]")
                seen.add(key)

        # If multiple env.sh failures, suggest bug report
        env_failure_count = sum(1 for check, _ in self.checks_failed if "env.sh" in check)
        if env_failure_count >= 3:
            self.console.print(
                "XX\n[yellow]⚠️  Multiple env.sh issues detected. XX"
                "If regenerating doesn't help, this may be a bug in wrknv.[/yellow]"
            )
            self.console.print("  [dim]Please report at: https://github.com/provide-io/wrknv/issues[/dim]")

    def xǁWrknvDoctorǁ_print_recommendations__mutmut_103(self) -> None:
        """Print recommendations based on failed checks."""
        self.console.print("\n[bold]Recommendations:[/bold]")

        recommendations = []

        # Priority 1: Fix critical failures
        has_env_issues = False
        for check, _details in self.checks_failed:
            if "env.sh" in check or "Workenv Path" in check:
                has_env_issues = True
            elif "uv" in check:
                recommendations.append(("🔧 Install uv", "curl -LsSf https://astral.sh/uv/install.sh | sh"))
            elif "wrknv Installation" in check:
                recommendations.append(("🔧 Reinstall wrknv", "See installation docs"))

        # If there are any env.sh issues, recommend regeneration
        if has_env_issues:
            recommendations.insert(0, ("🔄 Regenerate env.sh", "wrknv generate --force"))
            recommendations.append(("📝 After regenerating", "source env.sh"))

        # Priority 2: Address warnings
        for check, _details in self.checks_warning:
            if ".venv Directory" in check:
                recommendations.append(("🗑️  Remove .venv", "rm -rf .venv && source env.sh"))
            elif "workenv Directory" in check and not has_env_issues:
                recommendations.append(("🔧 Check workenv", "wrknv doctor"))
            elif "Virtual Environment" in check:
                recommendations.append(("🚪 Exit current venv", "deactivate && source env.sh"))

        # Remove duplicates and print
        seen = set()
        for title, command in recommendations:
            key = command
            if key not in seen:
                self.console.print(f"\n  {title}")
                self.console.print(f"  [dim]$ {command}[/dim]")
                seen.add(key)

        # If multiple env.sh failures, suggest bug report
        env_failure_count = sum(1 for check, _ in self.checks_failed if "env.sh" in check)
        if env_failure_count >= 3:
            self.console.print(
                "\n[yellow]⚠️  multiple env.sh issues detected. "
                "If regenerating doesn't help, this may be a bug in wrknv.[/yellow]"
            )
            self.console.print("  [dim]Please report at: https://github.com/provide-io/wrknv/issues[/dim]")

    def xǁWrknvDoctorǁ_print_recommendations__mutmut_104(self) -> None:
        """Print recommendations based on failed checks."""
        self.console.print("\n[bold]Recommendations:[/bold]")

        recommendations = []

        # Priority 1: Fix critical failures
        has_env_issues = False
        for check, _details in self.checks_failed:
            if "env.sh" in check or "Workenv Path" in check:
                has_env_issues = True
            elif "uv" in check:
                recommendations.append(("🔧 Install uv", "curl -LsSf https://astral.sh/uv/install.sh | sh"))
            elif "wrknv Installation" in check:
                recommendations.append(("🔧 Reinstall wrknv", "See installation docs"))

        # If there are any env.sh issues, recommend regeneration
        if has_env_issues:
            recommendations.insert(0, ("🔄 Regenerate env.sh", "wrknv generate --force"))
            recommendations.append(("📝 After regenerating", "source env.sh"))

        # Priority 2: Address warnings
        for check, _details in self.checks_warning:
            if ".venv Directory" in check:
                recommendations.append(("🗑️  Remove .venv", "rm -rf .venv && source env.sh"))
            elif "workenv Directory" in check and not has_env_issues:
                recommendations.append(("🔧 Check workenv", "wrknv doctor"))
            elif "Virtual Environment" in check:
                recommendations.append(("🚪 Exit current venv", "deactivate && source env.sh"))

        # Remove duplicates and print
        seen = set()
        for title, command in recommendations:
            key = command
            if key not in seen:
                self.console.print(f"\n  {title}")
                self.console.print(f"  [dim]$ {command}[/dim]")
                seen.add(key)

        # If multiple env.sh failures, suggest bug report
        env_failure_count = sum(1 for check, _ in self.checks_failed if "env.sh" in check)
        if env_failure_count >= 3:
            self.console.print(
                "\n[YELLOW]⚠️  MULTIPLE ENV.SH ISSUES DETECTED. "
                "If regenerating doesn't help, this may be a bug in wrknv.[/yellow]"
            )
            self.console.print("  [dim]Please report at: https://github.com/provide-io/wrknv/issues[/dim]")

    def xǁWrknvDoctorǁ_print_recommendations__mutmut_105(self) -> None:
        """Print recommendations based on failed checks."""
        self.console.print("\n[bold]Recommendations:[/bold]")

        recommendations = []

        # Priority 1: Fix critical failures
        has_env_issues = False
        for check, _details in self.checks_failed:
            if "env.sh" in check or "Workenv Path" in check:
                has_env_issues = True
            elif "uv" in check:
                recommendations.append(("🔧 Install uv", "curl -LsSf https://astral.sh/uv/install.sh | sh"))
            elif "wrknv Installation" in check:
                recommendations.append(("🔧 Reinstall wrknv", "See installation docs"))

        # If there are any env.sh issues, recommend regeneration
        if has_env_issues:
            recommendations.insert(0, ("🔄 Regenerate env.sh", "wrknv generate --force"))
            recommendations.append(("📝 After regenerating", "source env.sh"))

        # Priority 2: Address warnings
        for check, _details in self.checks_warning:
            if ".venv Directory" in check:
                recommendations.append(("🗑️  Remove .venv", "rm -rf .venv && source env.sh"))
            elif "workenv Directory" in check and not has_env_issues:
                recommendations.append(("🔧 Check workenv", "wrknv doctor"))
            elif "Virtual Environment" in check:
                recommendations.append(("🚪 Exit current venv", "deactivate && source env.sh"))

        # Remove duplicates and print
        seen = set()
        for title, command in recommendations:
            key = command
            if key not in seen:
                self.console.print(f"\n  {title}")
                self.console.print(f"  [dim]$ {command}[/dim]")
                seen.add(key)

        # If multiple env.sh failures, suggest bug report
        env_failure_count = sum(1 for check, _ in self.checks_failed if "env.sh" in check)
        if env_failure_count >= 3:
            self.console.print(
                "\n[yellow]⚠️  Multiple env.sh issues detected. "
                "XXIf regenerating doesn't help, this may be a bug in wrknv.[/yellow]XX"
            )
            self.console.print("  [dim]Please report at: https://github.com/provide-io/wrknv/issues[/dim]")

    def xǁWrknvDoctorǁ_print_recommendations__mutmut_106(self) -> None:
        """Print recommendations based on failed checks."""
        self.console.print("\n[bold]Recommendations:[/bold]")

        recommendations = []

        # Priority 1: Fix critical failures
        has_env_issues = False
        for check, _details in self.checks_failed:
            if "env.sh" in check or "Workenv Path" in check:
                has_env_issues = True
            elif "uv" in check:
                recommendations.append(("🔧 Install uv", "curl -LsSf https://astral.sh/uv/install.sh | sh"))
            elif "wrknv Installation" in check:
                recommendations.append(("🔧 Reinstall wrknv", "See installation docs"))

        # If there are any env.sh issues, recommend regeneration
        if has_env_issues:
            recommendations.insert(0, ("🔄 Regenerate env.sh", "wrknv generate --force"))
            recommendations.append(("📝 After regenerating", "source env.sh"))

        # Priority 2: Address warnings
        for check, _details in self.checks_warning:
            if ".venv Directory" in check:
                recommendations.append(("🗑️  Remove .venv", "rm -rf .venv && source env.sh"))
            elif "workenv Directory" in check and not has_env_issues:
                recommendations.append(("🔧 Check workenv", "wrknv doctor"))
            elif "Virtual Environment" in check:
                recommendations.append(("🚪 Exit current venv", "deactivate && source env.sh"))

        # Remove duplicates and print
        seen = set()
        for title, command in recommendations:
            key = command
            if key not in seen:
                self.console.print(f"\n  {title}")
                self.console.print(f"  [dim]$ {command}[/dim]")
                seen.add(key)

        # If multiple env.sh failures, suggest bug report
        env_failure_count = sum(1 for check, _ in self.checks_failed if "env.sh" in check)
        if env_failure_count >= 3:
            self.console.print(
                "\n[yellow]⚠️  Multiple env.sh issues detected. "
                "if regenerating doesn't help, this may be a bug in wrknv.[/yellow]"
            )
            self.console.print("  [dim]Please report at: https://github.com/provide-io/wrknv/issues[/dim]")

    def xǁWrknvDoctorǁ_print_recommendations__mutmut_107(self) -> None:
        """Print recommendations based on failed checks."""
        self.console.print("\n[bold]Recommendations:[/bold]")

        recommendations = []

        # Priority 1: Fix critical failures
        has_env_issues = False
        for check, _details in self.checks_failed:
            if "env.sh" in check or "Workenv Path" in check:
                has_env_issues = True
            elif "uv" in check:
                recommendations.append(("🔧 Install uv", "curl -LsSf https://astral.sh/uv/install.sh | sh"))
            elif "wrknv Installation" in check:
                recommendations.append(("🔧 Reinstall wrknv", "See installation docs"))

        # If there are any env.sh issues, recommend regeneration
        if has_env_issues:
            recommendations.insert(0, ("🔄 Regenerate env.sh", "wrknv generate --force"))
            recommendations.append(("📝 After regenerating", "source env.sh"))

        # Priority 2: Address warnings
        for check, _details in self.checks_warning:
            if ".venv Directory" in check:
                recommendations.append(("🗑️  Remove .venv", "rm -rf .venv && source env.sh"))
            elif "workenv Directory" in check and not has_env_issues:
                recommendations.append(("🔧 Check workenv", "wrknv doctor"))
            elif "Virtual Environment" in check:
                recommendations.append(("🚪 Exit current venv", "deactivate && source env.sh"))

        # Remove duplicates and print
        seen = set()
        for title, command in recommendations:
            key = command
            if key not in seen:
                self.console.print(f"\n  {title}")
                self.console.print(f"  [dim]$ {command}[/dim]")
                seen.add(key)

        # If multiple env.sh failures, suggest bug report
        env_failure_count = sum(1 for check, _ in self.checks_failed if "env.sh" in check)
        if env_failure_count >= 3:
            self.console.print(
                "\n[yellow]⚠️  Multiple env.sh issues detected. "
                "IF REGENERATING DOESN'T HELP, THIS MAY BE A BUG IN WRKNV.[/YELLOW]"
            )
            self.console.print("  [dim]Please report at: https://github.com/provide-io/wrknv/issues[/dim]")

    def xǁWrknvDoctorǁ_print_recommendations__mutmut_108(self) -> None:
        """Print recommendations based on failed checks."""
        self.console.print("\n[bold]Recommendations:[/bold]")

        recommendations = []

        # Priority 1: Fix critical failures
        has_env_issues = False
        for check, _details in self.checks_failed:
            if "env.sh" in check or "Workenv Path" in check:
                has_env_issues = True
            elif "uv" in check:
                recommendations.append(("🔧 Install uv", "curl -LsSf https://astral.sh/uv/install.sh | sh"))
            elif "wrknv Installation" in check:
                recommendations.append(("🔧 Reinstall wrknv", "See installation docs"))

        # If there are any env.sh issues, recommend regeneration
        if has_env_issues:
            recommendations.insert(0, ("🔄 Regenerate env.sh", "wrknv generate --force"))
            recommendations.append(("📝 After regenerating", "source env.sh"))

        # Priority 2: Address warnings
        for check, _details in self.checks_warning:
            if ".venv Directory" in check:
                recommendations.append(("🗑️  Remove .venv", "rm -rf .venv && source env.sh"))
            elif "workenv Directory" in check and not has_env_issues:
                recommendations.append(("🔧 Check workenv", "wrknv doctor"))
            elif "Virtual Environment" in check:
                recommendations.append(("🚪 Exit current venv", "deactivate && source env.sh"))

        # Remove duplicates and print
        seen = set()
        for title, command in recommendations:
            key = command
            if key not in seen:
                self.console.print(f"\n  {title}")
                self.console.print(f"  [dim]$ {command}[/dim]")
                seen.add(key)

        # If multiple env.sh failures, suggest bug report
        env_failure_count = sum(1 for check, _ in self.checks_failed if "env.sh" in check)
        if env_failure_count >= 3:
            self.console.print(
                "\n[yellow]⚠️  Multiple env.sh issues detected. "
                "If regenerating doesn't help, this may be a bug in wrknv.[/yellow]"
            )
            self.console.print(None)

    def xǁWrknvDoctorǁ_print_recommendations__mutmut_109(self) -> None:
        """Print recommendations based on failed checks."""
        self.console.print("\n[bold]Recommendations:[/bold]")

        recommendations = []

        # Priority 1: Fix critical failures
        has_env_issues = False
        for check, _details in self.checks_failed:
            if "env.sh" in check or "Workenv Path" in check:
                has_env_issues = True
            elif "uv" in check:
                recommendations.append(("🔧 Install uv", "curl -LsSf https://astral.sh/uv/install.sh | sh"))
            elif "wrknv Installation" in check:
                recommendations.append(("🔧 Reinstall wrknv", "See installation docs"))

        # If there are any env.sh issues, recommend regeneration
        if has_env_issues:
            recommendations.insert(0, ("🔄 Regenerate env.sh", "wrknv generate --force"))
            recommendations.append(("📝 After regenerating", "source env.sh"))

        # Priority 2: Address warnings
        for check, _details in self.checks_warning:
            if ".venv Directory" in check:
                recommendations.append(("🗑️  Remove .venv", "rm -rf .venv && source env.sh"))
            elif "workenv Directory" in check and not has_env_issues:
                recommendations.append(("🔧 Check workenv", "wrknv doctor"))
            elif "Virtual Environment" in check:
                recommendations.append(("🚪 Exit current venv", "deactivate && source env.sh"))

        # Remove duplicates and print
        seen = set()
        for title, command in recommendations:
            key = command
            if key not in seen:
                self.console.print(f"\n  {title}")
                self.console.print(f"  [dim]$ {command}[/dim]")
                seen.add(key)

        # If multiple env.sh failures, suggest bug report
        env_failure_count = sum(1 for check, _ in self.checks_failed if "env.sh" in check)
        if env_failure_count >= 3:
            self.console.print(
                "\n[yellow]⚠️  Multiple env.sh issues detected. "
                "If regenerating doesn't help, this may be a bug in wrknv.[/yellow]"
            )
            self.console.print("XX  [dim]Please report at: https://github.com/provide-io/wrknv/issues[/dim]XX")

    def xǁWrknvDoctorǁ_print_recommendations__mutmut_110(self) -> None:
        """Print recommendations based on failed checks."""
        self.console.print("\n[bold]Recommendations:[/bold]")

        recommendations = []

        # Priority 1: Fix critical failures
        has_env_issues = False
        for check, _details in self.checks_failed:
            if "env.sh" in check or "Workenv Path" in check:
                has_env_issues = True
            elif "uv" in check:
                recommendations.append(("🔧 Install uv", "curl -LsSf https://astral.sh/uv/install.sh | sh"))
            elif "wrknv Installation" in check:
                recommendations.append(("🔧 Reinstall wrknv", "See installation docs"))

        # If there are any env.sh issues, recommend regeneration
        if has_env_issues:
            recommendations.insert(0, ("🔄 Regenerate env.sh", "wrknv generate --force"))
            recommendations.append(("📝 After regenerating", "source env.sh"))

        # Priority 2: Address warnings
        for check, _details in self.checks_warning:
            if ".venv Directory" in check:
                recommendations.append(("🗑️  Remove .venv", "rm -rf .venv && source env.sh"))
            elif "workenv Directory" in check and not has_env_issues:
                recommendations.append(("🔧 Check workenv", "wrknv doctor"))
            elif "Virtual Environment" in check:
                recommendations.append(("🚪 Exit current venv", "deactivate && source env.sh"))

        # Remove duplicates and print
        seen = set()
        for title, command in recommendations:
            key = command
            if key not in seen:
                self.console.print(f"\n  {title}")
                self.console.print(f"  [dim]$ {command}[/dim]")
                seen.add(key)

        # If multiple env.sh failures, suggest bug report
        env_failure_count = sum(1 for check, _ in self.checks_failed if "env.sh" in check)
        if env_failure_count >= 3:
            self.console.print(
                "\n[yellow]⚠️  Multiple env.sh issues detected. "
                "If regenerating doesn't help, this may be a bug in wrknv.[/yellow]"
            )
            self.console.print("  [dim]please report at: https://github.com/provide-io/wrknv/issues[/dim]")

    def xǁWrknvDoctorǁ_print_recommendations__mutmut_111(self) -> None:
        """Print recommendations based on failed checks."""
        self.console.print("\n[bold]Recommendations:[/bold]")

        recommendations = []

        # Priority 1: Fix critical failures
        has_env_issues = False
        for check, _details in self.checks_failed:
            if "env.sh" in check or "Workenv Path" in check:
                has_env_issues = True
            elif "uv" in check:
                recommendations.append(("🔧 Install uv", "curl -LsSf https://astral.sh/uv/install.sh | sh"))
            elif "wrknv Installation" in check:
                recommendations.append(("🔧 Reinstall wrknv", "See installation docs"))

        # If there are any env.sh issues, recommend regeneration
        if has_env_issues:
            recommendations.insert(0, ("🔄 Regenerate env.sh", "wrknv generate --force"))
            recommendations.append(("📝 After regenerating", "source env.sh"))

        # Priority 2: Address warnings
        for check, _details in self.checks_warning:
            if ".venv Directory" in check:
                recommendations.append(("🗑️  Remove .venv", "rm -rf .venv && source env.sh"))
            elif "workenv Directory" in check and not has_env_issues:
                recommendations.append(("🔧 Check workenv", "wrknv doctor"))
            elif "Virtual Environment" in check:
                recommendations.append(("🚪 Exit current venv", "deactivate && source env.sh"))

        # Remove duplicates and print
        seen = set()
        for title, command in recommendations:
            key = command
            if key not in seen:
                self.console.print(f"\n  {title}")
                self.console.print(f"  [dim]$ {command}[/dim]")
                seen.add(key)

        # If multiple env.sh failures, suggest bug report
        env_failure_count = sum(1 for check, _ in self.checks_failed if "env.sh" in check)
        if env_failure_count >= 3:
            self.console.print(
                "\n[yellow]⚠️  Multiple env.sh issues detected. "
                "If regenerating doesn't help, this may be a bug in wrknv.[/yellow]"
            )
            self.console.print("  [DIM]PLEASE REPORT AT: HTTPS://GITHUB.COM/PROVIDE-IO/WRKNV/ISSUES[/DIM]")
    
    xǁWrknvDoctorǁ_print_recommendations__mutmut_mutants : ClassVar[MutantDict] = {
    'xǁWrknvDoctorǁ_print_recommendations__mutmut_1': xǁWrknvDoctorǁ_print_recommendations__mutmut_1, 
        'xǁWrknvDoctorǁ_print_recommendations__mutmut_2': xǁWrknvDoctorǁ_print_recommendations__mutmut_2, 
        'xǁWrknvDoctorǁ_print_recommendations__mutmut_3': xǁWrknvDoctorǁ_print_recommendations__mutmut_3, 
        'xǁWrknvDoctorǁ_print_recommendations__mutmut_4': xǁWrknvDoctorǁ_print_recommendations__mutmut_4, 
        'xǁWrknvDoctorǁ_print_recommendations__mutmut_5': xǁWrknvDoctorǁ_print_recommendations__mutmut_5, 
        'xǁWrknvDoctorǁ_print_recommendations__mutmut_6': xǁWrknvDoctorǁ_print_recommendations__mutmut_6, 
        'xǁWrknvDoctorǁ_print_recommendations__mutmut_7': xǁWrknvDoctorǁ_print_recommendations__mutmut_7, 
        'xǁWrknvDoctorǁ_print_recommendations__mutmut_8': xǁWrknvDoctorǁ_print_recommendations__mutmut_8, 
        'xǁWrknvDoctorǁ_print_recommendations__mutmut_9': xǁWrknvDoctorǁ_print_recommendations__mutmut_9, 
        'xǁWrknvDoctorǁ_print_recommendations__mutmut_10': xǁWrknvDoctorǁ_print_recommendations__mutmut_10, 
        'xǁWrknvDoctorǁ_print_recommendations__mutmut_11': xǁWrknvDoctorǁ_print_recommendations__mutmut_11, 
        'xǁWrknvDoctorǁ_print_recommendations__mutmut_12': xǁWrknvDoctorǁ_print_recommendations__mutmut_12, 
        'xǁWrknvDoctorǁ_print_recommendations__mutmut_13': xǁWrknvDoctorǁ_print_recommendations__mutmut_13, 
        'xǁWrknvDoctorǁ_print_recommendations__mutmut_14': xǁWrknvDoctorǁ_print_recommendations__mutmut_14, 
        'xǁWrknvDoctorǁ_print_recommendations__mutmut_15': xǁWrknvDoctorǁ_print_recommendations__mutmut_15, 
        'xǁWrknvDoctorǁ_print_recommendations__mutmut_16': xǁWrknvDoctorǁ_print_recommendations__mutmut_16, 
        'xǁWrknvDoctorǁ_print_recommendations__mutmut_17': xǁWrknvDoctorǁ_print_recommendations__mutmut_17, 
        'xǁWrknvDoctorǁ_print_recommendations__mutmut_18': xǁWrknvDoctorǁ_print_recommendations__mutmut_18, 
        'xǁWrknvDoctorǁ_print_recommendations__mutmut_19': xǁWrknvDoctorǁ_print_recommendations__mutmut_19, 
        'xǁWrknvDoctorǁ_print_recommendations__mutmut_20': xǁWrknvDoctorǁ_print_recommendations__mutmut_20, 
        'xǁWrknvDoctorǁ_print_recommendations__mutmut_21': xǁWrknvDoctorǁ_print_recommendations__mutmut_21, 
        'xǁWrknvDoctorǁ_print_recommendations__mutmut_22': xǁWrknvDoctorǁ_print_recommendations__mutmut_22, 
        'xǁWrknvDoctorǁ_print_recommendations__mutmut_23': xǁWrknvDoctorǁ_print_recommendations__mutmut_23, 
        'xǁWrknvDoctorǁ_print_recommendations__mutmut_24': xǁWrknvDoctorǁ_print_recommendations__mutmut_24, 
        'xǁWrknvDoctorǁ_print_recommendations__mutmut_25': xǁWrknvDoctorǁ_print_recommendations__mutmut_25, 
        'xǁWrknvDoctorǁ_print_recommendations__mutmut_26': xǁWrknvDoctorǁ_print_recommendations__mutmut_26, 
        'xǁWrknvDoctorǁ_print_recommendations__mutmut_27': xǁWrknvDoctorǁ_print_recommendations__mutmut_27, 
        'xǁWrknvDoctorǁ_print_recommendations__mutmut_28': xǁWrknvDoctorǁ_print_recommendations__mutmut_28, 
        'xǁWrknvDoctorǁ_print_recommendations__mutmut_29': xǁWrknvDoctorǁ_print_recommendations__mutmut_29, 
        'xǁWrknvDoctorǁ_print_recommendations__mutmut_30': xǁWrknvDoctorǁ_print_recommendations__mutmut_30, 
        'xǁWrknvDoctorǁ_print_recommendations__mutmut_31': xǁWrknvDoctorǁ_print_recommendations__mutmut_31, 
        'xǁWrknvDoctorǁ_print_recommendations__mutmut_32': xǁWrknvDoctorǁ_print_recommendations__mutmut_32, 
        'xǁWrknvDoctorǁ_print_recommendations__mutmut_33': xǁWrknvDoctorǁ_print_recommendations__mutmut_33, 
        'xǁWrknvDoctorǁ_print_recommendations__mutmut_34': xǁWrknvDoctorǁ_print_recommendations__mutmut_34, 
        'xǁWrknvDoctorǁ_print_recommendations__mutmut_35': xǁWrknvDoctorǁ_print_recommendations__mutmut_35, 
        'xǁWrknvDoctorǁ_print_recommendations__mutmut_36': xǁWrknvDoctorǁ_print_recommendations__mutmut_36, 
        'xǁWrknvDoctorǁ_print_recommendations__mutmut_37': xǁWrknvDoctorǁ_print_recommendations__mutmut_37, 
        'xǁWrknvDoctorǁ_print_recommendations__mutmut_38': xǁWrknvDoctorǁ_print_recommendations__mutmut_38, 
        'xǁWrknvDoctorǁ_print_recommendations__mutmut_39': xǁWrknvDoctorǁ_print_recommendations__mutmut_39, 
        'xǁWrknvDoctorǁ_print_recommendations__mutmut_40': xǁWrknvDoctorǁ_print_recommendations__mutmut_40, 
        'xǁWrknvDoctorǁ_print_recommendations__mutmut_41': xǁWrknvDoctorǁ_print_recommendations__mutmut_41, 
        'xǁWrknvDoctorǁ_print_recommendations__mutmut_42': xǁWrknvDoctorǁ_print_recommendations__mutmut_42, 
        'xǁWrknvDoctorǁ_print_recommendations__mutmut_43': xǁWrknvDoctorǁ_print_recommendations__mutmut_43, 
        'xǁWrknvDoctorǁ_print_recommendations__mutmut_44': xǁWrknvDoctorǁ_print_recommendations__mutmut_44, 
        'xǁWrknvDoctorǁ_print_recommendations__mutmut_45': xǁWrknvDoctorǁ_print_recommendations__mutmut_45, 
        'xǁWrknvDoctorǁ_print_recommendations__mutmut_46': xǁWrknvDoctorǁ_print_recommendations__mutmut_46, 
        'xǁWrknvDoctorǁ_print_recommendations__mutmut_47': xǁWrknvDoctorǁ_print_recommendations__mutmut_47, 
        'xǁWrknvDoctorǁ_print_recommendations__mutmut_48': xǁWrknvDoctorǁ_print_recommendations__mutmut_48, 
        'xǁWrknvDoctorǁ_print_recommendations__mutmut_49': xǁWrknvDoctorǁ_print_recommendations__mutmut_49, 
        'xǁWrknvDoctorǁ_print_recommendations__mutmut_50': xǁWrknvDoctorǁ_print_recommendations__mutmut_50, 
        'xǁWrknvDoctorǁ_print_recommendations__mutmut_51': xǁWrknvDoctorǁ_print_recommendations__mutmut_51, 
        'xǁWrknvDoctorǁ_print_recommendations__mutmut_52': xǁWrknvDoctorǁ_print_recommendations__mutmut_52, 
        'xǁWrknvDoctorǁ_print_recommendations__mutmut_53': xǁWrknvDoctorǁ_print_recommendations__mutmut_53, 
        'xǁWrknvDoctorǁ_print_recommendations__mutmut_54': xǁWrknvDoctorǁ_print_recommendations__mutmut_54, 
        'xǁWrknvDoctorǁ_print_recommendations__mutmut_55': xǁWrknvDoctorǁ_print_recommendations__mutmut_55, 
        'xǁWrknvDoctorǁ_print_recommendations__mutmut_56': xǁWrknvDoctorǁ_print_recommendations__mutmut_56, 
        'xǁWrknvDoctorǁ_print_recommendations__mutmut_57': xǁWrknvDoctorǁ_print_recommendations__mutmut_57, 
        'xǁWrknvDoctorǁ_print_recommendations__mutmut_58': xǁWrknvDoctorǁ_print_recommendations__mutmut_58, 
        'xǁWrknvDoctorǁ_print_recommendations__mutmut_59': xǁWrknvDoctorǁ_print_recommendations__mutmut_59, 
        'xǁWrknvDoctorǁ_print_recommendations__mutmut_60': xǁWrknvDoctorǁ_print_recommendations__mutmut_60, 
        'xǁWrknvDoctorǁ_print_recommendations__mutmut_61': xǁWrknvDoctorǁ_print_recommendations__mutmut_61, 
        'xǁWrknvDoctorǁ_print_recommendations__mutmut_62': xǁWrknvDoctorǁ_print_recommendations__mutmut_62, 
        'xǁWrknvDoctorǁ_print_recommendations__mutmut_63': xǁWrknvDoctorǁ_print_recommendations__mutmut_63, 
        'xǁWrknvDoctorǁ_print_recommendations__mutmut_64': xǁWrknvDoctorǁ_print_recommendations__mutmut_64, 
        'xǁWrknvDoctorǁ_print_recommendations__mutmut_65': xǁWrknvDoctorǁ_print_recommendations__mutmut_65, 
        'xǁWrknvDoctorǁ_print_recommendations__mutmut_66': xǁWrknvDoctorǁ_print_recommendations__mutmut_66, 
        'xǁWrknvDoctorǁ_print_recommendations__mutmut_67': xǁWrknvDoctorǁ_print_recommendations__mutmut_67, 
        'xǁWrknvDoctorǁ_print_recommendations__mutmut_68': xǁWrknvDoctorǁ_print_recommendations__mutmut_68, 
        'xǁWrknvDoctorǁ_print_recommendations__mutmut_69': xǁWrknvDoctorǁ_print_recommendations__mutmut_69, 
        'xǁWrknvDoctorǁ_print_recommendations__mutmut_70': xǁWrknvDoctorǁ_print_recommendations__mutmut_70, 
        'xǁWrknvDoctorǁ_print_recommendations__mutmut_71': xǁWrknvDoctorǁ_print_recommendations__mutmut_71, 
        'xǁWrknvDoctorǁ_print_recommendations__mutmut_72': xǁWrknvDoctorǁ_print_recommendations__mutmut_72, 
        'xǁWrknvDoctorǁ_print_recommendations__mutmut_73': xǁWrknvDoctorǁ_print_recommendations__mutmut_73, 
        'xǁWrknvDoctorǁ_print_recommendations__mutmut_74': xǁWrknvDoctorǁ_print_recommendations__mutmut_74, 
        'xǁWrknvDoctorǁ_print_recommendations__mutmut_75': xǁWrknvDoctorǁ_print_recommendations__mutmut_75, 
        'xǁWrknvDoctorǁ_print_recommendations__mutmut_76': xǁWrknvDoctorǁ_print_recommendations__mutmut_76, 
        'xǁWrknvDoctorǁ_print_recommendations__mutmut_77': xǁWrknvDoctorǁ_print_recommendations__mutmut_77, 
        'xǁWrknvDoctorǁ_print_recommendations__mutmut_78': xǁWrknvDoctorǁ_print_recommendations__mutmut_78, 
        'xǁWrknvDoctorǁ_print_recommendations__mutmut_79': xǁWrknvDoctorǁ_print_recommendations__mutmut_79, 
        'xǁWrknvDoctorǁ_print_recommendations__mutmut_80': xǁWrknvDoctorǁ_print_recommendations__mutmut_80, 
        'xǁWrknvDoctorǁ_print_recommendations__mutmut_81': xǁWrknvDoctorǁ_print_recommendations__mutmut_81, 
        'xǁWrknvDoctorǁ_print_recommendations__mutmut_82': xǁWrknvDoctorǁ_print_recommendations__mutmut_82, 
        'xǁWrknvDoctorǁ_print_recommendations__mutmut_83': xǁWrknvDoctorǁ_print_recommendations__mutmut_83, 
        'xǁWrknvDoctorǁ_print_recommendations__mutmut_84': xǁWrknvDoctorǁ_print_recommendations__mutmut_84, 
        'xǁWrknvDoctorǁ_print_recommendations__mutmut_85': xǁWrknvDoctorǁ_print_recommendations__mutmut_85, 
        'xǁWrknvDoctorǁ_print_recommendations__mutmut_86': xǁWrknvDoctorǁ_print_recommendations__mutmut_86, 
        'xǁWrknvDoctorǁ_print_recommendations__mutmut_87': xǁWrknvDoctorǁ_print_recommendations__mutmut_87, 
        'xǁWrknvDoctorǁ_print_recommendations__mutmut_88': xǁWrknvDoctorǁ_print_recommendations__mutmut_88, 
        'xǁWrknvDoctorǁ_print_recommendations__mutmut_89': xǁWrknvDoctorǁ_print_recommendations__mutmut_89, 
        'xǁWrknvDoctorǁ_print_recommendations__mutmut_90': xǁWrknvDoctorǁ_print_recommendations__mutmut_90, 
        'xǁWrknvDoctorǁ_print_recommendations__mutmut_91': xǁWrknvDoctorǁ_print_recommendations__mutmut_91, 
        'xǁWrknvDoctorǁ_print_recommendations__mutmut_92': xǁWrknvDoctorǁ_print_recommendations__mutmut_92, 
        'xǁWrknvDoctorǁ_print_recommendations__mutmut_93': xǁWrknvDoctorǁ_print_recommendations__mutmut_93, 
        'xǁWrknvDoctorǁ_print_recommendations__mutmut_94': xǁWrknvDoctorǁ_print_recommendations__mutmut_94, 
        'xǁWrknvDoctorǁ_print_recommendations__mutmut_95': xǁWrknvDoctorǁ_print_recommendations__mutmut_95, 
        'xǁWrknvDoctorǁ_print_recommendations__mutmut_96': xǁWrknvDoctorǁ_print_recommendations__mutmut_96, 
        'xǁWrknvDoctorǁ_print_recommendations__mutmut_97': xǁWrknvDoctorǁ_print_recommendations__mutmut_97, 
        'xǁWrknvDoctorǁ_print_recommendations__mutmut_98': xǁWrknvDoctorǁ_print_recommendations__mutmut_98, 
        'xǁWrknvDoctorǁ_print_recommendations__mutmut_99': xǁWrknvDoctorǁ_print_recommendations__mutmut_99, 
        'xǁWrknvDoctorǁ_print_recommendations__mutmut_100': xǁWrknvDoctorǁ_print_recommendations__mutmut_100, 
        'xǁWrknvDoctorǁ_print_recommendations__mutmut_101': xǁWrknvDoctorǁ_print_recommendations__mutmut_101, 
        'xǁWrknvDoctorǁ_print_recommendations__mutmut_102': xǁWrknvDoctorǁ_print_recommendations__mutmut_102, 
        'xǁWrknvDoctorǁ_print_recommendations__mutmut_103': xǁWrknvDoctorǁ_print_recommendations__mutmut_103, 
        'xǁWrknvDoctorǁ_print_recommendations__mutmut_104': xǁWrknvDoctorǁ_print_recommendations__mutmut_104, 
        'xǁWrknvDoctorǁ_print_recommendations__mutmut_105': xǁWrknvDoctorǁ_print_recommendations__mutmut_105, 
        'xǁWrknvDoctorǁ_print_recommendations__mutmut_106': xǁWrknvDoctorǁ_print_recommendations__mutmut_106, 
        'xǁWrknvDoctorǁ_print_recommendations__mutmut_107': xǁWrknvDoctorǁ_print_recommendations__mutmut_107, 
        'xǁWrknvDoctorǁ_print_recommendations__mutmut_108': xǁWrknvDoctorǁ_print_recommendations__mutmut_108, 
        'xǁWrknvDoctorǁ_print_recommendations__mutmut_109': xǁWrknvDoctorǁ_print_recommendations__mutmut_109, 
        'xǁWrknvDoctorǁ_print_recommendations__mutmut_110': xǁWrknvDoctorǁ_print_recommendations__mutmut_110, 
        'xǁWrknvDoctorǁ_print_recommendations__mutmut_111': xǁWrknvDoctorǁ_print_recommendations__mutmut_111
    }
    
    def _print_recommendations(self, *args, **kwargs):
        result = _mutmut_trampoline(object.__getattribute__(self, "xǁWrknvDoctorǁ_print_recommendations__mutmut_orig"), object.__getattribute__(self, "xǁWrknvDoctorǁ_print_recommendations__mutmut_mutants"), args, kwargs, self)
        return result 
    
    _print_recommendations.__signature__ = _mutmut_signature(xǁWrknvDoctorǁ_print_recommendations__mutmut_orig)
    xǁWrknvDoctorǁ_print_recommendations__mutmut_orig.__name__ = 'xǁWrknvDoctorǁ_print_recommendations'


def x_run_doctor__mutmut_orig(verbose: bool = False) -> int:
    """
    Run the doctor diagnostic tool.

    Args:
        verbose: If True, show detailed output.

    Returns:
        Exit code.
    """
    doctor = WrknvDoctor()
    return doctor.run(verbose)


def x_run_doctor__mutmut_1(verbose: bool = True) -> int:
    """
    Run the doctor diagnostic tool.

    Args:
        verbose: If True, show detailed output.

    Returns:
        Exit code.
    """
    doctor = WrknvDoctor()
    return doctor.run(verbose)


def x_run_doctor__mutmut_2(verbose: bool = False) -> int:
    """
    Run the doctor diagnostic tool.

    Args:
        verbose: If True, show detailed output.

    Returns:
        Exit code.
    """
    doctor = None
    return doctor.run(verbose)


def x_run_doctor__mutmut_3(verbose: bool = False) -> int:
    """
    Run the doctor diagnostic tool.

    Args:
        verbose: If True, show detailed output.

    Returns:
        Exit code.
    """
    doctor = WrknvDoctor()
    return doctor.run(None)

x_run_doctor__mutmut_mutants : ClassVar[MutantDict] = {
'x_run_doctor__mutmut_1': x_run_doctor__mutmut_1, 
    'x_run_doctor__mutmut_2': x_run_doctor__mutmut_2, 
    'x_run_doctor__mutmut_3': x_run_doctor__mutmut_3
}

def run_doctor(*args, **kwargs):
    result = _mutmut_trampoline(x_run_doctor__mutmut_orig, x_run_doctor__mutmut_mutants, args, kwargs)
    return result 

run_doctor.__signature__ = _mutmut_signature(x_run_doctor__mutmut_orig)
x_run_doctor__mutmut_orig.__name__ = 'x_run_doctor'


# 🧰🌍🔚
