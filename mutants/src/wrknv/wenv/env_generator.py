#
# SPDX-FileCopyrightText: Copyright (c) provide.io llc. All rights reserved.
# SPDX-License-Identifier: Apache-2.0
#

"""Environment Script Generator
============================
Generate env.sh and env.ps1 scripts from templates."""

from __future__ import annotations

from pathlib import Path
from typing import Any

from jinja2 import Environment, FileSystemLoader, select_autoescape
from provide.foundation import logger

from wrknv.cli.visual import Emoji, print_success
from inspect import signature as _mutmut_signature
from typing import Annotated
from typing import Callable
from typing import ClassVar


MutantDict = Annotated[dict[str, Callable], "Mutant"]


def _mutmut_trampoline(orig, mutants, call_args, call_kwargs, self_arg = None):
    """Forward call to original or mutated function, depending on the environment"""
    import os
    mutant_under_test = os.environ['MUTANT_UNDER_TEST']
    if mutant_under_test == 'fail':
        from mutmut.__main__ import MutmutProgrammaticFailException
        raise MutmutProgrammaticFailException('Failed programmatically')      
    elif mutant_under_test == 'stats':
        from mutmut.__main__ import record_trampoline_hit
        record_trampoline_hit(orig.__module__ + '.' + orig.__name__)
        result = orig(*call_args, **call_kwargs)
        return result
    prefix = orig.__module__ + '.' + orig.__name__ + '__mutmut_'
    if not mutant_under_test.startswith(prefix):
        result = orig(*call_args, **call_kwargs)
        return result
    mutant_name = mutant_under_test.rpartition('.')[-1]
    if self_arg is not None:
        # call to a class method where self is not bound
        result = mutants[mutant_name](self_arg, *call_args, **call_kwargs)
    else:
        result = mutants[mutant_name](*call_args, **call_kwargs)
    return result


class EnvScriptGenerator:
    """Generate environment setup scripts for both bash and PowerShell."""

    def xǁEnvScriptGeneratorǁ__init____mutmut_orig(self, template_base_dir: Path | None = None) -> None:
        """Initialize the generator with template directory."""
        if template_base_dir is None:
            template_base_dir = Path(__file__).parent.parent / "templates" / "env"

        self.template_base_dir = template_base_dir

        # Create separate environments for sh and pwsh templates
        self.sh_env = Environment(
            loader=FileSystemLoader(template_base_dir / "sh"),
            autoescape=select_autoescape(),
            trim_blocks=True,
            lstrip_blocks=True,
        )

        self.ps1_env = Environment(
            loader=FileSystemLoader(template_base_dir / "pwsh"),
            autoescape=select_autoescape(),
            trim_blocks=True,
            lstrip_blocks=True,
        )

    def xǁEnvScriptGeneratorǁ__init____mutmut_1(self, template_base_dir: Path | None = None) -> None:
        """Initialize the generator with template directory."""
        if template_base_dir is not None:
            template_base_dir = Path(__file__).parent.parent / "templates" / "env"

        self.template_base_dir = template_base_dir

        # Create separate environments for sh and pwsh templates
        self.sh_env = Environment(
            loader=FileSystemLoader(template_base_dir / "sh"),
            autoescape=select_autoescape(),
            trim_blocks=True,
            lstrip_blocks=True,
        )

        self.ps1_env = Environment(
            loader=FileSystemLoader(template_base_dir / "pwsh"),
            autoescape=select_autoescape(),
            trim_blocks=True,
            lstrip_blocks=True,
        )

    def xǁEnvScriptGeneratorǁ__init____mutmut_2(self, template_base_dir: Path | None = None) -> None:
        """Initialize the generator with template directory."""
        if template_base_dir is None:
            template_base_dir = None

        self.template_base_dir = template_base_dir

        # Create separate environments for sh and pwsh templates
        self.sh_env = Environment(
            loader=FileSystemLoader(template_base_dir / "sh"),
            autoescape=select_autoescape(),
            trim_blocks=True,
            lstrip_blocks=True,
        )

        self.ps1_env = Environment(
            loader=FileSystemLoader(template_base_dir / "pwsh"),
            autoescape=select_autoescape(),
            trim_blocks=True,
            lstrip_blocks=True,
        )

    def xǁEnvScriptGeneratorǁ__init____mutmut_3(self, template_base_dir: Path | None = None) -> None:
        """Initialize the generator with template directory."""
        if template_base_dir is None:
            template_base_dir = Path(__file__).parent.parent / "templates" * "env"

        self.template_base_dir = template_base_dir

        # Create separate environments for sh and pwsh templates
        self.sh_env = Environment(
            loader=FileSystemLoader(template_base_dir / "sh"),
            autoescape=select_autoescape(),
            trim_blocks=True,
            lstrip_blocks=True,
        )

        self.ps1_env = Environment(
            loader=FileSystemLoader(template_base_dir / "pwsh"),
            autoescape=select_autoescape(),
            trim_blocks=True,
            lstrip_blocks=True,
        )

    def xǁEnvScriptGeneratorǁ__init____mutmut_4(self, template_base_dir: Path | None = None) -> None:
        """Initialize the generator with template directory."""
        if template_base_dir is None:
            template_base_dir = Path(__file__).parent.parent * "templates" / "env"

        self.template_base_dir = template_base_dir

        # Create separate environments for sh and pwsh templates
        self.sh_env = Environment(
            loader=FileSystemLoader(template_base_dir / "sh"),
            autoescape=select_autoescape(),
            trim_blocks=True,
            lstrip_blocks=True,
        )

        self.ps1_env = Environment(
            loader=FileSystemLoader(template_base_dir / "pwsh"),
            autoescape=select_autoescape(),
            trim_blocks=True,
            lstrip_blocks=True,
        )

    def xǁEnvScriptGeneratorǁ__init____mutmut_5(self, template_base_dir: Path | None = None) -> None:
        """Initialize the generator with template directory."""
        if template_base_dir is None:
            template_base_dir = Path(None).parent.parent / "templates" / "env"

        self.template_base_dir = template_base_dir

        # Create separate environments for sh and pwsh templates
        self.sh_env = Environment(
            loader=FileSystemLoader(template_base_dir / "sh"),
            autoescape=select_autoescape(),
            trim_blocks=True,
            lstrip_blocks=True,
        )

        self.ps1_env = Environment(
            loader=FileSystemLoader(template_base_dir / "pwsh"),
            autoescape=select_autoescape(),
            trim_blocks=True,
            lstrip_blocks=True,
        )

    def xǁEnvScriptGeneratorǁ__init____mutmut_6(self, template_base_dir: Path | None = None) -> None:
        """Initialize the generator with template directory."""
        if template_base_dir is None:
            template_base_dir = Path(__file__).parent.parent / "XXtemplatesXX" / "env"

        self.template_base_dir = template_base_dir

        # Create separate environments for sh and pwsh templates
        self.sh_env = Environment(
            loader=FileSystemLoader(template_base_dir / "sh"),
            autoescape=select_autoescape(),
            trim_blocks=True,
            lstrip_blocks=True,
        )

        self.ps1_env = Environment(
            loader=FileSystemLoader(template_base_dir / "pwsh"),
            autoescape=select_autoescape(),
            trim_blocks=True,
            lstrip_blocks=True,
        )

    def xǁEnvScriptGeneratorǁ__init____mutmut_7(self, template_base_dir: Path | None = None) -> None:
        """Initialize the generator with template directory."""
        if template_base_dir is None:
            template_base_dir = Path(__file__).parent.parent / "TEMPLATES" / "env"

        self.template_base_dir = template_base_dir

        # Create separate environments for sh and pwsh templates
        self.sh_env = Environment(
            loader=FileSystemLoader(template_base_dir / "sh"),
            autoescape=select_autoescape(),
            trim_blocks=True,
            lstrip_blocks=True,
        )

        self.ps1_env = Environment(
            loader=FileSystemLoader(template_base_dir / "pwsh"),
            autoescape=select_autoescape(),
            trim_blocks=True,
            lstrip_blocks=True,
        )

    def xǁEnvScriptGeneratorǁ__init____mutmut_8(self, template_base_dir: Path | None = None) -> None:
        """Initialize the generator with template directory."""
        if template_base_dir is None:
            template_base_dir = Path(__file__).parent.parent / "templates" / "XXenvXX"

        self.template_base_dir = template_base_dir

        # Create separate environments for sh and pwsh templates
        self.sh_env = Environment(
            loader=FileSystemLoader(template_base_dir / "sh"),
            autoescape=select_autoescape(),
            trim_blocks=True,
            lstrip_blocks=True,
        )

        self.ps1_env = Environment(
            loader=FileSystemLoader(template_base_dir / "pwsh"),
            autoescape=select_autoescape(),
            trim_blocks=True,
            lstrip_blocks=True,
        )

    def xǁEnvScriptGeneratorǁ__init____mutmut_9(self, template_base_dir: Path | None = None) -> None:
        """Initialize the generator with template directory."""
        if template_base_dir is None:
            template_base_dir = Path(__file__).parent.parent / "templates" / "ENV"

        self.template_base_dir = template_base_dir

        # Create separate environments for sh and pwsh templates
        self.sh_env = Environment(
            loader=FileSystemLoader(template_base_dir / "sh"),
            autoescape=select_autoescape(),
            trim_blocks=True,
            lstrip_blocks=True,
        )

        self.ps1_env = Environment(
            loader=FileSystemLoader(template_base_dir / "pwsh"),
            autoescape=select_autoescape(),
            trim_blocks=True,
            lstrip_blocks=True,
        )

    def xǁEnvScriptGeneratorǁ__init____mutmut_10(self, template_base_dir: Path | None = None) -> None:
        """Initialize the generator with template directory."""
        if template_base_dir is None:
            template_base_dir = Path(__file__).parent.parent / "templates" / "env"

        self.template_base_dir = None

        # Create separate environments for sh and pwsh templates
        self.sh_env = Environment(
            loader=FileSystemLoader(template_base_dir / "sh"),
            autoescape=select_autoescape(),
            trim_blocks=True,
            lstrip_blocks=True,
        )

        self.ps1_env = Environment(
            loader=FileSystemLoader(template_base_dir / "pwsh"),
            autoescape=select_autoescape(),
            trim_blocks=True,
            lstrip_blocks=True,
        )

    def xǁEnvScriptGeneratorǁ__init____mutmut_11(self, template_base_dir: Path | None = None) -> None:
        """Initialize the generator with template directory."""
        if template_base_dir is None:
            template_base_dir = Path(__file__).parent.parent / "templates" / "env"

        self.template_base_dir = template_base_dir

        # Create separate environments for sh and pwsh templates
        self.sh_env = None

        self.ps1_env = Environment(
            loader=FileSystemLoader(template_base_dir / "pwsh"),
            autoescape=select_autoescape(),
            trim_blocks=True,
            lstrip_blocks=True,
        )

    def xǁEnvScriptGeneratorǁ__init____mutmut_12(self, template_base_dir: Path | None = None) -> None:
        """Initialize the generator with template directory."""
        if template_base_dir is None:
            template_base_dir = Path(__file__).parent.parent / "templates" / "env"

        self.template_base_dir = template_base_dir

        # Create separate environments for sh and pwsh templates
        self.sh_env = Environment(
            loader=None,
            autoescape=select_autoescape(),
            trim_blocks=True,
            lstrip_blocks=True,
        )

        self.ps1_env = Environment(
            loader=FileSystemLoader(template_base_dir / "pwsh"),
            autoescape=select_autoescape(),
            trim_blocks=True,
            lstrip_blocks=True,
        )

    def xǁEnvScriptGeneratorǁ__init____mutmut_13(self, template_base_dir: Path | None = None) -> None:
        """Initialize the generator with template directory."""
        if template_base_dir is None:
            template_base_dir = Path(__file__).parent.parent / "templates" / "env"

        self.template_base_dir = template_base_dir

        # Create separate environments for sh and pwsh templates
        self.sh_env = Environment(
            loader=FileSystemLoader(template_base_dir / "sh"),
            autoescape=None,
            trim_blocks=True,
            lstrip_blocks=True,
        )

        self.ps1_env = Environment(
            loader=FileSystemLoader(template_base_dir / "pwsh"),
            autoescape=select_autoescape(),
            trim_blocks=True,
            lstrip_blocks=True,
        )

    def xǁEnvScriptGeneratorǁ__init____mutmut_14(self, template_base_dir: Path | None = None) -> None:
        """Initialize the generator with template directory."""
        if template_base_dir is None:
            template_base_dir = Path(__file__).parent.parent / "templates" / "env"

        self.template_base_dir = template_base_dir

        # Create separate environments for sh and pwsh templates
        self.sh_env = Environment(
            loader=FileSystemLoader(template_base_dir / "sh"),
            autoescape=select_autoescape(),
            trim_blocks=None,
            lstrip_blocks=True,
        )

        self.ps1_env = Environment(
            loader=FileSystemLoader(template_base_dir / "pwsh"),
            autoescape=select_autoescape(),
            trim_blocks=True,
            lstrip_blocks=True,
        )

    def xǁEnvScriptGeneratorǁ__init____mutmut_15(self, template_base_dir: Path | None = None) -> None:
        """Initialize the generator with template directory."""
        if template_base_dir is None:
            template_base_dir = Path(__file__).parent.parent / "templates" / "env"

        self.template_base_dir = template_base_dir

        # Create separate environments for sh and pwsh templates
        self.sh_env = Environment(
            loader=FileSystemLoader(template_base_dir / "sh"),
            autoescape=select_autoescape(),
            trim_blocks=True,
            lstrip_blocks=None,
        )

        self.ps1_env = Environment(
            loader=FileSystemLoader(template_base_dir / "pwsh"),
            autoescape=select_autoescape(),
            trim_blocks=True,
            lstrip_blocks=True,
        )

    def xǁEnvScriptGeneratorǁ__init____mutmut_16(self, template_base_dir: Path | None = None) -> None:
        """Initialize the generator with template directory."""
        if template_base_dir is None:
            template_base_dir = Path(__file__).parent.parent / "templates" / "env"

        self.template_base_dir = template_base_dir

        # Create separate environments for sh and pwsh templates
        self.sh_env = Environment(
            autoescape=select_autoescape(),
            trim_blocks=True,
            lstrip_blocks=True,
        )

        self.ps1_env = Environment(
            loader=FileSystemLoader(template_base_dir / "pwsh"),
            autoescape=select_autoescape(),
            trim_blocks=True,
            lstrip_blocks=True,
        )

    def xǁEnvScriptGeneratorǁ__init____mutmut_17(self, template_base_dir: Path | None = None) -> None:
        """Initialize the generator with template directory."""
        if template_base_dir is None:
            template_base_dir = Path(__file__).parent.parent / "templates" / "env"

        self.template_base_dir = template_base_dir

        # Create separate environments for sh and pwsh templates
        self.sh_env = Environment(
            loader=FileSystemLoader(template_base_dir / "sh"),
            trim_blocks=True,
            lstrip_blocks=True,
        )

        self.ps1_env = Environment(
            loader=FileSystemLoader(template_base_dir / "pwsh"),
            autoescape=select_autoescape(),
            trim_blocks=True,
            lstrip_blocks=True,
        )

    def xǁEnvScriptGeneratorǁ__init____mutmut_18(self, template_base_dir: Path | None = None) -> None:
        """Initialize the generator with template directory."""
        if template_base_dir is None:
            template_base_dir = Path(__file__).parent.parent / "templates" / "env"

        self.template_base_dir = template_base_dir

        # Create separate environments for sh and pwsh templates
        self.sh_env = Environment(
            loader=FileSystemLoader(template_base_dir / "sh"),
            autoescape=select_autoescape(),
            lstrip_blocks=True,
        )

        self.ps1_env = Environment(
            loader=FileSystemLoader(template_base_dir / "pwsh"),
            autoescape=select_autoescape(),
            trim_blocks=True,
            lstrip_blocks=True,
        )

    def xǁEnvScriptGeneratorǁ__init____mutmut_19(self, template_base_dir: Path | None = None) -> None:
        """Initialize the generator with template directory."""
        if template_base_dir is None:
            template_base_dir = Path(__file__).parent.parent / "templates" / "env"

        self.template_base_dir = template_base_dir

        # Create separate environments for sh and pwsh templates
        self.sh_env = Environment(
            loader=FileSystemLoader(template_base_dir / "sh"),
            autoescape=select_autoescape(),
            trim_blocks=True,
            )

        self.ps1_env = Environment(
            loader=FileSystemLoader(template_base_dir / "pwsh"),
            autoescape=select_autoescape(),
            trim_blocks=True,
            lstrip_blocks=True,
        )

    def xǁEnvScriptGeneratorǁ__init____mutmut_20(self, template_base_dir: Path | None = None) -> None:
        """Initialize the generator with template directory."""
        if template_base_dir is None:
            template_base_dir = Path(__file__).parent.parent / "templates" / "env"

        self.template_base_dir = template_base_dir

        # Create separate environments for sh and pwsh templates
        self.sh_env = Environment(
            loader=FileSystemLoader(None),
            autoescape=select_autoescape(),
            trim_blocks=True,
            lstrip_blocks=True,
        )

        self.ps1_env = Environment(
            loader=FileSystemLoader(template_base_dir / "pwsh"),
            autoescape=select_autoescape(),
            trim_blocks=True,
            lstrip_blocks=True,
        )

    def xǁEnvScriptGeneratorǁ__init____mutmut_21(self, template_base_dir: Path | None = None) -> None:
        """Initialize the generator with template directory."""
        if template_base_dir is None:
            template_base_dir = Path(__file__).parent.parent / "templates" / "env"

        self.template_base_dir = template_base_dir

        # Create separate environments for sh and pwsh templates
        self.sh_env = Environment(
            loader=FileSystemLoader(template_base_dir * "sh"),
            autoescape=select_autoescape(),
            trim_blocks=True,
            lstrip_blocks=True,
        )

        self.ps1_env = Environment(
            loader=FileSystemLoader(template_base_dir / "pwsh"),
            autoescape=select_autoescape(),
            trim_blocks=True,
            lstrip_blocks=True,
        )

    def xǁEnvScriptGeneratorǁ__init____mutmut_22(self, template_base_dir: Path | None = None) -> None:
        """Initialize the generator with template directory."""
        if template_base_dir is None:
            template_base_dir = Path(__file__).parent.parent / "templates" / "env"

        self.template_base_dir = template_base_dir

        # Create separate environments for sh and pwsh templates
        self.sh_env = Environment(
            loader=FileSystemLoader(template_base_dir / "XXshXX"),
            autoescape=select_autoescape(),
            trim_blocks=True,
            lstrip_blocks=True,
        )

        self.ps1_env = Environment(
            loader=FileSystemLoader(template_base_dir / "pwsh"),
            autoescape=select_autoescape(),
            trim_blocks=True,
            lstrip_blocks=True,
        )

    def xǁEnvScriptGeneratorǁ__init____mutmut_23(self, template_base_dir: Path | None = None) -> None:
        """Initialize the generator with template directory."""
        if template_base_dir is None:
            template_base_dir = Path(__file__).parent.parent / "templates" / "env"

        self.template_base_dir = template_base_dir

        # Create separate environments for sh and pwsh templates
        self.sh_env = Environment(
            loader=FileSystemLoader(template_base_dir / "SH"),
            autoescape=select_autoescape(),
            trim_blocks=True,
            lstrip_blocks=True,
        )

        self.ps1_env = Environment(
            loader=FileSystemLoader(template_base_dir / "pwsh"),
            autoescape=select_autoescape(),
            trim_blocks=True,
            lstrip_blocks=True,
        )

    def xǁEnvScriptGeneratorǁ__init____mutmut_24(self, template_base_dir: Path | None = None) -> None:
        """Initialize the generator with template directory."""
        if template_base_dir is None:
            template_base_dir = Path(__file__).parent.parent / "templates" / "env"

        self.template_base_dir = template_base_dir

        # Create separate environments for sh and pwsh templates
        self.sh_env = Environment(
            loader=FileSystemLoader(template_base_dir / "sh"),
            autoescape=select_autoescape(),
            trim_blocks=False,
            lstrip_blocks=True,
        )

        self.ps1_env = Environment(
            loader=FileSystemLoader(template_base_dir / "pwsh"),
            autoescape=select_autoescape(),
            trim_blocks=True,
            lstrip_blocks=True,
        )

    def xǁEnvScriptGeneratorǁ__init____mutmut_25(self, template_base_dir: Path | None = None) -> None:
        """Initialize the generator with template directory."""
        if template_base_dir is None:
            template_base_dir = Path(__file__).parent.parent / "templates" / "env"

        self.template_base_dir = template_base_dir

        # Create separate environments for sh and pwsh templates
        self.sh_env = Environment(
            loader=FileSystemLoader(template_base_dir / "sh"),
            autoescape=select_autoescape(),
            trim_blocks=True,
            lstrip_blocks=False,
        )

        self.ps1_env = Environment(
            loader=FileSystemLoader(template_base_dir / "pwsh"),
            autoescape=select_autoescape(),
            trim_blocks=True,
            lstrip_blocks=True,
        )

    def xǁEnvScriptGeneratorǁ__init____mutmut_26(self, template_base_dir: Path | None = None) -> None:
        """Initialize the generator with template directory."""
        if template_base_dir is None:
            template_base_dir = Path(__file__).parent.parent / "templates" / "env"

        self.template_base_dir = template_base_dir

        # Create separate environments for sh and pwsh templates
        self.sh_env = Environment(
            loader=FileSystemLoader(template_base_dir / "sh"),
            autoescape=select_autoescape(),
            trim_blocks=True,
            lstrip_blocks=True,
        )

        self.ps1_env = None

    def xǁEnvScriptGeneratorǁ__init____mutmut_27(self, template_base_dir: Path | None = None) -> None:
        """Initialize the generator with template directory."""
        if template_base_dir is None:
            template_base_dir = Path(__file__).parent.parent / "templates" / "env"

        self.template_base_dir = template_base_dir

        # Create separate environments for sh and pwsh templates
        self.sh_env = Environment(
            loader=FileSystemLoader(template_base_dir / "sh"),
            autoescape=select_autoescape(),
            trim_blocks=True,
            lstrip_blocks=True,
        )

        self.ps1_env = Environment(
            loader=None,
            autoescape=select_autoescape(),
            trim_blocks=True,
            lstrip_blocks=True,
        )

    def xǁEnvScriptGeneratorǁ__init____mutmut_28(self, template_base_dir: Path | None = None) -> None:
        """Initialize the generator with template directory."""
        if template_base_dir is None:
            template_base_dir = Path(__file__).parent.parent / "templates" / "env"

        self.template_base_dir = template_base_dir

        # Create separate environments for sh and pwsh templates
        self.sh_env = Environment(
            loader=FileSystemLoader(template_base_dir / "sh"),
            autoescape=select_autoescape(),
            trim_blocks=True,
            lstrip_blocks=True,
        )

        self.ps1_env = Environment(
            loader=FileSystemLoader(template_base_dir / "pwsh"),
            autoescape=None,
            trim_blocks=True,
            lstrip_blocks=True,
        )

    def xǁEnvScriptGeneratorǁ__init____mutmut_29(self, template_base_dir: Path | None = None) -> None:
        """Initialize the generator with template directory."""
        if template_base_dir is None:
            template_base_dir = Path(__file__).parent.parent / "templates" / "env"

        self.template_base_dir = template_base_dir

        # Create separate environments for sh and pwsh templates
        self.sh_env = Environment(
            loader=FileSystemLoader(template_base_dir / "sh"),
            autoescape=select_autoescape(),
            trim_blocks=True,
            lstrip_blocks=True,
        )

        self.ps1_env = Environment(
            loader=FileSystemLoader(template_base_dir / "pwsh"),
            autoescape=select_autoescape(),
            trim_blocks=None,
            lstrip_blocks=True,
        )

    def xǁEnvScriptGeneratorǁ__init____mutmut_30(self, template_base_dir: Path | None = None) -> None:
        """Initialize the generator with template directory."""
        if template_base_dir is None:
            template_base_dir = Path(__file__).parent.parent / "templates" / "env"

        self.template_base_dir = template_base_dir

        # Create separate environments for sh and pwsh templates
        self.sh_env = Environment(
            loader=FileSystemLoader(template_base_dir / "sh"),
            autoescape=select_autoescape(),
            trim_blocks=True,
            lstrip_blocks=True,
        )

        self.ps1_env = Environment(
            loader=FileSystemLoader(template_base_dir / "pwsh"),
            autoescape=select_autoescape(),
            trim_blocks=True,
            lstrip_blocks=None,
        )

    def xǁEnvScriptGeneratorǁ__init____mutmut_31(self, template_base_dir: Path | None = None) -> None:
        """Initialize the generator with template directory."""
        if template_base_dir is None:
            template_base_dir = Path(__file__).parent.parent / "templates" / "env"

        self.template_base_dir = template_base_dir

        # Create separate environments for sh and pwsh templates
        self.sh_env = Environment(
            loader=FileSystemLoader(template_base_dir / "sh"),
            autoescape=select_autoescape(),
            trim_blocks=True,
            lstrip_blocks=True,
        )

        self.ps1_env = Environment(
            autoescape=select_autoescape(),
            trim_blocks=True,
            lstrip_blocks=True,
        )

    def xǁEnvScriptGeneratorǁ__init____mutmut_32(self, template_base_dir: Path | None = None) -> None:
        """Initialize the generator with template directory."""
        if template_base_dir is None:
            template_base_dir = Path(__file__).parent.parent / "templates" / "env"

        self.template_base_dir = template_base_dir

        # Create separate environments for sh and pwsh templates
        self.sh_env = Environment(
            loader=FileSystemLoader(template_base_dir / "sh"),
            autoescape=select_autoescape(),
            trim_blocks=True,
            lstrip_blocks=True,
        )

        self.ps1_env = Environment(
            loader=FileSystemLoader(template_base_dir / "pwsh"),
            trim_blocks=True,
            lstrip_blocks=True,
        )

    def xǁEnvScriptGeneratorǁ__init____mutmut_33(self, template_base_dir: Path | None = None) -> None:
        """Initialize the generator with template directory."""
        if template_base_dir is None:
            template_base_dir = Path(__file__).parent.parent / "templates" / "env"

        self.template_base_dir = template_base_dir

        # Create separate environments for sh and pwsh templates
        self.sh_env = Environment(
            loader=FileSystemLoader(template_base_dir / "sh"),
            autoescape=select_autoescape(),
            trim_blocks=True,
            lstrip_blocks=True,
        )

        self.ps1_env = Environment(
            loader=FileSystemLoader(template_base_dir / "pwsh"),
            autoescape=select_autoescape(),
            lstrip_blocks=True,
        )

    def xǁEnvScriptGeneratorǁ__init____mutmut_34(self, template_base_dir: Path | None = None) -> None:
        """Initialize the generator with template directory."""
        if template_base_dir is None:
            template_base_dir = Path(__file__).parent.parent / "templates" / "env"

        self.template_base_dir = template_base_dir

        # Create separate environments for sh and pwsh templates
        self.sh_env = Environment(
            loader=FileSystemLoader(template_base_dir / "sh"),
            autoescape=select_autoescape(),
            trim_blocks=True,
            lstrip_blocks=True,
        )

        self.ps1_env = Environment(
            loader=FileSystemLoader(template_base_dir / "pwsh"),
            autoescape=select_autoescape(),
            trim_blocks=True,
            )

    def xǁEnvScriptGeneratorǁ__init____mutmut_35(self, template_base_dir: Path | None = None) -> None:
        """Initialize the generator with template directory."""
        if template_base_dir is None:
            template_base_dir = Path(__file__).parent.parent / "templates" / "env"

        self.template_base_dir = template_base_dir

        # Create separate environments for sh and pwsh templates
        self.sh_env = Environment(
            loader=FileSystemLoader(template_base_dir / "sh"),
            autoescape=select_autoescape(),
            trim_blocks=True,
            lstrip_blocks=True,
        )

        self.ps1_env = Environment(
            loader=FileSystemLoader(None),
            autoescape=select_autoescape(),
            trim_blocks=True,
            lstrip_blocks=True,
        )

    def xǁEnvScriptGeneratorǁ__init____mutmut_36(self, template_base_dir: Path | None = None) -> None:
        """Initialize the generator with template directory."""
        if template_base_dir is None:
            template_base_dir = Path(__file__).parent.parent / "templates" / "env"

        self.template_base_dir = template_base_dir

        # Create separate environments for sh and pwsh templates
        self.sh_env = Environment(
            loader=FileSystemLoader(template_base_dir / "sh"),
            autoescape=select_autoescape(),
            trim_blocks=True,
            lstrip_blocks=True,
        )

        self.ps1_env = Environment(
            loader=FileSystemLoader(template_base_dir * "pwsh"),
            autoescape=select_autoescape(),
            trim_blocks=True,
            lstrip_blocks=True,
        )

    def xǁEnvScriptGeneratorǁ__init____mutmut_37(self, template_base_dir: Path | None = None) -> None:
        """Initialize the generator with template directory."""
        if template_base_dir is None:
            template_base_dir = Path(__file__).parent.parent / "templates" / "env"

        self.template_base_dir = template_base_dir

        # Create separate environments for sh and pwsh templates
        self.sh_env = Environment(
            loader=FileSystemLoader(template_base_dir / "sh"),
            autoescape=select_autoescape(),
            trim_blocks=True,
            lstrip_blocks=True,
        )

        self.ps1_env = Environment(
            loader=FileSystemLoader(template_base_dir / "XXpwshXX"),
            autoescape=select_autoescape(),
            trim_blocks=True,
            lstrip_blocks=True,
        )

    def xǁEnvScriptGeneratorǁ__init____mutmut_38(self, template_base_dir: Path | None = None) -> None:
        """Initialize the generator with template directory."""
        if template_base_dir is None:
            template_base_dir = Path(__file__).parent.parent / "templates" / "env"

        self.template_base_dir = template_base_dir

        # Create separate environments for sh and pwsh templates
        self.sh_env = Environment(
            loader=FileSystemLoader(template_base_dir / "sh"),
            autoescape=select_autoescape(),
            trim_blocks=True,
            lstrip_blocks=True,
        )

        self.ps1_env = Environment(
            loader=FileSystemLoader(template_base_dir / "PWSH"),
            autoescape=select_autoescape(),
            trim_blocks=True,
            lstrip_blocks=True,
        )

    def xǁEnvScriptGeneratorǁ__init____mutmut_39(self, template_base_dir: Path | None = None) -> None:
        """Initialize the generator with template directory."""
        if template_base_dir is None:
            template_base_dir = Path(__file__).parent.parent / "templates" / "env"

        self.template_base_dir = template_base_dir

        # Create separate environments for sh and pwsh templates
        self.sh_env = Environment(
            loader=FileSystemLoader(template_base_dir / "sh"),
            autoescape=select_autoescape(),
            trim_blocks=True,
            lstrip_blocks=True,
        )

        self.ps1_env = Environment(
            loader=FileSystemLoader(template_base_dir / "pwsh"),
            autoescape=select_autoescape(),
            trim_blocks=False,
            lstrip_blocks=True,
        )

    def xǁEnvScriptGeneratorǁ__init____mutmut_40(self, template_base_dir: Path | None = None) -> None:
        """Initialize the generator with template directory."""
        if template_base_dir is None:
            template_base_dir = Path(__file__).parent.parent / "templates" / "env"

        self.template_base_dir = template_base_dir

        # Create separate environments for sh and pwsh templates
        self.sh_env = Environment(
            loader=FileSystemLoader(template_base_dir / "sh"),
            autoescape=select_autoescape(),
            trim_blocks=True,
            lstrip_blocks=True,
        )

        self.ps1_env = Environment(
            loader=FileSystemLoader(template_base_dir / "pwsh"),
            autoescape=select_autoescape(),
            trim_blocks=True,
            lstrip_blocks=False,
        )
    
    xǁEnvScriptGeneratorǁ__init____mutmut_mutants : ClassVar[MutantDict] = {
    'xǁEnvScriptGeneratorǁ__init____mutmut_1': xǁEnvScriptGeneratorǁ__init____mutmut_1, 
        'xǁEnvScriptGeneratorǁ__init____mutmut_2': xǁEnvScriptGeneratorǁ__init____mutmut_2, 
        'xǁEnvScriptGeneratorǁ__init____mutmut_3': xǁEnvScriptGeneratorǁ__init____mutmut_3, 
        'xǁEnvScriptGeneratorǁ__init____mutmut_4': xǁEnvScriptGeneratorǁ__init____mutmut_4, 
        'xǁEnvScriptGeneratorǁ__init____mutmut_5': xǁEnvScriptGeneratorǁ__init____mutmut_5, 
        'xǁEnvScriptGeneratorǁ__init____mutmut_6': xǁEnvScriptGeneratorǁ__init____mutmut_6, 
        'xǁEnvScriptGeneratorǁ__init____mutmut_7': xǁEnvScriptGeneratorǁ__init____mutmut_7, 
        'xǁEnvScriptGeneratorǁ__init____mutmut_8': xǁEnvScriptGeneratorǁ__init____mutmut_8, 
        'xǁEnvScriptGeneratorǁ__init____mutmut_9': xǁEnvScriptGeneratorǁ__init____mutmut_9, 
        'xǁEnvScriptGeneratorǁ__init____mutmut_10': xǁEnvScriptGeneratorǁ__init____mutmut_10, 
        'xǁEnvScriptGeneratorǁ__init____mutmut_11': xǁEnvScriptGeneratorǁ__init____mutmut_11, 
        'xǁEnvScriptGeneratorǁ__init____mutmut_12': xǁEnvScriptGeneratorǁ__init____mutmut_12, 
        'xǁEnvScriptGeneratorǁ__init____mutmut_13': xǁEnvScriptGeneratorǁ__init____mutmut_13, 
        'xǁEnvScriptGeneratorǁ__init____mutmut_14': xǁEnvScriptGeneratorǁ__init____mutmut_14, 
        'xǁEnvScriptGeneratorǁ__init____mutmut_15': xǁEnvScriptGeneratorǁ__init____mutmut_15, 
        'xǁEnvScriptGeneratorǁ__init____mutmut_16': xǁEnvScriptGeneratorǁ__init____mutmut_16, 
        'xǁEnvScriptGeneratorǁ__init____mutmut_17': xǁEnvScriptGeneratorǁ__init____mutmut_17, 
        'xǁEnvScriptGeneratorǁ__init____mutmut_18': xǁEnvScriptGeneratorǁ__init____mutmut_18, 
        'xǁEnvScriptGeneratorǁ__init____mutmut_19': xǁEnvScriptGeneratorǁ__init____mutmut_19, 
        'xǁEnvScriptGeneratorǁ__init____mutmut_20': xǁEnvScriptGeneratorǁ__init____mutmut_20, 
        'xǁEnvScriptGeneratorǁ__init____mutmut_21': xǁEnvScriptGeneratorǁ__init____mutmut_21, 
        'xǁEnvScriptGeneratorǁ__init____mutmut_22': xǁEnvScriptGeneratorǁ__init____mutmut_22, 
        'xǁEnvScriptGeneratorǁ__init____mutmut_23': xǁEnvScriptGeneratorǁ__init____mutmut_23, 
        'xǁEnvScriptGeneratorǁ__init____mutmut_24': xǁEnvScriptGeneratorǁ__init____mutmut_24, 
        'xǁEnvScriptGeneratorǁ__init____mutmut_25': xǁEnvScriptGeneratorǁ__init____mutmut_25, 
        'xǁEnvScriptGeneratorǁ__init____mutmut_26': xǁEnvScriptGeneratorǁ__init____mutmut_26, 
        'xǁEnvScriptGeneratorǁ__init____mutmut_27': xǁEnvScriptGeneratorǁ__init____mutmut_27, 
        'xǁEnvScriptGeneratorǁ__init____mutmut_28': xǁEnvScriptGeneratorǁ__init____mutmut_28, 
        'xǁEnvScriptGeneratorǁ__init____mutmut_29': xǁEnvScriptGeneratorǁ__init____mutmut_29, 
        'xǁEnvScriptGeneratorǁ__init____mutmut_30': xǁEnvScriptGeneratorǁ__init____mutmut_30, 
        'xǁEnvScriptGeneratorǁ__init____mutmut_31': xǁEnvScriptGeneratorǁ__init____mutmut_31, 
        'xǁEnvScriptGeneratorǁ__init____mutmut_32': xǁEnvScriptGeneratorǁ__init____mutmut_32, 
        'xǁEnvScriptGeneratorǁ__init____mutmut_33': xǁEnvScriptGeneratorǁ__init____mutmut_33, 
        'xǁEnvScriptGeneratorǁ__init____mutmut_34': xǁEnvScriptGeneratorǁ__init____mutmut_34, 
        'xǁEnvScriptGeneratorǁ__init____mutmut_35': xǁEnvScriptGeneratorǁ__init____mutmut_35, 
        'xǁEnvScriptGeneratorǁ__init____mutmut_36': xǁEnvScriptGeneratorǁ__init____mutmut_36, 
        'xǁEnvScriptGeneratorǁ__init____mutmut_37': xǁEnvScriptGeneratorǁ__init____mutmut_37, 
        'xǁEnvScriptGeneratorǁ__init____mutmut_38': xǁEnvScriptGeneratorǁ__init____mutmut_38, 
        'xǁEnvScriptGeneratorǁ__init____mutmut_39': xǁEnvScriptGeneratorǁ__init____mutmut_39, 
        'xǁEnvScriptGeneratorǁ__init____mutmut_40': xǁEnvScriptGeneratorǁ__init____mutmut_40
    }
    
    def __init__(self, *args, **kwargs):
        result = _mutmut_trampoline(object.__getattribute__(self, "xǁEnvScriptGeneratorǁ__init____mutmut_orig"), object.__getattribute__(self, "xǁEnvScriptGeneratorǁ__init____mutmut_mutants"), args, kwargs, self)
        return result 
    
    __init__.__signature__ = _mutmut_signature(xǁEnvScriptGeneratorǁ__init____mutmut_orig)
    xǁEnvScriptGeneratorǁ__init____mutmut_orig.__name__ = 'xǁEnvScriptGeneratorǁ__init__'

    def xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_orig(
        self,
        project_name: str,
        output_path: Path,
        script_type: str = "sh",
        **kwargs: Any,
    ) -> None:
        """Generate an environment setup script.

        Args:
            project_name: Name of the project
            output_path: Path to write the script
            script_type: Either "sh" for bash or "ps1" for PowerShell
            **kwargs: Additional template variables
        """
        # Default configuration
        config = {
            "project_name": project_name,
            "env_profile_var": f"{project_name.upper().replace('-', '_')}_PROFILE",
            "venv_prefix": project_name.lower(),
            "use_spinner": script_type == "sh",  # PowerShell doesn't need spinner
            "strict_project_check": False,
            "install_siblings": True,
            "sibling_patterns": [],  # No default peers - specified in config
            "special_siblings": [],  # No default peers - specified in config
            "create_log_dir": True,
            "deduplicate_path": True,
            "include_tool_verification": True,  # Enable for wrknv
            "cleanup_logs": True,
            "tools_to_verify": [
                {
                    "name": "Python",
                    "command": "python",
                    "check_type": "command",
                    "var_name": "PYTHON",
                    "version_cmd": "python --version 2>&1",
                },
                {
                    "name": "UV",
                    "command": "uv",
                    "check_type": "command",
                    "var_name": "UV",
                    "version_cmd": "uv --version 2>&1",
                },
                {
                    "name": "wrknv",
                    "command": "wrknv",
                    "check_type": "command",
                    "var_name": "WRKENV",
                    "version_cmd": "wrknv --version 2>&1 || echo 'No version info'",
                },
                {
                    "name": "ibmtf",
                    "command": "ibmtf",
                    "check_type": "command",
                    "var_name": "IBMTF",
                    "version_cmd": "ibmtf version 2>&1 | head -1 || echo 'Not installed'",
                },
                {
                    "name": "tofu",
                    "command": "tofu",
                    "check_type": "command",
                    "var_name": "TOFU",
                    "version_cmd": "tofu version 2>&1 | head -1 || echo 'Not installed'",
                },
            ],
            "useful_commands": [
                {
                    "command": f"{project_name.lower()} --help",
                    "description": f"{project_name} CLI",
                },
                {"command": "wrknv status", "description": "Check tool versions"},
                {
                    "command": "wrknv container status",
                    "description": "Container status",
                },
                {"command": "pytest", "description": "Run tests"},
                {"command": "deactivate", "description": "Exit environment"},
            ],
        }

        # Update with user-provided kwargs
        config.update(kwargs)

        # Select appropriate template and environment
        if script_type == "sh":
            template = self.sh_env.get_template("base.sh.j2")
        elif script_type == "ps1":
            template = self.ps1_env.get_template("base.ps1.j2")
        else:
            raise ValueError(f"Unknown script type: {script_type}")

        # Render and write
        content = template.render(**config)
        output_path.write_text(content)

        # Make executable if it's a shell script
        if script_type == "sh":
            output_path.chmod(output_path.stat().st_mode | 0o111)

    def xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_1(
        self,
        project_name: str,
        output_path: Path,
        script_type: str = "XXshXX",
        **kwargs: Any,
    ) -> None:
        """Generate an environment setup script.

        Args:
            project_name: Name of the project
            output_path: Path to write the script
            script_type: Either "sh" for bash or "ps1" for PowerShell
            **kwargs: Additional template variables
        """
        # Default configuration
        config = {
            "project_name": project_name,
            "env_profile_var": f"{project_name.upper().replace('-', '_')}_PROFILE",
            "venv_prefix": project_name.lower(),
            "use_spinner": script_type == "sh",  # PowerShell doesn't need spinner
            "strict_project_check": False,
            "install_siblings": True,
            "sibling_patterns": [],  # No default peers - specified in config
            "special_siblings": [],  # No default peers - specified in config
            "create_log_dir": True,
            "deduplicate_path": True,
            "include_tool_verification": True,  # Enable for wrknv
            "cleanup_logs": True,
            "tools_to_verify": [
                {
                    "name": "Python",
                    "command": "python",
                    "check_type": "command",
                    "var_name": "PYTHON",
                    "version_cmd": "python --version 2>&1",
                },
                {
                    "name": "UV",
                    "command": "uv",
                    "check_type": "command",
                    "var_name": "UV",
                    "version_cmd": "uv --version 2>&1",
                },
                {
                    "name": "wrknv",
                    "command": "wrknv",
                    "check_type": "command",
                    "var_name": "WRKENV",
                    "version_cmd": "wrknv --version 2>&1 || echo 'No version info'",
                },
                {
                    "name": "ibmtf",
                    "command": "ibmtf",
                    "check_type": "command",
                    "var_name": "IBMTF",
                    "version_cmd": "ibmtf version 2>&1 | head -1 || echo 'Not installed'",
                },
                {
                    "name": "tofu",
                    "command": "tofu",
                    "check_type": "command",
                    "var_name": "TOFU",
                    "version_cmd": "tofu version 2>&1 | head -1 || echo 'Not installed'",
                },
            ],
            "useful_commands": [
                {
                    "command": f"{project_name.lower()} --help",
                    "description": f"{project_name} CLI",
                },
                {"command": "wrknv status", "description": "Check tool versions"},
                {
                    "command": "wrknv container status",
                    "description": "Container status",
                },
                {"command": "pytest", "description": "Run tests"},
                {"command": "deactivate", "description": "Exit environment"},
            ],
        }

        # Update with user-provided kwargs
        config.update(kwargs)

        # Select appropriate template and environment
        if script_type == "sh":
            template = self.sh_env.get_template("base.sh.j2")
        elif script_type == "ps1":
            template = self.ps1_env.get_template("base.ps1.j2")
        else:
            raise ValueError(f"Unknown script type: {script_type}")

        # Render and write
        content = template.render(**config)
        output_path.write_text(content)

        # Make executable if it's a shell script
        if script_type == "sh":
            output_path.chmod(output_path.stat().st_mode | 0o111)

    def xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_2(
        self,
        project_name: str,
        output_path: Path,
        script_type: str = "SH",
        **kwargs: Any,
    ) -> None:
        """Generate an environment setup script.

        Args:
            project_name: Name of the project
            output_path: Path to write the script
            script_type: Either "sh" for bash or "ps1" for PowerShell
            **kwargs: Additional template variables
        """
        # Default configuration
        config = {
            "project_name": project_name,
            "env_profile_var": f"{project_name.upper().replace('-', '_')}_PROFILE",
            "venv_prefix": project_name.lower(),
            "use_spinner": script_type == "sh",  # PowerShell doesn't need spinner
            "strict_project_check": False,
            "install_siblings": True,
            "sibling_patterns": [],  # No default peers - specified in config
            "special_siblings": [],  # No default peers - specified in config
            "create_log_dir": True,
            "deduplicate_path": True,
            "include_tool_verification": True,  # Enable for wrknv
            "cleanup_logs": True,
            "tools_to_verify": [
                {
                    "name": "Python",
                    "command": "python",
                    "check_type": "command",
                    "var_name": "PYTHON",
                    "version_cmd": "python --version 2>&1",
                },
                {
                    "name": "UV",
                    "command": "uv",
                    "check_type": "command",
                    "var_name": "UV",
                    "version_cmd": "uv --version 2>&1",
                },
                {
                    "name": "wrknv",
                    "command": "wrknv",
                    "check_type": "command",
                    "var_name": "WRKENV",
                    "version_cmd": "wrknv --version 2>&1 || echo 'No version info'",
                },
                {
                    "name": "ibmtf",
                    "command": "ibmtf",
                    "check_type": "command",
                    "var_name": "IBMTF",
                    "version_cmd": "ibmtf version 2>&1 | head -1 || echo 'Not installed'",
                },
                {
                    "name": "tofu",
                    "command": "tofu",
                    "check_type": "command",
                    "var_name": "TOFU",
                    "version_cmd": "tofu version 2>&1 | head -1 || echo 'Not installed'",
                },
            ],
            "useful_commands": [
                {
                    "command": f"{project_name.lower()} --help",
                    "description": f"{project_name} CLI",
                },
                {"command": "wrknv status", "description": "Check tool versions"},
                {
                    "command": "wrknv container status",
                    "description": "Container status",
                },
                {"command": "pytest", "description": "Run tests"},
                {"command": "deactivate", "description": "Exit environment"},
            ],
        }

        # Update with user-provided kwargs
        config.update(kwargs)

        # Select appropriate template and environment
        if script_type == "sh":
            template = self.sh_env.get_template("base.sh.j2")
        elif script_type == "ps1":
            template = self.ps1_env.get_template("base.ps1.j2")
        else:
            raise ValueError(f"Unknown script type: {script_type}")

        # Render and write
        content = template.render(**config)
        output_path.write_text(content)

        # Make executable if it's a shell script
        if script_type == "sh":
            output_path.chmod(output_path.stat().st_mode | 0o111)

    def xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_3(
        self,
        project_name: str,
        output_path: Path,
        script_type: str = "sh",
        **kwargs: Any,
    ) -> None:
        """Generate an environment setup script.

        Args:
            project_name: Name of the project
            output_path: Path to write the script
            script_type: Either "sh" for bash or "ps1" for PowerShell
            **kwargs: Additional template variables
        """
        # Default configuration
        config = None

        # Update with user-provided kwargs
        config.update(kwargs)

        # Select appropriate template and environment
        if script_type == "sh":
            template = self.sh_env.get_template("base.sh.j2")
        elif script_type == "ps1":
            template = self.ps1_env.get_template("base.ps1.j2")
        else:
            raise ValueError(f"Unknown script type: {script_type}")

        # Render and write
        content = template.render(**config)
        output_path.write_text(content)

        # Make executable if it's a shell script
        if script_type == "sh":
            output_path.chmod(output_path.stat().st_mode | 0o111)

    def xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_4(
        self,
        project_name: str,
        output_path: Path,
        script_type: str = "sh",
        **kwargs: Any,
    ) -> None:
        """Generate an environment setup script.

        Args:
            project_name: Name of the project
            output_path: Path to write the script
            script_type: Either "sh" for bash or "ps1" for PowerShell
            **kwargs: Additional template variables
        """
        # Default configuration
        config = {
            "XXproject_nameXX": project_name,
            "env_profile_var": f"{project_name.upper().replace('-', '_')}_PROFILE",
            "venv_prefix": project_name.lower(),
            "use_spinner": script_type == "sh",  # PowerShell doesn't need spinner
            "strict_project_check": False,
            "install_siblings": True,
            "sibling_patterns": [],  # No default peers - specified in config
            "special_siblings": [],  # No default peers - specified in config
            "create_log_dir": True,
            "deduplicate_path": True,
            "include_tool_verification": True,  # Enable for wrknv
            "cleanup_logs": True,
            "tools_to_verify": [
                {
                    "name": "Python",
                    "command": "python",
                    "check_type": "command",
                    "var_name": "PYTHON",
                    "version_cmd": "python --version 2>&1",
                },
                {
                    "name": "UV",
                    "command": "uv",
                    "check_type": "command",
                    "var_name": "UV",
                    "version_cmd": "uv --version 2>&1",
                },
                {
                    "name": "wrknv",
                    "command": "wrknv",
                    "check_type": "command",
                    "var_name": "WRKENV",
                    "version_cmd": "wrknv --version 2>&1 || echo 'No version info'",
                },
                {
                    "name": "ibmtf",
                    "command": "ibmtf",
                    "check_type": "command",
                    "var_name": "IBMTF",
                    "version_cmd": "ibmtf version 2>&1 | head -1 || echo 'Not installed'",
                },
                {
                    "name": "tofu",
                    "command": "tofu",
                    "check_type": "command",
                    "var_name": "TOFU",
                    "version_cmd": "tofu version 2>&1 | head -1 || echo 'Not installed'",
                },
            ],
            "useful_commands": [
                {
                    "command": f"{project_name.lower()} --help",
                    "description": f"{project_name} CLI",
                },
                {"command": "wrknv status", "description": "Check tool versions"},
                {
                    "command": "wrknv container status",
                    "description": "Container status",
                },
                {"command": "pytest", "description": "Run tests"},
                {"command": "deactivate", "description": "Exit environment"},
            ],
        }

        # Update with user-provided kwargs
        config.update(kwargs)

        # Select appropriate template and environment
        if script_type == "sh":
            template = self.sh_env.get_template("base.sh.j2")
        elif script_type == "ps1":
            template = self.ps1_env.get_template("base.ps1.j2")
        else:
            raise ValueError(f"Unknown script type: {script_type}")

        # Render and write
        content = template.render(**config)
        output_path.write_text(content)

        # Make executable if it's a shell script
        if script_type == "sh":
            output_path.chmod(output_path.stat().st_mode | 0o111)

    def xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_5(
        self,
        project_name: str,
        output_path: Path,
        script_type: str = "sh",
        **kwargs: Any,
    ) -> None:
        """Generate an environment setup script.

        Args:
            project_name: Name of the project
            output_path: Path to write the script
            script_type: Either "sh" for bash or "ps1" for PowerShell
            **kwargs: Additional template variables
        """
        # Default configuration
        config = {
            "PROJECT_NAME": project_name,
            "env_profile_var": f"{project_name.upper().replace('-', '_')}_PROFILE",
            "venv_prefix": project_name.lower(),
            "use_spinner": script_type == "sh",  # PowerShell doesn't need spinner
            "strict_project_check": False,
            "install_siblings": True,
            "sibling_patterns": [],  # No default peers - specified in config
            "special_siblings": [],  # No default peers - specified in config
            "create_log_dir": True,
            "deduplicate_path": True,
            "include_tool_verification": True,  # Enable for wrknv
            "cleanup_logs": True,
            "tools_to_verify": [
                {
                    "name": "Python",
                    "command": "python",
                    "check_type": "command",
                    "var_name": "PYTHON",
                    "version_cmd": "python --version 2>&1",
                },
                {
                    "name": "UV",
                    "command": "uv",
                    "check_type": "command",
                    "var_name": "UV",
                    "version_cmd": "uv --version 2>&1",
                },
                {
                    "name": "wrknv",
                    "command": "wrknv",
                    "check_type": "command",
                    "var_name": "WRKENV",
                    "version_cmd": "wrknv --version 2>&1 || echo 'No version info'",
                },
                {
                    "name": "ibmtf",
                    "command": "ibmtf",
                    "check_type": "command",
                    "var_name": "IBMTF",
                    "version_cmd": "ibmtf version 2>&1 | head -1 || echo 'Not installed'",
                },
                {
                    "name": "tofu",
                    "command": "tofu",
                    "check_type": "command",
                    "var_name": "TOFU",
                    "version_cmd": "tofu version 2>&1 | head -1 || echo 'Not installed'",
                },
            ],
            "useful_commands": [
                {
                    "command": f"{project_name.lower()} --help",
                    "description": f"{project_name} CLI",
                },
                {"command": "wrknv status", "description": "Check tool versions"},
                {
                    "command": "wrknv container status",
                    "description": "Container status",
                },
                {"command": "pytest", "description": "Run tests"},
                {"command": "deactivate", "description": "Exit environment"},
            ],
        }

        # Update with user-provided kwargs
        config.update(kwargs)

        # Select appropriate template and environment
        if script_type == "sh":
            template = self.sh_env.get_template("base.sh.j2")
        elif script_type == "ps1":
            template = self.ps1_env.get_template("base.ps1.j2")
        else:
            raise ValueError(f"Unknown script type: {script_type}")

        # Render and write
        content = template.render(**config)
        output_path.write_text(content)

        # Make executable if it's a shell script
        if script_type == "sh":
            output_path.chmod(output_path.stat().st_mode | 0o111)

    def xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_6(
        self,
        project_name: str,
        output_path: Path,
        script_type: str = "sh",
        **kwargs: Any,
    ) -> None:
        """Generate an environment setup script.

        Args:
            project_name: Name of the project
            output_path: Path to write the script
            script_type: Either "sh" for bash or "ps1" for PowerShell
            **kwargs: Additional template variables
        """
        # Default configuration
        config = {
            "project_name": project_name,
            "XXenv_profile_varXX": f"{project_name.upper().replace('-', '_')}_PROFILE",
            "venv_prefix": project_name.lower(),
            "use_spinner": script_type == "sh",  # PowerShell doesn't need spinner
            "strict_project_check": False,
            "install_siblings": True,
            "sibling_patterns": [],  # No default peers - specified in config
            "special_siblings": [],  # No default peers - specified in config
            "create_log_dir": True,
            "deduplicate_path": True,
            "include_tool_verification": True,  # Enable for wrknv
            "cleanup_logs": True,
            "tools_to_verify": [
                {
                    "name": "Python",
                    "command": "python",
                    "check_type": "command",
                    "var_name": "PYTHON",
                    "version_cmd": "python --version 2>&1",
                },
                {
                    "name": "UV",
                    "command": "uv",
                    "check_type": "command",
                    "var_name": "UV",
                    "version_cmd": "uv --version 2>&1",
                },
                {
                    "name": "wrknv",
                    "command": "wrknv",
                    "check_type": "command",
                    "var_name": "WRKENV",
                    "version_cmd": "wrknv --version 2>&1 || echo 'No version info'",
                },
                {
                    "name": "ibmtf",
                    "command": "ibmtf",
                    "check_type": "command",
                    "var_name": "IBMTF",
                    "version_cmd": "ibmtf version 2>&1 | head -1 || echo 'Not installed'",
                },
                {
                    "name": "tofu",
                    "command": "tofu",
                    "check_type": "command",
                    "var_name": "TOFU",
                    "version_cmd": "tofu version 2>&1 | head -1 || echo 'Not installed'",
                },
            ],
            "useful_commands": [
                {
                    "command": f"{project_name.lower()} --help",
                    "description": f"{project_name} CLI",
                },
                {"command": "wrknv status", "description": "Check tool versions"},
                {
                    "command": "wrknv container status",
                    "description": "Container status",
                },
                {"command": "pytest", "description": "Run tests"},
                {"command": "deactivate", "description": "Exit environment"},
            ],
        }

        # Update with user-provided kwargs
        config.update(kwargs)

        # Select appropriate template and environment
        if script_type == "sh":
            template = self.sh_env.get_template("base.sh.j2")
        elif script_type == "ps1":
            template = self.ps1_env.get_template("base.ps1.j2")
        else:
            raise ValueError(f"Unknown script type: {script_type}")

        # Render and write
        content = template.render(**config)
        output_path.write_text(content)

        # Make executable if it's a shell script
        if script_type == "sh":
            output_path.chmod(output_path.stat().st_mode | 0o111)

    def xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_7(
        self,
        project_name: str,
        output_path: Path,
        script_type: str = "sh",
        **kwargs: Any,
    ) -> None:
        """Generate an environment setup script.

        Args:
            project_name: Name of the project
            output_path: Path to write the script
            script_type: Either "sh" for bash or "ps1" for PowerShell
            **kwargs: Additional template variables
        """
        # Default configuration
        config = {
            "project_name": project_name,
            "ENV_PROFILE_VAR": f"{project_name.upper().replace('-', '_')}_PROFILE",
            "venv_prefix": project_name.lower(),
            "use_spinner": script_type == "sh",  # PowerShell doesn't need spinner
            "strict_project_check": False,
            "install_siblings": True,
            "sibling_patterns": [],  # No default peers - specified in config
            "special_siblings": [],  # No default peers - specified in config
            "create_log_dir": True,
            "deduplicate_path": True,
            "include_tool_verification": True,  # Enable for wrknv
            "cleanup_logs": True,
            "tools_to_verify": [
                {
                    "name": "Python",
                    "command": "python",
                    "check_type": "command",
                    "var_name": "PYTHON",
                    "version_cmd": "python --version 2>&1",
                },
                {
                    "name": "UV",
                    "command": "uv",
                    "check_type": "command",
                    "var_name": "UV",
                    "version_cmd": "uv --version 2>&1",
                },
                {
                    "name": "wrknv",
                    "command": "wrknv",
                    "check_type": "command",
                    "var_name": "WRKENV",
                    "version_cmd": "wrknv --version 2>&1 || echo 'No version info'",
                },
                {
                    "name": "ibmtf",
                    "command": "ibmtf",
                    "check_type": "command",
                    "var_name": "IBMTF",
                    "version_cmd": "ibmtf version 2>&1 | head -1 || echo 'Not installed'",
                },
                {
                    "name": "tofu",
                    "command": "tofu",
                    "check_type": "command",
                    "var_name": "TOFU",
                    "version_cmd": "tofu version 2>&1 | head -1 || echo 'Not installed'",
                },
            ],
            "useful_commands": [
                {
                    "command": f"{project_name.lower()} --help",
                    "description": f"{project_name} CLI",
                },
                {"command": "wrknv status", "description": "Check tool versions"},
                {
                    "command": "wrknv container status",
                    "description": "Container status",
                },
                {"command": "pytest", "description": "Run tests"},
                {"command": "deactivate", "description": "Exit environment"},
            ],
        }

        # Update with user-provided kwargs
        config.update(kwargs)

        # Select appropriate template and environment
        if script_type == "sh":
            template = self.sh_env.get_template("base.sh.j2")
        elif script_type == "ps1":
            template = self.ps1_env.get_template("base.ps1.j2")
        else:
            raise ValueError(f"Unknown script type: {script_type}")

        # Render and write
        content = template.render(**config)
        output_path.write_text(content)

        # Make executable if it's a shell script
        if script_type == "sh":
            output_path.chmod(output_path.stat().st_mode | 0o111)

    def xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_8(
        self,
        project_name: str,
        output_path: Path,
        script_type: str = "sh",
        **kwargs: Any,
    ) -> None:
        """Generate an environment setup script.

        Args:
            project_name: Name of the project
            output_path: Path to write the script
            script_type: Either "sh" for bash or "ps1" for PowerShell
            **kwargs: Additional template variables
        """
        # Default configuration
        config = {
            "project_name": project_name,
            "env_profile_var": f"{project_name.upper().replace(None, '_')}_PROFILE",
            "venv_prefix": project_name.lower(),
            "use_spinner": script_type == "sh",  # PowerShell doesn't need spinner
            "strict_project_check": False,
            "install_siblings": True,
            "sibling_patterns": [],  # No default peers - specified in config
            "special_siblings": [],  # No default peers - specified in config
            "create_log_dir": True,
            "deduplicate_path": True,
            "include_tool_verification": True,  # Enable for wrknv
            "cleanup_logs": True,
            "tools_to_verify": [
                {
                    "name": "Python",
                    "command": "python",
                    "check_type": "command",
                    "var_name": "PYTHON",
                    "version_cmd": "python --version 2>&1",
                },
                {
                    "name": "UV",
                    "command": "uv",
                    "check_type": "command",
                    "var_name": "UV",
                    "version_cmd": "uv --version 2>&1",
                },
                {
                    "name": "wrknv",
                    "command": "wrknv",
                    "check_type": "command",
                    "var_name": "WRKENV",
                    "version_cmd": "wrknv --version 2>&1 || echo 'No version info'",
                },
                {
                    "name": "ibmtf",
                    "command": "ibmtf",
                    "check_type": "command",
                    "var_name": "IBMTF",
                    "version_cmd": "ibmtf version 2>&1 | head -1 || echo 'Not installed'",
                },
                {
                    "name": "tofu",
                    "command": "tofu",
                    "check_type": "command",
                    "var_name": "TOFU",
                    "version_cmd": "tofu version 2>&1 | head -1 || echo 'Not installed'",
                },
            ],
            "useful_commands": [
                {
                    "command": f"{project_name.lower()} --help",
                    "description": f"{project_name} CLI",
                },
                {"command": "wrknv status", "description": "Check tool versions"},
                {
                    "command": "wrknv container status",
                    "description": "Container status",
                },
                {"command": "pytest", "description": "Run tests"},
                {"command": "deactivate", "description": "Exit environment"},
            ],
        }

        # Update with user-provided kwargs
        config.update(kwargs)

        # Select appropriate template and environment
        if script_type == "sh":
            template = self.sh_env.get_template("base.sh.j2")
        elif script_type == "ps1":
            template = self.ps1_env.get_template("base.ps1.j2")
        else:
            raise ValueError(f"Unknown script type: {script_type}")

        # Render and write
        content = template.render(**config)
        output_path.write_text(content)

        # Make executable if it's a shell script
        if script_type == "sh":
            output_path.chmod(output_path.stat().st_mode | 0o111)

    def xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_9(
        self,
        project_name: str,
        output_path: Path,
        script_type: str = "sh",
        **kwargs: Any,
    ) -> None:
        """Generate an environment setup script.

        Args:
            project_name: Name of the project
            output_path: Path to write the script
            script_type: Either "sh" for bash or "ps1" for PowerShell
            **kwargs: Additional template variables
        """
        # Default configuration
        config = {
            "project_name": project_name,
            "env_profile_var": f"{project_name.upper().replace('-', None)}_PROFILE",
            "venv_prefix": project_name.lower(),
            "use_spinner": script_type == "sh",  # PowerShell doesn't need spinner
            "strict_project_check": False,
            "install_siblings": True,
            "sibling_patterns": [],  # No default peers - specified in config
            "special_siblings": [],  # No default peers - specified in config
            "create_log_dir": True,
            "deduplicate_path": True,
            "include_tool_verification": True,  # Enable for wrknv
            "cleanup_logs": True,
            "tools_to_verify": [
                {
                    "name": "Python",
                    "command": "python",
                    "check_type": "command",
                    "var_name": "PYTHON",
                    "version_cmd": "python --version 2>&1",
                },
                {
                    "name": "UV",
                    "command": "uv",
                    "check_type": "command",
                    "var_name": "UV",
                    "version_cmd": "uv --version 2>&1",
                },
                {
                    "name": "wrknv",
                    "command": "wrknv",
                    "check_type": "command",
                    "var_name": "WRKENV",
                    "version_cmd": "wrknv --version 2>&1 || echo 'No version info'",
                },
                {
                    "name": "ibmtf",
                    "command": "ibmtf",
                    "check_type": "command",
                    "var_name": "IBMTF",
                    "version_cmd": "ibmtf version 2>&1 | head -1 || echo 'Not installed'",
                },
                {
                    "name": "tofu",
                    "command": "tofu",
                    "check_type": "command",
                    "var_name": "TOFU",
                    "version_cmd": "tofu version 2>&1 | head -1 || echo 'Not installed'",
                },
            ],
            "useful_commands": [
                {
                    "command": f"{project_name.lower()} --help",
                    "description": f"{project_name} CLI",
                },
                {"command": "wrknv status", "description": "Check tool versions"},
                {
                    "command": "wrknv container status",
                    "description": "Container status",
                },
                {"command": "pytest", "description": "Run tests"},
                {"command": "deactivate", "description": "Exit environment"},
            ],
        }

        # Update with user-provided kwargs
        config.update(kwargs)

        # Select appropriate template and environment
        if script_type == "sh":
            template = self.sh_env.get_template("base.sh.j2")
        elif script_type == "ps1":
            template = self.ps1_env.get_template("base.ps1.j2")
        else:
            raise ValueError(f"Unknown script type: {script_type}")

        # Render and write
        content = template.render(**config)
        output_path.write_text(content)

        # Make executable if it's a shell script
        if script_type == "sh":
            output_path.chmod(output_path.stat().st_mode | 0o111)

    def xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_10(
        self,
        project_name: str,
        output_path: Path,
        script_type: str = "sh",
        **kwargs: Any,
    ) -> None:
        """Generate an environment setup script.

        Args:
            project_name: Name of the project
            output_path: Path to write the script
            script_type: Either "sh" for bash or "ps1" for PowerShell
            **kwargs: Additional template variables
        """
        # Default configuration
        config = {
            "project_name": project_name,
            "env_profile_var": f"{project_name.upper().replace('_')}_PROFILE",
            "venv_prefix": project_name.lower(),
            "use_spinner": script_type == "sh",  # PowerShell doesn't need spinner
            "strict_project_check": False,
            "install_siblings": True,
            "sibling_patterns": [],  # No default peers - specified in config
            "special_siblings": [],  # No default peers - specified in config
            "create_log_dir": True,
            "deduplicate_path": True,
            "include_tool_verification": True,  # Enable for wrknv
            "cleanup_logs": True,
            "tools_to_verify": [
                {
                    "name": "Python",
                    "command": "python",
                    "check_type": "command",
                    "var_name": "PYTHON",
                    "version_cmd": "python --version 2>&1",
                },
                {
                    "name": "UV",
                    "command": "uv",
                    "check_type": "command",
                    "var_name": "UV",
                    "version_cmd": "uv --version 2>&1",
                },
                {
                    "name": "wrknv",
                    "command": "wrknv",
                    "check_type": "command",
                    "var_name": "WRKENV",
                    "version_cmd": "wrknv --version 2>&1 || echo 'No version info'",
                },
                {
                    "name": "ibmtf",
                    "command": "ibmtf",
                    "check_type": "command",
                    "var_name": "IBMTF",
                    "version_cmd": "ibmtf version 2>&1 | head -1 || echo 'Not installed'",
                },
                {
                    "name": "tofu",
                    "command": "tofu",
                    "check_type": "command",
                    "var_name": "TOFU",
                    "version_cmd": "tofu version 2>&1 | head -1 || echo 'Not installed'",
                },
            ],
            "useful_commands": [
                {
                    "command": f"{project_name.lower()} --help",
                    "description": f"{project_name} CLI",
                },
                {"command": "wrknv status", "description": "Check tool versions"},
                {
                    "command": "wrknv container status",
                    "description": "Container status",
                },
                {"command": "pytest", "description": "Run tests"},
                {"command": "deactivate", "description": "Exit environment"},
            ],
        }

        # Update with user-provided kwargs
        config.update(kwargs)

        # Select appropriate template and environment
        if script_type == "sh":
            template = self.sh_env.get_template("base.sh.j2")
        elif script_type == "ps1":
            template = self.ps1_env.get_template("base.ps1.j2")
        else:
            raise ValueError(f"Unknown script type: {script_type}")

        # Render and write
        content = template.render(**config)
        output_path.write_text(content)

        # Make executable if it's a shell script
        if script_type == "sh":
            output_path.chmod(output_path.stat().st_mode | 0o111)

    def xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_11(
        self,
        project_name: str,
        output_path: Path,
        script_type: str = "sh",
        **kwargs: Any,
    ) -> None:
        """Generate an environment setup script.

        Args:
            project_name: Name of the project
            output_path: Path to write the script
            script_type: Either "sh" for bash or "ps1" for PowerShell
            **kwargs: Additional template variables
        """
        # Default configuration
        config = {
            "project_name": project_name,
            "env_profile_var": f"{project_name.upper().replace('-', )}_PROFILE",
            "venv_prefix": project_name.lower(),
            "use_spinner": script_type == "sh",  # PowerShell doesn't need spinner
            "strict_project_check": False,
            "install_siblings": True,
            "sibling_patterns": [],  # No default peers - specified in config
            "special_siblings": [],  # No default peers - specified in config
            "create_log_dir": True,
            "deduplicate_path": True,
            "include_tool_verification": True,  # Enable for wrknv
            "cleanup_logs": True,
            "tools_to_verify": [
                {
                    "name": "Python",
                    "command": "python",
                    "check_type": "command",
                    "var_name": "PYTHON",
                    "version_cmd": "python --version 2>&1",
                },
                {
                    "name": "UV",
                    "command": "uv",
                    "check_type": "command",
                    "var_name": "UV",
                    "version_cmd": "uv --version 2>&1",
                },
                {
                    "name": "wrknv",
                    "command": "wrknv",
                    "check_type": "command",
                    "var_name": "WRKENV",
                    "version_cmd": "wrknv --version 2>&1 || echo 'No version info'",
                },
                {
                    "name": "ibmtf",
                    "command": "ibmtf",
                    "check_type": "command",
                    "var_name": "IBMTF",
                    "version_cmd": "ibmtf version 2>&1 | head -1 || echo 'Not installed'",
                },
                {
                    "name": "tofu",
                    "command": "tofu",
                    "check_type": "command",
                    "var_name": "TOFU",
                    "version_cmd": "tofu version 2>&1 | head -1 || echo 'Not installed'",
                },
            ],
            "useful_commands": [
                {
                    "command": f"{project_name.lower()} --help",
                    "description": f"{project_name} CLI",
                },
                {"command": "wrknv status", "description": "Check tool versions"},
                {
                    "command": "wrknv container status",
                    "description": "Container status",
                },
                {"command": "pytest", "description": "Run tests"},
                {"command": "deactivate", "description": "Exit environment"},
            ],
        }

        # Update with user-provided kwargs
        config.update(kwargs)

        # Select appropriate template and environment
        if script_type == "sh":
            template = self.sh_env.get_template("base.sh.j2")
        elif script_type == "ps1":
            template = self.ps1_env.get_template("base.ps1.j2")
        else:
            raise ValueError(f"Unknown script type: {script_type}")

        # Render and write
        content = template.render(**config)
        output_path.write_text(content)

        # Make executable if it's a shell script
        if script_type == "sh":
            output_path.chmod(output_path.stat().st_mode | 0o111)

    def xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_12(
        self,
        project_name: str,
        output_path: Path,
        script_type: str = "sh",
        **kwargs: Any,
    ) -> None:
        """Generate an environment setup script.

        Args:
            project_name: Name of the project
            output_path: Path to write the script
            script_type: Either "sh" for bash or "ps1" for PowerShell
            **kwargs: Additional template variables
        """
        # Default configuration
        config = {
            "project_name": project_name,
            "env_profile_var": f"{project_name.lower().replace('-', '_')}_PROFILE",
            "venv_prefix": project_name.lower(),
            "use_spinner": script_type == "sh",  # PowerShell doesn't need spinner
            "strict_project_check": False,
            "install_siblings": True,
            "sibling_patterns": [],  # No default peers - specified in config
            "special_siblings": [],  # No default peers - specified in config
            "create_log_dir": True,
            "deduplicate_path": True,
            "include_tool_verification": True,  # Enable for wrknv
            "cleanup_logs": True,
            "tools_to_verify": [
                {
                    "name": "Python",
                    "command": "python",
                    "check_type": "command",
                    "var_name": "PYTHON",
                    "version_cmd": "python --version 2>&1",
                },
                {
                    "name": "UV",
                    "command": "uv",
                    "check_type": "command",
                    "var_name": "UV",
                    "version_cmd": "uv --version 2>&1",
                },
                {
                    "name": "wrknv",
                    "command": "wrknv",
                    "check_type": "command",
                    "var_name": "WRKENV",
                    "version_cmd": "wrknv --version 2>&1 || echo 'No version info'",
                },
                {
                    "name": "ibmtf",
                    "command": "ibmtf",
                    "check_type": "command",
                    "var_name": "IBMTF",
                    "version_cmd": "ibmtf version 2>&1 | head -1 || echo 'Not installed'",
                },
                {
                    "name": "tofu",
                    "command": "tofu",
                    "check_type": "command",
                    "var_name": "TOFU",
                    "version_cmd": "tofu version 2>&1 | head -1 || echo 'Not installed'",
                },
            ],
            "useful_commands": [
                {
                    "command": f"{project_name.lower()} --help",
                    "description": f"{project_name} CLI",
                },
                {"command": "wrknv status", "description": "Check tool versions"},
                {
                    "command": "wrknv container status",
                    "description": "Container status",
                },
                {"command": "pytest", "description": "Run tests"},
                {"command": "deactivate", "description": "Exit environment"},
            ],
        }

        # Update with user-provided kwargs
        config.update(kwargs)

        # Select appropriate template and environment
        if script_type == "sh":
            template = self.sh_env.get_template("base.sh.j2")
        elif script_type == "ps1":
            template = self.ps1_env.get_template("base.ps1.j2")
        else:
            raise ValueError(f"Unknown script type: {script_type}")

        # Render and write
        content = template.render(**config)
        output_path.write_text(content)

        # Make executable if it's a shell script
        if script_type == "sh":
            output_path.chmod(output_path.stat().st_mode | 0o111)

    def xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_13(
        self,
        project_name: str,
        output_path: Path,
        script_type: str = "sh",
        **kwargs: Any,
    ) -> None:
        """Generate an environment setup script.

        Args:
            project_name: Name of the project
            output_path: Path to write the script
            script_type: Either "sh" for bash or "ps1" for PowerShell
            **kwargs: Additional template variables
        """
        # Default configuration
        config = {
            "project_name": project_name,
            "env_profile_var": f"{project_name.upper().replace('XX-XX', '_')}_PROFILE",
            "venv_prefix": project_name.lower(),
            "use_spinner": script_type == "sh",  # PowerShell doesn't need spinner
            "strict_project_check": False,
            "install_siblings": True,
            "sibling_patterns": [],  # No default peers - specified in config
            "special_siblings": [],  # No default peers - specified in config
            "create_log_dir": True,
            "deduplicate_path": True,
            "include_tool_verification": True,  # Enable for wrknv
            "cleanup_logs": True,
            "tools_to_verify": [
                {
                    "name": "Python",
                    "command": "python",
                    "check_type": "command",
                    "var_name": "PYTHON",
                    "version_cmd": "python --version 2>&1",
                },
                {
                    "name": "UV",
                    "command": "uv",
                    "check_type": "command",
                    "var_name": "UV",
                    "version_cmd": "uv --version 2>&1",
                },
                {
                    "name": "wrknv",
                    "command": "wrknv",
                    "check_type": "command",
                    "var_name": "WRKENV",
                    "version_cmd": "wrknv --version 2>&1 || echo 'No version info'",
                },
                {
                    "name": "ibmtf",
                    "command": "ibmtf",
                    "check_type": "command",
                    "var_name": "IBMTF",
                    "version_cmd": "ibmtf version 2>&1 | head -1 || echo 'Not installed'",
                },
                {
                    "name": "tofu",
                    "command": "tofu",
                    "check_type": "command",
                    "var_name": "TOFU",
                    "version_cmd": "tofu version 2>&1 | head -1 || echo 'Not installed'",
                },
            ],
            "useful_commands": [
                {
                    "command": f"{project_name.lower()} --help",
                    "description": f"{project_name} CLI",
                },
                {"command": "wrknv status", "description": "Check tool versions"},
                {
                    "command": "wrknv container status",
                    "description": "Container status",
                },
                {"command": "pytest", "description": "Run tests"},
                {"command": "deactivate", "description": "Exit environment"},
            ],
        }

        # Update with user-provided kwargs
        config.update(kwargs)

        # Select appropriate template and environment
        if script_type == "sh":
            template = self.sh_env.get_template("base.sh.j2")
        elif script_type == "ps1":
            template = self.ps1_env.get_template("base.ps1.j2")
        else:
            raise ValueError(f"Unknown script type: {script_type}")

        # Render and write
        content = template.render(**config)
        output_path.write_text(content)

        # Make executable if it's a shell script
        if script_type == "sh":
            output_path.chmod(output_path.stat().st_mode | 0o111)

    def xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_14(
        self,
        project_name: str,
        output_path: Path,
        script_type: str = "sh",
        **kwargs: Any,
    ) -> None:
        """Generate an environment setup script.

        Args:
            project_name: Name of the project
            output_path: Path to write the script
            script_type: Either "sh" for bash or "ps1" for PowerShell
            **kwargs: Additional template variables
        """
        # Default configuration
        config = {
            "project_name": project_name,
            "env_profile_var": f"{project_name.upper().replace('-', 'XX_XX')}_PROFILE",
            "venv_prefix": project_name.lower(),
            "use_spinner": script_type == "sh",  # PowerShell doesn't need spinner
            "strict_project_check": False,
            "install_siblings": True,
            "sibling_patterns": [],  # No default peers - specified in config
            "special_siblings": [],  # No default peers - specified in config
            "create_log_dir": True,
            "deduplicate_path": True,
            "include_tool_verification": True,  # Enable for wrknv
            "cleanup_logs": True,
            "tools_to_verify": [
                {
                    "name": "Python",
                    "command": "python",
                    "check_type": "command",
                    "var_name": "PYTHON",
                    "version_cmd": "python --version 2>&1",
                },
                {
                    "name": "UV",
                    "command": "uv",
                    "check_type": "command",
                    "var_name": "UV",
                    "version_cmd": "uv --version 2>&1",
                },
                {
                    "name": "wrknv",
                    "command": "wrknv",
                    "check_type": "command",
                    "var_name": "WRKENV",
                    "version_cmd": "wrknv --version 2>&1 || echo 'No version info'",
                },
                {
                    "name": "ibmtf",
                    "command": "ibmtf",
                    "check_type": "command",
                    "var_name": "IBMTF",
                    "version_cmd": "ibmtf version 2>&1 | head -1 || echo 'Not installed'",
                },
                {
                    "name": "tofu",
                    "command": "tofu",
                    "check_type": "command",
                    "var_name": "TOFU",
                    "version_cmd": "tofu version 2>&1 | head -1 || echo 'Not installed'",
                },
            ],
            "useful_commands": [
                {
                    "command": f"{project_name.lower()} --help",
                    "description": f"{project_name} CLI",
                },
                {"command": "wrknv status", "description": "Check tool versions"},
                {
                    "command": "wrknv container status",
                    "description": "Container status",
                },
                {"command": "pytest", "description": "Run tests"},
                {"command": "deactivate", "description": "Exit environment"},
            ],
        }

        # Update with user-provided kwargs
        config.update(kwargs)

        # Select appropriate template and environment
        if script_type == "sh":
            template = self.sh_env.get_template("base.sh.j2")
        elif script_type == "ps1":
            template = self.ps1_env.get_template("base.ps1.j2")
        else:
            raise ValueError(f"Unknown script type: {script_type}")

        # Render and write
        content = template.render(**config)
        output_path.write_text(content)

        # Make executable if it's a shell script
        if script_type == "sh":
            output_path.chmod(output_path.stat().st_mode | 0o111)

    def xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_15(
        self,
        project_name: str,
        output_path: Path,
        script_type: str = "sh",
        **kwargs: Any,
    ) -> None:
        """Generate an environment setup script.

        Args:
            project_name: Name of the project
            output_path: Path to write the script
            script_type: Either "sh" for bash or "ps1" for PowerShell
            **kwargs: Additional template variables
        """
        # Default configuration
        config = {
            "project_name": project_name,
            "env_profile_var": f"{project_name.upper().replace('-', '_')}_PROFILE",
            "XXvenv_prefixXX": project_name.lower(),
            "use_spinner": script_type == "sh",  # PowerShell doesn't need spinner
            "strict_project_check": False,
            "install_siblings": True,
            "sibling_patterns": [],  # No default peers - specified in config
            "special_siblings": [],  # No default peers - specified in config
            "create_log_dir": True,
            "deduplicate_path": True,
            "include_tool_verification": True,  # Enable for wrknv
            "cleanup_logs": True,
            "tools_to_verify": [
                {
                    "name": "Python",
                    "command": "python",
                    "check_type": "command",
                    "var_name": "PYTHON",
                    "version_cmd": "python --version 2>&1",
                },
                {
                    "name": "UV",
                    "command": "uv",
                    "check_type": "command",
                    "var_name": "UV",
                    "version_cmd": "uv --version 2>&1",
                },
                {
                    "name": "wrknv",
                    "command": "wrknv",
                    "check_type": "command",
                    "var_name": "WRKENV",
                    "version_cmd": "wrknv --version 2>&1 || echo 'No version info'",
                },
                {
                    "name": "ibmtf",
                    "command": "ibmtf",
                    "check_type": "command",
                    "var_name": "IBMTF",
                    "version_cmd": "ibmtf version 2>&1 | head -1 || echo 'Not installed'",
                },
                {
                    "name": "tofu",
                    "command": "tofu",
                    "check_type": "command",
                    "var_name": "TOFU",
                    "version_cmd": "tofu version 2>&1 | head -1 || echo 'Not installed'",
                },
            ],
            "useful_commands": [
                {
                    "command": f"{project_name.lower()} --help",
                    "description": f"{project_name} CLI",
                },
                {"command": "wrknv status", "description": "Check tool versions"},
                {
                    "command": "wrknv container status",
                    "description": "Container status",
                },
                {"command": "pytest", "description": "Run tests"},
                {"command": "deactivate", "description": "Exit environment"},
            ],
        }

        # Update with user-provided kwargs
        config.update(kwargs)

        # Select appropriate template and environment
        if script_type == "sh":
            template = self.sh_env.get_template("base.sh.j2")
        elif script_type == "ps1":
            template = self.ps1_env.get_template("base.ps1.j2")
        else:
            raise ValueError(f"Unknown script type: {script_type}")

        # Render and write
        content = template.render(**config)
        output_path.write_text(content)

        # Make executable if it's a shell script
        if script_type == "sh":
            output_path.chmod(output_path.stat().st_mode | 0o111)

    def xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_16(
        self,
        project_name: str,
        output_path: Path,
        script_type: str = "sh",
        **kwargs: Any,
    ) -> None:
        """Generate an environment setup script.

        Args:
            project_name: Name of the project
            output_path: Path to write the script
            script_type: Either "sh" for bash or "ps1" for PowerShell
            **kwargs: Additional template variables
        """
        # Default configuration
        config = {
            "project_name": project_name,
            "env_profile_var": f"{project_name.upper().replace('-', '_')}_PROFILE",
            "VENV_PREFIX": project_name.lower(),
            "use_spinner": script_type == "sh",  # PowerShell doesn't need spinner
            "strict_project_check": False,
            "install_siblings": True,
            "sibling_patterns": [],  # No default peers - specified in config
            "special_siblings": [],  # No default peers - specified in config
            "create_log_dir": True,
            "deduplicate_path": True,
            "include_tool_verification": True,  # Enable for wrknv
            "cleanup_logs": True,
            "tools_to_verify": [
                {
                    "name": "Python",
                    "command": "python",
                    "check_type": "command",
                    "var_name": "PYTHON",
                    "version_cmd": "python --version 2>&1",
                },
                {
                    "name": "UV",
                    "command": "uv",
                    "check_type": "command",
                    "var_name": "UV",
                    "version_cmd": "uv --version 2>&1",
                },
                {
                    "name": "wrknv",
                    "command": "wrknv",
                    "check_type": "command",
                    "var_name": "WRKENV",
                    "version_cmd": "wrknv --version 2>&1 || echo 'No version info'",
                },
                {
                    "name": "ibmtf",
                    "command": "ibmtf",
                    "check_type": "command",
                    "var_name": "IBMTF",
                    "version_cmd": "ibmtf version 2>&1 | head -1 || echo 'Not installed'",
                },
                {
                    "name": "tofu",
                    "command": "tofu",
                    "check_type": "command",
                    "var_name": "TOFU",
                    "version_cmd": "tofu version 2>&1 | head -1 || echo 'Not installed'",
                },
            ],
            "useful_commands": [
                {
                    "command": f"{project_name.lower()} --help",
                    "description": f"{project_name} CLI",
                },
                {"command": "wrknv status", "description": "Check tool versions"},
                {
                    "command": "wrknv container status",
                    "description": "Container status",
                },
                {"command": "pytest", "description": "Run tests"},
                {"command": "deactivate", "description": "Exit environment"},
            ],
        }

        # Update with user-provided kwargs
        config.update(kwargs)

        # Select appropriate template and environment
        if script_type == "sh":
            template = self.sh_env.get_template("base.sh.j2")
        elif script_type == "ps1":
            template = self.ps1_env.get_template("base.ps1.j2")
        else:
            raise ValueError(f"Unknown script type: {script_type}")

        # Render and write
        content = template.render(**config)
        output_path.write_text(content)

        # Make executable if it's a shell script
        if script_type == "sh":
            output_path.chmod(output_path.stat().st_mode | 0o111)

    def xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_17(
        self,
        project_name: str,
        output_path: Path,
        script_type: str = "sh",
        **kwargs: Any,
    ) -> None:
        """Generate an environment setup script.

        Args:
            project_name: Name of the project
            output_path: Path to write the script
            script_type: Either "sh" for bash or "ps1" for PowerShell
            **kwargs: Additional template variables
        """
        # Default configuration
        config = {
            "project_name": project_name,
            "env_profile_var": f"{project_name.upper().replace('-', '_')}_PROFILE",
            "venv_prefix": project_name.upper(),
            "use_spinner": script_type == "sh",  # PowerShell doesn't need spinner
            "strict_project_check": False,
            "install_siblings": True,
            "sibling_patterns": [],  # No default peers - specified in config
            "special_siblings": [],  # No default peers - specified in config
            "create_log_dir": True,
            "deduplicate_path": True,
            "include_tool_verification": True,  # Enable for wrknv
            "cleanup_logs": True,
            "tools_to_verify": [
                {
                    "name": "Python",
                    "command": "python",
                    "check_type": "command",
                    "var_name": "PYTHON",
                    "version_cmd": "python --version 2>&1",
                },
                {
                    "name": "UV",
                    "command": "uv",
                    "check_type": "command",
                    "var_name": "UV",
                    "version_cmd": "uv --version 2>&1",
                },
                {
                    "name": "wrknv",
                    "command": "wrknv",
                    "check_type": "command",
                    "var_name": "WRKENV",
                    "version_cmd": "wrknv --version 2>&1 || echo 'No version info'",
                },
                {
                    "name": "ibmtf",
                    "command": "ibmtf",
                    "check_type": "command",
                    "var_name": "IBMTF",
                    "version_cmd": "ibmtf version 2>&1 | head -1 || echo 'Not installed'",
                },
                {
                    "name": "tofu",
                    "command": "tofu",
                    "check_type": "command",
                    "var_name": "TOFU",
                    "version_cmd": "tofu version 2>&1 | head -1 || echo 'Not installed'",
                },
            ],
            "useful_commands": [
                {
                    "command": f"{project_name.lower()} --help",
                    "description": f"{project_name} CLI",
                },
                {"command": "wrknv status", "description": "Check tool versions"},
                {
                    "command": "wrknv container status",
                    "description": "Container status",
                },
                {"command": "pytest", "description": "Run tests"},
                {"command": "deactivate", "description": "Exit environment"},
            ],
        }

        # Update with user-provided kwargs
        config.update(kwargs)

        # Select appropriate template and environment
        if script_type == "sh":
            template = self.sh_env.get_template("base.sh.j2")
        elif script_type == "ps1":
            template = self.ps1_env.get_template("base.ps1.j2")
        else:
            raise ValueError(f"Unknown script type: {script_type}")

        # Render and write
        content = template.render(**config)
        output_path.write_text(content)

        # Make executable if it's a shell script
        if script_type == "sh":
            output_path.chmod(output_path.stat().st_mode | 0o111)

    def xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_18(
        self,
        project_name: str,
        output_path: Path,
        script_type: str = "sh",
        **kwargs: Any,
    ) -> None:
        """Generate an environment setup script.

        Args:
            project_name: Name of the project
            output_path: Path to write the script
            script_type: Either "sh" for bash or "ps1" for PowerShell
            **kwargs: Additional template variables
        """
        # Default configuration
        config = {
            "project_name": project_name,
            "env_profile_var": f"{project_name.upper().replace('-', '_')}_PROFILE",
            "venv_prefix": project_name.lower(),
            "XXuse_spinnerXX": script_type == "sh",  # PowerShell doesn't need spinner
            "strict_project_check": False,
            "install_siblings": True,
            "sibling_patterns": [],  # No default peers - specified in config
            "special_siblings": [],  # No default peers - specified in config
            "create_log_dir": True,
            "deduplicate_path": True,
            "include_tool_verification": True,  # Enable for wrknv
            "cleanup_logs": True,
            "tools_to_verify": [
                {
                    "name": "Python",
                    "command": "python",
                    "check_type": "command",
                    "var_name": "PYTHON",
                    "version_cmd": "python --version 2>&1",
                },
                {
                    "name": "UV",
                    "command": "uv",
                    "check_type": "command",
                    "var_name": "UV",
                    "version_cmd": "uv --version 2>&1",
                },
                {
                    "name": "wrknv",
                    "command": "wrknv",
                    "check_type": "command",
                    "var_name": "WRKENV",
                    "version_cmd": "wrknv --version 2>&1 || echo 'No version info'",
                },
                {
                    "name": "ibmtf",
                    "command": "ibmtf",
                    "check_type": "command",
                    "var_name": "IBMTF",
                    "version_cmd": "ibmtf version 2>&1 | head -1 || echo 'Not installed'",
                },
                {
                    "name": "tofu",
                    "command": "tofu",
                    "check_type": "command",
                    "var_name": "TOFU",
                    "version_cmd": "tofu version 2>&1 | head -1 || echo 'Not installed'",
                },
            ],
            "useful_commands": [
                {
                    "command": f"{project_name.lower()} --help",
                    "description": f"{project_name} CLI",
                },
                {"command": "wrknv status", "description": "Check tool versions"},
                {
                    "command": "wrknv container status",
                    "description": "Container status",
                },
                {"command": "pytest", "description": "Run tests"},
                {"command": "deactivate", "description": "Exit environment"},
            ],
        }

        # Update with user-provided kwargs
        config.update(kwargs)

        # Select appropriate template and environment
        if script_type == "sh":
            template = self.sh_env.get_template("base.sh.j2")
        elif script_type == "ps1":
            template = self.ps1_env.get_template("base.ps1.j2")
        else:
            raise ValueError(f"Unknown script type: {script_type}")

        # Render and write
        content = template.render(**config)
        output_path.write_text(content)

        # Make executable if it's a shell script
        if script_type == "sh":
            output_path.chmod(output_path.stat().st_mode | 0o111)

    def xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_19(
        self,
        project_name: str,
        output_path: Path,
        script_type: str = "sh",
        **kwargs: Any,
    ) -> None:
        """Generate an environment setup script.

        Args:
            project_name: Name of the project
            output_path: Path to write the script
            script_type: Either "sh" for bash or "ps1" for PowerShell
            **kwargs: Additional template variables
        """
        # Default configuration
        config = {
            "project_name": project_name,
            "env_profile_var": f"{project_name.upper().replace('-', '_')}_PROFILE",
            "venv_prefix": project_name.lower(),
            "USE_SPINNER": script_type == "sh",  # PowerShell doesn't need spinner
            "strict_project_check": False,
            "install_siblings": True,
            "sibling_patterns": [],  # No default peers - specified in config
            "special_siblings": [],  # No default peers - specified in config
            "create_log_dir": True,
            "deduplicate_path": True,
            "include_tool_verification": True,  # Enable for wrknv
            "cleanup_logs": True,
            "tools_to_verify": [
                {
                    "name": "Python",
                    "command": "python",
                    "check_type": "command",
                    "var_name": "PYTHON",
                    "version_cmd": "python --version 2>&1",
                },
                {
                    "name": "UV",
                    "command": "uv",
                    "check_type": "command",
                    "var_name": "UV",
                    "version_cmd": "uv --version 2>&1",
                },
                {
                    "name": "wrknv",
                    "command": "wrknv",
                    "check_type": "command",
                    "var_name": "WRKENV",
                    "version_cmd": "wrknv --version 2>&1 || echo 'No version info'",
                },
                {
                    "name": "ibmtf",
                    "command": "ibmtf",
                    "check_type": "command",
                    "var_name": "IBMTF",
                    "version_cmd": "ibmtf version 2>&1 | head -1 || echo 'Not installed'",
                },
                {
                    "name": "tofu",
                    "command": "tofu",
                    "check_type": "command",
                    "var_name": "TOFU",
                    "version_cmd": "tofu version 2>&1 | head -1 || echo 'Not installed'",
                },
            ],
            "useful_commands": [
                {
                    "command": f"{project_name.lower()} --help",
                    "description": f"{project_name} CLI",
                },
                {"command": "wrknv status", "description": "Check tool versions"},
                {
                    "command": "wrknv container status",
                    "description": "Container status",
                },
                {"command": "pytest", "description": "Run tests"},
                {"command": "deactivate", "description": "Exit environment"},
            ],
        }

        # Update with user-provided kwargs
        config.update(kwargs)

        # Select appropriate template and environment
        if script_type == "sh":
            template = self.sh_env.get_template("base.sh.j2")
        elif script_type == "ps1":
            template = self.ps1_env.get_template("base.ps1.j2")
        else:
            raise ValueError(f"Unknown script type: {script_type}")

        # Render and write
        content = template.render(**config)
        output_path.write_text(content)

        # Make executable if it's a shell script
        if script_type == "sh":
            output_path.chmod(output_path.stat().st_mode | 0o111)

    def xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_20(
        self,
        project_name: str,
        output_path: Path,
        script_type: str = "sh",
        **kwargs: Any,
    ) -> None:
        """Generate an environment setup script.

        Args:
            project_name: Name of the project
            output_path: Path to write the script
            script_type: Either "sh" for bash or "ps1" for PowerShell
            **kwargs: Additional template variables
        """
        # Default configuration
        config = {
            "project_name": project_name,
            "env_profile_var": f"{project_name.upper().replace('-', '_')}_PROFILE",
            "venv_prefix": project_name.lower(),
            "use_spinner": script_type != "sh",  # PowerShell doesn't need spinner
            "strict_project_check": False,
            "install_siblings": True,
            "sibling_patterns": [],  # No default peers - specified in config
            "special_siblings": [],  # No default peers - specified in config
            "create_log_dir": True,
            "deduplicate_path": True,
            "include_tool_verification": True,  # Enable for wrknv
            "cleanup_logs": True,
            "tools_to_verify": [
                {
                    "name": "Python",
                    "command": "python",
                    "check_type": "command",
                    "var_name": "PYTHON",
                    "version_cmd": "python --version 2>&1",
                },
                {
                    "name": "UV",
                    "command": "uv",
                    "check_type": "command",
                    "var_name": "UV",
                    "version_cmd": "uv --version 2>&1",
                },
                {
                    "name": "wrknv",
                    "command": "wrknv",
                    "check_type": "command",
                    "var_name": "WRKENV",
                    "version_cmd": "wrknv --version 2>&1 || echo 'No version info'",
                },
                {
                    "name": "ibmtf",
                    "command": "ibmtf",
                    "check_type": "command",
                    "var_name": "IBMTF",
                    "version_cmd": "ibmtf version 2>&1 | head -1 || echo 'Not installed'",
                },
                {
                    "name": "tofu",
                    "command": "tofu",
                    "check_type": "command",
                    "var_name": "TOFU",
                    "version_cmd": "tofu version 2>&1 | head -1 || echo 'Not installed'",
                },
            ],
            "useful_commands": [
                {
                    "command": f"{project_name.lower()} --help",
                    "description": f"{project_name} CLI",
                },
                {"command": "wrknv status", "description": "Check tool versions"},
                {
                    "command": "wrknv container status",
                    "description": "Container status",
                },
                {"command": "pytest", "description": "Run tests"},
                {"command": "deactivate", "description": "Exit environment"},
            ],
        }

        # Update with user-provided kwargs
        config.update(kwargs)

        # Select appropriate template and environment
        if script_type == "sh":
            template = self.sh_env.get_template("base.sh.j2")
        elif script_type == "ps1":
            template = self.ps1_env.get_template("base.ps1.j2")
        else:
            raise ValueError(f"Unknown script type: {script_type}")

        # Render and write
        content = template.render(**config)
        output_path.write_text(content)

        # Make executable if it's a shell script
        if script_type == "sh":
            output_path.chmod(output_path.stat().st_mode | 0o111)

    def xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_21(
        self,
        project_name: str,
        output_path: Path,
        script_type: str = "sh",
        **kwargs: Any,
    ) -> None:
        """Generate an environment setup script.

        Args:
            project_name: Name of the project
            output_path: Path to write the script
            script_type: Either "sh" for bash or "ps1" for PowerShell
            **kwargs: Additional template variables
        """
        # Default configuration
        config = {
            "project_name": project_name,
            "env_profile_var": f"{project_name.upper().replace('-', '_')}_PROFILE",
            "venv_prefix": project_name.lower(),
            "use_spinner": script_type == "XXshXX",  # PowerShell doesn't need spinner
            "strict_project_check": False,
            "install_siblings": True,
            "sibling_patterns": [],  # No default peers - specified in config
            "special_siblings": [],  # No default peers - specified in config
            "create_log_dir": True,
            "deduplicate_path": True,
            "include_tool_verification": True,  # Enable for wrknv
            "cleanup_logs": True,
            "tools_to_verify": [
                {
                    "name": "Python",
                    "command": "python",
                    "check_type": "command",
                    "var_name": "PYTHON",
                    "version_cmd": "python --version 2>&1",
                },
                {
                    "name": "UV",
                    "command": "uv",
                    "check_type": "command",
                    "var_name": "UV",
                    "version_cmd": "uv --version 2>&1",
                },
                {
                    "name": "wrknv",
                    "command": "wrknv",
                    "check_type": "command",
                    "var_name": "WRKENV",
                    "version_cmd": "wrknv --version 2>&1 || echo 'No version info'",
                },
                {
                    "name": "ibmtf",
                    "command": "ibmtf",
                    "check_type": "command",
                    "var_name": "IBMTF",
                    "version_cmd": "ibmtf version 2>&1 | head -1 || echo 'Not installed'",
                },
                {
                    "name": "tofu",
                    "command": "tofu",
                    "check_type": "command",
                    "var_name": "TOFU",
                    "version_cmd": "tofu version 2>&1 | head -1 || echo 'Not installed'",
                },
            ],
            "useful_commands": [
                {
                    "command": f"{project_name.lower()} --help",
                    "description": f"{project_name} CLI",
                },
                {"command": "wrknv status", "description": "Check tool versions"},
                {
                    "command": "wrknv container status",
                    "description": "Container status",
                },
                {"command": "pytest", "description": "Run tests"},
                {"command": "deactivate", "description": "Exit environment"},
            ],
        }

        # Update with user-provided kwargs
        config.update(kwargs)

        # Select appropriate template and environment
        if script_type == "sh":
            template = self.sh_env.get_template("base.sh.j2")
        elif script_type == "ps1":
            template = self.ps1_env.get_template("base.ps1.j2")
        else:
            raise ValueError(f"Unknown script type: {script_type}")

        # Render and write
        content = template.render(**config)
        output_path.write_text(content)

        # Make executable if it's a shell script
        if script_type == "sh":
            output_path.chmod(output_path.stat().st_mode | 0o111)

    def xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_22(
        self,
        project_name: str,
        output_path: Path,
        script_type: str = "sh",
        **kwargs: Any,
    ) -> None:
        """Generate an environment setup script.

        Args:
            project_name: Name of the project
            output_path: Path to write the script
            script_type: Either "sh" for bash or "ps1" for PowerShell
            **kwargs: Additional template variables
        """
        # Default configuration
        config = {
            "project_name": project_name,
            "env_profile_var": f"{project_name.upper().replace('-', '_')}_PROFILE",
            "venv_prefix": project_name.lower(),
            "use_spinner": script_type == "SH",  # PowerShell doesn't need spinner
            "strict_project_check": False,
            "install_siblings": True,
            "sibling_patterns": [],  # No default peers - specified in config
            "special_siblings": [],  # No default peers - specified in config
            "create_log_dir": True,
            "deduplicate_path": True,
            "include_tool_verification": True,  # Enable for wrknv
            "cleanup_logs": True,
            "tools_to_verify": [
                {
                    "name": "Python",
                    "command": "python",
                    "check_type": "command",
                    "var_name": "PYTHON",
                    "version_cmd": "python --version 2>&1",
                },
                {
                    "name": "UV",
                    "command": "uv",
                    "check_type": "command",
                    "var_name": "UV",
                    "version_cmd": "uv --version 2>&1",
                },
                {
                    "name": "wrknv",
                    "command": "wrknv",
                    "check_type": "command",
                    "var_name": "WRKENV",
                    "version_cmd": "wrknv --version 2>&1 || echo 'No version info'",
                },
                {
                    "name": "ibmtf",
                    "command": "ibmtf",
                    "check_type": "command",
                    "var_name": "IBMTF",
                    "version_cmd": "ibmtf version 2>&1 | head -1 || echo 'Not installed'",
                },
                {
                    "name": "tofu",
                    "command": "tofu",
                    "check_type": "command",
                    "var_name": "TOFU",
                    "version_cmd": "tofu version 2>&1 | head -1 || echo 'Not installed'",
                },
            ],
            "useful_commands": [
                {
                    "command": f"{project_name.lower()} --help",
                    "description": f"{project_name} CLI",
                },
                {"command": "wrknv status", "description": "Check tool versions"},
                {
                    "command": "wrknv container status",
                    "description": "Container status",
                },
                {"command": "pytest", "description": "Run tests"},
                {"command": "deactivate", "description": "Exit environment"},
            ],
        }

        # Update with user-provided kwargs
        config.update(kwargs)

        # Select appropriate template and environment
        if script_type == "sh":
            template = self.sh_env.get_template("base.sh.j2")
        elif script_type == "ps1":
            template = self.ps1_env.get_template("base.ps1.j2")
        else:
            raise ValueError(f"Unknown script type: {script_type}")

        # Render and write
        content = template.render(**config)
        output_path.write_text(content)

        # Make executable if it's a shell script
        if script_type == "sh":
            output_path.chmod(output_path.stat().st_mode | 0o111)

    def xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_23(
        self,
        project_name: str,
        output_path: Path,
        script_type: str = "sh",
        **kwargs: Any,
    ) -> None:
        """Generate an environment setup script.

        Args:
            project_name: Name of the project
            output_path: Path to write the script
            script_type: Either "sh" for bash or "ps1" for PowerShell
            **kwargs: Additional template variables
        """
        # Default configuration
        config = {
            "project_name": project_name,
            "env_profile_var": f"{project_name.upper().replace('-', '_')}_PROFILE",
            "venv_prefix": project_name.lower(),
            "use_spinner": script_type == "sh",  # PowerShell doesn't need spinner
            "XXstrict_project_checkXX": False,
            "install_siblings": True,
            "sibling_patterns": [],  # No default peers - specified in config
            "special_siblings": [],  # No default peers - specified in config
            "create_log_dir": True,
            "deduplicate_path": True,
            "include_tool_verification": True,  # Enable for wrknv
            "cleanup_logs": True,
            "tools_to_verify": [
                {
                    "name": "Python",
                    "command": "python",
                    "check_type": "command",
                    "var_name": "PYTHON",
                    "version_cmd": "python --version 2>&1",
                },
                {
                    "name": "UV",
                    "command": "uv",
                    "check_type": "command",
                    "var_name": "UV",
                    "version_cmd": "uv --version 2>&1",
                },
                {
                    "name": "wrknv",
                    "command": "wrknv",
                    "check_type": "command",
                    "var_name": "WRKENV",
                    "version_cmd": "wrknv --version 2>&1 || echo 'No version info'",
                },
                {
                    "name": "ibmtf",
                    "command": "ibmtf",
                    "check_type": "command",
                    "var_name": "IBMTF",
                    "version_cmd": "ibmtf version 2>&1 | head -1 || echo 'Not installed'",
                },
                {
                    "name": "tofu",
                    "command": "tofu",
                    "check_type": "command",
                    "var_name": "TOFU",
                    "version_cmd": "tofu version 2>&1 | head -1 || echo 'Not installed'",
                },
            ],
            "useful_commands": [
                {
                    "command": f"{project_name.lower()} --help",
                    "description": f"{project_name} CLI",
                },
                {"command": "wrknv status", "description": "Check tool versions"},
                {
                    "command": "wrknv container status",
                    "description": "Container status",
                },
                {"command": "pytest", "description": "Run tests"},
                {"command": "deactivate", "description": "Exit environment"},
            ],
        }

        # Update with user-provided kwargs
        config.update(kwargs)

        # Select appropriate template and environment
        if script_type == "sh":
            template = self.sh_env.get_template("base.sh.j2")
        elif script_type == "ps1":
            template = self.ps1_env.get_template("base.ps1.j2")
        else:
            raise ValueError(f"Unknown script type: {script_type}")

        # Render and write
        content = template.render(**config)
        output_path.write_text(content)

        # Make executable if it's a shell script
        if script_type == "sh":
            output_path.chmod(output_path.stat().st_mode | 0o111)

    def xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_24(
        self,
        project_name: str,
        output_path: Path,
        script_type: str = "sh",
        **kwargs: Any,
    ) -> None:
        """Generate an environment setup script.

        Args:
            project_name: Name of the project
            output_path: Path to write the script
            script_type: Either "sh" for bash or "ps1" for PowerShell
            **kwargs: Additional template variables
        """
        # Default configuration
        config = {
            "project_name": project_name,
            "env_profile_var": f"{project_name.upper().replace('-', '_')}_PROFILE",
            "venv_prefix": project_name.lower(),
            "use_spinner": script_type == "sh",  # PowerShell doesn't need spinner
            "STRICT_PROJECT_CHECK": False,
            "install_siblings": True,
            "sibling_patterns": [],  # No default peers - specified in config
            "special_siblings": [],  # No default peers - specified in config
            "create_log_dir": True,
            "deduplicate_path": True,
            "include_tool_verification": True,  # Enable for wrknv
            "cleanup_logs": True,
            "tools_to_verify": [
                {
                    "name": "Python",
                    "command": "python",
                    "check_type": "command",
                    "var_name": "PYTHON",
                    "version_cmd": "python --version 2>&1",
                },
                {
                    "name": "UV",
                    "command": "uv",
                    "check_type": "command",
                    "var_name": "UV",
                    "version_cmd": "uv --version 2>&1",
                },
                {
                    "name": "wrknv",
                    "command": "wrknv",
                    "check_type": "command",
                    "var_name": "WRKENV",
                    "version_cmd": "wrknv --version 2>&1 || echo 'No version info'",
                },
                {
                    "name": "ibmtf",
                    "command": "ibmtf",
                    "check_type": "command",
                    "var_name": "IBMTF",
                    "version_cmd": "ibmtf version 2>&1 | head -1 || echo 'Not installed'",
                },
                {
                    "name": "tofu",
                    "command": "tofu",
                    "check_type": "command",
                    "var_name": "TOFU",
                    "version_cmd": "tofu version 2>&1 | head -1 || echo 'Not installed'",
                },
            ],
            "useful_commands": [
                {
                    "command": f"{project_name.lower()} --help",
                    "description": f"{project_name} CLI",
                },
                {"command": "wrknv status", "description": "Check tool versions"},
                {
                    "command": "wrknv container status",
                    "description": "Container status",
                },
                {"command": "pytest", "description": "Run tests"},
                {"command": "deactivate", "description": "Exit environment"},
            ],
        }

        # Update with user-provided kwargs
        config.update(kwargs)

        # Select appropriate template and environment
        if script_type == "sh":
            template = self.sh_env.get_template("base.sh.j2")
        elif script_type == "ps1":
            template = self.ps1_env.get_template("base.ps1.j2")
        else:
            raise ValueError(f"Unknown script type: {script_type}")

        # Render and write
        content = template.render(**config)
        output_path.write_text(content)

        # Make executable if it's a shell script
        if script_type == "sh":
            output_path.chmod(output_path.stat().st_mode | 0o111)

    def xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_25(
        self,
        project_name: str,
        output_path: Path,
        script_type: str = "sh",
        **kwargs: Any,
    ) -> None:
        """Generate an environment setup script.

        Args:
            project_name: Name of the project
            output_path: Path to write the script
            script_type: Either "sh" for bash or "ps1" for PowerShell
            **kwargs: Additional template variables
        """
        # Default configuration
        config = {
            "project_name": project_name,
            "env_profile_var": f"{project_name.upper().replace('-', '_')}_PROFILE",
            "venv_prefix": project_name.lower(),
            "use_spinner": script_type == "sh",  # PowerShell doesn't need spinner
            "strict_project_check": True,
            "install_siblings": True,
            "sibling_patterns": [],  # No default peers - specified in config
            "special_siblings": [],  # No default peers - specified in config
            "create_log_dir": True,
            "deduplicate_path": True,
            "include_tool_verification": True,  # Enable for wrknv
            "cleanup_logs": True,
            "tools_to_verify": [
                {
                    "name": "Python",
                    "command": "python",
                    "check_type": "command",
                    "var_name": "PYTHON",
                    "version_cmd": "python --version 2>&1",
                },
                {
                    "name": "UV",
                    "command": "uv",
                    "check_type": "command",
                    "var_name": "UV",
                    "version_cmd": "uv --version 2>&1",
                },
                {
                    "name": "wrknv",
                    "command": "wrknv",
                    "check_type": "command",
                    "var_name": "WRKENV",
                    "version_cmd": "wrknv --version 2>&1 || echo 'No version info'",
                },
                {
                    "name": "ibmtf",
                    "command": "ibmtf",
                    "check_type": "command",
                    "var_name": "IBMTF",
                    "version_cmd": "ibmtf version 2>&1 | head -1 || echo 'Not installed'",
                },
                {
                    "name": "tofu",
                    "command": "tofu",
                    "check_type": "command",
                    "var_name": "TOFU",
                    "version_cmd": "tofu version 2>&1 | head -1 || echo 'Not installed'",
                },
            ],
            "useful_commands": [
                {
                    "command": f"{project_name.lower()} --help",
                    "description": f"{project_name} CLI",
                },
                {"command": "wrknv status", "description": "Check tool versions"},
                {
                    "command": "wrknv container status",
                    "description": "Container status",
                },
                {"command": "pytest", "description": "Run tests"},
                {"command": "deactivate", "description": "Exit environment"},
            ],
        }

        # Update with user-provided kwargs
        config.update(kwargs)

        # Select appropriate template and environment
        if script_type == "sh":
            template = self.sh_env.get_template("base.sh.j2")
        elif script_type == "ps1":
            template = self.ps1_env.get_template("base.ps1.j2")
        else:
            raise ValueError(f"Unknown script type: {script_type}")

        # Render and write
        content = template.render(**config)
        output_path.write_text(content)

        # Make executable if it's a shell script
        if script_type == "sh":
            output_path.chmod(output_path.stat().st_mode | 0o111)

    def xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_26(
        self,
        project_name: str,
        output_path: Path,
        script_type: str = "sh",
        **kwargs: Any,
    ) -> None:
        """Generate an environment setup script.

        Args:
            project_name: Name of the project
            output_path: Path to write the script
            script_type: Either "sh" for bash or "ps1" for PowerShell
            **kwargs: Additional template variables
        """
        # Default configuration
        config = {
            "project_name": project_name,
            "env_profile_var": f"{project_name.upper().replace('-', '_')}_PROFILE",
            "venv_prefix": project_name.lower(),
            "use_spinner": script_type == "sh",  # PowerShell doesn't need spinner
            "strict_project_check": False,
            "XXinstall_siblingsXX": True,
            "sibling_patterns": [],  # No default peers - specified in config
            "special_siblings": [],  # No default peers - specified in config
            "create_log_dir": True,
            "deduplicate_path": True,
            "include_tool_verification": True,  # Enable for wrknv
            "cleanup_logs": True,
            "tools_to_verify": [
                {
                    "name": "Python",
                    "command": "python",
                    "check_type": "command",
                    "var_name": "PYTHON",
                    "version_cmd": "python --version 2>&1",
                },
                {
                    "name": "UV",
                    "command": "uv",
                    "check_type": "command",
                    "var_name": "UV",
                    "version_cmd": "uv --version 2>&1",
                },
                {
                    "name": "wrknv",
                    "command": "wrknv",
                    "check_type": "command",
                    "var_name": "WRKENV",
                    "version_cmd": "wrknv --version 2>&1 || echo 'No version info'",
                },
                {
                    "name": "ibmtf",
                    "command": "ibmtf",
                    "check_type": "command",
                    "var_name": "IBMTF",
                    "version_cmd": "ibmtf version 2>&1 | head -1 || echo 'Not installed'",
                },
                {
                    "name": "tofu",
                    "command": "tofu",
                    "check_type": "command",
                    "var_name": "TOFU",
                    "version_cmd": "tofu version 2>&1 | head -1 || echo 'Not installed'",
                },
            ],
            "useful_commands": [
                {
                    "command": f"{project_name.lower()} --help",
                    "description": f"{project_name} CLI",
                },
                {"command": "wrknv status", "description": "Check tool versions"},
                {
                    "command": "wrknv container status",
                    "description": "Container status",
                },
                {"command": "pytest", "description": "Run tests"},
                {"command": "deactivate", "description": "Exit environment"},
            ],
        }

        # Update with user-provided kwargs
        config.update(kwargs)

        # Select appropriate template and environment
        if script_type == "sh":
            template = self.sh_env.get_template("base.sh.j2")
        elif script_type == "ps1":
            template = self.ps1_env.get_template("base.ps1.j2")
        else:
            raise ValueError(f"Unknown script type: {script_type}")

        # Render and write
        content = template.render(**config)
        output_path.write_text(content)

        # Make executable if it's a shell script
        if script_type == "sh":
            output_path.chmod(output_path.stat().st_mode | 0o111)

    def xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_27(
        self,
        project_name: str,
        output_path: Path,
        script_type: str = "sh",
        **kwargs: Any,
    ) -> None:
        """Generate an environment setup script.

        Args:
            project_name: Name of the project
            output_path: Path to write the script
            script_type: Either "sh" for bash or "ps1" for PowerShell
            **kwargs: Additional template variables
        """
        # Default configuration
        config = {
            "project_name": project_name,
            "env_profile_var": f"{project_name.upper().replace('-', '_')}_PROFILE",
            "venv_prefix": project_name.lower(),
            "use_spinner": script_type == "sh",  # PowerShell doesn't need spinner
            "strict_project_check": False,
            "INSTALL_SIBLINGS": True,
            "sibling_patterns": [],  # No default peers - specified in config
            "special_siblings": [],  # No default peers - specified in config
            "create_log_dir": True,
            "deduplicate_path": True,
            "include_tool_verification": True,  # Enable for wrknv
            "cleanup_logs": True,
            "tools_to_verify": [
                {
                    "name": "Python",
                    "command": "python",
                    "check_type": "command",
                    "var_name": "PYTHON",
                    "version_cmd": "python --version 2>&1",
                },
                {
                    "name": "UV",
                    "command": "uv",
                    "check_type": "command",
                    "var_name": "UV",
                    "version_cmd": "uv --version 2>&1",
                },
                {
                    "name": "wrknv",
                    "command": "wrknv",
                    "check_type": "command",
                    "var_name": "WRKENV",
                    "version_cmd": "wrknv --version 2>&1 || echo 'No version info'",
                },
                {
                    "name": "ibmtf",
                    "command": "ibmtf",
                    "check_type": "command",
                    "var_name": "IBMTF",
                    "version_cmd": "ibmtf version 2>&1 | head -1 || echo 'Not installed'",
                },
                {
                    "name": "tofu",
                    "command": "tofu",
                    "check_type": "command",
                    "var_name": "TOFU",
                    "version_cmd": "tofu version 2>&1 | head -1 || echo 'Not installed'",
                },
            ],
            "useful_commands": [
                {
                    "command": f"{project_name.lower()} --help",
                    "description": f"{project_name} CLI",
                },
                {"command": "wrknv status", "description": "Check tool versions"},
                {
                    "command": "wrknv container status",
                    "description": "Container status",
                },
                {"command": "pytest", "description": "Run tests"},
                {"command": "deactivate", "description": "Exit environment"},
            ],
        }

        # Update with user-provided kwargs
        config.update(kwargs)

        # Select appropriate template and environment
        if script_type == "sh":
            template = self.sh_env.get_template("base.sh.j2")
        elif script_type == "ps1":
            template = self.ps1_env.get_template("base.ps1.j2")
        else:
            raise ValueError(f"Unknown script type: {script_type}")

        # Render and write
        content = template.render(**config)
        output_path.write_text(content)

        # Make executable if it's a shell script
        if script_type == "sh":
            output_path.chmod(output_path.stat().st_mode | 0o111)

    def xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_28(
        self,
        project_name: str,
        output_path: Path,
        script_type: str = "sh",
        **kwargs: Any,
    ) -> None:
        """Generate an environment setup script.

        Args:
            project_name: Name of the project
            output_path: Path to write the script
            script_type: Either "sh" for bash or "ps1" for PowerShell
            **kwargs: Additional template variables
        """
        # Default configuration
        config = {
            "project_name": project_name,
            "env_profile_var": f"{project_name.upper().replace('-', '_')}_PROFILE",
            "venv_prefix": project_name.lower(),
            "use_spinner": script_type == "sh",  # PowerShell doesn't need spinner
            "strict_project_check": False,
            "install_siblings": False,
            "sibling_patterns": [],  # No default peers - specified in config
            "special_siblings": [],  # No default peers - specified in config
            "create_log_dir": True,
            "deduplicate_path": True,
            "include_tool_verification": True,  # Enable for wrknv
            "cleanup_logs": True,
            "tools_to_verify": [
                {
                    "name": "Python",
                    "command": "python",
                    "check_type": "command",
                    "var_name": "PYTHON",
                    "version_cmd": "python --version 2>&1",
                },
                {
                    "name": "UV",
                    "command": "uv",
                    "check_type": "command",
                    "var_name": "UV",
                    "version_cmd": "uv --version 2>&1",
                },
                {
                    "name": "wrknv",
                    "command": "wrknv",
                    "check_type": "command",
                    "var_name": "WRKENV",
                    "version_cmd": "wrknv --version 2>&1 || echo 'No version info'",
                },
                {
                    "name": "ibmtf",
                    "command": "ibmtf",
                    "check_type": "command",
                    "var_name": "IBMTF",
                    "version_cmd": "ibmtf version 2>&1 | head -1 || echo 'Not installed'",
                },
                {
                    "name": "tofu",
                    "command": "tofu",
                    "check_type": "command",
                    "var_name": "TOFU",
                    "version_cmd": "tofu version 2>&1 | head -1 || echo 'Not installed'",
                },
            ],
            "useful_commands": [
                {
                    "command": f"{project_name.lower()} --help",
                    "description": f"{project_name} CLI",
                },
                {"command": "wrknv status", "description": "Check tool versions"},
                {
                    "command": "wrknv container status",
                    "description": "Container status",
                },
                {"command": "pytest", "description": "Run tests"},
                {"command": "deactivate", "description": "Exit environment"},
            ],
        }

        # Update with user-provided kwargs
        config.update(kwargs)

        # Select appropriate template and environment
        if script_type == "sh":
            template = self.sh_env.get_template("base.sh.j2")
        elif script_type == "ps1":
            template = self.ps1_env.get_template("base.ps1.j2")
        else:
            raise ValueError(f"Unknown script type: {script_type}")

        # Render and write
        content = template.render(**config)
        output_path.write_text(content)

        # Make executable if it's a shell script
        if script_type == "sh":
            output_path.chmod(output_path.stat().st_mode | 0o111)

    def xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_29(
        self,
        project_name: str,
        output_path: Path,
        script_type: str = "sh",
        **kwargs: Any,
    ) -> None:
        """Generate an environment setup script.

        Args:
            project_name: Name of the project
            output_path: Path to write the script
            script_type: Either "sh" for bash or "ps1" for PowerShell
            **kwargs: Additional template variables
        """
        # Default configuration
        config = {
            "project_name": project_name,
            "env_profile_var": f"{project_name.upper().replace('-', '_')}_PROFILE",
            "venv_prefix": project_name.lower(),
            "use_spinner": script_type == "sh",  # PowerShell doesn't need spinner
            "strict_project_check": False,
            "install_siblings": True,
            "XXsibling_patternsXX": [],  # No default peers - specified in config
            "special_siblings": [],  # No default peers - specified in config
            "create_log_dir": True,
            "deduplicate_path": True,
            "include_tool_verification": True,  # Enable for wrknv
            "cleanup_logs": True,
            "tools_to_verify": [
                {
                    "name": "Python",
                    "command": "python",
                    "check_type": "command",
                    "var_name": "PYTHON",
                    "version_cmd": "python --version 2>&1",
                },
                {
                    "name": "UV",
                    "command": "uv",
                    "check_type": "command",
                    "var_name": "UV",
                    "version_cmd": "uv --version 2>&1",
                },
                {
                    "name": "wrknv",
                    "command": "wrknv",
                    "check_type": "command",
                    "var_name": "WRKENV",
                    "version_cmd": "wrknv --version 2>&1 || echo 'No version info'",
                },
                {
                    "name": "ibmtf",
                    "command": "ibmtf",
                    "check_type": "command",
                    "var_name": "IBMTF",
                    "version_cmd": "ibmtf version 2>&1 | head -1 || echo 'Not installed'",
                },
                {
                    "name": "tofu",
                    "command": "tofu",
                    "check_type": "command",
                    "var_name": "TOFU",
                    "version_cmd": "tofu version 2>&1 | head -1 || echo 'Not installed'",
                },
            ],
            "useful_commands": [
                {
                    "command": f"{project_name.lower()} --help",
                    "description": f"{project_name} CLI",
                },
                {"command": "wrknv status", "description": "Check tool versions"},
                {
                    "command": "wrknv container status",
                    "description": "Container status",
                },
                {"command": "pytest", "description": "Run tests"},
                {"command": "deactivate", "description": "Exit environment"},
            ],
        }

        # Update with user-provided kwargs
        config.update(kwargs)

        # Select appropriate template and environment
        if script_type == "sh":
            template = self.sh_env.get_template("base.sh.j2")
        elif script_type == "ps1":
            template = self.ps1_env.get_template("base.ps1.j2")
        else:
            raise ValueError(f"Unknown script type: {script_type}")

        # Render and write
        content = template.render(**config)
        output_path.write_text(content)

        # Make executable if it's a shell script
        if script_type == "sh":
            output_path.chmod(output_path.stat().st_mode | 0o111)

    def xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_30(
        self,
        project_name: str,
        output_path: Path,
        script_type: str = "sh",
        **kwargs: Any,
    ) -> None:
        """Generate an environment setup script.

        Args:
            project_name: Name of the project
            output_path: Path to write the script
            script_type: Either "sh" for bash or "ps1" for PowerShell
            **kwargs: Additional template variables
        """
        # Default configuration
        config = {
            "project_name": project_name,
            "env_profile_var": f"{project_name.upper().replace('-', '_')}_PROFILE",
            "venv_prefix": project_name.lower(),
            "use_spinner": script_type == "sh",  # PowerShell doesn't need spinner
            "strict_project_check": False,
            "install_siblings": True,
            "SIBLING_PATTERNS": [],  # No default peers - specified in config
            "special_siblings": [],  # No default peers - specified in config
            "create_log_dir": True,
            "deduplicate_path": True,
            "include_tool_verification": True,  # Enable for wrknv
            "cleanup_logs": True,
            "tools_to_verify": [
                {
                    "name": "Python",
                    "command": "python",
                    "check_type": "command",
                    "var_name": "PYTHON",
                    "version_cmd": "python --version 2>&1",
                },
                {
                    "name": "UV",
                    "command": "uv",
                    "check_type": "command",
                    "var_name": "UV",
                    "version_cmd": "uv --version 2>&1",
                },
                {
                    "name": "wrknv",
                    "command": "wrknv",
                    "check_type": "command",
                    "var_name": "WRKENV",
                    "version_cmd": "wrknv --version 2>&1 || echo 'No version info'",
                },
                {
                    "name": "ibmtf",
                    "command": "ibmtf",
                    "check_type": "command",
                    "var_name": "IBMTF",
                    "version_cmd": "ibmtf version 2>&1 | head -1 || echo 'Not installed'",
                },
                {
                    "name": "tofu",
                    "command": "tofu",
                    "check_type": "command",
                    "var_name": "TOFU",
                    "version_cmd": "tofu version 2>&1 | head -1 || echo 'Not installed'",
                },
            ],
            "useful_commands": [
                {
                    "command": f"{project_name.lower()} --help",
                    "description": f"{project_name} CLI",
                },
                {"command": "wrknv status", "description": "Check tool versions"},
                {
                    "command": "wrknv container status",
                    "description": "Container status",
                },
                {"command": "pytest", "description": "Run tests"},
                {"command": "deactivate", "description": "Exit environment"},
            ],
        }

        # Update with user-provided kwargs
        config.update(kwargs)

        # Select appropriate template and environment
        if script_type == "sh":
            template = self.sh_env.get_template("base.sh.j2")
        elif script_type == "ps1":
            template = self.ps1_env.get_template("base.ps1.j2")
        else:
            raise ValueError(f"Unknown script type: {script_type}")

        # Render and write
        content = template.render(**config)
        output_path.write_text(content)

        # Make executable if it's a shell script
        if script_type == "sh":
            output_path.chmod(output_path.stat().st_mode | 0o111)

    def xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_31(
        self,
        project_name: str,
        output_path: Path,
        script_type: str = "sh",
        **kwargs: Any,
    ) -> None:
        """Generate an environment setup script.

        Args:
            project_name: Name of the project
            output_path: Path to write the script
            script_type: Either "sh" for bash or "ps1" for PowerShell
            **kwargs: Additional template variables
        """
        # Default configuration
        config = {
            "project_name": project_name,
            "env_profile_var": f"{project_name.upper().replace('-', '_')}_PROFILE",
            "venv_prefix": project_name.lower(),
            "use_spinner": script_type == "sh",  # PowerShell doesn't need spinner
            "strict_project_check": False,
            "install_siblings": True,
            "sibling_patterns": [],  # No default peers - specified in config
            "XXspecial_siblingsXX": [],  # No default peers - specified in config
            "create_log_dir": True,
            "deduplicate_path": True,
            "include_tool_verification": True,  # Enable for wrknv
            "cleanup_logs": True,
            "tools_to_verify": [
                {
                    "name": "Python",
                    "command": "python",
                    "check_type": "command",
                    "var_name": "PYTHON",
                    "version_cmd": "python --version 2>&1",
                },
                {
                    "name": "UV",
                    "command": "uv",
                    "check_type": "command",
                    "var_name": "UV",
                    "version_cmd": "uv --version 2>&1",
                },
                {
                    "name": "wrknv",
                    "command": "wrknv",
                    "check_type": "command",
                    "var_name": "WRKENV",
                    "version_cmd": "wrknv --version 2>&1 || echo 'No version info'",
                },
                {
                    "name": "ibmtf",
                    "command": "ibmtf",
                    "check_type": "command",
                    "var_name": "IBMTF",
                    "version_cmd": "ibmtf version 2>&1 | head -1 || echo 'Not installed'",
                },
                {
                    "name": "tofu",
                    "command": "tofu",
                    "check_type": "command",
                    "var_name": "TOFU",
                    "version_cmd": "tofu version 2>&1 | head -1 || echo 'Not installed'",
                },
            ],
            "useful_commands": [
                {
                    "command": f"{project_name.lower()} --help",
                    "description": f"{project_name} CLI",
                },
                {"command": "wrknv status", "description": "Check tool versions"},
                {
                    "command": "wrknv container status",
                    "description": "Container status",
                },
                {"command": "pytest", "description": "Run tests"},
                {"command": "deactivate", "description": "Exit environment"},
            ],
        }

        # Update with user-provided kwargs
        config.update(kwargs)

        # Select appropriate template and environment
        if script_type == "sh":
            template = self.sh_env.get_template("base.sh.j2")
        elif script_type == "ps1":
            template = self.ps1_env.get_template("base.ps1.j2")
        else:
            raise ValueError(f"Unknown script type: {script_type}")

        # Render and write
        content = template.render(**config)
        output_path.write_text(content)

        # Make executable if it's a shell script
        if script_type == "sh":
            output_path.chmod(output_path.stat().st_mode | 0o111)

    def xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_32(
        self,
        project_name: str,
        output_path: Path,
        script_type: str = "sh",
        **kwargs: Any,
    ) -> None:
        """Generate an environment setup script.

        Args:
            project_name: Name of the project
            output_path: Path to write the script
            script_type: Either "sh" for bash or "ps1" for PowerShell
            **kwargs: Additional template variables
        """
        # Default configuration
        config = {
            "project_name": project_name,
            "env_profile_var": f"{project_name.upper().replace('-', '_')}_PROFILE",
            "venv_prefix": project_name.lower(),
            "use_spinner": script_type == "sh",  # PowerShell doesn't need spinner
            "strict_project_check": False,
            "install_siblings": True,
            "sibling_patterns": [],  # No default peers - specified in config
            "SPECIAL_SIBLINGS": [],  # No default peers - specified in config
            "create_log_dir": True,
            "deduplicate_path": True,
            "include_tool_verification": True,  # Enable for wrknv
            "cleanup_logs": True,
            "tools_to_verify": [
                {
                    "name": "Python",
                    "command": "python",
                    "check_type": "command",
                    "var_name": "PYTHON",
                    "version_cmd": "python --version 2>&1",
                },
                {
                    "name": "UV",
                    "command": "uv",
                    "check_type": "command",
                    "var_name": "UV",
                    "version_cmd": "uv --version 2>&1",
                },
                {
                    "name": "wrknv",
                    "command": "wrknv",
                    "check_type": "command",
                    "var_name": "WRKENV",
                    "version_cmd": "wrknv --version 2>&1 || echo 'No version info'",
                },
                {
                    "name": "ibmtf",
                    "command": "ibmtf",
                    "check_type": "command",
                    "var_name": "IBMTF",
                    "version_cmd": "ibmtf version 2>&1 | head -1 || echo 'Not installed'",
                },
                {
                    "name": "tofu",
                    "command": "tofu",
                    "check_type": "command",
                    "var_name": "TOFU",
                    "version_cmd": "tofu version 2>&1 | head -1 || echo 'Not installed'",
                },
            ],
            "useful_commands": [
                {
                    "command": f"{project_name.lower()} --help",
                    "description": f"{project_name} CLI",
                },
                {"command": "wrknv status", "description": "Check tool versions"},
                {
                    "command": "wrknv container status",
                    "description": "Container status",
                },
                {"command": "pytest", "description": "Run tests"},
                {"command": "deactivate", "description": "Exit environment"},
            ],
        }

        # Update with user-provided kwargs
        config.update(kwargs)

        # Select appropriate template and environment
        if script_type == "sh":
            template = self.sh_env.get_template("base.sh.j2")
        elif script_type == "ps1":
            template = self.ps1_env.get_template("base.ps1.j2")
        else:
            raise ValueError(f"Unknown script type: {script_type}")

        # Render and write
        content = template.render(**config)
        output_path.write_text(content)

        # Make executable if it's a shell script
        if script_type == "sh":
            output_path.chmod(output_path.stat().st_mode | 0o111)

    def xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_33(
        self,
        project_name: str,
        output_path: Path,
        script_type: str = "sh",
        **kwargs: Any,
    ) -> None:
        """Generate an environment setup script.

        Args:
            project_name: Name of the project
            output_path: Path to write the script
            script_type: Either "sh" for bash or "ps1" for PowerShell
            **kwargs: Additional template variables
        """
        # Default configuration
        config = {
            "project_name": project_name,
            "env_profile_var": f"{project_name.upper().replace('-', '_')}_PROFILE",
            "venv_prefix": project_name.lower(),
            "use_spinner": script_type == "sh",  # PowerShell doesn't need spinner
            "strict_project_check": False,
            "install_siblings": True,
            "sibling_patterns": [],  # No default peers - specified in config
            "special_siblings": [],  # No default peers - specified in config
            "XXcreate_log_dirXX": True,
            "deduplicate_path": True,
            "include_tool_verification": True,  # Enable for wrknv
            "cleanup_logs": True,
            "tools_to_verify": [
                {
                    "name": "Python",
                    "command": "python",
                    "check_type": "command",
                    "var_name": "PYTHON",
                    "version_cmd": "python --version 2>&1",
                },
                {
                    "name": "UV",
                    "command": "uv",
                    "check_type": "command",
                    "var_name": "UV",
                    "version_cmd": "uv --version 2>&1",
                },
                {
                    "name": "wrknv",
                    "command": "wrknv",
                    "check_type": "command",
                    "var_name": "WRKENV",
                    "version_cmd": "wrknv --version 2>&1 || echo 'No version info'",
                },
                {
                    "name": "ibmtf",
                    "command": "ibmtf",
                    "check_type": "command",
                    "var_name": "IBMTF",
                    "version_cmd": "ibmtf version 2>&1 | head -1 || echo 'Not installed'",
                },
                {
                    "name": "tofu",
                    "command": "tofu",
                    "check_type": "command",
                    "var_name": "TOFU",
                    "version_cmd": "tofu version 2>&1 | head -1 || echo 'Not installed'",
                },
            ],
            "useful_commands": [
                {
                    "command": f"{project_name.lower()} --help",
                    "description": f"{project_name} CLI",
                },
                {"command": "wrknv status", "description": "Check tool versions"},
                {
                    "command": "wrknv container status",
                    "description": "Container status",
                },
                {"command": "pytest", "description": "Run tests"},
                {"command": "deactivate", "description": "Exit environment"},
            ],
        }

        # Update with user-provided kwargs
        config.update(kwargs)

        # Select appropriate template and environment
        if script_type == "sh":
            template = self.sh_env.get_template("base.sh.j2")
        elif script_type == "ps1":
            template = self.ps1_env.get_template("base.ps1.j2")
        else:
            raise ValueError(f"Unknown script type: {script_type}")

        # Render and write
        content = template.render(**config)
        output_path.write_text(content)

        # Make executable if it's a shell script
        if script_type == "sh":
            output_path.chmod(output_path.stat().st_mode | 0o111)

    def xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_34(
        self,
        project_name: str,
        output_path: Path,
        script_type: str = "sh",
        **kwargs: Any,
    ) -> None:
        """Generate an environment setup script.

        Args:
            project_name: Name of the project
            output_path: Path to write the script
            script_type: Either "sh" for bash or "ps1" for PowerShell
            **kwargs: Additional template variables
        """
        # Default configuration
        config = {
            "project_name": project_name,
            "env_profile_var": f"{project_name.upper().replace('-', '_')}_PROFILE",
            "venv_prefix": project_name.lower(),
            "use_spinner": script_type == "sh",  # PowerShell doesn't need spinner
            "strict_project_check": False,
            "install_siblings": True,
            "sibling_patterns": [],  # No default peers - specified in config
            "special_siblings": [],  # No default peers - specified in config
            "CREATE_LOG_DIR": True,
            "deduplicate_path": True,
            "include_tool_verification": True,  # Enable for wrknv
            "cleanup_logs": True,
            "tools_to_verify": [
                {
                    "name": "Python",
                    "command": "python",
                    "check_type": "command",
                    "var_name": "PYTHON",
                    "version_cmd": "python --version 2>&1",
                },
                {
                    "name": "UV",
                    "command": "uv",
                    "check_type": "command",
                    "var_name": "UV",
                    "version_cmd": "uv --version 2>&1",
                },
                {
                    "name": "wrknv",
                    "command": "wrknv",
                    "check_type": "command",
                    "var_name": "WRKENV",
                    "version_cmd": "wrknv --version 2>&1 || echo 'No version info'",
                },
                {
                    "name": "ibmtf",
                    "command": "ibmtf",
                    "check_type": "command",
                    "var_name": "IBMTF",
                    "version_cmd": "ibmtf version 2>&1 | head -1 || echo 'Not installed'",
                },
                {
                    "name": "tofu",
                    "command": "tofu",
                    "check_type": "command",
                    "var_name": "TOFU",
                    "version_cmd": "tofu version 2>&1 | head -1 || echo 'Not installed'",
                },
            ],
            "useful_commands": [
                {
                    "command": f"{project_name.lower()} --help",
                    "description": f"{project_name} CLI",
                },
                {"command": "wrknv status", "description": "Check tool versions"},
                {
                    "command": "wrknv container status",
                    "description": "Container status",
                },
                {"command": "pytest", "description": "Run tests"},
                {"command": "deactivate", "description": "Exit environment"},
            ],
        }

        # Update with user-provided kwargs
        config.update(kwargs)

        # Select appropriate template and environment
        if script_type == "sh":
            template = self.sh_env.get_template("base.sh.j2")
        elif script_type == "ps1":
            template = self.ps1_env.get_template("base.ps1.j2")
        else:
            raise ValueError(f"Unknown script type: {script_type}")

        # Render and write
        content = template.render(**config)
        output_path.write_text(content)

        # Make executable if it's a shell script
        if script_type == "sh":
            output_path.chmod(output_path.stat().st_mode | 0o111)

    def xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_35(
        self,
        project_name: str,
        output_path: Path,
        script_type: str = "sh",
        **kwargs: Any,
    ) -> None:
        """Generate an environment setup script.

        Args:
            project_name: Name of the project
            output_path: Path to write the script
            script_type: Either "sh" for bash or "ps1" for PowerShell
            **kwargs: Additional template variables
        """
        # Default configuration
        config = {
            "project_name": project_name,
            "env_profile_var": f"{project_name.upper().replace('-', '_')}_PROFILE",
            "venv_prefix": project_name.lower(),
            "use_spinner": script_type == "sh",  # PowerShell doesn't need spinner
            "strict_project_check": False,
            "install_siblings": True,
            "sibling_patterns": [],  # No default peers - specified in config
            "special_siblings": [],  # No default peers - specified in config
            "create_log_dir": False,
            "deduplicate_path": True,
            "include_tool_verification": True,  # Enable for wrknv
            "cleanup_logs": True,
            "tools_to_verify": [
                {
                    "name": "Python",
                    "command": "python",
                    "check_type": "command",
                    "var_name": "PYTHON",
                    "version_cmd": "python --version 2>&1",
                },
                {
                    "name": "UV",
                    "command": "uv",
                    "check_type": "command",
                    "var_name": "UV",
                    "version_cmd": "uv --version 2>&1",
                },
                {
                    "name": "wrknv",
                    "command": "wrknv",
                    "check_type": "command",
                    "var_name": "WRKENV",
                    "version_cmd": "wrknv --version 2>&1 || echo 'No version info'",
                },
                {
                    "name": "ibmtf",
                    "command": "ibmtf",
                    "check_type": "command",
                    "var_name": "IBMTF",
                    "version_cmd": "ibmtf version 2>&1 | head -1 || echo 'Not installed'",
                },
                {
                    "name": "tofu",
                    "command": "tofu",
                    "check_type": "command",
                    "var_name": "TOFU",
                    "version_cmd": "tofu version 2>&1 | head -1 || echo 'Not installed'",
                },
            ],
            "useful_commands": [
                {
                    "command": f"{project_name.lower()} --help",
                    "description": f"{project_name} CLI",
                },
                {"command": "wrknv status", "description": "Check tool versions"},
                {
                    "command": "wrknv container status",
                    "description": "Container status",
                },
                {"command": "pytest", "description": "Run tests"},
                {"command": "deactivate", "description": "Exit environment"},
            ],
        }

        # Update with user-provided kwargs
        config.update(kwargs)

        # Select appropriate template and environment
        if script_type == "sh":
            template = self.sh_env.get_template("base.sh.j2")
        elif script_type == "ps1":
            template = self.ps1_env.get_template("base.ps1.j2")
        else:
            raise ValueError(f"Unknown script type: {script_type}")

        # Render and write
        content = template.render(**config)
        output_path.write_text(content)

        # Make executable if it's a shell script
        if script_type == "sh":
            output_path.chmod(output_path.stat().st_mode | 0o111)

    def xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_36(
        self,
        project_name: str,
        output_path: Path,
        script_type: str = "sh",
        **kwargs: Any,
    ) -> None:
        """Generate an environment setup script.

        Args:
            project_name: Name of the project
            output_path: Path to write the script
            script_type: Either "sh" for bash or "ps1" for PowerShell
            **kwargs: Additional template variables
        """
        # Default configuration
        config = {
            "project_name": project_name,
            "env_profile_var": f"{project_name.upper().replace('-', '_')}_PROFILE",
            "venv_prefix": project_name.lower(),
            "use_spinner": script_type == "sh",  # PowerShell doesn't need spinner
            "strict_project_check": False,
            "install_siblings": True,
            "sibling_patterns": [],  # No default peers - specified in config
            "special_siblings": [],  # No default peers - specified in config
            "create_log_dir": True,
            "XXdeduplicate_pathXX": True,
            "include_tool_verification": True,  # Enable for wrknv
            "cleanup_logs": True,
            "tools_to_verify": [
                {
                    "name": "Python",
                    "command": "python",
                    "check_type": "command",
                    "var_name": "PYTHON",
                    "version_cmd": "python --version 2>&1",
                },
                {
                    "name": "UV",
                    "command": "uv",
                    "check_type": "command",
                    "var_name": "UV",
                    "version_cmd": "uv --version 2>&1",
                },
                {
                    "name": "wrknv",
                    "command": "wrknv",
                    "check_type": "command",
                    "var_name": "WRKENV",
                    "version_cmd": "wrknv --version 2>&1 || echo 'No version info'",
                },
                {
                    "name": "ibmtf",
                    "command": "ibmtf",
                    "check_type": "command",
                    "var_name": "IBMTF",
                    "version_cmd": "ibmtf version 2>&1 | head -1 || echo 'Not installed'",
                },
                {
                    "name": "tofu",
                    "command": "tofu",
                    "check_type": "command",
                    "var_name": "TOFU",
                    "version_cmd": "tofu version 2>&1 | head -1 || echo 'Not installed'",
                },
            ],
            "useful_commands": [
                {
                    "command": f"{project_name.lower()} --help",
                    "description": f"{project_name} CLI",
                },
                {"command": "wrknv status", "description": "Check tool versions"},
                {
                    "command": "wrknv container status",
                    "description": "Container status",
                },
                {"command": "pytest", "description": "Run tests"},
                {"command": "deactivate", "description": "Exit environment"},
            ],
        }

        # Update with user-provided kwargs
        config.update(kwargs)

        # Select appropriate template and environment
        if script_type == "sh":
            template = self.sh_env.get_template("base.sh.j2")
        elif script_type == "ps1":
            template = self.ps1_env.get_template("base.ps1.j2")
        else:
            raise ValueError(f"Unknown script type: {script_type}")

        # Render and write
        content = template.render(**config)
        output_path.write_text(content)

        # Make executable if it's a shell script
        if script_type == "sh":
            output_path.chmod(output_path.stat().st_mode | 0o111)

    def xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_37(
        self,
        project_name: str,
        output_path: Path,
        script_type: str = "sh",
        **kwargs: Any,
    ) -> None:
        """Generate an environment setup script.

        Args:
            project_name: Name of the project
            output_path: Path to write the script
            script_type: Either "sh" for bash or "ps1" for PowerShell
            **kwargs: Additional template variables
        """
        # Default configuration
        config = {
            "project_name": project_name,
            "env_profile_var": f"{project_name.upper().replace('-', '_')}_PROFILE",
            "venv_prefix": project_name.lower(),
            "use_spinner": script_type == "sh",  # PowerShell doesn't need spinner
            "strict_project_check": False,
            "install_siblings": True,
            "sibling_patterns": [],  # No default peers - specified in config
            "special_siblings": [],  # No default peers - specified in config
            "create_log_dir": True,
            "DEDUPLICATE_PATH": True,
            "include_tool_verification": True,  # Enable for wrknv
            "cleanup_logs": True,
            "tools_to_verify": [
                {
                    "name": "Python",
                    "command": "python",
                    "check_type": "command",
                    "var_name": "PYTHON",
                    "version_cmd": "python --version 2>&1",
                },
                {
                    "name": "UV",
                    "command": "uv",
                    "check_type": "command",
                    "var_name": "UV",
                    "version_cmd": "uv --version 2>&1",
                },
                {
                    "name": "wrknv",
                    "command": "wrknv",
                    "check_type": "command",
                    "var_name": "WRKENV",
                    "version_cmd": "wrknv --version 2>&1 || echo 'No version info'",
                },
                {
                    "name": "ibmtf",
                    "command": "ibmtf",
                    "check_type": "command",
                    "var_name": "IBMTF",
                    "version_cmd": "ibmtf version 2>&1 | head -1 || echo 'Not installed'",
                },
                {
                    "name": "tofu",
                    "command": "tofu",
                    "check_type": "command",
                    "var_name": "TOFU",
                    "version_cmd": "tofu version 2>&1 | head -1 || echo 'Not installed'",
                },
            ],
            "useful_commands": [
                {
                    "command": f"{project_name.lower()} --help",
                    "description": f"{project_name} CLI",
                },
                {"command": "wrknv status", "description": "Check tool versions"},
                {
                    "command": "wrknv container status",
                    "description": "Container status",
                },
                {"command": "pytest", "description": "Run tests"},
                {"command": "deactivate", "description": "Exit environment"},
            ],
        }

        # Update with user-provided kwargs
        config.update(kwargs)

        # Select appropriate template and environment
        if script_type == "sh":
            template = self.sh_env.get_template("base.sh.j2")
        elif script_type == "ps1":
            template = self.ps1_env.get_template("base.ps1.j2")
        else:
            raise ValueError(f"Unknown script type: {script_type}")

        # Render and write
        content = template.render(**config)
        output_path.write_text(content)

        # Make executable if it's a shell script
        if script_type == "sh":
            output_path.chmod(output_path.stat().st_mode | 0o111)

    def xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_38(
        self,
        project_name: str,
        output_path: Path,
        script_type: str = "sh",
        **kwargs: Any,
    ) -> None:
        """Generate an environment setup script.

        Args:
            project_name: Name of the project
            output_path: Path to write the script
            script_type: Either "sh" for bash or "ps1" for PowerShell
            **kwargs: Additional template variables
        """
        # Default configuration
        config = {
            "project_name": project_name,
            "env_profile_var": f"{project_name.upper().replace('-', '_')}_PROFILE",
            "venv_prefix": project_name.lower(),
            "use_spinner": script_type == "sh",  # PowerShell doesn't need spinner
            "strict_project_check": False,
            "install_siblings": True,
            "sibling_patterns": [],  # No default peers - specified in config
            "special_siblings": [],  # No default peers - specified in config
            "create_log_dir": True,
            "deduplicate_path": False,
            "include_tool_verification": True,  # Enable for wrknv
            "cleanup_logs": True,
            "tools_to_verify": [
                {
                    "name": "Python",
                    "command": "python",
                    "check_type": "command",
                    "var_name": "PYTHON",
                    "version_cmd": "python --version 2>&1",
                },
                {
                    "name": "UV",
                    "command": "uv",
                    "check_type": "command",
                    "var_name": "UV",
                    "version_cmd": "uv --version 2>&1",
                },
                {
                    "name": "wrknv",
                    "command": "wrknv",
                    "check_type": "command",
                    "var_name": "WRKENV",
                    "version_cmd": "wrknv --version 2>&1 || echo 'No version info'",
                },
                {
                    "name": "ibmtf",
                    "command": "ibmtf",
                    "check_type": "command",
                    "var_name": "IBMTF",
                    "version_cmd": "ibmtf version 2>&1 | head -1 || echo 'Not installed'",
                },
                {
                    "name": "tofu",
                    "command": "tofu",
                    "check_type": "command",
                    "var_name": "TOFU",
                    "version_cmd": "tofu version 2>&1 | head -1 || echo 'Not installed'",
                },
            ],
            "useful_commands": [
                {
                    "command": f"{project_name.lower()} --help",
                    "description": f"{project_name} CLI",
                },
                {"command": "wrknv status", "description": "Check tool versions"},
                {
                    "command": "wrknv container status",
                    "description": "Container status",
                },
                {"command": "pytest", "description": "Run tests"},
                {"command": "deactivate", "description": "Exit environment"},
            ],
        }

        # Update with user-provided kwargs
        config.update(kwargs)

        # Select appropriate template and environment
        if script_type == "sh":
            template = self.sh_env.get_template("base.sh.j2")
        elif script_type == "ps1":
            template = self.ps1_env.get_template("base.ps1.j2")
        else:
            raise ValueError(f"Unknown script type: {script_type}")

        # Render and write
        content = template.render(**config)
        output_path.write_text(content)

        # Make executable if it's a shell script
        if script_type == "sh":
            output_path.chmod(output_path.stat().st_mode | 0o111)

    def xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_39(
        self,
        project_name: str,
        output_path: Path,
        script_type: str = "sh",
        **kwargs: Any,
    ) -> None:
        """Generate an environment setup script.

        Args:
            project_name: Name of the project
            output_path: Path to write the script
            script_type: Either "sh" for bash or "ps1" for PowerShell
            **kwargs: Additional template variables
        """
        # Default configuration
        config = {
            "project_name": project_name,
            "env_profile_var": f"{project_name.upper().replace('-', '_')}_PROFILE",
            "venv_prefix": project_name.lower(),
            "use_spinner": script_type == "sh",  # PowerShell doesn't need spinner
            "strict_project_check": False,
            "install_siblings": True,
            "sibling_patterns": [],  # No default peers - specified in config
            "special_siblings": [],  # No default peers - specified in config
            "create_log_dir": True,
            "deduplicate_path": True,
            "XXinclude_tool_verificationXX": True,  # Enable for wrknv
            "cleanup_logs": True,
            "tools_to_verify": [
                {
                    "name": "Python",
                    "command": "python",
                    "check_type": "command",
                    "var_name": "PYTHON",
                    "version_cmd": "python --version 2>&1",
                },
                {
                    "name": "UV",
                    "command": "uv",
                    "check_type": "command",
                    "var_name": "UV",
                    "version_cmd": "uv --version 2>&1",
                },
                {
                    "name": "wrknv",
                    "command": "wrknv",
                    "check_type": "command",
                    "var_name": "WRKENV",
                    "version_cmd": "wrknv --version 2>&1 || echo 'No version info'",
                },
                {
                    "name": "ibmtf",
                    "command": "ibmtf",
                    "check_type": "command",
                    "var_name": "IBMTF",
                    "version_cmd": "ibmtf version 2>&1 | head -1 || echo 'Not installed'",
                },
                {
                    "name": "tofu",
                    "command": "tofu",
                    "check_type": "command",
                    "var_name": "TOFU",
                    "version_cmd": "tofu version 2>&1 | head -1 || echo 'Not installed'",
                },
            ],
            "useful_commands": [
                {
                    "command": f"{project_name.lower()} --help",
                    "description": f"{project_name} CLI",
                },
                {"command": "wrknv status", "description": "Check tool versions"},
                {
                    "command": "wrknv container status",
                    "description": "Container status",
                },
                {"command": "pytest", "description": "Run tests"},
                {"command": "deactivate", "description": "Exit environment"},
            ],
        }

        # Update with user-provided kwargs
        config.update(kwargs)

        # Select appropriate template and environment
        if script_type == "sh":
            template = self.sh_env.get_template("base.sh.j2")
        elif script_type == "ps1":
            template = self.ps1_env.get_template("base.ps1.j2")
        else:
            raise ValueError(f"Unknown script type: {script_type}")

        # Render and write
        content = template.render(**config)
        output_path.write_text(content)

        # Make executable if it's a shell script
        if script_type == "sh":
            output_path.chmod(output_path.stat().st_mode | 0o111)

    def xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_40(
        self,
        project_name: str,
        output_path: Path,
        script_type: str = "sh",
        **kwargs: Any,
    ) -> None:
        """Generate an environment setup script.

        Args:
            project_name: Name of the project
            output_path: Path to write the script
            script_type: Either "sh" for bash or "ps1" for PowerShell
            **kwargs: Additional template variables
        """
        # Default configuration
        config = {
            "project_name": project_name,
            "env_profile_var": f"{project_name.upper().replace('-', '_')}_PROFILE",
            "venv_prefix": project_name.lower(),
            "use_spinner": script_type == "sh",  # PowerShell doesn't need spinner
            "strict_project_check": False,
            "install_siblings": True,
            "sibling_patterns": [],  # No default peers - specified in config
            "special_siblings": [],  # No default peers - specified in config
            "create_log_dir": True,
            "deduplicate_path": True,
            "INCLUDE_TOOL_VERIFICATION": True,  # Enable for wrknv
            "cleanup_logs": True,
            "tools_to_verify": [
                {
                    "name": "Python",
                    "command": "python",
                    "check_type": "command",
                    "var_name": "PYTHON",
                    "version_cmd": "python --version 2>&1",
                },
                {
                    "name": "UV",
                    "command": "uv",
                    "check_type": "command",
                    "var_name": "UV",
                    "version_cmd": "uv --version 2>&1",
                },
                {
                    "name": "wrknv",
                    "command": "wrknv",
                    "check_type": "command",
                    "var_name": "WRKENV",
                    "version_cmd": "wrknv --version 2>&1 || echo 'No version info'",
                },
                {
                    "name": "ibmtf",
                    "command": "ibmtf",
                    "check_type": "command",
                    "var_name": "IBMTF",
                    "version_cmd": "ibmtf version 2>&1 | head -1 || echo 'Not installed'",
                },
                {
                    "name": "tofu",
                    "command": "tofu",
                    "check_type": "command",
                    "var_name": "TOFU",
                    "version_cmd": "tofu version 2>&1 | head -1 || echo 'Not installed'",
                },
            ],
            "useful_commands": [
                {
                    "command": f"{project_name.lower()} --help",
                    "description": f"{project_name} CLI",
                },
                {"command": "wrknv status", "description": "Check tool versions"},
                {
                    "command": "wrknv container status",
                    "description": "Container status",
                },
                {"command": "pytest", "description": "Run tests"},
                {"command": "deactivate", "description": "Exit environment"},
            ],
        }

        # Update with user-provided kwargs
        config.update(kwargs)

        # Select appropriate template and environment
        if script_type == "sh":
            template = self.sh_env.get_template("base.sh.j2")
        elif script_type == "ps1":
            template = self.ps1_env.get_template("base.ps1.j2")
        else:
            raise ValueError(f"Unknown script type: {script_type}")

        # Render and write
        content = template.render(**config)
        output_path.write_text(content)

        # Make executable if it's a shell script
        if script_type == "sh":
            output_path.chmod(output_path.stat().st_mode | 0o111)

    def xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_41(
        self,
        project_name: str,
        output_path: Path,
        script_type: str = "sh",
        **kwargs: Any,
    ) -> None:
        """Generate an environment setup script.

        Args:
            project_name: Name of the project
            output_path: Path to write the script
            script_type: Either "sh" for bash or "ps1" for PowerShell
            **kwargs: Additional template variables
        """
        # Default configuration
        config = {
            "project_name": project_name,
            "env_profile_var": f"{project_name.upper().replace('-', '_')}_PROFILE",
            "venv_prefix": project_name.lower(),
            "use_spinner": script_type == "sh",  # PowerShell doesn't need spinner
            "strict_project_check": False,
            "install_siblings": True,
            "sibling_patterns": [],  # No default peers - specified in config
            "special_siblings": [],  # No default peers - specified in config
            "create_log_dir": True,
            "deduplicate_path": True,
            "include_tool_verification": False,  # Enable for wrknv
            "cleanup_logs": True,
            "tools_to_verify": [
                {
                    "name": "Python",
                    "command": "python",
                    "check_type": "command",
                    "var_name": "PYTHON",
                    "version_cmd": "python --version 2>&1",
                },
                {
                    "name": "UV",
                    "command": "uv",
                    "check_type": "command",
                    "var_name": "UV",
                    "version_cmd": "uv --version 2>&1",
                },
                {
                    "name": "wrknv",
                    "command": "wrknv",
                    "check_type": "command",
                    "var_name": "WRKENV",
                    "version_cmd": "wrknv --version 2>&1 || echo 'No version info'",
                },
                {
                    "name": "ibmtf",
                    "command": "ibmtf",
                    "check_type": "command",
                    "var_name": "IBMTF",
                    "version_cmd": "ibmtf version 2>&1 | head -1 || echo 'Not installed'",
                },
                {
                    "name": "tofu",
                    "command": "tofu",
                    "check_type": "command",
                    "var_name": "TOFU",
                    "version_cmd": "tofu version 2>&1 | head -1 || echo 'Not installed'",
                },
            ],
            "useful_commands": [
                {
                    "command": f"{project_name.lower()} --help",
                    "description": f"{project_name} CLI",
                },
                {"command": "wrknv status", "description": "Check tool versions"},
                {
                    "command": "wrknv container status",
                    "description": "Container status",
                },
                {"command": "pytest", "description": "Run tests"},
                {"command": "deactivate", "description": "Exit environment"},
            ],
        }

        # Update with user-provided kwargs
        config.update(kwargs)

        # Select appropriate template and environment
        if script_type == "sh":
            template = self.sh_env.get_template("base.sh.j2")
        elif script_type == "ps1":
            template = self.ps1_env.get_template("base.ps1.j2")
        else:
            raise ValueError(f"Unknown script type: {script_type}")

        # Render and write
        content = template.render(**config)
        output_path.write_text(content)

        # Make executable if it's a shell script
        if script_type == "sh":
            output_path.chmod(output_path.stat().st_mode | 0o111)

    def xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_42(
        self,
        project_name: str,
        output_path: Path,
        script_type: str = "sh",
        **kwargs: Any,
    ) -> None:
        """Generate an environment setup script.

        Args:
            project_name: Name of the project
            output_path: Path to write the script
            script_type: Either "sh" for bash or "ps1" for PowerShell
            **kwargs: Additional template variables
        """
        # Default configuration
        config = {
            "project_name": project_name,
            "env_profile_var": f"{project_name.upper().replace('-', '_')}_PROFILE",
            "venv_prefix": project_name.lower(),
            "use_spinner": script_type == "sh",  # PowerShell doesn't need spinner
            "strict_project_check": False,
            "install_siblings": True,
            "sibling_patterns": [],  # No default peers - specified in config
            "special_siblings": [],  # No default peers - specified in config
            "create_log_dir": True,
            "deduplicate_path": True,
            "include_tool_verification": True,  # Enable for wrknv
            "XXcleanup_logsXX": True,
            "tools_to_verify": [
                {
                    "name": "Python",
                    "command": "python",
                    "check_type": "command",
                    "var_name": "PYTHON",
                    "version_cmd": "python --version 2>&1",
                },
                {
                    "name": "UV",
                    "command": "uv",
                    "check_type": "command",
                    "var_name": "UV",
                    "version_cmd": "uv --version 2>&1",
                },
                {
                    "name": "wrknv",
                    "command": "wrknv",
                    "check_type": "command",
                    "var_name": "WRKENV",
                    "version_cmd": "wrknv --version 2>&1 || echo 'No version info'",
                },
                {
                    "name": "ibmtf",
                    "command": "ibmtf",
                    "check_type": "command",
                    "var_name": "IBMTF",
                    "version_cmd": "ibmtf version 2>&1 | head -1 || echo 'Not installed'",
                },
                {
                    "name": "tofu",
                    "command": "tofu",
                    "check_type": "command",
                    "var_name": "TOFU",
                    "version_cmd": "tofu version 2>&1 | head -1 || echo 'Not installed'",
                },
            ],
            "useful_commands": [
                {
                    "command": f"{project_name.lower()} --help",
                    "description": f"{project_name} CLI",
                },
                {"command": "wrknv status", "description": "Check tool versions"},
                {
                    "command": "wrknv container status",
                    "description": "Container status",
                },
                {"command": "pytest", "description": "Run tests"},
                {"command": "deactivate", "description": "Exit environment"},
            ],
        }

        # Update with user-provided kwargs
        config.update(kwargs)

        # Select appropriate template and environment
        if script_type == "sh":
            template = self.sh_env.get_template("base.sh.j2")
        elif script_type == "ps1":
            template = self.ps1_env.get_template("base.ps1.j2")
        else:
            raise ValueError(f"Unknown script type: {script_type}")

        # Render and write
        content = template.render(**config)
        output_path.write_text(content)

        # Make executable if it's a shell script
        if script_type == "sh":
            output_path.chmod(output_path.stat().st_mode | 0o111)

    def xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_43(
        self,
        project_name: str,
        output_path: Path,
        script_type: str = "sh",
        **kwargs: Any,
    ) -> None:
        """Generate an environment setup script.

        Args:
            project_name: Name of the project
            output_path: Path to write the script
            script_type: Either "sh" for bash or "ps1" for PowerShell
            **kwargs: Additional template variables
        """
        # Default configuration
        config = {
            "project_name": project_name,
            "env_profile_var": f"{project_name.upper().replace('-', '_')}_PROFILE",
            "venv_prefix": project_name.lower(),
            "use_spinner": script_type == "sh",  # PowerShell doesn't need spinner
            "strict_project_check": False,
            "install_siblings": True,
            "sibling_patterns": [],  # No default peers - specified in config
            "special_siblings": [],  # No default peers - specified in config
            "create_log_dir": True,
            "deduplicate_path": True,
            "include_tool_verification": True,  # Enable for wrknv
            "CLEANUP_LOGS": True,
            "tools_to_verify": [
                {
                    "name": "Python",
                    "command": "python",
                    "check_type": "command",
                    "var_name": "PYTHON",
                    "version_cmd": "python --version 2>&1",
                },
                {
                    "name": "UV",
                    "command": "uv",
                    "check_type": "command",
                    "var_name": "UV",
                    "version_cmd": "uv --version 2>&1",
                },
                {
                    "name": "wrknv",
                    "command": "wrknv",
                    "check_type": "command",
                    "var_name": "WRKENV",
                    "version_cmd": "wrknv --version 2>&1 || echo 'No version info'",
                },
                {
                    "name": "ibmtf",
                    "command": "ibmtf",
                    "check_type": "command",
                    "var_name": "IBMTF",
                    "version_cmd": "ibmtf version 2>&1 | head -1 || echo 'Not installed'",
                },
                {
                    "name": "tofu",
                    "command": "tofu",
                    "check_type": "command",
                    "var_name": "TOFU",
                    "version_cmd": "tofu version 2>&1 | head -1 || echo 'Not installed'",
                },
            ],
            "useful_commands": [
                {
                    "command": f"{project_name.lower()} --help",
                    "description": f"{project_name} CLI",
                },
                {"command": "wrknv status", "description": "Check tool versions"},
                {
                    "command": "wrknv container status",
                    "description": "Container status",
                },
                {"command": "pytest", "description": "Run tests"},
                {"command": "deactivate", "description": "Exit environment"},
            ],
        }

        # Update with user-provided kwargs
        config.update(kwargs)

        # Select appropriate template and environment
        if script_type == "sh":
            template = self.sh_env.get_template("base.sh.j2")
        elif script_type == "ps1":
            template = self.ps1_env.get_template("base.ps1.j2")
        else:
            raise ValueError(f"Unknown script type: {script_type}")

        # Render and write
        content = template.render(**config)
        output_path.write_text(content)

        # Make executable if it's a shell script
        if script_type == "sh":
            output_path.chmod(output_path.stat().st_mode | 0o111)

    def xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_44(
        self,
        project_name: str,
        output_path: Path,
        script_type: str = "sh",
        **kwargs: Any,
    ) -> None:
        """Generate an environment setup script.

        Args:
            project_name: Name of the project
            output_path: Path to write the script
            script_type: Either "sh" for bash or "ps1" for PowerShell
            **kwargs: Additional template variables
        """
        # Default configuration
        config = {
            "project_name": project_name,
            "env_profile_var": f"{project_name.upper().replace('-', '_')}_PROFILE",
            "venv_prefix": project_name.lower(),
            "use_spinner": script_type == "sh",  # PowerShell doesn't need spinner
            "strict_project_check": False,
            "install_siblings": True,
            "sibling_patterns": [],  # No default peers - specified in config
            "special_siblings": [],  # No default peers - specified in config
            "create_log_dir": True,
            "deduplicate_path": True,
            "include_tool_verification": True,  # Enable for wrknv
            "cleanup_logs": False,
            "tools_to_verify": [
                {
                    "name": "Python",
                    "command": "python",
                    "check_type": "command",
                    "var_name": "PYTHON",
                    "version_cmd": "python --version 2>&1",
                },
                {
                    "name": "UV",
                    "command": "uv",
                    "check_type": "command",
                    "var_name": "UV",
                    "version_cmd": "uv --version 2>&1",
                },
                {
                    "name": "wrknv",
                    "command": "wrknv",
                    "check_type": "command",
                    "var_name": "WRKENV",
                    "version_cmd": "wrknv --version 2>&1 || echo 'No version info'",
                },
                {
                    "name": "ibmtf",
                    "command": "ibmtf",
                    "check_type": "command",
                    "var_name": "IBMTF",
                    "version_cmd": "ibmtf version 2>&1 | head -1 || echo 'Not installed'",
                },
                {
                    "name": "tofu",
                    "command": "tofu",
                    "check_type": "command",
                    "var_name": "TOFU",
                    "version_cmd": "tofu version 2>&1 | head -1 || echo 'Not installed'",
                },
            ],
            "useful_commands": [
                {
                    "command": f"{project_name.lower()} --help",
                    "description": f"{project_name} CLI",
                },
                {"command": "wrknv status", "description": "Check tool versions"},
                {
                    "command": "wrknv container status",
                    "description": "Container status",
                },
                {"command": "pytest", "description": "Run tests"},
                {"command": "deactivate", "description": "Exit environment"},
            ],
        }

        # Update with user-provided kwargs
        config.update(kwargs)

        # Select appropriate template and environment
        if script_type == "sh":
            template = self.sh_env.get_template("base.sh.j2")
        elif script_type == "ps1":
            template = self.ps1_env.get_template("base.ps1.j2")
        else:
            raise ValueError(f"Unknown script type: {script_type}")

        # Render and write
        content = template.render(**config)
        output_path.write_text(content)

        # Make executable if it's a shell script
        if script_type == "sh":
            output_path.chmod(output_path.stat().st_mode | 0o111)

    def xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_45(
        self,
        project_name: str,
        output_path: Path,
        script_type: str = "sh",
        **kwargs: Any,
    ) -> None:
        """Generate an environment setup script.

        Args:
            project_name: Name of the project
            output_path: Path to write the script
            script_type: Either "sh" for bash or "ps1" for PowerShell
            **kwargs: Additional template variables
        """
        # Default configuration
        config = {
            "project_name": project_name,
            "env_profile_var": f"{project_name.upper().replace('-', '_')}_PROFILE",
            "venv_prefix": project_name.lower(),
            "use_spinner": script_type == "sh",  # PowerShell doesn't need spinner
            "strict_project_check": False,
            "install_siblings": True,
            "sibling_patterns": [],  # No default peers - specified in config
            "special_siblings": [],  # No default peers - specified in config
            "create_log_dir": True,
            "deduplicate_path": True,
            "include_tool_verification": True,  # Enable for wrknv
            "cleanup_logs": True,
            "XXtools_to_verifyXX": [
                {
                    "name": "Python",
                    "command": "python",
                    "check_type": "command",
                    "var_name": "PYTHON",
                    "version_cmd": "python --version 2>&1",
                },
                {
                    "name": "UV",
                    "command": "uv",
                    "check_type": "command",
                    "var_name": "UV",
                    "version_cmd": "uv --version 2>&1",
                },
                {
                    "name": "wrknv",
                    "command": "wrknv",
                    "check_type": "command",
                    "var_name": "WRKENV",
                    "version_cmd": "wrknv --version 2>&1 || echo 'No version info'",
                },
                {
                    "name": "ibmtf",
                    "command": "ibmtf",
                    "check_type": "command",
                    "var_name": "IBMTF",
                    "version_cmd": "ibmtf version 2>&1 | head -1 || echo 'Not installed'",
                },
                {
                    "name": "tofu",
                    "command": "tofu",
                    "check_type": "command",
                    "var_name": "TOFU",
                    "version_cmd": "tofu version 2>&1 | head -1 || echo 'Not installed'",
                },
            ],
            "useful_commands": [
                {
                    "command": f"{project_name.lower()} --help",
                    "description": f"{project_name} CLI",
                },
                {"command": "wrknv status", "description": "Check tool versions"},
                {
                    "command": "wrknv container status",
                    "description": "Container status",
                },
                {"command": "pytest", "description": "Run tests"},
                {"command": "deactivate", "description": "Exit environment"},
            ],
        }

        # Update with user-provided kwargs
        config.update(kwargs)

        # Select appropriate template and environment
        if script_type == "sh":
            template = self.sh_env.get_template("base.sh.j2")
        elif script_type == "ps1":
            template = self.ps1_env.get_template("base.ps1.j2")
        else:
            raise ValueError(f"Unknown script type: {script_type}")

        # Render and write
        content = template.render(**config)
        output_path.write_text(content)

        # Make executable if it's a shell script
        if script_type == "sh":
            output_path.chmod(output_path.stat().st_mode | 0o111)

    def xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_46(
        self,
        project_name: str,
        output_path: Path,
        script_type: str = "sh",
        **kwargs: Any,
    ) -> None:
        """Generate an environment setup script.

        Args:
            project_name: Name of the project
            output_path: Path to write the script
            script_type: Either "sh" for bash or "ps1" for PowerShell
            **kwargs: Additional template variables
        """
        # Default configuration
        config = {
            "project_name": project_name,
            "env_profile_var": f"{project_name.upper().replace('-', '_')}_PROFILE",
            "venv_prefix": project_name.lower(),
            "use_spinner": script_type == "sh",  # PowerShell doesn't need spinner
            "strict_project_check": False,
            "install_siblings": True,
            "sibling_patterns": [],  # No default peers - specified in config
            "special_siblings": [],  # No default peers - specified in config
            "create_log_dir": True,
            "deduplicate_path": True,
            "include_tool_verification": True,  # Enable for wrknv
            "cleanup_logs": True,
            "TOOLS_TO_VERIFY": [
                {
                    "name": "Python",
                    "command": "python",
                    "check_type": "command",
                    "var_name": "PYTHON",
                    "version_cmd": "python --version 2>&1",
                },
                {
                    "name": "UV",
                    "command": "uv",
                    "check_type": "command",
                    "var_name": "UV",
                    "version_cmd": "uv --version 2>&1",
                },
                {
                    "name": "wrknv",
                    "command": "wrknv",
                    "check_type": "command",
                    "var_name": "WRKENV",
                    "version_cmd": "wrknv --version 2>&1 || echo 'No version info'",
                },
                {
                    "name": "ibmtf",
                    "command": "ibmtf",
                    "check_type": "command",
                    "var_name": "IBMTF",
                    "version_cmd": "ibmtf version 2>&1 | head -1 || echo 'Not installed'",
                },
                {
                    "name": "tofu",
                    "command": "tofu",
                    "check_type": "command",
                    "var_name": "TOFU",
                    "version_cmd": "tofu version 2>&1 | head -1 || echo 'Not installed'",
                },
            ],
            "useful_commands": [
                {
                    "command": f"{project_name.lower()} --help",
                    "description": f"{project_name} CLI",
                },
                {"command": "wrknv status", "description": "Check tool versions"},
                {
                    "command": "wrknv container status",
                    "description": "Container status",
                },
                {"command": "pytest", "description": "Run tests"},
                {"command": "deactivate", "description": "Exit environment"},
            ],
        }

        # Update with user-provided kwargs
        config.update(kwargs)

        # Select appropriate template and environment
        if script_type == "sh":
            template = self.sh_env.get_template("base.sh.j2")
        elif script_type == "ps1":
            template = self.ps1_env.get_template("base.ps1.j2")
        else:
            raise ValueError(f"Unknown script type: {script_type}")

        # Render and write
        content = template.render(**config)
        output_path.write_text(content)

        # Make executable if it's a shell script
        if script_type == "sh":
            output_path.chmod(output_path.stat().st_mode | 0o111)

    def xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_47(
        self,
        project_name: str,
        output_path: Path,
        script_type: str = "sh",
        **kwargs: Any,
    ) -> None:
        """Generate an environment setup script.

        Args:
            project_name: Name of the project
            output_path: Path to write the script
            script_type: Either "sh" for bash or "ps1" for PowerShell
            **kwargs: Additional template variables
        """
        # Default configuration
        config = {
            "project_name": project_name,
            "env_profile_var": f"{project_name.upper().replace('-', '_')}_PROFILE",
            "venv_prefix": project_name.lower(),
            "use_spinner": script_type == "sh",  # PowerShell doesn't need spinner
            "strict_project_check": False,
            "install_siblings": True,
            "sibling_patterns": [],  # No default peers - specified in config
            "special_siblings": [],  # No default peers - specified in config
            "create_log_dir": True,
            "deduplicate_path": True,
            "include_tool_verification": True,  # Enable for wrknv
            "cleanup_logs": True,
            "tools_to_verify": [
                {
                    "XXnameXX": "Python",
                    "command": "python",
                    "check_type": "command",
                    "var_name": "PYTHON",
                    "version_cmd": "python --version 2>&1",
                },
                {
                    "name": "UV",
                    "command": "uv",
                    "check_type": "command",
                    "var_name": "UV",
                    "version_cmd": "uv --version 2>&1",
                },
                {
                    "name": "wrknv",
                    "command": "wrknv",
                    "check_type": "command",
                    "var_name": "WRKENV",
                    "version_cmd": "wrknv --version 2>&1 || echo 'No version info'",
                },
                {
                    "name": "ibmtf",
                    "command": "ibmtf",
                    "check_type": "command",
                    "var_name": "IBMTF",
                    "version_cmd": "ibmtf version 2>&1 | head -1 || echo 'Not installed'",
                },
                {
                    "name": "tofu",
                    "command": "tofu",
                    "check_type": "command",
                    "var_name": "TOFU",
                    "version_cmd": "tofu version 2>&1 | head -1 || echo 'Not installed'",
                },
            ],
            "useful_commands": [
                {
                    "command": f"{project_name.lower()} --help",
                    "description": f"{project_name} CLI",
                },
                {"command": "wrknv status", "description": "Check tool versions"},
                {
                    "command": "wrknv container status",
                    "description": "Container status",
                },
                {"command": "pytest", "description": "Run tests"},
                {"command": "deactivate", "description": "Exit environment"},
            ],
        }

        # Update with user-provided kwargs
        config.update(kwargs)

        # Select appropriate template and environment
        if script_type == "sh":
            template = self.sh_env.get_template("base.sh.j2")
        elif script_type == "ps1":
            template = self.ps1_env.get_template("base.ps1.j2")
        else:
            raise ValueError(f"Unknown script type: {script_type}")

        # Render and write
        content = template.render(**config)
        output_path.write_text(content)

        # Make executable if it's a shell script
        if script_type == "sh":
            output_path.chmod(output_path.stat().st_mode | 0o111)

    def xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_48(
        self,
        project_name: str,
        output_path: Path,
        script_type: str = "sh",
        **kwargs: Any,
    ) -> None:
        """Generate an environment setup script.

        Args:
            project_name: Name of the project
            output_path: Path to write the script
            script_type: Either "sh" for bash or "ps1" for PowerShell
            **kwargs: Additional template variables
        """
        # Default configuration
        config = {
            "project_name": project_name,
            "env_profile_var": f"{project_name.upper().replace('-', '_')}_PROFILE",
            "venv_prefix": project_name.lower(),
            "use_spinner": script_type == "sh",  # PowerShell doesn't need spinner
            "strict_project_check": False,
            "install_siblings": True,
            "sibling_patterns": [],  # No default peers - specified in config
            "special_siblings": [],  # No default peers - specified in config
            "create_log_dir": True,
            "deduplicate_path": True,
            "include_tool_verification": True,  # Enable for wrknv
            "cleanup_logs": True,
            "tools_to_verify": [
                {
                    "NAME": "Python",
                    "command": "python",
                    "check_type": "command",
                    "var_name": "PYTHON",
                    "version_cmd": "python --version 2>&1",
                },
                {
                    "name": "UV",
                    "command": "uv",
                    "check_type": "command",
                    "var_name": "UV",
                    "version_cmd": "uv --version 2>&1",
                },
                {
                    "name": "wrknv",
                    "command": "wrknv",
                    "check_type": "command",
                    "var_name": "WRKENV",
                    "version_cmd": "wrknv --version 2>&1 || echo 'No version info'",
                },
                {
                    "name": "ibmtf",
                    "command": "ibmtf",
                    "check_type": "command",
                    "var_name": "IBMTF",
                    "version_cmd": "ibmtf version 2>&1 | head -1 || echo 'Not installed'",
                },
                {
                    "name": "tofu",
                    "command": "tofu",
                    "check_type": "command",
                    "var_name": "TOFU",
                    "version_cmd": "tofu version 2>&1 | head -1 || echo 'Not installed'",
                },
            ],
            "useful_commands": [
                {
                    "command": f"{project_name.lower()} --help",
                    "description": f"{project_name} CLI",
                },
                {"command": "wrknv status", "description": "Check tool versions"},
                {
                    "command": "wrknv container status",
                    "description": "Container status",
                },
                {"command": "pytest", "description": "Run tests"},
                {"command": "deactivate", "description": "Exit environment"},
            ],
        }

        # Update with user-provided kwargs
        config.update(kwargs)

        # Select appropriate template and environment
        if script_type == "sh":
            template = self.sh_env.get_template("base.sh.j2")
        elif script_type == "ps1":
            template = self.ps1_env.get_template("base.ps1.j2")
        else:
            raise ValueError(f"Unknown script type: {script_type}")

        # Render and write
        content = template.render(**config)
        output_path.write_text(content)

        # Make executable if it's a shell script
        if script_type == "sh":
            output_path.chmod(output_path.stat().st_mode | 0o111)

    def xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_49(
        self,
        project_name: str,
        output_path: Path,
        script_type: str = "sh",
        **kwargs: Any,
    ) -> None:
        """Generate an environment setup script.

        Args:
            project_name: Name of the project
            output_path: Path to write the script
            script_type: Either "sh" for bash or "ps1" for PowerShell
            **kwargs: Additional template variables
        """
        # Default configuration
        config = {
            "project_name": project_name,
            "env_profile_var": f"{project_name.upper().replace('-', '_')}_PROFILE",
            "venv_prefix": project_name.lower(),
            "use_spinner": script_type == "sh",  # PowerShell doesn't need spinner
            "strict_project_check": False,
            "install_siblings": True,
            "sibling_patterns": [],  # No default peers - specified in config
            "special_siblings": [],  # No default peers - specified in config
            "create_log_dir": True,
            "deduplicate_path": True,
            "include_tool_verification": True,  # Enable for wrknv
            "cleanup_logs": True,
            "tools_to_verify": [
                {
                    "name": "XXPythonXX",
                    "command": "python",
                    "check_type": "command",
                    "var_name": "PYTHON",
                    "version_cmd": "python --version 2>&1",
                },
                {
                    "name": "UV",
                    "command": "uv",
                    "check_type": "command",
                    "var_name": "UV",
                    "version_cmd": "uv --version 2>&1",
                },
                {
                    "name": "wrknv",
                    "command": "wrknv",
                    "check_type": "command",
                    "var_name": "WRKENV",
                    "version_cmd": "wrknv --version 2>&1 || echo 'No version info'",
                },
                {
                    "name": "ibmtf",
                    "command": "ibmtf",
                    "check_type": "command",
                    "var_name": "IBMTF",
                    "version_cmd": "ibmtf version 2>&1 | head -1 || echo 'Not installed'",
                },
                {
                    "name": "tofu",
                    "command": "tofu",
                    "check_type": "command",
                    "var_name": "TOFU",
                    "version_cmd": "tofu version 2>&1 | head -1 || echo 'Not installed'",
                },
            ],
            "useful_commands": [
                {
                    "command": f"{project_name.lower()} --help",
                    "description": f"{project_name} CLI",
                },
                {"command": "wrknv status", "description": "Check tool versions"},
                {
                    "command": "wrknv container status",
                    "description": "Container status",
                },
                {"command": "pytest", "description": "Run tests"},
                {"command": "deactivate", "description": "Exit environment"},
            ],
        }

        # Update with user-provided kwargs
        config.update(kwargs)

        # Select appropriate template and environment
        if script_type == "sh":
            template = self.sh_env.get_template("base.sh.j2")
        elif script_type == "ps1":
            template = self.ps1_env.get_template("base.ps1.j2")
        else:
            raise ValueError(f"Unknown script type: {script_type}")

        # Render and write
        content = template.render(**config)
        output_path.write_text(content)

        # Make executable if it's a shell script
        if script_type == "sh":
            output_path.chmod(output_path.stat().st_mode | 0o111)

    def xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_50(
        self,
        project_name: str,
        output_path: Path,
        script_type: str = "sh",
        **kwargs: Any,
    ) -> None:
        """Generate an environment setup script.

        Args:
            project_name: Name of the project
            output_path: Path to write the script
            script_type: Either "sh" for bash or "ps1" for PowerShell
            **kwargs: Additional template variables
        """
        # Default configuration
        config = {
            "project_name": project_name,
            "env_profile_var": f"{project_name.upper().replace('-', '_')}_PROFILE",
            "venv_prefix": project_name.lower(),
            "use_spinner": script_type == "sh",  # PowerShell doesn't need spinner
            "strict_project_check": False,
            "install_siblings": True,
            "sibling_patterns": [],  # No default peers - specified in config
            "special_siblings": [],  # No default peers - specified in config
            "create_log_dir": True,
            "deduplicate_path": True,
            "include_tool_verification": True,  # Enable for wrknv
            "cleanup_logs": True,
            "tools_to_verify": [
                {
                    "name": "python",
                    "command": "python",
                    "check_type": "command",
                    "var_name": "PYTHON",
                    "version_cmd": "python --version 2>&1",
                },
                {
                    "name": "UV",
                    "command": "uv",
                    "check_type": "command",
                    "var_name": "UV",
                    "version_cmd": "uv --version 2>&1",
                },
                {
                    "name": "wrknv",
                    "command": "wrknv",
                    "check_type": "command",
                    "var_name": "WRKENV",
                    "version_cmd": "wrknv --version 2>&1 || echo 'No version info'",
                },
                {
                    "name": "ibmtf",
                    "command": "ibmtf",
                    "check_type": "command",
                    "var_name": "IBMTF",
                    "version_cmd": "ibmtf version 2>&1 | head -1 || echo 'Not installed'",
                },
                {
                    "name": "tofu",
                    "command": "tofu",
                    "check_type": "command",
                    "var_name": "TOFU",
                    "version_cmd": "tofu version 2>&1 | head -1 || echo 'Not installed'",
                },
            ],
            "useful_commands": [
                {
                    "command": f"{project_name.lower()} --help",
                    "description": f"{project_name} CLI",
                },
                {"command": "wrknv status", "description": "Check tool versions"},
                {
                    "command": "wrknv container status",
                    "description": "Container status",
                },
                {"command": "pytest", "description": "Run tests"},
                {"command": "deactivate", "description": "Exit environment"},
            ],
        }

        # Update with user-provided kwargs
        config.update(kwargs)

        # Select appropriate template and environment
        if script_type == "sh":
            template = self.sh_env.get_template("base.sh.j2")
        elif script_type == "ps1":
            template = self.ps1_env.get_template("base.ps1.j2")
        else:
            raise ValueError(f"Unknown script type: {script_type}")

        # Render and write
        content = template.render(**config)
        output_path.write_text(content)

        # Make executable if it's a shell script
        if script_type == "sh":
            output_path.chmod(output_path.stat().st_mode | 0o111)

    def xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_51(
        self,
        project_name: str,
        output_path: Path,
        script_type: str = "sh",
        **kwargs: Any,
    ) -> None:
        """Generate an environment setup script.

        Args:
            project_name: Name of the project
            output_path: Path to write the script
            script_type: Either "sh" for bash or "ps1" for PowerShell
            **kwargs: Additional template variables
        """
        # Default configuration
        config = {
            "project_name": project_name,
            "env_profile_var": f"{project_name.upper().replace('-', '_')}_PROFILE",
            "venv_prefix": project_name.lower(),
            "use_spinner": script_type == "sh",  # PowerShell doesn't need spinner
            "strict_project_check": False,
            "install_siblings": True,
            "sibling_patterns": [],  # No default peers - specified in config
            "special_siblings": [],  # No default peers - specified in config
            "create_log_dir": True,
            "deduplicate_path": True,
            "include_tool_verification": True,  # Enable for wrknv
            "cleanup_logs": True,
            "tools_to_verify": [
                {
                    "name": "PYTHON",
                    "command": "python",
                    "check_type": "command",
                    "var_name": "PYTHON",
                    "version_cmd": "python --version 2>&1",
                },
                {
                    "name": "UV",
                    "command": "uv",
                    "check_type": "command",
                    "var_name": "UV",
                    "version_cmd": "uv --version 2>&1",
                },
                {
                    "name": "wrknv",
                    "command": "wrknv",
                    "check_type": "command",
                    "var_name": "WRKENV",
                    "version_cmd": "wrknv --version 2>&1 || echo 'No version info'",
                },
                {
                    "name": "ibmtf",
                    "command": "ibmtf",
                    "check_type": "command",
                    "var_name": "IBMTF",
                    "version_cmd": "ibmtf version 2>&1 | head -1 || echo 'Not installed'",
                },
                {
                    "name": "tofu",
                    "command": "tofu",
                    "check_type": "command",
                    "var_name": "TOFU",
                    "version_cmd": "tofu version 2>&1 | head -1 || echo 'Not installed'",
                },
            ],
            "useful_commands": [
                {
                    "command": f"{project_name.lower()} --help",
                    "description": f"{project_name} CLI",
                },
                {"command": "wrknv status", "description": "Check tool versions"},
                {
                    "command": "wrknv container status",
                    "description": "Container status",
                },
                {"command": "pytest", "description": "Run tests"},
                {"command": "deactivate", "description": "Exit environment"},
            ],
        }

        # Update with user-provided kwargs
        config.update(kwargs)

        # Select appropriate template and environment
        if script_type == "sh":
            template = self.sh_env.get_template("base.sh.j2")
        elif script_type == "ps1":
            template = self.ps1_env.get_template("base.ps1.j2")
        else:
            raise ValueError(f"Unknown script type: {script_type}")

        # Render and write
        content = template.render(**config)
        output_path.write_text(content)

        # Make executable if it's a shell script
        if script_type == "sh":
            output_path.chmod(output_path.stat().st_mode | 0o111)

    def xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_52(
        self,
        project_name: str,
        output_path: Path,
        script_type: str = "sh",
        **kwargs: Any,
    ) -> None:
        """Generate an environment setup script.

        Args:
            project_name: Name of the project
            output_path: Path to write the script
            script_type: Either "sh" for bash or "ps1" for PowerShell
            **kwargs: Additional template variables
        """
        # Default configuration
        config = {
            "project_name": project_name,
            "env_profile_var": f"{project_name.upper().replace('-', '_')}_PROFILE",
            "venv_prefix": project_name.lower(),
            "use_spinner": script_type == "sh",  # PowerShell doesn't need spinner
            "strict_project_check": False,
            "install_siblings": True,
            "sibling_patterns": [],  # No default peers - specified in config
            "special_siblings": [],  # No default peers - specified in config
            "create_log_dir": True,
            "deduplicate_path": True,
            "include_tool_verification": True,  # Enable for wrknv
            "cleanup_logs": True,
            "tools_to_verify": [
                {
                    "name": "Python",
                    "XXcommandXX": "python",
                    "check_type": "command",
                    "var_name": "PYTHON",
                    "version_cmd": "python --version 2>&1",
                },
                {
                    "name": "UV",
                    "command": "uv",
                    "check_type": "command",
                    "var_name": "UV",
                    "version_cmd": "uv --version 2>&1",
                },
                {
                    "name": "wrknv",
                    "command": "wrknv",
                    "check_type": "command",
                    "var_name": "WRKENV",
                    "version_cmd": "wrknv --version 2>&1 || echo 'No version info'",
                },
                {
                    "name": "ibmtf",
                    "command": "ibmtf",
                    "check_type": "command",
                    "var_name": "IBMTF",
                    "version_cmd": "ibmtf version 2>&1 | head -1 || echo 'Not installed'",
                },
                {
                    "name": "tofu",
                    "command": "tofu",
                    "check_type": "command",
                    "var_name": "TOFU",
                    "version_cmd": "tofu version 2>&1 | head -1 || echo 'Not installed'",
                },
            ],
            "useful_commands": [
                {
                    "command": f"{project_name.lower()} --help",
                    "description": f"{project_name} CLI",
                },
                {"command": "wrknv status", "description": "Check tool versions"},
                {
                    "command": "wrknv container status",
                    "description": "Container status",
                },
                {"command": "pytest", "description": "Run tests"},
                {"command": "deactivate", "description": "Exit environment"},
            ],
        }

        # Update with user-provided kwargs
        config.update(kwargs)

        # Select appropriate template and environment
        if script_type == "sh":
            template = self.sh_env.get_template("base.sh.j2")
        elif script_type == "ps1":
            template = self.ps1_env.get_template("base.ps1.j2")
        else:
            raise ValueError(f"Unknown script type: {script_type}")

        # Render and write
        content = template.render(**config)
        output_path.write_text(content)

        # Make executable if it's a shell script
        if script_type == "sh":
            output_path.chmod(output_path.stat().st_mode | 0o111)

    def xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_53(
        self,
        project_name: str,
        output_path: Path,
        script_type: str = "sh",
        **kwargs: Any,
    ) -> None:
        """Generate an environment setup script.

        Args:
            project_name: Name of the project
            output_path: Path to write the script
            script_type: Either "sh" for bash or "ps1" for PowerShell
            **kwargs: Additional template variables
        """
        # Default configuration
        config = {
            "project_name": project_name,
            "env_profile_var": f"{project_name.upper().replace('-', '_')}_PROFILE",
            "venv_prefix": project_name.lower(),
            "use_spinner": script_type == "sh",  # PowerShell doesn't need spinner
            "strict_project_check": False,
            "install_siblings": True,
            "sibling_patterns": [],  # No default peers - specified in config
            "special_siblings": [],  # No default peers - specified in config
            "create_log_dir": True,
            "deduplicate_path": True,
            "include_tool_verification": True,  # Enable for wrknv
            "cleanup_logs": True,
            "tools_to_verify": [
                {
                    "name": "Python",
                    "COMMAND": "python",
                    "check_type": "command",
                    "var_name": "PYTHON",
                    "version_cmd": "python --version 2>&1",
                },
                {
                    "name": "UV",
                    "command": "uv",
                    "check_type": "command",
                    "var_name": "UV",
                    "version_cmd": "uv --version 2>&1",
                },
                {
                    "name": "wrknv",
                    "command": "wrknv",
                    "check_type": "command",
                    "var_name": "WRKENV",
                    "version_cmd": "wrknv --version 2>&1 || echo 'No version info'",
                },
                {
                    "name": "ibmtf",
                    "command": "ibmtf",
                    "check_type": "command",
                    "var_name": "IBMTF",
                    "version_cmd": "ibmtf version 2>&1 | head -1 || echo 'Not installed'",
                },
                {
                    "name": "tofu",
                    "command": "tofu",
                    "check_type": "command",
                    "var_name": "TOFU",
                    "version_cmd": "tofu version 2>&1 | head -1 || echo 'Not installed'",
                },
            ],
            "useful_commands": [
                {
                    "command": f"{project_name.lower()} --help",
                    "description": f"{project_name} CLI",
                },
                {"command": "wrknv status", "description": "Check tool versions"},
                {
                    "command": "wrknv container status",
                    "description": "Container status",
                },
                {"command": "pytest", "description": "Run tests"},
                {"command": "deactivate", "description": "Exit environment"},
            ],
        }

        # Update with user-provided kwargs
        config.update(kwargs)

        # Select appropriate template and environment
        if script_type == "sh":
            template = self.sh_env.get_template("base.sh.j2")
        elif script_type == "ps1":
            template = self.ps1_env.get_template("base.ps1.j2")
        else:
            raise ValueError(f"Unknown script type: {script_type}")

        # Render and write
        content = template.render(**config)
        output_path.write_text(content)

        # Make executable if it's a shell script
        if script_type == "sh":
            output_path.chmod(output_path.stat().st_mode | 0o111)

    def xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_54(
        self,
        project_name: str,
        output_path: Path,
        script_type: str = "sh",
        **kwargs: Any,
    ) -> None:
        """Generate an environment setup script.

        Args:
            project_name: Name of the project
            output_path: Path to write the script
            script_type: Either "sh" for bash or "ps1" for PowerShell
            **kwargs: Additional template variables
        """
        # Default configuration
        config = {
            "project_name": project_name,
            "env_profile_var": f"{project_name.upper().replace('-', '_')}_PROFILE",
            "venv_prefix": project_name.lower(),
            "use_spinner": script_type == "sh",  # PowerShell doesn't need spinner
            "strict_project_check": False,
            "install_siblings": True,
            "sibling_patterns": [],  # No default peers - specified in config
            "special_siblings": [],  # No default peers - specified in config
            "create_log_dir": True,
            "deduplicate_path": True,
            "include_tool_verification": True,  # Enable for wrknv
            "cleanup_logs": True,
            "tools_to_verify": [
                {
                    "name": "Python",
                    "command": "XXpythonXX",
                    "check_type": "command",
                    "var_name": "PYTHON",
                    "version_cmd": "python --version 2>&1",
                },
                {
                    "name": "UV",
                    "command": "uv",
                    "check_type": "command",
                    "var_name": "UV",
                    "version_cmd": "uv --version 2>&1",
                },
                {
                    "name": "wrknv",
                    "command": "wrknv",
                    "check_type": "command",
                    "var_name": "WRKENV",
                    "version_cmd": "wrknv --version 2>&1 || echo 'No version info'",
                },
                {
                    "name": "ibmtf",
                    "command": "ibmtf",
                    "check_type": "command",
                    "var_name": "IBMTF",
                    "version_cmd": "ibmtf version 2>&1 | head -1 || echo 'Not installed'",
                },
                {
                    "name": "tofu",
                    "command": "tofu",
                    "check_type": "command",
                    "var_name": "TOFU",
                    "version_cmd": "tofu version 2>&1 | head -1 || echo 'Not installed'",
                },
            ],
            "useful_commands": [
                {
                    "command": f"{project_name.lower()} --help",
                    "description": f"{project_name} CLI",
                },
                {"command": "wrknv status", "description": "Check tool versions"},
                {
                    "command": "wrknv container status",
                    "description": "Container status",
                },
                {"command": "pytest", "description": "Run tests"},
                {"command": "deactivate", "description": "Exit environment"},
            ],
        }

        # Update with user-provided kwargs
        config.update(kwargs)

        # Select appropriate template and environment
        if script_type == "sh":
            template = self.sh_env.get_template("base.sh.j2")
        elif script_type == "ps1":
            template = self.ps1_env.get_template("base.ps1.j2")
        else:
            raise ValueError(f"Unknown script type: {script_type}")

        # Render and write
        content = template.render(**config)
        output_path.write_text(content)

        # Make executable if it's a shell script
        if script_type == "sh":
            output_path.chmod(output_path.stat().st_mode | 0o111)

    def xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_55(
        self,
        project_name: str,
        output_path: Path,
        script_type: str = "sh",
        **kwargs: Any,
    ) -> None:
        """Generate an environment setup script.

        Args:
            project_name: Name of the project
            output_path: Path to write the script
            script_type: Either "sh" for bash or "ps1" for PowerShell
            **kwargs: Additional template variables
        """
        # Default configuration
        config = {
            "project_name": project_name,
            "env_profile_var": f"{project_name.upper().replace('-', '_')}_PROFILE",
            "venv_prefix": project_name.lower(),
            "use_spinner": script_type == "sh",  # PowerShell doesn't need spinner
            "strict_project_check": False,
            "install_siblings": True,
            "sibling_patterns": [],  # No default peers - specified in config
            "special_siblings": [],  # No default peers - specified in config
            "create_log_dir": True,
            "deduplicate_path": True,
            "include_tool_verification": True,  # Enable for wrknv
            "cleanup_logs": True,
            "tools_to_verify": [
                {
                    "name": "Python",
                    "command": "PYTHON",
                    "check_type": "command",
                    "var_name": "PYTHON",
                    "version_cmd": "python --version 2>&1",
                },
                {
                    "name": "UV",
                    "command": "uv",
                    "check_type": "command",
                    "var_name": "UV",
                    "version_cmd": "uv --version 2>&1",
                },
                {
                    "name": "wrknv",
                    "command": "wrknv",
                    "check_type": "command",
                    "var_name": "WRKENV",
                    "version_cmd": "wrknv --version 2>&1 || echo 'No version info'",
                },
                {
                    "name": "ibmtf",
                    "command": "ibmtf",
                    "check_type": "command",
                    "var_name": "IBMTF",
                    "version_cmd": "ibmtf version 2>&1 | head -1 || echo 'Not installed'",
                },
                {
                    "name": "tofu",
                    "command": "tofu",
                    "check_type": "command",
                    "var_name": "TOFU",
                    "version_cmd": "tofu version 2>&1 | head -1 || echo 'Not installed'",
                },
            ],
            "useful_commands": [
                {
                    "command": f"{project_name.lower()} --help",
                    "description": f"{project_name} CLI",
                },
                {"command": "wrknv status", "description": "Check tool versions"},
                {
                    "command": "wrknv container status",
                    "description": "Container status",
                },
                {"command": "pytest", "description": "Run tests"},
                {"command": "deactivate", "description": "Exit environment"},
            ],
        }

        # Update with user-provided kwargs
        config.update(kwargs)

        # Select appropriate template and environment
        if script_type == "sh":
            template = self.sh_env.get_template("base.sh.j2")
        elif script_type == "ps1":
            template = self.ps1_env.get_template("base.ps1.j2")
        else:
            raise ValueError(f"Unknown script type: {script_type}")

        # Render and write
        content = template.render(**config)
        output_path.write_text(content)

        # Make executable if it's a shell script
        if script_type == "sh":
            output_path.chmod(output_path.stat().st_mode | 0o111)

    def xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_56(
        self,
        project_name: str,
        output_path: Path,
        script_type: str = "sh",
        **kwargs: Any,
    ) -> None:
        """Generate an environment setup script.

        Args:
            project_name: Name of the project
            output_path: Path to write the script
            script_type: Either "sh" for bash or "ps1" for PowerShell
            **kwargs: Additional template variables
        """
        # Default configuration
        config = {
            "project_name": project_name,
            "env_profile_var": f"{project_name.upper().replace('-', '_')}_PROFILE",
            "venv_prefix": project_name.lower(),
            "use_spinner": script_type == "sh",  # PowerShell doesn't need spinner
            "strict_project_check": False,
            "install_siblings": True,
            "sibling_patterns": [],  # No default peers - specified in config
            "special_siblings": [],  # No default peers - specified in config
            "create_log_dir": True,
            "deduplicate_path": True,
            "include_tool_verification": True,  # Enable for wrknv
            "cleanup_logs": True,
            "tools_to_verify": [
                {
                    "name": "Python",
                    "command": "python",
                    "XXcheck_typeXX": "command",
                    "var_name": "PYTHON",
                    "version_cmd": "python --version 2>&1",
                },
                {
                    "name": "UV",
                    "command": "uv",
                    "check_type": "command",
                    "var_name": "UV",
                    "version_cmd": "uv --version 2>&1",
                },
                {
                    "name": "wrknv",
                    "command": "wrknv",
                    "check_type": "command",
                    "var_name": "WRKENV",
                    "version_cmd": "wrknv --version 2>&1 || echo 'No version info'",
                },
                {
                    "name": "ibmtf",
                    "command": "ibmtf",
                    "check_type": "command",
                    "var_name": "IBMTF",
                    "version_cmd": "ibmtf version 2>&1 | head -1 || echo 'Not installed'",
                },
                {
                    "name": "tofu",
                    "command": "tofu",
                    "check_type": "command",
                    "var_name": "TOFU",
                    "version_cmd": "tofu version 2>&1 | head -1 || echo 'Not installed'",
                },
            ],
            "useful_commands": [
                {
                    "command": f"{project_name.lower()} --help",
                    "description": f"{project_name} CLI",
                },
                {"command": "wrknv status", "description": "Check tool versions"},
                {
                    "command": "wrknv container status",
                    "description": "Container status",
                },
                {"command": "pytest", "description": "Run tests"},
                {"command": "deactivate", "description": "Exit environment"},
            ],
        }

        # Update with user-provided kwargs
        config.update(kwargs)

        # Select appropriate template and environment
        if script_type == "sh":
            template = self.sh_env.get_template("base.sh.j2")
        elif script_type == "ps1":
            template = self.ps1_env.get_template("base.ps1.j2")
        else:
            raise ValueError(f"Unknown script type: {script_type}")

        # Render and write
        content = template.render(**config)
        output_path.write_text(content)

        # Make executable if it's a shell script
        if script_type == "sh":
            output_path.chmod(output_path.stat().st_mode | 0o111)

    def xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_57(
        self,
        project_name: str,
        output_path: Path,
        script_type: str = "sh",
        **kwargs: Any,
    ) -> None:
        """Generate an environment setup script.

        Args:
            project_name: Name of the project
            output_path: Path to write the script
            script_type: Either "sh" for bash or "ps1" for PowerShell
            **kwargs: Additional template variables
        """
        # Default configuration
        config = {
            "project_name": project_name,
            "env_profile_var": f"{project_name.upper().replace('-', '_')}_PROFILE",
            "venv_prefix": project_name.lower(),
            "use_spinner": script_type == "sh",  # PowerShell doesn't need spinner
            "strict_project_check": False,
            "install_siblings": True,
            "sibling_patterns": [],  # No default peers - specified in config
            "special_siblings": [],  # No default peers - specified in config
            "create_log_dir": True,
            "deduplicate_path": True,
            "include_tool_verification": True,  # Enable for wrknv
            "cleanup_logs": True,
            "tools_to_verify": [
                {
                    "name": "Python",
                    "command": "python",
                    "CHECK_TYPE": "command",
                    "var_name": "PYTHON",
                    "version_cmd": "python --version 2>&1",
                },
                {
                    "name": "UV",
                    "command": "uv",
                    "check_type": "command",
                    "var_name": "UV",
                    "version_cmd": "uv --version 2>&1",
                },
                {
                    "name": "wrknv",
                    "command": "wrknv",
                    "check_type": "command",
                    "var_name": "WRKENV",
                    "version_cmd": "wrknv --version 2>&1 || echo 'No version info'",
                },
                {
                    "name": "ibmtf",
                    "command": "ibmtf",
                    "check_type": "command",
                    "var_name": "IBMTF",
                    "version_cmd": "ibmtf version 2>&1 | head -1 || echo 'Not installed'",
                },
                {
                    "name": "tofu",
                    "command": "tofu",
                    "check_type": "command",
                    "var_name": "TOFU",
                    "version_cmd": "tofu version 2>&1 | head -1 || echo 'Not installed'",
                },
            ],
            "useful_commands": [
                {
                    "command": f"{project_name.lower()} --help",
                    "description": f"{project_name} CLI",
                },
                {"command": "wrknv status", "description": "Check tool versions"},
                {
                    "command": "wrknv container status",
                    "description": "Container status",
                },
                {"command": "pytest", "description": "Run tests"},
                {"command": "deactivate", "description": "Exit environment"},
            ],
        }

        # Update with user-provided kwargs
        config.update(kwargs)

        # Select appropriate template and environment
        if script_type == "sh":
            template = self.sh_env.get_template("base.sh.j2")
        elif script_type == "ps1":
            template = self.ps1_env.get_template("base.ps1.j2")
        else:
            raise ValueError(f"Unknown script type: {script_type}")

        # Render and write
        content = template.render(**config)
        output_path.write_text(content)

        # Make executable if it's a shell script
        if script_type == "sh":
            output_path.chmod(output_path.stat().st_mode | 0o111)

    def xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_58(
        self,
        project_name: str,
        output_path: Path,
        script_type: str = "sh",
        **kwargs: Any,
    ) -> None:
        """Generate an environment setup script.

        Args:
            project_name: Name of the project
            output_path: Path to write the script
            script_type: Either "sh" for bash or "ps1" for PowerShell
            **kwargs: Additional template variables
        """
        # Default configuration
        config = {
            "project_name": project_name,
            "env_profile_var": f"{project_name.upper().replace('-', '_')}_PROFILE",
            "venv_prefix": project_name.lower(),
            "use_spinner": script_type == "sh",  # PowerShell doesn't need spinner
            "strict_project_check": False,
            "install_siblings": True,
            "sibling_patterns": [],  # No default peers - specified in config
            "special_siblings": [],  # No default peers - specified in config
            "create_log_dir": True,
            "deduplicate_path": True,
            "include_tool_verification": True,  # Enable for wrknv
            "cleanup_logs": True,
            "tools_to_verify": [
                {
                    "name": "Python",
                    "command": "python",
                    "check_type": "XXcommandXX",
                    "var_name": "PYTHON",
                    "version_cmd": "python --version 2>&1",
                },
                {
                    "name": "UV",
                    "command": "uv",
                    "check_type": "command",
                    "var_name": "UV",
                    "version_cmd": "uv --version 2>&1",
                },
                {
                    "name": "wrknv",
                    "command": "wrknv",
                    "check_type": "command",
                    "var_name": "WRKENV",
                    "version_cmd": "wrknv --version 2>&1 || echo 'No version info'",
                },
                {
                    "name": "ibmtf",
                    "command": "ibmtf",
                    "check_type": "command",
                    "var_name": "IBMTF",
                    "version_cmd": "ibmtf version 2>&1 | head -1 || echo 'Not installed'",
                },
                {
                    "name": "tofu",
                    "command": "tofu",
                    "check_type": "command",
                    "var_name": "TOFU",
                    "version_cmd": "tofu version 2>&1 | head -1 || echo 'Not installed'",
                },
            ],
            "useful_commands": [
                {
                    "command": f"{project_name.lower()} --help",
                    "description": f"{project_name} CLI",
                },
                {"command": "wrknv status", "description": "Check tool versions"},
                {
                    "command": "wrknv container status",
                    "description": "Container status",
                },
                {"command": "pytest", "description": "Run tests"},
                {"command": "deactivate", "description": "Exit environment"},
            ],
        }

        # Update with user-provided kwargs
        config.update(kwargs)

        # Select appropriate template and environment
        if script_type == "sh":
            template = self.sh_env.get_template("base.sh.j2")
        elif script_type == "ps1":
            template = self.ps1_env.get_template("base.ps1.j2")
        else:
            raise ValueError(f"Unknown script type: {script_type}")

        # Render and write
        content = template.render(**config)
        output_path.write_text(content)

        # Make executable if it's a shell script
        if script_type == "sh":
            output_path.chmod(output_path.stat().st_mode | 0o111)

    def xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_59(
        self,
        project_name: str,
        output_path: Path,
        script_type: str = "sh",
        **kwargs: Any,
    ) -> None:
        """Generate an environment setup script.

        Args:
            project_name: Name of the project
            output_path: Path to write the script
            script_type: Either "sh" for bash or "ps1" for PowerShell
            **kwargs: Additional template variables
        """
        # Default configuration
        config = {
            "project_name": project_name,
            "env_profile_var": f"{project_name.upper().replace('-', '_')}_PROFILE",
            "venv_prefix": project_name.lower(),
            "use_spinner": script_type == "sh",  # PowerShell doesn't need spinner
            "strict_project_check": False,
            "install_siblings": True,
            "sibling_patterns": [],  # No default peers - specified in config
            "special_siblings": [],  # No default peers - specified in config
            "create_log_dir": True,
            "deduplicate_path": True,
            "include_tool_verification": True,  # Enable for wrknv
            "cleanup_logs": True,
            "tools_to_verify": [
                {
                    "name": "Python",
                    "command": "python",
                    "check_type": "COMMAND",
                    "var_name": "PYTHON",
                    "version_cmd": "python --version 2>&1",
                },
                {
                    "name": "UV",
                    "command": "uv",
                    "check_type": "command",
                    "var_name": "UV",
                    "version_cmd": "uv --version 2>&1",
                },
                {
                    "name": "wrknv",
                    "command": "wrknv",
                    "check_type": "command",
                    "var_name": "WRKENV",
                    "version_cmd": "wrknv --version 2>&1 || echo 'No version info'",
                },
                {
                    "name": "ibmtf",
                    "command": "ibmtf",
                    "check_type": "command",
                    "var_name": "IBMTF",
                    "version_cmd": "ibmtf version 2>&1 | head -1 || echo 'Not installed'",
                },
                {
                    "name": "tofu",
                    "command": "tofu",
                    "check_type": "command",
                    "var_name": "TOFU",
                    "version_cmd": "tofu version 2>&1 | head -1 || echo 'Not installed'",
                },
            ],
            "useful_commands": [
                {
                    "command": f"{project_name.lower()} --help",
                    "description": f"{project_name} CLI",
                },
                {"command": "wrknv status", "description": "Check tool versions"},
                {
                    "command": "wrknv container status",
                    "description": "Container status",
                },
                {"command": "pytest", "description": "Run tests"},
                {"command": "deactivate", "description": "Exit environment"},
            ],
        }

        # Update with user-provided kwargs
        config.update(kwargs)

        # Select appropriate template and environment
        if script_type == "sh":
            template = self.sh_env.get_template("base.sh.j2")
        elif script_type == "ps1":
            template = self.ps1_env.get_template("base.ps1.j2")
        else:
            raise ValueError(f"Unknown script type: {script_type}")

        # Render and write
        content = template.render(**config)
        output_path.write_text(content)

        # Make executable if it's a shell script
        if script_type == "sh":
            output_path.chmod(output_path.stat().st_mode | 0o111)

    def xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_60(
        self,
        project_name: str,
        output_path: Path,
        script_type: str = "sh",
        **kwargs: Any,
    ) -> None:
        """Generate an environment setup script.

        Args:
            project_name: Name of the project
            output_path: Path to write the script
            script_type: Either "sh" for bash or "ps1" for PowerShell
            **kwargs: Additional template variables
        """
        # Default configuration
        config = {
            "project_name": project_name,
            "env_profile_var": f"{project_name.upper().replace('-', '_')}_PROFILE",
            "venv_prefix": project_name.lower(),
            "use_spinner": script_type == "sh",  # PowerShell doesn't need spinner
            "strict_project_check": False,
            "install_siblings": True,
            "sibling_patterns": [],  # No default peers - specified in config
            "special_siblings": [],  # No default peers - specified in config
            "create_log_dir": True,
            "deduplicate_path": True,
            "include_tool_verification": True,  # Enable for wrknv
            "cleanup_logs": True,
            "tools_to_verify": [
                {
                    "name": "Python",
                    "command": "python",
                    "check_type": "command",
                    "XXvar_nameXX": "PYTHON",
                    "version_cmd": "python --version 2>&1",
                },
                {
                    "name": "UV",
                    "command": "uv",
                    "check_type": "command",
                    "var_name": "UV",
                    "version_cmd": "uv --version 2>&1",
                },
                {
                    "name": "wrknv",
                    "command": "wrknv",
                    "check_type": "command",
                    "var_name": "WRKENV",
                    "version_cmd": "wrknv --version 2>&1 || echo 'No version info'",
                },
                {
                    "name": "ibmtf",
                    "command": "ibmtf",
                    "check_type": "command",
                    "var_name": "IBMTF",
                    "version_cmd": "ibmtf version 2>&1 | head -1 || echo 'Not installed'",
                },
                {
                    "name": "tofu",
                    "command": "tofu",
                    "check_type": "command",
                    "var_name": "TOFU",
                    "version_cmd": "tofu version 2>&1 | head -1 || echo 'Not installed'",
                },
            ],
            "useful_commands": [
                {
                    "command": f"{project_name.lower()} --help",
                    "description": f"{project_name} CLI",
                },
                {"command": "wrknv status", "description": "Check tool versions"},
                {
                    "command": "wrknv container status",
                    "description": "Container status",
                },
                {"command": "pytest", "description": "Run tests"},
                {"command": "deactivate", "description": "Exit environment"},
            ],
        }

        # Update with user-provided kwargs
        config.update(kwargs)

        # Select appropriate template and environment
        if script_type == "sh":
            template = self.sh_env.get_template("base.sh.j2")
        elif script_type == "ps1":
            template = self.ps1_env.get_template("base.ps1.j2")
        else:
            raise ValueError(f"Unknown script type: {script_type}")

        # Render and write
        content = template.render(**config)
        output_path.write_text(content)

        # Make executable if it's a shell script
        if script_type == "sh":
            output_path.chmod(output_path.stat().st_mode | 0o111)

    def xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_61(
        self,
        project_name: str,
        output_path: Path,
        script_type: str = "sh",
        **kwargs: Any,
    ) -> None:
        """Generate an environment setup script.

        Args:
            project_name: Name of the project
            output_path: Path to write the script
            script_type: Either "sh" for bash or "ps1" for PowerShell
            **kwargs: Additional template variables
        """
        # Default configuration
        config = {
            "project_name": project_name,
            "env_profile_var": f"{project_name.upper().replace('-', '_')}_PROFILE",
            "venv_prefix": project_name.lower(),
            "use_spinner": script_type == "sh",  # PowerShell doesn't need spinner
            "strict_project_check": False,
            "install_siblings": True,
            "sibling_patterns": [],  # No default peers - specified in config
            "special_siblings": [],  # No default peers - specified in config
            "create_log_dir": True,
            "deduplicate_path": True,
            "include_tool_verification": True,  # Enable for wrknv
            "cleanup_logs": True,
            "tools_to_verify": [
                {
                    "name": "Python",
                    "command": "python",
                    "check_type": "command",
                    "VAR_NAME": "PYTHON",
                    "version_cmd": "python --version 2>&1",
                },
                {
                    "name": "UV",
                    "command": "uv",
                    "check_type": "command",
                    "var_name": "UV",
                    "version_cmd": "uv --version 2>&1",
                },
                {
                    "name": "wrknv",
                    "command": "wrknv",
                    "check_type": "command",
                    "var_name": "WRKENV",
                    "version_cmd": "wrknv --version 2>&1 || echo 'No version info'",
                },
                {
                    "name": "ibmtf",
                    "command": "ibmtf",
                    "check_type": "command",
                    "var_name": "IBMTF",
                    "version_cmd": "ibmtf version 2>&1 | head -1 || echo 'Not installed'",
                },
                {
                    "name": "tofu",
                    "command": "tofu",
                    "check_type": "command",
                    "var_name": "TOFU",
                    "version_cmd": "tofu version 2>&1 | head -1 || echo 'Not installed'",
                },
            ],
            "useful_commands": [
                {
                    "command": f"{project_name.lower()} --help",
                    "description": f"{project_name} CLI",
                },
                {"command": "wrknv status", "description": "Check tool versions"},
                {
                    "command": "wrknv container status",
                    "description": "Container status",
                },
                {"command": "pytest", "description": "Run tests"},
                {"command": "deactivate", "description": "Exit environment"},
            ],
        }

        # Update with user-provided kwargs
        config.update(kwargs)

        # Select appropriate template and environment
        if script_type == "sh":
            template = self.sh_env.get_template("base.sh.j2")
        elif script_type == "ps1":
            template = self.ps1_env.get_template("base.ps1.j2")
        else:
            raise ValueError(f"Unknown script type: {script_type}")

        # Render and write
        content = template.render(**config)
        output_path.write_text(content)

        # Make executable if it's a shell script
        if script_type == "sh":
            output_path.chmod(output_path.stat().st_mode | 0o111)

    def xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_62(
        self,
        project_name: str,
        output_path: Path,
        script_type: str = "sh",
        **kwargs: Any,
    ) -> None:
        """Generate an environment setup script.

        Args:
            project_name: Name of the project
            output_path: Path to write the script
            script_type: Either "sh" for bash or "ps1" for PowerShell
            **kwargs: Additional template variables
        """
        # Default configuration
        config = {
            "project_name": project_name,
            "env_profile_var": f"{project_name.upper().replace('-', '_')}_PROFILE",
            "venv_prefix": project_name.lower(),
            "use_spinner": script_type == "sh",  # PowerShell doesn't need spinner
            "strict_project_check": False,
            "install_siblings": True,
            "sibling_patterns": [],  # No default peers - specified in config
            "special_siblings": [],  # No default peers - specified in config
            "create_log_dir": True,
            "deduplicate_path": True,
            "include_tool_verification": True,  # Enable for wrknv
            "cleanup_logs": True,
            "tools_to_verify": [
                {
                    "name": "Python",
                    "command": "python",
                    "check_type": "command",
                    "var_name": "XXPYTHONXX",
                    "version_cmd": "python --version 2>&1",
                },
                {
                    "name": "UV",
                    "command": "uv",
                    "check_type": "command",
                    "var_name": "UV",
                    "version_cmd": "uv --version 2>&1",
                },
                {
                    "name": "wrknv",
                    "command": "wrknv",
                    "check_type": "command",
                    "var_name": "WRKENV",
                    "version_cmd": "wrknv --version 2>&1 || echo 'No version info'",
                },
                {
                    "name": "ibmtf",
                    "command": "ibmtf",
                    "check_type": "command",
                    "var_name": "IBMTF",
                    "version_cmd": "ibmtf version 2>&1 | head -1 || echo 'Not installed'",
                },
                {
                    "name": "tofu",
                    "command": "tofu",
                    "check_type": "command",
                    "var_name": "TOFU",
                    "version_cmd": "tofu version 2>&1 | head -1 || echo 'Not installed'",
                },
            ],
            "useful_commands": [
                {
                    "command": f"{project_name.lower()} --help",
                    "description": f"{project_name} CLI",
                },
                {"command": "wrknv status", "description": "Check tool versions"},
                {
                    "command": "wrknv container status",
                    "description": "Container status",
                },
                {"command": "pytest", "description": "Run tests"},
                {"command": "deactivate", "description": "Exit environment"},
            ],
        }

        # Update with user-provided kwargs
        config.update(kwargs)

        # Select appropriate template and environment
        if script_type == "sh":
            template = self.sh_env.get_template("base.sh.j2")
        elif script_type == "ps1":
            template = self.ps1_env.get_template("base.ps1.j2")
        else:
            raise ValueError(f"Unknown script type: {script_type}")

        # Render and write
        content = template.render(**config)
        output_path.write_text(content)

        # Make executable if it's a shell script
        if script_type == "sh":
            output_path.chmod(output_path.stat().st_mode | 0o111)

    def xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_63(
        self,
        project_name: str,
        output_path: Path,
        script_type: str = "sh",
        **kwargs: Any,
    ) -> None:
        """Generate an environment setup script.

        Args:
            project_name: Name of the project
            output_path: Path to write the script
            script_type: Either "sh" for bash or "ps1" for PowerShell
            **kwargs: Additional template variables
        """
        # Default configuration
        config = {
            "project_name": project_name,
            "env_profile_var": f"{project_name.upper().replace('-', '_')}_PROFILE",
            "venv_prefix": project_name.lower(),
            "use_spinner": script_type == "sh",  # PowerShell doesn't need spinner
            "strict_project_check": False,
            "install_siblings": True,
            "sibling_patterns": [],  # No default peers - specified in config
            "special_siblings": [],  # No default peers - specified in config
            "create_log_dir": True,
            "deduplicate_path": True,
            "include_tool_verification": True,  # Enable for wrknv
            "cleanup_logs": True,
            "tools_to_verify": [
                {
                    "name": "Python",
                    "command": "python",
                    "check_type": "command",
                    "var_name": "python",
                    "version_cmd": "python --version 2>&1",
                },
                {
                    "name": "UV",
                    "command": "uv",
                    "check_type": "command",
                    "var_name": "UV",
                    "version_cmd": "uv --version 2>&1",
                },
                {
                    "name": "wrknv",
                    "command": "wrknv",
                    "check_type": "command",
                    "var_name": "WRKENV",
                    "version_cmd": "wrknv --version 2>&1 || echo 'No version info'",
                },
                {
                    "name": "ibmtf",
                    "command": "ibmtf",
                    "check_type": "command",
                    "var_name": "IBMTF",
                    "version_cmd": "ibmtf version 2>&1 | head -1 || echo 'Not installed'",
                },
                {
                    "name": "tofu",
                    "command": "tofu",
                    "check_type": "command",
                    "var_name": "TOFU",
                    "version_cmd": "tofu version 2>&1 | head -1 || echo 'Not installed'",
                },
            ],
            "useful_commands": [
                {
                    "command": f"{project_name.lower()} --help",
                    "description": f"{project_name} CLI",
                },
                {"command": "wrknv status", "description": "Check tool versions"},
                {
                    "command": "wrknv container status",
                    "description": "Container status",
                },
                {"command": "pytest", "description": "Run tests"},
                {"command": "deactivate", "description": "Exit environment"},
            ],
        }

        # Update with user-provided kwargs
        config.update(kwargs)

        # Select appropriate template and environment
        if script_type == "sh":
            template = self.sh_env.get_template("base.sh.j2")
        elif script_type == "ps1":
            template = self.ps1_env.get_template("base.ps1.j2")
        else:
            raise ValueError(f"Unknown script type: {script_type}")

        # Render and write
        content = template.render(**config)
        output_path.write_text(content)

        # Make executable if it's a shell script
        if script_type == "sh":
            output_path.chmod(output_path.stat().st_mode | 0o111)

    def xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_64(
        self,
        project_name: str,
        output_path: Path,
        script_type: str = "sh",
        **kwargs: Any,
    ) -> None:
        """Generate an environment setup script.

        Args:
            project_name: Name of the project
            output_path: Path to write the script
            script_type: Either "sh" for bash or "ps1" for PowerShell
            **kwargs: Additional template variables
        """
        # Default configuration
        config = {
            "project_name": project_name,
            "env_profile_var": f"{project_name.upper().replace('-', '_')}_PROFILE",
            "venv_prefix": project_name.lower(),
            "use_spinner": script_type == "sh",  # PowerShell doesn't need spinner
            "strict_project_check": False,
            "install_siblings": True,
            "sibling_patterns": [],  # No default peers - specified in config
            "special_siblings": [],  # No default peers - specified in config
            "create_log_dir": True,
            "deduplicate_path": True,
            "include_tool_verification": True,  # Enable for wrknv
            "cleanup_logs": True,
            "tools_to_verify": [
                {
                    "name": "Python",
                    "command": "python",
                    "check_type": "command",
                    "var_name": "PYTHON",
                    "XXversion_cmdXX": "python --version 2>&1",
                },
                {
                    "name": "UV",
                    "command": "uv",
                    "check_type": "command",
                    "var_name": "UV",
                    "version_cmd": "uv --version 2>&1",
                },
                {
                    "name": "wrknv",
                    "command": "wrknv",
                    "check_type": "command",
                    "var_name": "WRKENV",
                    "version_cmd": "wrknv --version 2>&1 || echo 'No version info'",
                },
                {
                    "name": "ibmtf",
                    "command": "ibmtf",
                    "check_type": "command",
                    "var_name": "IBMTF",
                    "version_cmd": "ibmtf version 2>&1 | head -1 || echo 'Not installed'",
                },
                {
                    "name": "tofu",
                    "command": "tofu",
                    "check_type": "command",
                    "var_name": "TOFU",
                    "version_cmd": "tofu version 2>&1 | head -1 || echo 'Not installed'",
                },
            ],
            "useful_commands": [
                {
                    "command": f"{project_name.lower()} --help",
                    "description": f"{project_name} CLI",
                },
                {"command": "wrknv status", "description": "Check tool versions"},
                {
                    "command": "wrknv container status",
                    "description": "Container status",
                },
                {"command": "pytest", "description": "Run tests"},
                {"command": "deactivate", "description": "Exit environment"},
            ],
        }

        # Update with user-provided kwargs
        config.update(kwargs)

        # Select appropriate template and environment
        if script_type == "sh":
            template = self.sh_env.get_template("base.sh.j2")
        elif script_type == "ps1":
            template = self.ps1_env.get_template("base.ps1.j2")
        else:
            raise ValueError(f"Unknown script type: {script_type}")

        # Render and write
        content = template.render(**config)
        output_path.write_text(content)

        # Make executable if it's a shell script
        if script_type == "sh":
            output_path.chmod(output_path.stat().st_mode | 0o111)

    def xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_65(
        self,
        project_name: str,
        output_path: Path,
        script_type: str = "sh",
        **kwargs: Any,
    ) -> None:
        """Generate an environment setup script.

        Args:
            project_name: Name of the project
            output_path: Path to write the script
            script_type: Either "sh" for bash or "ps1" for PowerShell
            **kwargs: Additional template variables
        """
        # Default configuration
        config = {
            "project_name": project_name,
            "env_profile_var": f"{project_name.upper().replace('-', '_')}_PROFILE",
            "venv_prefix": project_name.lower(),
            "use_spinner": script_type == "sh",  # PowerShell doesn't need spinner
            "strict_project_check": False,
            "install_siblings": True,
            "sibling_patterns": [],  # No default peers - specified in config
            "special_siblings": [],  # No default peers - specified in config
            "create_log_dir": True,
            "deduplicate_path": True,
            "include_tool_verification": True,  # Enable for wrknv
            "cleanup_logs": True,
            "tools_to_verify": [
                {
                    "name": "Python",
                    "command": "python",
                    "check_type": "command",
                    "var_name": "PYTHON",
                    "VERSION_CMD": "python --version 2>&1",
                },
                {
                    "name": "UV",
                    "command": "uv",
                    "check_type": "command",
                    "var_name": "UV",
                    "version_cmd": "uv --version 2>&1",
                },
                {
                    "name": "wrknv",
                    "command": "wrknv",
                    "check_type": "command",
                    "var_name": "WRKENV",
                    "version_cmd": "wrknv --version 2>&1 || echo 'No version info'",
                },
                {
                    "name": "ibmtf",
                    "command": "ibmtf",
                    "check_type": "command",
                    "var_name": "IBMTF",
                    "version_cmd": "ibmtf version 2>&1 | head -1 || echo 'Not installed'",
                },
                {
                    "name": "tofu",
                    "command": "tofu",
                    "check_type": "command",
                    "var_name": "TOFU",
                    "version_cmd": "tofu version 2>&1 | head -1 || echo 'Not installed'",
                },
            ],
            "useful_commands": [
                {
                    "command": f"{project_name.lower()} --help",
                    "description": f"{project_name} CLI",
                },
                {"command": "wrknv status", "description": "Check tool versions"},
                {
                    "command": "wrknv container status",
                    "description": "Container status",
                },
                {"command": "pytest", "description": "Run tests"},
                {"command": "deactivate", "description": "Exit environment"},
            ],
        }

        # Update with user-provided kwargs
        config.update(kwargs)

        # Select appropriate template and environment
        if script_type == "sh":
            template = self.sh_env.get_template("base.sh.j2")
        elif script_type == "ps1":
            template = self.ps1_env.get_template("base.ps1.j2")
        else:
            raise ValueError(f"Unknown script type: {script_type}")

        # Render and write
        content = template.render(**config)
        output_path.write_text(content)

        # Make executable if it's a shell script
        if script_type == "sh":
            output_path.chmod(output_path.stat().st_mode | 0o111)

    def xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_66(
        self,
        project_name: str,
        output_path: Path,
        script_type: str = "sh",
        **kwargs: Any,
    ) -> None:
        """Generate an environment setup script.

        Args:
            project_name: Name of the project
            output_path: Path to write the script
            script_type: Either "sh" for bash or "ps1" for PowerShell
            **kwargs: Additional template variables
        """
        # Default configuration
        config = {
            "project_name": project_name,
            "env_profile_var": f"{project_name.upper().replace('-', '_')}_PROFILE",
            "venv_prefix": project_name.lower(),
            "use_spinner": script_type == "sh",  # PowerShell doesn't need spinner
            "strict_project_check": False,
            "install_siblings": True,
            "sibling_patterns": [],  # No default peers - specified in config
            "special_siblings": [],  # No default peers - specified in config
            "create_log_dir": True,
            "deduplicate_path": True,
            "include_tool_verification": True,  # Enable for wrknv
            "cleanup_logs": True,
            "tools_to_verify": [
                {
                    "name": "Python",
                    "command": "python",
                    "check_type": "command",
                    "var_name": "PYTHON",
                    "version_cmd": "XXpython --version 2>&1XX",
                },
                {
                    "name": "UV",
                    "command": "uv",
                    "check_type": "command",
                    "var_name": "UV",
                    "version_cmd": "uv --version 2>&1",
                },
                {
                    "name": "wrknv",
                    "command": "wrknv",
                    "check_type": "command",
                    "var_name": "WRKENV",
                    "version_cmd": "wrknv --version 2>&1 || echo 'No version info'",
                },
                {
                    "name": "ibmtf",
                    "command": "ibmtf",
                    "check_type": "command",
                    "var_name": "IBMTF",
                    "version_cmd": "ibmtf version 2>&1 | head -1 || echo 'Not installed'",
                },
                {
                    "name": "tofu",
                    "command": "tofu",
                    "check_type": "command",
                    "var_name": "TOFU",
                    "version_cmd": "tofu version 2>&1 | head -1 || echo 'Not installed'",
                },
            ],
            "useful_commands": [
                {
                    "command": f"{project_name.lower()} --help",
                    "description": f"{project_name} CLI",
                },
                {"command": "wrknv status", "description": "Check tool versions"},
                {
                    "command": "wrknv container status",
                    "description": "Container status",
                },
                {"command": "pytest", "description": "Run tests"},
                {"command": "deactivate", "description": "Exit environment"},
            ],
        }

        # Update with user-provided kwargs
        config.update(kwargs)

        # Select appropriate template and environment
        if script_type == "sh":
            template = self.sh_env.get_template("base.sh.j2")
        elif script_type == "ps1":
            template = self.ps1_env.get_template("base.ps1.j2")
        else:
            raise ValueError(f"Unknown script type: {script_type}")

        # Render and write
        content = template.render(**config)
        output_path.write_text(content)

        # Make executable if it's a shell script
        if script_type == "sh":
            output_path.chmod(output_path.stat().st_mode | 0o111)

    def xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_67(
        self,
        project_name: str,
        output_path: Path,
        script_type: str = "sh",
        **kwargs: Any,
    ) -> None:
        """Generate an environment setup script.

        Args:
            project_name: Name of the project
            output_path: Path to write the script
            script_type: Either "sh" for bash or "ps1" for PowerShell
            **kwargs: Additional template variables
        """
        # Default configuration
        config = {
            "project_name": project_name,
            "env_profile_var": f"{project_name.upper().replace('-', '_')}_PROFILE",
            "venv_prefix": project_name.lower(),
            "use_spinner": script_type == "sh",  # PowerShell doesn't need spinner
            "strict_project_check": False,
            "install_siblings": True,
            "sibling_patterns": [],  # No default peers - specified in config
            "special_siblings": [],  # No default peers - specified in config
            "create_log_dir": True,
            "deduplicate_path": True,
            "include_tool_verification": True,  # Enable for wrknv
            "cleanup_logs": True,
            "tools_to_verify": [
                {
                    "name": "Python",
                    "command": "python",
                    "check_type": "command",
                    "var_name": "PYTHON",
                    "version_cmd": "PYTHON --VERSION 2>&1",
                },
                {
                    "name": "UV",
                    "command": "uv",
                    "check_type": "command",
                    "var_name": "UV",
                    "version_cmd": "uv --version 2>&1",
                },
                {
                    "name": "wrknv",
                    "command": "wrknv",
                    "check_type": "command",
                    "var_name": "WRKENV",
                    "version_cmd": "wrknv --version 2>&1 || echo 'No version info'",
                },
                {
                    "name": "ibmtf",
                    "command": "ibmtf",
                    "check_type": "command",
                    "var_name": "IBMTF",
                    "version_cmd": "ibmtf version 2>&1 | head -1 || echo 'Not installed'",
                },
                {
                    "name": "tofu",
                    "command": "tofu",
                    "check_type": "command",
                    "var_name": "TOFU",
                    "version_cmd": "tofu version 2>&1 | head -1 || echo 'Not installed'",
                },
            ],
            "useful_commands": [
                {
                    "command": f"{project_name.lower()} --help",
                    "description": f"{project_name} CLI",
                },
                {"command": "wrknv status", "description": "Check tool versions"},
                {
                    "command": "wrknv container status",
                    "description": "Container status",
                },
                {"command": "pytest", "description": "Run tests"},
                {"command": "deactivate", "description": "Exit environment"},
            ],
        }

        # Update with user-provided kwargs
        config.update(kwargs)

        # Select appropriate template and environment
        if script_type == "sh":
            template = self.sh_env.get_template("base.sh.j2")
        elif script_type == "ps1":
            template = self.ps1_env.get_template("base.ps1.j2")
        else:
            raise ValueError(f"Unknown script type: {script_type}")

        # Render and write
        content = template.render(**config)
        output_path.write_text(content)

        # Make executable if it's a shell script
        if script_type == "sh":
            output_path.chmod(output_path.stat().st_mode | 0o111)

    def xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_68(
        self,
        project_name: str,
        output_path: Path,
        script_type: str = "sh",
        **kwargs: Any,
    ) -> None:
        """Generate an environment setup script.

        Args:
            project_name: Name of the project
            output_path: Path to write the script
            script_type: Either "sh" for bash or "ps1" for PowerShell
            **kwargs: Additional template variables
        """
        # Default configuration
        config = {
            "project_name": project_name,
            "env_profile_var": f"{project_name.upper().replace('-', '_')}_PROFILE",
            "venv_prefix": project_name.lower(),
            "use_spinner": script_type == "sh",  # PowerShell doesn't need spinner
            "strict_project_check": False,
            "install_siblings": True,
            "sibling_patterns": [],  # No default peers - specified in config
            "special_siblings": [],  # No default peers - specified in config
            "create_log_dir": True,
            "deduplicate_path": True,
            "include_tool_verification": True,  # Enable for wrknv
            "cleanup_logs": True,
            "tools_to_verify": [
                {
                    "name": "Python",
                    "command": "python",
                    "check_type": "command",
                    "var_name": "PYTHON",
                    "version_cmd": "python --version 2>&1",
                },
                {
                    "XXnameXX": "UV",
                    "command": "uv",
                    "check_type": "command",
                    "var_name": "UV",
                    "version_cmd": "uv --version 2>&1",
                },
                {
                    "name": "wrknv",
                    "command": "wrknv",
                    "check_type": "command",
                    "var_name": "WRKENV",
                    "version_cmd": "wrknv --version 2>&1 || echo 'No version info'",
                },
                {
                    "name": "ibmtf",
                    "command": "ibmtf",
                    "check_type": "command",
                    "var_name": "IBMTF",
                    "version_cmd": "ibmtf version 2>&1 | head -1 || echo 'Not installed'",
                },
                {
                    "name": "tofu",
                    "command": "tofu",
                    "check_type": "command",
                    "var_name": "TOFU",
                    "version_cmd": "tofu version 2>&1 | head -1 || echo 'Not installed'",
                },
            ],
            "useful_commands": [
                {
                    "command": f"{project_name.lower()} --help",
                    "description": f"{project_name} CLI",
                },
                {"command": "wrknv status", "description": "Check tool versions"},
                {
                    "command": "wrknv container status",
                    "description": "Container status",
                },
                {"command": "pytest", "description": "Run tests"},
                {"command": "deactivate", "description": "Exit environment"},
            ],
        }

        # Update with user-provided kwargs
        config.update(kwargs)

        # Select appropriate template and environment
        if script_type == "sh":
            template = self.sh_env.get_template("base.sh.j2")
        elif script_type == "ps1":
            template = self.ps1_env.get_template("base.ps1.j2")
        else:
            raise ValueError(f"Unknown script type: {script_type}")

        # Render and write
        content = template.render(**config)
        output_path.write_text(content)

        # Make executable if it's a shell script
        if script_type == "sh":
            output_path.chmod(output_path.stat().st_mode | 0o111)

    def xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_69(
        self,
        project_name: str,
        output_path: Path,
        script_type: str = "sh",
        **kwargs: Any,
    ) -> None:
        """Generate an environment setup script.

        Args:
            project_name: Name of the project
            output_path: Path to write the script
            script_type: Either "sh" for bash or "ps1" for PowerShell
            **kwargs: Additional template variables
        """
        # Default configuration
        config = {
            "project_name": project_name,
            "env_profile_var": f"{project_name.upper().replace('-', '_')}_PROFILE",
            "venv_prefix": project_name.lower(),
            "use_spinner": script_type == "sh",  # PowerShell doesn't need spinner
            "strict_project_check": False,
            "install_siblings": True,
            "sibling_patterns": [],  # No default peers - specified in config
            "special_siblings": [],  # No default peers - specified in config
            "create_log_dir": True,
            "deduplicate_path": True,
            "include_tool_verification": True,  # Enable for wrknv
            "cleanup_logs": True,
            "tools_to_verify": [
                {
                    "name": "Python",
                    "command": "python",
                    "check_type": "command",
                    "var_name": "PYTHON",
                    "version_cmd": "python --version 2>&1",
                },
                {
                    "NAME": "UV",
                    "command": "uv",
                    "check_type": "command",
                    "var_name": "UV",
                    "version_cmd": "uv --version 2>&1",
                },
                {
                    "name": "wrknv",
                    "command": "wrknv",
                    "check_type": "command",
                    "var_name": "WRKENV",
                    "version_cmd": "wrknv --version 2>&1 || echo 'No version info'",
                },
                {
                    "name": "ibmtf",
                    "command": "ibmtf",
                    "check_type": "command",
                    "var_name": "IBMTF",
                    "version_cmd": "ibmtf version 2>&1 | head -1 || echo 'Not installed'",
                },
                {
                    "name": "tofu",
                    "command": "tofu",
                    "check_type": "command",
                    "var_name": "TOFU",
                    "version_cmd": "tofu version 2>&1 | head -1 || echo 'Not installed'",
                },
            ],
            "useful_commands": [
                {
                    "command": f"{project_name.lower()} --help",
                    "description": f"{project_name} CLI",
                },
                {"command": "wrknv status", "description": "Check tool versions"},
                {
                    "command": "wrknv container status",
                    "description": "Container status",
                },
                {"command": "pytest", "description": "Run tests"},
                {"command": "deactivate", "description": "Exit environment"},
            ],
        }

        # Update with user-provided kwargs
        config.update(kwargs)

        # Select appropriate template and environment
        if script_type == "sh":
            template = self.sh_env.get_template("base.sh.j2")
        elif script_type == "ps1":
            template = self.ps1_env.get_template("base.ps1.j2")
        else:
            raise ValueError(f"Unknown script type: {script_type}")

        # Render and write
        content = template.render(**config)
        output_path.write_text(content)

        # Make executable if it's a shell script
        if script_type == "sh":
            output_path.chmod(output_path.stat().st_mode | 0o111)

    def xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_70(
        self,
        project_name: str,
        output_path: Path,
        script_type: str = "sh",
        **kwargs: Any,
    ) -> None:
        """Generate an environment setup script.

        Args:
            project_name: Name of the project
            output_path: Path to write the script
            script_type: Either "sh" for bash or "ps1" for PowerShell
            **kwargs: Additional template variables
        """
        # Default configuration
        config = {
            "project_name": project_name,
            "env_profile_var": f"{project_name.upper().replace('-', '_')}_PROFILE",
            "venv_prefix": project_name.lower(),
            "use_spinner": script_type == "sh",  # PowerShell doesn't need spinner
            "strict_project_check": False,
            "install_siblings": True,
            "sibling_patterns": [],  # No default peers - specified in config
            "special_siblings": [],  # No default peers - specified in config
            "create_log_dir": True,
            "deduplicate_path": True,
            "include_tool_verification": True,  # Enable for wrknv
            "cleanup_logs": True,
            "tools_to_verify": [
                {
                    "name": "Python",
                    "command": "python",
                    "check_type": "command",
                    "var_name": "PYTHON",
                    "version_cmd": "python --version 2>&1",
                },
                {
                    "name": "XXUVXX",
                    "command": "uv",
                    "check_type": "command",
                    "var_name": "UV",
                    "version_cmd": "uv --version 2>&1",
                },
                {
                    "name": "wrknv",
                    "command": "wrknv",
                    "check_type": "command",
                    "var_name": "WRKENV",
                    "version_cmd": "wrknv --version 2>&1 || echo 'No version info'",
                },
                {
                    "name": "ibmtf",
                    "command": "ibmtf",
                    "check_type": "command",
                    "var_name": "IBMTF",
                    "version_cmd": "ibmtf version 2>&1 | head -1 || echo 'Not installed'",
                },
                {
                    "name": "tofu",
                    "command": "tofu",
                    "check_type": "command",
                    "var_name": "TOFU",
                    "version_cmd": "tofu version 2>&1 | head -1 || echo 'Not installed'",
                },
            ],
            "useful_commands": [
                {
                    "command": f"{project_name.lower()} --help",
                    "description": f"{project_name} CLI",
                },
                {"command": "wrknv status", "description": "Check tool versions"},
                {
                    "command": "wrknv container status",
                    "description": "Container status",
                },
                {"command": "pytest", "description": "Run tests"},
                {"command": "deactivate", "description": "Exit environment"},
            ],
        }

        # Update with user-provided kwargs
        config.update(kwargs)

        # Select appropriate template and environment
        if script_type == "sh":
            template = self.sh_env.get_template("base.sh.j2")
        elif script_type == "ps1":
            template = self.ps1_env.get_template("base.ps1.j2")
        else:
            raise ValueError(f"Unknown script type: {script_type}")

        # Render and write
        content = template.render(**config)
        output_path.write_text(content)

        # Make executable if it's a shell script
        if script_type == "sh":
            output_path.chmod(output_path.stat().st_mode | 0o111)

    def xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_71(
        self,
        project_name: str,
        output_path: Path,
        script_type: str = "sh",
        **kwargs: Any,
    ) -> None:
        """Generate an environment setup script.

        Args:
            project_name: Name of the project
            output_path: Path to write the script
            script_type: Either "sh" for bash or "ps1" for PowerShell
            **kwargs: Additional template variables
        """
        # Default configuration
        config = {
            "project_name": project_name,
            "env_profile_var": f"{project_name.upper().replace('-', '_')}_PROFILE",
            "venv_prefix": project_name.lower(),
            "use_spinner": script_type == "sh",  # PowerShell doesn't need spinner
            "strict_project_check": False,
            "install_siblings": True,
            "sibling_patterns": [],  # No default peers - specified in config
            "special_siblings": [],  # No default peers - specified in config
            "create_log_dir": True,
            "deduplicate_path": True,
            "include_tool_verification": True,  # Enable for wrknv
            "cleanup_logs": True,
            "tools_to_verify": [
                {
                    "name": "Python",
                    "command": "python",
                    "check_type": "command",
                    "var_name": "PYTHON",
                    "version_cmd": "python --version 2>&1",
                },
                {
                    "name": "uv",
                    "command": "uv",
                    "check_type": "command",
                    "var_name": "UV",
                    "version_cmd": "uv --version 2>&1",
                },
                {
                    "name": "wrknv",
                    "command": "wrknv",
                    "check_type": "command",
                    "var_name": "WRKENV",
                    "version_cmd": "wrknv --version 2>&1 || echo 'No version info'",
                },
                {
                    "name": "ibmtf",
                    "command": "ibmtf",
                    "check_type": "command",
                    "var_name": "IBMTF",
                    "version_cmd": "ibmtf version 2>&1 | head -1 || echo 'Not installed'",
                },
                {
                    "name": "tofu",
                    "command": "tofu",
                    "check_type": "command",
                    "var_name": "TOFU",
                    "version_cmd": "tofu version 2>&1 | head -1 || echo 'Not installed'",
                },
            ],
            "useful_commands": [
                {
                    "command": f"{project_name.lower()} --help",
                    "description": f"{project_name} CLI",
                },
                {"command": "wrknv status", "description": "Check tool versions"},
                {
                    "command": "wrknv container status",
                    "description": "Container status",
                },
                {"command": "pytest", "description": "Run tests"},
                {"command": "deactivate", "description": "Exit environment"},
            ],
        }

        # Update with user-provided kwargs
        config.update(kwargs)

        # Select appropriate template and environment
        if script_type == "sh":
            template = self.sh_env.get_template("base.sh.j2")
        elif script_type == "ps1":
            template = self.ps1_env.get_template("base.ps1.j2")
        else:
            raise ValueError(f"Unknown script type: {script_type}")

        # Render and write
        content = template.render(**config)
        output_path.write_text(content)

        # Make executable if it's a shell script
        if script_type == "sh":
            output_path.chmod(output_path.stat().st_mode | 0o111)

    def xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_72(
        self,
        project_name: str,
        output_path: Path,
        script_type: str = "sh",
        **kwargs: Any,
    ) -> None:
        """Generate an environment setup script.

        Args:
            project_name: Name of the project
            output_path: Path to write the script
            script_type: Either "sh" for bash or "ps1" for PowerShell
            **kwargs: Additional template variables
        """
        # Default configuration
        config = {
            "project_name": project_name,
            "env_profile_var": f"{project_name.upper().replace('-', '_')}_PROFILE",
            "venv_prefix": project_name.lower(),
            "use_spinner": script_type == "sh",  # PowerShell doesn't need spinner
            "strict_project_check": False,
            "install_siblings": True,
            "sibling_patterns": [],  # No default peers - specified in config
            "special_siblings": [],  # No default peers - specified in config
            "create_log_dir": True,
            "deduplicate_path": True,
            "include_tool_verification": True,  # Enable for wrknv
            "cleanup_logs": True,
            "tools_to_verify": [
                {
                    "name": "Python",
                    "command": "python",
                    "check_type": "command",
                    "var_name": "PYTHON",
                    "version_cmd": "python --version 2>&1",
                },
                {
                    "name": "UV",
                    "XXcommandXX": "uv",
                    "check_type": "command",
                    "var_name": "UV",
                    "version_cmd": "uv --version 2>&1",
                },
                {
                    "name": "wrknv",
                    "command": "wrknv",
                    "check_type": "command",
                    "var_name": "WRKENV",
                    "version_cmd": "wrknv --version 2>&1 || echo 'No version info'",
                },
                {
                    "name": "ibmtf",
                    "command": "ibmtf",
                    "check_type": "command",
                    "var_name": "IBMTF",
                    "version_cmd": "ibmtf version 2>&1 | head -1 || echo 'Not installed'",
                },
                {
                    "name": "tofu",
                    "command": "tofu",
                    "check_type": "command",
                    "var_name": "TOFU",
                    "version_cmd": "tofu version 2>&1 | head -1 || echo 'Not installed'",
                },
            ],
            "useful_commands": [
                {
                    "command": f"{project_name.lower()} --help",
                    "description": f"{project_name} CLI",
                },
                {"command": "wrknv status", "description": "Check tool versions"},
                {
                    "command": "wrknv container status",
                    "description": "Container status",
                },
                {"command": "pytest", "description": "Run tests"},
                {"command": "deactivate", "description": "Exit environment"},
            ],
        }

        # Update with user-provided kwargs
        config.update(kwargs)

        # Select appropriate template and environment
        if script_type == "sh":
            template = self.sh_env.get_template("base.sh.j2")
        elif script_type == "ps1":
            template = self.ps1_env.get_template("base.ps1.j2")
        else:
            raise ValueError(f"Unknown script type: {script_type}")

        # Render and write
        content = template.render(**config)
        output_path.write_text(content)

        # Make executable if it's a shell script
        if script_type == "sh":
            output_path.chmod(output_path.stat().st_mode | 0o111)

    def xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_73(
        self,
        project_name: str,
        output_path: Path,
        script_type: str = "sh",
        **kwargs: Any,
    ) -> None:
        """Generate an environment setup script.

        Args:
            project_name: Name of the project
            output_path: Path to write the script
            script_type: Either "sh" for bash or "ps1" for PowerShell
            **kwargs: Additional template variables
        """
        # Default configuration
        config = {
            "project_name": project_name,
            "env_profile_var": f"{project_name.upper().replace('-', '_')}_PROFILE",
            "venv_prefix": project_name.lower(),
            "use_spinner": script_type == "sh",  # PowerShell doesn't need spinner
            "strict_project_check": False,
            "install_siblings": True,
            "sibling_patterns": [],  # No default peers - specified in config
            "special_siblings": [],  # No default peers - specified in config
            "create_log_dir": True,
            "deduplicate_path": True,
            "include_tool_verification": True,  # Enable for wrknv
            "cleanup_logs": True,
            "tools_to_verify": [
                {
                    "name": "Python",
                    "command": "python",
                    "check_type": "command",
                    "var_name": "PYTHON",
                    "version_cmd": "python --version 2>&1",
                },
                {
                    "name": "UV",
                    "COMMAND": "uv",
                    "check_type": "command",
                    "var_name": "UV",
                    "version_cmd": "uv --version 2>&1",
                },
                {
                    "name": "wrknv",
                    "command": "wrknv",
                    "check_type": "command",
                    "var_name": "WRKENV",
                    "version_cmd": "wrknv --version 2>&1 || echo 'No version info'",
                },
                {
                    "name": "ibmtf",
                    "command": "ibmtf",
                    "check_type": "command",
                    "var_name": "IBMTF",
                    "version_cmd": "ibmtf version 2>&1 | head -1 || echo 'Not installed'",
                },
                {
                    "name": "tofu",
                    "command": "tofu",
                    "check_type": "command",
                    "var_name": "TOFU",
                    "version_cmd": "tofu version 2>&1 | head -1 || echo 'Not installed'",
                },
            ],
            "useful_commands": [
                {
                    "command": f"{project_name.lower()} --help",
                    "description": f"{project_name} CLI",
                },
                {"command": "wrknv status", "description": "Check tool versions"},
                {
                    "command": "wrknv container status",
                    "description": "Container status",
                },
                {"command": "pytest", "description": "Run tests"},
                {"command": "deactivate", "description": "Exit environment"},
            ],
        }

        # Update with user-provided kwargs
        config.update(kwargs)

        # Select appropriate template and environment
        if script_type == "sh":
            template = self.sh_env.get_template("base.sh.j2")
        elif script_type == "ps1":
            template = self.ps1_env.get_template("base.ps1.j2")
        else:
            raise ValueError(f"Unknown script type: {script_type}")

        # Render and write
        content = template.render(**config)
        output_path.write_text(content)

        # Make executable if it's a shell script
        if script_type == "sh":
            output_path.chmod(output_path.stat().st_mode | 0o111)

    def xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_74(
        self,
        project_name: str,
        output_path: Path,
        script_type: str = "sh",
        **kwargs: Any,
    ) -> None:
        """Generate an environment setup script.

        Args:
            project_name: Name of the project
            output_path: Path to write the script
            script_type: Either "sh" for bash or "ps1" for PowerShell
            **kwargs: Additional template variables
        """
        # Default configuration
        config = {
            "project_name": project_name,
            "env_profile_var": f"{project_name.upper().replace('-', '_')}_PROFILE",
            "venv_prefix": project_name.lower(),
            "use_spinner": script_type == "sh",  # PowerShell doesn't need spinner
            "strict_project_check": False,
            "install_siblings": True,
            "sibling_patterns": [],  # No default peers - specified in config
            "special_siblings": [],  # No default peers - specified in config
            "create_log_dir": True,
            "deduplicate_path": True,
            "include_tool_verification": True,  # Enable for wrknv
            "cleanup_logs": True,
            "tools_to_verify": [
                {
                    "name": "Python",
                    "command": "python",
                    "check_type": "command",
                    "var_name": "PYTHON",
                    "version_cmd": "python --version 2>&1",
                },
                {
                    "name": "UV",
                    "command": "XXuvXX",
                    "check_type": "command",
                    "var_name": "UV",
                    "version_cmd": "uv --version 2>&1",
                },
                {
                    "name": "wrknv",
                    "command": "wrknv",
                    "check_type": "command",
                    "var_name": "WRKENV",
                    "version_cmd": "wrknv --version 2>&1 || echo 'No version info'",
                },
                {
                    "name": "ibmtf",
                    "command": "ibmtf",
                    "check_type": "command",
                    "var_name": "IBMTF",
                    "version_cmd": "ibmtf version 2>&1 | head -1 || echo 'Not installed'",
                },
                {
                    "name": "tofu",
                    "command": "tofu",
                    "check_type": "command",
                    "var_name": "TOFU",
                    "version_cmd": "tofu version 2>&1 | head -1 || echo 'Not installed'",
                },
            ],
            "useful_commands": [
                {
                    "command": f"{project_name.lower()} --help",
                    "description": f"{project_name} CLI",
                },
                {"command": "wrknv status", "description": "Check tool versions"},
                {
                    "command": "wrknv container status",
                    "description": "Container status",
                },
                {"command": "pytest", "description": "Run tests"},
                {"command": "deactivate", "description": "Exit environment"},
            ],
        }

        # Update with user-provided kwargs
        config.update(kwargs)

        # Select appropriate template and environment
        if script_type == "sh":
            template = self.sh_env.get_template("base.sh.j2")
        elif script_type == "ps1":
            template = self.ps1_env.get_template("base.ps1.j2")
        else:
            raise ValueError(f"Unknown script type: {script_type}")

        # Render and write
        content = template.render(**config)
        output_path.write_text(content)

        # Make executable if it's a shell script
        if script_type == "sh":
            output_path.chmod(output_path.stat().st_mode | 0o111)

    def xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_75(
        self,
        project_name: str,
        output_path: Path,
        script_type: str = "sh",
        **kwargs: Any,
    ) -> None:
        """Generate an environment setup script.

        Args:
            project_name: Name of the project
            output_path: Path to write the script
            script_type: Either "sh" for bash or "ps1" for PowerShell
            **kwargs: Additional template variables
        """
        # Default configuration
        config = {
            "project_name": project_name,
            "env_profile_var": f"{project_name.upper().replace('-', '_')}_PROFILE",
            "venv_prefix": project_name.lower(),
            "use_spinner": script_type == "sh",  # PowerShell doesn't need spinner
            "strict_project_check": False,
            "install_siblings": True,
            "sibling_patterns": [],  # No default peers - specified in config
            "special_siblings": [],  # No default peers - specified in config
            "create_log_dir": True,
            "deduplicate_path": True,
            "include_tool_verification": True,  # Enable for wrknv
            "cleanup_logs": True,
            "tools_to_verify": [
                {
                    "name": "Python",
                    "command": "python",
                    "check_type": "command",
                    "var_name": "PYTHON",
                    "version_cmd": "python --version 2>&1",
                },
                {
                    "name": "UV",
                    "command": "UV",
                    "check_type": "command",
                    "var_name": "UV",
                    "version_cmd": "uv --version 2>&1",
                },
                {
                    "name": "wrknv",
                    "command": "wrknv",
                    "check_type": "command",
                    "var_name": "WRKENV",
                    "version_cmd": "wrknv --version 2>&1 || echo 'No version info'",
                },
                {
                    "name": "ibmtf",
                    "command": "ibmtf",
                    "check_type": "command",
                    "var_name": "IBMTF",
                    "version_cmd": "ibmtf version 2>&1 | head -1 || echo 'Not installed'",
                },
                {
                    "name": "tofu",
                    "command": "tofu",
                    "check_type": "command",
                    "var_name": "TOFU",
                    "version_cmd": "tofu version 2>&1 | head -1 || echo 'Not installed'",
                },
            ],
            "useful_commands": [
                {
                    "command": f"{project_name.lower()} --help",
                    "description": f"{project_name} CLI",
                },
                {"command": "wrknv status", "description": "Check tool versions"},
                {
                    "command": "wrknv container status",
                    "description": "Container status",
                },
                {"command": "pytest", "description": "Run tests"},
                {"command": "deactivate", "description": "Exit environment"},
            ],
        }

        # Update with user-provided kwargs
        config.update(kwargs)

        # Select appropriate template and environment
        if script_type == "sh":
            template = self.sh_env.get_template("base.sh.j2")
        elif script_type == "ps1":
            template = self.ps1_env.get_template("base.ps1.j2")
        else:
            raise ValueError(f"Unknown script type: {script_type}")

        # Render and write
        content = template.render(**config)
        output_path.write_text(content)

        # Make executable if it's a shell script
        if script_type == "sh":
            output_path.chmod(output_path.stat().st_mode | 0o111)

    def xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_76(
        self,
        project_name: str,
        output_path: Path,
        script_type: str = "sh",
        **kwargs: Any,
    ) -> None:
        """Generate an environment setup script.

        Args:
            project_name: Name of the project
            output_path: Path to write the script
            script_type: Either "sh" for bash or "ps1" for PowerShell
            **kwargs: Additional template variables
        """
        # Default configuration
        config = {
            "project_name": project_name,
            "env_profile_var": f"{project_name.upper().replace('-', '_')}_PROFILE",
            "venv_prefix": project_name.lower(),
            "use_spinner": script_type == "sh",  # PowerShell doesn't need spinner
            "strict_project_check": False,
            "install_siblings": True,
            "sibling_patterns": [],  # No default peers - specified in config
            "special_siblings": [],  # No default peers - specified in config
            "create_log_dir": True,
            "deduplicate_path": True,
            "include_tool_verification": True,  # Enable for wrknv
            "cleanup_logs": True,
            "tools_to_verify": [
                {
                    "name": "Python",
                    "command": "python",
                    "check_type": "command",
                    "var_name": "PYTHON",
                    "version_cmd": "python --version 2>&1",
                },
                {
                    "name": "UV",
                    "command": "uv",
                    "XXcheck_typeXX": "command",
                    "var_name": "UV",
                    "version_cmd": "uv --version 2>&1",
                },
                {
                    "name": "wrknv",
                    "command": "wrknv",
                    "check_type": "command",
                    "var_name": "WRKENV",
                    "version_cmd": "wrknv --version 2>&1 || echo 'No version info'",
                },
                {
                    "name": "ibmtf",
                    "command": "ibmtf",
                    "check_type": "command",
                    "var_name": "IBMTF",
                    "version_cmd": "ibmtf version 2>&1 | head -1 || echo 'Not installed'",
                },
                {
                    "name": "tofu",
                    "command": "tofu",
                    "check_type": "command",
                    "var_name": "TOFU",
                    "version_cmd": "tofu version 2>&1 | head -1 || echo 'Not installed'",
                },
            ],
            "useful_commands": [
                {
                    "command": f"{project_name.lower()} --help",
                    "description": f"{project_name} CLI",
                },
                {"command": "wrknv status", "description": "Check tool versions"},
                {
                    "command": "wrknv container status",
                    "description": "Container status",
                },
                {"command": "pytest", "description": "Run tests"},
                {"command": "deactivate", "description": "Exit environment"},
            ],
        }

        # Update with user-provided kwargs
        config.update(kwargs)

        # Select appropriate template and environment
        if script_type == "sh":
            template = self.sh_env.get_template("base.sh.j2")
        elif script_type == "ps1":
            template = self.ps1_env.get_template("base.ps1.j2")
        else:
            raise ValueError(f"Unknown script type: {script_type}")

        # Render and write
        content = template.render(**config)
        output_path.write_text(content)

        # Make executable if it's a shell script
        if script_type == "sh":
            output_path.chmod(output_path.stat().st_mode | 0o111)

    def xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_77(
        self,
        project_name: str,
        output_path: Path,
        script_type: str = "sh",
        **kwargs: Any,
    ) -> None:
        """Generate an environment setup script.

        Args:
            project_name: Name of the project
            output_path: Path to write the script
            script_type: Either "sh" for bash or "ps1" for PowerShell
            **kwargs: Additional template variables
        """
        # Default configuration
        config = {
            "project_name": project_name,
            "env_profile_var": f"{project_name.upper().replace('-', '_')}_PROFILE",
            "venv_prefix": project_name.lower(),
            "use_spinner": script_type == "sh",  # PowerShell doesn't need spinner
            "strict_project_check": False,
            "install_siblings": True,
            "sibling_patterns": [],  # No default peers - specified in config
            "special_siblings": [],  # No default peers - specified in config
            "create_log_dir": True,
            "deduplicate_path": True,
            "include_tool_verification": True,  # Enable for wrknv
            "cleanup_logs": True,
            "tools_to_verify": [
                {
                    "name": "Python",
                    "command": "python",
                    "check_type": "command",
                    "var_name": "PYTHON",
                    "version_cmd": "python --version 2>&1",
                },
                {
                    "name": "UV",
                    "command": "uv",
                    "CHECK_TYPE": "command",
                    "var_name": "UV",
                    "version_cmd": "uv --version 2>&1",
                },
                {
                    "name": "wrknv",
                    "command": "wrknv",
                    "check_type": "command",
                    "var_name": "WRKENV",
                    "version_cmd": "wrknv --version 2>&1 || echo 'No version info'",
                },
                {
                    "name": "ibmtf",
                    "command": "ibmtf",
                    "check_type": "command",
                    "var_name": "IBMTF",
                    "version_cmd": "ibmtf version 2>&1 | head -1 || echo 'Not installed'",
                },
                {
                    "name": "tofu",
                    "command": "tofu",
                    "check_type": "command",
                    "var_name": "TOFU",
                    "version_cmd": "tofu version 2>&1 | head -1 || echo 'Not installed'",
                },
            ],
            "useful_commands": [
                {
                    "command": f"{project_name.lower()} --help",
                    "description": f"{project_name} CLI",
                },
                {"command": "wrknv status", "description": "Check tool versions"},
                {
                    "command": "wrknv container status",
                    "description": "Container status",
                },
                {"command": "pytest", "description": "Run tests"},
                {"command": "deactivate", "description": "Exit environment"},
            ],
        }

        # Update with user-provided kwargs
        config.update(kwargs)

        # Select appropriate template and environment
        if script_type == "sh":
            template = self.sh_env.get_template("base.sh.j2")
        elif script_type == "ps1":
            template = self.ps1_env.get_template("base.ps1.j2")
        else:
            raise ValueError(f"Unknown script type: {script_type}")

        # Render and write
        content = template.render(**config)
        output_path.write_text(content)

        # Make executable if it's a shell script
        if script_type == "sh":
            output_path.chmod(output_path.stat().st_mode | 0o111)

    def xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_78(
        self,
        project_name: str,
        output_path: Path,
        script_type: str = "sh",
        **kwargs: Any,
    ) -> None:
        """Generate an environment setup script.

        Args:
            project_name: Name of the project
            output_path: Path to write the script
            script_type: Either "sh" for bash or "ps1" for PowerShell
            **kwargs: Additional template variables
        """
        # Default configuration
        config = {
            "project_name": project_name,
            "env_profile_var": f"{project_name.upper().replace('-', '_')}_PROFILE",
            "venv_prefix": project_name.lower(),
            "use_spinner": script_type == "sh",  # PowerShell doesn't need spinner
            "strict_project_check": False,
            "install_siblings": True,
            "sibling_patterns": [],  # No default peers - specified in config
            "special_siblings": [],  # No default peers - specified in config
            "create_log_dir": True,
            "deduplicate_path": True,
            "include_tool_verification": True,  # Enable for wrknv
            "cleanup_logs": True,
            "tools_to_verify": [
                {
                    "name": "Python",
                    "command": "python",
                    "check_type": "command",
                    "var_name": "PYTHON",
                    "version_cmd": "python --version 2>&1",
                },
                {
                    "name": "UV",
                    "command": "uv",
                    "check_type": "XXcommandXX",
                    "var_name": "UV",
                    "version_cmd": "uv --version 2>&1",
                },
                {
                    "name": "wrknv",
                    "command": "wrknv",
                    "check_type": "command",
                    "var_name": "WRKENV",
                    "version_cmd": "wrknv --version 2>&1 || echo 'No version info'",
                },
                {
                    "name": "ibmtf",
                    "command": "ibmtf",
                    "check_type": "command",
                    "var_name": "IBMTF",
                    "version_cmd": "ibmtf version 2>&1 | head -1 || echo 'Not installed'",
                },
                {
                    "name": "tofu",
                    "command": "tofu",
                    "check_type": "command",
                    "var_name": "TOFU",
                    "version_cmd": "tofu version 2>&1 | head -1 || echo 'Not installed'",
                },
            ],
            "useful_commands": [
                {
                    "command": f"{project_name.lower()} --help",
                    "description": f"{project_name} CLI",
                },
                {"command": "wrknv status", "description": "Check tool versions"},
                {
                    "command": "wrknv container status",
                    "description": "Container status",
                },
                {"command": "pytest", "description": "Run tests"},
                {"command": "deactivate", "description": "Exit environment"},
            ],
        }

        # Update with user-provided kwargs
        config.update(kwargs)

        # Select appropriate template and environment
        if script_type == "sh":
            template = self.sh_env.get_template("base.sh.j2")
        elif script_type == "ps1":
            template = self.ps1_env.get_template("base.ps1.j2")
        else:
            raise ValueError(f"Unknown script type: {script_type}")

        # Render and write
        content = template.render(**config)
        output_path.write_text(content)

        # Make executable if it's a shell script
        if script_type == "sh":
            output_path.chmod(output_path.stat().st_mode | 0o111)

    def xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_79(
        self,
        project_name: str,
        output_path: Path,
        script_type: str = "sh",
        **kwargs: Any,
    ) -> None:
        """Generate an environment setup script.

        Args:
            project_name: Name of the project
            output_path: Path to write the script
            script_type: Either "sh" for bash or "ps1" for PowerShell
            **kwargs: Additional template variables
        """
        # Default configuration
        config = {
            "project_name": project_name,
            "env_profile_var": f"{project_name.upper().replace('-', '_')}_PROFILE",
            "venv_prefix": project_name.lower(),
            "use_spinner": script_type == "sh",  # PowerShell doesn't need spinner
            "strict_project_check": False,
            "install_siblings": True,
            "sibling_patterns": [],  # No default peers - specified in config
            "special_siblings": [],  # No default peers - specified in config
            "create_log_dir": True,
            "deduplicate_path": True,
            "include_tool_verification": True,  # Enable for wrknv
            "cleanup_logs": True,
            "tools_to_verify": [
                {
                    "name": "Python",
                    "command": "python",
                    "check_type": "command",
                    "var_name": "PYTHON",
                    "version_cmd": "python --version 2>&1",
                },
                {
                    "name": "UV",
                    "command": "uv",
                    "check_type": "COMMAND",
                    "var_name": "UV",
                    "version_cmd": "uv --version 2>&1",
                },
                {
                    "name": "wrknv",
                    "command": "wrknv",
                    "check_type": "command",
                    "var_name": "WRKENV",
                    "version_cmd": "wrknv --version 2>&1 || echo 'No version info'",
                },
                {
                    "name": "ibmtf",
                    "command": "ibmtf",
                    "check_type": "command",
                    "var_name": "IBMTF",
                    "version_cmd": "ibmtf version 2>&1 | head -1 || echo 'Not installed'",
                },
                {
                    "name": "tofu",
                    "command": "tofu",
                    "check_type": "command",
                    "var_name": "TOFU",
                    "version_cmd": "tofu version 2>&1 | head -1 || echo 'Not installed'",
                },
            ],
            "useful_commands": [
                {
                    "command": f"{project_name.lower()} --help",
                    "description": f"{project_name} CLI",
                },
                {"command": "wrknv status", "description": "Check tool versions"},
                {
                    "command": "wrknv container status",
                    "description": "Container status",
                },
                {"command": "pytest", "description": "Run tests"},
                {"command": "deactivate", "description": "Exit environment"},
            ],
        }

        # Update with user-provided kwargs
        config.update(kwargs)

        # Select appropriate template and environment
        if script_type == "sh":
            template = self.sh_env.get_template("base.sh.j2")
        elif script_type == "ps1":
            template = self.ps1_env.get_template("base.ps1.j2")
        else:
            raise ValueError(f"Unknown script type: {script_type}")

        # Render and write
        content = template.render(**config)
        output_path.write_text(content)

        # Make executable if it's a shell script
        if script_type == "sh":
            output_path.chmod(output_path.stat().st_mode | 0o111)

    def xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_80(
        self,
        project_name: str,
        output_path: Path,
        script_type: str = "sh",
        **kwargs: Any,
    ) -> None:
        """Generate an environment setup script.

        Args:
            project_name: Name of the project
            output_path: Path to write the script
            script_type: Either "sh" for bash or "ps1" for PowerShell
            **kwargs: Additional template variables
        """
        # Default configuration
        config = {
            "project_name": project_name,
            "env_profile_var": f"{project_name.upper().replace('-', '_')}_PROFILE",
            "venv_prefix": project_name.lower(),
            "use_spinner": script_type == "sh",  # PowerShell doesn't need spinner
            "strict_project_check": False,
            "install_siblings": True,
            "sibling_patterns": [],  # No default peers - specified in config
            "special_siblings": [],  # No default peers - specified in config
            "create_log_dir": True,
            "deduplicate_path": True,
            "include_tool_verification": True,  # Enable for wrknv
            "cleanup_logs": True,
            "tools_to_verify": [
                {
                    "name": "Python",
                    "command": "python",
                    "check_type": "command",
                    "var_name": "PYTHON",
                    "version_cmd": "python --version 2>&1",
                },
                {
                    "name": "UV",
                    "command": "uv",
                    "check_type": "command",
                    "XXvar_nameXX": "UV",
                    "version_cmd": "uv --version 2>&1",
                },
                {
                    "name": "wrknv",
                    "command": "wrknv",
                    "check_type": "command",
                    "var_name": "WRKENV",
                    "version_cmd": "wrknv --version 2>&1 || echo 'No version info'",
                },
                {
                    "name": "ibmtf",
                    "command": "ibmtf",
                    "check_type": "command",
                    "var_name": "IBMTF",
                    "version_cmd": "ibmtf version 2>&1 | head -1 || echo 'Not installed'",
                },
                {
                    "name": "tofu",
                    "command": "tofu",
                    "check_type": "command",
                    "var_name": "TOFU",
                    "version_cmd": "tofu version 2>&1 | head -1 || echo 'Not installed'",
                },
            ],
            "useful_commands": [
                {
                    "command": f"{project_name.lower()} --help",
                    "description": f"{project_name} CLI",
                },
                {"command": "wrknv status", "description": "Check tool versions"},
                {
                    "command": "wrknv container status",
                    "description": "Container status",
                },
                {"command": "pytest", "description": "Run tests"},
                {"command": "deactivate", "description": "Exit environment"},
            ],
        }

        # Update with user-provided kwargs
        config.update(kwargs)

        # Select appropriate template and environment
        if script_type == "sh":
            template = self.sh_env.get_template("base.sh.j2")
        elif script_type == "ps1":
            template = self.ps1_env.get_template("base.ps1.j2")
        else:
            raise ValueError(f"Unknown script type: {script_type}")

        # Render and write
        content = template.render(**config)
        output_path.write_text(content)

        # Make executable if it's a shell script
        if script_type == "sh":
            output_path.chmod(output_path.stat().st_mode | 0o111)

    def xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_81(
        self,
        project_name: str,
        output_path: Path,
        script_type: str = "sh",
        **kwargs: Any,
    ) -> None:
        """Generate an environment setup script.

        Args:
            project_name: Name of the project
            output_path: Path to write the script
            script_type: Either "sh" for bash or "ps1" for PowerShell
            **kwargs: Additional template variables
        """
        # Default configuration
        config = {
            "project_name": project_name,
            "env_profile_var": f"{project_name.upper().replace('-', '_')}_PROFILE",
            "venv_prefix": project_name.lower(),
            "use_spinner": script_type == "sh",  # PowerShell doesn't need spinner
            "strict_project_check": False,
            "install_siblings": True,
            "sibling_patterns": [],  # No default peers - specified in config
            "special_siblings": [],  # No default peers - specified in config
            "create_log_dir": True,
            "deduplicate_path": True,
            "include_tool_verification": True,  # Enable for wrknv
            "cleanup_logs": True,
            "tools_to_verify": [
                {
                    "name": "Python",
                    "command": "python",
                    "check_type": "command",
                    "var_name": "PYTHON",
                    "version_cmd": "python --version 2>&1",
                },
                {
                    "name": "UV",
                    "command": "uv",
                    "check_type": "command",
                    "VAR_NAME": "UV",
                    "version_cmd": "uv --version 2>&1",
                },
                {
                    "name": "wrknv",
                    "command": "wrknv",
                    "check_type": "command",
                    "var_name": "WRKENV",
                    "version_cmd": "wrknv --version 2>&1 || echo 'No version info'",
                },
                {
                    "name": "ibmtf",
                    "command": "ibmtf",
                    "check_type": "command",
                    "var_name": "IBMTF",
                    "version_cmd": "ibmtf version 2>&1 | head -1 || echo 'Not installed'",
                },
                {
                    "name": "tofu",
                    "command": "tofu",
                    "check_type": "command",
                    "var_name": "TOFU",
                    "version_cmd": "tofu version 2>&1 | head -1 || echo 'Not installed'",
                },
            ],
            "useful_commands": [
                {
                    "command": f"{project_name.lower()} --help",
                    "description": f"{project_name} CLI",
                },
                {"command": "wrknv status", "description": "Check tool versions"},
                {
                    "command": "wrknv container status",
                    "description": "Container status",
                },
                {"command": "pytest", "description": "Run tests"},
                {"command": "deactivate", "description": "Exit environment"},
            ],
        }

        # Update with user-provided kwargs
        config.update(kwargs)

        # Select appropriate template and environment
        if script_type == "sh":
            template = self.sh_env.get_template("base.sh.j2")
        elif script_type == "ps1":
            template = self.ps1_env.get_template("base.ps1.j2")
        else:
            raise ValueError(f"Unknown script type: {script_type}")

        # Render and write
        content = template.render(**config)
        output_path.write_text(content)

        # Make executable if it's a shell script
        if script_type == "sh":
            output_path.chmod(output_path.stat().st_mode | 0o111)

    def xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_82(
        self,
        project_name: str,
        output_path: Path,
        script_type: str = "sh",
        **kwargs: Any,
    ) -> None:
        """Generate an environment setup script.

        Args:
            project_name: Name of the project
            output_path: Path to write the script
            script_type: Either "sh" for bash or "ps1" for PowerShell
            **kwargs: Additional template variables
        """
        # Default configuration
        config = {
            "project_name": project_name,
            "env_profile_var": f"{project_name.upper().replace('-', '_')}_PROFILE",
            "venv_prefix": project_name.lower(),
            "use_spinner": script_type == "sh",  # PowerShell doesn't need spinner
            "strict_project_check": False,
            "install_siblings": True,
            "sibling_patterns": [],  # No default peers - specified in config
            "special_siblings": [],  # No default peers - specified in config
            "create_log_dir": True,
            "deduplicate_path": True,
            "include_tool_verification": True,  # Enable for wrknv
            "cleanup_logs": True,
            "tools_to_verify": [
                {
                    "name": "Python",
                    "command": "python",
                    "check_type": "command",
                    "var_name": "PYTHON",
                    "version_cmd": "python --version 2>&1",
                },
                {
                    "name": "UV",
                    "command": "uv",
                    "check_type": "command",
                    "var_name": "XXUVXX",
                    "version_cmd": "uv --version 2>&1",
                },
                {
                    "name": "wrknv",
                    "command": "wrknv",
                    "check_type": "command",
                    "var_name": "WRKENV",
                    "version_cmd": "wrknv --version 2>&1 || echo 'No version info'",
                },
                {
                    "name": "ibmtf",
                    "command": "ibmtf",
                    "check_type": "command",
                    "var_name": "IBMTF",
                    "version_cmd": "ibmtf version 2>&1 | head -1 || echo 'Not installed'",
                },
                {
                    "name": "tofu",
                    "command": "tofu",
                    "check_type": "command",
                    "var_name": "TOFU",
                    "version_cmd": "tofu version 2>&1 | head -1 || echo 'Not installed'",
                },
            ],
            "useful_commands": [
                {
                    "command": f"{project_name.lower()} --help",
                    "description": f"{project_name} CLI",
                },
                {"command": "wrknv status", "description": "Check tool versions"},
                {
                    "command": "wrknv container status",
                    "description": "Container status",
                },
                {"command": "pytest", "description": "Run tests"},
                {"command": "deactivate", "description": "Exit environment"},
            ],
        }

        # Update with user-provided kwargs
        config.update(kwargs)

        # Select appropriate template and environment
        if script_type == "sh":
            template = self.sh_env.get_template("base.sh.j2")
        elif script_type == "ps1":
            template = self.ps1_env.get_template("base.ps1.j2")
        else:
            raise ValueError(f"Unknown script type: {script_type}")

        # Render and write
        content = template.render(**config)
        output_path.write_text(content)

        # Make executable if it's a shell script
        if script_type == "sh":
            output_path.chmod(output_path.stat().st_mode | 0o111)

    def xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_83(
        self,
        project_name: str,
        output_path: Path,
        script_type: str = "sh",
        **kwargs: Any,
    ) -> None:
        """Generate an environment setup script.

        Args:
            project_name: Name of the project
            output_path: Path to write the script
            script_type: Either "sh" for bash or "ps1" for PowerShell
            **kwargs: Additional template variables
        """
        # Default configuration
        config = {
            "project_name": project_name,
            "env_profile_var": f"{project_name.upper().replace('-', '_')}_PROFILE",
            "venv_prefix": project_name.lower(),
            "use_spinner": script_type == "sh",  # PowerShell doesn't need spinner
            "strict_project_check": False,
            "install_siblings": True,
            "sibling_patterns": [],  # No default peers - specified in config
            "special_siblings": [],  # No default peers - specified in config
            "create_log_dir": True,
            "deduplicate_path": True,
            "include_tool_verification": True,  # Enable for wrknv
            "cleanup_logs": True,
            "tools_to_verify": [
                {
                    "name": "Python",
                    "command": "python",
                    "check_type": "command",
                    "var_name": "PYTHON",
                    "version_cmd": "python --version 2>&1",
                },
                {
                    "name": "UV",
                    "command": "uv",
                    "check_type": "command",
                    "var_name": "uv",
                    "version_cmd": "uv --version 2>&1",
                },
                {
                    "name": "wrknv",
                    "command": "wrknv",
                    "check_type": "command",
                    "var_name": "WRKENV",
                    "version_cmd": "wrknv --version 2>&1 || echo 'No version info'",
                },
                {
                    "name": "ibmtf",
                    "command": "ibmtf",
                    "check_type": "command",
                    "var_name": "IBMTF",
                    "version_cmd": "ibmtf version 2>&1 | head -1 || echo 'Not installed'",
                },
                {
                    "name": "tofu",
                    "command": "tofu",
                    "check_type": "command",
                    "var_name": "TOFU",
                    "version_cmd": "tofu version 2>&1 | head -1 || echo 'Not installed'",
                },
            ],
            "useful_commands": [
                {
                    "command": f"{project_name.lower()} --help",
                    "description": f"{project_name} CLI",
                },
                {"command": "wrknv status", "description": "Check tool versions"},
                {
                    "command": "wrknv container status",
                    "description": "Container status",
                },
                {"command": "pytest", "description": "Run tests"},
                {"command": "deactivate", "description": "Exit environment"},
            ],
        }

        # Update with user-provided kwargs
        config.update(kwargs)

        # Select appropriate template and environment
        if script_type == "sh":
            template = self.sh_env.get_template("base.sh.j2")
        elif script_type == "ps1":
            template = self.ps1_env.get_template("base.ps1.j2")
        else:
            raise ValueError(f"Unknown script type: {script_type}")

        # Render and write
        content = template.render(**config)
        output_path.write_text(content)

        # Make executable if it's a shell script
        if script_type == "sh":
            output_path.chmod(output_path.stat().st_mode | 0o111)

    def xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_84(
        self,
        project_name: str,
        output_path: Path,
        script_type: str = "sh",
        **kwargs: Any,
    ) -> None:
        """Generate an environment setup script.

        Args:
            project_name: Name of the project
            output_path: Path to write the script
            script_type: Either "sh" for bash or "ps1" for PowerShell
            **kwargs: Additional template variables
        """
        # Default configuration
        config = {
            "project_name": project_name,
            "env_profile_var": f"{project_name.upper().replace('-', '_')}_PROFILE",
            "venv_prefix": project_name.lower(),
            "use_spinner": script_type == "sh",  # PowerShell doesn't need spinner
            "strict_project_check": False,
            "install_siblings": True,
            "sibling_patterns": [],  # No default peers - specified in config
            "special_siblings": [],  # No default peers - specified in config
            "create_log_dir": True,
            "deduplicate_path": True,
            "include_tool_verification": True,  # Enable for wrknv
            "cleanup_logs": True,
            "tools_to_verify": [
                {
                    "name": "Python",
                    "command": "python",
                    "check_type": "command",
                    "var_name": "PYTHON",
                    "version_cmd": "python --version 2>&1",
                },
                {
                    "name": "UV",
                    "command": "uv",
                    "check_type": "command",
                    "var_name": "UV",
                    "XXversion_cmdXX": "uv --version 2>&1",
                },
                {
                    "name": "wrknv",
                    "command": "wrknv",
                    "check_type": "command",
                    "var_name": "WRKENV",
                    "version_cmd": "wrknv --version 2>&1 || echo 'No version info'",
                },
                {
                    "name": "ibmtf",
                    "command": "ibmtf",
                    "check_type": "command",
                    "var_name": "IBMTF",
                    "version_cmd": "ibmtf version 2>&1 | head -1 || echo 'Not installed'",
                },
                {
                    "name": "tofu",
                    "command": "tofu",
                    "check_type": "command",
                    "var_name": "TOFU",
                    "version_cmd": "tofu version 2>&1 | head -1 || echo 'Not installed'",
                },
            ],
            "useful_commands": [
                {
                    "command": f"{project_name.lower()} --help",
                    "description": f"{project_name} CLI",
                },
                {"command": "wrknv status", "description": "Check tool versions"},
                {
                    "command": "wrknv container status",
                    "description": "Container status",
                },
                {"command": "pytest", "description": "Run tests"},
                {"command": "deactivate", "description": "Exit environment"},
            ],
        }

        # Update with user-provided kwargs
        config.update(kwargs)

        # Select appropriate template and environment
        if script_type == "sh":
            template = self.sh_env.get_template("base.sh.j2")
        elif script_type == "ps1":
            template = self.ps1_env.get_template("base.ps1.j2")
        else:
            raise ValueError(f"Unknown script type: {script_type}")

        # Render and write
        content = template.render(**config)
        output_path.write_text(content)

        # Make executable if it's a shell script
        if script_type == "sh":
            output_path.chmod(output_path.stat().st_mode | 0o111)

    def xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_85(
        self,
        project_name: str,
        output_path: Path,
        script_type: str = "sh",
        **kwargs: Any,
    ) -> None:
        """Generate an environment setup script.

        Args:
            project_name: Name of the project
            output_path: Path to write the script
            script_type: Either "sh" for bash or "ps1" for PowerShell
            **kwargs: Additional template variables
        """
        # Default configuration
        config = {
            "project_name": project_name,
            "env_profile_var": f"{project_name.upper().replace('-', '_')}_PROFILE",
            "venv_prefix": project_name.lower(),
            "use_spinner": script_type == "sh",  # PowerShell doesn't need spinner
            "strict_project_check": False,
            "install_siblings": True,
            "sibling_patterns": [],  # No default peers - specified in config
            "special_siblings": [],  # No default peers - specified in config
            "create_log_dir": True,
            "deduplicate_path": True,
            "include_tool_verification": True,  # Enable for wrknv
            "cleanup_logs": True,
            "tools_to_verify": [
                {
                    "name": "Python",
                    "command": "python",
                    "check_type": "command",
                    "var_name": "PYTHON",
                    "version_cmd": "python --version 2>&1",
                },
                {
                    "name": "UV",
                    "command": "uv",
                    "check_type": "command",
                    "var_name": "UV",
                    "VERSION_CMD": "uv --version 2>&1",
                },
                {
                    "name": "wrknv",
                    "command": "wrknv",
                    "check_type": "command",
                    "var_name": "WRKENV",
                    "version_cmd": "wrknv --version 2>&1 || echo 'No version info'",
                },
                {
                    "name": "ibmtf",
                    "command": "ibmtf",
                    "check_type": "command",
                    "var_name": "IBMTF",
                    "version_cmd": "ibmtf version 2>&1 | head -1 || echo 'Not installed'",
                },
                {
                    "name": "tofu",
                    "command": "tofu",
                    "check_type": "command",
                    "var_name": "TOFU",
                    "version_cmd": "tofu version 2>&1 | head -1 || echo 'Not installed'",
                },
            ],
            "useful_commands": [
                {
                    "command": f"{project_name.lower()} --help",
                    "description": f"{project_name} CLI",
                },
                {"command": "wrknv status", "description": "Check tool versions"},
                {
                    "command": "wrknv container status",
                    "description": "Container status",
                },
                {"command": "pytest", "description": "Run tests"},
                {"command": "deactivate", "description": "Exit environment"},
            ],
        }

        # Update with user-provided kwargs
        config.update(kwargs)

        # Select appropriate template and environment
        if script_type == "sh":
            template = self.sh_env.get_template("base.sh.j2")
        elif script_type == "ps1":
            template = self.ps1_env.get_template("base.ps1.j2")
        else:
            raise ValueError(f"Unknown script type: {script_type}")

        # Render and write
        content = template.render(**config)
        output_path.write_text(content)

        # Make executable if it's a shell script
        if script_type == "sh":
            output_path.chmod(output_path.stat().st_mode | 0o111)

    def xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_86(
        self,
        project_name: str,
        output_path: Path,
        script_type: str = "sh",
        **kwargs: Any,
    ) -> None:
        """Generate an environment setup script.

        Args:
            project_name: Name of the project
            output_path: Path to write the script
            script_type: Either "sh" for bash or "ps1" for PowerShell
            **kwargs: Additional template variables
        """
        # Default configuration
        config = {
            "project_name": project_name,
            "env_profile_var": f"{project_name.upper().replace('-', '_')}_PROFILE",
            "venv_prefix": project_name.lower(),
            "use_spinner": script_type == "sh",  # PowerShell doesn't need spinner
            "strict_project_check": False,
            "install_siblings": True,
            "sibling_patterns": [],  # No default peers - specified in config
            "special_siblings": [],  # No default peers - specified in config
            "create_log_dir": True,
            "deduplicate_path": True,
            "include_tool_verification": True,  # Enable for wrknv
            "cleanup_logs": True,
            "tools_to_verify": [
                {
                    "name": "Python",
                    "command": "python",
                    "check_type": "command",
                    "var_name": "PYTHON",
                    "version_cmd": "python --version 2>&1",
                },
                {
                    "name": "UV",
                    "command": "uv",
                    "check_type": "command",
                    "var_name": "UV",
                    "version_cmd": "XXuv --version 2>&1XX",
                },
                {
                    "name": "wrknv",
                    "command": "wrknv",
                    "check_type": "command",
                    "var_name": "WRKENV",
                    "version_cmd": "wrknv --version 2>&1 || echo 'No version info'",
                },
                {
                    "name": "ibmtf",
                    "command": "ibmtf",
                    "check_type": "command",
                    "var_name": "IBMTF",
                    "version_cmd": "ibmtf version 2>&1 | head -1 || echo 'Not installed'",
                },
                {
                    "name": "tofu",
                    "command": "tofu",
                    "check_type": "command",
                    "var_name": "TOFU",
                    "version_cmd": "tofu version 2>&1 | head -1 || echo 'Not installed'",
                },
            ],
            "useful_commands": [
                {
                    "command": f"{project_name.lower()} --help",
                    "description": f"{project_name} CLI",
                },
                {"command": "wrknv status", "description": "Check tool versions"},
                {
                    "command": "wrknv container status",
                    "description": "Container status",
                },
                {"command": "pytest", "description": "Run tests"},
                {"command": "deactivate", "description": "Exit environment"},
            ],
        }

        # Update with user-provided kwargs
        config.update(kwargs)

        # Select appropriate template and environment
        if script_type == "sh":
            template = self.sh_env.get_template("base.sh.j2")
        elif script_type == "ps1":
            template = self.ps1_env.get_template("base.ps1.j2")
        else:
            raise ValueError(f"Unknown script type: {script_type}")

        # Render and write
        content = template.render(**config)
        output_path.write_text(content)

        # Make executable if it's a shell script
        if script_type == "sh":
            output_path.chmod(output_path.stat().st_mode | 0o111)

    def xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_87(
        self,
        project_name: str,
        output_path: Path,
        script_type: str = "sh",
        **kwargs: Any,
    ) -> None:
        """Generate an environment setup script.

        Args:
            project_name: Name of the project
            output_path: Path to write the script
            script_type: Either "sh" for bash or "ps1" for PowerShell
            **kwargs: Additional template variables
        """
        # Default configuration
        config = {
            "project_name": project_name,
            "env_profile_var": f"{project_name.upper().replace('-', '_')}_PROFILE",
            "venv_prefix": project_name.lower(),
            "use_spinner": script_type == "sh",  # PowerShell doesn't need spinner
            "strict_project_check": False,
            "install_siblings": True,
            "sibling_patterns": [],  # No default peers - specified in config
            "special_siblings": [],  # No default peers - specified in config
            "create_log_dir": True,
            "deduplicate_path": True,
            "include_tool_verification": True,  # Enable for wrknv
            "cleanup_logs": True,
            "tools_to_verify": [
                {
                    "name": "Python",
                    "command": "python",
                    "check_type": "command",
                    "var_name": "PYTHON",
                    "version_cmd": "python --version 2>&1",
                },
                {
                    "name": "UV",
                    "command": "uv",
                    "check_type": "command",
                    "var_name": "UV",
                    "version_cmd": "UV --VERSION 2>&1",
                },
                {
                    "name": "wrknv",
                    "command": "wrknv",
                    "check_type": "command",
                    "var_name": "WRKENV",
                    "version_cmd": "wrknv --version 2>&1 || echo 'No version info'",
                },
                {
                    "name": "ibmtf",
                    "command": "ibmtf",
                    "check_type": "command",
                    "var_name": "IBMTF",
                    "version_cmd": "ibmtf version 2>&1 | head -1 || echo 'Not installed'",
                },
                {
                    "name": "tofu",
                    "command": "tofu",
                    "check_type": "command",
                    "var_name": "TOFU",
                    "version_cmd": "tofu version 2>&1 | head -1 || echo 'Not installed'",
                },
            ],
            "useful_commands": [
                {
                    "command": f"{project_name.lower()} --help",
                    "description": f"{project_name} CLI",
                },
                {"command": "wrknv status", "description": "Check tool versions"},
                {
                    "command": "wrknv container status",
                    "description": "Container status",
                },
                {"command": "pytest", "description": "Run tests"},
                {"command": "deactivate", "description": "Exit environment"},
            ],
        }

        # Update with user-provided kwargs
        config.update(kwargs)

        # Select appropriate template and environment
        if script_type == "sh":
            template = self.sh_env.get_template("base.sh.j2")
        elif script_type == "ps1":
            template = self.ps1_env.get_template("base.ps1.j2")
        else:
            raise ValueError(f"Unknown script type: {script_type}")

        # Render and write
        content = template.render(**config)
        output_path.write_text(content)

        # Make executable if it's a shell script
        if script_type == "sh":
            output_path.chmod(output_path.stat().st_mode | 0o111)

    def xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_88(
        self,
        project_name: str,
        output_path: Path,
        script_type: str = "sh",
        **kwargs: Any,
    ) -> None:
        """Generate an environment setup script.

        Args:
            project_name: Name of the project
            output_path: Path to write the script
            script_type: Either "sh" for bash or "ps1" for PowerShell
            **kwargs: Additional template variables
        """
        # Default configuration
        config = {
            "project_name": project_name,
            "env_profile_var": f"{project_name.upper().replace('-', '_')}_PROFILE",
            "venv_prefix": project_name.lower(),
            "use_spinner": script_type == "sh",  # PowerShell doesn't need spinner
            "strict_project_check": False,
            "install_siblings": True,
            "sibling_patterns": [],  # No default peers - specified in config
            "special_siblings": [],  # No default peers - specified in config
            "create_log_dir": True,
            "deduplicate_path": True,
            "include_tool_verification": True,  # Enable for wrknv
            "cleanup_logs": True,
            "tools_to_verify": [
                {
                    "name": "Python",
                    "command": "python",
                    "check_type": "command",
                    "var_name": "PYTHON",
                    "version_cmd": "python --version 2>&1",
                },
                {
                    "name": "UV",
                    "command": "uv",
                    "check_type": "command",
                    "var_name": "UV",
                    "version_cmd": "uv --version 2>&1",
                },
                {
                    "XXnameXX": "wrknv",
                    "command": "wrknv",
                    "check_type": "command",
                    "var_name": "WRKENV",
                    "version_cmd": "wrknv --version 2>&1 || echo 'No version info'",
                },
                {
                    "name": "ibmtf",
                    "command": "ibmtf",
                    "check_type": "command",
                    "var_name": "IBMTF",
                    "version_cmd": "ibmtf version 2>&1 | head -1 || echo 'Not installed'",
                },
                {
                    "name": "tofu",
                    "command": "tofu",
                    "check_type": "command",
                    "var_name": "TOFU",
                    "version_cmd": "tofu version 2>&1 | head -1 || echo 'Not installed'",
                },
            ],
            "useful_commands": [
                {
                    "command": f"{project_name.lower()} --help",
                    "description": f"{project_name} CLI",
                },
                {"command": "wrknv status", "description": "Check tool versions"},
                {
                    "command": "wrknv container status",
                    "description": "Container status",
                },
                {"command": "pytest", "description": "Run tests"},
                {"command": "deactivate", "description": "Exit environment"},
            ],
        }

        # Update with user-provided kwargs
        config.update(kwargs)

        # Select appropriate template and environment
        if script_type == "sh":
            template = self.sh_env.get_template("base.sh.j2")
        elif script_type == "ps1":
            template = self.ps1_env.get_template("base.ps1.j2")
        else:
            raise ValueError(f"Unknown script type: {script_type}")

        # Render and write
        content = template.render(**config)
        output_path.write_text(content)

        # Make executable if it's a shell script
        if script_type == "sh":
            output_path.chmod(output_path.stat().st_mode | 0o111)

    def xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_89(
        self,
        project_name: str,
        output_path: Path,
        script_type: str = "sh",
        **kwargs: Any,
    ) -> None:
        """Generate an environment setup script.

        Args:
            project_name: Name of the project
            output_path: Path to write the script
            script_type: Either "sh" for bash or "ps1" for PowerShell
            **kwargs: Additional template variables
        """
        # Default configuration
        config = {
            "project_name": project_name,
            "env_profile_var": f"{project_name.upper().replace('-', '_')}_PROFILE",
            "venv_prefix": project_name.lower(),
            "use_spinner": script_type == "sh",  # PowerShell doesn't need spinner
            "strict_project_check": False,
            "install_siblings": True,
            "sibling_patterns": [],  # No default peers - specified in config
            "special_siblings": [],  # No default peers - specified in config
            "create_log_dir": True,
            "deduplicate_path": True,
            "include_tool_verification": True,  # Enable for wrknv
            "cleanup_logs": True,
            "tools_to_verify": [
                {
                    "name": "Python",
                    "command": "python",
                    "check_type": "command",
                    "var_name": "PYTHON",
                    "version_cmd": "python --version 2>&1",
                },
                {
                    "name": "UV",
                    "command": "uv",
                    "check_type": "command",
                    "var_name": "UV",
                    "version_cmd": "uv --version 2>&1",
                },
                {
                    "NAME": "wrknv",
                    "command": "wrknv",
                    "check_type": "command",
                    "var_name": "WRKENV",
                    "version_cmd": "wrknv --version 2>&1 || echo 'No version info'",
                },
                {
                    "name": "ibmtf",
                    "command": "ibmtf",
                    "check_type": "command",
                    "var_name": "IBMTF",
                    "version_cmd": "ibmtf version 2>&1 | head -1 || echo 'Not installed'",
                },
                {
                    "name": "tofu",
                    "command": "tofu",
                    "check_type": "command",
                    "var_name": "TOFU",
                    "version_cmd": "tofu version 2>&1 | head -1 || echo 'Not installed'",
                },
            ],
            "useful_commands": [
                {
                    "command": f"{project_name.lower()} --help",
                    "description": f"{project_name} CLI",
                },
                {"command": "wrknv status", "description": "Check tool versions"},
                {
                    "command": "wrknv container status",
                    "description": "Container status",
                },
                {"command": "pytest", "description": "Run tests"},
                {"command": "deactivate", "description": "Exit environment"},
            ],
        }

        # Update with user-provided kwargs
        config.update(kwargs)

        # Select appropriate template and environment
        if script_type == "sh":
            template = self.sh_env.get_template("base.sh.j2")
        elif script_type == "ps1":
            template = self.ps1_env.get_template("base.ps1.j2")
        else:
            raise ValueError(f"Unknown script type: {script_type}")

        # Render and write
        content = template.render(**config)
        output_path.write_text(content)

        # Make executable if it's a shell script
        if script_type == "sh":
            output_path.chmod(output_path.stat().st_mode | 0o111)

    def xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_90(
        self,
        project_name: str,
        output_path: Path,
        script_type: str = "sh",
        **kwargs: Any,
    ) -> None:
        """Generate an environment setup script.

        Args:
            project_name: Name of the project
            output_path: Path to write the script
            script_type: Either "sh" for bash or "ps1" for PowerShell
            **kwargs: Additional template variables
        """
        # Default configuration
        config = {
            "project_name": project_name,
            "env_profile_var": f"{project_name.upper().replace('-', '_')}_PROFILE",
            "venv_prefix": project_name.lower(),
            "use_spinner": script_type == "sh",  # PowerShell doesn't need spinner
            "strict_project_check": False,
            "install_siblings": True,
            "sibling_patterns": [],  # No default peers - specified in config
            "special_siblings": [],  # No default peers - specified in config
            "create_log_dir": True,
            "deduplicate_path": True,
            "include_tool_verification": True,  # Enable for wrknv
            "cleanup_logs": True,
            "tools_to_verify": [
                {
                    "name": "Python",
                    "command": "python",
                    "check_type": "command",
                    "var_name": "PYTHON",
                    "version_cmd": "python --version 2>&1",
                },
                {
                    "name": "UV",
                    "command": "uv",
                    "check_type": "command",
                    "var_name": "UV",
                    "version_cmd": "uv --version 2>&1",
                },
                {
                    "name": "XXwrknvXX",
                    "command": "wrknv",
                    "check_type": "command",
                    "var_name": "WRKENV",
                    "version_cmd": "wrknv --version 2>&1 || echo 'No version info'",
                },
                {
                    "name": "ibmtf",
                    "command": "ibmtf",
                    "check_type": "command",
                    "var_name": "IBMTF",
                    "version_cmd": "ibmtf version 2>&1 | head -1 || echo 'Not installed'",
                },
                {
                    "name": "tofu",
                    "command": "tofu",
                    "check_type": "command",
                    "var_name": "TOFU",
                    "version_cmd": "tofu version 2>&1 | head -1 || echo 'Not installed'",
                },
            ],
            "useful_commands": [
                {
                    "command": f"{project_name.lower()} --help",
                    "description": f"{project_name} CLI",
                },
                {"command": "wrknv status", "description": "Check tool versions"},
                {
                    "command": "wrknv container status",
                    "description": "Container status",
                },
                {"command": "pytest", "description": "Run tests"},
                {"command": "deactivate", "description": "Exit environment"},
            ],
        }

        # Update with user-provided kwargs
        config.update(kwargs)

        # Select appropriate template and environment
        if script_type == "sh":
            template = self.sh_env.get_template("base.sh.j2")
        elif script_type == "ps1":
            template = self.ps1_env.get_template("base.ps1.j2")
        else:
            raise ValueError(f"Unknown script type: {script_type}")

        # Render and write
        content = template.render(**config)
        output_path.write_text(content)

        # Make executable if it's a shell script
        if script_type == "sh":
            output_path.chmod(output_path.stat().st_mode | 0o111)

    def xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_91(
        self,
        project_name: str,
        output_path: Path,
        script_type: str = "sh",
        **kwargs: Any,
    ) -> None:
        """Generate an environment setup script.

        Args:
            project_name: Name of the project
            output_path: Path to write the script
            script_type: Either "sh" for bash or "ps1" for PowerShell
            **kwargs: Additional template variables
        """
        # Default configuration
        config = {
            "project_name": project_name,
            "env_profile_var": f"{project_name.upper().replace('-', '_')}_PROFILE",
            "venv_prefix": project_name.lower(),
            "use_spinner": script_type == "sh",  # PowerShell doesn't need spinner
            "strict_project_check": False,
            "install_siblings": True,
            "sibling_patterns": [],  # No default peers - specified in config
            "special_siblings": [],  # No default peers - specified in config
            "create_log_dir": True,
            "deduplicate_path": True,
            "include_tool_verification": True,  # Enable for wrknv
            "cleanup_logs": True,
            "tools_to_verify": [
                {
                    "name": "Python",
                    "command": "python",
                    "check_type": "command",
                    "var_name": "PYTHON",
                    "version_cmd": "python --version 2>&1",
                },
                {
                    "name": "UV",
                    "command": "uv",
                    "check_type": "command",
                    "var_name": "UV",
                    "version_cmd": "uv --version 2>&1",
                },
                {
                    "name": "WRKNV",
                    "command": "wrknv",
                    "check_type": "command",
                    "var_name": "WRKENV",
                    "version_cmd": "wrknv --version 2>&1 || echo 'No version info'",
                },
                {
                    "name": "ibmtf",
                    "command": "ibmtf",
                    "check_type": "command",
                    "var_name": "IBMTF",
                    "version_cmd": "ibmtf version 2>&1 | head -1 || echo 'Not installed'",
                },
                {
                    "name": "tofu",
                    "command": "tofu",
                    "check_type": "command",
                    "var_name": "TOFU",
                    "version_cmd": "tofu version 2>&1 | head -1 || echo 'Not installed'",
                },
            ],
            "useful_commands": [
                {
                    "command": f"{project_name.lower()} --help",
                    "description": f"{project_name} CLI",
                },
                {"command": "wrknv status", "description": "Check tool versions"},
                {
                    "command": "wrknv container status",
                    "description": "Container status",
                },
                {"command": "pytest", "description": "Run tests"},
                {"command": "deactivate", "description": "Exit environment"},
            ],
        }

        # Update with user-provided kwargs
        config.update(kwargs)

        # Select appropriate template and environment
        if script_type == "sh":
            template = self.sh_env.get_template("base.sh.j2")
        elif script_type == "ps1":
            template = self.ps1_env.get_template("base.ps1.j2")
        else:
            raise ValueError(f"Unknown script type: {script_type}")

        # Render and write
        content = template.render(**config)
        output_path.write_text(content)

        # Make executable if it's a shell script
        if script_type == "sh":
            output_path.chmod(output_path.stat().st_mode | 0o111)

    def xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_92(
        self,
        project_name: str,
        output_path: Path,
        script_type: str = "sh",
        **kwargs: Any,
    ) -> None:
        """Generate an environment setup script.

        Args:
            project_name: Name of the project
            output_path: Path to write the script
            script_type: Either "sh" for bash or "ps1" for PowerShell
            **kwargs: Additional template variables
        """
        # Default configuration
        config = {
            "project_name": project_name,
            "env_profile_var": f"{project_name.upper().replace('-', '_')}_PROFILE",
            "venv_prefix": project_name.lower(),
            "use_spinner": script_type == "sh",  # PowerShell doesn't need spinner
            "strict_project_check": False,
            "install_siblings": True,
            "sibling_patterns": [],  # No default peers - specified in config
            "special_siblings": [],  # No default peers - specified in config
            "create_log_dir": True,
            "deduplicate_path": True,
            "include_tool_verification": True,  # Enable for wrknv
            "cleanup_logs": True,
            "tools_to_verify": [
                {
                    "name": "Python",
                    "command": "python",
                    "check_type": "command",
                    "var_name": "PYTHON",
                    "version_cmd": "python --version 2>&1",
                },
                {
                    "name": "UV",
                    "command": "uv",
                    "check_type": "command",
                    "var_name": "UV",
                    "version_cmd": "uv --version 2>&1",
                },
                {
                    "name": "wrknv",
                    "XXcommandXX": "wrknv",
                    "check_type": "command",
                    "var_name": "WRKENV",
                    "version_cmd": "wrknv --version 2>&1 || echo 'No version info'",
                },
                {
                    "name": "ibmtf",
                    "command": "ibmtf",
                    "check_type": "command",
                    "var_name": "IBMTF",
                    "version_cmd": "ibmtf version 2>&1 | head -1 || echo 'Not installed'",
                },
                {
                    "name": "tofu",
                    "command": "tofu",
                    "check_type": "command",
                    "var_name": "TOFU",
                    "version_cmd": "tofu version 2>&1 | head -1 || echo 'Not installed'",
                },
            ],
            "useful_commands": [
                {
                    "command": f"{project_name.lower()} --help",
                    "description": f"{project_name} CLI",
                },
                {"command": "wrknv status", "description": "Check tool versions"},
                {
                    "command": "wrknv container status",
                    "description": "Container status",
                },
                {"command": "pytest", "description": "Run tests"},
                {"command": "deactivate", "description": "Exit environment"},
            ],
        }

        # Update with user-provided kwargs
        config.update(kwargs)

        # Select appropriate template and environment
        if script_type == "sh":
            template = self.sh_env.get_template("base.sh.j2")
        elif script_type == "ps1":
            template = self.ps1_env.get_template("base.ps1.j2")
        else:
            raise ValueError(f"Unknown script type: {script_type}")

        # Render and write
        content = template.render(**config)
        output_path.write_text(content)

        # Make executable if it's a shell script
        if script_type == "sh":
            output_path.chmod(output_path.stat().st_mode | 0o111)

    def xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_93(
        self,
        project_name: str,
        output_path: Path,
        script_type: str = "sh",
        **kwargs: Any,
    ) -> None:
        """Generate an environment setup script.

        Args:
            project_name: Name of the project
            output_path: Path to write the script
            script_type: Either "sh" for bash or "ps1" for PowerShell
            **kwargs: Additional template variables
        """
        # Default configuration
        config = {
            "project_name": project_name,
            "env_profile_var": f"{project_name.upper().replace('-', '_')}_PROFILE",
            "venv_prefix": project_name.lower(),
            "use_spinner": script_type == "sh",  # PowerShell doesn't need spinner
            "strict_project_check": False,
            "install_siblings": True,
            "sibling_patterns": [],  # No default peers - specified in config
            "special_siblings": [],  # No default peers - specified in config
            "create_log_dir": True,
            "deduplicate_path": True,
            "include_tool_verification": True,  # Enable for wrknv
            "cleanup_logs": True,
            "tools_to_verify": [
                {
                    "name": "Python",
                    "command": "python",
                    "check_type": "command",
                    "var_name": "PYTHON",
                    "version_cmd": "python --version 2>&1",
                },
                {
                    "name": "UV",
                    "command": "uv",
                    "check_type": "command",
                    "var_name": "UV",
                    "version_cmd": "uv --version 2>&1",
                },
                {
                    "name": "wrknv",
                    "COMMAND": "wrknv",
                    "check_type": "command",
                    "var_name": "WRKENV",
                    "version_cmd": "wrknv --version 2>&1 || echo 'No version info'",
                },
                {
                    "name": "ibmtf",
                    "command": "ibmtf",
                    "check_type": "command",
                    "var_name": "IBMTF",
                    "version_cmd": "ibmtf version 2>&1 | head -1 || echo 'Not installed'",
                },
                {
                    "name": "tofu",
                    "command": "tofu",
                    "check_type": "command",
                    "var_name": "TOFU",
                    "version_cmd": "tofu version 2>&1 | head -1 || echo 'Not installed'",
                },
            ],
            "useful_commands": [
                {
                    "command": f"{project_name.lower()} --help",
                    "description": f"{project_name} CLI",
                },
                {"command": "wrknv status", "description": "Check tool versions"},
                {
                    "command": "wrknv container status",
                    "description": "Container status",
                },
                {"command": "pytest", "description": "Run tests"},
                {"command": "deactivate", "description": "Exit environment"},
            ],
        }

        # Update with user-provided kwargs
        config.update(kwargs)

        # Select appropriate template and environment
        if script_type == "sh":
            template = self.sh_env.get_template("base.sh.j2")
        elif script_type == "ps1":
            template = self.ps1_env.get_template("base.ps1.j2")
        else:
            raise ValueError(f"Unknown script type: {script_type}")

        # Render and write
        content = template.render(**config)
        output_path.write_text(content)

        # Make executable if it's a shell script
        if script_type == "sh":
            output_path.chmod(output_path.stat().st_mode | 0o111)

    def xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_94(
        self,
        project_name: str,
        output_path: Path,
        script_type: str = "sh",
        **kwargs: Any,
    ) -> None:
        """Generate an environment setup script.

        Args:
            project_name: Name of the project
            output_path: Path to write the script
            script_type: Either "sh" for bash or "ps1" for PowerShell
            **kwargs: Additional template variables
        """
        # Default configuration
        config = {
            "project_name": project_name,
            "env_profile_var": f"{project_name.upper().replace('-', '_')}_PROFILE",
            "venv_prefix": project_name.lower(),
            "use_spinner": script_type == "sh",  # PowerShell doesn't need spinner
            "strict_project_check": False,
            "install_siblings": True,
            "sibling_patterns": [],  # No default peers - specified in config
            "special_siblings": [],  # No default peers - specified in config
            "create_log_dir": True,
            "deduplicate_path": True,
            "include_tool_verification": True,  # Enable for wrknv
            "cleanup_logs": True,
            "tools_to_verify": [
                {
                    "name": "Python",
                    "command": "python",
                    "check_type": "command",
                    "var_name": "PYTHON",
                    "version_cmd": "python --version 2>&1",
                },
                {
                    "name": "UV",
                    "command": "uv",
                    "check_type": "command",
                    "var_name": "UV",
                    "version_cmd": "uv --version 2>&1",
                },
                {
                    "name": "wrknv",
                    "command": "XXwrknvXX",
                    "check_type": "command",
                    "var_name": "WRKENV",
                    "version_cmd": "wrknv --version 2>&1 || echo 'No version info'",
                },
                {
                    "name": "ibmtf",
                    "command": "ibmtf",
                    "check_type": "command",
                    "var_name": "IBMTF",
                    "version_cmd": "ibmtf version 2>&1 | head -1 || echo 'Not installed'",
                },
                {
                    "name": "tofu",
                    "command": "tofu",
                    "check_type": "command",
                    "var_name": "TOFU",
                    "version_cmd": "tofu version 2>&1 | head -1 || echo 'Not installed'",
                },
            ],
            "useful_commands": [
                {
                    "command": f"{project_name.lower()} --help",
                    "description": f"{project_name} CLI",
                },
                {"command": "wrknv status", "description": "Check tool versions"},
                {
                    "command": "wrknv container status",
                    "description": "Container status",
                },
                {"command": "pytest", "description": "Run tests"},
                {"command": "deactivate", "description": "Exit environment"},
            ],
        }

        # Update with user-provided kwargs
        config.update(kwargs)

        # Select appropriate template and environment
        if script_type == "sh":
            template = self.sh_env.get_template("base.sh.j2")
        elif script_type == "ps1":
            template = self.ps1_env.get_template("base.ps1.j2")
        else:
            raise ValueError(f"Unknown script type: {script_type}")

        # Render and write
        content = template.render(**config)
        output_path.write_text(content)

        # Make executable if it's a shell script
        if script_type == "sh":
            output_path.chmod(output_path.stat().st_mode | 0o111)

    def xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_95(
        self,
        project_name: str,
        output_path: Path,
        script_type: str = "sh",
        **kwargs: Any,
    ) -> None:
        """Generate an environment setup script.

        Args:
            project_name: Name of the project
            output_path: Path to write the script
            script_type: Either "sh" for bash or "ps1" for PowerShell
            **kwargs: Additional template variables
        """
        # Default configuration
        config = {
            "project_name": project_name,
            "env_profile_var": f"{project_name.upper().replace('-', '_')}_PROFILE",
            "venv_prefix": project_name.lower(),
            "use_spinner": script_type == "sh",  # PowerShell doesn't need spinner
            "strict_project_check": False,
            "install_siblings": True,
            "sibling_patterns": [],  # No default peers - specified in config
            "special_siblings": [],  # No default peers - specified in config
            "create_log_dir": True,
            "deduplicate_path": True,
            "include_tool_verification": True,  # Enable for wrknv
            "cleanup_logs": True,
            "tools_to_verify": [
                {
                    "name": "Python",
                    "command": "python",
                    "check_type": "command",
                    "var_name": "PYTHON",
                    "version_cmd": "python --version 2>&1",
                },
                {
                    "name": "UV",
                    "command": "uv",
                    "check_type": "command",
                    "var_name": "UV",
                    "version_cmd": "uv --version 2>&1",
                },
                {
                    "name": "wrknv",
                    "command": "WRKNV",
                    "check_type": "command",
                    "var_name": "WRKENV",
                    "version_cmd": "wrknv --version 2>&1 || echo 'No version info'",
                },
                {
                    "name": "ibmtf",
                    "command": "ibmtf",
                    "check_type": "command",
                    "var_name": "IBMTF",
                    "version_cmd": "ibmtf version 2>&1 | head -1 || echo 'Not installed'",
                },
                {
                    "name": "tofu",
                    "command": "tofu",
                    "check_type": "command",
                    "var_name": "TOFU",
                    "version_cmd": "tofu version 2>&1 | head -1 || echo 'Not installed'",
                },
            ],
            "useful_commands": [
                {
                    "command": f"{project_name.lower()} --help",
                    "description": f"{project_name} CLI",
                },
                {"command": "wrknv status", "description": "Check tool versions"},
                {
                    "command": "wrknv container status",
                    "description": "Container status",
                },
                {"command": "pytest", "description": "Run tests"},
                {"command": "deactivate", "description": "Exit environment"},
            ],
        }

        # Update with user-provided kwargs
        config.update(kwargs)

        # Select appropriate template and environment
        if script_type == "sh":
            template = self.sh_env.get_template("base.sh.j2")
        elif script_type == "ps1":
            template = self.ps1_env.get_template("base.ps1.j2")
        else:
            raise ValueError(f"Unknown script type: {script_type}")

        # Render and write
        content = template.render(**config)
        output_path.write_text(content)

        # Make executable if it's a shell script
        if script_type == "sh":
            output_path.chmod(output_path.stat().st_mode | 0o111)

    def xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_96(
        self,
        project_name: str,
        output_path: Path,
        script_type: str = "sh",
        **kwargs: Any,
    ) -> None:
        """Generate an environment setup script.

        Args:
            project_name: Name of the project
            output_path: Path to write the script
            script_type: Either "sh" for bash or "ps1" for PowerShell
            **kwargs: Additional template variables
        """
        # Default configuration
        config = {
            "project_name": project_name,
            "env_profile_var": f"{project_name.upper().replace('-', '_')}_PROFILE",
            "venv_prefix": project_name.lower(),
            "use_spinner": script_type == "sh",  # PowerShell doesn't need spinner
            "strict_project_check": False,
            "install_siblings": True,
            "sibling_patterns": [],  # No default peers - specified in config
            "special_siblings": [],  # No default peers - specified in config
            "create_log_dir": True,
            "deduplicate_path": True,
            "include_tool_verification": True,  # Enable for wrknv
            "cleanup_logs": True,
            "tools_to_verify": [
                {
                    "name": "Python",
                    "command": "python",
                    "check_type": "command",
                    "var_name": "PYTHON",
                    "version_cmd": "python --version 2>&1",
                },
                {
                    "name": "UV",
                    "command": "uv",
                    "check_type": "command",
                    "var_name": "UV",
                    "version_cmd": "uv --version 2>&1",
                },
                {
                    "name": "wrknv",
                    "command": "wrknv",
                    "XXcheck_typeXX": "command",
                    "var_name": "WRKENV",
                    "version_cmd": "wrknv --version 2>&1 || echo 'No version info'",
                },
                {
                    "name": "ibmtf",
                    "command": "ibmtf",
                    "check_type": "command",
                    "var_name": "IBMTF",
                    "version_cmd": "ibmtf version 2>&1 | head -1 || echo 'Not installed'",
                },
                {
                    "name": "tofu",
                    "command": "tofu",
                    "check_type": "command",
                    "var_name": "TOFU",
                    "version_cmd": "tofu version 2>&1 | head -1 || echo 'Not installed'",
                },
            ],
            "useful_commands": [
                {
                    "command": f"{project_name.lower()} --help",
                    "description": f"{project_name} CLI",
                },
                {"command": "wrknv status", "description": "Check tool versions"},
                {
                    "command": "wrknv container status",
                    "description": "Container status",
                },
                {"command": "pytest", "description": "Run tests"},
                {"command": "deactivate", "description": "Exit environment"},
            ],
        }

        # Update with user-provided kwargs
        config.update(kwargs)

        # Select appropriate template and environment
        if script_type == "sh":
            template = self.sh_env.get_template("base.sh.j2")
        elif script_type == "ps1":
            template = self.ps1_env.get_template("base.ps1.j2")
        else:
            raise ValueError(f"Unknown script type: {script_type}")

        # Render and write
        content = template.render(**config)
        output_path.write_text(content)

        # Make executable if it's a shell script
        if script_type == "sh":
            output_path.chmod(output_path.stat().st_mode | 0o111)

    def xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_97(
        self,
        project_name: str,
        output_path: Path,
        script_type: str = "sh",
        **kwargs: Any,
    ) -> None:
        """Generate an environment setup script.

        Args:
            project_name: Name of the project
            output_path: Path to write the script
            script_type: Either "sh" for bash or "ps1" for PowerShell
            **kwargs: Additional template variables
        """
        # Default configuration
        config = {
            "project_name": project_name,
            "env_profile_var": f"{project_name.upper().replace('-', '_')}_PROFILE",
            "venv_prefix": project_name.lower(),
            "use_spinner": script_type == "sh",  # PowerShell doesn't need spinner
            "strict_project_check": False,
            "install_siblings": True,
            "sibling_patterns": [],  # No default peers - specified in config
            "special_siblings": [],  # No default peers - specified in config
            "create_log_dir": True,
            "deduplicate_path": True,
            "include_tool_verification": True,  # Enable for wrknv
            "cleanup_logs": True,
            "tools_to_verify": [
                {
                    "name": "Python",
                    "command": "python",
                    "check_type": "command",
                    "var_name": "PYTHON",
                    "version_cmd": "python --version 2>&1",
                },
                {
                    "name": "UV",
                    "command": "uv",
                    "check_type": "command",
                    "var_name": "UV",
                    "version_cmd": "uv --version 2>&1",
                },
                {
                    "name": "wrknv",
                    "command": "wrknv",
                    "CHECK_TYPE": "command",
                    "var_name": "WRKENV",
                    "version_cmd": "wrknv --version 2>&1 || echo 'No version info'",
                },
                {
                    "name": "ibmtf",
                    "command": "ibmtf",
                    "check_type": "command",
                    "var_name": "IBMTF",
                    "version_cmd": "ibmtf version 2>&1 | head -1 || echo 'Not installed'",
                },
                {
                    "name": "tofu",
                    "command": "tofu",
                    "check_type": "command",
                    "var_name": "TOFU",
                    "version_cmd": "tofu version 2>&1 | head -1 || echo 'Not installed'",
                },
            ],
            "useful_commands": [
                {
                    "command": f"{project_name.lower()} --help",
                    "description": f"{project_name} CLI",
                },
                {"command": "wrknv status", "description": "Check tool versions"},
                {
                    "command": "wrknv container status",
                    "description": "Container status",
                },
                {"command": "pytest", "description": "Run tests"},
                {"command": "deactivate", "description": "Exit environment"},
            ],
        }

        # Update with user-provided kwargs
        config.update(kwargs)

        # Select appropriate template and environment
        if script_type == "sh":
            template = self.sh_env.get_template("base.sh.j2")
        elif script_type == "ps1":
            template = self.ps1_env.get_template("base.ps1.j2")
        else:
            raise ValueError(f"Unknown script type: {script_type}")

        # Render and write
        content = template.render(**config)
        output_path.write_text(content)

        # Make executable if it's a shell script
        if script_type == "sh":
            output_path.chmod(output_path.stat().st_mode | 0o111)

    def xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_98(
        self,
        project_name: str,
        output_path: Path,
        script_type: str = "sh",
        **kwargs: Any,
    ) -> None:
        """Generate an environment setup script.

        Args:
            project_name: Name of the project
            output_path: Path to write the script
            script_type: Either "sh" for bash or "ps1" for PowerShell
            **kwargs: Additional template variables
        """
        # Default configuration
        config = {
            "project_name": project_name,
            "env_profile_var": f"{project_name.upper().replace('-', '_')}_PROFILE",
            "venv_prefix": project_name.lower(),
            "use_spinner": script_type == "sh",  # PowerShell doesn't need spinner
            "strict_project_check": False,
            "install_siblings": True,
            "sibling_patterns": [],  # No default peers - specified in config
            "special_siblings": [],  # No default peers - specified in config
            "create_log_dir": True,
            "deduplicate_path": True,
            "include_tool_verification": True,  # Enable for wrknv
            "cleanup_logs": True,
            "tools_to_verify": [
                {
                    "name": "Python",
                    "command": "python",
                    "check_type": "command",
                    "var_name": "PYTHON",
                    "version_cmd": "python --version 2>&1",
                },
                {
                    "name": "UV",
                    "command": "uv",
                    "check_type": "command",
                    "var_name": "UV",
                    "version_cmd": "uv --version 2>&1",
                },
                {
                    "name": "wrknv",
                    "command": "wrknv",
                    "check_type": "XXcommandXX",
                    "var_name": "WRKENV",
                    "version_cmd": "wrknv --version 2>&1 || echo 'No version info'",
                },
                {
                    "name": "ibmtf",
                    "command": "ibmtf",
                    "check_type": "command",
                    "var_name": "IBMTF",
                    "version_cmd": "ibmtf version 2>&1 | head -1 || echo 'Not installed'",
                },
                {
                    "name": "tofu",
                    "command": "tofu",
                    "check_type": "command",
                    "var_name": "TOFU",
                    "version_cmd": "tofu version 2>&1 | head -1 || echo 'Not installed'",
                },
            ],
            "useful_commands": [
                {
                    "command": f"{project_name.lower()} --help",
                    "description": f"{project_name} CLI",
                },
                {"command": "wrknv status", "description": "Check tool versions"},
                {
                    "command": "wrknv container status",
                    "description": "Container status",
                },
                {"command": "pytest", "description": "Run tests"},
                {"command": "deactivate", "description": "Exit environment"},
            ],
        }

        # Update with user-provided kwargs
        config.update(kwargs)

        # Select appropriate template and environment
        if script_type == "sh":
            template = self.sh_env.get_template("base.sh.j2")
        elif script_type == "ps1":
            template = self.ps1_env.get_template("base.ps1.j2")
        else:
            raise ValueError(f"Unknown script type: {script_type}")

        # Render and write
        content = template.render(**config)
        output_path.write_text(content)

        # Make executable if it's a shell script
        if script_type == "sh":
            output_path.chmod(output_path.stat().st_mode | 0o111)

    def xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_99(
        self,
        project_name: str,
        output_path: Path,
        script_type: str = "sh",
        **kwargs: Any,
    ) -> None:
        """Generate an environment setup script.

        Args:
            project_name: Name of the project
            output_path: Path to write the script
            script_type: Either "sh" for bash or "ps1" for PowerShell
            **kwargs: Additional template variables
        """
        # Default configuration
        config = {
            "project_name": project_name,
            "env_profile_var": f"{project_name.upper().replace('-', '_')}_PROFILE",
            "venv_prefix": project_name.lower(),
            "use_spinner": script_type == "sh",  # PowerShell doesn't need spinner
            "strict_project_check": False,
            "install_siblings": True,
            "sibling_patterns": [],  # No default peers - specified in config
            "special_siblings": [],  # No default peers - specified in config
            "create_log_dir": True,
            "deduplicate_path": True,
            "include_tool_verification": True,  # Enable for wrknv
            "cleanup_logs": True,
            "tools_to_verify": [
                {
                    "name": "Python",
                    "command": "python",
                    "check_type": "command",
                    "var_name": "PYTHON",
                    "version_cmd": "python --version 2>&1",
                },
                {
                    "name": "UV",
                    "command": "uv",
                    "check_type": "command",
                    "var_name": "UV",
                    "version_cmd": "uv --version 2>&1",
                },
                {
                    "name": "wrknv",
                    "command": "wrknv",
                    "check_type": "COMMAND",
                    "var_name": "WRKENV",
                    "version_cmd": "wrknv --version 2>&1 || echo 'No version info'",
                },
                {
                    "name": "ibmtf",
                    "command": "ibmtf",
                    "check_type": "command",
                    "var_name": "IBMTF",
                    "version_cmd": "ibmtf version 2>&1 | head -1 || echo 'Not installed'",
                },
                {
                    "name": "tofu",
                    "command": "tofu",
                    "check_type": "command",
                    "var_name": "TOFU",
                    "version_cmd": "tofu version 2>&1 | head -1 || echo 'Not installed'",
                },
            ],
            "useful_commands": [
                {
                    "command": f"{project_name.lower()} --help",
                    "description": f"{project_name} CLI",
                },
                {"command": "wrknv status", "description": "Check tool versions"},
                {
                    "command": "wrknv container status",
                    "description": "Container status",
                },
                {"command": "pytest", "description": "Run tests"},
                {"command": "deactivate", "description": "Exit environment"},
            ],
        }

        # Update with user-provided kwargs
        config.update(kwargs)

        # Select appropriate template and environment
        if script_type == "sh":
            template = self.sh_env.get_template("base.sh.j2")
        elif script_type == "ps1":
            template = self.ps1_env.get_template("base.ps1.j2")
        else:
            raise ValueError(f"Unknown script type: {script_type}")

        # Render and write
        content = template.render(**config)
        output_path.write_text(content)

        # Make executable if it's a shell script
        if script_type == "sh":
            output_path.chmod(output_path.stat().st_mode | 0o111)

    def xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_100(
        self,
        project_name: str,
        output_path: Path,
        script_type: str = "sh",
        **kwargs: Any,
    ) -> None:
        """Generate an environment setup script.

        Args:
            project_name: Name of the project
            output_path: Path to write the script
            script_type: Either "sh" for bash or "ps1" for PowerShell
            **kwargs: Additional template variables
        """
        # Default configuration
        config = {
            "project_name": project_name,
            "env_profile_var": f"{project_name.upper().replace('-', '_')}_PROFILE",
            "venv_prefix": project_name.lower(),
            "use_spinner": script_type == "sh",  # PowerShell doesn't need spinner
            "strict_project_check": False,
            "install_siblings": True,
            "sibling_patterns": [],  # No default peers - specified in config
            "special_siblings": [],  # No default peers - specified in config
            "create_log_dir": True,
            "deduplicate_path": True,
            "include_tool_verification": True,  # Enable for wrknv
            "cleanup_logs": True,
            "tools_to_verify": [
                {
                    "name": "Python",
                    "command": "python",
                    "check_type": "command",
                    "var_name": "PYTHON",
                    "version_cmd": "python --version 2>&1",
                },
                {
                    "name": "UV",
                    "command": "uv",
                    "check_type": "command",
                    "var_name": "UV",
                    "version_cmd": "uv --version 2>&1",
                },
                {
                    "name": "wrknv",
                    "command": "wrknv",
                    "check_type": "command",
                    "XXvar_nameXX": "WRKENV",
                    "version_cmd": "wrknv --version 2>&1 || echo 'No version info'",
                },
                {
                    "name": "ibmtf",
                    "command": "ibmtf",
                    "check_type": "command",
                    "var_name": "IBMTF",
                    "version_cmd": "ibmtf version 2>&1 | head -1 || echo 'Not installed'",
                },
                {
                    "name": "tofu",
                    "command": "tofu",
                    "check_type": "command",
                    "var_name": "TOFU",
                    "version_cmd": "tofu version 2>&1 | head -1 || echo 'Not installed'",
                },
            ],
            "useful_commands": [
                {
                    "command": f"{project_name.lower()} --help",
                    "description": f"{project_name} CLI",
                },
                {"command": "wrknv status", "description": "Check tool versions"},
                {
                    "command": "wrknv container status",
                    "description": "Container status",
                },
                {"command": "pytest", "description": "Run tests"},
                {"command": "deactivate", "description": "Exit environment"},
            ],
        }

        # Update with user-provided kwargs
        config.update(kwargs)

        # Select appropriate template and environment
        if script_type == "sh":
            template = self.sh_env.get_template("base.sh.j2")
        elif script_type == "ps1":
            template = self.ps1_env.get_template("base.ps1.j2")
        else:
            raise ValueError(f"Unknown script type: {script_type}")

        # Render and write
        content = template.render(**config)
        output_path.write_text(content)

        # Make executable if it's a shell script
        if script_type == "sh":
            output_path.chmod(output_path.stat().st_mode | 0o111)

    def xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_101(
        self,
        project_name: str,
        output_path: Path,
        script_type: str = "sh",
        **kwargs: Any,
    ) -> None:
        """Generate an environment setup script.

        Args:
            project_name: Name of the project
            output_path: Path to write the script
            script_type: Either "sh" for bash or "ps1" for PowerShell
            **kwargs: Additional template variables
        """
        # Default configuration
        config = {
            "project_name": project_name,
            "env_profile_var": f"{project_name.upper().replace('-', '_')}_PROFILE",
            "venv_prefix": project_name.lower(),
            "use_spinner": script_type == "sh",  # PowerShell doesn't need spinner
            "strict_project_check": False,
            "install_siblings": True,
            "sibling_patterns": [],  # No default peers - specified in config
            "special_siblings": [],  # No default peers - specified in config
            "create_log_dir": True,
            "deduplicate_path": True,
            "include_tool_verification": True,  # Enable for wrknv
            "cleanup_logs": True,
            "tools_to_verify": [
                {
                    "name": "Python",
                    "command": "python",
                    "check_type": "command",
                    "var_name": "PYTHON",
                    "version_cmd": "python --version 2>&1",
                },
                {
                    "name": "UV",
                    "command": "uv",
                    "check_type": "command",
                    "var_name": "UV",
                    "version_cmd": "uv --version 2>&1",
                },
                {
                    "name": "wrknv",
                    "command": "wrknv",
                    "check_type": "command",
                    "VAR_NAME": "WRKENV",
                    "version_cmd": "wrknv --version 2>&1 || echo 'No version info'",
                },
                {
                    "name": "ibmtf",
                    "command": "ibmtf",
                    "check_type": "command",
                    "var_name": "IBMTF",
                    "version_cmd": "ibmtf version 2>&1 | head -1 || echo 'Not installed'",
                },
                {
                    "name": "tofu",
                    "command": "tofu",
                    "check_type": "command",
                    "var_name": "TOFU",
                    "version_cmd": "tofu version 2>&1 | head -1 || echo 'Not installed'",
                },
            ],
            "useful_commands": [
                {
                    "command": f"{project_name.lower()} --help",
                    "description": f"{project_name} CLI",
                },
                {"command": "wrknv status", "description": "Check tool versions"},
                {
                    "command": "wrknv container status",
                    "description": "Container status",
                },
                {"command": "pytest", "description": "Run tests"},
                {"command": "deactivate", "description": "Exit environment"},
            ],
        }

        # Update with user-provided kwargs
        config.update(kwargs)

        # Select appropriate template and environment
        if script_type == "sh":
            template = self.sh_env.get_template("base.sh.j2")
        elif script_type == "ps1":
            template = self.ps1_env.get_template("base.ps1.j2")
        else:
            raise ValueError(f"Unknown script type: {script_type}")

        # Render and write
        content = template.render(**config)
        output_path.write_text(content)

        # Make executable if it's a shell script
        if script_type == "sh":
            output_path.chmod(output_path.stat().st_mode | 0o111)

    def xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_102(
        self,
        project_name: str,
        output_path: Path,
        script_type: str = "sh",
        **kwargs: Any,
    ) -> None:
        """Generate an environment setup script.

        Args:
            project_name: Name of the project
            output_path: Path to write the script
            script_type: Either "sh" for bash or "ps1" for PowerShell
            **kwargs: Additional template variables
        """
        # Default configuration
        config = {
            "project_name": project_name,
            "env_profile_var": f"{project_name.upper().replace('-', '_')}_PROFILE",
            "venv_prefix": project_name.lower(),
            "use_spinner": script_type == "sh",  # PowerShell doesn't need spinner
            "strict_project_check": False,
            "install_siblings": True,
            "sibling_patterns": [],  # No default peers - specified in config
            "special_siblings": [],  # No default peers - specified in config
            "create_log_dir": True,
            "deduplicate_path": True,
            "include_tool_verification": True,  # Enable for wrknv
            "cleanup_logs": True,
            "tools_to_verify": [
                {
                    "name": "Python",
                    "command": "python",
                    "check_type": "command",
                    "var_name": "PYTHON",
                    "version_cmd": "python --version 2>&1",
                },
                {
                    "name": "UV",
                    "command": "uv",
                    "check_type": "command",
                    "var_name": "UV",
                    "version_cmd": "uv --version 2>&1",
                },
                {
                    "name": "wrknv",
                    "command": "wrknv",
                    "check_type": "command",
                    "var_name": "XXWRKENVXX",
                    "version_cmd": "wrknv --version 2>&1 || echo 'No version info'",
                },
                {
                    "name": "ibmtf",
                    "command": "ibmtf",
                    "check_type": "command",
                    "var_name": "IBMTF",
                    "version_cmd": "ibmtf version 2>&1 | head -1 || echo 'Not installed'",
                },
                {
                    "name": "tofu",
                    "command": "tofu",
                    "check_type": "command",
                    "var_name": "TOFU",
                    "version_cmd": "tofu version 2>&1 | head -1 || echo 'Not installed'",
                },
            ],
            "useful_commands": [
                {
                    "command": f"{project_name.lower()} --help",
                    "description": f"{project_name} CLI",
                },
                {"command": "wrknv status", "description": "Check tool versions"},
                {
                    "command": "wrknv container status",
                    "description": "Container status",
                },
                {"command": "pytest", "description": "Run tests"},
                {"command": "deactivate", "description": "Exit environment"},
            ],
        }

        # Update with user-provided kwargs
        config.update(kwargs)

        # Select appropriate template and environment
        if script_type == "sh":
            template = self.sh_env.get_template("base.sh.j2")
        elif script_type == "ps1":
            template = self.ps1_env.get_template("base.ps1.j2")
        else:
            raise ValueError(f"Unknown script type: {script_type}")

        # Render and write
        content = template.render(**config)
        output_path.write_text(content)

        # Make executable if it's a shell script
        if script_type == "sh":
            output_path.chmod(output_path.stat().st_mode | 0o111)

    def xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_103(
        self,
        project_name: str,
        output_path: Path,
        script_type: str = "sh",
        **kwargs: Any,
    ) -> None:
        """Generate an environment setup script.

        Args:
            project_name: Name of the project
            output_path: Path to write the script
            script_type: Either "sh" for bash or "ps1" for PowerShell
            **kwargs: Additional template variables
        """
        # Default configuration
        config = {
            "project_name": project_name,
            "env_profile_var": f"{project_name.upper().replace('-', '_')}_PROFILE",
            "venv_prefix": project_name.lower(),
            "use_spinner": script_type == "sh",  # PowerShell doesn't need spinner
            "strict_project_check": False,
            "install_siblings": True,
            "sibling_patterns": [],  # No default peers - specified in config
            "special_siblings": [],  # No default peers - specified in config
            "create_log_dir": True,
            "deduplicate_path": True,
            "include_tool_verification": True,  # Enable for wrknv
            "cleanup_logs": True,
            "tools_to_verify": [
                {
                    "name": "Python",
                    "command": "python",
                    "check_type": "command",
                    "var_name": "PYTHON",
                    "version_cmd": "python --version 2>&1",
                },
                {
                    "name": "UV",
                    "command": "uv",
                    "check_type": "command",
                    "var_name": "UV",
                    "version_cmd": "uv --version 2>&1",
                },
                {
                    "name": "wrknv",
                    "command": "wrknv",
                    "check_type": "command",
                    "var_name": "wrkenv",
                    "version_cmd": "wrknv --version 2>&1 || echo 'No version info'",
                },
                {
                    "name": "ibmtf",
                    "command": "ibmtf",
                    "check_type": "command",
                    "var_name": "IBMTF",
                    "version_cmd": "ibmtf version 2>&1 | head -1 || echo 'Not installed'",
                },
                {
                    "name": "tofu",
                    "command": "tofu",
                    "check_type": "command",
                    "var_name": "TOFU",
                    "version_cmd": "tofu version 2>&1 | head -1 || echo 'Not installed'",
                },
            ],
            "useful_commands": [
                {
                    "command": f"{project_name.lower()} --help",
                    "description": f"{project_name} CLI",
                },
                {"command": "wrknv status", "description": "Check tool versions"},
                {
                    "command": "wrknv container status",
                    "description": "Container status",
                },
                {"command": "pytest", "description": "Run tests"},
                {"command": "deactivate", "description": "Exit environment"},
            ],
        }

        # Update with user-provided kwargs
        config.update(kwargs)

        # Select appropriate template and environment
        if script_type == "sh":
            template = self.sh_env.get_template("base.sh.j2")
        elif script_type == "ps1":
            template = self.ps1_env.get_template("base.ps1.j2")
        else:
            raise ValueError(f"Unknown script type: {script_type}")

        # Render and write
        content = template.render(**config)
        output_path.write_text(content)

        # Make executable if it's a shell script
        if script_type == "sh":
            output_path.chmod(output_path.stat().st_mode | 0o111)

    def xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_104(
        self,
        project_name: str,
        output_path: Path,
        script_type: str = "sh",
        **kwargs: Any,
    ) -> None:
        """Generate an environment setup script.

        Args:
            project_name: Name of the project
            output_path: Path to write the script
            script_type: Either "sh" for bash or "ps1" for PowerShell
            **kwargs: Additional template variables
        """
        # Default configuration
        config = {
            "project_name": project_name,
            "env_profile_var": f"{project_name.upper().replace('-', '_')}_PROFILE",
            "venv_prefix": project_name.lower(),
            "use_spinner": script_type == "sh",  # PowerShell doesn't need spinner
            "strict_project_check": False,
            "install_siblings": True,
            "sibling_patterns": [],  # No default peers - specified in config
            "special_siblings": [],  # No default peers - specified in config
            "create_log_dir": True,
            "deduplicate_path": True,
            "include_tool_verification": True,  # Enable for wrknv
            "cleanup_logs": True,
            "tools_to_verify": [
                {
                    "name": "Python",
                    "command": "python",
                    "check_type": "command",
                    "var_name": "PYTHON",
                    "version_cmd": "python --version 2>&1",
                },
                {
                    "name": "UV",
                    "command": "uv",
                    "check_type": "command",
                    "var_name": "UV",
                    "version_cmd": "uv --version 2>&1",
                },
                {
                    "name": "wrknv",
                    "command": "wrknv",
                    "check_type": "command",
                    "var_name": "WRKENV",
                    "XXversion_cmdXX": "wrknv --version 2>&1 || echo 'No version info'",
                },
                {
                    "name": "ibmtf",
                    "command": "ibmtf",
                    "check_type": "command",
                    "var_name": "IBMTF",
                    "version_cmd": "ibmtf version 2>&1 | head -1 || echo 'Not installed'",
                },
                {
                    "name": "tofu",
                    "command": "tofu",
                    "check_type": "command",
                    "var_name": "TOFU",
                    "version_cmd": "tofu version 2>&1 | head -1 || echo 'Not installed'",
                },
            ],
            "useful_commands": [
                {
                    "command": f"{project_name.lower()} --help",
                    "description": f"{project_name} CLI",
                },
                {"command": "wrknv status", "description": "Check tool versions"},
                {
                    "command": "wrknv container status",
                    "description": "Container status",
                },
                {"command": "pytest", "description": "Run tests"},
                {"command": "deactivate", "description": "Exit environment"},
            ],
        }

        # Update with user-provided kwargs
        config.update(kwargs)

        # Select appropriate template and environment
        if script_type == "sh":
            template = self.sh_env.get_template("base.sh.j2")
        elif script_type == "ps1":
            template = self.ps1_env.get_template("base.ps1.j2")
        else:
            raise ValueError(f"Unknown script type: {script_type}")

        # Render and write
        content = template.render(**config)
        output_path.write_text(content)

        # Make executable if it's a shell script
        if script_type == "sh":
            output_path.chmod(output_path.stat().st_mode | 0o111)

    def xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_105(
        self,
        project_name: str,
        output_path: Path,
        script_type: str = "sh",
        **kwargs: Any,
    ) -> None:
        """Generate an environment setup script.

        Args:
            project_name: Name of the project
            output_path: Path to write the script
            script_type: Either "sh" for bash or "ps1" for PowerShell
            **kwargs: Additional template variables
        """
        # Default configuration
        config = {
            "project_name": project_name,
            "env_profile_var": f"{project_name.upper().replace('-', '_')}_PROFILE",
            "venv_prefix": project_name.lower(),
            "use_spinner": script_type == "sh",  # PowerShell doesn't need spinner
            "strict_project_check": False,
            "install_siblings": True,
            "sibling_patterns": [],  # No default peers - specified in config
            "special_siblings": [],  # No default peers - specified in config
            "create_log_dir": True,
            "deduplicate_path": True,
            "include_tool_verification": True,  # Enable for wrknv
            "cleanup_logs": True,
            "tools_to_verify": [
                {
                    "name": "Python",
                    "command": "python",
                    "check_type": "command",
                    "var_name": "PYTHON",
                    "version_cmd": "python --version 2>&1",
                },
                {
                    "name": "UV",
                    "command": "uv",
                    "check_type": "command",
                    "var_name": "UV",
                    "version_cmd": "uv --version 2>&1",
                },
                {
                    "name": "wrknv",
                    "command": "wrknv",
                    "check_type": "command",
                    "var_name": "WRKENV",
                    "VERSION_CMD": "wrknv --version 2>&1 || echo 'No version info'",
                },
                {
                    "name": "ibmtf",
                    "command": "ibmtf",
                    "check_type": "command",
                    "var_name": "IBMTF",
                    "version_cmd": "ibmtf version 2>&1 | head -1 || echo 'Not installed'",
                },
                {
                    "name": "tofu",
                    "command": "tofu",
                    "check_type": "command",
                    "var_name": "TOFU",
                    "version_cmd": "tofu version 2>&1 | head -1 || echo 'Not installed'",
                },
            ],
            "useful_commands": [
                {
                    "command": f"{project_name.lower()} --help",
                    "description": f"{project_name} CLI",
                },
                {"command": "wrknv status", "description": "Check tool versions"},
                {
                    "command": "wrknv container status",
                    "description": "Container status",
                },
                {"command": "pytest", "description": "Run tests"},
                {"command": "deactivate", "description": "Exit environment"},
            ],
        }

        # Update with user-provided kwargs
        config.update(kwargs)

        # Select appropriate template and environment
        if script_type == "sh":
            template = self.sh_env.get_template("base.sh.j2")
        elif script_type == "ps1":
            template = self.ps1_env.get_template("base.ps1.j2")
        else:
            raise ValueError(f"Unknown script type: {script_type}")

        # Render and write
        content = template.render(**config)
        output_path.write_text(content)

        # Make executable if it's a shell script
        if script_type == "sh":
            output_path.chmod(output_path.stat().st_mode | 0o111)

    def xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_106(
        self,
        project_name: str,
        output_path: Path,
        script_type: str = "sh",
        **kwargs: Any,
    ) -> None:
        """Generate an environment setup script.

        Args:
            project_name: Name of the project
            output_path: Path to write the script
            script_type: Either "sh" for bash or "ps1" for PowerShell
            **kwargs: Additional template variables
        """
        # Default configuration
        config = {
            "project_name": project_name,
            "env_profile_var": f"{project_name.upper().replace('-', '_')}_PROFILE",
            "venv_prefix": project_name.lower(),
            "use_spinner": script_type == "sh",  # PowerShell doesn't need spinner
            "strict_project_check": False,
            "install_siblings": True,
            "sibling_patterns": [],  # No default peers - specified in config
            "special_siblings": [],  # No default peers - specified in config
            "create_log_dir": True,
            "deduplicate_path": True,
            "include_tool_verification": True,  # Enable for wrknv
            "cleanup_logs": True,
            "tools_to_verify": [
                {
                    "name": "Python",
                    "command": "python",
                    "check_type": "command",
                    "var_name": "PYTHON",
                    "version_cmd": "python --version 2>&1",
                },
                {
                    "name": "UV",
                    "command": "uv",
                    "check_type": "command",
                    "var_name": "UV",
                    "version_cmd": "uv --version 2>&1",
                },
                {
                    "name": "wrknv",
                    "command": "wrknv",
                    "check_type": "command",
                    "var_name": "WRKENV",
                    "version_cmd": "XXwrknv --version 2>&1 || echo 'No version info'XX",
                },
                {
                    "name": "ibmtf",
                    "command": "ibmtf",
                    "check_type": "command",
                    "var_name": "IBMTF",
                    "version_cmd": "ibmtf version 2>&1 | head -1 || echo 'Not installed'",
                },
                {
                    "name": "tofu",
                    "command": "tofu",
                    "check_type": "command",
                    "var_name": "TOFU",
                    "version_cmd": "tofu version 2>&1 | head -1 || echo 'Not installed'",
                },
            ],
            "useful_commands": [
                {
                    "command": f"{project_name.lower()} --help",
                    "description": f"{project_name} CLI",
                },
                {"command": "wrknv status", "description": "Check tool versions"},
                {
                    "command": "wrknv container status",
                    "description": "Container status",
                },
                {"command": "pytest", "description": "Run tests"},
                {"command": "deactivate", "description": "Exit environment"},
            ],
        }

        # Update with user-provided kwargs
        config.update(kwargs)

        # Select appropriate template and environment
        if script_type == "sh":
            template = self.sh_env.get_template("base.sh.j2")
        elif script_type == "ps1":
            template = self.ps1_env.get_template("base.ps1.j2")
        else:
            raise ValueError(f"Unknown script type: {script_type}")

        # Render and write
        content = template.render(**config)
        output_path.write_text(content)

        # Make executable if it's a shell script
        if script_type == "sh":
            output_path.chmod(output_path.stat().st_mode | 0o111)

    def xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_107(
        self,
        project_name: str,
        output_path: Path,
        script_type: str = "sh",
        **kwargs: Any,
    ) -> None:
        """Generate an environment setup script.

        Args:
            project_name: Name of the project
            output_path: Path to write the script
            script_type: Either "sh" for bash or "ps1" for PowerShell
            **kwargs: Additional template variables
        """
        # Default configuration
        config = {
            "project_name": project_name,
            "env_profile_var": f"{project_name.upper().replace('-', '_')}_PROFILE",
            "venv_prefix": project_name.lower(),
            "use_spinner": script_type == "sh",  # PowerShell doesn't need spinner
            "strict_project_check": False,
            "install_siblings": True,
            "sibling_patterns": [],  # No default peers - specified in config
            "special_siblings": [],  # No default peers - specified in config
            "create_log_dir": True,
            "deduplicate_path": True,
            "include_tool_verification": True,  # Enable for wrknv
            "cleanup_logs": True,
            "tools_to_verify": [
                {
                    "name": "Python",
                    "command": "python",
                    "check_type": "command",
                    "var_name": "PYTHON",
                    "version_cmd": "python --version 2>&1",
                },
                {
                    "name": "UV",
                    "command": "uv",
                    "check_type": "command",
                    "var_name": "UV",
                    "version_cmd": "uv --version 2>&1",
                },
                {
                    "name": "wrknv",
                    "command": "wrknv",
                    "check_type": "command",
                    "var_name": "WRKENV",
                    "version_cmd": "wrknv --version 2>&1 || echo 'no version info'",
                },
                {
                    "name": "ibmtf",
                    "command": "ibmtf",
                    "check_type": "command",
                    "var_name": "IBMTF",
                    "version_cmd": "ibmtf version 2>&1 | head -1 || echo 'Not installed'",
                },
                {
                    "name": "tofu",
                    "command": "tofu",
                    "check_type": "command",
                    "var_name": "TOFU",
                    "version_cmd": "tofu version 2>&1 | head -1 || echo 'Not installed'",
                },
            ],
            "useful_commands": [
                {
                    "command": f"{project_name.lower()} --help",
                    "description": f"{project_name} CLI",
                },
                {"command": "wrknv status", "description": "Check tool versions"},
                {
                    "command": "wrknv container status",
                    "description": "Container status",
                },
                {"command": "pytest", "description": "Run tests"},
                {"command": "deactivate", "description": "Exit environment"},
            ],
        }

        # Update with user-provided kwargs
        config.update(kwargs)

        # Select appropriate template and environment
        if script_type == "sh":
            template = self.sh_env.get_template("base.sh.j2")
        elif script_type == "ps1":
            template = self.ps1_env.get_template("base.ps1.j2")
        else:
            raise ValueError(f"Unknown script type: {script_type}")

        # Render and write
        content = template.render(**config)
        output_path.write_text(content)

        # Make executable if it's a shell script
        if script_type == "sh":
            output_path.chmod(output_path.stat().st_mode | 0o111)

    def xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_108(
        self,
        project_name: str,
        output_path: Path,
        script_type: str = "sh",
        **kwargs: Any,
    ) -> None:
        """Generate an environment setup script.

        Args:
            project_name: Name of the project
            output_path: Path to write the script
            script_type: Either "sh" for bash or "ps1" for PowerShell
            **kwargs: Additional template variables
        """
        # Default configuration
        config = {
            "project_name": project_name,
            "env_profile_var": f"{project_name.upper().replace('-', '_')}_PROFILE",
            "venv_prefix": project_name.lower(),
            "use_spinner": script_type == "sh",  # PowerShell doesn't need spinner
            "strict_project_check": False,
            "install_siblings": True,
            "sibling_patterns": [],  # No default peers - specified in config
            "special_siblings": [],  # No default peers - specified in config
            "create_log_dir": True,
            "deduplicate_path": True,
            "include_tool_verification": True,  # Enable for wrknv
            "cleanup_logs": True,
            "tools_to_verify": [
                {
                    "name": "Python",
                    "command": "python",
                    "check_type": "command",
                    "var_name": "PYTHON",
                    "version_cmd": "python --version 2>&1",
                },
                {
                    "name": "UV",
                    "command": "uv",
                    "check_type": "command",
                    "var_name": "UV",
                    "version_cmd": "uv --version 2>&1",
                },
                {
                    "name": "wrknv",
                    "command": "wrknv",
                    "check_type": "command",
                    "var_name": "WRKENV",
                    "version_cmd": "WRKNV --VERSION 2>&1 || ECHO 'NO VERSION INFO'",
                },
                {
                    "name": "ibmtf",
                    "command": "ibmtf",
                    "check_type": "command",
                    "var_name": "IBMTF",
                    "version_cmd": "ibmtf version 2>&1 | head -1 || echo 'Not installed'",
                },
                {
                    "name": "tofu",
                    "command": "tofu",
                    "check_type": "command",
                    "var_name": "TOFU",
                    "version_cmd": "tofu version 2>&1 | head -1 || echo 'Not installed'",
                },
            ],
            "useful_commands": [
                {
                    "command": f"{project_name.lower()} --help",
                    "description": f"{project_name} CLI",
                },
                {"command": "wrknv status", "description": "Check tool versions"},
                {
                    "command": "wrknv container status",
                    "description": "Container status",
                },
                {"command": "pytest", "description": "Run tests"},
                {"command": "deactivate", "description": "Exit environment"},
            ],
        }

        # Update with user-provided kwargs
        config.update(kwargs)

        # Select appropriate template and environment
        if script_type == "sh":
            template = self.sh_env.get_template("base.sh.j2")
        elif script_type == "ps1":
            template = self.ps1_env.get_template("base.ps1.j2")
        else:
            raise ValueError(f"Unknown script type: {script_type}")

        # Render and write
        content = template.render(**config)
        output_path.write_text(content)

        # Make executable if it's a shell script
        if script_type == "sh":
            output_path.chmod(output_path.stat().st_mode | 0o111)

    def xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_109(
        self,
        project_name: str,
        output_path: Path,
        script_type: str = "sh",
        **kwargs: Any,
    ) -> None:
        """Generate an environment setup script.

        Args:
            project_name: Name of the project
            output_path: Path to write the script
            script_type: Either "sh" for bash or "ps1" for PowerShell
            **kwargs: Additional template variables
        """
        # Default configuration
        config = {
            "project_name": project_name,
            "env_profile_var": f"{project_name.upper().replace('-', '_')}_PROFILE",
            "venv_prefix": project_name.lower(),
            "use_spinner": script_type == "sh",  # PowerShell doesn't need spinner
            "strict_project_check": False,
            "install_siblings": True,
            "sibling_patterns": [],  # No default peers - specified in config
            "special_siblings": [],  # No default peers - specified in config
            "create_log_dir": True,
            "deduplicate_path": True,
            "include_tool_verification": True,  # Enable for wrknv
            "cleanup_logs": True,
            "tools_to_verify": [
                {
                    "name": "Python",
                    "command": "python",
                    "check_type": "command",
                    "var_name": "PYTHON",
                    "version_cmd": "python --version 2>&1",
                },
                {
                    "name": "UV",
                    "command": "uv",
                    "check_type": "command",
                    "var_name": "UV",
                    "version_cmd": "uv --version 2>&1",
                },
                {
                    "name": "wrknv",
                    "command": "wrknv",
                    "check_type": "command",
                    "var_name": "WRKENV",
                    "version_cmd": "wrknv --version 2>&1 || echo 'No version info'",
                },
                {
                    "XXnameXX": "ibmtf",
                    "command": "ibmtf",
                    "check_type": "command",
                    "var_name": "IBMTF",
                    "version_cmd": "ibmtf version 2>&1 | head -1 || echo 'Not installed'",
                },
                {
                    "name": "tofu",
                    "command": "tofu",
                    "check_type": "command",
                    "var_name": "TOFU",
                    "version_cmd": "tofu version 2>&1 | head -1 || echo 'Not installed'",
                },
            ],
            "useful_commands": [
                {
                    "command": f"{project_name.lower()} --help",
                    "description": f"{project_name} CLI",
                },
                {"command": "wrknv status", "description": "Check tool versions"},
                {
                    "command": "wrknv container status",
                    "description": "Container status",
                },
                {"command": "pytest", "description": "Run tests"},
                {"command": "deactivate", "description": "Exit environment"},
            ],
        }

        # Update with user-provided kwargs
        config.update(kwargs)

        # Select appropriate template and environment
        if script_type == "sh":
            template = self.sh_env.get_template("base.sh.j2")
        elif script_type == "ps1":
            template = self.ps1_env.get_template("base.ps1.j2")
        else:
            raise ValueError(f"Unknown script type: {script_type}")

        # Render and write
        content = template.render(**config)
        output_path.write_text(content)

        # Make executable if it's a shell script
        if script_type == "sh":
            output_path.chmod(output_path.stat().st_mode | 0o111)

    def xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_110(
        self,
        project_name: str,
        output_path: Path,
        script_type: str = "sh",
        **kwargs: Any,
    ) -> None:
        """Generate an environment setup script.

        Args:
            project_name: Name of the project
            output_path: Path to write the script
            script_type: Either "sh" for bash or "ps1" for PowerShell
            **kwargs: Additional template variables
        """
        # Default configuration
        config = {
            "project_name": project_name,
            "env_profile_var": f"{project_name.upper().replace('-', '_')}_PROFILE",
            "venv_prefix": project_name.lower(),
            "use_spinner": script_type == "sh",  # PowerShell doesn't need spinner
            "strict_project_check": False,
            "install_siblings": True,
            "sibling_patterns": [],  # No default peers - specified in config
            "special_siblings": [],  # No default peers - specified in config
            "create_log_dir": True,
            "deduplicate_path": True,
            "include_tool_verification": True,  # Enable for wrknv
            "cleanup_logs": True,
            "tools_to_verify": [
                {
                    "name": "Python",
                    "command": "python",
                    "check_type": "command",
                    "var_name": "PYTHON",
                    "version_cmd": "python --version 2>&1",
                },
                {
                    "name": "UV",
                    "command": "uv",
                    "check_type": "command",
                    "var_name": "UV",
                    "version_cmd": "uv --version 2>&1",
                },
                {
                    "name": "wrknv",
                    "command": "wrknv",
                    "check_type": "command",
                    "var_name": "WRKENV",
                    "version_cmd": "wrknv --version 2>&1 || echo 'No version info'",
                },
                {
                    "NAME": "ibmtf",
                    "command": "ibmtf",
                    "check_type": "command",
                    "var_name": "IBMTF",
                    "version_cmd": "ibmtf version 2>&1 | head -1 || echo 'Not installed'",
                },
                {
                    "name": "tofu",
                    "command": "tofu",
                    "check_type": "command",
                    "var_name": "TOFU",
                    "version_cmd": "tofu version 2>&1 | head -1 || echo 'Not installed'",
                },
            ],
            "useful_commands": [
                {
                    "command": f"{project_name.lower()} --help",
                    "description": f"{project_name} CLI",
                },
                {"command": "wrknv status", "description": "Check tool versions"},
                {
                    "command": "wrknv container status",
                    "description": "Container status",
                },
                {"command": "pytest", "description": "Run tests"},
                {"command": "deactivate", "description": "Exit environment"},
            ],
        }

        # Update with user-provided kwargs
        config.update(kwargs)

        # Select appropriate template and environment
        if script_type == "sh":
            template = self.sh_env.get_template("base.sh.j2")
        elif script_type == "ps1":
            template = self.ps1_env.get_template("base.ps1.j2")
        else:
            raise ValueError(f"Unknown script type: {script_type}")

        # Render and write
        content = template.render(**config)
        output_path.write_text(content)

        # Make executable if it's a shell script
        if script_type == "sh":
            output_path.chmod(output_path.stat().st_mode | 0o111)

    def xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_111(
        self,
        project_name: str,
        output_path: Path,
        script_type: str = "sh",
        **kwargs: Any,
    ) -> None:
        """Generate an environment setup script.

        Args:
            project_name: Name of the project
            output_path: Path to write the script
            script_type: Either "sh" for bash or "ps1" for PowerShell
            **kwargs: Additional template variables
        """
        # Default configuration
        config = {
            "project_name": project_name,
            "env_profile_var": f"{project_name.upper().replace('-', '_')}_PROFILE",
            "venv_prefix": project_name.lower(),
            "use_spinner": script_type == "sh",  # PowerShell doesn't need spinner
            "strict_project_check": False,
            "install_siblings": True,
            "sibling_patterns": [],  # No default peers - specified in config
            "special_siblings": [],  # No default peers - specified in config
            "create_log_dir": True,
            "deduplicate_path": True,
            "include_tool_verification": True,  # Enable for wrknv
            "cleanup_logs": True,
            "tools_to_verify": [
                {
                    "name": "Python",
                    "command": "python",
                    "check_type": "command",
                    "var_name": "PYTHON",
                    "version_cmd": "python --version 2>&1",
                },
                {
                    "name": "UV",
                    "command": "uv",
                    "check_type": "command",
                    "var_name": "UV",
                    "version_cmd": "uv --version 2>&1",
                },
                {
                    "name": "wrknv",
                    "command": "wrknv",
                    "check_type": "command",
                    "var_name": "WRKENV",
                    "version_cmd": "wrknv --version 2>&1 || echo 'No version info'",
                },
                {
                    "name": "XXibmtfXX",
                    "command": "ibmtf",
                    "check_type": "command",
                    "var_name": "IBMTF",
                    "version_cmd": "ibmtf version 2>&1 | head -1 || echo 'Not installed'",
                },
                {
                    "name": "tofu",
                    "command": "tofu",
                    "check_type": "command",
                    "var_name": "TOFU",
                    "version_cmd": "tofu version 2>&1 | head -1 || echo 'Not installed'",
                },
            ],
            "useful_commands": [
                {
                    "command": f"{project_name.lower()} --help",
                    "description": f"{project_name} CLI",
                },
                {"command": "wrknv status", "description": "Check tool versions"},
                {
                    "command": "wrknv container status",
                    "description": "Container status",
                },
                {"command": "pytest", "description": "Run tests"},
                {"command": "deactivate", "description": "Exit environment"},
            ],
        }

        # Update with user-provided kwargs
        config.update(kwargs)

        # Select appropriate template and environment
        if script_type == "sh":
            template = self.sh_env.get_template("base.sh.j2")
        elif script_type == "ps1":
            template = self.ps1_env.get_template("base.ps1.j2")
        else:
            raise ValueError(f"Unknown script type: {script_type}")

        # Render and write
        content = template.render(**config)
        output_path.write_text(content)

        # Make executable if it's a shell script
        if script_type == "sh":
            output_path.chmod(output_path.stat().st_mode | 0o111)

    def xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_112(
        self,
        project_name: str,
        output_path: Path,
        script_type: str = "sh",
        **kwargs: Any,
    ) -> None:
        """Generate an environment setup script.

        Args:
            project_name: Name of the project
            output_path: Path to write the script
            script_type: Either "sh" for bash or "ps1" for PowerShell
            **kwargs: Additional template variables
        """
        # Default configuration
        config = {
            "project_name": project_name,
            "env_profile_var": f"{project_name.upper().replace('-', '_')}_PROFILE",
            "venv_prefix": project_name.lower(),
            "use_spinner": script_type == "sh",  # PowerShell doesn't need spinner
            "strict_project_check": False,
            "install_siblings": True,
            "sibling_patterns": [],  # No default peers - specified in config
            "special_siblings": [],  # No default peers - specified in config
            "create_log_dir": True,
            "deduplicate_path": True,
            "include_tool_verification": True,  # Enable for wrknv
            "cleanup_logs": True,
            "tools_to_verify": [
                {
                    "name": "Python",
                    "command": "python",
                    "check_type": "command",
                    "var_name": "PYTHON",
                    "version_cmd": "python --version 2>&1",
                },
                {
                    "name": "UV",
                    "command": "uv",
                    "check_type": "command",
                    "var_name": "UV",
                    "version_cmd": "uv --version 2>&1",
                },
                {
                    "name": "wrknv",
                    "command": "wrknv",
                    "check_type": "command",
                    "var_name": "WRKENV",
                    "version_cmd": "wrknv --version 2>&1 || echo 'No version info'",
                },
                {
                    "name": "IBMTF",
                    "command": "ibmtf",
                    "check_type": "command",
                    "var_name": "IBMTF",
                    "version_cmd": "ibmtf version 2>&1 | head -1 || echo 'Not installed'",
                },
                {
                    "name": "tofu",
                    "command": "tofu",
                    "check_type": "command",
                    "var_name": "TOFU",
                    "version_cmd": "tofu version 2>&1 | head -1 || echo 'Not installed'",
                },
            ],
            "useful_commands": [
                {
                    "command": f"{project_name.lower()} --help",
                    "description": f"{project_name} CLI",
                },
                {"command": "wrknv status", "description": "Check tool versions"},
                {
                    "command": "wrknv container status",
                    "description": "Container status",
                },
                {"command": "pytest", "description": "Run tests"},
                {"command": "deactivate", "description": "Exit environment"},
            ],
        }

        # Update with user-provided kwargs
        config.update(kwargs)

        # Select appropriate template and environment
        if script_type == "sh":
            template = self.sh_env.get_template("base.sh.j2")
        elif script_type == "ps1":
            template = self.ps1_env.get_template("base.ps1.j2")
        else:
            raise ValueError(f"Unknown script type: {script_type}")

        # Render and write
        content = template.render(**config)
        output_path.write_text(content)

        # Make executable if it's a shell script
        if script_type == "sh":
            output_path.chmod(output_path.stat().st_mode | 0o111)

    def xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_113(
        self,
        project_name: str,
        output_path: Path,
        script_type: str = "sh",
        **kwargs: Any,
    ) -> None:
        """Generate an environment setup script.

        Args:
            project_name: Name of the project
            output_path: Path to write the script
            script_type: Either "sh" for bash or "ps1" for PowerShell
            **kwargs: Additional template variables
        """
        # Default configuration
        config = {
            "project_name": project_name,
            "env_profile_var": f"{project_name.upper().replace('-', '_')}_PROFILE",
            "venv_prefix": project_name.lower(),
            "use_spinner": script_type == "sh",  # PowerShell doesn't need spinner
            "strict_project_check": False,
            "install_siblings": True,
            "sibling_patterns": [],  # No default peers - specified in config
            "special_siblings": [],  # No default peers - specified in config
            "create_log_dir": True,
            "deduplicate_path": True,
            "include_tool_verification": True,  # Enable for wrknv
            "cleanup_logs": True,
            "tools_to_verify": [
                {
                    "name": "Python",
                    "command": "python",
                    "check_type": "command",
                    "var_name": "PYTHON",
                    "version_cmd": "python --version 2>&1",
                },
                {
                    "name": "UV",
                    "command": "uv",
                    "check_type": "command",
                    "var_name": "UV",
                    "version_cmd": "uv --version 2>&1",
                },
                {
                    "name": "wrknv",
                    "command": "wrknv",
                    "check_type": "command",
                    "var_name": "WRKENV",
                    "version_cmd": "wrknv --version 2>&1 || echo 'No version info'",
                },
                {
                    "name": "ibmtf",
                    "XXcommandXX": "ibmtf",
                    "check_type": "command",
                    "var_name": "IBMTF",
                    "version_cmd": "ibmtf version 2>&1 | head -1 || echo 'Not installed'",
                },
                {
                    "name": "tofu",
                    "command": "tofu",
                    "check_type": "command",
                    "var_name": "TOFU",
                    "version_cmd": "tofu version 2>&1 | head -1 || echo 'Not installed'",
                },
            ],
            "useful_commands": [
                {
                    "command": f"{project_name.lower()} --help",
                    "description": f"{project_name} CLI",
                },
                {"command": "wrknv status", "description": "Check tool versions"},
                {
                    "command": "wrknv container status",
                    "description": "Container status",
                },
                {"command": "pytest", "description": "Run tests"},
                {"command": "deactivate", "description": "Exit environment"},
            ],
        }

        # Update with user-provided kwargs
        config.update(kwargs)

        # Select appropriate template and environment
        if script_type == "sh":
            template = self.sh_env.get_template("base.sh.j2")
        elif script_type == "ps1":
            template = self.ps1_env.get_template("base.ps1.j2")
        else:
            raise ValueError(f"Unknown script type: {script_type}")

        # Render and write
        content = template.render(**config)
        output_path.write_text(content)

        # Make executable if it's a shell script
        if script_type == "sh":
            output_path.chmod(output_path.stat().st_mode | 0o111)

    def xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_114(
        self,
        project_name: str,
        output_path: Path,
        script_type: str = "sh",
        **kwargs: Any,
    ) -> None:
        """Generate an environment setup script.

        Args:
            project_name: Name of the project
            output_path: Path to write the script
            script_type: Either "sh" for bash or "ps1" for PowerShell
            **kwargs: Additional template variables
        """
        # Default configuration
        config = {
            "project_name": project_name,
            "env_profile_var": f"{project_name.upper().replace('-', '_')}_PROFILE",
            "venv_prefix": project_name.lower(),
            "use_spinner": script_type == "sh",  # PowerShell doesn't need spinner
            "strict_project_check": False,
            "install_siblings": True,
            "sibling_patterns": [],  # No default peers - specified in config
            "special_siblings": [],  # No default peers - specified in config
            "create_log_dir": True,
            "deduplicate_path": True,
            "include_tool_verification": True,  # Enable for wrknv
            "cleanup_logs": True,
            "tools_to_verify": [
                {
                    "name": "Python",
                    "command": "python",
                    "check_type": "command",
                    "var_name": "PYTHON",
                    "version_cmd": "python --version 2>&1",
                },
                {
                    "name": "UV",
                    "command": "uv",
                    "check_type": "command",
                    "var_name": "UV",
                    "version_cmd": "uv --version 2>&1",
                },
                {
                    "name": "wrknv",
                    "command": "wrknv",
                    "check_type": "command",
                    "var_name": "WRKENV",
                    "version_cmd": "wrknv --version 2>&1 || echo 'No version info'",
                },
                {
                    "name": "ibmtf",
                    "COMMAND": "ibmtf",
                    "check_type": "command",
                    "var_name": "IBMTF",
                    "version_cmd": "ibmtf version 2>&1 | head -1 || echo 'Not installed'",
                },
                {
                    "name": "tofu",
                    "command": "tofu",
                    "check_type": "command",
                    "var_name": "TOFU",
                    "version_cmd": "tofu version 2>&1 | head -1 || echo 'Not installed'",
                },
            ],
            "useful_commands": [
                {
                    "command": f"{project_name.lower()} --help",
                    "description": f"{project_name} CLI",
                },
                {"command": "wrknv status", "description": "Check tool versions"},
                {
                    "command": "wrknv container status",
                    "description": "Container status",
                },
                {"command": "pytest", "description": "Run tests"},
                {"command": "deactivate", "description": "Exit environment"},
            ],
        }

        # Update with user-provided kwargs
        config.update(kwargs)

        # Select appropriate template and environment
        if script_type == "sh":
            template = self.sh_env.get_template("base.sh.j2")
        elif script_type == "ps1":
            template = self.ps1_env.get_template("base.ps1.j2")
        else:
            raise ValueError(f"Unknown script type: {script_type}")

        # Render and write
        content = template.render(**config)
        output_path.write_text(content)

        # Make executable if it's a shell script
        if script_type == "sh":
            output_path.chmod(output_path.stat().st_mode | 0o111)

    def xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_115(
        self,
        project_name: str,
        output_path: Path,
        script_type: str = "sh",
        **kwargs: Any,
    ) -> None:
        """Generate an environment setup script.

        Args:
            project_name: Name of the project
            output_path: Path to write the script
            script_type: Either "sh" for bash or "ps1" for PowerShell
            **kwargs: Additional template variables
        """
        # Default configuration
        config = {
            "project_name": project_name,
            "env_profile_var": f"{project_name.upper().replace('-', '_')}_PROFILE",
            "venv_prefix": project_name.lower(),
            "use_spinner": script_type == "sh",  # PowerShell doesn't need spinner
            "strict_project_check": False,
            "install_siblings": True,
            "sibling_patterns": [],  # No default peers - specified in config
            "special_siblings": [],  # No default peers - specified in config
            "create_log_dir": True,
            "deduplicate_path": True,
            "include_tool_verification": True,  # Enable for wrknv
            "cleanup_logs": True,
            "tools_to_verify": [
                {
                    "name": "Python",
                    "command": "python",
                    "check_type": "command",
                    "var_name": "PYTHON",
                    "version_cmd": "python --version 2>&1",
                },
                {
                    "name": "UV",
                    "command": "uv",
                    "check_type": "command",
                    "var_name": "UV",
                    "version_cmd": "uv --version 2>&1",
                },
                {
                    "name": "wrknv",
                    "command": "wrknv",
                    "check_type": "command",
                    "var_name": "WRKENV",
                    "version_cmd": "wrknv --version 2>&1 || echo 'No version info'",
                },
                {
                    "name": "ibmtf",
                    "command": "XXibmtfXX",
                    "check_type": "command",
                    "var_name": "IBMTF",
                    "version_cmd": "ibmtf version 2>&1 | head -1 || echo 'Not installed'",
                },
                {
                    "name": "tofu",
                    "command": "tofu",
                    "check_type": "command",
                    "var_name": "TOFU",
                    "version_cmd": "tofu version 2>&1 | head -1 || echo 'Not installed'",
                },
            ],
            "useful_commands": [
                {
                    "command": f"{project_name.lower()} --help",
                    "description": f"{project_name} CLI",
                },
                {"command": "wrknv status", "description": "Check tool versions"},
                {
                    "command": "wrknv container status",
                    "description": "Container status",
                },
                {"command": "pytest", "description": "Run tests"},
                {"command": "deactivate", "description": "Exit environment"},
            ],
        }

        # Update with user-provided kwargs
        config.update(kwargs)

        # Select appropriate template and environment
        if script_type == "sh":
            template = self.sh_env.get_template("base.sh.j2")
        elif script_type == "ps1":
            template = self.ps1_env.get_template("base.ps1.j2")
        else:
            raise ValueError(f"Unknown script type: {script_type}")

        # Render and write
        content = template.render(**config)
        output_path.write_text(content)

        # Make executable if it's a shell script
        if script_type == "sh":
            output_path.chmod(output_path.stat().st_mode | 0o111)

    def xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_116(
        self,
        project_name: str,
        output_path: Path,
        script_type: str = "sh",
        **kwargs: Any,
    ) -> None:
        """Generate an environment setup script.

        Args:
            project_name: Name of the project
            output_path: Path to write the script
            script_type: Either "sh" for bash or "ps1" for PowerShell
            **kwargs: Additional template variables
        """
        # Default configuration
        config = {
            "project_name": project_name,
            "env_profile_var": f"{project_name.upper().replace('-', '_')}_PROFILE",
            "venv_prefix": project_name.lower(),
            "use_spinner": script_type == "sh",  # PowerShell doesn't need spinner
            "strict_project_check": False,
            "install_siblings": True,
            "sibling_patterns": [],  # No default peers - specified in config
            "special_siblings": [],  # No default peers - specified in config
            "create_log_dir": True,
            "deduplicate_path": True,
            "include_tool_verification": True,  # Enable for wrknv
            "cleanup_logs": True,
            "tools_to_verify": [
                {
                    "name": "Python",
                    "command": "python",
                    "check_type": "command",
                    "var_name": "PYTHON",
                    "version_cmd": "python --version 2>&1",
                },
                {
                    "name": "UV",
                    "command": "uv",
                    "check_type": "command",
                    "var_name": "UV",
                    "version_cmd": "uv --version 2>&1",
                },
                {
                    "name": "wrknv",
                    "command": "wrknv",
                    "check_type": "command",
                    "var_name": "WRKENV",
                    "version_cmd": "wrknv --version 2>&1 || echo 'No version info'",
                },
                {
                    "name": "ibmtf",
                    "command": "IBMTF",
                    "check_type": "command",
                    "var_name": "IBMTF",
                    "version_cmd": "ibmtf version 2>&1 | head -1 || echo 'Not installed'",
                },
                {
                    "name": "tofu",
                    "command": "tofu",
                    "check_type": "command",
                    "var_name": "TOFU",
                    "version_cmd": "tofu version 2>&1 | head -1 || echo 'Not installed'",
                },
            ],
            "useful_commands": [
                {
                    "command": f"{project_name.lower()} --help",
                    "description": f"{project_name} CLI",
                },
                {"command": "wrknv status", "description": "Check tool versions"},
                {
                    "command": "wrknv container status",
                    "description": "Container status",
                },
                {"command": "pytest", "description": "Run tests"},
                {"command": "deactivate", "description": "Exit environment"},
            ],
        }

        # Update with user-provided kwargs
        config.update(kwargs)

        # Select appropriate template and environment
        if script_type == "sh":
            template = self.sh_env.get_template("base.sh.j2")
        elif script_type == "ps1":
            template = self.ps1_env.get_template("base.ps1.j2")
        else:
            raise ValueError(f"Unknown script type: {script_type}")

        # Render and write
        content = template.render(**config)
        output_path.write_text(content)

        # Make executable if it's a shell script
        if script_type == "sh":
            output_path.chmod(output_path.stat().st_mode | 0o111)

    def xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_117(
        self,
        project_name: str,
        output_path: Path,
        script_type: str = "sh",
        **kwargs: Any,
    ) -> None:
        """Generate an environment setup script.

        Args:
            project_name: Name of the project
            output_path: Path to write the script
            script_type: Either "sh" for bash or "ps1" for PowerShell
            **kwargs: Additional template variables
        """
        # Default configuration
        config = {
            "project_name": project_name,
            "env_profile_var": f"{project_name.upper().replace('-', '_')}_PROFILE",
            "venv_prefix": project_name.lower(),
            "use_spinner": script_type == "sh",  # PowerShell doesn't need spinner
            "strict_project_check": False,
            "install_siblings": True,
            "sibling_patterns": [],  # No default peers - specified in config
            "special_siblings": [],  # No default peers - specified in config
            "create_log_dir": True,
            "deduplicate_path": True,
            "include_tool_verification": True,  # Enable for wrknv
            "cleanup_logs": True,
            "tools_to_verify": [
                {
                    "name": "Python",
                    "command": "python",
                    "check_type": "command",
                    "var_name": "PYTHON",
                    "version_cmd": "python --version 2>&1",
                },
                {
                    "name": "UV",
                    "command": "uv",
                    "check_type": "command",
                    "var_name": "UV",
                    "version_cmd": "uv --version 2>&1",
                },
                {
                    "name": "wrknv",
                    "command": "wrknv",
                    "check_type": "command",
                    "var_name": "WRKENV",
                    "version_cmd": "wrknv --version 2>&1 || echo 'No version info'",
                },
                {
                    "name": "ibmtf",
                    "command": "ibmtf",
                    "XXcheck_typeXX": "command",
                    "var_name": "IBMTF",
                    "version_cmd": "ibmtf version 2>&1 | head -1 || echo 'Not installed'",
                },
                {
                    "name": "tofu",
                    "command": "tofu",
                    "check_type": "command",
                    "var_name": "TOFU",
                    "version_cmd": "tofu version 2>&1 | head -1 || echo 'Not installed'",
                },
            ],
            "useful_commands": [
                {
                    "command": f"{project_name.lower()} --help",
                    "description": f"{project_name} CLI",
                },
                {"command": "wrknv status", "description": "Check tool versions"},
                {
                    "command": "wrknv container status",
                    "description": "Container status",
                },
                {"command": "pytest", "description": "Run tests"},
                {"command": "deactivate", "description": "Exit environment"},
            ],
        }

        # Update with user-provided kwargs
        config.update(kwargs)

        # Select appropriate template and environment
        if script_type == "sh":
            template = self.sh_env.get_template("base.sh.j2")
        elif script_type == "ps1":
            template = self.ps1_env.get_template("base.ps1.j2")
        else:
            raise ValueError(f"Unknown script type: {script_type}")

        # Render and write
        content = template.render(**config)
        output_path.write_text(content)

        # Make executable if it's a shell script
        if script_type == "sh":
            output_path.chmod(output_path.stat().st_mode | 0o111)

    def xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_118(
        self,
        project_name: str,
        output_path: Path,
        script_type: str = "sh",
        **kwargs: Any,
    ) -> None:
        """Generate an environment setup script.

        Args:
            project_name: Name of the project
            output_path: Path to write the script
            script_type: Either "sh" for bash or "ps1" for PowerShell
            **kwargs: Additional template variables
        """
        # Default configuration
        config = {
            "project_name": project_name,
            "env_profile_var": f"{project_name.upper().replace('-', '_')}_PROFILE",
            "venv_prefix": project_name.lower(),
            "use_spinner": script_type == "sh",  # PowerShell doesn't need spinner
            "strict_project_check": False,
            "install_siblings": True,
            "sibling_patterns": [],  # No default peers - specified in config
            "special_siblings": [],  # No default peers - specified in config
            "create_log_dir": True,
            "deduplicate_path": True,
            "include_tool_verification": True,  # Enable for wrknv
            "cleanup_logs": True,
            "tools_to_verify": [
                {
                    "name": "Python",
                    "command": "python",
                    "check_type": "command",
                    "var_name": "PYTHON",
                    "version_cmd": "python --version 2>&1",
                },
                {
                    "name": "UV",
                    "command": "uv",
                    "check_type": "command",
                    "var_name": "UV",
                    "version_cmd": "uv --version 2>&1",
                },
                {
                    "name": "wrknv",
                    "command": "wrknv",
                    "check_type": "command",
                    "var_name": "WRKENV",
                    "version_cmd": "wrknv --version 2>&1 || echo 'No version info'",
                },
                {
                    "name": "ibmtf",
                    "command": "ibmtf",
                    "CHECK_TYPE": "command",
                    "var_name": "IBMTF",
                    "version_cmd": "ibmtf version 2>&1 | head -1 || echo 'Not installed'",
                },
                {
                    "name": "tofu",
                    "command": "tofu",
                    "check_type": "command",
                    "var_name": "TOFU",
                    "version_cmd": "tofu version 2>&1 | head -1 || echo 'Not installed'",
                },
            ],
            "useful_commands": [
                {
                    "command": f"{project_name.lower()} --help",
                    "description": f"{project_name} CLI",
                },
                {"command": "wrknv status", "description": "Check tool versions"},
                {
                    "command": "wrknv container status",
                    "description": "Container status",
                },
                {"command": "pytest", "description": "Run tests"},
                {"command": "deactivate", "description": "Exit environment"},
            ],
        }

        # Update with user-provided kwargs
        config.update(kwargs)

        # Select appropriate template and environment
        if script_type == "sh":
            template = self.sh_env.get_template("base.sh.j2")
        elif script_type == "ps1":
            template = self.ps1_env.get_template("base.ps1.j2")
        else:
            raise ValueError(f"Unknown script type: {script_type}")

        # Render and write
        content = template.render(**config)
        output_path.write_text(content)

        # Make executable if it's a shell script
        if script_type == "sh":
            output_path.chmod(output_path.stat().st_mode | 0o111)

    def xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_119(
        self,
        project_name: str,
        output_path: Path,
        script_type: str = "sh",
        **kwargs: Any,
    ) -> None:
        """Generate an environment setup script.

        Args:
            project_name: Name of the project
            output_path: Path to write the script
            script_type: Either "sh" for bash or "ps1" for PowerShell
            **kwargs: Additional template variables
        """
        # Default configuration
        config = {
            "project_name": project_name,
            "env_profile_var": f"{project_name.upper().replace('-', '_')}_PROFILE",
            "venv_prefix": project_name.lower(),
            "use_spinner": script_type == "sh",  # PowerShell doesn't need spinner
            "strict_project_check": False,
            "install_siblings": True,
            "sibling_patterns": [],  # No default peers - specified in config
            "special_siblings": [],  # No default peers - specified in config
            "create_log_dir": True,
            "deduplicate_path": True,
            "include_tool_verification": True,  # Enable for wrknv
            "cleanup_logs": True,
            "tools_to_verify": [
                {
                    "name": "Python",
                    "command": "python",
                    "check_type": "command",
                    "var_name": "PYTHON",
                    "version_cmd": "python --version 2>&1",
                },
                {
                    "name": "UV",
                    "command": "uv",
                    "check_type": "command",
                    "var_name": "UV",
                    "version_cmd": "uv --version 2>&1",
                },
                {
                    "name": "wrknv",
                    "command": "wrknv",
                    "check_type": "command",
                    "var_name": "WRKENV",
                    "version_cmd": "wrknv --version 2>&1 || echo 'No version info'",
                },
                {
                    "name": "ibmtf",
                    "command": "ibmtf",
                    "check_type": "XXcommandXX",
                    "var_name": "IBMTF",
                    "version_cmd": "ibmtf version 2>&1 | head -1 || echo 'Not installed'",
                },
                {
                    "name": "tofu",
                    "command": "tofu",
                    "check_type": "command",
                    "var_name": "TOFU",
                    "version_cmd": "tofu version 2>&1 | head -1 || echo 'Not installed'",
                },
            ],
            "useful_commands": [
                {
                    "command": f"{project_name.lower()} --help",
                    "description": f"{project_name} CLI",
                },
                {"command": "wrknv status", "description": "Check tool versions"},
                {
                    "command": "wrknv container status",
                    "description": "Container status",
                },
                {"command": "pytest", "description": "Run tests"},
                {"command": "deactivate", "description": "Exit environment"},
            ],
        }

        # Update with user-provided kwargs
        config.update(kwargs)

        # Select appropriate template and environment
        if script_type == "sh":
            template = self.sh_env.get_template("base.sh.j2")
        elif script_type == "ps1":
            template = self.ps1_env.get_template("base.ps1.j2")
        else:
            raise ValueError(f"Unknown script type: {script_type}")

        # Render and write
        content = template.render(**config)
        output_path.write_text(content)

        # Make executable if it's a shell script
        if script_type == "sh":
            output_path.chmod(output_path.stat().st_mode | 0o111)

    def xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_120(
        self,
        project_name: str,
        output_path: Path,
        script_type: str = "sh",
        **kwargs: Any,
    ) -> None:
        """Generate an environment setup script.

        Args:
            project_name: Name of the project
            output_path: Path to write the script
            script_type: Either "sh" for bash or "ps1" for PowerShell
            **kwargs: Additional template variables
        """
        # Default configuration
        config = {
            "project_name": project_name,
            "env_profile_var": f"{project_name.upper().replace('-', '_')}_PROFILE",
            "venv_prefix": project_name.lower(),
            "use_spinner": script_type == "sh",  # PowerShell doesn't need spinner
            "strict_project_check": False,
            "install_siblings": True,
            "sibling_patterns": [],  # No default peers - specified in config
            "special_siblings": [],  # No default peers - specified in config
            "create_log_dir": True,
            "deduplicate_path": True,
            "include_tool_verification": True,  # Enable for wrknv
            "cleanup_logs": True,
            "tools_to_verify": [
                {
                    "name": "Python",
                    "command": "python",
                    "check_type": "command",
                    "var_name": "PYTHON",
                    "version_cmd": "python --version 2>&1",
                },
                {
                    "name": "UV",
                    "command": "uv",
                    "check_type": "command",
                    "var_name": "UV",
                    "version_cmd": "uv --version 2>&1",
                },
                {
                    "name": "wrknv",
                    "command": "wrknv",
                    "check_type": "command",
                    "var_name": "WRKENV",
                    "version_cmd": "wrknv --version 2>&1 || echo 'No version info'",
                },
                {
                    "name": "ibmtf",
                    "command": "ibmtf",
                    "check_type": "COMMAND",
                    "var_name": "IBMTF",
                    "version_cmd": "ibmtf version 2>&1 | head -1 || echo 'Not installed'",
                },
                {
                    "name": "tofu",
                    "command": "tofu",
                    "check_type": "command",
                    "var_name": "TOFU",
                    "version_cmd": "tofu version 2>&1 | head -1 || echo 'Not installed'",
                },
            ],
            "useful_commands": [
                {
                    "command": f"{project_name.lower()} --help",
                    "description": f"{project_name} CLI",
                },
                {"command": "wrknv status", "description": "Check tool versions"},
                {
                    "command": "wrknv container status",
                    "description": "Container status",
                },
                {"command": "pytest", "description": "Run tests"},
                {"command": "deactivate", "description": "Exit environment"},
            ],
        }

        # Update with user-provided kwargs
        config.update(kwargs)

        # Select appropriate template and environment
        if script_type == "sh":
            template = self.sh_env.get_template("base.sh.j2")
        elif script_type == "ps1":
            template = self.ps1_env.get_template("base.ps1.j2")
        else:
            raise ValueError(f"Unknown script type: {script_type}")

        # Render and write
        content = template.render(**config)
        output_path.write_text(content)

        # Make executable if it's a shell script
        if script_type == "sh":
            output_path.chmod(output_path.stat().st_mode | 0o111)

    def xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_121(
        self,
        project_name: str,
        output_path: Path,
        script_type: str = "sh",
        **kwargs: Any,
    ) -> None:
        """Generate an environment setup script.

        Args:
            project_name: Name of the project
            output_path: Path to write the script
            script_type: Either "sh" for bash or "ps1" for PowerShell
            **kwargs: Additional template variables
        """
        # Default configuration
        config = {
            "project_name": project_name,
            "env_profile_var": f"{project_name.upper().replace('-', '_')}_PROFILE",
            "venv_prefix": project_name.lower(),
            "use_spinner": script_type == "sh",  # PowerShell doesn't need spinner
            "strict_project_check": False,
            "install_siblings": True,
            "sibling_patterns": [],  # No default peers - specified in config
            "special_siblings": [],  # No default peers - specified in config
            "create_log_dir": True,
            "deduplicate_path": True,
            "include_tool_verification": True,  # Enable for wrknv
            "cleanup_logs": True,
            "tools_to_verify": [
                {
                    "name": "Python",
                    "command": "python",
                    "check_type": "command",
                    "var_name": "PYTHON",
                    "version_cmd": "python --version 2>&1",
                },
                {
                    "name": "UV",
                    "command": "uv",
                    "check_type": "command",
                    "var_name": "UV",
                    "version_cmd": "uv --version 2>&1",
                },
                {
                    "name": "wrknv",
                    "command": "wrknv",
                    "check_type": "command",
                    "var_name": "WRKENV",
                    "version_cmd": "wrknv --version 2>&1 || echo 'No version info'",
                },
                {
                    "name": "ibmtf",
                    "command": "ibmtf",
                    "check_type": "command",
                    "XXvar_nameXX": "IBMTF",
                    "version_cmd": "ibmtf version 2>&1 | head -1 || echo 'Not installed'",
                },
                {
                    "name": "tofu",
                    "command": "tofu",
                    "check_type": "command",
                    "var_name": "TOFU",
                    "version_cmd": "tofu version 2>&1 | head -1 || echo 'Not installed'",
                },
            ],
            "useful_commands": [
                {
                    "command": f"{project_name.lower()} --help",
                    "description": f"{project_name} CLI",
                },
                {"command": "wrknv status", "description": "Check tool versions"},
                {
                    "command": "wrknv container status",
                    "description": "Container status",
                },
                {"command": "pytest", "description": "Run tests"},
                {"command": "deactivate", "description": "Exit environment"},
            ],
        }

        # Update with user-provided kwargs
        config.update(kwargs)

        # Select appropriate template and environment
        if script_type == "sh":
            template = self.sh_env.get_template("base.sh.j2")
        elif script_type == "ps1":
            template = self.ps1_env.get_template("base.ps1.j2")
        else:
            raise ValueError(f"Unknown script type: {script_type}")

        # Render and write
        content = template.render(**config)
        output_path.write_text(content)

        # Make executable if it's a shell script
        if script_type == "sh":
            output_path.chmod(output_path.stat().st_mode | 0o111)

    def xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_122(
        self,
        project_name: str,
        output_path: Path,
        script_type: str = "sh",
        **kwargs: Any,
    ) -> None:
        """Generate an environment setup script.

        Args:
            project_name: Name of the project
            output_path: Path to write the script
            script_type: Either "sh" for bash or "ps1" for PowerShell
            **kwargs: Additional template variables
        """
        # Default configuration
        config = {
            "project_name": project_name,
            "env_profile_var": f"{project_name.upper().replace('-', '_')}_PROFILE",
            "venv_prefix": project_name.lower(),
            "use_spinner": script_type == "sh",  # PowerShell doesn't need spinner
            "strict_project_check": False,
            "install_siblings": True,
            "sibling_patterns": [],  # No default peers - specified in config
            "special_siblings": [],  # No default peers - specified in config
            "create_log_dir": True,
            "deduplicate_path": True,
            "include_tool_verification": True,  # Enable for wrknv
            "cleanup_logs": True,
            "tools_to_verify": [
                {
                    "name": "Python",
                    "command": "python",
                    "check_type": "command",
                    "var_name": "PYTHON",
                    "version_cmd": "python --version 2>&1",
                },
                {
                    "name": "UV",
                    "command": "uv",
                    "check_type": "command",
                    "var_name": "UV",
                    "version_cmd": "uv --version 2>&1",
                },
                {
                    "name": "wrknv",
                    "command": "wrknv",
                    "check_type": "command",
                    "var_name": "WRKENV",
                    "version_cmd": "wrknv --version 2>&1 || echo 'No version info'",
                },
                {
                    "name": "ibmtf",
                    "command": "ibmtf",
                    "check_type": "command",
                    "VAR_NAME": "IBMTF",
                    "version_cmd": "ibmtf version 2>&1 | head -1 || echo 'Not installed'",
                },
                {
                    "name": "tofu",
                    "command": "tofu",
                    "check_type": "command",
                    "var_name": "TOFU",
                    "version_cmd": "tofu version 2>&1 | head -1 || echo 'Not installed'",
                },
            ],
            "useful_commands": [
                {
                    "command": f"{project_name.lower()} --help",
                    "description": f"{project_name} CLI",
                },
                {"command": "wrknv status", "description": "Check tool versions"},
                {
                    "command": "wrknv container status",
                    "description": "Container status",
                },
                {"command": "pytest", "description": "Run tests"},
                {"command": "deactivate", "description": "Exit environment"},
            ],
        }

        # Update with user-provided kwargs
        config.update(kwargs)

        # Select appropriate template and environment
        if script_type == "sh":
            template = self.sh_env.get_template("base.sh.j2")
        elif script_type == "ps1":
            template = self.ps1_env.get_template("base.ps1.j2")
        else:
            raise ValueError(f"Unknown script type: {script_type}")

        # Render and write
        content = template.render(**config)
        output_path.write_text(content)

        # Make executable if it's a shell script
        if script_type == "sh":
            output_path.chmod(output_path.stat().st_mode | 0o111)

    def xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_123(
        self,
        project_name: str,
        output_path: Path,
        script_type: str = "sh",
        **kwargs: Any,
    ) -> None:
        """Generate an environment setup script.

        Args:
            project_name: Name of the project
            output_path: Path to write the script
            script_type: Either "sh" for bash or "ps1" for PowerShell
            **kwargs: Additional template variables
        """
        # Default configuration
        config = {
            "project_name": project_name,
            "env_profile_var": f"{project_name.upper().replace('-', '_')}_PROFILE",
            "venv_prefix": project_name.lower(),
            "use_spinner": script_type == "sh",  # PowerShell doesn't need spinner
            "strict_project_check": False,
            "install_siblings": True,
            "sibling_patterns": [],  # No default peers - specified in config
            "special_siblings": [],  # No default peers - specified in config
            "create_log_dir": True,
            "deduplicate_path": True,
            "include_tool_verification": True,  # Enable for wrknv
            "cleanup_logs": True,
            "tools_to_verify": [
                {
                    "name": "Python",
                    "command": "python",
                    "check_type": "command",
                    "var_name": "PYTHON",
                    "version_cmd": "python --version 2>&1",
                },
                {
                    "name": "UV",
                    "command": "uv",
                    "check_type": "command",
                    "var_name": "UV",
                    "version_cmd": "uv --version 2>&1",
                },
                {
                    "name": "wrknv",
                    "command": "wrknv",
                    "check_type": "command",
                    "var_name": "WRKENV",
                    "version_cmd": "wrknv --version 2>&1 || echo 'No version info'",
                },
                {
                    "name": "ibmtf",
                    "command": "ibmtf",
                    "check_type": "command",
                    "var_name": "XXIBMTFXX",
                    "version_cmd": "ibmtf version 2>&1 | head -1 || echo 'Not installed'",
                },
                {
                    "name": "tofu",
                    "command": "tofu",
                    "check_type": "command",
                    "var_name": "TOFU",
                    "version_cmd": "tofu version 2>&1 | head -1 || echo 'Not installed'",
                },
            ],
            "useful_commands": [
                {
                    "command": f"{project_name.lower()} --help",
                    "description": f"{project_name} CLI",
                },
                {"command": "wrknv status", "description": "Check tool versions"},
                {
                    "command": "wrknv container status",
                    "description": "Container status",
                },
                {"command": "pytest", "description": "Run tests"},
                {"command": "deactivate", "description": "Exit environment"},
            ],
        }

        # Update with user-provided kwargs
        config.update(kwargs)

        # Select appropriate template and environment
        if script_type == "sh":
            template = self.sh_env.get_template("base.sh.j2")
        elif script_type == "ps1":
            template = self.ps1_env.get_template("base.ps1.j2")
        else:
            raise ValueError(f"Unknown script type: {script_type}")

        # Render and write
        content = template.render(**config)
        output_path.write_text(content)

        # Make executable if it's a shell script
        if script_type == "sh":
            output_path.chmod(output_path.stat().st_mode | 0o111)

    def xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_124(
        self,
        project_name: str,
        output_path: Path,
        script_type: str = "sh",
        **kwargs: Any,
    ) -> None:
        """Generate an environment setup script.

        Args:
            project_name: Name of the project
            output_path: Path to write the script
            script_type: Either "sh" for bash or "ps1" for PowerShell
            **kwargs: Additional template variables
        """
        # Default configuration
        config = {
            "project_name": project_name,
            "env_profile_var": f"{project_name.upper().replace('-', '_')}_PROFILE",
            "venv_prefix": project_name.lower(),
            "use_spinner": script_type == "sh",  # PowerShell doesn't need spinner
            "strict_project_check": False,
            "install_siblings": True,
            "sibling_patterns": [],  # No default peers - specified in config
            "special_siblings": [],  # No default peers - specified in config
            "create_log_dir": True,
            "deduplicate_path": True,
            "include_tool_verification": True,  # Enable for wrknv
            "cleanup_logs": True,
            "tools_to_verify": [
                {
                    "name": "Python",
                    "command": "python",
                    "check_type": "command",
                    "var_name": "PYTHON",
                    "version_cmd": "python --version 2>&1",
                },
                {
                    "name": "UV",
                    "command": "uv",
                    "check_type": "command",
                    "var_name": "UV",
                    "version_cmd": "uv --version 2>&1",
                },
                {
                    "name": "wrknv",
                    "command": "wrknv",
                    "check_type": "command",
                    "var_name": "WRKENV",
                    "version_cmd": "wrknv --version 2>&1 || echo 'No version info'",
                },
                {
                    "name": "ibmtf",
                    "command": "ibmtf",
                    "check_type": "command",
                    "var_name": "ibmtf",
                    "version_cmd": "ibmtf version 2>&1 | head -1 || echo 'Not installed'",
                },
                {
                    "name": "tofu",
                    "command": "tofu",
                    "check_type": "command",
                    "var_name": "TOFU",
                    "version_cmd": "tofu version 2>&1 | head -1 || echo 'Not installed'",
                },
            ],
            "useful_commands": [
                {
                    "command": f"{project_name.lower()} --help",
                    "description": f"{project_name} CLI",
                },
                {"command": "wrknv status", "description": "Check tool versions"},
                {
                    "command": "wrknv container status",
                    "description": "Container status",
                },
                {"command": "pytest", "description": "Run tests"},
                {"command": "deactivate", "description": "Exit environment"},
            ],
        }

        # Update with user-provided kwargs
        config.update(kwargs)

        # Select appropriate template and environment
        if script_type == "sh":
            template = self.sh_env.get_template("base.sh.j2")
        elif script_type == "ps1":
            template = self.ps1_env.get_template("base.ps1.j2")
        else:
            raise ValueError(f"Unknown script type: {script_type}")

        # Render and write
        content = template.render(**config)
        output_path.write_text(content)

        # Make executable if it's a shell script
        if script_type == "sh":
            output_path.chmod(output_path.stat().st_mode | 0o111)

    def xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_125(
        self,
        project_name: str,
        output_path: Path,
        script_type: str = "sh",
        **kwargs: Any,
    ) -> None:
        """Generate an environment setup script.

        Args:
            project_name: Name of the project
            output_path: Path to write the script
            script_type: Either "sh" for bash or "ps1" for PowerShell
            **kwargs: Additional template variables
        """
        # Default configuration
        config = {
            "project_name": project_name,
            "env_profile_var": f"{project_name.upper().replace('-', '_')}_PROFILE",
            "venv_prefix": project_name.lower(),
            "use_spinner": script_type == "sh",  # PowerShell doesn't need spinner
            "strict_project_check": False,
            "install_siblings": True,
            "sibling_patterns": [],  # No default peers - specified in config
            "special_siblings": [],  # No default peers - specified in config
            "create_log_dir": True,
            "deduplicate_path": True,
            "include_tool_verification": True,  # Enable for wrknv
            "cleanup_logs": True,
            "tools_to_verify": [
                {
                    "name": "Python",
                    "command": "python",
                    "check_type": "command",
                    "var_name": "PYTHON",
                    "version_cmd": "python --version 2>&1",
                },
                {
                    "name": "UV",
                    "command": "uv",
                    "check_type": "command",
                    "var_name": "UV",
                    "version_cmd": "uv --version 2>&1",
                },
                {
                    "name": "wrknv",
                    "command": "wrknv",
                    "check_type": "command",
                    "var_name": "WRKENV",
                    "version_cmd": "wrknv --version 2>&1 || echo 'No version info'",
                },
                {
                    "name": "ibmtf",
                    "command": "ibmtf",
                    "check_type": "command",
                    "var_name": "IBMTF",
                    "XXversion_cmdXX": "ibmtf version 2>&1 | head -1 || echo 'Not installed'",
                },
                {
                    "name": "tofu",
                    "command": "tofu",
                    "check_type": "command",
                    "var_name": "TOFU",
                    "version_cmd": "tofu version 2>&1 | head -1 || echo 'Not installed'",
                },
            ],
            "useful_commands": [
                {
                    "command": f"{project_name.lower()} --help",
                    "description": f"{project_name} CLI",
                },
                {"command": "wrknv status", "description": "Check tool versions"},
                {
                    "command": "wrknv container status",
                    "description": "Container status",
                },
                {"command": "pytest", "description": "Run tests"},
                {"command": "deactivate", "description": "Exit environment"},
            ],
        }

        # Update with user-provided kwargs
        config.update(kwargs)

        # Select appropriate template and environment
        if script_type == "sh":
            template = self.sh_env.get_template("base.sh.j2")
        elif script_type == "ps1":
            template = self.ps1_env.get_template("base.ps1.j2")
        else:
            raise ValueError(f"Unknown script type: {script_type}")

        # Render and write
        content = template.render(**config)
        output_path.write_text(content)

        # Make executable if it's a shell script
        if script_type == "sh":
            output_path.chmod(output_path.stat().st_mode | 0o111)

    def xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_126(
        self,
        project_name: str,
        output_path: Path,
        script_type: str = "sh",
        **kwargs: Any,
    ) -> None:
        """Generate an environment setup script.

        Args:
            project_name: Name of the project
            output_path: Path to write the script
            script_type: Either "sh" for bash or "ps1" for PowerShell
            **kwargs: Additional template variables
        """
        # Default configuration
        config = {
            "project_name": project_name,
            "env_profile_var": f"{project_name.upper().replace('-', '_')}_PROFILE",
            "venv_prefix": project_name.lower(),
            "use_spinner": script_type == "sh",  # PowerShell doesn't need spinner
            "strict_project_check": False,
            "install_siblings": True,
            "sibling_patterns": [],  # No default peers - specified in config
            "special_siblings": [],  # No default peers - specified in config
            "create_log_dir": True,
            "deduplicate_path": True,
            "include_tool_verification": True,  # Enable for wrknv
            "cleanup_logs": True,
            "tools_to_verify": [
                {
                    "name": "Python",
                    "command": "python",
                    "check_type": "command",
                    "var_name": "PYTHON",
                    "version_cmd": "python --version 2>&1",
                },
                {
                    "name": "UV",
                    "command": "uv",
                    "check_type": "command",
                    "var_name": "UV",
                    "version_cmd": "uv --version 2>&1",
                },
                {
                    "name": "wrknv",
                    "command": "wrknv",
                    "check_type": "command",
                    "var_name": "WRKENV",
                    "version_cmd": "wrknv --version 2>&1 || echo 'No version info'",
                },
                {
                    "name": "ibmtf",
                    "command": "ibmtf",
                    "check_type": "command",
                    "var_name": "IBMTF",
                    "VERSION_CMD": "ibmtf version 2>&1 | head -1 || echo 'Not installed'",
                },
                {
                    "name": "tofu",
                    "command": "tofu",
                    "check_type": "command",
                    "var_name": "TOFU",
                    "version_cmd": "tofu version 2>&1 | head -1 || echo 'Not installed'",
                },
            ],
            "useful_commands": [
                {
                    "command": f"{project_name.lower()} --help",
                    "description": f"{project_name} CLI",
                },
                {"command": "wrknv status", "description": "Check tool versions"},
                {
                    "command": "wrknv container status",
                    "description": "Container status",
                },
                {"command": "pytest", "description": "Run tests"},
                {"command": "deactivate", "description": "Exit environment"},
            ],
        }

        # Update with user-provided kwargs
        config.update(kwargs)

        # Select appropriate template and environment
        if script_type == "sh":
            template = self.sh_env.get_template("base.sh.j2")
        elif script_type == "ps1":
            template = self.ps1_env.get_template("base.ps1.j2")
        else:
            raise ValueError(f"Unknown script type: {script_type}")

        # Render and write
        content = template.render(**config)
        output_path.write_text(content)

        # Make executable if it's a shell script
        if script_type == "sh":
            output_path.chmod(output_path.stat().st_mode | 0o111)

    def xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_127(
        self,
        project_name: str,
        output_path: Path,
        script_type: str = "sh",
        **kwargs: Any,
    ) -> None:
        """Generate an environment setup script.

        Args:
            project_name: Name of the project
            output_path: Path to write the script
            script_type: Either "sh" for bash or "ps1" for PowerShell
            **kwargs: Additional template variables
        """
        # Default configuration
        config = {
            "project_name": project_name,
            "env_profile_var": f"{project_name.upper().replace('-', '_')}_PROFILE",
            "venv_prefix": project_name.lower(),
            "use_spinner": script_type == "sh",  # PowerShell doesn't need spinner
            "strict_project_check": False,
            "install_siblings": True,
            "sibling_patterns": [],  # No default peers - specified in config
            "special_siblings": [],  # No default peers - specified in config
            "create_log_dir": True,
            "deduplicate_path": True,
            "include_tool_verification": True,  # Enable for wrknv
            "cleanup_logs": True,
            "tools_to_verify": [
                {
                    "name": "Python",
                    "command": "python",
                    "check_type": "command",
                    "var_name": "PYTHON",
                    "version_cmd": "python --version 2>&1",
                },
                {
                    "name": "UV",
                    "command": "uv",
                    "check_type": "command",
                    "var_name": "UV",
                    "version_cmd": "uv --version 2>&1",
                },
                {
                    "name": "wrknv",
                    "command": "wrknv",
                    "check_type": "command",
                    "var_name": "WRKENV",
                    "version_cmd": "wrknv --version 2>&1 || echo 'No version info'",
                },
                {
                    "name": "ibmtf",
                    "command": "ibmtf",
                    "check_type": "command",
                    "var_name": "IBMTF",
                    "version_cmd": "XXibmtf version 2>&1 | head -1 || echo 'Not installed'XX",
                },
                {
                    "name": "tofu",
                    "command": "tofu",
                    "check_type": "command",
                    "var_name": "TOFU",
                    "version_cmd": "tofu version 2>&1 | head -1 || echo 'Not installed'",
                },
            ],
            "useful_commands": [
                {
                    "command": f"{project_name.lower()} --help",
                    "description": f"{project_name} CLI",
                },
                {"command": "wrknv status", "description": "Check tool versions"},
                {
                    "command": "wrknv container status",
                    "description": "Container status",
                },
                {"command": "pytest", "description": "Run tests"},
                {"command": "deactivate", "description": "Exit environment"},
            ],
        }

        # Update with user-provided kwargs
        config.update(kwargs)

        # Select appropriate template and environment
        if script_type == "sh":
            template = self.sh_env.get_template("base.sh.j2")
        elif script_type == "ps1":
            template = self.ps1_env.get_template("base.ps1.j2")
        else:
            raise ValueError(f"Unknown script type: {script_type}")

        # Render and write
        content = template.render(**config)
        output_path.write_text(content)

        # Make executable if it's a shell script
        if script_type == "sh":
            output_path.chmod(output_path.stat().st_mode | 0o111)

    def xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_128(
        self,
        project_name: str,
        output_path: Path,
        script_type: str = "sh",
        **kwargs: Any,
    ) -> None:
        """Generate an environment setup script.

        Args:
            project_name: Name of the project
            output_path: Path to write the script
            script_type: Either "sh" for bash or "ps1" for PowerShell
            **kwargs: Additional template variables
        """
        # Default configuration
        config = {
            "project_name": project_name,
            "env_profile_var": f"{project_name.upper().replace('-', '_')}_PROFILE",
            "venv_prefix": project_name.lower(),
            "use_spinner": script_type == "sh",  # PowerShell doesn't need spinner
            "strict_project_check": False,
            "install_siblings": True,
            "sibling_patterns": [],  # No default peers - specified in config
            "special_siblings": [],  # No default peers - specified in config
            "create_log_dir": True,
            "deduplicate_path": True,
            "include_tool_verification": True,  # Enable for wrknv
            "cleanup_logs": True,
            "tools_to_verify": [
                {
                    "name": "Python",
                    "command": "python",
                    "check_type": "command",
                    "var_name": "PYTHON",
                    "version_cmd": "python --version 2>&1",
                },
                {
                    "name": "UV",
                    "command": "uv",
                    "check_type": "command",
                    "var_name": "UV",
                    "version_cmd": "uv --version 2>&1",
                },
                {
                    "name": "wrknv",
                    "command": "wrknv",
                    "check_type": "command",
                    "var_name": "WRKENV",
                    "version_cmd": "wrknv --version 2>&1 || echo 'No version info'",
                },
                {
                    "name": "ibmtf",
                    "command": "ibmtf",
                    "check_type": "command",
                    "var_name": "IBMTF",
                    "version_cmd": "ibmtf version 2>&1 | head -1 || echo 'not installed'",
                },
                {
                    "name": "tofu",
                    "command": "tofu",
                    "check_type": "command",
                    "var_name": "TOFU",
                    "version_cmd": "tofu version 2>&1 | head -1 || echo 'Not installed'",
                },
            ],
            "useful_commands": [
                {
                    "command": f"{project_name.lower()} --help",
                    "description": f"{project_name} CLI",
                },
                {"command": "wrknv status", "description": "Check tool versions"},
                {
                    "command": "wrknv container status",
                    "description": "Container status",
                },
                {"command": "pytest", "description": "Run tests"},
                {"command": "deactivate", "description": "Exit environment"},
            ],
        }

        # Update with user-provided kwargs
        config.update(kwargs)

        # Select appropriate template and environment
        if script_type == "sh":
            template = self.sh_env.get_template("base.sh.j2")
        elif script_type == "ps1":
            template = self.ps1_env.get_template("base.ps1.j2")
        else:
            raise ValueError(f"Unknown script type: {script_type}")

        # Render and write
        content = template.render(**config)
        output_path.write_text(content)

        # Make executable if it's a shell script
        if script_type == "sh":
            output_path.chmod(output_path.stat().st_mode | 0o111)

    def xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_129(
        self,
        project_name: str,
        output_path: Path,
        script_type: str = "sh",
        **kwargs: Any,
    ) -> None:
        """Generate an environment setup script.

        Args:
            project_name: Name of the project
            output_path: Path to write the script
            script_type: Either "sh" for bash or "ps1" for PowerShell
            **kwargs: Additional template variables
        """
        # Default configuration
        config = {
            "project_name": project_name,
            "env_profile_var": f"{project_name.upper().replace('-', '_')}_PROFILE",
            "venv_prefix": project_name.lower(),
            "use_spinner": script_type == "sh",  # PowerShell doesn't need spinner
            "strict_project_check": False,
            "install_siblings": True,
            "sibling_patterns": [],  # No default peers - specified in config
            "special_siblings": [],  # No default peers - specified in config
            "create_log_dir": True,
            "deduplicate_path": True,
            "include_tool_verification": True,  # Enable for wrknv
            "cleanup_logs": True,
            "tools_to_verify": [
                {
                    "name": "Python",
                    "command": "python",
                    "check_type": "command",
                    "var_name": "PYTHON",
                    "version_cmd": "python --version 2>&1",
                },
                {
                    "name": "UV",
                    "command": "uv",
                    "check_type": "command",
                    "var_name": "UV",
                    "version_cmd": "uv --version 2>&1",
                },
                {
                    "name": "wrknv",
                    "command": "wrknv",
                    "check_type": "command",
                    "var_name": "WRKENV",
                    "version_cmd": "wrknv --version 2>&1 || echo 'No version info'",
                },
                {
                    "name": "ibmtf",
                    "command": "ibmtf",
                    "check_type": "command",
                    "var_name": "IBMTF",
                    "version_cmd": "IBMTF VERSION 2>&1 | HEAD -1 || ECHO 'NOT INSTALLED'",
                },
                {
                    "name": "tofu",
                    "command": "tofu",
                    "check_type": "command",
                    "var_name": "TOFU",
                    "version_cmd": "tofu version 2>&1 | head -1 || echo 'Not installed'",
                },
            ],
            "useful_commands": [
                {
                    "command": f"{project_name.lower()} --help",
                    "description": f"{project_name} CLI",
                },
                {"command": "wrknv status", "description": "Check tool versions"},
                {
                    "command": "wrknv container status",
                    "description": "Container status",
                },
                {"command": "pytest", "description": "Run tests"},
                {"command": "deactivate", "description": "Exit environment"},
            ],
        }

        # Update with user-provided kwargs
        config.update(kwargs)

        # Select appropriate template and environment
        if script_type == "sh":
            template = self.sh_env.get_template("base.sh.j2")
        elif script_type == "ps1":
            template = self.ps1_env.get_template("base.ps1.j2")
        else:
            raise ValueError(f"Unknown script type: {script_type}")

        # Render and write
        content = template.render(**config)
        output_path.write_text(content)

        # Make executable if it's a shell script
        if script_type == "sh":
            output_path.chmod(output_path.stat().st_mode | 0o111)

    def xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_130(
        self,
        project_name: str,
        output_path: Path,
        script_type: str = "sh",
        **kwargs: Any,
    ) -> None:
        """Generate an environment setup script.

        Args:
            project_name: Name of the project
            output_path: Path to write the script
            script_type: Either "sh" for bash or "ps1" for PowerShell
            **kwargs: Additional template variables
        """
        # Default configuration
        config = {
            "project_name": project_name,
            "env_profile_var": f"{project_name.upper().replace('-', '_')}_PROFILE",
            "venv_prefix": project_name.lower(),
            "use_spinner": script_type == "sh",  # PowerShell doesn't need spinner
            "strict_project_check": False,
            "install_siblings": True,
            "sibling_patterns": [],  # No default peers - specified in config
            "special_siblings": [],  # No default peers - specified in config
            "create_log_dir": True,
            "deduplicate_path": True,
            "include_tool_verification": True,  # Enable for wrknv
            "cleanup_logs": True,
            "tools_to_verify": [
                {
                    "name": "Python",
                    "command": "python",
                    "check_type": "command",
                    "var_name": "PYTHON",
                    "version_cmd": "python --version 2>&1",
                },
                {
                    "name": "UV",
                    "command": "uv",
                    "check_type": "command",
                    "var_name": "UV",
                    "version_cmd": "uv --version 2>&1",
                },
                {
                    "name": "wrknv",
                    "command": "wrknv",
                    "check_type": "command",
                    "var_name": "WRKENV",
                    "version_cmd": "wrknv --version 2>&1 || echo 'No version info'",
                },
                {
                    "name": "ibmtf",
                    "command": "ibmtf",
                    "check_type": "command",
                    "var_name": "IBMTF",
                    "version_cmd": "ibmtf version 2>&1 | head -1 || echo 'Not installed'",
                },
                {
                    "XXnameXX": "tofu",
                    "command": "tofu",
                    "check_type": "command",
                    "var_name": "TOFU",
                    "version_cmd": "tofu version 2>&1 | head -1 || echo 'Not installed'",
                },
            ],
            "useful_commands": [
                {
                    "command": f"{project_name.lower()} --help",
                    "description": f"{project_name} CLI",
                },
                {"command": "wrknv status", "description": "Check tool versions"},
                {
                    "command": "wrknv container status",
                    "description": "Container status",
                },
                {"command": "pytest", "description": "Run tests"},
                {"command": "deactivate", "description": "Exit environment"},
            ],
        }

        # Update with user-provided kwargs
        config.update(kwargs)

        # Select appropriate template and environment
        if script_type == "sh":
            template = self.sh_env.get_template("base.sh.j2")
        elif script_type == "ps1":
            template = self.ps1_env.get_template("base.ps1.j2")
        else:
            raise ValueError(f"Unknown script type: {script_type}")

        # Render and write
        content = template.render(**config)
        output_path.write_text(content)

        # Make executable if it's a shell script
        if script_type == "sh":
            output_path.chmod(output_path.stat().st_mode | 0o111)

    def xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_131(
        self,
        project_name: str,
        output_path: Path,
        script_type: str = "sh",
        **kwargs: Any,
    ) -> None:
        """Generate an environment setup script.

        Args:
            project_name: Name of the project
            output_path: Path to write the script
            script_type: Either "sh" for bash or "ps1" for PowerShell
            **kwargs: Additional template variables
        """
        # Default configuration
        config = {
            "project_name": project_name,
            "env_profile_var": f"{project_name.upper().replace('-', '_')}_PROFILE",
            "venv_prefix": project_name.lower(),
            "use_spinner": script_type == "sh",  # PowerShell doesn't need spinner
            "strict_project_check": False,
            "install_siblings": True,
            "sibling_patterns": [],  # No default peers - specified in config
            "special_siblings": [],  # No default peers - specified in config
            "create_log_dir": True,
            "deduplicate_path": True,
            "include_tool_verification": True,  # Enable for wrknv
            "cleanup_logs": True,
            "tools_to_verify": [
                {
                    "name": "Python",
                    "command": "python",
                    "check_type": "command",
                    "var_name": "PYTHON",
                    "version_cmd": "python --version 2>&1",
                },
                {
                    "name": "UV",
                    "command": "uv",
                    "check_type": "command",
                    "var_name": "UV",
                    "version_cmd": "uv --version 2>&1",
                },
                {
                    "name": "wrknv",
                    "command": "wrknv",
                    "check_type": "command",
                    "var_name": "WRKENV",
                    "version_cmd": "wrknv --version 2>&1 || echo 'No version info'",
                },
                {
                    "name": "ibmtf",
                    "command": "ibmtf",
                    "check_type": "command",
                    "var_name": "IBMTF",
                    "version_cmd": "ibmtf version 2>&1 | head -1 || echo 'Not installed'",
                },
                {
                    "NAME": "tofu",
                    "command": "tofu",
                    "check_type": "command",
                    "var_name": "TOFU",
                    "version_cmd": "tofu version 2>&1 | head -1 || echo 'Not installed'",
                },
            ],
            "useful_commands": [
                {
                    "command": f"{project_name.lower()} --help",
                    "description": f"{project_name} CLI",
                },
                {"command": "wrknv status", "description": "Check tool versions"},
                {
                    "command": "wrknv container status",
                    "description": "Container status",
                },
                {"command": "pytest", "description": "Run tests"},
                {"command": "deactivate", "description": "Exit environment"},
            ],
        }

        # Update with user-provided kwargs
        config.update(kwargs)

        # Select appropriate template and environment
        if script_type == "sh":
            template = self.sh_env.get_template("base.sh.j2")
        elif script_type == "ps1":
            template = self.ps1_env.get_template("base.ps1.j2")
        else:
            raise ValueError(f"Unknown script type: {script_type}")

        # Render and write
        content = template.render(**config)
        output_path.write_text(content)

        # Make executable if it's a shell script
        if script_type == "sh":
            output_path.chmod(output_path.stat().st_mode | 0o111)

    def xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_132(
        self,
        project_name: str,
        output_path: Path,
        script_type: str = "sh",
        **kwargs: Any,
    ) -> None:
        """Generate an environment setup script.

        Args:
            project_name: Name of the project
            output_path: Path to write the script
            script_type: Either "sh" for bash or "ps1" for PowerShell
            **kwargs: Additional template variables
        """
        # Default configuration
        config = {
            "project_name": project_name,
            "env_profile_var": f"{project_name.upper().replace('-', '_')}_PROFILE",
            "venv_prefix": project_name.lower(),
            "use_spinner": script_type == "sh",  # PowerShell doesn't need spinner
            "strict_project_check": False,
            "install_siblings": True,
            "sibling_patterns": [],  # No default peers - specified in config
            "special_siblings": [],  # No default peers - specified in config
            "create_log_dir": True,
            "deduplicate_path": True,
            "include_tool_verification": True,  # Enable for wrknv
            "cleanup_logs": True,
            "tools_to_verify": [
                {
                    "name": "Python",
                    "command": "python",
                    "check_type": "command",
                    "var_name": "PYTHON",
                    "version_cmd": "python --version 2>&1",
                },
                {
                    "name": "UV",
                    "command": "uv",
                    "check_type": "command",
                    "var_name": "UV",
                    "version_cmd": "uv --version 2>&1",
                },
                {
                    "name": "wrknv",
                    "command": "wrknv",
                    "check_type": "command",
                    "var_name": "WRKENV",
                    "version_cmd": "wrknv --version 2>&1 || echo 'No version info'",
                },
                {
                    "name": "ibmtf",
                    "command": "ibmtf",
                    "check_type": "command",
                    "var_name": "IBMTF",
                    "version_cmd": "ibmtf version 2>&1 | head -1 || echo 'Not installed'",
                },
                {
                    "name": "XXtofuXX",
                    "command": "tofu",
                    "check_type": "command",
                    "var_name": "TOFU",
                    "version_cmd": "tofu version 2>&1 | head -1 || echo 'Not installed'",
                },
            ],
            "useful_commands": [
                {
                    "command": f"{project_name.lower()} --help",
                    "description": f"{project_name} CLI",
                },
                {"command": "wrknv status", "description": "Check tool versions"},
                {
                    "command": "wrknv container status",
                    "description": "Container status",
                },
                {"command": "pytest", "description": "Run tests"},
                {"command": "deactivate", "description": "Exit environment"},
            ],
        }

        # Update with user-provided kwargs
        config.update(kwargs)

        # Select appropriate template and environment
        if script_type == "sh":
            template = self.sh_env.get_template("base.sh.j2")
        elif script_type == "ps1":
            template = self.ps1_env.get_template("base.ps1.j2")
        else:
            raise ValueError(f"Unknown script type: {script_type}")

        # Render and write
        content = template.render(**config)
        output_path.write_text(content)

        # Make executable if it's a shell script
        if script_type == "sh":
            output_path.chmod(output_path.stat().st_mode | 0o111)

    def xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_133(
        self,
        project_name: str,
        output_path: Path,
        script_type: str = "sh",
        **kwargs: Any,
    ) -> None:
        """Generate an environment setup script.

        Args:
            project_name: Name of the project
            output_path: Path to write the script
            script_type: Either "sh" for bash or "ps1" for PowerShell
            **kwargs: Additional template variables
        """
        # Default configuration
        config = {
            "project_name": project_name,
            "env_profile_var": f"{project_name.upper().replace('-', '_')}_PROFILE",
            "venv_prefix": project_name.lower(),
            "use_spinner": script_type == "sh",  # PowerShell doesn't need spinner
            "strict_project_check": False,
            "install_siblings": True,
            "sibling_patterns": [],  # No default peers - specified in config
            "special_siblings": [],  # No default peers - specified in config
            "create_log_dir": True,
            "deduplicate_path": True,
            "include_tool_verification": True,  # Enable for wrknv
            "cleanup_logs": True,
            "tools_to_verify": [
                {
                    "name": "Python",
                    "command": "python",
                    "check_type": "command",
                    "var_name": "PYTHON",
                    "version_cmd": "python --version 2>&1",
                },
                {
                    "name": "UV",
                    "command": "uv",
                    "check_type": "command",
                    "var_name": "UV",
                    "version_cmd": "uv --version 2>&1",
                },
                {
                    "name": "wrknv",
                    "command": "wrknv",
                    "check_type": "command",
                    "var_name": "WRKENV",
                    "version_cmd": "wrknv --version 2>&1 || echo 'No version info'",
                },
                {
                    "name": "ibmtf",
                    "command": "ibmtf",
                    "check_type": "command",
                    "var_name": "IBMTF",
                    "version_cmd": "ibmtf version 2>&1 | head -1 || echo 'Not installed'",
                },
                {
                    "name": "TOFU",
                    "command": "tofu",
                    "check_type": "command",
                    "var_name": "TOFU",
                    "version_cmd": "tofu version 2>&1 | head -1 || echo 'Not installed'",
                },
            ],
            "useful_commands": [
                {
                    "command": f"{project_name.lower()} --help",
                    "description": f"{project_name} CLI",
                },
                {"command": "wrknv status", "description": "Check tool versions"},
                {
                    "command": "wrknv container status",
                    "description": "Container status",
                },
                {"command": "pytest", "description": "Run tests"},
                {"command": "deactivate", "description": "Exit environment"},
            ],
        }

        # Update with user-provided kwargs
        config.update(kwargs)

        # Select appropriate template and environment
        if script_type == "sh":
            template = self.sh_env.get_template("base.sh.j2")
        elif script_type == "ps1":
            template = self.ps1_env.get_template("base.ps1.j2")
        else:
            raise ValueError(f"Unknown script type: {script_type}")

        # Render and write
        content = template.render(**config)
        output_path.write_text(content)

        # Make executable if it's a shell script
        if script_type == "sh":
            output_path.chmod(output_path.stat().st_mode | 0o111)

    def xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_134(
        self,
        project_name: str,
        output_path: Path,
        script_type: str = "sh",
        **kwargs: Any,
    ) -> None:
        """Generate an environment setup script.

        Args:
            project_name: Name of the project
            output_path: Path to write the script
            script_type: Either "sh" for bash or "ps1" for PowerShell
            **kwargs: Additional template variables
        """
        # Default configuration
        config = {
            "project_name": project_name,
            "env_profile_var": f"{project_name.upper().replace('-', '_')}_PROFILE",
            "venv_prefix": project_name.lower(),
            "use_spinner": script_type == "sh",  # PowerShell doesn't need spinner
            "strict_project_check": False,
            "install_siblings": True,
            "sibling_patterns": [],  # No default peers - specified in config
            "special_siblings": [],  # No default peers - specified in config
            "create_log_dir": True,
            "deduplicate_path": True,
            "include_tool_verification": True,  # Enable for wrknv
            "cleanup_logs": True,
            "tools_to_verify": [
                {
                    "name": "Python",
                    "command": "python",
                    "check_type": "command",
                    "var_name": "PYTHON",
                    "version_cmd": "python --version 2>&1",
                },
                {
                    "name": "UV",
                    "command": "uv",
                    "check_type": "command",
                    "var_name": "UV",
                    "version_cmd": "uv --version 2>&1",
                },
                {
                    "name": "wrknv",
                    "command": "wrknv",
                    "check_type": "command",
                    "var_name": "WRKENV",
                    "version_cmd": "wrknv --version 2>&1 || echo 'No version info'",
                },
                {
                    "name": "ibmtf",
                    "command": "ibmtf",
                    "check_type": "command",
                    "var_name": "IBMTF",
                    "version_cmd": "ibmtf version 2>&1 | head -1 || echo 'Not installed'",
                },
                {
                    "name": "tofu",
                    "XXcommandXX": "tofu",
                    "check_type": "command",
                    "var_name": "TOFU",
                    "version_cmd": "tofu version 2>&1 | head -1 || echo 'Not installed'",
                },
            ],
            "useful_commands": [
                {
                    "command": f"{project_name.lower()} --help",
                    "description": f"{project_name} CLI",
                },
                {"command": "wrknv status", "description": "Check tool versions"},
                {
                    "command": "wrknv container status",
                    "description": "Container status",
                },
                {"command": "pytest", "description": "Run tests"},
                {"command": "deactivate", "description": "Exit environment"},
            ],
        }

        # Update with user-provided kwargs
        config.update(kwargs)

        # Select appropriate template and environment
        if script_type == "sh":
            template = self.sh_env.get_template("base.sh.j2")
        elif script_type == "ps1":
            template = self.ps1_env.get_template("base.ps1.j2")
        else:
            raise ValueError(f"Unknown script type: {script_type}")

        # Render and write
        content = template.render(**config)
        output_path.write_text(content)

        # Make executable if it's a shell script
        if script_type == "sh":
            output_path.chmod(output_path.stat().st_mode | 0o111)

    def xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_135(
        self,
        project_name: str,
        output_path: Path,
        script_type: str = "sh",
        **kwargs: Any,
    ) -> None:
        """Generate an environment setup script.

        Args:
            project_name: Name of the project
            output_path: Path to write the script
            script_type: Either "sh" for bash or "ps1" for PowerShell
            **kwargs: Additional template variables
        """
        # Default configuration
        config = {
            "project_name": project_name,
            "env_profile_var": f"{project_name.upper().replace('-', '_')}_PROFILE",
            "venv_prefix": project_name.lower(),
            "use_spinner": script_type == "sh",  # PowerShell doesn't need spinner
            "strict_project_check": False,
            "install_siblings": True,
            "sibling_patterns": [],  # No default peers - specified in config
            "special_siblings": [],  # No default peers - specified in config
            "create_log_dir": True,
            "deduplicate_path": True,
            "include_tool_verification": True,  # Enable for wrknv
            "cleanup_logs": True,
            "tools_to_verify": [
                {
                    "name": "Python",
                    "command": "python",
                    "check_type": "command",
                    "var_name": "PYTHON",
                    "version_cmd": "python --version 2>&1",
                },
                {
                    "name": "UV",
                    "command": "uv",
                    "check_type": "command",
                    "var_name": "UV",
                    "version_cmd": "uv --version 2>&1",
                },
                {
                    "name": "wrknv",
                    "command": "wrknv",
                    "check_type": "command",
                    "var_name": "WRKENV",
                    "version_cmd": "wrknv --version 2>&1 || echo 'No version info'",
                },
                {
                    "name": "ibmtf",
                    "command": "ibmtf",
                    "check_type": "command",
                    "var_name": "IBMTF",
                    "version_cmd": "ibmtf version 2>&1 | head -1 || echo 'Not installed'",
                },
                {
                    "name": "tofu",
                    "COMMAND": "tofu",
                    "check_type": "command",
                    "var_name": "TOFU",
                    "version_cmd": "tofu version 2>&1 | head -1 || echo 'Not installed'",
                },
            ],
            "useful_commands": [
                {
                    "command": f"{project_name.lower()} --help",
                    "description": f"{project_name} CLI",
                },
                {"command": "wrknv status", "description": "Check tool versions"},
                {
                    "command": "wrknv container status",
                    "description": "Container status",
                },
                {"command": "pytest", "description": "Run tests"},
                {"command": "deactivate", "description": "Exit environment"},
            ],
        }

        # Update with user-provided kwargs
        config.update(kwargs)

        # Select appropriate template and environment
        if script_type == "sh":
            template = self.sh_env.get_template("base.sh.j2")
        elif script_type == "ps1":
            template = self.ps1_env.get_template("base.ps1.j2")
        else:
            raise ValueError(f"Unknown script type: {script_type}")

        # Render and write
        content = template.render(**config)
        output_path.write_text(content)

        # Make executable if it's a shell script
        if script_type == "sh":
            output_path.chmod(output_path.stat().st_mode | 0o111)

    def xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_136(
        self,
        project_name: str,
        output_path: Path,
        script_type: str = "sh",
        **kwargs: Any,
    ) -> None:
        """Generate an environment setup script.

        Args:
            project_name: Name of the project
            output_path: Path to write the script
            script_type: Either "sh" for bash or "ps1" for PowerShell
            **kwargs: Additional template variables
        """
        # Default configuration
        config = {
            "project_name": project_name,
            "env_profile_var": f"{project_name.upper().replace('-', '_')}_PROFILE",
            "venv_prefix": project_name.lower(),
            "use_spinner": script_type == "sh",  # PowerShell doesn't need spinner
            "strict_project_check": False,
            "install_siblings": True,
            "sibling_patterns": [],  # No default peers - specified in config
            "special_siblings": [],  # No default peers - specified in config
            "create_log_dir": True,
            "deduplicate_path": True,
            "include_tool_verification": True,  # Enable for wrknv
            "cleanup_logs": True,
            "tools_to_verify": [
                {
                    "name": "Python",
                    "command": "python",
                    "check_type": "command",
                    "var_name": "PYTHON",
                    "version_cmd": "python --version 2>&1",
                },
                {
                    "name": "UV",
                    "command": "uv",
                    "check_type": "command",
                    "var_name": "UV",
                    "version_cmd": "uv --version 2>&1",
                },
                {
                    "name": "wrknv",
                    "command": "wrknv",
                    "check_type": "command",
                    "var_name": "WRKENV",
                    "version_cmd": "wrknv --version 2>&1 || echo 'No version info'",
                },
                {
                    "name": "ibmtf",
                    "command": "ibmtf",
                    "check_type": "command",
                    "var_name": "IBMTF",
                    "version_cmd": "ibmtf version 2>&1 | head -1 || echo 'Not installed'",
                },
                {
                    "name": "tofu",
                    "command": "XXtofuXX",
                    "check_type": "command",
                    "var_name": "TOFU",
                    "version_cmd": "tofu version 2>&1 | head -1 || echo 'Not installed'",
                },
            ],
            "useful_commands": [
                {
                    "command": f"{project_name.lower()} --help",
                    "description": f"{project_name} CLI",
                },
                {"command": "wrknv status", "description": "Check tool versions"},
                {
                    "command": "wrknv container status",
                    "description": "Container status",
                },
                {"command": "pytest", "description": "Run tests"},
                {"command": "deactivate", "description": "Exit environment"},
            ],
        }

        # Update with user-provided kwargs
        config.update(kwargs)

        # Select appropriate template and environment
        if script_type == "sh":
            template = self.sh_env.get_template("base.sh.j2")
        elif script_type == "ps1":
            template = self.ps1_env.get_template("base.ps1.j2")
        else:
            raise ValueError(f"Unknown script type: {script_type}")

        # Render and write
        content = template.render(**config)
        output_path.write_text(content)

        # Make executable if it's a shell script
        if script_type == "sh":
            output_path.chmod(output_path.stat().st_mode | 0o111)

    def xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_137(
        self,
        project_name: str,
        output_path: Path,
        script_type: str = "sh",
        **kwargs: Any,
    ) -> None:
        """Generate an environment setup script.

        Args:
            project_name: Name of the project
            output_path: Path to write the script
            script_type: Either "sh" for bash or "ps1" for PowerShell
            **kwargs: Additional template variables
        """
        # Default configuration
        config = {
            "project_name": project_name,
            "env_profile_var": f"{project_name.upper().replace('-', '_')}_PROFILE",
            "venv_prefix": project_name.lower(),
            "use_spinner": script_type == "sh",  # PowerShell doesn't need spinner
            "strict_project_check": False,
            "install_siblings": True,
            "sibling_patterns": [],  # No default peers - specified in config
            "special_siblings": [],  # No default peers - specified in config
            "create_log_dir": True,
            "deduplicate_path": True,
            "include_tool_verification": True,  # Enable for wrknv
            "cleanup_logs": True,
            "tools_to_verify": [
                {
                    "name": "Python",
                    "command": "python",
                    "check_type": "command",
                    "var_name": "PYTHON",
                    "version_cmd": "python --version 2>&1",
                },
                {
                    "name": "UV",
                    "command": "uv",
                    "check_type": "command",
                    "var_name": "UV",
                    "version_cmd": "uv --version 2>&1",
                },
                {
                    "name": "wrknv",
                    "command": "wrknv",
                    "check_type": "command",
                    "var_name": "WRKENV",
                    "version_cmd": "wrknv --version 2>&1 || echo 'No version info'",
                },
                {
                    "name": "ibmtf",
                    "command": "ibmtf",
                    "check_type": "command",
                    "var_name": "IBMTF",
                    "version_cmd": "ibmtf version 2>&1 | head -1 || echo 'Not installed'",
                },
                {
                    "name": "tofu",
                    "command": "TOFU",
                    "check_type": "command",
                    "var_name": "TOFU",
                    "version_cmd": "tofu version 2>&1 | head -1 || echo 'Not installed'",
                },
            ],
            "useful_commands": [
                {
                    "command": f"{project_name.lower()} --help",
                    "description": f"{project_name} CLI",
                },
                {"command": "wrknv status", "description": "Check tool versions"},
                {
                    "command": "wrknv container status",
                    "description": "Container status",
                },
                {"command": "pytest", "description": "Run tests"},
                {"command": "deactivate", "description": "Exit environment"},
            ],
        }

        # Update with user-provided kwargs
        config.update(kwargs)

        # Select appropriate template and environment
        if script_type == "sh":
            template = self.sh_env.get_template("base.sh.j2")
        elif script_type == "ps1":
            template = self.ps1_env.get_template("base.ps1.j2")
        else:
            raise ValueError(f"Unknown script type: {script_type}")

        # Render and write
        content = template.render(**config)
        output_path.write_text(content)

        # Make executable if it's a shell script
        if script_type == "sh":
            output_path.chmod(output_path.stat().st_mode | 0o111)

    def xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_138(
        self,
        project_name: str,
        output_path: Path,
        script_type: str = "sh",
        **kwargs: Any,
    ) -> None:
        """Generate an environment setup script.

        Args:
            project_name: Name of the project
            output_path: Path to write the script
            script_type: Either "sh" for bash or "ps1" for PowerShell
            **kwargs: Additional template variables
        """
        # Default configuration
        config = {
            "project_name": project_name,
            "env_profile_var": f"{project_name.upper().replace('-', '_')}_PROFILE",
            "venv_prefix": project_name.lower(),
            "use_spinner": script_type == "sh",  # PowerShell doesn't need spinner
            "strict_project_check": False,
            "install_siblings": True,
            "sibling_patterns": [],  # No default peers - specified in config
            "special_siblings": [],  # No default peers - specified in config
            "create_log_dir": True,
            "deduplicate_path": True,
            "include_tool_verification": True,  # Enable for wrknv
            "cleanup_logs": True,
            "tools_to_verify": [
                {
                    "name": "Python",
                    "command": "python",
                    "check_type": "command",
                    "var_name": "PYTHON",
                    "version_cmd": "python --version 2>&1",
                },
                {
                    "name": "UV",
                    "command": "uv",
                    "check_type": "command",
                    "var_name": "UV",
                    "version_cmd": "uv --version 2>&1",
                },
                {
                    "name": "wrknv",
                    "command": "wrknv",
                    "check_type": "command",
                    "var_name": "WRKENV",
                    "version_cmd": "wrknv --version 2>&1 || echo 'No version info'",
                },
                {
                    "name": "ibmtf",
                    "command": "ibmtf",
                    "check_type": "command",
                    "var_name": "IBMTF",
                    "version_cmd": "ibmtf version 2>&1 | head -1 || echo 'Not installed'",
                },
                {
                    "name": "tofu",
                    "command": "tofu",
                    "XXcheck_typeXX": "command",
                    "var_name": "TOFU",
                    "version_cmd": "tofu version 2>&1 | head -1 || echo 'Not installed'",
                },
            ],
            "useful_commands": [
                {
                    "command": f"{project_name.lower()} --help",
                    "description": f"{project_name} CLI",
                },
                {"command": "wrknv status", "description": "Check tool versions"},
                {
                    "command": "wrknv container status",
                    "description": "Container status",
                },
                {"command": "pytest", "description": "Run tests"},
                {"command": "deactivate", "description": "Exit environment"},
            ],
        }

        # Update with user-provided kwargs
        config.update(kwargs)

        # Select appropriate template and environment
        if script_type == "sh":
            template = self.sh_env.get_template("base.sh.j2")
        elif script_type == "ps1":
            template = self.ps1_env.get_template("base.ps1.j2")
        else:
            raise ValueError(f"Unknown script type: {script_type}")

        # Render and write
        content = template.render(**config)
        output_path.write_text(content)

        # Make executable if it's a shell script
        if script_type == "sh":
            output_path.chmod(output_path.stat().st_mode | 0o111)

    def xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_139(
        self,
        project_name: str,
        output_path: Path,
        script_type: str = "sh",
        **kwargs: Any,
    ) -> None:
        """Generate an environment setup script.

        Args:
            project_name: Name of the project
            output_path: Path to write the script
            script_type: Either "sh" for bash or "ps1" for PowerShell
            **kwargs: Additional template variables
        """
        # Default configuration
        config = {
            "project_name": project_name,
            "env_profile_var": f"{project_name.upper().replace('-', '_')}_PROFILE",
            "venv_prefix": project_name.lower(),
            "use_spinner": script_type == "sh",  # PowerShell doesn't need spinner
            "strict_project_check": False,
            "install_siblings": True,
            "sibling_patterns": [],  # No default peers - specified in config
            "special_siblings": [],  # No default peers - specified in config
            "create_log_dir": True,
            "deduplicate_path": True,
            "include_tool_verification": True,  # Enable for wrknv
            "cleanup_logs": True,
            "tools_to_verify": [
                {
                    "name": "Python",
                    "command": "python",
                    "check_type": "command",
                    "var_name": "PYTHON",
                    "version_cmd": "python --version 2>&1",
                },
                {
                    "name": "UV",
                    "command": "uv",
                    "check_type": "command",
                    "var_name": "UV",
                    "version_cmd": "uv --version 2>&1",
                },
                {
                    "name": "wrknv",
                    "command": "wrknv",
                    "check_type": "command",
                    "var_name": "WRKENV",
                    "version_cmd": "wrknv --version 2>&1 || echo 'No version info'",
                },
                {
                    "name": "ibmtf",
                    "command": "ibmtf",
                    "check_type": "command",
                    "var_name": "IBMTF",
                    "version_cmd": "ibmtf version 2>&1 | head -1 || echo 'Not installed'",
                },
                {
                    "name": "tofu",
                    "command": "tofu",
                    "CHECK_TYPE": "command",
                    "var_name": "TOFU",
                    "version_cmd": "tofu version 2>&1 | head -1 || echo 'Not installed'",
                },
            ],
            "useful_commands": [
                {
                    "command": f"{project_name.lower()} --help",
                    "description": f"{project_name} CLI",
                },
                {"command": "wrknv status", "description": "Check tool versions"},
                {
                    "command": "wrknv container status",
                    "description": "Container status",
                },
                {"command": "pytest", "description": "Run tests"},
                {"command": "deactivate", "description": "Exit environment"},
            ],
        }

        # Update with user-provided kwargs
        config.update(kwargs)

        # Select appropriate template and environment
        if script_type == "sh":
            template = self.sh_env.get_template("base.sh.j2")
        elif script_type == "ps1":
            template = self.ps1_env.get_template("base.ps1.j2")
        else:
            raise ValueError(f"Unknown script type: {script_type}")

        # Render and write
        content = template.render(**config)
        output_path.write_text(content)

        # Make executable if it's a shell script
        if script_type == "sh":
            output_path.chmod(output_path.stat().st_mode | 0o111)

    def xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_140(
        self,
        project_name: str,
        output_path: Path,
        script_type: str = "sh",
        **kwargs: Any,
    ) -> None:
        """Generate an environment setup script.

        Args:
            project_name: Name of the project
            output_path: Path to write the script
            script_type: Either "sh" for bash or "ps1" for PowerShell
            **kwargs: Additional template variables
        """
        # Default configuration
        config = {
            "project_name": project_name,
            "env_profile_var": f"{project_name.upper().replace('-', '_')}_PROFILE",
            "venv_prefix": project_name.lower(),
            "use_spinner": script_type == "sh",  # PowerShell doesn't need spinner
            "strict_project_check": False,
            "install_siblings": True,
            "sibling_patterns": [],  # No default peers - specified in config
            "special_siblings": [],  # No default peers - specified in config
            "create_log_dir": True,
            "deduplicate_path": True,
            "include_tool_verification": True,  # Enable for wrknv
            "cleanup_logs": True,
            "tools_to_verify": [
                {
                    "name": "Python",
                    "command": "python",
                    "check_type": "command",
                    "var_name": "PYTHON",
                    "version_cmd": "python --version 2>&1",
                },
                {
                    "name": "UV",
                    "command": "uv",
                    "check_type": "command",
                    "var_name": "UV",
                    "version_cmd": "uv --version 2>&1",
                },
                {
                    "name": "wrknv",
                    "command": "wrknv",
                    "check_type": "command",
                    "var_name": "WRKENV",
                    "version_cmd": "wrknv --version 2>&1 || echo 'No version info'",
                },
                {
                    "name": "ibmtf",
                    "command": "ibmtf",
                    "check_type": "command",
                    "var_name": "IBMTF",
                    "version_cmd": "ibmtf version 2>&1 | head -1 || echo 'Not installed'",
                },
                {
                    "name": "tofu",
                    "command": "tofu",
                    "check_type": "XXcommandXX",
                    "var_name": "TOFU",
                    "version_cmd": "tofu version 2>&1 | head -1 || echo 'Not installed'",
                },
            ],
            "useful_commands": [
                {
                    "command": f"{project_name.lower()} --help",
                    "description": f"{project_name} CLI",
                },
                {"command": "wrknv status", "description": "Check tool versions"},
                {
                    "command": "wrknv container status",
                    "description": "Container status",
                },
                {"command": "pytest", "description": "Run tests"},
                {"command": "deactivate", "description": "Exit environment"},
            ],
        }

        # Update with user-provided kwargs
        config.update(kwargs)

        # Select appropriate template and environment
        if script_type == "sh":
            template = self.sh_env.get_template("base.sh.j2")
        elif script_type == "ps1":
            template = self.ps1_env.get_template("base.ps1.j2")
        else:
            raise ValueError(f"Unknown script type: {script_type}")

        # Render and write
        content = template.render(**config)
        output_path.write_text(content)

        # Make executable if it's a shell script
        if script_type == "sh":
            output_path.chmod(output_path.stat().st_mode | 0o111)

    def xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_141(
        self,
        project_name: str,
        output_path: Path,
        script_type: str = "sh",
        **kwargs: Any,
    ) -> None:
        """Generate an environment setup script.

        Args:
            project_name: Name of the project
            output_path: Path to write the script
            script_type: Either "sh" for bash or "ps1" for PowerShell
            **kwargs: Additional template variables
        """
        # Default configuration
        config = {
            "project_name": project_name,
            "env_profile_var": f"{project_name.upper().replace('-', '_')}_PROFILE",
            "venv_prefix": project_name.lower(),
            "use_spinner": script_type == "sh",  # PowerShell doesn't need spinner
            "strict_project_check": False,
            "install_siblings": True,
            "sibling_patterns": [],  # No default peers - specified in config
            "special_siblings": [],  # No default peers - specified in config
            "create_log_dir": True,
            "deduplicate_path": True,
            "include_tool_verification": True,  # Enable for wrknv
            "cleanup_logs": True,
            "tools_to_verify": [
                {
                    "name": "Python",
                    "command": "python",
                    "check_type": "command",
                    "var_name": "PYTHON",
                    "version_cmd": "python --version 2>&1",
                },
                {
                    "name": "UV",
                    "command": "uv",
                    "check_type": "command",
                    "var_name": "UV",
                    "version_cmd": "uv --version 2>&1",
                },
                {
                    "name": "wrknv",
                    "command": "wrknv",
                    "check_type": "command",
                    "var_name": "WRKENV",
                    "version_cmd": "wrknv --version 2>&1 || echo 'No version info'",
                },
                {
                    "name": "ibmtf",
                    "command": "ibmtf",
                    "check_type": "command",
                    "var_name": "IBMTF",
                    "version_cmd": "ibmtf version 2>&1 | head -1 || echo 'Not installed'",
                },
                {
                    "name": "tofu",
                    "command": "tofu",
                    "check_type": "COMMAND",
                    "var_name": "TOFU",
                    "version_cmd": "tofu version 2>&1 | head -1 || echo 'Not installed'",
                },
            ],
            "useful_commands": [
                {
                    "command": f"{project_name.lower()} --help",
                    "description": f"{project_name} CLI",
                },
                {"command": "wrknv status", "description": "Check tool versions"},
                {
                    "command": "wrknv container status",
                    "description": "Container status",
                },
                {"command": "pytest", "description": "Run tests"},
                {"command": "deactivate", "description": "Exit environment"},
            ],
        }

        # Update with user-provided kwargs
        config.update(kwargs)

        # Select appropriate template and environment
        if script_type == "sh":
            template = self.sh_env.get_template("base.sh.j2")
        elif script_type == "ps1":
            template = self.ps1_env.get_template("base.ps1.j2")
        else:
            raise ValueError(f"Unknown script type: {script_type}")

        # Render and write
        content = template.render(**config)
        output_path.write_text(content)

        # Make executable if it's a shell script
        if script_type == "sh":
            output_path.chmod(output_path.stat().st_mode | 0o111)

    def xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_142(
        self,
        project_name: str,
        output_path: Path,
        script_type: str = "sh",
        **kwargs: Any,
    ) -> None:
        """Generate an environment setup script.

        Args:
            project_name: Name of the project
            output_path: Path to write the script
            script_type: Either "sh" for bash or "ps1" for PowerShell
            **kwargs: Additional template variables
        """
        # Default configuration
        config = {
            "project_name": project_name,
            "env_profile_var": f"{project_name.upper().replace('-', '_')}_PROFILE",
            "venv_prefix": project_name.lower(),
            "use_spinner": script_type == "sh",  # PowerShell doesn't need spinner
            "strict_project_check": False,
            "install_siblings": True,
            "sibling_patterns": [],  # No default peers - specified in config
            "special_siblings": [],  # No default peers - specified in config
            "create_log_dir": True,
            "deduplicate_path": True,
            "include_tool_verification": True,  # Enable for wrknv
            "cleanup_logs": True,
            "tools_to_verify": [
                {
                    "name": "Python",
                    "command": "python",
                    "check_type": "command",
                    "var_name": "PYTHON",
                    "version_cmd": "python --version 2>&1",
                },
                {
                    "name": "UV",
                    "command": "uv",
                    "check_type": "command",
                    "var_name": "UV",
                    "version_cmd": "uv --version 2>&1",
                },
                {
                    "name": "wrknv",
                    "command": "wrknv",
                    "check_type": "command",
                    "var_name": "WRKENV",
                    "version_cmd": "wrknv --version 2>&1 || echo 'No version info'",
                },
                {
                    "name": "ibmtf",
                    "command": "ibmtf",
                    "check_type": "command",
                    "var_name": "IBMTF",
                    "version_cmd": "ibmtf version 2>&1 | head -1 || echo 'Not installed'",
                },
                {
                    "name": "tofu",
                    "command": "tofu",
                    "check_type": "command",
                    "XXvar_nameXX": "TOFU",
                    "version_cmd": "tofu version 2>&1 | head -1 || echo 'Not installed'",
                },
            ],
            "useful_commands": [
                {
                    "command": f"{project_name.lower()} --help",
                    "description": f"{project_name} CLI",
                },
                {"command": "wrknv status", "description": "Check tool versions"},
                {
                    "command": "wrknv container status",
                    "description": "Container status",
                },
                {"command": "pytest", "description": "Run tests"},
                {"command": "deactivate", "description": "Exit environment"},
            ],
        }

        # Update with user-provided kwargs
        config.update(kwargs)

        # Select appropriate template and environment
        if script_type == "sh":
            template = self.sh_env.get_template("base.sh.j2")
        elif script_type == "ps1":
            template = self.ps1_env.get_template("base.ps1.j2")
        else:
            raise ValueError(f"Unknown script type: {script_type}")

        # Render and write
        content = template.render(**config)
        output_path.write_text(content)

        # Make executable if it's a shell script
        if script_type == "sh":
            output_path.chmod(output_path.stat().st_mode | 0o111)

    def xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_143(
        self,
        project_name: str,
        output_path: Path,
        script_type: str = "sh",
        **kwargs: Any,
    ) -> None:
        """Generate an environment setup script.

        Args:
            project_name: Name of the project
            output_path: Path to write the script
            script_type: Either "sh" for bash or "ps1" for PowerShell
            **kwargs: Additional template variables
        """
        # Default configuration
        config = {
            "project_name": project_name,
            "env_profile_var": f"{project_name.upper().replace('-', '_')}_PROFILE",
            "venv_prefix": project_name.lower(),
            "use_spinner": script_type == "sh",  # PowerShell doesn't need spinner
            "strict_project_check": False,
            "install_siblings": True,
            "sibling_patterns": [],  # No default peers - specified in config
            "special_siblings": [],  # No default peers - specified in config
            "create_log_dir": True,
            "deduplicate_path": True,
            "include_tool_verification": True,  # Enable for wrknv
            "cleanup_logs": True,
            "tools_to_verify": [
                {
                    "name": "Python",
                    "command": "python",
                    "check_type": "command",
                    "var_name": "PYTHON",
                    "version_cmd": "python --version 2>&1",
                },
                {
                    "name": "UV",
                    "command": "uv",
                    "check_type": "command",
                    "var_name": "UV",
                    "version_cmd": "uv --version 2>&1",
                },
                {
                    "name": "wrknv",
                    "command": "wrknv",
                    "check_type": "command",
                    "var_name": "WRKENV",
                    "version_cmd": "wrknv --version 2>&1 || echo 'No version info'",
                },
                {
                    "name": "ibmtf",
                    "command": "ibmtf",
                    "check_type": "command",
                    "var_name": "IBMTF",
                    "version_cmd": "ibmtf version 2>&1 | head -1 || echo 'Not installed'",
                },
                {
                    "name": "tofu",
                    "command": "tofu",
                    "check_type": "command",
                    "VAR_NAME": "TOFU",
                    "version_cmd": "tofu version 2>&1 | head -1 || echo 'Not installed'",
                },
            ],
            "useful_commands": [
                {
                    "command": f"{project_name.lower()} --help",
                    "description": f"{project_name} CLI",
                },
                {"command": "wrknv status", "description": "Check tool versions"},
                {
                    "command": "wrknv container status",
                    "description": "Container status",
                },
                {"command": "pytest", "description": "Run tests"},
                {"command": "deactivate", "description": "Exit environment"},
            ],
        }

        # Update with user-provided kwargs
        config.update(kwargs)

        # Select appropriate template and environment
        if script_type == "sh":
            template = self.sh_env.get_template("base.sh.j2")
        elif script_type == "ps1":
            template = self.ps1_env.get_template("base.ps1.j2")
        else:
            raise ValueError(f"Unknown script type: {script_type}")

        # Render and write
        content = template.render(**config)
        output_path.write_text(content)

        # Make executable if it's a shell script
        if script_type == "sh":
            output_path.chmod(output_path.stat().st_mode | 0o111)

    def xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_144(
        self,
        project_name: str,
        output_path: Path,
        script_type: str = "sh",
        **kwargs: Any,
    ) -> None:
        """Generate an environment setup script.

        Args:
            project_name: Name of the project
            output_path: Path to write the script
            script_type: Either "sh" for bash or "ps1" for PowerShell
            **kwargs: Additional template variables
        """
        # Default configuration
        config = {
            "project_name": project_name,
            "env_profile_var": f"{project_name.upper().replace('-', '_')}_PROFILE",
            "venv_prefix": project_name.lower(),
            "use_spinner": script_type == "sh",  # PowerShell doesn't need spinner
            "strict_project_check": False,
            "install_siblings": True,
            "sibling_patterns": [],  # No default peers - specified in config
            "special_siblings": [],  # No default peers - specified in config
            "create_log_dir": True,
            "deduplicate_path": True,
            "include_tool_verification": True,  # Enable for wrknv
            "cleanup_logs": True,
            "tools_to_verify": [
                {
                    "name": "Python",
                    "command": "python",
                    "check_type": "command",
                    "var_name": "PYTHON",
                    "version_cmd": "python --version 2>&1",
                },
                {
                    "name": "UV",
                    "command": "uv",
                    "check_type": "command",
                    "var_name": "UV",
                    "version_cmd": "uv --version 2>&1",
                },
                {
                    "name": "wrknv",
                    "command": "wrknv",
                    "check_type": "command",
                    "var_name": "WRKENV",
                    "version_cmd": "wrknv --version 2>&1 || echo 'No version info'",
                },
                {
                    "name": "ibmtf",
                    "command": "ibmtf",
                    "check_type": "command",
                    "var_name": "IBMTF",
                    "version_cmd": "ibmtf version 2>&1 | head -1 || echo 'Not installed'",
                },
                {
                    "name": "tofu",
                    "command": "tofu",
                    "check_type": "command",
                    "var_name": "XXTOFUXX",
                    "version_cmd": "tofu version 2>&1 | head -1 || echo 'Not installed'",
                },
            ],
            "useful_commands": [
                {
                    "command": f"{project_name.lower()} --help",
                    "description": f"{project_name} CLI",
                },
                {"command": "wrknv status", "description": "Check tool versions"},
                {
                    "command": "wrknv container status",
                    "description": "Container status",
                },
                {"command": "pytest", "description": "Run tests"},
                {"command": "deactivate", "description": "Exit environment"},
            ],
        }

        # Update with user-provided kwargs
        config.update(kwargs)

        # Select appropriate template and environment
        if script_type == "sh":
            template = self.sh_env.get_template("base.sh.j2")
        elif script_type == "ps1":
            template = self.ps1_env.get_template("base.ps1.j2")
        else:
            raise ValueError(f"Unknown script type: {script_type}")

        # Render and write
        content = template.render(**config)
        output_path.write_text(content)

        # Make executable if it's a shell script
        if script_type == "sh":
            output_path.chmod(output_path.stat().st_mode | 0o111)

    def xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_145(
        self,
        project_name: str,
        output_path: Path,
        script_type: str = "sh",
        **kwargs: Any,
    ) -> None:
        """Generate an environment setup script.

        Args:
            project_name: Name of the project
            output_path: Path to write the script
            script_type: Either "sh" for bash or "ps1" for PowerShell
            **kwargs: Additional template variables
        """
        # Default configuration
        config = {
            "project_name": project_name,
            "env_profile_var": f"{project_name.upper().replace('-', '_')}_PROFILE",
            "venv_prefix": project_name.lower(),
            "use_spinner": script_type == "sh",  # PowerShell doesn't need spinner
            "strict_project_check": False,
            "install_siblings": True,
            "sibling_patterns": [],  # No default peers - specified in config
            "special_siblings": [],  # No default peers - specified in config
            "create_log_dir": True,
            "deduplicate_path": True,
            "include_tool_verification": True,  # Enable for wrknv
            "cleanup_logs": True,
            "tools_to_verify": [
                {
                    "name": "Python",
                    "command": "python",
                    "check_type": "command",
                    "var_name": "PYTHON",
                    "version_cmd": "python --version 2>&1",
                },
                {
                    "name": "UV",
                    "command": "uv",
                    "check_type": "command",
                    "var_name": "UV",
                    "version_cmd": "uv --version 2>&1",
                },
                {
                    "name": "wrknv",
                    "command": "wrknv",
                    "check_type": "command",
                    "var_name": "WRKENV",
                    "version_cmd": "wrknv --version 2>&1 || echo 'No version info'",
                },
                {
                    "name": "ibmtf",
                    "command": "ibmtf",
                    "check_type": "command",
                    "var_name": "IBMTF",
                    "version_cmd": "ibmtf version 2>&1 | head -1 || echo 'Not installed'",
                },
                {
                    "name": "tofu",
                    "command": "tofu",
                    "check_type": "command",
                    "var_name": "tofu",
                    "version_cmd": "tofu version 2>&1 | head -1 || echo 'Not installed'",
                },
            ],
            "useful_commands": [
                {
                    "command": f"{project_name.lower()} --help",
                    "description": f"{project_name} CLI",
                },
                {"command": "wrknv status", "description": "Check tool versions"},
                {
                    "command": "wrknv container status",
                    "description": "Container status",
                },
                {"command": "pytest", "description": "Run tests"},
                {"command": "deactivate", "description": "Exit environment"},
            ],
        }

        # Update with user-provided kwargs
        config.update(kwargs)

        # Select appropriate template and environment
        if script_type == "sh":
            template = self.sh_env.get_template("base.sh.j2")
        elif script_type == "ps1":
            template = self.ps1_env.get_template("base.ps1.j2")
        else:
            raise ValueError(f"Unknown script type: {script_type}")

        # Render and write
        content = template.render(**config)
        output_path.write_text(content)

        # Make executable if it's a shell script
        if script_type == "sh":
            output_path.chmod(output_path.stat().st_mode | 0o111)

    def xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_146(
        self,
        project_name: str,
        output_path: Path,
        script_type: str = "sh",
        **kwargs: Any,
    ) -> None:
        """Generate an environment setup script.

        Args:
            project_name: Name of the project
            output_path: Path to write the script
            script_type: Either "sh" for bash or "ps1" for PowerShell
            **kwargs: Additional template variables
        """
        # Default configuration
        config = {
            "project_name": project_name,
            "env_profile_var": f"{project_name.upper().replace('-', '_')}_PROFILE",
            "venv_prefix": project_name.lower(),
            "use_spinner": script_type == "sh",  # PowerShell doesn't need spinner
            "strict_project_check": False,
            "install_siblings": True,
            "sibling_patterns": [],  # No default peers - specified in config
            "special_siblings": [],  # No default peers - specified in config
            "create_log_dir": True,
            "deduplicate_path": True,
            "include_tool_verification": True,  # Enable for wrknv
            "cleanup_logs": True,
            "tools_to_verify": [
                {
                    "name": "Python",
                    "command": "python",
                    "check_type": "command",
                    "var_name": "PYTHON",
                    "version_cmd": "python --version 2>&1",
                },
                {
                    "name": "UV",
                    "command": "uv",
                    "check_type": "command",
                    "var_name": "UV",
                    "version_cmd": "uv --version 2>&1",
                },
                {
                    "name": "wrknv",
                    "command": "wrknv",
                    "check_type": "command",
                    "var_name": "WRKENV",
                    "version_cmd": "wrknv --version 2>&1 || echo 'No version info'",
                },
                {
                    "name": "ibmtf",
                    "command": "ibmtf",
                    "check_type": "command",
                    "var_name": "IBMTF",
                    "version_cmd": "ibmtf version 2>&1 | head -1 || echo 'Not installed'",
                },
                {
                    "name": "tofu",
                    "command": "tofu",
                    "check_type": "command",
                    "var_name": "TOFU",
                    "XXversion_cmdXX": "tofu version 2>&1 | head -1 || echo 'Not installed'",
                },
            ],
            "useful_commands": [
                {
                    "command": f"{project_name.lower()} --help",
                    "description": f"{project_name} CLI",
                },
                {"command": "wrknv status", "description": "Check tool versions"},
                {
                    "command": "wrknv container status",
                    "description": "Container status",
                },
                {"command": "pytest", "description": "Run tests"},
                {"command": "deactivate", "description": "Exit environment"},
            ],
        }

        # Update with user-provided kwargs
        config.update(kwargs)

        # Select appropriate template and environment
        if script_type == "sh":
            template = self.sh_env.get_template("base.sh.j2")
        elif script_type == "ps1":
            template = self.ps1_env.get_template("base.ps1.j2")
        else:
            raise ValueError(f"Unknown script type: {script_type}")

        # Render and write
        content = template.render(**config)
        output_path.write_text(content)

        # Make executable if it's a shell script
        if script_type == "sh":
            output_path.chmod(output_path.stat().st_mode | 0o111)

    def xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_147(
        self,
        project_name: str,
        output_path: Path,
        script_type: str = "sh",
        **kwargs: Any,
    ) -> None:
        """Generate an environment setup script.

        Args:
            project_name: Name of the project
            output_path: Path to write the script
            script_type: Either "sh" for bash or "ps1" for PowerShell
            **kwargs: Additional template variables
        """
        # Default configuration
        config = {
            "project_name": project_name,
            "env_profile_var": f"{project_name.upper().replace('-', '_')}_PROFILE",
            "venv_prefix": project_name.lower(),
            "use_spinner": script_type == "sh",  # PowerShell doesn't need spinner
            "strict_project_check": False,
            "install_siblings": True,
            "sibling_patterns": [],  # No default peers - specified in config
            "special_siblings": [],  # No default peers - specified in config
            "create_log_dir": True,
            "deduplicate_path": True,
            "include_tool_verification": True,  # Enable for wrknv
            "cleanup_logs": True,
            "tools_to_verify": [
                {
                    "name": "Python",
                    "command": "python",
                    "check_type": "command",
                    "var_name": "PYTHON",
                    "version_cmd": "python --version 2>&1",
                },
                {
                    "name": "UV",
                    "command": "uv",
                    "check_type": "command",
                    "var_name": "UV",
                    "version_cmd": "uv --version 2>&1",
                },
                {
                    "name": "wrknv",
                    "command": "wrknv",
                    "check_type": "command",
                    "var_name": "WRKENV",
                    "version_cmd": "wrknv --version 2>&1 || echo 'No version info'",
                },
                {
                    "name": "ibmtf",
                    "command": "ibmtf",
                    "check_type": "command",
                    "var_name": "IBMTF",
                    "version_cmd": "ibmtf version 2>&1 | head -1 || echo 'Not installed'",
                },
                {
                    "name": "tofu",
                    "command": "tofu",
                    "check_type": "command",
                    "var_name": "TOFU",
                    "VERSION_CMD": "tofu version 2>&1 | head -1 || echo 'Not installed'",
                },
            ],
            "useful_commands": [
                {
                    "command": f"{project_name.lower()} --help",
                    "description": f"{project_name} CLI",
                },
                {"command": "wrknv status", "description": "Check tool versions"},
                {
                    "command": "wrknv container status",
                    "description": "Container status",
                },
                {"command": "pytest", "description": "Run tests"},
                {"command": "deactivate", "description": "Exit environment"},
            ],
        }

        # Update with user-provided kwargs
        config.update(kwargs)

        # Select appropriate template and environment
        if script_type == "sh":
            template = self.sh_env.get_template("base.sh.j2")
        elif script_type == "ps1":
            template = self.ps1_env.get_template("base.ps1.j2")
        else:
            raise ValueError(f"Unknown script type: {script_type}")

        # Render and write
        content = template.render(**config)
        output_path.write_text(content)

        # Make executable if it's a shell script
        if script_type == "sh":
            output_path.chmod(output_path.stat().st_mode | 0o111)

    def xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_148(
        self,
        project_name: str,
        output_path: Path,
        script_type: str = "sh",
        **kwargs: Any,
    ) -> None:
        """Generate an environment setup script.

        Args:
            project_name: Name of the project
            output_path: Path to write the script
            script_type: Either "sh" for bash or "ps1" for PowerShell
            **kwargs: Additional template variables
        """
        # Default configuration
        config = {
            "project_name": project_name,
            "env_profile_var": f"{project_name.upper().replace('-', '_')}_PROFILE",
            "venv_prefix": project_name.lower(),
            "use_spinner": script_type == "sh",  # PowerShell doesn't need spinner
            "strict_project_check": False,
            "install_siblings": True,
            "sibling_patterns": [],  # No default peers - specified in config
            "special_siblings": [],  # No default peers - specified in config
            "create_log_dir": True,
            "deduplicate_path": True,
            "include_tool_verification": True,  # Enable for wrknv
            "cleanup_logs": True,
            "tools_to_verify": [
                {
                    "name": "Python",
                    "command": "python",
                    "check_type": "command",
                    "var_name": "PYTHON",
                    "version_cmd": "python --version 2>&1",
                },
                {
                    "name": "UV",
                    "command": "uv",
                    "check_type": "command",
                    "var_name": "UV",
                    "version_cmd": "uv --version 2>&1",
                },
                {
                    "name": "wrknv",
                    "command": "wrknv",
                    "check_type": "command",
                    "var_name": "WRKENV",
                    "version_cmd": "wrknv --version 2>&1 || echo 'No version info'",
                },
                {
                    "name": "ibmtf",
                    "command": "ibmtf",
                    "check_type": "command",
                    "var_name": "IBMTF",
                    "version_cmd": "ibmtf version 2>&1 | head -1 || echo 'Not installed'",
                },
                {
                    "name": "tofu",
                    "command": "tofu",
                    "check_type": "command",
                    "var_name": "TOFU",
                    "version_cmd": "XXtofu version 2>&1 | head -1 || echo 'Not installed'XX",
                },
            ],
            "useful_commands": [
                {
                    "command": f"{project_name.lower()} --help",
                    "description": f"{project_name} CLI",
                },
                {"command": "wrknv status", "description": "Check tool versions"},
                {
                    "command": "wrknv container status",
                    "description": "Container status",
                },
                {"command": "pytest", "description": "Run tests"},
                {"command": "deactivate", "description": "Exit environment"},
            ],
        }

        # Update with user-provided kwargs
        config.update(kwargs)

        # Select appropriate template and environment
        if script_type == "sh":
            template = self.sh_env.get_template("base.sh.j2")
        elif script_type == "ps1":
            template = self.ps1_env.get_template("base.ps1.j2")
        else:
            raise ValueError(f"Unknown script type: {script_type}")

        # Render and write
        content = template.render(**config)
        output_path.write_text(content)

        # Make executable if it's a shell script
        if script_type == "sh":
            output_path.chmod(output_path.stat().st_mode | 0o111)

    def xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_149(
        self,
        project_name: str,
        output_path: Path,
        script_type: str = "sh",
        **kwargs: Any,
    ) -> None:
        """Generate an environment setup script.

        Args:
            project_name: Name of the project
            output_path: Path to write the script
            script_type: Either "sh" for bash or "ps1" for PowerShell
            **kwargs: Additional template variables
        """
        # Default configuration
        config = {
            "project_name": project_name,
            "env_profile_var": f"{project_name.upper().replace('-', '_')}_PROFILE",
            "venv_prefix": project_name.lower(),
            "use_spinner": script_type == "sh",  # PowerShell doesn't need spinner
            "strict_project_check": False,
            "install_siblings": True,
            "sibling_patterns": [],  # No default peers - specified in config
            "special_siblings": [],  # No default peers - specified in config
            "create_log_dir": True,
            "deduplicate_path": True,
            "include_tool_verification": True,  # Enable for wrknv
            "cleanup_logs": True,
            "tools_to_verify": [
                {
                    "name": "Python",
                    "command": "python",
                    "check_type": "command",
                    "var_name": "PYTHON",
                    "version_cmd": "python --version 2>&1",
                },
                {
                    "name": "UV",
                    "command": "uv",
                    "check_type": "command",
                    "var_name": "UV",
                    "version_cmd": "uv --version 2>&1",
                },
                {
                    "name": "wrknv",
                    "command": "wrknv",
                    "check_type": "command",
                    "var_name": "WRKENV",
                    "version_cmd": "wrknv --version 2>&1 || echo 'No version info'",
                },
                {
                    "name": "ibmtf",
                    "command": "ibmtf",
                    "check_type": "command",
                    "var_name": "IBMTF",
                    "version_cmd": "ibmtf version 2>&1 | head -1 || echo 'Not installed'",
                },
                {
                    "name": "tofu",
                    "command": "tofu",
                    "check_type": "command",
                    "var_name": "TOFU",
                    "version_cmd": "tofu version 2>&1 | head -1 || echo 'not installed'",
                },
            ],
            "useful_commands": [
                {
                    "command": f"{project_name.lower()} --help",
                    "description": f"{project_name} CLI",
                },
                {"command": "wrknv status", "description": "Check tool versions"},
                {
                    "command": "wrknv container status",
                    "description": "Container status",
                },
                {"command": "pytest", "description": "Run tests"},
                {"command": "deactivate", "description": "Exit environment"},
            ],
        }

        # Update with user-provided kwargs
        config.update(kwargs)

        # Select appropriate template and environment
        if script_type == "sh":
            template = self.sh_env.get_template("base.sh.j2")
        elif script_type == "ps1":
            template = self.ps1_env.get_template("base.ps1.j2")
        else:
            raise ValueError(f"Unknown script type: {script_type}")

        # Render and write
        content = template.render(**config)
        output_path.write_text(content)

        # Make executable if it's a shell script
        if script_type == "sh":
            output_path.chmod(output_path.stat().st_mode | 0o111)

    def xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_150(
        self,
        project_name: str,
        output_path: Path,
        script_type: str = "sh",
        **kwargs: Any,
    ) -> None:
        """Generate an environment setup script.

        Args:
            project_name: Name of the project
            output_path: Path to write the script
            script_type: Either "sh" for bash or "ps1" for PowerShell
            **kwargs: Additional template variables
        """
        # Default configuration
        config = {
            "project_name": project_name,
            "env_profile_var": f"{project_name.upper().replace('-', '_')}_PROFILE",
            "venv_prefix": project_name.lower(),
            "use_spinner": script_type == "sh",  # PowerShell doesn't need spinner
            "strict_project_check": False,
            "install_siblings": True,
            "sibling_patterns": [],  # No default peers - specified in config
            "special_siblings": [],  # No default peers - specified in config
            "create_log_dir": True,
            "deduplicate_path": True,
            "include_tool_verification": True,  # Enable for wrknv
            "cleanup_logs": True,
            "tools_to_verify": [
                {
                    "name": "Python",
                    "command": "python",
                    "check_type": "command",
                    "var_name": "PYTHON",
                    "version_cmd": "python --version 2>&1",
                },
                {
                    "name": "UV",
                    "command": "uv",
                    "check_type": "command",
                    "var_name": "UV",
                    "version_cmd": "uv --version 2>&1",
                },
                {
                    "name": "wrknv",
                    "command": "wrknv",
                    "check_type": "command",
                    "var_name": "WRKENV",
                    "version_cmd": "wrknv --version 2>&1 || echo 'No version info'",
                },
                {
                    "name": "ibmtf",
                    "command": "ibmtf",
                    "check_type": "command",
                    "var_name": "IBMTF",
                    "version_cmd": "ibmtf version 2>&1 | head -1 || echo 'Not installed'",
                },
                {
                    "name": "tofu",
                    "command": "tofu",
                    "check_type": "command",
                    "var_name": "TOFU",
                    "version_cmd": "TOFU VERSION 2>&1 | HEAD -1 || ECHO 'NOT INSTALLED'",
                },
            ],
            "useful_commands": [
                {
                    "command": f"{project_name.lower()} --help",
                    "description": f"{project_name} CLI",
                },
                {"command": "wrknv status", "description": "Check tool versions"},
                {
                    "command": "wrknv container status",
                    "description": "Container status",
                },
                {"command": "pytest", "description": "Run tests"},
                {"command": "deactivate", "description": "Exit environment"},
            ],
        }

        # Update with user-provided kwargs
        config.update(kwargs)

        # Select appropriate template and environment
        if script_type == "sh":
            template = self.sh_env.get_template("base.sh.j2")
        elif script_type == "ps1":
            template = self.ps1_env.get_template("base.ps1.j2")
        else:
            raise ValueError(f"Unknown script type: {script_type}")

        # Render and write
        content = template.render(**config)
        output_path.write_text(content)

        # Make executable if it's a shell script
        if script_type == "sh":
            output_path.chmod(output_path.stat().st_mode | 0o111)

    def xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_151(
        self,
        project_name: str,
        output_path: Path,
        script_type: str = "sh",
        **kwargs: Any,
    ) -> None:
        """Generate an environment setup script.

        Args:
            project_name: Name of the project
            output_path: Path to write the script
            script_type: Either "sh" for bash or "ps1" for PowerShell
            **kwargs: Additional template variables
        """
        # Default configuration
        config = {
            "project_name": project_name,
            "env_profile_var": f"{project_name.upper().replace('-', '_')}_PROFILE",
            "venv_prefix": project_name.lower(),
            "use_spinner": script_type == "sh",  # PowerShell doesn't need spinner
            "strict_project_check": False,
            "install_siblings": True,
            "sibling_patterns": [],  # No default peers - specified in config
            "special_siblings": [],  # No default peers - specified in config
            "create_log_dir": True,
            "deduplicate_path": True,
            "include_tool_verification": True,  # Enable for wrknv
            "cleanup_logs": True,
            "tools_to_verify": [
                {
                    "name": "Python",
                    "command": "python",
                    "check_type": "command",
                    "var_name": "PYTHON",
                    "version_cmd": "python --version 2>&1",
                },
                {
                    "name": "UV",
                    "command": "uv",
                    "check_type": "command",
                    "var_name": "UV",
                    "version_cmd": "uv --version 2>&1",
                },
                {
                    "name": "wrknv",
                    "command": "wrknv",
                    "check_type": "command",
                    "var_name": "WRKENV",
                    "version_cmd": "wrknv --version 2>&1 || echo 'No version info'",
                },
                {
                    "name": "ibmtf",
                    "command": "ibmtf",
                    "check_type": "command",
                    "var_name": "IBMTF",
                    "version_cmd": "ibmtf version 2>&1 | head -1 || echo 'Not installed'",
                },
                {
                    "name": "tofu",
                    "command": "tofu",
                    "check_type": "command",
                    "var_name": "TOFU",
                    "version_cmd": "tofu version 2>&1 | head -1 || echo 'Not installed'",
                },
            ],
            "XXuseful_commandsXX": [
                {
                    "command": f"{project_name.lower()} --help",
                    "description": f"{project_name} CLI",
                },
                {"command": "wrknv status", "description": "Check tool versions"},
                {
                    "command": "wrknv container status",
                    "description": "Container status",
                },
                {"command": "pytest", "description": "Run tests"},
                {"command": "deactivate", "description": "Exit environment"},
            ],
        }

        # Update with user-provided kwargs
        config.update(kwargs)

        # Select appropriate template and environment
        if script_type == "sh":
            template = self.sh_env.get_template("base.sh.j2")
        elif script_type == "ps1":
            template = self.ps1_env.get_template("base.ps1.j2")
        else:
            raise ValueError(f"Unknown script type: {script_type}")

        # Render and write
        content = template.render(**config)
        output_path.write_text(content)

        # Make executable if it's a shell script
        if script_type == "sh":
            output_path.chmod(output_path.stat().st_mode | 0o111)

    def xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_152(
        self,
        project_name: str,
        output_path: Path,
        script_type: str = "sh",
        **kwargs: Any,
    ) -> None:
        """Generate an environment setup script.

        Args:
            project_name: Name of the project
            output_path: Path to write the script
            script_type: Either "sh" for bash or "ps1" for PowerShell
            **kwargs: Additional template variables
        """
        # Default configuration
        config = {
            "project_name": project_name,
            "env_profile_var": f"{project_name.upper().replace('-', '_')}_PROFILE",
            "venv_prefix": project_name.lower(),
            "use_spinner": script_type == "sh",  # PowerShell doesn't need spinner
            "strict_project_check": False,
            "install_siblings": True,
            "sibling_patterns": [],  # No default peers - specified in config
            "special_siblings": [],  # No default peers - specified in config
            "create_log_dir": True,
            "deduplicate_path": True,
            "include_tool_verification": True,  # Enable for wrknv
            "cleanup_logs": True,
            "tools_to_verify": [
                {
                    "name": "Python",
                    "command": "python",
                    "check_type": "command",
                    "var_name": "PYTHON",
                    "version_cmd": "python --version 2>&1",
                },
                {
                    "name": "UV",
                    "command": "uv",
                    "check_type": "command",
                    "var_name": "UV",
                    "version_cmd": "uv --version 2>&1",
                },
                {
                    "name": "wrknv",
                    "command": "wrknv",
                    "check_type": "command",
                    "var_name": "WRKENV",
                    "version_cmd": "wrknv --version 2>&1 || echo 'No version info'",
                },
                {
                    "name": "ibmtf",
                    "command": "ibmtf",
                    "check_type": "command",
                    "var_name": "IBMTF",
                    "version_cmd": "ibmtf version 2>&1 | head -1 || echo 'Not installed'",
                },
                {
                    "name": "tofu",
                    "command": "tofu",
                    "check_type": "command",
                    "var_name": "TOFU",
                    "version_cmd": "tofu version 2>&1 | head -1 || echo 'Not installed'",
                },
            ],
            "USEFUL_COMMANDS": [
                {
                    "command": f"{project_name.lower()} --help",
                    "description": f"{project_name} CLI",
                },
                {"command": "wrknv status", "description": "Check tool versions"},
                {
                    "command": "wrknv container status",
                    "description": "Container status",
                },
                {"command": "pytest", "description": "Run tests"},
                {"command": "deactivate", "description": "Exit environment"},
            ],
        }

        # Update with user-provided kwargs
        config.update(kwargs)

        # Select appropriate template and environment
        if script_type == "sh":
            template = self.sh_env.get_template("base.sh.j2")
        elif script_type == "ps1":
            template = self.ps1_env.get_template("base.ps1.j2")
        else:
            raise ValueError(f"Unknown script type: {script_type}")

        # Render and write
        content = template.render(**config)
        output_path.write_text(content)

        # Make executable if it's a shell script
        if script_type == "sh":
            output_path.chmod(output_path.stat().st_mode | 0o111)

    def xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_153(
        self,
        project_name: str,
        output_path: Path,
        script_type: str = "sh",
        **kwargs: Any,
    ) -> None:
        """Generate an environment setup script.

        Args:
            project_name: Name of the project
            output_path: Path to write the script
            script_type: Either "sh" for bash or "ps1" for PowerShell
            **kwargs: Additional template variables
        """
        # Default configuration
        config = {
            "project_name": project_name,
            "env_profile_var": f"{project_name.upper().replace('-', '_')}_PROFILE",
            "venv_prefix": project_name.lower(),
            "use_spinner": script_type == "sh",  # PowerShell doesn't need spinner
            "strict_project_check": False,
            "install_siblings": True,
            "sibling_patterns": [],  # No default peers - specified in config
            "special_siblings": [],  # No default peers - specified in config
            "create_log_dir": True,
            "deduplicate_path": True,
            "include_tool_verification": True,  # Enable for wrknv
            "cleanup_logs": True,
            "tools_to_verify": [
                {
                    "name": "Python",
                    "command": "python",
                    "check_type": "command",
                    "var_name": "PYTHON",
                    "version_cmd": "python --version 2>&1",
                },
                {
                    "name": "UV",
                    "command": "uv",
                    "check_type": "command",
                    "var_name": "UV",
                    "version_cmd": "uv --version 2>&1",
                },
                {
                    "name": "wrknv",
                    "command": "wrknv",
                    "check_type": "command",
                    "var_name": "WRKENV",
                    "version_cmd": "wrknv --version 2>&1 || echo 'No version info'",
                },
                {
                    "name": "ibmtf",
                    "command": "ibmtf",
                    "check_type": "command",
                    "var_name": "IBMTF",
                    "version_cmd": "ibmtf version 2>&1 | head -1 || echo 'Not installed'",
                },
                {
                    "name": "tofu",
                    "command": "tofu",
                    "check_type": "command",
                    "var_name": "TOFU",
                    "version_cmd": "tofu version 2>&1 | head -1 || echo 'Not installed'",
                },
            ],
            "useful_commands": [
                {
                    "XXcommandXX": f"{project_name.lower()} --help",
                    "description": f"{project_name} CLI",
                },
                {"command": "wrknv status", "description": "Check tool versions"},
                {
                    "command": "wrknv container status",
                    "description": "Container status",
                },
                {"command": "pytest", "description": "Run tests"},
                {"command": "deactivate", "description": "Exit environment"},
            ],
        }

        # Update with user-provided kwargs
        config.update(kwargs)

        # Select appropriate template and environment
        if script_type == "sh":
            template = self.sh_env.get_template("base.sh.j2")
        elif script_type == "ps1":
            template = self.ps1_env.get_template("base.ps1.j2")
        else:
            raise ValueError(f"Unknown script type: {script_type}")

        # Render and write
        content = template.render(**config)
        output_path.write_text(content)

        # Make executable if it's a shell script
        if script_type == "sh":
            output_path.chmod(output_path.stat().st_mode | 0o111)

    def xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_154(
        self,
        project_name: str,
        output_path: Path,
        script_type: str = "sh",
        **kwargs: Any,
    ) -> None:
        """Generate an environment setup script.

        Args:
            project_name: Name of the project
            output_path: Path to write the script
            script_type: Either "sh" for bash or "ps1" for PowerShell
            **kwargs: Additional template variables
        """
        # Default configuration
        config = {
            "project_name": project_name,
            "env_profile_var": f"{project_name.upper().replace('-', '_')}_PROFILE",
            "venv_prefix": project_name.lower(),
            "use_spinner": script_type == "sh",  # PowerShell doesn't need spinner
            "strict_project_check": False,
            "install_siblings": True,
            "sibling_patterns": [],  # No default peers - specified in config
            "special_siblings": [],  # No default peers - specified in config
            "create_log_dir": True,
            "deduplicate_path": True,
            "include_tool_verification": True,  # Enable for wrknv
            "cleanup_logs": True,
            "tools_to_verify": [
                {
                    "name": "Python",
                    "command": "python",
                    "check_type": "command",
                    "var_name": "PYTHON",
                    "version_cmd": "python --version 2>&1",
                },
                {
                    "name": "UV",
                    "command": "uv",
                    "check_type": "command",
                    "var_name": "UV",
                    "version_cmd": "uv --version 2>&1",
                },
                {
                    "name": "wrknv",
                    "command": "wrknv",
                    "check_type": "command",
                    "var_name": "WRKENV",
                    "version_cmd": "wrknv --version 2>&1 || echo 'No version info'",
                },
                {
                    "name": "ibmtf",
                    "command": "ibmtf",
                    "check_type": "command",
                    "var_name": "IBMTF",
                    "version_cmd": "ibmtf version 2>&1 | head -1 || echo 'Not installed'",
                },
                {
                    "name": "tofu",
                    "command": "tofu",
                    "check_type": "command",
                    "var_name": "TOFU",
                    "version_cmd": "tofu version 2>&1 | head -1 || echo 'Not installed'",
                },
            ],
            "useful_commands": [
                {
                    "COMMAND": f"{project_name.lower()} --help",
                    "description": f"{project_name} CLI",
                },
                {"command": "wrknv status", "description": "Check tool versions"},
                {
                    "command": "wrknv container status",
                    "description": "Container status",
                },
                {"command": "pytest", "description": "Run tests"},
                {"command": "deactivate", "description": "Exit environment"},
            ],
        }

        # Update with user-provided kwargs
        config.update(kwargs)

        # Select appropriate template and environment
        if script_type == "sh":
            template = self.sh_env.get_template("base.sh.j2")
        elif script_type == "ps1":
            template = self.ps1_env.get_template("base.ps1.j2")
        else:
            raise ValueError(f"Unknown script type: {script_type}")

        # Render and write
        content = template.render(**config)
        output_path.write_text(content)

        # Make executable if it's a shell script
        if script_type == "sh":
            output_path.chmod(output_path.stat().st_mode | 0o111)

    def xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_155(
        self,
        project_name: str,
        output_path: Path,
        script_type: str = "sh",
        **kwargs: Any,
    ) -> None:
        """Generate an environment setup script.

        Args:
            project_name: Name of the project
            output_path: Path to write the script
            script_type: Either "sh" for bash or "ps1" for PowerShell
            **kwargs: Additional template variables
        """
        # Default configuration
        config = {
            "project_name": project_name,
            "env_profile_var": f"{project_name.upper().replace('-', '_')}_PROFILE",
            "venv_prefix": project_name.lower(),
            "use_spinner": script_type == "sh",  # PowerShell doesn't need spinner
            "strict_project_check": False,
            "install_siblings": True,
            "sibling_patterns": [],  # No default peers - specified in config
            "special_siblings": [],  # No default peers - specified in config
            "create_log_dir": True,
            "deduplicate_path": True,
            "include_tool_verification": True,  # Enable for wrknv
            "cleanup_logs": True,
            "tools_to_verify": [
                {
                    "name": "Python",
                    "command": "python",
                    "check_type": "command",
                    "var_name": "PYTHON",
                    "version_cmd": "python --version 2>&1",
                },
                {
                    "name": "UV",
                    "command": "uv",
                    "check_type": "command",
                    "var_name": "UV",
                    "version_cmd": "uv --version 2>&1",
                },
                {
                    "name": "wrknv",
                    "command": "wrknv",
                    "check_type": "command",
                    "var_name": "WRKENV",
                    "version_cmd": "wrknv --version 2>&1 || echo 'No version info'",
                },
                {
                    "name": "ibmtf",
                    "command": "ibmtf",
                    "check_type": "command",
                    "var_name": "IBMTF",
                    "version_cmd": "ibmtf version 2>&1 | head -1 || echo 'Not installed'",
                },
                {
                    "name": "tofu",
                    "command": "tofu",
                    "check_type": "command",
                    "var_name": "TOFU",
                    "version_cmd": "tofu version 2>&1 | head -1 || echo 'Not installed'",
                },
            ],
            "useful_commands": [
                {
                    "command": f"{project_name.upper()} --help",
                    "description": f"{project_name} CLI",
                },
                {"command": "wrknv status", "description": "Check tool versions"},
                {
                    "command": "wrknv container status",
                    "description": "Container status",
                },
                {"command": "pytest", "description": "Run tests"},
                {"command": "deactivate", "description": "Exit environment"},
            ],
        }

        # Update with user-provided kwargs
        config.update(kwargs)

        # Select appropriate template and environment
        if script_type == "sh":
            template = self.sh_env.get_template("base.sh.j2")
        elif script_type == "ps1":
            template = self.ps1_env.get_template("base.ps1.j2")
        else:
            raise ValueError(f"Unknown script type: {script_type}")

        # Render and write
        content = template.render(**config)
        output_path.write_text(content)

        # Make executable if it's a shell script
        if script_type == "sh":
            output_path.chmod(output_path.stat().st_mode | 0o111)

    def xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_156(
        self,
        project_name: str,
        output_path: Path,
        script_type: str = "sh",
        **kwargs: Any,
    ) -> None:
        """Generate an environment setup script.

        Args:
            project_name: Name of the project
            output_path: Path to write the script
            script_type: Either "sh" for bash or "ps1" for PowerShell
            **kwargs: Additional template variables
        """
        # Default configuration
        config = {
            "project_name": project_name,
            "env_profile_var": f"{project_name.upper().replace('-', '_')}_PROFILE",
            "venv_prefix": project_name.lower(),
            "use_spinner": script_type == "sh",  # PowerShell doesn't need spinner
            "strict_project_check": False,
            "install_siblings": True,
            "sibling_patterns": [],  # No default peers - specified in config
            "special_siblings": [],  # No default peers - specified in config
            "create_log_dir": True,
            "deduplicate_path": True,
            "include_tool_verification": True,  # Enable for wrknv
            "cleanup_logs": True,
            "tools_to_verify": [
                {
                    "name": "Python",
                    "command": "python",
                    "check_type": "command",
                    "var_name": "PYTHON",
                    "version_cmd": "python --version 2>&1",
                },
                {
                    "name": "UV",
                    "command": "uv",
                    "check_type": "command",
                    "var_name": "UV",
                    "version_cmd": "uv --version 2>&1",
                },
                {
                    "name": "wrknv",
                    "command": "wrknv",
                    "check_type": "command",
                    "var_name": "WRKENV",
                    "version_cmd": "wrknv --version 2>&1 || echo 'No version info'",
                },
                {
                    "name": "ibmtf",
                    "command": "ibmtf",
                    "check_type": "command",
                    "var_name": "IBMTF",
                    "version_cmd": "ibmtf version 2>&1 | head -1 || echo 'Not installed'",
                },
                {
                    "name": "tofu",
                    "command": "tofu",
                    "check_type": "command",
                    "var_name": "TOFU",
                    "version_cmd": "tofu version 2>&1 | head -1 || echo 'Not installed'",
                },
            ],
            "useful_commands": [
                {
                    "command": f"{project_name.lower()} --help",
                    "XXdescriptionXX": f"{project_name} CLI",
                },
                {"command": "wrknv status", "description": "Check tool versions"},
                {
                    "command": "wrknv container status",
                    "description": "Container status",
                },
                {"command": "pytest", "description": "Run tests"},
                {"command": "deactivate", "description": "Exit environment"},
            ],
        }

        # Update with user-provided kwargs
        config.update(kwargs)

        # Select appropriate template and environment
        if script_type == "sh":
            template = self.sh_env.get_template("base.sh.j2")
        elif script_type == "ps1":
            template = self.ps1_env.get_template("base.ps1.j2")
        else:
            raise ValueError(f"Unknown script type: {script_type}")

        # Render and write
        content = template.render(**config)
        output_path.write_text(content)

        # Make executable if it's a shell script
        if script_type == "sh":
            output_path.chmod(output_path.stat().st_mode | 0o111)

    def xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_157(
        self,
        project_name: str,
        output_path: Path,
        script_type: str = "sh",
        **kwargs: Any,
    ) -> None:
        """Generate an environment setup script.

        Args:
            project_name: Name of the project
            output_path: Path to write the script
            script_type: Either "sh" for bash or "ps1" for PowerShell
            **kwargs: Additional template variables
        """
        # Default configuration
        config = {
            "project_name": project_name,
            "env_profile_var": f"{project_name.upper().replace('-', '_')}_PROFILE",
            "venv_prefix": project_name.lower(),
            "use_spinner": script_type == "sh",  # PowerShell doesn't need spinner
            "strict_project_check": False,
            "install_siblings": True,
            "sibling_patterns": [],  # No default peers - specified in config
            "special_siblings": [],  # No default peers - specified in config
            "create_log_dir": True,
            "deduplicate_path": True,
            "include_tool_verification": True,  # Enable for wrknv
            "cleanup_logs": True,
            "tools_to_verify": [
                {
                    "name": "Python",
                    "command": "python",
                    "check_type": "command",
                    "var_name": "PYTHON",
                    "version_cmd": "python --version 2>&1",
                },
                {
                    "name": "UV",
                    "command": "uv",
                    "check_type": "command",
                    "var_name": "UV",
                    "version_cmd": "uv --version 2>&1",
                },
                {
                    "name": "wrknv",
                    "command": "wrknv",
                    "check_type": "command",
                    "var_name": "WRKENV",
                    "version_cmd": "wrknv --version 2>&1 || echo 'No version info'",
                },
                {
                    "name": "ibmtf",
                    "command": "ibmtf",
                    "check_type": "command",
                    "var_name": "IBMTF",
                    "version_cmd": "ibmtf version 2>&1 | head -1 || echo 'Not installed'",
                },
                {
                    "name": "tofu",
                    "command": "tofu",
                    "check_type": "command",
                    "var_name": "TOFU",
                    "version_cmd": "tofu version 2>&1 | head -1 || echo 'Not installed'",
                },
            ],
            "useful_commands": [
                {
                    "command": f"{project_name.lower()} --help",
                    "DESCRIPTION": f"{project_name} CLI",
                },
                {"command": "wrknv status", "description": "Check tool versions"},
                {
                    "command": "wrknv container status",
                    "description": "Container status",
                },
                {"command": "pytest", "description": "Run tests"},
                {"command": "deactivate", "description": "Exit environment"},
            ],
        }

        # Update with user-provided kwargs
        config.update(kwargs)

        # Select appropriate template and environment
        if script_type == "sh":
            template = self.sh_env.get_template("base.sh.j2")
        elif script_type == "ps1":
            template = self.ps1_env.get_template("base.ps1.j2")
        else:
            raise ValueError(f"Unknown script type: {script_type}")

        # Render and write
        content = template.render(**config)
        output_path.write_text(content)

        # Make executable if it's a shell script
        if script_type == "sh":
            output_path.chmod(output_path.stat().st_mode | 0o111)

    def xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_158(
        self,
        project_name: str,
        output_path: Path,
        script_type: str = "sh",
        **kwargs: Any,
    ) -> None:
        """Generate an environment setup script.

        Args:
            project_name: Name of the project
            output_path: Path to write the script
            script_type: Either "sh" for bash or "ps1" for PowerShell
            **kwargs: Additional template variables
        """
        # Default configuration
        config = {
            "project_name": project_name,
            "env_profile_var": f"{project_name.upper().replace('-', '_')}_PROFILE",
            "venv_prefix": project_name.lower(),
            "use_spinner": script_type == "sh",  # PowerShell doesn't need spinner
            "strict_project_check": False,
            "install_siblings": True,
            "sibling_patterns": [],  # No default peers - specified in config
            "special_siblings": [],  # No default peers - specified in config
            "create_log_dir": True,
            "deduplicate_path": True,
            "include_tool_verification": True,  # Enable for wrknv
            "cleanup_logs": True,
            "tools_to_verify": [
                {
                    "name": "Python",
                    "command": "python",
                    "check_type": "command",
                    "var_name": "PYTHON",
                    "version_cmd": "python --version 2>&1",
                },
                {
                    "name": "UV",
                    "command": "uv",
                    "check_type": "command",
                    "var_name": "UV",
                    "version_cmd": "uv --version 2>&1",
                },
                {
                    "name": "wrknv",
                    "command": "wrknv",
                    "check_type": "command",
                    "var_name": "WRKENV",
                    "version_cmd": "wrknv --version 2>&1 || echo 'No version info'",
                },
                {
                    "name": "ibmtf",
                    "command": "ibmtf",
                    "check_type": "command",
                    "var_name": "IBMTF",
                    "version_cmd": "ibmtf version 2>&1 | head -1 || echo 'Not installed'",
                },
                {
                    "name": "tofu",
                    "command": "tofu",
                    "check_type": "command",
                    "var_name": "TOFU",
                    "version_cmd": "tofu version 2>&1 | head -1 || echo 'Not installed'",
                },
            ],
            "useful_commands": [
                {
                    "command": f"{project_name.lower()} --help",
                    "description": f"{project_name} CLI",
                },
                {"XXcommandXX": "wrknv status", "description": "Check tool versions"},
                {
                    "command": "wrknv container status",
                    "description": "Container status",
                },
                {"command": "pytest", "description": "Run tests"},
                {"command": "deactivate", "description": "Exit environment"},
            ],
        }

        # Update with user-provided kwargs
        config.update(kwargs)

        # Select appropriate template and environment
        if script_type == "sh":
            template = self.sh_env.get_template("base.sh.j2")
        elif script_type == "ps1":
            template = self.ps1_env.get_template("base.ps1.j2")
        else:
            raise ValueError(f"Unknown script type: {script_type}")

        # Render and write
        content = template.render(**config)
        output_path.write_text(content)

        # Make executable if it's a shell script
        if script_type == "sh":
            output_path.chmod(output_path.stat().st_mode | 0o111)

    def xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_159(
        self,
        project_name: str,
        output_path: Path,
        script_type: str = "sh",
        **kwargs: Any,
    ) -> None:
        """Generate an environment setup script.

        Args:
            project_name: Name of the project
            output_path: Path to write the script
            script_type: Either "sh" for bash or "ps1" for PowerShell
            **kwargs: Additional template variables
        """
        # Default configuration
        config = {
            "project_name": project_name,
            "env_profile_var": f"{project_name.upper().replace('-', '_')}_PROFILE",
            "venv_prefix": project_name.lower(),
            "use_spinner": script_type == "sh",  # PowerShell doesn't need spinner
            "strict_project_check": False,
            "install_siblings": True,
            "sibling_patterns": [],  # No default peers - specified in config
            "special_siblings": [],  # No default peers - specified in config
            "create_log_dir": True,
            "deduplicate_path": True,
            "include_tool_verification": True,  # Enable for wrknv
            "cleanup_logs": True,
            "tools_to_verify": [
                {
                    "name": "Python",
                    "command": "python",
                    "check_type": "command",
                    "var_name": "PYTHON",
                    "version_cmd": "python --version 2>&1",
                },
                {
                    "name": "UV",
                    "command": "uv",
                    "check_type": "command",
                    "var_name": "UV",
                    "version_cmd": "uv --version 2>&1",
                },
                {
                    "name": "wrknv",
                    "command": "wrknv",
                    "check_type": "command",
                    "var_name": "WRKENV",
                    "version_cmd": "wrknv --version 2>&1 || echo 'No version info'",
                },
                {
                    "name": "ibmtf",
                    "command": "ibmtf",
                    "check_type": "command",
                    "var_name": "IBMTF",
                    "version_cmd": "ibmtf version 2>&1 | head -1 || echo 'Not installed'",
                },
                {
                    "name": "tofu",
                    "command": "tofu",
                    "check_type": "command",
                    "var_name": "TOFU",
                    "version_cmd": "tofu version 2>&1 | head -1 || echo 'Not installed'",
                },
            ],
            "useful_commands": [
                {
                    "command": f"{project_name.lower()} --help",
                    "description": f"{project_name} CLI",
                },
                {"COMMAND": "wrknv status", "description": "Check tool versions"},
                {
                    "command": "wrknv container status",
                    "description": "Container status",
                },
                {"command": "pytest", "description": "Run tests"},
                {"command": "deactivate", "description": "Exit environment"},
            ],
        }

        # Update with user-provided kwargs
        config.update(kwargs)

        # Select appropriate template and environment
        if script_type == "sh":
            template = self.sh_env.get_template("base.sh.j2")
        elif script_type == "ps1":
            template = self.ps1_env.get_template("base.ps1.j2")
        else:
            raise ValueError(f"Unknown script type: {script_type}")

        # Render and write
        content = template.render(**config)
        output_path.write_text(content)

        # Make executable if it's a shell script
        if script_type == "sh":
            output_path.chmod(output_path.stat().st_mode | 0o111)

    def xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_160(
        self,
        project_name: str,
        output_path: Path,
        script_type: str = "sh",
        **kwargs: Any,
    ) -> None:
        """Generate an environment setup script.

        Args:
            project_name: Name of the project
            output_path: Path to write the script
            script_type: Either "sh" for bash or "ps1" for PowerShell
            **kwargs: Additional template variables
        """
        # Default configuration
        config = {
            "project_name": project_name,
            "env_profile_var": f"{project_name.upper().replace('-', '_')}_PROFILE",
            "venv_prefix": project_name.lower(),
            "use_spinner": script_type == "sh",  # PowerShell doesn't need spinner
            "strict_project_check": False,
            "install_siblings": True,
            "sibling_patterns": [],  # No default peers - specified in config
            "special_siblings": [],  # No default peers - specified in config
            "create_log_dir": True,
            "deduplicate_path": True,
            "include_tool_verification": True,  # Enable for wrknv
            "cleanup_logs": True,
            "tools_to_verify": [
                {
                    "name": "Python",
                    "command": "python",
                    "check_type": "command",
                    "var_name": "PYTHON",
                    "version_cmd": "python --version 2>&1",
                },
                {
                    "name": "UV",
                    "command": "uv",
                    "check_type": "command",
                    "var_name": "UV",
                    "version_cmd": "uv --version 2>&1",
                },
                {
                    "name": "wrknv",
                    "command": "wrknv",
                    "check_type": "command",
                    "var_name": "WRKENV",
                    "version_cmd": "wrknv --version 2>&1 || echo 'No version info'",
                },
                {
                    "name": "ibmtf",
                    "command": "ibmtf",
                    "check_type": "command",
                    "var_name": "IBMTF",
                    "version_cmd": "ibmtf version 2>&1 | head -1 || echo 'Not installed'",
                },
                {
                    "name": "tofu",
                    "command": "tofu",
                    "check_type": "command",
                    "var_name": "TOFU",
                    "version_cmd": "tofu version 2>&1 | head -1 || echo 'Not installed'",
                },
            ],
            "useful_commands": [
                {
                    "command": f"{project_name.lower()} --help",
                    "description": f"{project_name} CLI",
                },
                {"command": "XXwrknv statusXX", "description": "Check tool versions"},
                {
                    "command": "wrknv container status",
                    "description": "Container status",
                },
                {"command": "pytest", "description": "Run tests"},
                {"command": "deactivate", "description": "Exit environment"},
            ],
        }

        # Update with user-provided kwargs
        config.update(kwargs)

        # Select appropriate template and environment
        if script_type == "sh":
            template = self.sh_env.get_template("base.sh.j2")
        elif script_type == "ps1":
            template = self.ps1_env.get_template("base.ps1.j2")
        else:
            raise ValueError(f"Unknown script type: {script_type}")

        # Render and write
        content = template.render(**config)
        output_path.write_text(content)

        # Make executable if it's a shell script
        if script_type == "sh":
            output_path.chmod(output_path.stat().st_mode | 0o111)

    def xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_161(
        self,
        project_name: str,
        output_path: Path,
        script_type: str = "sh",
        **kwargs: Any,
    ) -> None:
        """Generate an environment setup script.

        Args:
            project_name: Name of the project
            output_path: Path to write the script
            script_type: Either "sh" for bash or "ps1" for PowerShell
            **kwargs: Additional template variables
        """
        # Default configuration
        config = {
            "project_name": project_name,
            "env_profile_var": f"{project_name.upper().replace('-', '_')}_PROFILE",
            "venv_prefix": project_name.lower(),
            "use_spinner": script_type == "sh",  # PowerShell doesn't need spinner
            "strict_project_check": False,
            "install_siblings": True,
            "sibling_patterns": [],  # No default peers - specified in config
            "special_siblings": [],  # No default peers - specified in config
            "create_log_dir": True,
            "deduplicate_path": True,
            "include_tool_verification": True,  # Enable for wrknv
            "cleanup_logs": True,
            "tools_to_verify": [
                {
                    "name": "Python",
                    "command": "python",
                    "check_type": "command",
                    "var_name": "PYTHON",
                    "version_cmd": "python --version 2>&1",
                },
                {
                    "name": "UV",
                    "command": "uv",
                    "check_type": "command",
                    "var_name": "UV",
                    "version_cmd": "uv --version 2>&1",
                },
                {
                    "name": "wrknv",
                    "command": "wrknv",
                    "check_type": "command",
                    "var_name": "WRKENV",
                    "version_cmd": "wrknv --version 2>&1 || echo 'No version info'",
                },
                {
                    "name": "ibmtf",
                    "command": "ibmtf",
                    "check_type": "command",
                    "var_name": "IBMTF",
                    "version_cmd": "ibmtf version 2>&1 | head -1 || echo 'Not installed'",
                },
                {
                    "name": "tofu",
                    "command": "tofu",
                    "check_type": "command",
                    "var_name": "TOFU",
                    "version_cmd": "tofu version 2>&1 | head -1 || echo 'Not installed'",
                },
            ],
            "useful_commands": [
                {
                    "command": f"{project_name.lower()} --help",
                    "description": f"{project_name} CLI",
                },
                {"command": "WRKNV STATUS", "description": "Check tool versions"},
                {
                    "command": "wrknv container status",
                    "description": "Container status",
                },
                {"command": "pytest", "description": "Run tests"},
                {"command": "deactivate", "description": "Exit environment"},
            ],
        }

        # Update with user-provided kwargs
        config.update(kwargs)

        # Select appropriate template and environment
        if script_type == "sh":
            template = self.sh_env.get_template("base.sh.j2")
        elif script_type == "ps1":
            template = self.ps1_env.get_template("base.ps1.j2")
        else:
            raise ValueError(f"Unknown script type: {script_type}")

        # Render and write
        content = template.render(**config)
        output_path.write_text(content)

        # Make executable if it's a shell script
        if script_type == "sh":
            output_path.chmod(output_path.stat().st_mode | 0o111)

    def xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_162(
        self,
        project_name: str,
        output_path: Path,
        script_type: str = "sh",
        **kwargs: Any,
    ) -> None:
        """Generate an environment setup script.

        Args:
            project_name: Name of the project
            output_path: Path to write the script
            script_type: Either "sh" for bash or "ps1" for PowerShell
            **kwargs: Additional template variables
        """
        # Default configuration
        config = {
            "project_name": project_name,
            "env_profile_var": f"{project_name.upper().replace('-', '_')}_PROFILE",
            "venv_prefix": project_name.lower(),
            "use_spinner": script_type == "sh",  # PowerShell doesn't need spinner
            "strict_project_check": False,
            "install_siblings": True,
            "sibling_patterns": [],  # No default peers - specified in config
            "special_siblings": [],  # No default peers - specified in config
            "create_log_dir": True,
            "deduplicate_path": True,
            "include_tool_verification": True,  # Enable for wrknv
            "cleanup_logs": True,
            "tools_to_verify": [
                {
                    "name": "Python",
                    "command": "python",
                    "check_type": "command",
                    "var_name": "PYTHON",
                    "version_cmd": "python --version 2>&1",
                },
                {
                    "name": "UV",
                    "command": "uv",
                    "check_type": "command",
                    "var_name": "UV",
                    "version_cmd": "uv --version 2>&1",
                },
                {
                    "name": "wrknv",
                    "command": "wrknv",
                    "check_type": "command",
                    "var_name": "WRKENV",
                    "version_cmd": "wrknv --version 2>&1 || echo 'No version info'",
                },
                {
                    "name": "ibmtf",
                    "command": "ibmtf",
                    "check_type": "command",
                    "var_name": "IBMTF",
                    "version_cmd": "ibmtf version 2>&1 | head -1 || echo 'Not installed'",
                },
                {
                    "name": "tofu",
                    "command": "tofu",
                    "check_type": "command",
                    "var_name": "TOFU",
                    "version_cmd": "tofu version 2>&1 | head -1 || echo 'Not installed'",
                },
            ],
            "useful_commands": [
                {
                    "command": f"{project_name.lower()} --help",
                    "description": f"{project_name} CLI",
                },
                {"command": "wrknv status", "XXdescriptionXX": "Check tool versions"},
                {
                    "command": "wrknv container status",
                    "description": "Container status",
                },
                {"command": "pytest", "description": "Run tests"},
                {"command": "deactivate", "description": "Exit environment"},
            ],
        }

        # Update with user-provided kwargs
        config.update(kwargs)

        # Select appropriate template and environment
        if script_type == "sh":
            template = self.sh_env.get_template("base.sh.j2")
        elif script_type == "ps1":
            template = self.ps1_env.get_template("base.ps1.j2")
        else:
            raise ValueError(f"Unknown script type: {script_type}")

        # Render and write
        content = template.render(**config)
        output_path.write_text(content)

        # Make executable if it's a shell script
        if script_type == "sh":
            output_path.chmod(output_path.stat().st_mode | 0o111)

    def xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_163(
        self,
        project_name: str,
        output_path: Path,
        script_type: str = "sh",
        **kwargs: Any,
    ) -> None:
        """Generate an environment setup script.

        Args:
            project_name: Name of the project
            output_path: Path to write the script
            script_type: Either "sh" for bash or "ps1" for PowerShell
            **kwargs: Additional template variables
        """
        # Default configuration
        config = {
            "project_name": project_name,
            "env_profile_var": f"{project_name.upper().replace('-', '_')}_PROFILE",
            "venv_prefix": project_name.lower(),
            "use_spinner": script_type == "sh",  # PowerShell doesn't need spinner
            "strict_project_check": False,
            "install_siblings": True,
            "sibling_patterns": [],  # No default peers - specified in config
            "special_siblings": [],  # No default peers - specified in config
            "create_log_dir": True,
            "deduplicate_path": True,
            "include_tool_verification": True,  # Enable for wrknv
            "cleanup_logs": True,
            "tools_to_verify": [
                {
                    "name": "Python",
                    "command": "python",
                    "check_type": "command",
                    "var_name": "PYTHON",
                    "version_cmd": "python --version 2>&1",
                },
                {
                    "name": "UV",
                    "command": "uv",
                    "check_type": "command",
                    "var_name": "UV",
                    "version_cmd": "uv --version 2>&1",
                },
                {
                    "name": "wrknv",
                    "command": "wrknv",
                    "check_type": "command",
                    "var_name": "WRKENV",
                    "version_cmd": "wrknv --version 2>&1 || echo 'No version info'",
                },
                {
                    "name": "ibmtf",
                    "command": "ibmtf",
                    "check_type": "command",
                    "var_name": "IBMTF",
                    "version_cmd": "ibmtf version 2>&1 | head -1 || echo 'Not installed'",
                },
                {
                    "name": "tofu",
                    "command": "tofu",
                    "check_type": "command",
                    "var_name": "TOFU",
                    "version_cmd": "tofu version 2>&1 | head -1 || echo 'Not installed'",
                },
            ],
            "useful_commands": [
                {
                    "command": f"{project_name.lower()} --help",
                    "description": f"{project_name} CLI",
                },
                {"command": "wrknv status", "DESCRIPTION": "Check tool versions"},
                {
                    "command": "wrknv container status",
                    "description": "Container status",
                },
                {"command": "pytest", "description": "Run tests"},
                {"command": "deactivate", "description": "Exit environment"},
            ],
        }

        # Update with user-provided kwargs
        config.update(kwargs)

        # Select appropriate template and environment
        if script_type == "sh":
            template = self.sh_env.get_template("base.sh.j2")
        elif script_type == "ps1":
            template = self.ps1_env.get_template("base.ps1.j2")
        else:
            raise ValueError(f"Unknown script type: {script_type}")

        # Render and write
        content = template.render(**config)
        output_path.write_text(content)

        # Make executable if it's a shell script
        if script_type == "sh":
            output_path.chmod(output_path.stat().st_mode | 0o111)

    def xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_164(
        self,
        project_name: str,
        output_path: Path,
        script_type: str = "sh",
        **kwargs: Any,
    ) -> None:
        """Generate an environment setup script.

        Args:
            project_name: Name of the project
            output_path: Path to write the script
            script_type: Either "sh" for bash or "ps1" for PowerShell
            **kwargs: Additional template variables
        """
        # Default configuration
        config = {
            "project_name": project_name,
            "env_profile_var": f"{project_name.upper().replace('-', '_')}_PROFILE",
            "venv_prefix": project_name.lower(),
            "use_spinner": script_type == "sh",  # PowerShell doesn't need spinner
            "strict_project_check": False,
            "install_siblings": True,
            "sibling_patterns": [],  # No default peers - specified in config
            "special_siblings": [],  # No default peers - specified in config
            "create_log_dir": True,
            "deduplicate_path": True,
            "include_tool_verification": True,  # Enable for wrknv
            "cleanup_logs": True,
            "tools_to_verify": [
                {
                    "name": "Python",
                    "command": "python",
                    "check_type": "command",
                    "var_name": "PYTHON",
                    "version_cmd": "python --version 2>&1",
                },
                {
                    "name": "UV",
                    "command": "uv",
                    "check_type": "command",
                    "var_name": "UV",
                    "version_cmd": "uv --version 2>&1",
                },
                {
                    "name": "wrknv",
                    "command": "wrknv",
                    "check_type": "command",
                    "var_name": "WRKENV",
                    "version_cmd": "wrknv --version 2>&1 || echo 'No version info'",
                },
                {
                    "name": "ibmtf",
                    "command": "ibmtf",
                    "check_type": "command",
                    "var_name": "IBMTF",
                    "version_cmd": "ibmtf version 2>&1 | head -1 || echo 'Not installed'",
                },
                {
                    "name": "tofu",
                    "command": "tofu",
                    "check_type": "command",
                    "var_name": "TOFU",
                    "version_cmd": "tofu version 2>&1 | head -1 || echo 'Not installed'",
                },
            ],
            "useful_commands": [
                {
                    "command": f"{project_name.lower()} --help",
                    "description": f"{project_name} CLI",
                },
                {"command": "wrknv status", "description": "XXCheck tool versionsXX"},
                {
                    "command": "wrknv container status",
                    "description": "Container status",
                },
                {"command": "pytest", "description": "Run tests"},
                {"command": "deactivate", "description": "Exit environment"},
            ],
        }

        # Update with user-provided kwargs
        config.update(kwargs)

        # Select appropriate template and environment
        if script_type == "sh":
            template = self.sh_env.get_template("base.sh.j2")
        elif script_type == "ps1":
            template = self.ps1_env.get_template("base.ps1.j2")
        else:
            raise ValueError(f"Unknown script type: {script_type}")

        # Render and write
        content = template.render(**config)
        output_path.write_text(content)

        # Make executable if it's a shell script
        if script_type == "sh":
            output_path.chmod(output_path.stat().st_mode | 0o111)

    def xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_165(
        self,
        project_name: str,
        output_path: Path,
        script_type: str = "sh",
        **kwargs: Any,
    ) -> None:
        """Generate an environment setup script.

        Args:
            project_name: Name of the project
            output_path: Path to write the script
            script_type: Either "sh" for bash or "ps1" for PowerShell
            **kwargs: Additional template variables
        """
        # Default configuration
        config = {
            "project_name": project_name,
            "env_profile_var": f"{project_name.upper().replace('-', '_')}_PROFILE",
            "venv_prefix": project_name.lower(),
            "use_spinner": script_type == "sh",  # PowerShell doesn't need spinner
            "strict_project_check": False,
            "install_siblings": True,
            "sibling_patterns": [],  # No default peers - specified in config
            "special_siblings": [],  # No default peers - specified in config
            "create_log_dir": True,
            "deduplicate_path": True,
            "include_tool_verification": True,  # Enable for wrknv
            "cleanup_logs": True,
            "tools_to_verify": [
                {
                    "name": "Python",
                    "command": "python",
                    "check_type": "command",
                    "var_name": "PYTHON",
                    "version_cmd": "python --version 2>&1",
                },
                {
                    "name": "UV",
                    "command": "uv",
                    "check_type": "command",
                    "var_name": "UV",
                    "version_cmd": "uv --version 2>&1",
                },
                {
                    "name": "wrknv",
                    "command": "wrknv",
                    "check_type": "command",
                    "var_name": "WRKENV",
                    "version_cmd": "wrknv --version 2>&1 || echo 'No version info'",
                },
                {
                    "name": "ibmtf",
                    "command": "ibmtf",
                    "check_type": "command",
                    "var_name": "IBMTF",
                    "version_cmd": "ibmtf version 2>&1 | head -1 || echo 'Not installed'",
                },
                {
                    "name": "tofu",
                    "command": "tofu",
                    "check_type": "command",
                    "var_name": "TOFU",
                    "version_cmd": "tofu version 2>&1 | head -1 || echo 'Not installed'",
                },
            ],
            "useful_commands": [
                {
                    "command": f"{project_name.lower()} --help",
                    "description": f"{project_name} CLI",
                },
                {"command": "wrknv status", "description": "check tool versions"},
                {
                    "command": "wrknv container status",
                    "description": "Container status",
                },
                {"command": "pytest", "description": "Run tests"},
                {"command": "deactivate", "description": "Exit environment"},
            ],
        }

        # Update with user-provided kwargs
        config.update(kwargs)

        # Select appropriate template and environment
        if script_type == "sh":
            template = self.sh_env.get_template("base.sh.j2")
        elif script_type == "ps1":
            template = self.ps1_env.get_template("base.ps1.j2")
        else:
            raise ValueError(f"Unknown script type: {script_type}")

        # Render and write
        content = template.render(**config)
        output_path.write_text(content)

        # Make executable if it's a shell script
        if script_type == "sh":
            output_path.chmod(output_path.stat().st_mode | 0o111)

    def xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_166(
        self,
        project_name: str,
        output_path: Path,
        script_type: str = "sh",
        **kwargs: Any,
    ) -> None:
        """Generate an environment setup script.

        Args:
            project_name: Name of the project
            output_path: Path to write the script
            script_type: Either "sh" for bash or "ps1" for PowerShell
            **kwargs: Additional template variables
        """
        # Default configuration
        config = {
            "project_name": project_name,
            "env_profile_var": f"{project_name.upper().replace('-', '_')}_PROFILE",
            "venv_prefix": project_name.lower(),
            "use_spinner": script_type == "sh",  # PowerShell doesn't need spinner
            "strict_project_check": False,
            "install_siblings": True,
            "sibling_patterns": [],  # No default peers - specified in config
            "special_siblings": [],  # No default peers - specified in config
            "create_log_dir": True,
            "deduplicate_path": True,
            "include_tool_verification": True,  # Enable for wrknv
            "cleanup_logs": True,
            "tools_to_verify": [
                {
                    "name": "Python",
                    "command": "python",
                    "check_type": "command",
                    "var_name": "PYTHON",
                    "version_cmd": "python --version 2>&1",
                },
                {
                    "name": "UV",
                    "command": "uv",
                    "check_type": "command",
                    "var_name": "UV",
                    "version_cmd": "uv --version 2>&1",
                },
                {
                    "name": "wrknv",
                    "command": "wrknv",
                    "check_type": "command",
                    "var_name": "WRKENV",
                    "version_cmd": "wrknv --version 2>&1 || echo 'No version info'",
                },
                {
                    "name": "ibmtf",
                    "command": "ibmtf",
                    "check_type": "command",
                    "var_name": "IBMTF",
                    "version_cmd": "ibmtf version 2>&1 | head -1 || echo 'Not installed'",
                },
                {
                    "name": "tofu",
                    "command": "tofu",
                    "check_type": "command",
                    "var_name": "TOFU",
                    "version_cmd": "tofu version 2>&1 | head -1 || echo 'Not installed'",
                },
            ],
            "useful_commands": [
                {
                    "command": f"{project_name.lower()} --help",
                    "description": f"{project_name} CLI",
                },
                {"command": "wrknv status", "description": "CHECK TOOL VERSIONS"},
                {
                    "command": "wrknv container status",
                    "description": "Container status",
                },
                {"command": "pytest", "description": "Run tests"},
                {"command": "deactivate", "description": "Exit environment"},
            ],
        }

        # Update with user-provided kwargs
        config.update(kwargs)

        # Select appropriate template and environment
        if script_type == "sh":
            template = self.sh_env.get_template("base.sh.j2")
        elif script_type == "ps1":
            template = self.ps1_env.get_template("base.ps1.j2")
        else:
            raise ValueError(f"Unknown script type: {script_type}")

        # Render and write
        content = template.render(**config)
        output_path.write_text(content)

        # Make executable if it's a shell script
        if script_type == "sh":
            output_path.chmod(output_path.stat().st_mode | 0o111)

    def xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_167(
        self,
        project_name: str,
        output_path: Path,
        script_type: str = "sh",
        **kwargs: Any,
    ) -> None:
        """Generate an environment setup script.

        Args:
            project_name: Name of the project
            output_path: Path to write the script
            script_type: Either "sh" for bash or "ps1" for PowerShell
            **kwargs: Additional template variables
        """
        # Default configuration
        config = {
            "project_name": project_name,
            "env_profile_var": f"{project_name.upper().replace('-', '_')}_PROFILE",
            "venv_prefix": project_name.lower(),
            "use_spinner": script_type == "sh",  # PowerShell doesn't need spinner
            "strict_project_check": False,
            "install_siblings": True,
            "sibling_patterns": [],  # No default peers - specified in config
            "special_siblings": [],  # No default peers - specified in config
            "create_log_dir": True,
            "deduplicate_path": True,
            "include_tool_verification": True,  # Enable for wrknv
            "cleanup_logs": True,
            "tools_to_verify": [
                {
                    "name": "Python",
                    "command": "python",
                    "check_type": "command",
                    "var_name": "PYTHON",
                    "version_cmd": "python --version 2>&1",
                },
                {
                    "name": "UV",
                    "command": "uv",
                    "check_type": "command",
                    "var_name": "UV",
                    "version_cmd": "uv --version 2>&1",
                },
                {
                    "name": "wrknv",
                    "command": "wrknv",
                    "check_type": "command",
                    "var_name": "WRKENV",
                    "version_cmd": "wrknv --version 2>&1 || echo 'No version info'",
                },
                {
                    "name": "ibmtf",
                    "command": "ibmtf",
                    "check_type": "command",
                    "var_name": "IBMTF",
                    "version_cmd": "ibmtf version 2>&1 | head -1 || echo 'Not installed'",
                },
                {
                    "name": "tofu",
                    "command": "tofu",
                    "check_type": "command",
                    "var_name": "TOFU",
                    "version_cmd": "tofu version 2>&1 | head -1 || echo 'Not installed'",
                },
            ],
            "useful_commands": [
                {
                    "command": f"{project_name.lower()} --help",
                    "description": f"{project_name} CLI",
                },
                {"command": "wrknv status", "description": "Check tool versions"},
                {
                    "XXcommandXX": "wrknv container status",
                    "description": "Container status",
                },
                {"command": "pytest", "description": "Run tests"},
                {"command": "deactivate", "description": "Exit environment"},
            ],
        }

        # Update with user-provided kwargs
        config.update(kwargs)

        # Select appropriate template and environment
        if script_type == "sh":
            template = self.sh_env.get_template("base.sh.j2")
        elif script_type == "ps1":
            template = self.ps1_env.get_template("base.ps1.j2")
        else:
            raise ValueError(f"Unknown script type: {script_type}")

        # Render and write
        content = template.render(**config)
        output_path.write_text(content)

        # Make executable if it's a shell script
        if script_type == "sh":
            output_path.chmod(output_path.stat().st_mode | 0o111)

    def xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_168(
        self,
        project_name: str,
        output_path: Path,
        script_type: str = "sh",
        **kwargs: Any,
    ) -> None:
        """Generate an environment setup script.

        Args:
            project_name: Name of the project
            output_path: Path to write the script
            script_type: Either "sh" for bash or "ps1" for PowerShell
            **kwargs: Additional template variables
        """
        # Default configuration
        config = {
            "project_name": project_name,
            "env_profile_var": f"{project_name.upper().replace('-', '_')}_PROFILE",
            "venv_prefix": project_name.lower(),
            "use_spinner": script_type == "sh",  # PowerShell doesn't need spinner
            "strict_project_check": False,
            "install_siblings": True,
            "sibling_patterns": [],  # No default peers - specified in config
            "special_siblings": [],  # No default peers - specified in config
            "create_log_dir": True,
            "deduplicate_path": True,
            "include_tool_verification": True,  # Enable for wrknv
            "cleanup_logs": True,
            "tools_to_verify": [
                {
                    "name": "Python",
                    "command": "python",
                    "check_type": "command",
                    "var_name": "PYTHON",
                    "version_cmd": "python --version 2>&1",
                },
                {
                    "name": "UV",
                    "command": "uv",
                    "check_type": "command",
                    "var_name": "UV",
                    "version_cmd": "uv --version 2>&1",
                },
                {
                    "name": "wrknv",
                    "command": "wrknv",
                    "check_type": "command",
                    "var_name": "WRKENV",
                    "version_cmd": "wrknv --version 2>&1 || echo 'No version info'",
                },
                {
                    "name": "ibmtf",
                    "command": "ibmtf",
                    "check_type": "command",
                    "var_name": "IBMTF",
                    "version_cmd": "ibmtf version 2>&1 | head -1 || echo 'Not installed'",
                },
                {
                    "name": "tofu",
                    "command": "tofu",
                    "check_type": "command",
                    "var_name": "TOFU",
                    "version_cmd": "tofu version 2>&1 | head -1 || echo 'Not installed'",
                },
            ],
            "useful_commands": [
                {
                    "command": f"{project_name.lower()} --help",
                    "description": f"{project_name} CLI",
                },
                {"command": "wrknv status", "description": "Check tool versions"},
                {
                    "COMMAND": "wrknv container status",
                    "description": "Container status",
                },
                {"command": "pytest", "description": "Run tests"},
                {"command": "deactivate", "description": "Exit environment"},
            ],
        }

        # Update with user-provided kwargs
        config.update(kwargs)

        # Select appropriate template and environment
        if script_type == "sh":
            template = self.sh_env.get_template("base.sh.j2")
        elif script_type == "ps1":
            template = self.ps1_env.get_template("base.ps1.j2")
        else:
            raise ValueError(f"Unknown script type: {script_type}")

        # Render and write
        content = template.render(**config)
        output_path.write_text(content)

        # Make executable if it's a shell script
        if script_type == "sh":
            output_path.chmod(output_path.stat().st_mode | 0o111)

    def xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_169(
        self,
        project_name: str,
        output_path: Path,
        script_type: str = "sh",
        **kwargs: Any,
    ) -> None:
        """Generate an environment setup script.

        Args:
            project_name: Name of the project
            output_path: Path to write the script
            script_type: Either "sh" for bash or "ps1" for PowerShell
            **kwargs: Additional template variables
        """
        # Default configuration
        config = {
            "project_name": project_name,
            "env_profile_var": f"{project_name.upper().replace('-', '_')}_PROFILE",
            "venv_prefix": project_name.lower(),
            "use_spinner": script_type == "sh",  # PowerShell doesn't need spinner
            "strict_project_check": False,
            "install_siblings": True,
            "sibling_patterns": [],  # No default peers - specified in config
            "special_siblings": [],  # No default peers - specified in config
            "create_log_dir": True,
            "deduplicate_path": True,
            "include_tool_verification": True,  # Enable for wrknv
            "cleanup_logs": True,
            "tools_to_verify": [
                {
                    "name": "Python",
                    "command": "python",
                    "check_type": "command",
                    "var_name": "PYTHON",
                    "version_cmd": "python --version 2>&1",
                },
                {
                    "name": "UV",
                    "command": "uv",
                    "check_type": "command",
                    "var_name": "UV",
                    "version_cmd": "uv --version 2>&1",
                },
                {
                    "name": "wrknv",
                    "command": "wrknv",
                    "check_type": "command",
                    "var_name": "WRKENV",
                    "version_cmd": "wrknv --version 2>&1 || echo 'No version info'",
                },
                {
                    "name": "ibmtf",
                    "command": "ibmtf",
                    "check_type": "command",
                    "var_name": "IBMTF",
                    "version_cmd": "ibmtf version 2>&1 | head -1 || echo 'Not installed'",
                },
                {
                    "name": "tofu",
                    "command": "tofu",
                    "check_type": "command",
                    "var_name": "TOFU",
                    "version_cmd": "tofu version 2>&1 | head -1 || echo 'Not installed'",
                },
            ],
            "useful_commands": [
                {
                    "command": f"{project_name.lower()} --help",
                    "description": f"{project_name} CLI",
                },
                {"command": "wrknv status", "description": "Check tool versions"},
                {
                    "command": "XXwrknv container statusXX",
                    "description": "Container status",
                },
                {"command": "pytest", "description": "Run tests"},
                {"command": "deactivate", "description": "Exit environment"},
            ],
        }

        # Update with user-provided kwargs
        config.update(kwargs)

        # Select appropriate template and environment
        if script_type == "sh":
            template = self.sh_env.get_template("base.sh.j2")
        elif script_type == "ps1":
            template = self.ps1_env.get_template("base.ps1.j2")
        else:
            raise ValueError(f"Unknown script type: {script_type}")

        # Render and write
        content = template.render(**config)
        output_path.write_text(content)

        # Make executable if it's a shell script
        if script_type == "sh":
            output_path.chmod(output_path.stat().st_mode | 0o111)

    def xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_170(
        self,
        project_name: str,
        output_path: Path,
        script_type: str = "sh",
        **kwargs: Any,
    ) -> None:
        """Generate an environment setup script.

        Args:
            project_name: Name of the project
            output_path: Path to write the script
            script_type: Either "sh" for bash or "ps1" for PowerShell
            **kwargs: Additional template variables
        """
        # Default configuration
        config = {
            "project_name": project_name,
            "env_profile_var": f"{project_name.upper().replace('-', '_')}_PROFILE",
            "venv_prefix": project_name.lower(),
            "use_spinner": script_type == "sh",  # PowerShell doesn't need spinner
            "strict_project_check": False,
            "install_siblings": True,
            "sibling_patterns": [],  # No default peers - specified in config
            "special_siblings": [],  # No default peers - specified in config
            "create_log_dir": True,
            "deduplicate_path": True,
            "include_tool_verification": True,  # Enable for wrknv
            "cleanup_logs": True,
            "tools_to_verify": [
                {
                    "name": "Python",
                    "command": "python",
                    "check_type": "command",
                    "var_name": "PYTHON",
                    "version_cmd": "python --version 2>&1",
                },
                {
                    "name": "UV",
                    "command": "uv",
                    "check_type": "command",
                    "var_name": "UV",
                    "version_cmd": "uv --version 2>&1",
                },
                {
                    "name": "wrknv",
                    "command": "wrknv",
                    "check_type": "command",
                    "var_name": "WRKENV",
                    "version_cmd": "wrknv --version 2>&1 || echo 'No version info'",
                },
                {
                    "name": "ibmtf",
                    "command": "ibmtf",
                    "check_type": "command",
                    "var_name": "IBMTF",
                    "version_cmd": "ibmtf version 2>&1 | head -1 || echo 'Not installed'",
                },
                {
                    "name": "tofu",
                    "command": "tofu",
                    "check_type": "command",
                    "var_name": "TOFU",
                    "version_cmd": "tofu version 2>&1 | head -1 || echo 'Not installed'",
                },
            ],
            "useful_commands": [
                {
                    "command": f"{project_name.lower()} --help",
                    "description": f"{project_name} CLI",
                },
                {"command": "wrknv status", "description": "Check tool versions"},
                {
                    "command": "WRKNV CONTAINER STATUS",
                    "description": "Container status",
                },
                {"command": "pytest", "description": "Run tests"},
                {"command": "deactivate", "description": "Exit environment"},
            ],
        }

        # Update with user-provided kwargs
        config.update(kwargs)

        # Select appropriate template and environment
        if script_type == "sh":
            template = self.sh_env.get_template("base.sh.j2")
        elif script_type == "ps1":
            template = self.ps1_env.get_template("base.ps1.j2")
        else:
            raise ValueError(f"Unknown script type: {script_type}")

        # Render and write
        content = template.render(**config)
        output_path.write_text(content)

        # Make executable if it's a shell script
        if script_type == "sh":
            output_path.chmod(output_path.stat().st_mode | 0o111)

    def xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_171(
        self,
        project_name: str,
        output_path: Path,
        script_type: str = "sh",
        **kwargs: Any,
    ) -> None:
        """Generate an environment setup script.

        Args:
            project_name: Name of the project
            output_path: Path to write the script
            script_type: Either "sh" for bash or "ps1" for PowerShell
            **kwargs: Additional template variables
        """
        # Default configuration
        config = {
            "project_name": project_name,
            "env_profile_var": f"{project_name.upper().replace('-', '_')}_PROFILE",
            "venv_prefix": project_name.lower(),
            "use_spinner": script_type == "sh",  # PowerShell doesn't need spinner
            "strict_project_check": False,
            "install_siblings": True,
            "sibling_patterns": [],  # No default peers - specified in config
            "special_siblings": [],  # No default peers - specified in config
            "create_log_dir": True,
            "deduplicate_path": True,
            "include_tool_verification": True,  # Enable for wrknv
            "cleanup_logs": True,
            "tools_to_verify": [
                {
                    "name": "Python",
                    "command": "python",
                    "check_type": "command",
                    "var_name": "PYTHON",
                    "version_cmd": "python --version 2>&1",
                },
                {
                    "name": "UV",
                    "command": "uv",
                    "check_type": "command",
                    "var_name": "UV",
                    "version_cmd": "uv --version 2>&1",
                },
                {
                    "name": "wrknv",
                    "command": "wrknv",
                    "check_type": "command",
                    "var_name": "WRKENV",
                    "version_cmd": "wrknv --version 2>&1 || echo 'No version info'",
                },
                {
                    "name": "ibmtf",
                    "command": "ibmtf",
                    "check_type": "command",
                    "var_name": "IBMTF",
                    "version_cmd": "ibmtf version 2>&1 | head -1 || echo 'Not installed'",
                },
                {
                    "name": "tofu",
                    "command": "tofu",
                    "check_type": "command",
                    "var_name": "TOFU",
                    "version_cmd": "tofu version 2>&1 | head -1 || echo 'Not installed'",
                },
            ],
            "useful_commands": [
                {
                    "command": f"{project_name.lower()} --help",
                    "description": f"{project_name} CLI",
                },
                {"command": "wrknv status", "description": "Check tool versions"},
                {
                    "command": "wrknv container status",
                    "XXdescriptionXX": "Container status",
                },
                {"command": "pytest", "description": "Run tests"},
                {"command": "deactivate", "description": "Exit environment"},
            ],
        }

        # Update with user-provided kwargs
        config.update(kwargs)

        # Select appropriate template and environment
        if script_type == "sh":
            template = self.sh_env.get_template("base.sh.j2")
        elif script_type == "ps1":
            template = self.ps1_env.get_template("base.ps1.j2")
        else:
            raise ValueError(f"Unknown script type: {script_type}")

        # Render and write
        content = template.render(**config)
        output_path.write_text(content)

        # Make executable if it's a shell script
        if script_type == "sh":
            output_path.chmod(output_path.stat().st_mode | 0o111)

    def xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_172(
        self,
        project_name: str,
        output_path: Path,
        script_type: str = "sh",
        **kwargs: Any,
    ) -> None:
        """Generate an environment setup script.

        Args:
            project_name: Name of the project
            output_path: Path to write the script
            script_type: Either "sh" for bash or "ps1" for PowerShell
            **kwargs: Additional template variables
        """
        # Default configuration
        config = {
            "project_name": project_name,
            "env_profile_var": f"{project_name.upper().replace('-', '_')}_PROFILE",
            "venv_prefix": project_name.lower(),
            "use_spinner": script_type == "sh",  # PowerShell doesn't need spinner
            "strict_project_check": False,
            "install_siblings": True,
            "sibling_patterns": [],  # No default peers - specified in config
            "special_siblings": [],  # No default peers - specified in config
            "create_log_dir": True,
            "deduplicate_path": True,
            "include_tool_verification": True,  # Enable for wrknv
            "cleanup_logs": True,
            "tools_to_verify": [
                {
                    "name": "Python",
                    "command": "python",
                    "check_type": "command",
                    "var_name": "PYTHON",
                    "version_cmd": "python --version 2>&1",
                },
                {
                    "name": "UV",
                    "command": "uv",
                    "check_type": "command",
                    "var_name": "UV",
                    "version_cmd": "uv --version 2>&1",
                },
                {
                    "name": "wrknv",
                    "command": "wrknv",
                    "check_type": "command",
                    "var_name": "WRKENV",
                    "version_cmd": "wrknv --version 2>&1 || echo 'No version info'",
                },
                {
                    "name": "ibmtf",
                    "command": "ibmtf",
                    "check_type": "command",
                    "var_name": "IBMTF",
                    "version_cmd": "ibmtf version 2>&1 | head -1 || echo 'Not installed'",
                },
                {
                    "name": "tofu",
                    "command": "tofu",
                    "check_type": "command",
                    "var_name": "TOFU",
                    "version_cmd": "tofu version 2>&1 | head -1 || echo 'Not installed'",
                },
            ],
            "useful_commands": [
                {
                    "command": f"{project_name.lower()} --help",
                    "description": f"{project_name} CLI",
                },
                {"command": "wrknv status", "description": "Check tool versions"},
                {
                    "command": "wrknv container status",
                    "DESCRIPTION": "Container status",
                },
                {"command": "pytest", "description": "Run tests"},
                {"command": "deactivate", "description": "Exit environment"},
            ],
        }

        # Update with user-provided kwargs
        config.update(kwargs)

        # Select appropriate template and environment
        if script_type == "sh":
            template = self.sh_env.get_template("base.sh.j2")
        elif script_type == "ps1":
            template = self.ps1_env.get_template("base.ps1.j2")
        else:
            raise ValueError(f"Unknown script type: {script_type}")

        # Render and write
        content = template.render(**config)
        output_path.write_text(content)

        # Make executable if it's a shell script
        if script_type == "sh":
            output_path.chmod(output_path.stat().st_mode | 0o111)

    def xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_173(
        self,
        project_name: str,
        output_path: Path,
        script_type: str = "sh",
        **kwargs: Any,
    ) -> None:
        """Generate an environment setup script.

        Args:
            project_name: Name of the project
            output_path: Path to write the script
            script_type: Either "sh" for bash or "ps1" for PowerShell
            **kwargs: Additional template variables
        """
        # Default configuration
        config = {
            "project_name": project_name,
            "env_profile_var": f"{project_name.upper().replace('-', '_')}_PROFILE",
            "venv_prefix": project_name.lower(),
            "use_spinner": script_type == "sh",  # PowerShell doesn't need spinner
            "strict_project_check": False,
            "install_siblings": True,
            "sibling_patterns": [],  # No default peers - specified in config
            "special_siblings": [],  # No default peers - specified in config
            "create_log_dir": True,
            "deduplicate_path": True,
            "include_tool_verification": True,  # Enable for wrknv
            "cleanup_logs": True,
            "tools_to_verify": [
                {
                    "name": "Python",
                    "command": "python",
                    "check_type": "command",
                    "var_name": "PYTHON",
                    "version_cmd": "python --version 2>&1",
                },
                {
                    "name": "UV",
                    "command": "uv",
                    "check_type": "command",
                    "var_name": "UV",
                    "version_cmd": "uv --version 2>&1",
                },
                {
                    "name": "wrknv",
                    "command": "wrknv",
                    "check_type": "command",
                    "var_name": "WRKENV",
                    "version_cmd": "wrknv --version 2>&1 || echo 'No version info'",
                },
                {
                    "name": "ibmtf",
                    "command": "ibmtf",
                    "check_type": "command",
                    "var_name": "IBMTF",
                    "version_cmd": "ibmtf version 2>&1 | head -1 || echo 'Not installed'",
                },
                {
                    "name": "tofu",
                    "command": "tofu",
                    "check_type": "command",
                    "var_name": "TOFU",
                    "version_cmd": "tofu version 2>&1 | head -1 || echo 'Not installed'",
                },
            ],
            "useful_commands": [
                {
                    "command": f"{project_name.lower()} --help",
                    "description": f"{project_name} CLI",
                },
                {"command": "wrknv status", "description": "Check tool versions"},
                {
                    "command": "wrknv container status",
                    "description": "XXContainer statusXX",
                },
                {"command": "pytest", "description": "Run tests"},
                {"command": "deactivate", "description": "Exit environment"},
            ],
        }

        # Update with user-provided kwargs
        config.update(kwargs)

        # Select appropriate template and environment
        if script_type == "sh":
            template = self.sh_env.get_template("base.sh.j2")
        elif script_type == "ps1":
            template = self.ps1_env.get_template("base.ps1.j2")
        else:
            raise ValueError(f"Unknown script type: {script_type}")

        # Render and write
        content = template.render(**config)
        output_path.write_text(content)

        # Make executable if it's a shell script
        if script_type == "sh":
            output_path.chmod(output_path.stat().st_mode | 0o111)

    def xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_174(
        self,
        project_name: str,
        output_path: Path,
        script_type: str = "sh",
        **kwargs: Any,
    ) -> None:
        """Generate an environment setup script.

        Args:
            project_name: Name of the project
            output_path: Path to write the script
            script_type: Either "sh" for bash or "ps1" for PowerShell
            **kwargs: Additional template variables
        """
        # Default configuration
        config = {
            "project_name": project_name,
            "env_profile_var": f"{project_name.upper().replace('-', '_')}_PROFILE",
            "venv_prefix": project_name.lower(),
            "use_spinner": script_type == "sh",  # PowerShell doesn't need spinner
            "strict_project_check": False,
            "install_siblings": True,
            "sibling_patterns": [],  # No default peers - specified in config
            "special_siblings": [],  # No default peers - specified in config
            "create_log_dir": True,
            "deduplicate_path": True,
            "include_tool_verification": True,  # Enable for wrknv
            "cleanup_logs": True,
            "tools_to_verify": [
                {
                    "name": "Python",
                    "command": "python",
                    "check_type": "command",
                    "var_name": "PYTHON",
                    "version_cmd": "python --version 2>&1",
                },
                {
                    "name": "UV",
                    "command": "uv",
                    "check_type": "command",
                    "var_name": "UV",
                    "version_cmd": "uv --version 2>&1",
                },
                {
                    "name": "wrknv",
                    "command": "wrknv",
                    "check_type": "command",
                    "var_name": "WRKENV",
                    "version_cmd": "wrknv --version 2>&1 || echo 'No version info'",
                },
                {
                    "name": "ibmtf",
                    "command": "ibmtf",
                    "check_type": "command",
                    "var_name": "IBMTF",
                    "version_cmd": "ibmtf version 2>&1 | head -1 || echo 'Not installed'",
                },
                {
                    "name": "tofu",
                    "command": "tofu",
                    "check_type": "command",
                    "var_name": "TOFU",
                    "version_cmd": "tofu version 2>&1 | head -1 || echo 'Not installed'",
                },
            ],
            "useful_commands": [
                {
                    "command": f"{project_name.lower()} --help",
                    "description": f"{project_name} CLI",
                },
                {"command": "wrknv status", "description": "Check tool versions"},
                {
                    "command": "wrknv container status",
                    "description": "container status",
                },
                {"command": "pytest", "description": "Run tests"},
                {"command": "deactivate", "description": "Exit environment"},
            ],
        }

        # Update with user-provided kwargs
        config.update(kwargs)

        # Select appropriate template and environment
        if script_type == "sh":
            template = self.sh_env.get_template("base.sh.j2")
        elif script_type == "ps1":
            template = self.ps1_env.get_template("base.ps1.j2")
        else:
            raise ValueError(f"Unknown script type: {script_type}")

        # Render and write
        content = template.render(**config)
        output_path.write_text(content)

        # Make executable if it's a shell script
        if script_type == "sh":
            output_path.chmod(output_path.stat().st_mode | 0o111)

    def xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_175(
        self,
        project_name: str,
        output_path: Path,
        script_type: str = "sh",
        **kwargs: Any,
    ) -> None:
        """Generate an environment setup script.

        Args:
            project_name: Name of the project
            output_path: Path to write the script
            script_type: Either "sh" for bash or "ps1" for PowerShell
            **kwargs: Additional template variables
        """
        # Default configuration
        config = {
            "project_name": project_name,
            "env_profile_var": f"{project_name.upper().replace('-', '_')}_PROFILE",
            "venv_prefix": project_name.lower(),
            "use_spinner": script_type == "sh",  # PowerShell doesn't need spinner
            "strict_project_check": False,
            "install_siblings": True,
            "sibling_patterns": [],  # No default peers - specified in config
            "special_siblings": [],  # No default peers - specified in config
            "create_log_dir": True,
            "deduplicate_path": True,
            "include_tool_verification": True,  # Enable for wrknv
            "cleanup_logs": True,
            "tools_to_verify": [
                {
                    "name": "Python",
                    "command": "python",
                    "check_type": "command",
                    "var_name": "PYTHON",
                    "version_cmd": "python --version 2>&1",
                },
                {
                    "name": "UV",
                    "command": "uv",
                    "check_type": "command",
                    "var_name": "UV",
                    "version_cmd": "uv --version 2>&1",
                },
                {
                    "name": "wrknv",
                    "command": "wrknv",
                    "check_type": "command",
                    "var_name": "WRKENV",
                    "version_cmd": "wrknv --version 2>&1 || echo 'No version info'",
                },
                {
                    "name": "ibmtf",
                    "command": "ibmtf",
                    "check_type": "command",
                    "var_name": "IBMTF",
                    "version_cmd": "ibmtf version 2>&1 | head -1 || echo 'Not installed'",
                },
                {
                    "name": "tofu",
                    "command": "tofu",
                    "check_type": "command",
                    "var_name": "TOFU",
                    "version_cmd": "tofu version 2>&1 | head -1 || echo 'Not installed'",
                },
            ],
            "useful_commands": [
                {
                    "command": f"{project_name.lower()} --help",
                    "description": f"{project_name} CLI",
                },
                {"command": "wrknv status", "description": "Check tool versions"},
                {
                    "command": "wrknv container status",
                    "description": "CONTAINER STATUS",
                },
                {"command": "pytest", "description": "Run tests"},
                {"command": "deactivate", "description": "Exit environment"},
            ],
        }

        # Update with user-provided kwargs
        config.update(kwargs)

        # Select appropriate template and environment
        if script_type == "sh":
            template = self.sh_env.get_template("base.sh.j2")
        elif script_type == "ps1":
            template = self.ps1_env.get_template("base.ps1.j2")
        else:
            raise ValueError(f"Unknown script type: {script_type}")

        # Render and write
        content = template.render(**config)
        output_path.write_text(content)

        # Make executable if it's a shell script
        if script_type == "sh":
            output_path.chmod(output_path.stat().st_mode | 0o111)

    def xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_176(
        self,
        project_name: str,
        output_path: Path,
        script_type: str = "sh",
        **kwargs: Any,
    ) -> None:
        """Generate an environment setup script.

        Args:
            project_name: Name of the project
            output_path: Path to write the script
            script_type: Either "sh" for bash or "ps1" for PowerShell
            **kwargs: Additional template variables
        """
        # Default configuration
        config = {
            "project_name": project_name,
            "env_profile_var": f"{project_name.upper().replace('-', '_')}_PROFILE",
            "venv_prefix": project_name.lower(),
            "use_spinner": script_type == "sh",  # PowerShell doesn't need spinner
            "strict_project_check": False,
            "install_siblings": True,
            "sibling_patterns": [],  # No default peers - specified in config
            "special_siblings": [],  # No default peers - specified in config
            "create_log_dir": True,
            "deduplicate_path": True,
            "include_tool_verification": True,  # Enable for wrknv
            "cleanup_logs": True,
            "tools_to_verify": [
                {
                    "name": "Python",
                    "command": "python",
                    "check_type": "command",
                    "var_name": "PYTHON",
                    "version_cmd": "python --version 2>&1",
                },
                {
                    "name": "UV",
                    "command": "uv",
                    "check_type": "command",
                    "var_name": "UV",
                    "version_cmd": "uv --version 2>&1",
                },
                {
                    "name": "wrknv",
                    "command": "wrknv",
                    "check_type": "command",
                    "var_name": "WRKENV",
                    "version_cmd": "wrknv --version 2>&1 || echo 'No version info'",
                },
                {
                    "name": "ibmtf",
                    "command": "ibmtf",
                    "check_type": "command",
                    "var_name": "IBMTF",
                    "version_cmd": "ibmtf version 2>&1 | head -1 || echo 'Not installed'",
                },
                {
                    "name": "tofu",
                    "command": "tofu",
                    "check_type": "command",
                    "var_name": "TOFU",
                    "version_cmd": "tofu version 2>&1 | head -1 || echo 'Not installed'",
                },
            ],
            "useful_commands": [
                {
                    "command": f"{project_name.lower()} --help",
                    "description": f"{project_name} CLI",
                },
                {"command": "wrknv status", "description": "Check tool versions"},
                {
                    "command": "wrknv container status",
                    "description": "Container status",
                },
                {"XXcommandXX": "pytest", "description": "Run tests"},
                {"command": "deactivate", "description": "Exit environment"},
            ],
        }

        # Update with user-provided kwargs
        config.update(kwargs)

        # Select appropriate template and environment
        if script_type == "sh":
            template = self.sh_env.get_template("base.sh.j2")
        elif script_type == "ps1":
            template = self.ps1_env.get_template("base.ps1.j2")
        else:
            raise ValueError(f"Unknown script type: {script_type}")

        # Render and write
        content = template.render(**config)
        output_path.write_text(content)

        # Make executable if it's a shell script
        if script_type == "sh":
            output_path.chmod(output_path.stat().st_mode | 0o111)

    def xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_177(
        self,
        project_name: str,
        output_path: Path,
        script_type: str = "sh",
        **kwargs: Any,
    ) -> None:
        """Generate an environment setup script.

        Args:
            project_name: Name of the project
            output_path: Path to write the script
            script_type: Either "sh" for bash or "ps1" for PowerShell
            **kwargs: Additional template variables
        """
        # Default configuration
        config = {
            "project_name": project_name,
            "env_profile_var": f"{project_name.upper().replace('-', '_')}_PROFILE",
            "venv_prefix": project_name.lower(),
            "use_spinner": script_type == "sh",  # PowerShell doesn't need spinner
            "strict_project_check": False,
            "install_siblings": True,
            "sibling_patterns": [],  # No default peers - specified in config
            "special_siblings": [],  # No default peers - specified in config
            "create_log_dir": True,
            "deduplicate_path": True,
            "include_tool_verification": True,  # Enable for wrknv
            "cleanup_logs": True,
            "tools_to_verify": [
                {
                    "name": "Python",
                    "command": "python",
                    "check_type": "command",
                    "var_name": "PYTHON",
                    "version_cmd": "python --version 2>&1",
                },
                {
                    "name": "UV",
                    "command": "uv",
                    "check_type": "command",
                    "var_name": "UV",
                    "version_cmd": "uv --version 2>&1",
                },
                {
                    "name": "wrknv",
                    "command": "wrknv",
                    "check_type": "command",
                    "var_name": "WRKENV",
                    "version_cmd": "wrknv --version 2>&1 || echo 'No version info'",
                },
                {
                    "name": "ibmtf",
                    "command": "ibmtf",
                    "check_type": "command",
                    "var_name": "IBMTF",
                    "version_cmd": "ibmtf version 2>&1 | head -1 || echo 'Not installed'",
                },
                {
                    "name": "tofu",
                    "command": "tofu",
                    "check_type": "command",
                    "var_name": "TOFU",
                    "version_cmd": "tofu version 2>&1 | head -1 || echo 'Not installed'",
                },
            ],
            "useful_commands": [
                {
                    "command": f"{project_name.lower()} --help",
                    "description": f"{project_name} CLI",
                },
                {"command": "wrknv status", "description": "Check tool versions"},
                {
                    "command": "wrknv container status",
                    "description": "Container status",
                },
                {"COMMAND": "pytest", "description": "Run tests"},
                {"command": "deactivate", "description": "Exit environment"},
            ],
        }

        # Update with user-provided kwargs
        config.update(kwargs)

        # Select appropriate template and environment
        if script_type == "sh":
            template = self.sh_env.get_template("base.sh.j2")
        elif script_type == "ps1":
            template = self.ps1_env.get_template("base.ps1.j2")
        else:
            raise ValueError(f"Unknown script type: {script_type}")

        # Render and write
        content = template.render(**config)
        output_path.write_text(content)

        # Make executable if it's a shell script
        if script_type == "sh":
            output_path.chmod(output_path.stat().st_mode | 0o111)

    def xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_178(
        self,
        project_name: str,
        output_path: Path,
        script_type: str = "sh",
        **kwargs: Any,
    ) -> None:
        """Generate an environment setup script.

        Args:
            project_name: Name of the project
            output_path: Path to write the script
            script_type: Either "sh" for bash or "ps1" for PowerShell
            **kwargs: Additional template variables
        """
        # Default configuration
        config = {
            "project_name": project_name,
            "env_profile_var": f"{project_name.upper().replace('-', '_')}_PROFILE",
            "venv_prefix": project_name.lower(),
            "use_spinner": script_type == "sh",  # PowerShell doesn't need spinner
            "strict_project_check": False,
            "install_siblings": True,
            "sibling_patterns": [],  # No default peers - specified in config
            "special_siblings": [],  # No default peers - specified in config
            "create_log_dir": True,
            "deduplicate_path": True,
            "include_tool_verification": True,  # Enable for wrknv
            "cleanup_logs": True,
            "tools_to_verify": [
                {
                    "name": "Python",
                    "command": "python",
                    "check_type": "command",
                    "var_name": "PYTHON",
                    "version_cmd": "python --version 2>&1",
                },
                {
                    "name": "UV",
                    "command": "uv",
                    "check_type": "command",
                    "var_name": "UV",
                    "version_cmd": "uv --version 2>&1",
                },
                {
                    "name": "wrknv",
                    "command": "wrknv",
                    "check_type": "command",
                    "var_name": "WRKENV",
                    "version_cmd": "wrknv --version 2>&1 || echo 'No version info'",
                },
                {
                    "name": "ibmtf",
                    "command": "ibmtf",
                    "check_type": "command",
                    "var_name": "IBMTF",
                    "version_cmd": "ibmtf version 2>&1 | head -1 || echo 'Not installed'",
                },
                {
                    "name": "tofu",
                    "command": "tofu",
                    "check_type": "command",
                    "var_name": "TOFU",
                    "version_cmd": "tofu version 2>&1 | head -1 || echo 'Not installed'",
                },
            ],
            "useful_commands": [
                {
                    "command": f"{project_name.lower()} --help",
                    "description": f"{project_name} CLI",
                },
                {"command": "wrknv status", "description": "Check tool versions"},
                {
                    "command": "wrknv container status",
                    "description": "Container status",
                },
                {"command": "XXpytestXX", "description": "Run tests"},
                {"command": "deactivate", "description": "Exit environment"},
            ],
        }

        # Update with user-provided kwargs
        config.update(kwargs)

        # Select appropriate template and environment
        if script_type == "sh":
            template = self.sh_env.get_template("base.sh.j2")
        elif script_type == "ps1":
            template = self.ps1_env.get_template("base.ps1.j2")
        else:
            raise ValueError(f"Unknown script type: {script_type}")

        # Render and write
        content = template.render(**config)
        output_path.write_text(content)

        # Make executable if it's a shell script
        if script_type == "sh":
            output_path.chmod(output_path.stat().st_mode | 0o111)

    def xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_179(
        self,
        project_name: str,
        output_path: Path,
        script_type: str = "sh",
        **kwargs: Any,
    ) -> None:
        """Generate an environment setup script.

        Args:
            project_name: Name of the project
            output_path: Path to write the script
            script_type: Either "sh" for bash or "ps1" for PowerShell
            **kwargs: Additional template variables
        """
        # Default configuration
        config = {
            "project_name": project_name,
            "env_profile_var": f"{project_name.upper().replace('-', '_')}_PROFILE",
            "venv_prefix": project_name.lower(),
            "use_spinner": script_type == "sh",  # PowerShell doesn't need spinner
            "strict_project_check": False,
            "install_siblings": True,
            "sibling_patterns": [],  # No default peers - specified in config
            "special_siblings": [],  # No default peers - specified in config
            "create_log_dir": True,
            "deduplicate_path": True,
            "include_tool_verification": True,  # Enable for wrknv
            "cleanup_logs": True,
            "tools_to_verify": [
                {
                    "name": "Python",
                    "command": "python",
                    "check_type": "command",
                    "var_name": "PYTHON",
                    "version_cmd": "python --version 2>&1",
                },
                {
                    "name": "UV",
                    "command": "uv",
                    "check_type": "command",
                    "var_name": "UV",
                    "version_cmd": "uv --version 2>&1",
                },
                {
                    "name": "wrknv",
                    "command": "wrknv",
                    "check_type": "command",
                    "var_name": "WRKENV",
                    "version_cmd": "wrknv --version 2>&1 || echo 'No version info'",
                },
                {
                    "name": "ibmtf",
                    "command": "ibmtf",
                    "check_type": "command",
                    "var_name": "IBMTF",
                    "version_cmd": "ibmtf version 2>&1 | head -1 || echo 'Not installed'",
                },
                {
                    "name": "tofu",
                    "command": "tofu",
                    "check_type": "command",
                    "var_name": "TOFU",
                    "version_cmd": "tofu version 2>&1 | head -1 || echo 'Not installed'",
                },
            ],
            "useful_commands": [
                {
                    "command": f"{project_name.lower()} --help",
                    "description": f"{project_name} CLI",
                },
                {"command": "wrknv status", "description": "Check tool versions"},
                {
                    "command": "wrknv container status",
                    "description": "Container status",
                },
                {"command": "PYTEST", "description": "Run tests"},
                {"command": "deactivate", "description": "Exit environment"},
            ],
        }

        # Update with user-provided kwargs
        config.update(kwargs)

        # Select appropriate template and environment
        if script_type == "sh":
            template = self.sh_env.get_template("base.sh.j2")
        elif script_type == "ps1":
            template = self.ps1_env.get_template("base.ps1.j2")
        else:
            raise ValueError(f"Unknown script type: {script_type}")

        # Render and write
        content = template.render(**config)
        output_path.write_text(content)

        # Make executable if it's a shell script
        if script_type == "sh":
            output_path.chmod(output_path.stat().st_mode | 0o111)

    def xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_180(
        self,
        project_name: str,
        output_path: Path,
        script_type: str = "sh",
        **kwargs: Any,
    ) -> None:
        """Generate an environment setup script.

        Args:
            project_name: Name of the project
            output_path: Path to write the script
            script_type: Either "sh" for bash or "ps1" for PowerShell
            **kwargs: Additional template variables
        """
        # Default configuration
        config = {
            "project_name": project_name,
            "env_profile_var": f"{project_name.upper().replace('-', '_')}_PROFILE",
            "venv_prefix": project_name.lower(),
            "use_spinner": script_type == "sh",  # PowerShell doesn't need spinner
            "strict_project_check": False,
            "install_siblings": True,
            "sibling_patterns": [],  # No default peers - specified in config
            "special_siblings": [],  # No default peers - specified in config
            "create_log_dir": True,
            "deduplicate_path": True,
            "include_tool_verification": True,  # Enable for wrknv
            "cleanup_logs": True,
            "tools_to_verify": [
                {
                    "name": "Python",
                    "command": "python",
                    "check_type": "command",
                    "var_name": "PYTHON",
                    "version_cmd": "python --version 2>&1",
                },
                {
                    "name": "UV",
                    "command": "uv",
                    "check_type": "command",
                    "var_name": "UV",
                    "version_cmd": "uv --version 2>&1",
                },
                {
                    "name": "wrknv",
                    "command": "wrknv",
                    "check_type": "command",
                    "var_name": "WRKENV",
                    "version_cmd": "wrknv --version 2>&1 || echo 'No version info'",
                },
                {
                    "name": "ibmtf",
                    "command": "ibmtf",
                    "check_type": "command",
                    "var_name": "IBMTF",
                    "version_cmd": "ibmtf version 2>&1 | head -1 || echo 'Not installed'",
                },
                {
                    "name": "tofu",
                    "command": "tofu",
                    "check_type": "command",
                    "var_name": "TOFU",
                    "version_cmd": "tofu version 2>&1 | head -1 || echo 'Not installed'",
                },
            ],
            "useful_commands": [
                {
                    "command": f"{project_name.lower()} --help",
                    "description": f"{project_name} CLI",
                },
                {"command": "wrknv status", "description": "Check tool versions"},
                {
                    "command": "wrknv container status",
                    "description": "Container status",
                },
                {"command": "pytest", "XXdescriptionXX": "Run tests"},
                {"command": "deactivate", "description": "Exit environment"},
            ],
        }

        # Update with user-provided kwargs
        config.update(kwargs)

        # Select appropriate template and environment
        if script_type == "sh":
            template = self.sh_env.get_template("base.sh.j2")
        elif script_type == "ps1":
            template = self.ps1_env.get_template("base.ps1.j2")
        else:
            raise ValueError(f"Unknown script type: {script_type}")

        # Render and write
        content = template.render(**config)
        output_path.write_text(content)

        # Make executable if it's a shell script
        if script_type == "sh":
            output_path.chmod(output_path.stat().st_mode | 0o111)

    def xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_181(
        self,
        project_name: str,
        output_path: Path,
        script_type: str = "sh",
        **kwargs: Any,
    ) -> None:
        """Generate an environment setup script.

        Args:
            project_name: Name of the project
            output_path: Path to write the script
            script_type: Either "sh" for bash or "ps1" for PowerShell
            **kwargs: Additional template variables
        """
        # Default configuration
        config = {
            "project_name": project_name,
            "env_profile_var": f"{project_name.upper().replace('-', '_')}_PROFILE",
            "venv_prefix": project_name.lower(),
            "use_spinner": script_type == "sh",  # PowerShell doesn't need spinner
            "strict_project_check": False,
            "install_siblings": True,
            "sibling_patterns": [],  # No default peers - specified in config
            "special_siblings": [],  # No default peers - specified in config
            "create_log_dir": True,
            "deduplicate_path": True,
            "include_tool_verification": True,  # Enable for wrknv
            "cleanup_logs": True,
            "tools_to_verify": [
                {
                    "name": "Python",
                    "command": "python",
                    "check_type": "command",
                    "var_name": "PYTHON",
                    "version_cmd": "python --version 2>&1",
                },
                {
                    "name": "UV",
                    "command": "uv",
                    "check_type": "command",
                    "var_name": "UV",
                    "version_cmd": "uv --version 2>&1",
                },
                {
                    "name": "wrknv",
                    "command": "wrknv",
                    "check_type": "command",
                    "var_name": "WRKENV",
                    "version_cmd": "wrknv --version 2>&1 || echo 'No version info'",
                },
                {
                    "name": "ibmtf",
                    "command": "ibmtf",
                    "check_type": "command",
                    "var_name": "IBMTF",
                    "version_cmd": "ibmtf version 2>&1 | head -1 || echo 'Not installed'",
                },
                {
                    "name": "tofu",
                    "command": "tofu",
                    "check_type": "command",
                    "var_name": "TOFU",
                    "version_cmd": "tofu version 2>&1 | head -1 || echo 'Not installed'",
                },
            ],
            "useful_commands": [
                {
                    "command": f"{project_name.lower()} --help",
                    "description": f"{project_name} CLI",
                },
                {"command": "wrknv status", "description": "Check tool versions"},
                {
                    "command": "wrknv container status",
                    "description": "Container status",
                },
                {"command": "pytest", "DESCRIPTION": "Run tests"},
                {"command": "deactivate", "description": "Exit environment"},
            ],
        }

        # Update with user-provided kwargs
        config.update(kwargs)

        # Select appropriate template and environment
        if script_type == "sh":
            template = self.sh_env.get_template("base.sh.j2")
        elif script_type == "ps1":
            template = self.ps1_env.get_template("base.ps1.j2")
        else:
            raise ValueError(f"Unknown script type: {script_type}")

        # Render and write
        content = template.render(**config)
        output_path.write_text(content)

        # Make executable if it's a shell script
        if script_type == "sh":
            output_path.chmod(output_path.stat().st_mode | 0o111)

    def xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_182(
        self,
        project_name: str,
        output_path: Path,
        script_type: str = "sh",
        **kwargs: Any,
    ) -> None:
        """Generate an environment setup script.

        Args:
            project_name: Name of the project
            output_path: Path to write the script
            script_type: Either "sh" for bash or "ps1" for PowerShell
            **kwargs: Additional template variables
        """
        # Default configuration
        config = {
            "project_name": project_name,
            "env_profile_var": f"{project_name.upper().replace('-', '_')}_PROFILE",
            "venv_prefix": project_name.lower(),
            "use_spinner": script_type == "sh",  # PowerShell doesn't need spinner
            "strict_project_check": False,
            "install_siblings": True,
            "sibling_patterns": [],  # No default peers - specified in config
            "special_siblings": [],  # No default peers - specified in config
            "create_log_dir": True,
            "deduplicate_path": True,
            "include_tool_verification": True,  # Enable for wrknv
            "cleanup_logs": True,
            "tools_to_verify": [
                {
                    "name": "Python",
                    "command": "python",
                    "check_type": "command",
                    "var_name": "PYTHON",
                    "version_cmd": "python --version 2>&1",
                },
                {
                    "name": "UV",
                    "command": "uv",
                    "check_type": "command",
                    "var_name": "UV",
                    "version_cmd": "uv --version 2>&1",
                },
                {
                    "name": "wrknv",
                    "command": "wrknv",
                    "check_type": "command",
                    "var_name": "WRKENV",
                    "version_cmd": "wrknv --version 2>&1 || echo 'No version info'",
                },
                {
                    "name": "ibmtf",
                    "command": "ibmtf",
                    "check_type": "command",
                    "var_name": "IBMTF",
                    "version_cmd": "ibmtf version 2>&1 | head -1 || echo 'Not installed'",
                },
                {
                    "name": "tofu",
                    "command": "tofu",
                    "check_type": "command",
                    "var_name": "TOFU",
                    "version_cmd": "tofu version 2>&1 | head -1 || echo 'Not installed'",
                },
            ],
            "useful_commands": [
                {
                    "command": f"{project_name.lower()} --help",
                    "description": f"{project_name} CLI",
                },
                {"command": "wrknv status", "description": "Check tool versions"},
                {
                    "command": "wrknv container status",
                    "description": "Container status",
                },
                {"command": "pytest", "description": "XXRun testsXX"},
                {"command": "deactivate", "description": "Exit environment"},
            ],
        }

        # Update with user-provided kwargs
        config.update(kwargs)

        # Select appropriate template and environment
        if script_type == "sh":
            template = self.sh_env.get_template("base.sh.j2")
        elif script_type == "ps1":
            template = self.ps1_env.get_template("base.ps1.j2")
        else:
            raise ValueError(f"Unknown script type: {script_type}")

        # Render and write
        content = template.render(**config)
        output_path.write_text(content)

        # Make executable if it's a shell script
        if script_type == "sh":
            output_path.chmod(output_path.stat().st_mode | 0o111)

    def xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_183(
        self,
        project_name: str,
        output_path: Path,
        script_type: str = "sh",
        **kwargs: Any,
    ) -> None:
        """Generate an environment setup script.

        Args:
            project_name: Name of the project
            output_path: Path to write the script
            script_type: Either "sh" for bash or "ps1" for PowerShell
            **kwargs: Additional template variables
        """
        # Default configuration
        config = {
            "project_name": project_name,
            "env_profile_var": f"{project_name.upper().replace('-', '_')}_PROFILE",
            "venv_prefix": project_name.lower(),
            "use_spinner": script_type == "sh",  # PowerShell doesn't need spinner
            "strict_project_check": False,
            "install_siblings": True,
            "sibling_patterns": [],  # No default peers - specified in config
            "special_siblings": [],  # No default peers - specified in config
            "create_log_dir": True,
            "deduplicate_path": True,
            "include_tool_verification": True,  # Enable for wrknv
            "cleanup_logs": True,
            "tools_to_verify": [
                {
                    "name": "Python",
                    "command": "python",
                    "check_type": "command",
                    "var_name": "PYTHON",
                    "version_cmd": "python --version 2>&1",
                },
                {
                    "name": "UV",
                    "command": "uv",
                    "check_type": "command",
                    "var_name": "UV",
                    "version_cmd": "uv --version 2>&1",
                },
                {
                    "name": "wrknv",
                    "command": "wrknv",
                    "check_type": "command",
                    "var_name": "WRKENV",
                    "version_cmd": "wrknv --version 2>&1 || echo 'No version info'",
                },
                {
                    "name": "ibmtf",
                    "command": "ibmtf",
                    "check_type": "command",
                    "var_name": "IBMTF",
                    "version_cmd": "ibmtf version 2>&1 | head -1 || echo 'Not installed'",
                },
                {
                    "name": "tofu",
                    "command": "tofu",
                    "check_type": "command",
                    "var_name": "TOFU",
                    "version_cmd": "tofu version 2>&1 | head -1 || echo 'Not installed'",
                },
            ],
            "useful_commands": [
                {
                    "command": f"{project_name.lower()} --help",
                    "description": f"{project_name} CLI",
                },
                {"command": "wrknv status", "description": "Check tool versions"},
                {
                    "command": "wrknv container status",
                    "description": "Container status",
                },
                {"command": "pytest", "description": "run tests"},
                {"command": "deactivate", "description": "Exit environment"},
            ],
        }

        # Update with user-provided kwargs
        config.update(kwargs)

        # Select appropriate template and environment
        if script_type == "sh":
            template = self.sh_env.get_template("base.sh.j2")
        elif script_type == "ps1":
            template = self.ps1_env.get_template("base.ps1.j2")
        else:
            raise ValueError(f"Unknown script type: {script_type}")

        # Render and write
        content = template.render(**config)
        output_path.write_text(content)

        # Make executable if it's a shell script
        if script_type == "sh":
            output_path.chmod(output_path.stat().st_mode | 0o111)

    def xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_184(
        self,
        project_name: str,
        output_path: Path,
        script_type: str = "sh",
        **kwargs: Any,
    ) -> None:
        """Generate an environment setup script.

        Args:
            project_name: Name of the project
            output_path: Path to write the script
            script_type: Either "sh" for bash or "ps1" for PowerShell
            **kwargs: Additional template variables
        """
        # Default configuration
        config = {
            "project_name": project_name,
            "env_profile_var": f"{project_name.upper().replace('-', '_')}_PROFILE",
            "venv_prefix": project_name.lower(),
            "use_spinner": script_type == "sh",  # PowerShell doesn't need spinner
            "strict_project_check": False,
            "install_siblings": True,
            "sibling_patterns": [],  # No default peers - specified in config
            "special_siblings": [],  # No default peers - specified in config
            "create_log_dir": True,
            "deduplicate_path": True,
            "include_tool_verification": True,  # Enable for wrknv
            "cleanup_logs": True,
            "tools_to_verify": [
                {
                    "name": "Python",
                    "command": "python",
                    "check_type": "command",
                    "var_name": "PYTHON",
                    "version_cmd": "python --version 2>&1",
                },
                {
                    "name": "UV",
                    "command": "uv",
                    "check_type": "command",
                    "var_name": "UV",
                    "version_cmd": "uv --version 2>&1",
                },
                {
                    "name": "wrknv",
                    "command": "wrknv",
                    "check_type": "command",
                    "var_name": "WRKENV",
                    "version_cmd": "wrknv --version 2>&1 || echo 'No version info'",
                },
                {
                    "name": "ibmtf",
                    "command": "ibmtf",
                    "check_type": "command",
                    "var_name": "IBMTF",
                    "version_cmd": "ibmtf version 2>&1 | head -1 || echo 'Not installed'",
                },
                {
                    "name": "tofu",
                    "command": "tofu",
                    "check_type": "command",
                    "var_name": "TOFU",
                    "version_cmd": "tofu version 2>&1 | head -1 || echo 'Not installed'",
                },
            ],
            "useful_commands": [
                {
                    "command": f"{project_name.lower()} --help",
                    "description": f"{project_name} CLI",
                },
                {"command": "wrknv status", "description": "Check tool versions"},
                {
                    "command": "wrknv container status",
                    "description": "Container status",
                },
                {"command": "pytest", "description": "RUN TESTS"},
                {"command": "deactivate", "description": "Exit environment"},
            ],
        }

        # Update with user-provided kwargs
        config.update(kwargs)

        # Select appropriate template and environment
        if script_type == "sh":
            template = self.sh_env.get_template("base.sh.j2")
        elif script_type == "ps1":
            template = self.ps1_env.get_template("base.ps1.j2")
        else:
            raise ValueError(f"Unknown script type: {script_type}")

        # Render and write
        content = template.render(**config)
        output_path.write_text(content)

        # Make executable if it's a shell script
        if script_type == "sh":
            output_path.chmod(output_path.stat().st_mode | 0o111)

    def xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_185(
        self,
        project_name: str,
        output_path: Path,
        script_type: str = "sh",
        **kwargs: Any,
    ) -> None:
        """Generate an environment setup script.

        Args:
            project_name: Name of the project
            output_path: Path to write the script
            script_type: Either "sh" for bash or "ps1" for PowerShell
            **kwargs: Additional template variables
        """
        # Default configuration
        config = {
            "project_name": project_name,
            "env_profile_var": f"{project_name.upper().replace('-', '_')}_PROFILE",
            "venv_prefix": project_name.lower(),
            "use_spinner": script_type == "sh",  # PowerShell doesn't need spinner
            "strict_project_check": False,
            "install_siblings": True,
            "sibling_patterns": [],  # No default peers - specified in config
            "special_siblings": [],  # No default peers - specified in config
            "create_log_dir": True,
            "deduplicate_path": True,
            "include_tool_verification": True,  # Enable for wrknv
            "cleanup_logs": True,
            "tools_to_verify": [
                {
                    "name": "Python",
                    "command": "python",
                    "check_type": "command",
                    "var_name": "PYTHON",
                    "version_cmd": "python --version 2>&1",
                },
                {
                    "name": "UV",
                    "command": "uv",
                    "check_type": "command",
                    "var_name": "UV",
                    "version_cmd": "uv --version 2>&1",
                },
                {
                    "name": "wrknv",
                    "command": "wrknv",
                    "check_type": "command",
                    "var_name": "WRKENV",
                    "version_cmd": "wrknv --version 2>&1 || echo 'No version info'",
                },
                {
                    "name": "ibmtf",
                    "command": "ibmtf",
                    "check_type": "command",
                    "var_name": "IBMTF",
                    "version_cmd": "ibmtf version 2>&1 | head -1 || echo 'Not installed'",
                },
                {
                    "name": "tofu",
                    "command": "tofu",
                    "check_type": "command",
                    "var_name": "TOFU",
                    "version_cmd": "tofu version 2>&1 | head -1 || echo 'Not installed'",
                },
            ],
            "useful_commands": [
                {
                    "command": f"{project_name.lower()} --help",
                    "description": f"{project_name} CLI",
                },
                {"command": "wrknv status", "description": "Check tool versions"},
                {
                    "command": "wrknv container status",
                    "description": "Container status",
                },
                {"command": "pytest", "description": "Run tests"},
                {"XXcommandXX": "deactivate", "description": "Exit environment"},
            ],
        }

        # Update with user-provided kwargs
        config.update(kwargs)

        # Select appropriate template and environment
        if script_type == "sh":
            template = self.sh_env.get_template("base.sh.j2")
        elif script_type == "ps1":
            template = self.ps1_env.get_template("base.ps1.j2")
        else:
            raise ValueError(f"Unknown script type: {script_type}")

        # Render and write
        content = template.render(**config)
        output_path.write_text(content)

        # Make executable if it's a shell script
        if script_type == "sh":
            output_path.chmod(output_path.stat().st_mode | 0o111)

    def xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_186(
        self,
        project_name: str,
        output_path: Path,
        script_type: str = "sh",
        **kwargs: Any,
    ) -> None:
        """Generate an environment setup script.

        Args:
            project_name: Name of the project
            output_path: Path to write the script
            script_type: Either "sh" for bash or "ps1" for PowerShell
            **kwargs: Additional template variables
        """
        # Default configuration
        config = {
            "project_name": project_name,
            "env_profile_var": f"{project_name.upper().replace('-', '_')}_PROFILE",
            "venv_prefix": project_name.lower(),
            "use_spinner": script_type == "sh",  # PowerShell doesn't need spinner
            "strict_project_check": False,
            "install_siblings": True,
            "sibling_patterns": [],  # No default peers - specified in config
            "special_siblings": [],  # No default peers - specified in config
            "create_log_dir": True,
            "deduplicate_path": True,
            "include_tool_verification": True,  # Enable for wrknv
            "cleanup_logs": True,
            "tools_to_verify": [
                {
                    "name": "Python",
                    "command": "python",
                    "check_type": "command",
                    "var_name": "PYTHON",
                    "version_cmd": "python --version 2>&1",
                },
                {
                    "name": "UV",
                    "command": "uv",
                    "check_type": "command",
                    "var_name": "UV",
                    "version_cmd": "uv --version 2>&1",
                },
                {
                    "name": "wrknv",
                    "command": "wrknv",
                    "check_type": "command",
                    "var_name": "WRKENV",
                    "version_cmd": "wrknv --version 2>&1 || echo 'No version info'",
                },
                {
                    "name": "ibmtf",
                    "command": "ibmtf",
                    "check_type": "command",
                    "var_name": "IBMTF",
                    "version_cmd": "ibmtf version 2>&1 | head -1 || echo 'Not installed'",
                },
                {
                    "name": "tofu",
                    "command": "tofu",
                    "check_type": "command",
                    "var_name": "TOFU",
                    "version_cmd": "tofu version 2>&1 | head -1 || echo 'Not installed'",
                },
            ],
            "useful_commands": [
                {
                    "command": f"{project_name.lower()} --help",
                    "description": f"{project_name} CLI",
                },
                {"command": "wrknv status", "description": "Check tool versions"},
                {
                    "command": "wrknv container status",
                    "description": "Container status",
                },
                {"command": "pytest", "description": "Run tests"},
                {"COMMAND": "deactivate", "description": "Exit environment"},
            ],
        }

        # Update with user-provided kwargs
        config.update(kwargs)

        # Select appropriate template and environment
        if script_type == "sh":
            template = self.sh_env.get_template("base.sh.j2")
        elif script_type == "ps1":
            template = self.ps1_env.get_template("base.ps1.j2")
        else:
            raise ValueError(f"Unknown script type: {script_type}")

        # Render and write
        content = template.render(**config)
        output_path.write_text(content)

        # Make executable if it's a shell script
        if script_type == "sh":
            output_path.chmod(output_path.stat().st_mode | 0o111)

    def xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_187(
        self,
        project_name: str,
        output_path: Path,
        script_type: str = "sh",
        **kwargs: Any,
    ) -> None:
        """Generate an environment setup script.

        Args:
            project_name: Name of the project
            output_path: Path to write the script
            script_type: Either "sh" for bash or "ps1" for PowerShell
            **kwargs: Additional template variables
        """
        # Default configuration
        config = {
            "project_name": project_name,
            "env_profile_var": f"{project_name.upper().replace('-', '_')}_PROFILE",
            "venv_prefix": project_name.lower(),
            "use_spinner": script_type == "sh",  # PowerShell doesn't need spinner
            "strict_project_check": False,
            "install_siblings": True,
            "sibling_patterns": [],  # No default peers - specified in config
            "special_siblings": [],  # No default peers - specified in config
            "create_log_dir": True,
            "deduplicate_path": True,
            "include_tool_verification": True,  # Enable for wrknv
            "cleanup_logs": True,
            "tools_to_verify": [
                {
                    "name": "Python",
                    "command": "python",
                    "check_type": "command",
                    "var_name": "PYTHON",
                    "version_cmd": "python --version 2>&1",
                },
                {
                    "name": "UV",
                    "command": "uv",
                    "check_type": "command",
                    "var_name": "UV",
                    "version_cmd": "uv --version 2>&1",
                },
                {
                    "name": "wrknv",
                    "command": "wrknv",
                    "check_type": "command",
                    "var_name": "WRKENV",
                    "version_cmd": "wrknv --version 2>&1 || echo 'No version info'",
                },
                {
                    "name": "ibmtf",
                    "command": "ibmtf",
                    "check_type": "command",
                    "var_name": "IBMTF",
                    "version_cmd": "ibmtf version 2>&1 | head -1 || echo 'Not installed'",
                },
                {
                    "name": "tofu",
                    "command": "tofu",
                    "check_type": "command",
                    "var_name": "TOFU",
                    "version_cmd": "tofu version 2>&1 | head -1 || echo 'Not installed'",
                },
            ],
            "useful_commands": [
                {
                    "command": f"{project_name.lower()} --help",
                    "description": f"{project_name} CLI",
                },
                {"command": "wrknv status", "description": "Check tool versions"},
                {
                    "command": "wrknv container status",
                    "description": "Container status",
                },
                {"command": "pytest", "description": "Run tests"},
                {"command": "XXdeactivateXX", "description": "Exit environment"},
            ],
        }

        # Update with user-provided kwargs
        config.update(kwargs)

        # Select appropriate template and environment
        if script_type == "sh":
            template = self.sh_env.get_template("base.sh.j2")
        elif script_type == "ps1":
            template = self.ps1_env.get_template("base.ps1.j2")
        else:
            raise ValueError(f"Unknown script type: {script_type}")

        # Render and write
        content = template.render(**config)
        output_path.write_text(content)

        # Make executable if it's a shell script
        if script_type == "sh":
            output_path.chmod(output_path.stat().st_mode | 0o111)

    def xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_188(
        self,
        project_name: str,
        output_path: Path,
        script_type: str = "sh",
        **kwargs: Any,
    ) -> None:
        """Generate an environment setup script.

        Args:
            project_name: Name of the project
            output_path: Path to write the script
            script_type: Either "sh" for bash or "ps1" for PowerShell
            **kwargs: Additional template variables
        """
        # Default configuration
        config = {
            "project_name": project_name,
            "env_profile_var": f"{project_name.upper().replace('-', '_')}_PROFILE",
            "venv_prefix": project_name.lower(),
            "use_spinner": script_type == "sh",  # PowerShell doesn't need spinner
            "strict_project_check": False,
            "install_siblings": True,
            "sibling_patterns": [],  # No default peers - specified in config
            "special_siblings": [],  # No default peers - specified in config
            "create_log_dir": True,
            "deduplicate_path": True,
            "include_tool_verification": True,  # Enable for wrknv
            "cleanup_logs": True,
            "tools_to_verify": [
                {
                    "name": "Python",
                    "command": "python",
                    "check_type": "command",
                    "var_name": "PYTHON",
                    "version_cmd": "python --version 2>&1",
                },
                {
                    "name": "UV",
                    "command": "uv",
                    "check_type": "command",
                    "var_name": "UV",
                    "version_cmd": "uv --version 2>&1",
                },
                {
                    "name": "wrknv",
                    "command": "wrknv",
                    "check_type": "command",
                    "var_name": "WRKENV",
                    "version_cmd": "wrknv --version 2>&1 || echo 'No version info'",
                },
                {
                    "name": "ibmtf",
                    "command": "ibmtf",
                    "check_type": "command",
                    "var_name": "IBMTF",
                    "version_cmd": "ibmtf version 2>&1 | head -1 || echo 'Not installed'",
                },
                {
                    "name": "tofu",
                    "command": "tofu",
                    "check_type": "command",
                    "var_name": "TOFU",
                    "version_cmd": "tofu version 2>&1 | head -1 || echo 'Not installed'",
                },
            ],
            "useful_commands": [
                {
                    "command": f"{project_name.lower()} --help",
                    "description": f"{project_name} CLI",
                },
                {"command": "wrknv status", "description": "Check tool versions"},
                {
                    "command": "wrknv container status",
                    "description": "Container status",
                },
                {"command": "pytest", "description": "Run tests"},
                {"command": "DEACTIVATE", "description": "Exit environment"},
            ],
        }

        # Update with user-provided kwargs
        config.update(kwargs)

        # Select appropriate template and environment
        if script_type == "sh":
            template = self.sh_env.get_template("base.sh.j2")
        elif script_type == "ps1":
            template = self.ps1_env.get_template("base.ps1.j2")
        else:
            raise ValueError(f"Unknown script type: {script_type}")

        # Render and write
        content = template.render(**config)
        output_path.write_text(content)

        # Make executable if it's a shell script
        if script_type == "sh":
            output_path.chmod(output_path.stat().st_mode | 0o111)

    def xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_189(
        self,
        project_name: str,
        output_path: Path,
        script_type: str = "sh",
        **kwargs: Any,
    ) -> None:
        """Generate an environment setup script.

        Args:
            project_name: Name of the project
            output_path: Path to write the script
            script_type: Either "sh" for bash or "ps1" for PowerShell
            **kwargs: Additional template variables
        """
        # Default configuration
        config = {
            "project_name": project_name,
            "env_profile_var": f"{project_name.upper().replace('-', '_')}_PROFILE",
            "venv_prefix": project_name.lower(),
            "use_spinner": script_type == "sh",  # PowerShell doesn't need spinner
            "strict_project_check": False,
            "install_siblings": True,
            "sibling_patterns": [],  # No default peers - specified in config
            "special_siblings": [],  # No default peers - specified in config
            "create_log_dir": True,
            "deduplicate_path": True,
            "include_tool_verification": True,  # Enable for wrknv
            "cleanup_logs": True,
            "tools_to_verify": [
                {
                    "name": "Python",
                    "command": "python",
                    "check_type": "command",
                    "var_name": "PYTHON",
                    "version_cmd": "python --version 2>&1",
                },
                {
                    "name": "UV",
                    "command": "uv",
                    "check_type": "command",
                    "var_name": "UV",
                    "version_cmd": "uv --version 2>&1",
                },
                {
                    "name": "wrknv",
                    "command": "wrknv",
                    "check_type": "command",
                    "var_name": "WRKENV",
                    "version_cmd": "wrknv --version 2>&1 || echo 'No version info'",
                },
                {
                    "name": "ibmtf",
                    "command": "ibmtf",
                    "check_type": "command",
                    "var_name": "IBMTF",
                    "version_cmd": "ibmtf version 2>&1 | head -1 || echo 'Not installed'",
                },
                {
                    "name": "tofu",
                    "command": "tofu",
                    "check_type": "command",
                    "var_name": "TOFU",
                    "version_cmd": "tofu version 2>&1 | head -1 || echo 'Not installed'",
                },
            ],
            "useful_commands": [
                {
                    "command": f"{project_name.lower()} --help",
                    "description": f"{project_name} CLI",
                },
                {"command": "wrknv status", "description": "Check tool versions"},
                {
                    "command": "wrknv container status",
                    "description": "Container status",
                },
                {"command": "pytest", "description": "Run tests"},
                {"command": "deactivate", "XXdescriptionXX": "Exit environment"},
            ],
        }

        # Update with user-provided kwargs
        config.update(kwargs)

        # Select appropriate template and environment
        if script_type == "sh":
            template = self.sh_env.get_template("base.sh.j2")
        elif script_type == "ps1":
            template = self.ps1_env.get_template("base.ps1.j2")
        else:
            raise ValueError(f"Unknown script type: {script_type}")

        # Render and write
        content = template.render(**config)
        output_path.write_text(content)

        # Make executable if it's a shell script
        if script_type == "sh":
            output_path.chmod(output_path.stat().st_mode | 0o111)

    def xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_190(
        self,
        project_name: str,
        output_path: Path,
        script_type: str = "sh",
        **kwargs: Any,
    ) -> None:
        """Generate an environment setup script.

        Args:
            project_name: Name of the project
            output_path: Path to write the script
            script_type: Either "sh" for bash or "ps1" for PowerShell
            **kwargs: Additional template variables
        """
        # Default configuration
        config = {
            "project_name": project_name,
            "env_profile_var": f"{project_name.upper().replace('-', '_')}_PROFILE",
            "venv_prefix": project_name.lower(),
            "use_spinner": script_type == "sh",  # PowerShell doesn't need spinner
            "strict_project_check": False,
            "install_siblings": True,
            "sibling_patterns": [],  # No default peers - specified in config
            "special_siblings": [],  # No default peers - specified in config
            "create_log_dir": True,
            "deduplicate_path": True,
            "include_tool_verification": True,  # Enable for wrknv
            "cleanup_logs": True,
            "tools_to_verify": [
                {
                    "name": "Python",
                    "command": "python",
                    "check_type": "command",
                    "var_name": "PYTHON",
                    "version_cmd": "python --version 2>&1",
                },
                {
                    "name": "UV",
                    "command": "uv",
                    "check_type": "command",
                    "var_name": "UV",
                    "version_cmd": "uv --version 2>&1",
                },
                {
                    "name": "wrknv",
                    "command": "wrknv",
                    "check_type": "command",
                    "var_name": "WRKENV",
                    "version_cmd": "wrknv --version 2>&1 || echo 'No version info'",
                },
                {
                    "name": "ibmtf",
                    "command": "ibmtf",
                    "check_type": "command",
                    "var_name": "IBMTF",
                    "version_cmd": "ibmtf version 2>&1 | head -1 || echo 'Not installed'",
                },
                {
                    "name": "tofu",
                    "command": "tofu",
                    "check_type": "command",
                    "var_name": "TOFU",
                    "version_cmd": "tofu version 2>&1 | head -1 || echo 'Not installed'",
                },
            ],
            "useful_commands": [
                {
                    "command": f"{project_name.lower()} --help",
                    "description": f"{project_name} CLI",
                },
                {"command": "wrknv status", "description": "Check tool versions"},
                {
                    "command": "wrknv container status",
                    "description": "Container status",
                },
                {"command": "pytest", "description": "Run tests"},
                {"command": "deactivate", "DESCRIPTION": "Exit environment"},
            ],
        }

        # Update with user-provided kwargs
        config.update(kwargs)

        # Select appropriate template and environment
        if script_type == "sh":
            template = self.sh_env.get_template("base.sh.j2")
        elif script_type == "ps1":
            template = self.ps1_env.get_template("base.ps1.j2")
        else:
            raise ValueError(f"Unknown script type: {script_type}")

        # Render and write
        content = template.render(**config)
        output_path.write_text(content)

        # Make executable if it's a shell script
        if script_type == "sh":
            output_path.chmod(output_path.stat().st_mode | 0o111)

    def xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_191(
        self,
        project_name: str,
        output_path: Path,
        script_type: str = "sh",
        **kwargs: Any,
    ) -> None:
        """Generate an environment setup script.

        Args:
            project_name: Name of the project
            output_path: Path to write the script
            script_type: Either "sh" for bash or "ps1" for PowerShell
            **kwargs: Additional template variables
        """
        # Default configuration
        config = {
            "project_name": project_name,
            "env_profile_var": f"{project_name.upper().replace('-', '_')}_PROFILE",
            "venv_prefix": project_name.lower(),
            "use_spinner": script_type == "sh",  # PowerShell doesn't need spinner
            "strict_project_check": False,
            "install_siblings": True,
            "sibling_patterns": [],  # No default peers - specified in config
            "special_siblings": [],  # No default peers - specified in config
            "create_log_dir": True,
            "deduplicate_path": True,
            "include_tool_verification": True,  # Enable for wrknv
            "cleanup_logs": True,
            "tools_to_verify": [
                {
                    "name": "Python",
                    "command": "python",
                    "check_type": "command",
                    "var_name": "PYTHON",
                    "version_cmd": "python --version 2>&1",
                },
                {
                    "name": "UV",
                    "command": "uv",
                    "check_type": "command",
                    "var_name": "UV",
                    "version_cmd": "uv --version 2>&1",
                },
                {
                    "name": "wrknv",
                    "command": "wrknv",
                    "check_type": "command",
                    "var_name": "WRKENV",
                    "version_cmd": "wrknv --version 2>&1 || echo 'No version info'",
                },
                {
                    "name": "ibmtf",
                    "command": "ibmtf",
                    "check_type": "command",
                    "var_name": "IBMTF",
                    "version_cmd": "ibmtf version 2>&1 | head -1 || echo 'Not installed'",
                },
                {
                    "name": "tofu",
                    "command": "tofu",
                    "check_type": "command",
                    "var_name": "TOFU",
                    "version_cmd": "tofu version 2>&1 | head -1 || echo 'Not installed'",
                },
            ],
            "useful_commands": [
                {
                    "command": f"{project_name.lower()} --help",
                    "description": f"{project_name} CLI",
                },
                {"command": "wrknv status", "description": "Check tool versions"},
                {
                    "command": "wrknv container status",
                    "description": "Container status",
                },
                {"command": "pytest", "description": "Run tests"},
                {"command": "deactivate", "description": "XXExit environmentXX"},
            ],
        }

        # Update with user-provided kwargs
        config.update(kwargs)

        # Select appropriate template and environment
        if script_type == "sh":
            template = self.sh_env.get_template("base.sh.j2")
        elif script_type == "ps1":
            template = self.ps1_env.get_template("base.ps1.j2")
        else:
            raise ValueError(f"Unknown script type: {script_type}")

        # Render and write
        content = template.render(**config)
        output_path.write_text(content)

        # Make executable if it's a shell script
        if script_type == "sh":
            output_path.chmod(output_path.stat().st_mode | 0o111)

    def xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_192(
        self,
        project_name: str,
        output_path: Path,
        script_type: str = "sh",
        **kwargs: Any,
    ) -> None:
        """Generate an environment setup script.

        Args:
            project_name: Name of the project
            output_path: Path to write the script
            script_type: Either "sh" for bash or "ps1" for PowerShell
            **kwargs: Additional template variables
        """
        # Default configuration
        config = {
            "project_name": project_name,
            "env_profile_var": f"{project_name.upper().replace('-', '_')}_PROFILE",
            "venv_prefix": project_name.lower(),
            "use_spinner": script_type == "sh",  # PowerShell doesn't need spinner
            "strict_project_check": False,
            "install_siblings": True,
            "sibling_patterns": [],  # No default peers - specified in config
            "special_siblings": [],  # No default peers - specified in config
            "create_log_dir": True,
            "deduplicate_path": True,
            "include_tool_verification": True,  # Enable for wrknv
            "cleanup_logs": True,
            "tools_to_verify": [
                {
                    "name": "Python",
                    "command": "python",
                    "check_type": "command",
                    "var_name": "PYTHON",
                    "version_cmd": "python --version 2>&1",
                },
                {
                    "name": "UV",
                    "command": "uv",
                    "check_type": "command",
                    "var_name": "UV",
                    "version_cmd": "uv --version 2>&1",
                },
                {
                    "name": "wrknv",
                    "command": "wrknv",
                    "check_type": "command",
                    "var_name": "WRKENV",
                    "version_cmd": "wrknv --version 2>&1 || echo 'No version info'",
                },
                {
                    "name": "ibmtf",
                    "command": "ibmtf",
                    "check_type": "command",
                    "var_name": "IBMTF",
                    "version_cmd": "ibmtf version 2>&1 | head -1 || echo 'Not installed'",
                },
                {
                    "name": "tofu",
                    "command": "tofu",
                    "check_type": "command",
                    "var_name": "TOFU",
                    "version_cmd": "tofu version 2>&1 | head -1 || echo 'Not installed'",
                },
            ],
            "useful_commands": [
                {
                    "command": f"{project_name.lower()} --help",
                    "description": f"{project_name} CLI",
                },
                {"command": "wrknv status", "description": "Check tool versions"},
                {
                    "command": "wrknv container status",
                    "description": "Container status",
                },
                {"command": "pytest", "description": "Run tests"},
                {"command": "deactivate", "description": "exit environment"},
            ],
        }

        # Update with user-provided kwargs
        config.update(kwargs)

        # Select appropriate template and environment
        if script_type == "sh":
            template = self.sh_env.get_template("base.sh.j2")
        elif script_type == "ps1":
            template = self.ps1_env.get_template("base.ps1.j2")
        else:
            raise ValueError(f"Unknown script type: {script_type}")

        # Render and write
        content = template.render(**config)
        output_path.write_text(content)

        # Make executable if it's a shell script
        if script_type == "sh":
            output_path.chmod(output_path.stat().st_mode | 0o111)

    def xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_193(
        self,
        project_name: str,
        output_path: Path,
        script_type: str = "sh",
        **kwargs: Any,
    ) -> None:
        """Generate an environment setup script.

        Args:
            project_name: Name of the project
            output_path: Path to write the script
            script_type: Either "sh" for bash or "ps1" for PowerShell
            **kwargs: Additional template variables
        """
        # Default configuration
        config = {
            "project_name": project_name,
            "env_profile_var": f"{project_name.upper().replace('-', '_')}_PROFILE",
            "venv_prefix": project_name.lower(),
            "use_spinner": script_type == "sh",  # PowerShell doesn't need spinner
            "strict_project_check": False,
            "install_siblings": True,
            "sibling_patterns": [],  # No default peers - specified in config
            "special_siblings": [],  # No default peers - specified in config
            "create_log_dir": True,
            "deduplicate_path": True,
            "include_tool_verification": True,  # Enable for wrknv
            "cleanup_logs": True,
            "tools_to_verify": [
                {
                    "name": "Python",
                    "command": "python",
                    "check_type": "command",
                    "var_name": "PYTHON",
                    "version_cmd": "python --version 2>&1",
                },
                {
                    "name": "UV",
                    "command": "uv",
                    "check_type": "command",
                    "var_name": "UV",
                    "version_cmd": "uv --version 2>&1",
                },
                {
                    "name": "wrknv",
                    "command": "wrknv",
                    "check_type": "command",
                    "var_name": "WRKENV",
                    "version_cmd": "wrknv --version 2>&1 || echo 'No version info'",
                },
                {
                    "name": "ibmtf",
                    "command": "ibmtf",
                    "check_type": "command",
                    "var_name": "IBMTF",
                    "version_cmd": "ibmtf version 2>&1 | head -1 || echo 'Not installed'",
                },
                {
                    "name": "tofu",
                    "command": "tofu",
                    "check_type": "command",
                    "var_name": "TOFU",
                    "version_cmd": "tofu version 2>&1 | head -1 || echo 'Not installed'",
                },
            ],
            "useful_commands": [
                {
                    "command": f"{project_name.lower()} --help",
                    "description": f"{project_name} CLI",
                },
                {"command": "wrknv status", "description": "Check tool versions"},
                {
                    "command": "wrknv container status",
                    "description": "Container status",
                },
                {"command": "pytest", "description": "Run tests"},
                {"command": "deactivate", "description": "EXIT ENVIRONMENT"},
            ],
        }

        # Update with user-provided kwargs
        config.update(kwargs)

        # Select appropriate template and environment
        if script_type == "sh":
            template = self.sh_env.get_template("base.sh.j2")
        elif script_type == "ps1":
            template = self.ps1_env.get_template("base.ps1.j2")
        else:
            raise ValueError(f"Unknown script type: {script_type}")

        # Render and write
        content = template.render(**config)
        output_path.write_text(content)

        # Make executable if it's a shell script
        if script_type == "sh":
            output_path.chmod(output_path.stat().st_mode | 0o111)

    def xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_194(
        self,
        project_name: str,
        output_path: Path,
        script_type: str = "sh",
        **kwargs: Any,
    ) -> None:
        """Generate an environment setup script.

        Args:
            project_name: Name of the project
            output_path: Path to write the script
            script_type: Either "sh" for bash or "ps1" for PowerShell
            **kwargs: Additional template variables
        """
        # Default configuration
        config = {
            "project_name": project_name,
            "env_profile_var": f"{project_name.upper().replace('-', '_')}_PROFILE",
            "venv_prefix": project_name.lower(),
            "use_spinner": script_type == "sh",  # PowerShell doesn't need spinner
            "strict_project_check": False,
            "install_siblings": True,
            "sibling_patterns": [],  # No default peers - specified in config
            "special_siblings": [],  # No default peers - specified in config
            "create_log_dir": True,
            "deduplicate_path": True,
            "include_tool_verification": True,  # Enable for wrknv
            "cleanup_logs": True,
            "tools_to_verify": [
                {
                    "name": "Python",
                    "command": "python",
                    "check_type": "command",
                    "var_name": "PYTHON",
                    "version_cmd": "python --version 2>&1",
                },
                {
                    "name": "UV",
                    "command": "uv",
                    "check_type": "command",
                    "var_name": "UV",
                    "version_cmd": "uv --version 2>&1",
                },
                {
                    "name": "wrknv",
                    "command": "wrknv",
                    "check_type": "command",
                    "var_name": "WRKENV",
                    "version_cmd": "wrknv --version 2>&1 || echo 'No version info'",
                },
                {
                    "name": "ibmtf",
                    "command": "ibmtf",
                    "check_type": "command",
                    "var_name": "IBMTF",
                    "version_cmd": "ibmtf version 2>&1 | head -1 || echo 'Not installed'",
                },
                {
                    "name": "tofu",
                    "command": "tofu",
                    "check_type": "command",
                    "var_name": "TOFU",
                    "version_cmd": "tofu version 2>&1 | head -1 || echo 'Not installed'",
                },
            ],
            "useful_commands": [
                {
                    "command": f"{project_name.lower()} --help",
                    "description": f"{project_name} CLI",
                },
                {"command": "wrknv status", "description": "Check tool versions"},
                {
                    "command": "wrknv container status",
                    "description": "Container status",
                },
                {"command": "pytest", "description": "Run tests"},
                {"command": "deactivate", "description": "Exit environment"},
            ],
        }

        # Update with user-provided kwargs
        config.update(None)

        # Select appropriate template and environment
        if script_type == "sh":
            template = self.sh_env.get_template("base.sh.j2")
        elif script_type == "ps1":
            template = self.ps1_env.get_template("base.ps1.j2")
        else:
            raise ValueError(f"Unknown script type: {script_type}")

        # Render and write
        content = template.render(**config)
        output_path.write_text(content)

        # Make executable if it's a shell script
        if script_type == "sh":
            output_path.chmod(output_path.stat().st_mode | 0o111)

    def xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_195(
        self,
        project_name: str,
        output_path: Path,
        script_type: str = "sh",
        **kwargs: Any,
    ) -> None:
        """Generate an environment setup script.

        Args:
            project_name: Name of the project
            output_path: Path to write the script
            script_type: Either "sh" for bash or "ps1" for PowerShell
            **kwargs: Additional template variables
        """
        # Default configuration
        config = {
            "project_name": project_name,
            "env_profile_var": f"{project_name.upper().replace('-', '_')}_PROFILE",
            "venv_prefix": project_name.lower(),
            "use_spinner": script_type == "sh",  # PowerShell doesn't need spinner
            "strict_project_check": False,
            "install_siblings": True,
            "sibling_patterns": [],  # No default peers - specified in config
            "special_siblings": [],  # No default peers - specified in config
            "create_log_dir": True,
            "deduplicate_path": True,
            "include_tool_verification": True,  # Enable for wrknv
            "cleanup_logs": True,
            "tools_to_verify": [
                {
                    "name": "Python",
                    "command": "python",
                    "check_type": "command",
                    "var_name": "PYTHON",
                    "version_cmd": "python --version 2>&1",
                },
                {
                    "name": "UV",
                    "command": "uv",
                    "check_type": "command",
                    "var_name": "UV",
                    "version_cmd": "uv --version 2>&1",
                },
                {
                    "name": "wrknv",
                    "command": "wrknv",
                    "check_type": "command",
                    "var_name": "WRKENV",
                    "version_cmd": "wrknv --version 2>&1 || echo 'No version info'",
                },
                {
                    "name": "ibmtf",
                    "command": "ibmtf",
                    "check_type": "command",
                    "var_name": "IBMTF",
                    "version_cmd": "ibmtf version 2>&1 | head -1 || echo 'Not installed'",
                },
                {
                    "name": "tofu",
                    "command": "tofu",
                    "check_type": "command",
                    "var_name": "TOFU",
                    "version_cmd": "tofu version 2>&1 | head -1 || echo 'Not installed'",
                },
            ],
            "useful_commands": [
                {
                    "command": f"{project_name.lower()} --help",
                    "description": f"{project_name} CLI",
                },
                {"command": "wrknv status", "description": "Check tool versions"},
                {
                    "command": "wrknv container status",
                    "description": "Container status",
                },
                {"command": "pytest", "description": "Run tests"},
                {"command": "deactivate", "description": "Exit environment"},
            ],
        }

        # Update with user-provided kwargs
        config.update(kwargs)

        # Select appropriate template and environment
        if script_type != "sh":
            template = self.sh_env.get_template("base.sh.j2")
        elif script_type == "ps1":
            template = self.ps1_env.get_template("base.ps1.j2")
        else:
            raise ValueError(f"Unknown script type: {script_type}")

        # Render and write
        content = template.render(**config)
        output_path.write_text(content)

        # Make executable if it's a shell script
        if script_type == "sh":
            output_path.chmod(output_path.stat().st_mode | 0o111)

    def xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_196(
        self,
        project_name: str,
        output_path: Path,
        script_type: str = "sh",
        **kwargs: Any,
    ) -> None:
        """Generate an environment setup script.

        Args:
            project_name: Name of the project
            output_path: Path to write the script
            script_type: Either "sh" for bash or "ps1" for PowerShell
            **kwargs: Additional template variables
        """
        # Default configuration
        config = {
            "project_name": project_name,
            "env_profile_var": f"{project_name.upper().replace('-', '_')}_PROFILE",
            "venv_prefix": project_name.lower(),
            "use_spinner": script_type == "sh",  # PowerShell doesn't need spinner
            "strict_project_check": False,
            "install_siblings": True,
            "sibling_patterns": [],  # No default peers - specified in config
            "special_siblings": [],  # No default peers - specified in config
            "create_log_dir": True,
            "deduplicate_path": True,
            "include_tool_verification": True,  # Enable for wrknv
            "cleanup_logs": True,
            "tools_to_verify": [
                {
                    "name": "Python",
                    "command": "python",
                    "check_type": "command",
                    "var_name": "PYTHON",
                    "version_cmd": "python --version 2>&1",
                },
                {
                    "name": "UV",
                    "command": "uv",
                    "check_type": "command",
                    "var_name": "UV",
                    "version_cmd": "uv --version 2>&1",
                },
                {
                    "name": "wrknv",
                    "command": "wrknv",
                    "check_type": "command",
                    "var_name": "WRKENV",
                    "version_cmd": "wrknv --version 2>&1 || echo 'No version info'",
                },
                {
                    "name": "ibmtf",
                    "command": "ibmtf",
                    "check_type": "command",
                    "var_name": "IBMTF",
                    "version_cmd": "ibmtf version 2>&1 | head -1 || echo 'Not installed'",
                },
                {
                    "name": "tofu",
                    "command": "tofu",
                    "check_type": "command",
                    "var_name": "TOFU",
                    "version_cmd": "tofu version 2>&1 | head -1 || echo 'Not installed'",
                },
            ],
            "useful_commands": [
                {
                    "command": f"{project_name.lower()} --help",
                    "description": f"{project_name} CLI",
                },
                {"command": "wrknv status", "description": "Check tool versions"},
                {
                    "command": "wrknv container status",
                    "description": "Container status",
                },
                {"command": "pytest", "description": "Run tests"},
                {"command": "deactivate", "description": "Exit environment"},
            ],
        }

        # Update with user-provided kwargs
        config.update(kwargs)

        # Select appropriate template and environment
        if script_type == "XXshXX":
            template = self.sh_env.get_template("base.sh.j2")
        elif script_type == "ps1":
            template = self.ps1_env.get_template("base.ps1.j2")
        else:
            raise ValueError(f"Unknown script type: {script_type}")

        # Render and write
        content = template.render(**config)
        output_path.write_text(content)

        # Make executable if it's a shell script
        if script_type == "sh":
            output_path.chmod(output_path.stat().st_mode | 0o111)

    def xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_197(
        self,
        project_name: str,
        output_path: Path,
        script_type: str = "sh",
        **kwargs: Any,
    ) -> None:
        """Generate an environment setup script.

        Args:
            project_name: Name of the project
            output_path: Path to write the script
            script_type: Either "sh" for bash or "ps1" for PowerShell
            **kwargs: Additional template variables
        """
        # Default configuration
        config = {
            "project_name": project_name,
            "env_profile_var": f"{project_name.upper().replace('-', '_')}_PROFILE",
            "venv_prefix": project_name.lower(),
            "use_spinner": script_type == "sh",  # PowerShell doesn't need spinner
            "strict_project_check": False,
            "install_siblings": True,
            "sibling_patterns": [],  # No default peers - specified in config
            "special_siblings": [],  # No default peers - specified in config
            "create_log_dir": True,
            "deduplicate_path": True,
            "include_tool_verification": True,  # Enable for wrknv
            "cleanup_logs": True,
            "tools_to_verify": [
                {
                    "name": "Python",
                    "command": "python",
                    "check_type": "command",
                    "var_name": "PYTHON",
                    "version_cmd": "python --version 2>&1",
                },
                {
                    "name": "UV",
                    "command": "uv",
                    "check_type": "command",
                    "var_name": "UV",
                    "version_cmd": "uv --version 2>&1",
                },
                {
                    "name": "wrknv",
                    "command": "wrknv",
                    "check_type": "command",
                    "var_name": "WRKENV",
                    "version_cmd": "wrknv --version 2>&1 || echo 'No version info'",
                },
                {
                    "name": "ibmtf",
                    "command": "ibmtf",
                    "check_type": "command",
                    "var_name": "IBMTF",
                    "version_cmd": "ibmtf version 2>&1 | head -1 || echo 'Not installed'",
                },
                {
                    "name": "tofu",
                    "command": "tofu",
                    "check_type": "command",
                    "var_name": "TOFU",
                    "version_cmd": "tofu version 2>&1 | head -1 || echo 'Not installed'",
                },
            ],
            "useful_commands": [
                {
                    "command": f"{project_name.lower()} --help",
                    "description": f"{project_name} CLI",
                },
                {"command": "wrknv status", "description": "Check tool versions"},
                {
                    "command": "wrknv container status",
                    "description": "Container status",
                },
                {"command": "pytest", "description": "Run tests"},
                {"command": "deactivate", "description": "Exit environment"},
            ],
        }

        # Update with user-provided kwargs
        config.update(kwargs)

        # Select appropriate template and environment
        if script_type == "SH":
            template = self.sh_env.get_template("base.sh.j2")
        elif script_type == "ps1":
            template = self.ps1_env.get_template("base.ps1.j2")
        else:
            raise ValueError(f"Unknown script type: {script_type}")

        # Render and write
        content = template.render(**config)
        output_path.write_text(content)

        # Make executable if it's a shell script
        if script_type == "sh":
            output_path.chmod(output_path.stat().st_mode | 0o111)

    def xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_198(
        self,
        project_name: str,
        output_path: Path,
        script_type: str = "sh",
        **kwargs: Any,
    ) -> None:
        """Generate an environment setup script.

        Args:
            project_name: Name of the project
            output_path: Path to write the script
            script_type: Either "sh" for bash or "ps1" for PowerShell
            **kwargs: Additional template variables
        """
        # Default configuration
        config = {
            "project_name": project_name,
            "env_profile_var": f"{project_name.upper().replace('-', '_')}_PROFILE",
            "venv_prefix": project_name.lower(),
            "use_spinner": script_type == "sh",  # PowerShell doesn't need spinner
            "strict_project_check": False,
            "install_siblings": True,
            "sibling_patterns": [],  # No default peers - specified in config
            "special_siblings": [],  # No default peers - specified in config
            "create_log_dir": True,
            "deduplicate_path": True,
            "include_tool_verification": True,  # Enable for wrknv
            "cleanup_logs": True,
            "tools_to_verify": [
                {
                    "name": "Python",
                    "command": "python",
                    "check_type": "command",
                    "var_name": "PYTHON",
                    "version_cmd": "python --version 2>&1",
                },
                {
                    "name": "UV",
                    "command": "uv",
                    "check_type": "command",
                    "var_name": "UV",
                    "version_cmd": "uv --version 2>&1",
                },
                {
                    "name": "wrknv",
                    "command": "wrknv",
                    "check_type": "command",
                    "var_name": "WRKENV",
                    "version_cmd": "wrknv --version 2>&1 || echo 'No version info'",
                },
                {
                    "name": "ibmtf",
                    "command": "ibmtf",
                    "check_type": "command",
                    "var_name": "IBMTF",
                    "version_cmd": "ibmtf version 2>&1 | head -1 || echo 'Not installed'",
                },
                {
                    "name": "tofu",
                    "command": "tofu",
                    "check_type": "command",
                    "var_name": "TOFU",
                    "version_cmd": "tofu version 2>&1 | head -1 || echo 'Not installed'",
                },
            ],
            "useful_commands": [
                {
                    "command": f"{project_name.lower()} --help",
                    "description": f"{project_name} CLI",
                },
                {"command": "wrknv status", "description": "Check tool versions"},
                {
                    "command": "wrknv container status",
                    "description": "Container status",
                },
                {"command": "pytest", "description": "Run tests"},
                {"command": "deactivate", "description": "Exit environment"},
            ],
        }

        # Update with user-provided kwargs
        config.update(kwargs)

        # Select appropriate template and environment
        if script_type == "sh":
            template = None
        elif script_type == "ps1":
            template = self.ps1_env.get_template("base.ps1.j2")
        else:
            raise ValueError(f"Unknown script type: {script_type}")

        # Render and write
        content = template.render(**config)
        output_path.write_text(content)

        # Make executable if it's a shell script
        if script_type == "sh":
            output_path.chmod(output_path.stat().st_mode | 0o111)

    def xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_199(
        self,
        project_name: str,
        output_path: Path,
        script_type: str = "sh",
        **kwargs: Any,
    ) -> None:
        """Generate an environment setup script.

        Args:
            project_name: Name of the project
            output_path: Path to write the script
            script_type: Either "sh" for bash or "ps1" for PowerShell
            **kwargs: Additional template variables
        """
        # Default configuration
        config = {
            "project_name": project_name,
            "env_profile_var": f"{project_name.upper().replace('-', '_')}_PROFILE",
            "venv_prefix": project_name.lower(),
            "use_spinner": script_type == "sh",  # PowerShell doesn't need spinner
            "strict_project_check": False,
            "install_siblings": True,
            "sibling_patterns": [],  # No default peers - specified in config
            "special_siblings": [],  # No default peers - specified in config
            "create_log_dir": True,
            "deduplicate_path": True,
            "include_tool_verification": True,  # Enable for wrknv
            "cleanup_logs": True,
            "tools_to_verify": [
                {
                    "name": "Python",
                    "command": "python",
                    "check_type": "command",
                    "var_name": "PYTHON",
                    "version_cmd": "python --version 2>&1",
                },
                {
                    "name": "UV",
                    "command": "uv",
                    "check_type": "command",
                    "var_name": "UV",
                    "version_cmd": "uv --version 2>&1",
                },
                {
                    "name": "wrknv",
                    "command": "wrknv",
                    "check_type": "command",
                    "var_name": "WRKENV",
                    "version_cmd": "wrknv --version 2>&1 || echo 'No version info'",
                },
                {
                    "name": "ibmtf",
                    "command": "ibmtf",
                    "check_type": "command",
                    "var_name": "IBMTF",
                    "version_cmd": "ibmtf version 2>&1 | head -1 || echo 'Not installed'",
                },
                {
                    "name": "tofu",
                    "command": "tofu",
                    "check_type": "command",
                    "var_name": "TOFU",
                    "version_cmd": "tofu version 2>&1 | head -1 || echo 'Not installed'",
                },
            ],
            "useful_commands": [
                {
                    "command": f"{project_name.lower()} --help",
                    "description": f"{project_name} CLI",
                },
                {"command": "wrknv status", "description": "Check tool versions"},
                {
                    "command": "wrknv container status",
                    "description": "Container status",
                },
                {"command": "pytest", "description": "Run tests"},
                {"command": "deactivate", "description": "Exit environment"},
            ],
        }

        # Update with user-provided kwargs
        config.update(kwargs)

        # Select appropriate template and environment
        if script_type == "sh":
            template = self.sh_env.get_template(None)
        elif script_type == "ps1":
            template = self.ps1_env.get_template("base.ps1.j2")
        else:
            raise ValueError(f"Unknown script type: {script_type}")

        # Render and write
        content = template.render(**config)
        output_path.write_text(content)

        # Make executable if it's a shell script
        if script_type == "sh":
            output_path.chmod(output_path.stat().st_mode | 0o111)

    def xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_200(
        self,
        project_name: str,
        output_path: Path,
        script_type: str = "sh",
        **kwargs: Any,
    ) -> None:
        """Generate an environment setup script.

        Args:
            project_name: Name of the project
            output_path: Path to write the script
            script_type: Either "sh" for bash or "ps1" for PowerShell
            **kwargs: Additional template variables
        """
        # Default configuration
        config = {
            "project_name": project_name,
            "env_profile_var": f"{project_name.upper().replace('-', '_')}_PROFILE",
            "venv_prefix": project_name.lower(),
            "use_spinner": script_type == "sh",  # PowerShell doesn't need spinner
            "strict_project_check": False,
            "install_siblings": True,
            "sibling_patterns": [],  # No default peers - specified in config
            "special_siblings": [],  # No default peers - specified in config
            "create_log_dir": True,
            "deduplicate_path": True,
            "include_tool_verification": True,  # Enable for wrknv
            "cleanup_logs": True,
            "tools_to_verify": [
                {
                    "name": "Python",
                    "command": "python",
                    "check_type": "command",
                    "var_name": "PYTHON",
                    "version_cmd": "python --version 2>&1",
                },
                {
                    "name": "UV",
                    "command": "uv",
                    "check_type": "command",
                    "var_name": "UV",
                    "version_cmd": "uv --version 2>&1",
                },
                {
                    "name": "wrknv",
                    "command": "wrknv",
                    "check_type": "command",
                    "var_name": "WRKENV",
                    "version_cmd": "wrknv --version 2>&1 || echo 'No version info'",
                },
                {
                    "name": "ibmtf",
                    "command": "ibmtf",
                    "check_type": "command",
                    "var_name": "IBMTF",
                    "version_cmd": "ibmtf version 2>&1 | head -1 || echo 'Not installed'",
                },
                {
                    "name": "tofu",
                    "command": "tofu",
                    "check_type": "command",
                    "var_name": "TOFU",
                    "version_cmd": "tofu version 2>&1 | head -1 || echo 'Not installed'",
                },
            ],
            "useful_commands": [
                {
                    "command": f"{project_name.lower()} --help",
                    "description": f"{project_name} CLI",
                },
                {"command": "wrknv status", "description": "Check tool versions"},
                {
                    "command": "wrknv container status",
                    "description": "Container status",
                },
                {"command": "pytest", "description": "Run tests"},
                {"command": "deactivate", "description": "Exit environment"},
            ],
        }

        # Update with user-provided kwargs
        config.update(kwargs)

        # Select appropriate template and environment
        if script_type == "sh":
            template = self.sh_env.get_template("XXbase.sh.j2XX")
        elif script_type == "ps1":
            template = self.ps1_env.get_template("base.ps1.j2")
        else:
            raise ValueError(f"Unknown script type: {script_type}")

        # Render and write
        content = template.render(**config)
        output_path.write_text(content)

        # Make executable if it's a shell script
        if script_type == "sh":
            output_path.chmod(output_path.stat().st_mode | 0o111)

    def xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_201(
        self,
        project_name: str,
        output_path: Path,
        script_type: str = "sh",
        **kwargs: Any,
    ) -> None:
        """Generate an environment setup script.

        Args:
            project_name: Name of the project
            output_path: Path to write the script
            script_type: Either "sh" for bash or "ps1" for PowerShell
            **kwargs: Additional template variables
        """
        # Default configuration
        config = {
            "project_name": project_name,
            "env_profile_var": f"{project_name.upper().replace('-', '_')}_PROFILE",
            "venv_prefix": project_name.lower(),
            "use_spinner": script_type == "sh",  # PowerShell doesn't need spinner
            "strict_project_check": False,
            "install_siblings": True,
            "sibling_patterns": [],  # No default peers - specified in config
            "special_siblings": [],  # No default peers - specified in config
            "create_log_dir": True,
            "deduplicate_path": True,
            "include_tool_verification": True,  # Enable for wrknv
            "cleanup_logs": True,
            "tools_to_verify": [
                {
                    "name": "Python",
                    "command": "python",
                    "check_type": "command",
                    "var_name": "PYTHON",
                    "version_cmd": "python --version 2>&1",
                },
                {
                    "name": "UV",
                    "command": "uv",
                    "check_type": "command",
                    "var_name": "UV",
                    "version_cmd": "uv --version 2>&1",
                },
                {
                    "name": "wrknv",
                    "command": "wrknv",
                    "check_type": "command",
                    "var_name": "WRKENV",
                    "version_cmd": "wrknv --version 2>&1 || echo 'No version info'",
                },
                {
                    "name": "ibmtf",
                    "command": "ibmtf",
                    "check_type": "command",
                    "var_name": "IBMTF",
                    "version_cmd": "ibmtf version 2>&1 | head -1 || echo 'Not installed'",
                },
                {
                    "name": "tofu",
                    "command": "tofu",
                    "check_type": "command",
                    "var_name": "TOFU",
                    "version_cmd": "tofu version 2>&1 | head -1 || echo 'Not installed'",
                },
            ],
            "useful_commands": [
                {
                    "command": f"{project_name.lower()} --help",
                    "description": f"{project_name} CLI",
                },
                {"command": "wrknv status", "description": "Check tool versions"},
                {
                    "command": "wrknv container status",
                    "description": "Container status",
                },
                {"command": "pytest", "description": "Run tests"},
                {"command": "deactivate", "description": "Exit environment"},
            ],
        }

        # Update with user-provided kwargs
        config.update(kwargs)

        # Select appropriate template and environment
        if script_type == "sh":
            template = self.sh_env.get_template("BASE.SH.J2")
        elif script_type == "ps1":
            template = self.ps1_env.get_template("base.ps1.j2")
        else:
            raise ValueError(f"Unknown script type: {script_type}")

        # Render and write
        content = template.render(**config)
        output_path.write_text(content)

        # Make executable if it's a shell script
        if script_type == "sh":
            output_path.chmod(output_path.stat().st_mode | 0o111)

    def xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_202(
        self,
        project_name: str,
        output_path: Path,
        script_type: str = "sh",
        **kwargs: Any,
    ) -> None:
        """Generate an environment setup script.

        Args:
            project_name: Name of the project
            output_path: Path to write the script
            script_type: Either "sh" for bash or "ps1" for PowerShell
            **kwargs: Additional template variables
        """
        # Default configuration
        config = {
            "project_name": project_name,
            "env_profile_var": f"{project_name.upper().replace('-', '_')}_PROFILE",
            "venv_prefix": project_name.lower(),
            "use_spinner": script_type == "sh",  # PowerShell doesn't need spinner
            "strict_project_check": False,
            "install_siblings": True,
            "sibling_patterns": [],  # No default peers - specified in config
            "special_siblings": [],  # No default peers - specified in config
            "create_log_dir": True,
            "deduplicate_path": True,
            "include_tool_verification": True,  # Enable for wrknv
            "cleanup_logs": True,
            "tools_to_verify": [
                {
                    "name": "Python",
                    "command": "python",
                    "check_type": "command",
                    "var_name": "PYTHON",
                    "version_cmd": "python --version 2>&1",
                },
                {
                    "name": "UV",
                    "command": "uv",
                    "check_type": "command",
                    "var_name": "UV",
                    "version_cmd": "uv --version 2>&1",
                },
                {
                    "name": "wrknv",
                    "command": "wrknv",
                    "check_type": "command",
                    "var_name": "WRKENV",
                    "version_cmd": "wrknv --version 2>&1 || echo 'No version info'",
                },
                {
                    "name": "ibmtf",
                    "command": "ibmtf",
                    "check_type": "command",
                    "var_name": "IBMTF",
                    "version_cmd": "ibmtf version 2>&1 | head -1 || echo 'Not installed'",
                },
                {
                    "name": "tofu",
                    "command": "tofu",
                    "check_type": "command",
                    "var_name": "TOFU",
                    "version_cmd": "tofu version 2>&1 | head -1 || echo 'Not installed'",
                },
            ],
            "useful_commands": [
                {
                    "command": f"{project_name.lower()} --help",
                    "description": f"{project_name} CLI",
                },
                {"command": "wrknv status", "description": "Check tool versions"},
                {
                    "command": "wrknv container status",
                    "description": "Container status",
                },
                {"command": "pytest", "description": "Run tests"},
                {"command": "deactivate", "description": "Exit environment"},
            ],
        }

        # Update with user-provided kwargs
        config.update(kwargs)

        # Select appropriate template and environment
        if script_type == "sh":
            template = self.sh_env.get_template("base.sh.j2")
        elif script_type != "ps1":
            template = self.ps1_env.get_template("base.ps1.j2")
        else:
            raise ValueError(f"Unknown script type: {script_type}")

        # Render and write
        content = template.render(**config)
        output_path.write_text(content)

        # Make executable if it's a shell script
        if script_type == "sh":
            output_path.chmod(output_path.stat().st_mode | 0o111)

    def xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_203(
        self,
        project_name: str,
        output_path: Path,
        script_type: str = "sh",
        **kwargs: Any,
    ) -> None:
        """Generate an environment setup script.

        Args:
            project_name: Name of the project
            output_path: Path to write the script
            script_type: Either "sh" for bash or "ps1" for PowerShell
            **kwargs: Additional template variables
        """
        # Default configuration
        config = {
            "project_name": project_name,
            "env_profile_var": f"{project_name.upper().replace('-', '_')}_PROFILE",
            "venv_prefix": project_name.lower(),
            "use_spinner": script_type == "sh",  # PowerShell doesn't need spinner
            "strict_project_check": False,
            "install_siblings": True,
            "sibling_patterns": [],  # No default peers - specified in config
            "special_siblings": [],  # No default peers - specified in config
            "create_log_dir": True,
            "deduplicate_path": True,
            "include_tool_verification": True,  # Enable for wrknv
            "cleanup_logs": True,
            "tools_to_verify": [
                {
                    "name": "Python",
                    "command": "python",
                    "check_type": "command",
                    "var_name": "PYTHON",
                    "version_cmd": "python --version 2>&1",
                },
                {
                    "name": "UV",
                    "command": "uv",
                    "check_type": "command",
                    "var_name": "UV",
                    "version_cmd": "uv --version 2>&1",
                },
                {
                    "name": "wrknv",
                    "command": "wrknv",
                    "check_type": "command",
                    "var_name": "WRKENV",
                    "version_cmd": "wrknv --version 2>&1 || echo 'No version info'",
                },
                {
                    "name": "ibmtf",
                    "command": "ibmtf",
                    "check_type": "command",
                    "var_name": "IBMTF",
                    "version_cmd": "ibmtf version 2>&1 | head -1 || echo 'Not installed'",
                },
                {
                    "name": "tofu",
                    "command": "tofu",
                    "check_type": "command",
                    "var_name": "TOFU",
                    "version_cmd": "tofu version 2>&1 | head -1 || echo 'Not installed'",
                },
            ],
            "useful_commands": [
                {
                    "command": f"{project_name.lower()} --help",
                    "description": f"{project_name} CLI",
                },
                {"command": "wrknv status", "description": "Check tool versions"},
                {
                    "command": "wrknv container status",
                    "description": "Container status",
                },
                {"command": "pytest", "description": "Run tests"},
                {"command": "deactivate", "description": "Exit environment"},
            ],
        }

        # Update with user-provided kwargs
        config.update(kwargs)

        # Select appropriate template and environment
        if script_type == "sh":
            template = self.sh_env.get_template("base.sh.j2")
        elif script_type == "XXps1XX":
            template = self.ps1_env.get_template("base.ps1.j2")
        else:
            raise ValueError(f"Unknown script type: {script_type}")

        # Render and write
        content = template.render(**config)
        output_path.write_text(content)

        # Make executable if it's a shell script
        if script_type == "sh":
            output_path.chmod(output_path.stat().st_mode | 0o111)

    def xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_204(
        self,
        project_name: str,
        output_path: Path,
        script_type: str = "sh",
        **kwargs: Any,
    ) -> None:
        """Generate an environment setup script.

        Args:
            project_name: Name of the project
            output_path: Path to write the script
            script_type: Either "sh" for bash or "ps1" for PowerShell
            **kwargs: Additional template variables
        """
        # Default configuration
        config = {
            "project_name": project_name,
            "env_profile_var": f"{project_name.upper().replace('-', '_')}_PROFILE",
            "venv_prefix": project_name.lower(),
            "use_spinner": script_type == "sh",  # PowerShell doesn't need spinner
            "strict_project_check": False,
            "install_siblings": True,
            "sibling_patterns": [],  # No default peers - specified in config
            "special_siblings": [],  # No default peers - specified in config
            "create_log_dir": True,
            "deduplicate_path": True,
            "include_tool_verification": True,  # Enable for wrknv
            "cleanup_logs": True,
            "tools_to_verify": [
                {
                    "name": "Python",
                    "command": "python",
                    "check_type": "command",
                    "var_name": "PYTHON",
                    "version_cmd": "python --version 2>&1",
                },
                {
                    "name": "UV",
                    "command": "uv",
                    "check_type": "command",
                    "var_name": "UV",
                    "version_cmd": "uv --version 2>&1",
                },
                {
                    "name": "wrknv",
                    "command": "wrknv",
                    "check_type": "command",
                    "var_name": "WRKENV",
                    "version_cmd": "wrknv --version 2>&1 || echo 'No version info'",
                },
                {
                    "name": "ibmtf",
                    "command": "ibmtf",
                    "check_type": "command",
                    "var_name": "IBMTF",
                    "version_cmd": "ibmtf version 2>&1 | head -1 || echo 'Not installed'",
                },
                {
                    "name": "tofu",
                    "command": "tofu",
                    "check_type": "command",
                    "var_name": "TOFU",
                    "version_cmd": "tofu version 2>&1 | head -1 || echo 'Not installed'",
                },
            ],
            "useful_commands": [
                {
                    "command": f"{project_name.lower()} --help",
                    "description": f"{project_name} CLI",
                },
                {"command": "wrknv status", "description": "Check tool versions"},
                {
                    "command": "wrknv container status",
                    "description": "Container status",
                },
                {"command": "pytest", "description": "Run tests"},
                {"command": "deactivate", "description": "Exit environment"},
            ],
        }

        # Update with user-provided kwargs
        config.update(kwargs)

        # Select appropriate template and environment
        if script_type == "sh":
            template = self.sh_env.get_template("base.sh.j2")
        elif script_type == "PS1":
            template = self.ps1_env.get_template("base.ps1.j2")
        else:
            raise ValueError(f"Unknown script type: {script_type}")

        # Render and write
        content = template.render(**config)
        output_path.write_text(content)

        # Make executable if it's a shell script
        if script_type == "sh":
            output_path.chmod(output_path.stat().st_mode | 0o111)

    def xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_205(
        self,
        project_name: str,
        output_path: Path,
        script_type: str = "sh",
        **kwargs: Any,
    ) -> None:
        """Generate an environment setup script.

        Args:
            project_name: Name of the project
            output_path: Path to write the script
            script_type: Either "sh" for bash or "ps1" for PowerShell
            **kwargs: Additional template variables
        """
        # Default configuration
        config = {
            "project_name": project_name,
            "env_profile_var": f"{project_name.upper().replace('-', '_')}_PROFILE",
            "venv_prefix": project_name.lower(),
            "use_spinner": script_type == "sh",  # PowerShell doesn't need spinner
            "strict_project_check": False,
            "install_siblings": True,
            "sibling_patterns": [],  # No default peers - specified in config
            "special_siblings": [],  # No default peers - specified in config
            "create_log_dir": True,
            "deduplicate_path": True,
            "include_tool_verification": True,  # Enable for wrknv
            "cleanup_logs": True,
            "tools_to_verify": [
                {
                    "name": "Python",
                    "command": "python",
                    "check_type": "command",
                    "var_name": "PYTHON",
                    "version_cmd": "python --version 2>&1",
                },
                {
                    "name": "UV",
                    "command": "uv",
                    "check_type": "command",
                    "var_name": "UV",
                    "version_cmd": "uv --version 2>&1",
                },
                {
                    "name": "wrknv",
                    "command": "wrknv",
                    "check_type": "command",
                    "var_name": "WRKENV",
                    "version_cmd": "wrknv --version 2>&1 || echo 'No version info'",
                },
                {
                    "name": "ibmtf",
                    "command": "ibmtf",
                    "check_type": "command",
                    "var_name": "IBMTF",
                    "version_cmd": "ibmtf version 2>&1 | head -1 || echo 'Not installed'",
                },
                {
                    "name": "tofu",
                    "command": "tofu",
                    "check_type": "command",
                    "var_name": "TOFU",
                    "version_cmd": "tofu version 2>&1 | head -1 || echo 'Not installed'",
                },
            ],
            "useful_commands": [
                {
                    "command": f"{project_name.lower()} --help",
                    "description": f"{project_name} CLI",
                },
                {"command": "wrknv status", "description": "Check tool versions"},
                {
                    "command": "wrknv container status",
                    "description": "Container status",
                },
                {"command": "pytest", "description": "Run tests"},
                {"command": "deactivate", "description": "Exit environment"},
            ],
        }

        # Update with user-provided kwargs
        config.update(kwargs)

        # Select appropriate template and environment
        if script_type == "sh":
            template = self.sh_env.get_template("base.sh.j2")
        elif script_type == "ps1":
            template = None
        else:
            raise ValueError(f"Unknown script type: {script_type}")

        # Render and write
        content = template.render(**config)
        output_path.write_text(content)

        # Make executable if it's a shell script
        if script_type == "sh":
            output_path.chmod(output_path.stat().st_mode | 0o111)

    def xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_206(
        self,
        project_name: str,
        output_path: Path,
        script_type: str = "sh",
        **kwargs: Any,
    ) -> None:
        """Generate an environment setup script.

        Args:
            project_name: Name of the project
            output_path: Path to write the script
            script_type: Either "sh" for bash or "ps1" for PowerShell
            **kwargs: Additional template variables
        """
        # Default configuration
        config = {
            "project_name": project_name,
            "env_profile_var": f"{project_name.upper().replace('-', '_')}_PROFILE",
            "venv_prefix": project_name.lower(),
            "use_spinner": script_type == "sh",  # PowerShell doesn't need spinner
            "strict_project_check": False,
            "install_siblings": True,
            "sibling_patterns": [],  # No default peers - specified in config
            "special_siblings": [],  # No default peers - specified in config
            "create_log_dir": True,
            "deduplicate_path": True,
            "include_tool_verification": True,  # Enable for wrknv
            "cleanup_logs": True,
            "tools_to_verify": [
                {
                    "name": "Python",
                    "command": "python",
                    "check_type": "command",
                    "var_name": "PYTHON",
                    "version_cmd": "python --version 2>&1",
                },
                {
                    "name": "UV",
                    "command": "uv",
                    "check_type": "command",
                    "var_name": "UV",
                    "version_cmd": "uv --version 2>&1",
                },
                {
                    "name": "wrknv",
                    "command": "wrknv",
                    "check_type": "command",
                    "var_name": "WRKENV",
                    "version_cmd": "wrknv --version 2>&1 || echo 'No version info'",
                },
                {
                    "name": "ibmtf",
                    "command": "ibmtf",
                    "check_type": "command",
                    "var_name": "IBMTF",
                    "version_cmd": "ibmtf version 2>&1 | head -1 || echo 'Not installed'",
                },
                {
                    "name": "tofu",
                    "command": "tofu",
                    "check_type": "command",
                    "var_name": "TOFU",
                    "version_cmd": "tofu version 2>&1 | head -1 || echo 'Not installed'",
                },
            ],
            "useful_commands": [
                {
                    "command": f"{project_name.lower()} --help",
                    "description": f"{project_name} CLI",
                },
                {"command": "wrknv status", "description": "Check tool versions"},
                {
                    "command": "wrknv container status",
                    "description": "Container status",
                },
                {"command": "pytest", "description": "Run tests"},
                {"command": "deactivate", "description": "Exit environment"},
            ],
        }

        # Update with user-provided kwargs
        config.update(kwargs)

        # Select appropriate template and environment
        if script_type == "sh":
            template = self.sh_env.get_template("base.sh.j2")
        elif script_type == "ps1":
            template = self.ps1_env.get_template(None)
        else:
            raise ValueError(f"Unknown script type: {script_type}")

        # Render and write
        content = template.render(**config)
        output_path.write_text(content)

        # Make executable if it's a shell script
        if script_type == "sh":
            output_path.chmod(output_path.stat().st_mode | 0o111)

    def xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_207(
        self,
        project_name: str,
        output_path: Path,
        script_type: str = "sh",
        **kwargs: Any,
    ) -> None:
        """Generate an environment setup script.

        Args:
            project_name: Name of the project
            output_path: Path to write the script
            script_type: Either "sh" for bash or "ps1" for PowerShell
            **kwargs: Additional template variables
        """
        # Default configuration
        config = {
            "project_name": project_name,
            "env_profile_var": f"{project_name.upper().replace('-', '_')}_PROFILE",
            "venv_prefix": project_name.lower(),
            "use_spinner": script_type == "sh",  # PowerShell doesn't need spinner
            "strict_project_check": False,
            "install_siblings": True,
            "sibling_patterns": [],  # No default peers - specified in config
            "special_siblings": [],  # No default peers - specified in config
            "create_log_dir": True,
            "deduplicate_path": True,
            "include_tool_verification": True,  # Enable for wrknv
            "cleanup_logs": True,
            "tools_to_verify": [
                {
                    "name": "Python",
                    "command": "python",
                    "check_type": "command",
                    "var_name": "PYTHON",
                    "version_cmd": "python --version 2>&1",
                },
                {
                    "name": "UV",
                    "command": "uv",
                    "check_type": "command",
                    "var_name": "UV",
                    "version_cmd": "uv --version 2>&1",
                },
                {
                    "name": "wrknv",
                    "command": "wrknv",
                    "check_type": "command",
                    "var_name": "WRKENV",
                    "version_cmd": "wrknv --version 2>&1 || echo 'No version info'",
                },
                {
                    "name": "ibmtf",
                    "command": "ibmtf",
                    "check_type": "command",
                    "var_name": "IBMTF",
                    "version_cmd": "ibmtf version 2>&1 | head -1 || echo 'Not installed'",
                },
                {
                    "name": "tofu",
                    "command": "tofu",
                    "check_type": "command",
                    "var_name": "TOFU",
                    "version_cmd": "tofu version 2>&1 | head -1 || echo 'Not installed'",
                },
            ],
            "useful_commands": [
                {
                    "command": f"{project_name.lower()} --help",
                    "description": f"{project_name} CLI",
                },
                {"command": "wrknv status", "description": "Check tool versions"},
                {
                    "command": "wrknv container status",
                    "description": "Container status",
                },
                {"command": "pytest", "description": "Run tests"},
                {"command": "deactivate", "description": "Exit environment"},
            ],
        }

        # Update with user-provided kwargs
        config.update(kwargs)

        # Select appropriate template and environment
        if script_type == "sh":
            template = self.sh_env.get_template("base.sh.j2")
        elif script_type == "ps1":
            template = self.ps1_env.get_template("XXbase.ps1.j2XX")
        else:
            raise ValueError(f"Unknown script type: {script_type}")

        # Render and write
        content = template.render(**config)
        output_path.write_text(content)

        # Make executable if it's a shell script
        if script_type == "sh":
            output_path.chmod(output_path.stat().st_mode | 0o111)

    def xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_208(
        self,
        project_name: str,
        output_path: Path,
        script_type: str = "sh",
        **kwargs: Any,
    ) -> None:
        """Generate an environment setup script.

        Args:
            project_name: Name of the project
            output_path: Path to write the script
            script_type: Either "sh" for bash or "ps1" for PowerShell
            **kwargs: Additional template variables
        """
        # Default configuration
        config = {
            "project_name": project_name,
            "env_profile_var": f"{project_name.upper().replace('-', '_')}_PROFILE",
            "venv_prefix": project_name.lower(),
            "use_spinner": script_type == "sh",  # PowerShell doesn't need spinner
            "strict_project_check": False,
            "install_siblings": True,
            "sibling_patterns": [],  # No default peers - specified in config
            "special_siblings": [],  # No default peers - specified in config
            "create_log_dir": True,
            "deduplicate_path": True,
            "include_tool_verification": True,  # Enable for wrknv
            "cleanup_logs": True,
            "tools_to_verify": [
                {
                    "name": "Python",
                    "command": "python",
                    "check_type": "command",
                    "var_name": "PYTHON",
                    "version_cmd": "python --version 2>&1",
                },
                {
                    "name": "UV",
                    "command": "uv",
                    "check_type": "command",
                    "var_name": "UV",
                    "version_cmd": "uv --version 2>&1",
                },
                {
                    "name": "wrknv",
                    "command": "wrknv",
                    "check_type": "command",
                    "var_name": "WRKENV",
                    "version_cmd": "wrknv --version 2>&1 || echo 'No version info'",
                },
                {
                    "name": "ibmtf",
                    "command": "ibmtf",
                    "check_type": "command",
                    "var_name": "IBMTF",
                    "version_cmd": "ibmtf version 2>&1 | head -1 || echo 'Not installed'",
                },
                {
                    "name": "tofu",
                    "command": "tofu",
                    "check_type": "command",
                    "var_name": "TOFU",
                    "version_cmd": "tofu version 2>&1 | head -1 || echo 'Not installed'",
                },
            ],
            "useful_commands": [
                {
                    "command": f"{project_name.lower()} --help",
                    "description": f"{project_name} CLI",
                },
                {"command": "wrknv status", "description": "Check tool versions"},
                {
                    "command": "wrknv container status",
                    "description": "Container status",
                },
                {"command": "pytest", "description": "Run tests"},
                {"command": "deactivate", "description": "Exit environment"},
            ],
        }

        # Update with user-provided kwargs
        config.update(kwargs)

        # Select appropriate template and environment
        if script_type == "sh":
            template = self.sh_env.get_template("base.sh.j2")
        elif script_type == "ps1":
            template = self.ps1_env.get_template("BASE.PS1.J2")
        else:
            raise ValueError(f"Unknown script type: {script_type}")

        # Render and write
        content = template.render(**config)
        output_path.write_text(content)

        # Make executable if it's a shell script
        if script_type == "sh":
            output_path.chmod(output_path.stat().st_mode | 0o111)

    def xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_209(
        self,
        project_name: str,
        output_path: Path,
        script_type: str = "sh",
        **kwargs: Any,
    ) -> None:
        """Generate an environment setup script.

        Args:
            project_name: Name of the project
            output_path: Path to write the script
            script_type: Either "sh" for bash or "ps1" for PowerShell
            **kwargs: Additional template variables
        """
        # Default configuration
        config = {
            "project_name": project_name,
            "env_profile_var": f"{project_name.upper().replace('-', '_')}_PROFILE",
            "venv_prefix": project_name.lower(),
            "use_spinner": script_type == "sh",  # PowerShell doesn't need spinner
            "strict_project_check": False,
            "install_siblings": True,
            "sibling_patterns": [],  # No default peers - specified in config
            "special_siblings": [],  # No default peers - specified in config
            "create_log_dir": True,
            "deduplicate_path": True,
            "include_tool_verification": True,  # Enable for wrknv
            "cleanup_logs": True,
            "tools_to_verify": [
                {
                    "name": "Python",
                    "command": "python",
                    "check_type": "command",
                    "var_name": "PYTHON",
                    "version_cmd": "python --version 2>&1",
                },
                {
                    "name": "UV",
                    "command": "uv",
                    "check_type": "command",
                    "var_name": "UV",
                    "version_cmd": "uv --version 2>&1",
                },
                {
                    "name": "wrknv",
                    "command": "wrknv",
                    "check_type": "command",
                    "var_name": "WRKENV",
                    "version_cmd": "wrknv --version 2>&1 || echo 'No version info'",
                },
                {
                    "name": "ibmtf",
                    "command": "ibmtf",
                    "check_type": "command",
                    "var_name": "IBMTF",
                    "version_cmd": "ibmtf version 2>&1 | head -1 || echo 'Not installed'",
                },
                {
                    "name": "tofu",
                    "command": "tofu",
                    "check_type": "command",
                    "var_name": "TOFU",
                    "version_cmd": "tofu version 2>&1 | head -1 || echo 'Not installed'",
                },
            ],
            "useful_commands": [
                {
                    "command": f"{project_name.lower()} --help",
                    "description": f"{project_name} CLI",
                },
                {"command": "wrknv status", "description": "Check tool versions"},
                {
                    "command": "wrknv container status",
                    "description": "Container status",
                },
                {"command": "pytest", "description": "Run tests"},
                {"command": "deactivate", "description": "Exit environment"},
            ],
        }

        # Update with user-provided kwargs
        config.update(kwargs)

        # Select appropriate template and environment
        if script_type == "sh":
            template = self.sh_env.get_template("base.sh.j2")
        elif script_type == "ps1":
            template = self.ps1_env.get_template("base.ps1.j2")
        else:
            raise ValueError(None)

        # Render and write
        content = template.render(**config)
        output_path.write_text(content)

        # Make executable if it's a shell script
        if script_type == "sh":
            output_path.chmod(output_path.stat().st_mode | 0o111)

    def xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_210(
        self,
        project_name: str,
        output_path: Path,
        script_type: str = "sh",
        **kwargs: Any,
    ) -> None:
        """Generate an environment setup script.

        Args:
            project_name: Name of the project
            output_path: Path to write the script
            script_type: Either "sh" for bash or "ps1" for PowerShell
            **kwargs: Additional template variables
        """
        # Default configuration
        config = {
            "project_name": project_name,
            "env_profile_var": f"{project_name.upper().replace('-', '_')}_PROFILE",
            "venv_prefix": project_name.lower(),
            "use_spinner": script_type == "sh",  # PowerShell doesn't need spinner
            "strict_project_check": False,
            "install_siblings": True,
            "sibling_patterns": [],  # No default peers - specified in config
            "special_siblings": [],  # No default peers - specified in config
            "create_log_dir": True,
            "deduplicate_path": True,
            "include_tool_verification": True,  # Enable for wrknv
            "cleanup_logs": True,
            "tools_to_verify": [
                {
                    "name": "Python",
                    "command": "python",
                    "check_type": "command",
                    "var_name": "PYTHON",
                    "version_cmd": "python --version 2>&1",
                },
                {
                    "name": "UV",
                    "command": "uv",
                    "check_type": "command",
                    "var_name": "UV",
                    "version_cmd": "uv --version 2>&1",
                },
                {
                    "name": "wrknv",
                    "command": "wrknv",
                    "check_type": "command",
                    "var_name": "WRKENV",
                    "version_cmd": "wrknv --version 2>&1 || echo 'No version info'",
                },
                {
                    "name": "ibmtf",
                    "command": "ibmtf",
                    "check_type": "command",
                    "var_name": "IBMTF",
                    "version_cmd": "ibmtf version 2>&1 | head -1 || echo 'Not installed'",
                },
                {
                    "name": "tofu",
                    "command": "tofu",
                    "check_type": "command",
                    "var_name": "TOFU",
                    "version_cmd": "tofu version 2>&1 | head -1 || echo 'Not installed'",
                },
            ],
            "useful_commands": [
                {
                    "command": f"{project_name.lower()} --help",
                    "description": f"{project_name} CLI",
                },
                {"command": "wrknv status", "description": "Check tool versions"},
                {
                    "command": "wrknv container status",
                    "description": "Container status",
                },
                {"command": "pytest", "description": "Run tests"},
                {"command": "deactivate", "description": "Exit environment"},
            ],
        }

        # Update with user-provided kwargs
        config.update(kwargs)

        # Select appropriate template and environment
        if script_type == "sh":
            template = self.sh_env.get_template("base.sh.j2")
        elif script_type == "ps1":
            template = self.ps1_env.get_template("base.ps1.j2")
        else:
            raise ValueError(f"Unknown script type: {script_type}")

        # Render and write
        content = None
        output_path.write_text(content)

        # Make executable if it's a shell script
        if script_type == "sh":
            output_path.chmod(output_path.stat().st_mode | 0o111)

    def xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_211(
        self,
        project_name: str,
        output_path: Path,
        script_type: str = "sh",
        **kwargs: Any,
    ) -> None:
        """Generate an environment setup script.

        Args:
            project_name: Name of the project
            output_path: Path to write the script
            script_type: Either "sh" for bash or "ps1" for PowerShell
            **kwargs: Additional template variables
        """
        # Default configuration
        config = {
            "project_name": project_name,
            "env_profile_var": f"{project_name.upper().replace('-', '_')}_PROFILE",
            "venv_prefix": project_name.lower(),
            "use_spinner": script_type == "sh",  # PowerShell doesn't need spinner
            "strict_project_check": False,
            "install_siblings": True,
            "sibling_patterns": [],  # No default peers - specified in config
            "special_siblings": [],  # No default peers - specified in config
            "create_log_dir": True,
            "deduplicate_path": True,
            "include_tool_verification": True,  # Enable for wrknv
            "cleanup_logs": True,
            "tools_to_verify": [
                {
                    "name": "Python",
                    "command": "python",
                    "check_type": "command",
                    "var_name": "PYTHON",
                    "version_cmd": "python --version 2>&1",
                },
                {
                    "name": "UV",
                    "command": "uv",
                    "check_type": "command",
                    "var_name": "UV",
                    "version_cmd": "uv --version 2>&1",
                },
                {
                    "name": "wrknv",
                    "command": "wrknv",
                    "check_type": "command",
                    "var_name": "WRKENV",
                    "version_cmd": "wrknv --version 2>&1 || echo 'No version info'",
                },
                {
                    "name": "ibmtf",
                    "command": "ibmtf",
                    "check_type": "command",
                    "var_name": "IBMTF",
                    "version_cmd": "ibmtf version 2>&1 | head -1 || echo 'Not installed'",
                },
                {
                    "name": "tofu",
                    "command": "tofu",
                    "check_type": "command",
                    "var_name": "TOFU",
                    "version_cmd": "tofu version 2>&1 | head -1 || echo 'Not installed'",
                },
            ],
            "useful_commands": [
                {
                    "command": f"{project_name.lower()} --help",
                    "description": f"{project_name} CLI",
                },
                {"command": "wrknv status", "description": "Check tool versions"},
                {
                    "command": "wrknv container status",
                    "description": "Container status",
                },
                {"command": "pytest", "description": "Run tests"},
                {"command": "deactivate", "description": "Exit environment"},
            ],
        }

        # Update with user-provided kwargs
        config.update(kwargs)

        # Select appropriate template and environment
        if script_type == "sh":
            template = self.sh_env.get_template("base.sh.j2")
        elif script_type == "ps1":
            template = self.ps1_env.get_template("base.ps1.j2")
        else:
            raise ValueError(f"Unknown script type: {script_type}")

        # Render and write
        content = template.render(**config)
        output_path.write_text(None)

        # Make executable if it's a shell script
        if script_type == "sh":
            output_path.chmod(output_path.stat().st_mode | 0o111)

    def xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_212(
        self,
        project_name: str,
        output_path: Path,
        script_type: str = "sh",
        **kwargs: Any,
    ) -> None:
        """Generate an environment setup script.

        Args:
            project_name: Name of the project
            output_path: Path to write the script
            script_type: Either "sh" for bash or "ps1" for PowerShell
            **kwargs: Additional template variables
        """
        # Default configuration
        config = {
            "project_name": project_name,
            "env_profile_var": f"{project_name.upper().replace('-', '_')}_PROFILE",
            "venv_prefix": project_name.lower(),
            "use_spinner": script_type == "sh",  # PowerShell doesn't need spinner
            "strict_project_check": False,
            "install_siblings": True,
            "sibling_patterns": [],  # No default peers - specified in config
            "special_siblings": [],  # No default peers - specified in config
            "create_log_dir": True,
            "deduplicate_path": True,
            "include_tool_verification": True,  # Enable for wrknv
            "cleanup_logs": True,
            "tools_to_verify": [
                {
                    "name": "Python",
                    "command": "python",
                    "check_type": "command",
                    "var_name": "PYTHON",
                    "version_cmd": "python --version 2>&1",
                },
                {
                    "name": "UV",
                    "command": "uv",
                    "check_type": "command",
                    "var_name": "UV",
                    "version_cmd": "uv --version 2>&1",
                },
                {
                    "name": "wrknv",
                    "command": "wrknv",
                    "check_type": "command",
                    "var_name": "WRKENV",
                    "version_cmd": "wrknv --version 2>&1 || echo 'No version info'",
                },
                {
                    "name": "ibmtf",
                    "command": "ibmtf",
                    "check_type": "command",
                    "var_name": "IBMTF",
                    "version_cmd": "ibmtf version 2>&1 | head -1 || echo 'Not installed'",
                },
                {
                    "name": "tofu",
                    "command": "tofu",
                    "check_type": "command",
                    "var_name": "TOFU",
                    "version_cmd": "tofu version 2>&1 | head -1 || echo 'Not installed'",
                },
            ],
            "useful_commands": [
                {
                    "command": f"{project_name.lower()} --help",
                    "description": f"{project_name} CLI",
                },
                {"command": "wrknv status", "description": "Check tool versions"},
                {
                    "command": "wrknv container status",
                    "description": "Container status",
                },
                {"command": "pytest", "description": "Run tests"},
                {"command": "deactivate", "description": "Exit environment"},
            ],
        }

        # Update with user-provided kwargs
        config.update(kwargs)

        # Select appropriate template and environment
        if script_type == "sh":
            template = self.sh_env.get_template("base.sh.j2")
        elif script_type == "ps1":
            template = self.ps1_env.get_template("base.ps1.j2")
        else:
            raise ValueError(f"Unknown script type: {script_type}")

        # Render and write
        content = template.render(**config)
        output_path.write_text(content)

        # Make executable if it's a shell script
        if script_type != "sh":
            output_path.chmod(output_path.stat().st_mode | 0o111)

    def xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_213(
        self,
        project_name: str,
        output_path: Path,
        script_type: str = "sh",
        **kwargs: Any,
    ) -> None:
        """Generate an environment setup script.

        Args:
            project_name: Name of the project
            output_path: Path to write the script
            script_type: Either "sh" for bash or "ps1" for PowerShell
            **kwargs: Additional template variables
        """
        # Default configuration
        config = {
            "project_name": project_name,
            "env_profile_var": f"{project_name.upper().replace('-', '_')}_PROFILE",
            "venv_prefix": project_name.lower(),
            "use_spinner": script_type == "sh",  # PowerShell doesn't need spinner
            "strict_project_check": False,
            "install_siblings": True,
            "sibling_patterns": [],  # No default peers - specified in config
            "special_siblings": [],  # No default peers - specified in config
            "create_log_dir": True,
            "deduplicate_path": True,
            "include_tool_verification": True,  # Enable for wrknv
            "cleanup_logs": True,
            "tools_to_verify": [
                {
                    "name": "Python",
                    "command": "python",
                    "check_type": "command",
                    "var_name": "PYTHON",
                    "version_cmd": "python --version 2>&1",
                },
                {
                    "name": "UV",
                    "command": "uv",
                    "check_type": "command",
                    "var_name": "UV",
                    "version_cmd": "uv --version 2>&1",
                },
                {
                    "name": "wrknv",
                    "command": "wrknv",
                    "check_type": "command",
                    "var_name": "WRKENV",
                    "version_cmd": "wrknv --version 2>&1 || echo 'No version info'",
                },
                {
                    "name": "ibmtf",
                    "command": "ibmtf",
                    "check_type": "command",
                    "var_name": "IBMTF",
                    "version_cmd": "ibmtf version 2>&1 | head -1 || echo 'Not installed'",
                },
                {
                    "name": "tofu",
                    "command": "tofu",
                    "check_type": "command",
                    "var_name": "TOFU",
                    "version_cmd": "tofu version 2>&1 | head -1 || echo 'Not installed'",
                },
            ],
            "useful_commands": [
                {
                    "command": f"{project_name.lower()} --help",
                    "description": f"{project_name} CLI",
                },
                {"command": "wrknv status", "description": "Check tool versions"},
                {
                    "command": "wrknv container status",
                    "description": "Container status",
                },
                {"command": "pytest", "description": "Run tests"},
                {"command": "deactivate", "description": "Exit environment"},
            ],
        }

        # Update with user-provided kwargs
        config.update(kwargs)

        # Select appropriate template and environment
        if script_type == "sh":
            template = self.sh_env.get_template("base.sh.j2")
        elif script_type == "ps1":
            template = self.ps1_env.get_template("base.ps1.j2")
        else:
            raise ValueError(f"Unknown script type: {script_type}")

        # Render and write
        content = template.render(**config)
        output_path.write_text(content)

        # Make executable if it's a shell script
        if script_type == "XXshXX":
            output_path.chmod(output_path.stat().st_mode | 0o111)

    def xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_214(
        self,
        project_name: str,
        output_path: Path,
        script_type: str = "sh",
        **kwargs: Any,
    ) -> None:
        """Generate an environment setup script.

        Args:
            project_name: Name of the project
            output_path: Path to write the script
            script_type: Either "sh" for bash or "ps1" for PowerShell
            **kwargs: Additional template variables
        """
        # Default configuration
        config = {
            "project_name": project_name,
            "env_profile_var": f"{project_name.upper().replace('-', '_')}_PROFILE",
            "venv_prefix": project_name.lower(),
            "use_spinner": script_type == "sh",  # PowerShell doesn't need spinner
            "strict_project_check": False,
            "install_siblings": True,
            "sibling_patterns": [],  # No default peers - specified in config
            "special_siblings": [],  # No default peers - specified in config
            "create_log_dir": True,
            "deduplicate_path": True,
            "include_tool_verification": True,  # Enable for wrknv
            "cleanup_logs": True,
            "tools_to_verify": [
                {
                    "name": "Python",
                    "command": "python",
                    "check_type": "command",
                    "var_name": "PYTHON",
                    "version_cmd": "python --version 2>&1",
                },
                {
                    "name": "UV",
                    "command": "uv",
                    "check_type": "command",
                    "var_name": "UV",
                    "version_cmd": "uv --version 2>&1",
                },
                {
                    "name": "wrknv",
                    "command": "wrknv",
                    "check_type": "command",
                    "var_name": "WRKENV",
                    "version_cmd": "wrknv --version 2>&1 || echo 'No version info'",
                },
                {
                    "name": "ibmtf",
                    "command": "ibmtf",
                    "check_type": "command",
                    "var_name": "IBMTF",
                    "version_cmd": "ibmtf version 2>&1 | head -1 || echo 'Not installed'",
                },
                {
                    "name": "tofu",
                    "command": "tofu",
                    "check_type": "command",
                    "var_name": "TOFU",
                    "version_cmd": "tofu version 2>&1 | head -1 || echo 'Not installed'",
                },
            ],
            "useful_commands": [
                {
                    "command": f"{project_name.lower()} --help",
                    "description": f"{project_name} CLI",
                },
                {"command": "wrknv status", "description": "Check tool versions"},
                {
                    "command": "wrknv container status",
                    "description": "Container status",
                },
                {"command": "pytest", "description": "Run tests"},
                {"command": "deactivate", "description": "Exit environment"},
            ],
        }

        # Update with user-provided kwargs
        config.update(kwargs)

        # Select appropriate template and environment
        if script_type == "sh":
            template = self.sh_env.get_template("base.sh.j2")
        elif script_type == "ps1":
            template = self.ps1_env.get_template("base.ps1.j2")
        else:
            raise ValueError(f"Unknown script type: {script_type}")

        # Render and write
        content = template.render(**config)
        output_path.write_text(content)

        # Make executable if it's a shell script
        if script_type == "SH":
            output_path.chmod(output_path.stat().st_mode | 0o111)

    def xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_215(
        self,
        project_name: str,
        output_path: Path,
        script_type: str = "sh",
        **kwargs: Any,
    ) -> None:
        """Generate an environment setup script.

        Args:
            project_name: Name of the project
            output_path: Path to write the script
            script_type: Either "sh" for bash or "ps1" for PowerShell
            **kwargs: Additional template variables
        """
        # Default configuration
        config = {
            "project_name": project_name,
            "env_profile_var": f"{project_name.upper().replace('-', '_')}_PROFILE",
            "venv_prefix": project_name.lower(),
            "use_spinner": script_type == "sh",  # PowerShell doesn't need spinner
            "strict_project_check": False,
            "install_siblings": True,
            "sibling_patterns": [],  # No default peers - specified in config
            "special_siblings": [],  # No default peers - specified in config
            "create_log_dir": True,
            "deduplicate_path": True,
            "include_tool_verification": True,  # Enable for wrknv
            "cleanup_logs": True,
            "tools_to_verify": [
                {
                    "name": "Python",
                    "command": "python",
                    "check_type": "command",
                    "var_name": "PYTHON",
                    "version_cmd": "python --version 2>&1",
                },
                {
                    "name": "UV",
                    "command": "uv",
                    "check_type": "command",
                    "var_name": "UV",
                    "version_cmd": "uv --version 2>&1",
                },
                {
                    "name": "wrknv",
                    "command": "wrknv",
                    "check_type": "command",
                    "var_name": "WRKENV",
                    "version_cmd": "wrknv --version 2>&1 || echo 'No version info'",
                },
                {
                    "name": "ibmtf",
                    "command": "ibmtf",
                    "check_type": "command",
                    "var_name": "IBMTF",
                    "version_cmd": "ibmtf version 2>&1 | head -1 || echo 'Not installed'",
                },
                {
                    "name": "tofu",
                    "command": "tofu",
                    "check_type": "command",
                    "var_name": "TOFU",
                    "version_cmd": "tofu version 2>&1 | head -1 || echo 'Not installed'",
                },
            ],
            "useful_commands": [
                {
                    "command": f"{project_name.lower()} --help",
                    "description": f"{project_name} CLI",
                },
                {"command": "wrknv status", "description": "Check tool versions"},
                {
                    "command": "wrknv container status",
                    "description": "Container status",
                },
                {"command": "pytest", "description": "Run tests"},
                {"command": "deactivate", "description": "Exit environment"},
            ],
        }

        # Update with user-provided kwargs
        config.update(kwargs)

        # Select appropriate template and environment
        if script_type == "sh":
            template = self.sh_env.get_template("base.sh.j2")
        elif script_type == "ps1":
            template = self.ps1_env.get_template("base.ps1.j2")
        else:
            raise ValueError(f"Unknown script type: {script_type}")

        # Render and write
        content = template.render(**config)
        output_path.write_text(content)

        # Make executable if it's a shell script
        if script_type == "sh":
            output_path.chmod(None)

    def xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_216(
        self,
        project_name: str,
        output_path: Path,
        script_type: str = "sh",
        **kwargs: Any,
    ) -> None:
        """Generate an environment setup script.

        Args:
            project_name: Name of the project
            output_path: Path to write the script
            script_type: Either "sh" for bash or "ps1" for PowerShell
            **kwargs: Additional template variables
        """
        # Default configuration
        config = {
            "project_name": project_name,
            "env_profile_var": f"{project_name.upper().replace('-', '_')}_PROFILE",
            "venv_prefix": project_name.lower(),
            "use_spinner": script_type == "sh",  # PowerShell doesn't need spinner
            "strict_project_check": False,
            "install_siblings": True,
            "sibling_patterns": [],  # No default peers - specified in config
            "special_siblings": [],  # No default peers - specified in config
            "create_log_dir": True,
            "deduplicate_path": True,
            "include_tool_verification": True,  # Enable for wrknv
            "cleanup_logs": True,
            "tools_to_verify": [
                {
                    "name": "Python",
                    "command": "python",
                    "check_type": "command",
                    "var_name": "PYTHON",
                    "version_cmd": "python --version 2>&1",
                },
                {
                    "name": "UV",
                    "command": "uv",
                    "check_type": "command",
                    "var_name": "UV",
                    "version_cmd": "uv --version 2>&1",
                },
                {
                    "name": "wrknv",
                    "command": "wrknv",
                    "check_type": "command",
                    "var_name": "WRKENV",
                    "version_cmd": "wrknv --version 2>&1 || echo 'No version info'",
                },
                {
                    "name": "ibmtf",
                    "command": "ibmtf",
                    "check_type": "command",
                    "var_name": "IBMTF",
                    "version_cmd": "ibmtf version 2>&1 | head -1 || echo 'Not installed'",
                },
                {
                    "name": "tofu",
                    "command": "tofu",
                    "check_type": "command",
                    "var_name": "TOFU",
                    "version_cmd": "tofu version 2>&1 | head -1 || echo 'Not installed'",
                },
            ],
            "useful_commands": [
                {
                    "command": f"{project_name.lower()} --help",
                    "description": f"{project_name} CLI",
                },
                {"command": "wrknv status", "description": "Check tool versions"},
                {
                    "command": "wrknv container status",
                    "description": "Container status",
                },
                {"command": "pytest", "description": "Run tests"},
                {"command": "deactivate", "description": "Exit environment"},
            ],
        }

        # Update with user-provided kwargs
        config.update(kwargs)

        # Select appropriate template and environment
        if script_type == "sh":
            template = self.sh_env.get_template("base.sh.j2")
        elif script_type == "ps1":
            template = self.ps1_env.get_template("base.ps1.j2")
        else:
            raise ValueError(f"Unknown script type: {script_type}")

        # Render and write
        content = template.render(**config)
        output_path.write_text(content)

        # Make executable if it's a shell script
        if script_type == "sh":
            output_path.chmod(output_path.stat().st_mode & 0o111)

    def xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_217(
        self,
        project_name: str,
        output_path: Path,
        script_type: str = "sh",
        **kwargs: Any,
    ) -> None:
        """Generate an environment setup script.

        Args:
            project_name: Name of the project
            output_path: Path to write the script
            script_type: Either "sh" for bash or "ps1" for PowerShell
            **kwargs: Additional template variables
        """
        # Default configuration
        config = {
            "project_name": project_name,
            "env_profile_var": f"{project_name.upper().replace('-', '_')}_PROFILE",
            "venv_prefix": project_name.lower(),
            "use_spinner": script_type == "sh",  # PowerShell doesn't need spinner
            "strict_project_check": False,
            "install_siblings": True,
            "sibling_patterns": [],  # No default peers - specified in config
            "special_siblings": [],  # No default peers - specified in config
            "create_log_dir": True,
            "deduplicate_path": True,
            "include_tool_verification": True,  # Enable for wrknv
            "cleanup_logs": True,
            "tools_to_verify": [
                {
                    "name": "Python",
                    "command": "python",
                    "check_type": "command",
                    "var_name": "PYTHON",
                    "version_cmd": "python --version 2>&1",
                },
                {
                    "name": "UV",
                    "command": "uv",
                    "check_type": "command",
                    "var_name": "UV",
                    "version_cmd": "uv --version 2>&1",
                },
                {
                    "name": "wrknv",
                    "command": "wrknv",
                    "check_type": "command",
                    "var_name": "WRKENV",
                    "version_cmd": "wrknv --version 2>&1 || echo 'No version info'",
                },
                {
                    "name": "ibmtf",
                    "command": "ibmtf",
                    "check_type": "command",
                    "var_name": "IBMTF",
                    "version_cmd": "ibmtf version 2>&1 | head -1 || echo 'Not installed'",
                },
                {
                    "name": "tofu",
                    "command": "tofu",
                    "check_type": "command",
                    "var_name": "TOFU",
                    "version_cmd": "tofu version 2>&1 | head -1 || echo 'Not installed'",
                },
            ],
            "useful_commands": [
                {
                    "command": f"{project_name.lower()} --help",
                    "description": f"{project_name} CLI",
                },
                {"command": "wrknv status", "description": "Check tool versions"},
                {
                    "command": "wrknv container status",
                    "description": "Container status",
                },
                {"command": "pytest", "description": "Run tests"},
                {"command": "deactivate", "description": "Exit environment"},
            ],
        }

        # Update with user-provided kwargs
        config.update(kwargs)

        # Select appropriate template and environment
        if script_type == "sh":
            template = self.sh_env.get_template("base.sh.j2")
        elif script_type == "ps1":
            template = self.ps1_env.get_template("base.ps1.j2")
        else:
            raise ValueError(f"Unknown script type: {script_type}")

        # Render and write
        content = template.render(**config)
        output_path.write_text(content)

        # Make executable if it's a shell script
        if script_type == "sh":
            output_path.chmod(output_path.stat().st_mode | 74)
    
    xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_mutants : ClassVar[MutantDict] = {
    'xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_1': xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_1, 
        'xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_2': xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_2, 
        'xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_3': xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_3, 
        'xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_4': xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_4, 
        'xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_5': xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_5, 
        'xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_6': xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_6, 
        'xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_7': xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_7, 
        'xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_8': xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_8, 
        'xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_9': xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_9, 
        'xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_10': xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_10, 
        'xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_11': xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_11, 
        'xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_12': xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_12, 
        'xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_13': xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_13, 
        'xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_14': xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_14, 
        'xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_15': xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_15, 
        'xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_16': xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_16, 
        'xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_17': xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_17, 
        'xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_18': xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_18, 
        'xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_19': xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_19, 
        'xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_20': xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_20, 
        'xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_21': xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_21, 
        'xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_22': xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_22, 
        'xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_23': xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_23, 
        'xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_24': xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_24, 
        'xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_25': xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_25, 
        'xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_26': xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_26, 
        'xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_27': xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_27, 
        'xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_28': xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_28, 
        'xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_29': xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_29, 
        'xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_30': xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_30, 
        'xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_31': xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_31, 
        'xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_32': xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_32, 
        'xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_33': xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_33, 
        'xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_34': xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_34, 
        'xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_35': xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_35, 
        'xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_36': xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_36, 
        'xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_37': xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_37, 
        'xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_38': xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_38, 
        'xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_39': xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_39, 
        'xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_40': xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_40, 
        'xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_41': xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_41, 
        'xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_42': xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_42, 
        'xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_43': xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_43, 
        'xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_44': xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_44, 
        'xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_45': xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_45, 
        'xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_46': xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_46, 
        'xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_47': xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_47, 
        'xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_48': xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_48, 
        'xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_49': xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_49, 
        'xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_50': xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_50, 
        'xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_51': xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_51, 
        'xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_52': xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_52, 
        'xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_53': xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_53, 
        'xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_54': xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_54, 
        'xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_55': xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_55, 
        'xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_56': xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_56, 
        'xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_57': xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_57, 
        'xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_58': xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_58, 
        'xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_59': xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_59, 
        'xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_60': xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_60, 
        'xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_61': xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_61, 
        'xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_62': xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_62, 
        'xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_63': xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_63, 
        'xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_64': xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_64, 
        'xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_65': xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_65, 
        'xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_66': xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_66, 
        'xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_67': xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_67, 
        'xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_68': xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_68, 
        'xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_69': xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_69, 
        'xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_70': xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_70, 
        'xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_71': xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_71, 
        'xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_72': xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_72, 
        'xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_73': xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_73, 
        'xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_74': xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_74, 
        'xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_75': xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_75, 
        'xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_76': xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_76, 
        'xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_77': xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_77, 
        'xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_78': xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_78, 
        'xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_79': xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_79, 
        'xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_80': xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_80, 
        'xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_81': xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_81, 
        'xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_82': xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_82, 
        'xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_83': xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_83, 
        'xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_84': xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_84, 
        'xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_85': xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_85, 
        'xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_86': xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_86, 
        'xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_87': xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_87, 
        'xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_88': xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_88, 
        'xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_89': xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_89, 
        'xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_90': xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_90, 
        'xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_91': xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_91, 
        'xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_92': xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_92, 
        'xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_93': xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_93, 
        'xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_94': xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_94, 
        'xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_95': xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_95, 
        'xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_96': xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_96, 
        'xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_97': xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_97, 
        'xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_98': xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_98, 
        'xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_99': xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_99, 
        'xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_100': xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_100, 
        'xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_101': xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_101, 
        'xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_102': xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_102, 
        'xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_103': xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_103, 
        'xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_104': xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_104, 
        'xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_105': xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_105, 
        'xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_106': xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_106, 
        'xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_107': xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_107, 
        'xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_108': xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_108, 
        'xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_109': xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_109, 
        'xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_110': xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_110, 
        'xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_111': xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_111, 
        'xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_112': xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_112, 
        'xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_113': xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_113, 
        'xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_114': xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_114, 
        'xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_115': xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_115, 
        'xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_116': xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_116, 
        'xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_117': xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_117, 
        'xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_118': xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_118, 
        'xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_119': xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_119, 
        'xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_120': xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_120, 
        'xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_121': xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_121, 
        'xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_122': xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_122, 
        'xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_123': xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_123, 
        'xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_124': xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_124, 
        'xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_125': xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_125, 
        'xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_126': xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_126, 
        'xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_127': xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_127, 
        'xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_128': xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_128, 
        'xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_129': xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_129, 
        'xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_130': xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_130, 
        'xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_131': xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_131, 
        'xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_132': xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_132, 
        'xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_133': xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_133, 
        'xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_134': xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_134, 
        'xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_135': xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_135, 
        'xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_136': xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_136, 
        'xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_137': xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_137, 
        'xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_138': xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_138, 
        'xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_139': xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_139, 
        'xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_140': xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_140, 
        'xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_141': xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_141, 
        'xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_142': xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_142, 
        'xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_143': xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_143, 
        'xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_144': xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_144, 
        'xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_145': xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_145, 
        'xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_146': xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_146, 
        'xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_147': xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_147, 
        'xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_148': xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_148, 
        'xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_149': xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_149, 
        'xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_150': xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_150, 
        'xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_151': xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_151, 
        'xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_152': xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_152, 
        'xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_153': xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_153, 
        'xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_154': xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_154, 
        'xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_155': xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_155, 
        'xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_156': xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_156, 
        'xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_157': xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_157, 
        'xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_158': xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_158, 
        'xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_159': xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_159, 
        'xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_160': xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_160, 
        'xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_161': xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_161, 
        'xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_162': xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_162, 
        'xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_163': xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_163, 
        'xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_164': xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_164, 
        'xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_165': xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_165, 
        'xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_166': xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_166, 
        'xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_167': xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_167, 
        'xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_168': xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_168, 
        'xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_169': xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_169, 
        'xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_170': xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_170, 
        'xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_171': xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_171, 
        'xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_172': xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_172, 
        'xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_173': xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_173, 
        'xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_174': xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_174, 
        'xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_175': xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_175, 
        'xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_176': xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_176, 
        'xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_177': xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_177, 
        'xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_178': xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_178, 
        'xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_179': xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_179, 
        'xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_180': xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_180, 
        'xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_181': xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_181, 
        'xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_182': xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_182, 
        'xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_183': xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_183, 
        'xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_184': xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_184, 
        'xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_185': xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_185, 
        'xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_186': xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_186, 
        'xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_187': xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_187, 
        'xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_188': xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_188, 
        'xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_189': xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_189, 
        'xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_190': xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_190, 
        'xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_191': xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_191, 
        'xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_192': xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_192, 
        'xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_193': xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_193, 
        'xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_194': xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_194, 
        'xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_195': xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_195, 
        'xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_196': xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_196, 
        'xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_197': xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_197, 
        'xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_198': xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_198, 
        'xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_199': xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_199, 
        'xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_200': xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_200, 
        'xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_201': xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_201, 
        'xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_202': xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_202, 
        'xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_203': xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_203, 
        'xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_204': xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_204, 
        'xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_205': xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_205, 
        'xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_206': xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_206, 
        'xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_207': xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_207, 
        'xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_208': xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_208, 
        'xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_209': xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_209, 
        'xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_210': xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_210, 
        'xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_211': xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_211, 
        'xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_212': xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_212, 
        'xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_213': xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_213, 
        'xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_214': xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_214, 
        'xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_215': xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_215, 
        'xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_216': xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_216, 
        'xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_217': xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_217
    }
    
    def generate_env_script(self, *args, **kwargs):
        result = _mutmut_trampoline(object.__getattribute__(self, "xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_orig"), object.__getattribute__(self, "xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_mutants"), args, kwargs, self)
        return result 
    
    generate_env_script.__signature__ = _mutmut_signature(xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_orig)
    xǁEnvScriptGeneratorǁgenerate_env_script__mutmut_orig.__name__ = 'xǁEnvScriptGeneratorǁgenerate_env_script'

    def xǁEnvScriptGeneratorǁgenerate_both_scripts__mutmut_orig(
        self,
        project_name: str,
        project_dir: Path,
        **kwargs: Any,
    ) -> tuple[Path, Path]:
        """Generate both env.sh and env.ps1 scripts.

        Returns:
            Tuple of (env.sh path, env.ps1 path)
        """
        sh_path = project_dir / "env.sh"
        ps1_path = project_dir / "env.ps1"

        self.generate_env_script(project_name, sh_path, "sh", **kwargs)
        self.generate_env_script(project_name, ps1_path, "ps1", **kwargs)

        return sh_path, ps1_path

    def xǁEnvScriptGeneratorǁgenerate_both_scripts__mutmut_1(
        self,
        project_name: str,
        project_dir: Path,
        **kwargs: Any,
    ) -> tuple[Path, Path]:
        """Generate both env.sh and env.ps1 scripts.

        Returns:
            Tuple of (env.sh path, env.ps1 path)
        """
        sh_path = None
        ps1_path = project_dir / "env.ps1"

        self.generate_env_script(project_name, sh_path, "sh", **kwargs)
        self.generate_env_script(project_name, ps1_path, "ps1", **kwargs)

        return sh_path, ps1_path

    def xǁEnvScriptGeneratorǁgenerate_both_scripts__mutmut_2(
        self,
        project_name: str,
        project_dir: Path,
        **kwargs: Any,
    ) -> tuple[Path, Path]:
        """Generate both env.sh and env.ps1 scripts.

        Returns:
            Tuple of (env.sh path, env.ps1 path)
        """
        sh_path = project_dir * "env.sh"
        ps1_path = project_dir / "env.ps1"

        self.generate_env_script(project_name, sh_path, "sh", **kwargs)
        self.generate_env_script(project_name, ps1_path, "ps1", **kwargs)

        return sh_path, ps1_path

    def xǁEnvScriptGeneratorǁgenerate_both_scripts__mutmut_3(
        self,
        project_name: str,
        project_dir: Path,
        **kwargs: Any,
    ) -> tuple[Path, Path]:
        """Generate both env.sh and env.ps1 scripts.

        Returns:
            Tuple of (env.sh path, env.ps1 path)
        """
        sh_path = project_dir / "XXenv.shXX"
        ps1_path = project_dir / "env.ps1"

        self.generate_env_script(project_name, sh_path, "sh", **kwargs)
        self.generate_env_script(project_name, ps1_path, "ps1", **kwargs)

        return sh_path, ps1_path

    def xǁEnvScriptGeneratorǁgenerate_both_scripts__mutmut_4(
        self,
        project_name: str,
        project_dir: Path,
        **kwargs: Any,
    ) -> tuple[Path, Path]:
        """Generate both env.sh and env.ps1 scripts.

        Returns:
            Tuple of (env.sh path, env.ps1 path)
        """
        sh_path = project_dir / "ENV.SH"
        ps1_path = project_dir / "env.ps1"

        self.generate_env_script(project_name, sh_path, "sh", **kwargs)
        self.generate_env_script(project_name, ps1_path, "ps1", **kwargs)

        return sh_path, ps1_path

    def xǁEnvScriptGeneratorǁgenerate_both_scripts__mutmut_5(
        self,
        project_name: str,
        project_dir: Path,
        **kwargs: Any,
    ) -> tuple[Path, Path]:
        """Generate both env.sh and env.ps1 scripts.

        Returns:
            Tuple of (env.sh path, env.ps1 path)
        """
        sh_path = project_dir / "env.sh"
        ps1_path = None

        self.generate_env_script(project_name, sh_path, "sh", **kwargs)
        self.generate_env_script(project_name, ps1_path, "ps1", **kwargs)

        return sh_path, ps1_path

    def xǁEnvScriptGeneratorǁgenerate_both_scripts__mutmut_6(
        self,
        project_name: str,
        project_dir: Path,
        **kwargs: Any,
    ) -> tuple[Path, Path]:
        """Generate both env.sh and env.ps1 scripts.

        Returns:
            Tuple of (env.sh path, env.ps1 path)
        """
        sh_path = project_dir / "env.sh"
        ps1_path = project_dir * "env.ps1"

        self.generate_env_script(project_name, sh_path, "sh", **kwargs)
        self.generate_env_script(project_name, ps1_path, "ps1", **kwargs)

        return sh_path, ps1_path

    def xǁEnvScriptGeneratorǁgenerate_both_scripts__mutmut_7(
        self,
        project_name: str,
        project_dir: Path,
        **kwargs: Any,
    ) -> tuple[Path, Path]:
        """Generate both env.sh and env.ps1 scripts.

        Returns:
            Tuple of (env.sh path, env.ps1 path)
        """
        sh_path = project_dir / "env.sh"
        ps1_path = project_dir / "XXenv.ps1XX"

        self.generate_env_script(project_name, sh_path, "sh", **kwargs)
        self.generate_env_script(project_name, ps1_path, "ps1", **kwargs)

        return sh_path, ps1_path

    def xǁEnvScriptGeneratorǁgenerate_both_scripts__mutmut_8(
        self,
        project_name: str,
        project_dir: Path,
        **kwargs: Any,
    ) -> tuple[Path, Path]:
        """Generate both env.sh and env.ps1 scripts.

        Returns:
            Tuple of (env.sh path, env.ps1 path)
        """
        sh_path = project_dir / "env.sh"
        ps1_path = project_dir / "ENV.PS1"

        self.generate_env_script(project_name, sh_path, "sh", **kwargs)
        self.generate_env_script(project_name, ps1_path, "ps1", **kwargs)

        return sh_path, ps1_path

    def xǁEnvScriptGeneratorǁgenerate_both_scripts__mutmut_9(
        self,
        project_name: str,
        project_dir: Path,
        **kwargs: Any,
    ) -> tuple[Path, Path]:
        """Generate both env.sh and env.ps1 scripts.

        Returns:
            Tuple of (env.sh path, env.ps1 path)
        """
        sh_path = project_dir / "env.sh"
        ps1_path = project_dir / "env.ps1"

        self.generate_env_script(None, sh_path, "sh", **kwargs)
        self.generate_env_script(project_name, ps1_path, "ps1", **kwargs)

        return sh_path, ps1_path

    def xǁEnvScriptGeneratorǁgenerate_both_scripts__mutmut_10(
        self,
        project_name: str,
        project_dir: Path,
        **kwargs: Any,
    ) -> tuple[Path, Path]:
        """Generate both env.sh and env.ps1 scripts.

        Returns:
            Tuple of (env.sh path, env.ps1 path)
        """
        sh_path = project_dir / "env.sh"
        ps1_path = project_dir / "env.ps1"

        self.generate_env_script(project_name, None, "sh", **kwargs)
        self.generate_env_script(project_name, ps1_path, "ps1", **kwargs)

        return sh_path, ps1_path

    def xǁEnvScriptGeneratorǁgenerate_both_scripts__mutmut_11(
        self,
        project_name: str,
        project_dir: Path,
        **kwargs: Any,
    ) -> tuple[Path, Path]:
        """Generate both env.sh and env.ps1 scripts.

        Returns:
            Tuple of (env.sh path, env.ps1 path)
        """
        sh_path = project_dir / "env.sh"
        ps1_path = project_dir / "env.ps1"

        self.generate_env_script(project_name, sh_path, None, **kwargs)
        self.generate_env_script(project_name, ps1_path, "ps1", **kwargs)

        return sh_path, ps1_path

    def xǁEnvScriptGeneratorǁgenerate_both_scripts__mutmut_12(
        self,
        project_name: str,
        project_dir: Path,
        **kwargs: Any,
    ) -> tuple[Path, Path]:
        """Generate both env.sh and env.ps1 scripts.

        Returns:
            Tuple of (env.sh path, env.ps1 path)
        """
        sh_path = project_dir / "env.sh"
        ps1_path = project_dir / "env.ps1"

        self.generate_env_script(sh_path, "sh", **kwargs)
        self.generate_env_script(project_name, ps1_path, "ps1", **kwargs)

        return sh_path, ps1_path

    def xǁEnvScriptGeneratorǁgenerate_both_scripts__mutmut_13(
        self,
        project_name: str,
        project_dir: Path,
        **kwargs: Any,
    ) -> tuple[Path, Path]:
        """Generate both env.sh and env.ps1 scripts.

        Returns:
            Tuple of (env.sh path, env.ps1 path)
        """
        sh_path = project_dir / "env.sh"
        ps1_path = project_dir / "env.ps1"

        self.generate_env_script(project_name, "sh", **kwargs)
        self.generate_env_script(project_name, ps1_path, "ps1", **kwargs)

        return sh_path, ps1_path

    def xǁEnvScriptGeneratorǁgenerate_both_scripts__mutmut_14(
        self,
        project_name: str,
        project_dir: Path,
        **kwargs: Any,
    ) -> tuple[Path, Path]:
        """Generate both env.sh and env.ps1 scripts.

        Returns:
            Tuple of (env.sh path, env.ps1 path)
        """
        sh_path = project_dir / "env.sh"
        ps1_path = project_dir / "env.ps1"

        self.generate_env_script(project_name, sh_path, **kwargs)
        self.generate_env_script(project_name, ps1_path, "ps1", **kwargs)

        return sh_path, ps1_path

    def xǁEnvScriptGeneratorǁgenerate_both_scripts__mutmut_15(
        self,
        project_name: str,
        project_dir: Path,
        **kwargs: Any,
    ) -> tuple[Path, Path]:
        """Generate both env.sh and env.ps1 scripts.

        Returns:
            Tuple of (env.sh path, env.ps1 path)
        """
        sh_path = project_dir / "env.sh"
        ps1_path = project_dir / "env.ps1"

        self.generate_env_script(project_name, sh_path, "sh", )
        self.generate_env_script(project_name, ps1_path, "ps1", **kwargs)

        return sh_path, ps1_path

    def xǁEnvScriptGeneratorǁgenerate_both_scripts__mutmut_16(
        self,
        project_name: str,
        project_dir: Path,
        **kwargs: Any,
    ) -> tuple[Path, Path]:
        """Generate both env.sh and env.ps1 scripts.

        Returns:
            Tuple of (env.sh path, env.ps1 path)
        """
        sh_path = project_dir / "env.sh"
        ps1_path = project_dir / "env.ps1"

        self.generate_env_script(project_name, sh_path, "XXshXX", **kwargs)
        self.generate_env_script(project_name, ps1_path, "ps1", **kwargs)

        return sh_path, ps1_path

    def xǁEnvScriptGeneratorǁgenerate_both_scripts__mutmut_17(
        self,
        project_name: str,
        project_dir: Path,
        **kwargs: Any,
    ) -> tuple[Path, Path]:
        """Generate both env.sh and env.ps1 scripts.

        Returns:
            Tuple of (env.sh path, env.ps1 path)
        """
        sh_path = project_dir / "env.sh"
        ps1_path = project_dir / "env.ps1"

        self.generate_env_script(project_name, sh_path, "SH", **kwargs)
        self.generate_env_script(project_name, ps1_path, "ps1", **kwargs)

        return sh_path, ps1_path

    def xǁEnvScriptGeneratorǁgenerate_both_scripts__mutmut_18(
        self,
        project_name: str,
        project_dir: Path,
        **kwargs: Any,
    ) -> tuple[Path, Path]:
        """Generate both env.sh and env.ps1 scripts.

        Returns:
            Tuple of (env.sh path, env.ps1 path)
        """
        sh_path = project_dir / "env.sh"
        ps1_path = project_dir / "env.ps1"

        self.generate_env_script(project_name, sh_path, "sh", **kwargs)
        self.generate_env_script(None, ps1_path, "ps1", **kwargs)

        return sh_path, ps1_path

    def xǁEnvScriptGeneratorǁgenerate_both_scripts__mutmut_19(
        self,
        project_name: str,
        project_dir: Path,
        **kwargs: Any,
    ) -> tuple[Path, Path]:
        """Generate both env.sh and env.ps1 scripts.

        Returns:
            Tuple of (env.sh path, env.ps1 path)
        """
        sh_path = project_dir / "env.sh"
        ps1_path = project_dir / "env.ps1"

        self.generate_env_script(project_name, sh_path, "sh", **kwargs)
        self.generate_env_script(project_name, None, "ps1", **kwargs)

        return sh_path, ps1_path

    def xǁEnvScriptGeneratorǁgenerate_both_scripts__mutmut_20(
        self,
        project_name: str,
        project_dir: Path,
        **kwargs: Any,
    ) -> tuple[Path, Path]:
        """Generate both env.sh and env.ps1 scripts.

        Returns:
            Tuple of (env.sh path, env.ps1 path)
        """
        sh_path = project_dir / "env.sh"
        ps1_path = project_dir / "env.ps1"

        self.generate_env_script(project_name, sh_path, "sh", **kwargs)
        self.generate_env_script(project_name, ps1_path, None, **kwargs)

        return sh_path, ps1_path

    def xǁEnvScriptGeneratorǁgenerate_both_scripts__mutmut_21(
        self,
        project_name: str,
        project_dir: Path,
        **kwargs: Any,
    ) -> tuple[Path, Path]:
        """Generate both env.sh and env.ps1 scripts.

        Returns:
            Tuple of (env.sh path, env.ps1 path)
        """
        sh_path = project_dir / "env.sh"
        ps1_path = project_dir / "env.ps1"

        self.generate_env_script(project_name, sh_path, "sh", **kwargs)
        self.generate_env_script(ps1_path, "ps1", **kwargs)

        return sh_path, ps1_path

    def xǁEnvScriptGeneratorǁgenerate_both_scripts__mutmut_22(
        self,
        project_name: str,
        project_dir: Path,
        **kwargs: Any,
    ) -> tuple[Path, Path]:
        """Generate both env.sh and env.ps1 scripts.

        Returns:
            Tuple of (env.sh path, env.ps1 path)
        """
        sh_path = project_dir / "env.sh"
        ps1_path = project_dir / "env.ps1"

        self.generate_env_script(project_name, sh_path, "sh", **kwargs)
        self.generate_env_script(project_name, "ps1", **kwargs)

        return sh_path, ps1_path

    def xǁEnvScriptGeneratorǁgenerate_both_scripts__mutmut_23(
        self,
        project_name: str,
        project_dir: Path,
        **kwargs: Any,
    ) -> tuple[Path, Path]:
        """Generate both env.sh and env.ps1 scripts.

        Returns:
            Tuple of (env.sh path, env.ps1 path)
        """
        sh_path = project_dir / "env.sh"
        ps1_path = project_dir / "env.ps1"

        self.generate_env_script(project_name, sh_path, "sh", **kwargs)
        self.generate_env_script(project_name, ps1_path, **kwargs)

        return sh_path, ps1_path

    def xǁEnvScriptGeneratorǁgenerate_both_scripts__mutmut_24(
        self,
        project_name: str,
        project_dir: Path,
        **kwargs: Any,
    ) -> tuple[Path, Path]:
        """Generate both env.sh and env.ps1 scripts.

        Returns:
            Tuple of (env.sh path, env.ps1 path)
        """
        sh_path = project_dir / "env.sh"
        ps1_path = project_dir / "env.ps1"

        self.generate_env_script(project_name, sh_path, "sh", **kwargs)
        self.generate_env_script(project_name, ps1_path, "ps1", )

        return sh_path, ps1_path

    def xǁEnvScriptGeneratorǁgenerate_both_scripts__mutmut_25(
        self,
        project_name: str,
        project_dir: Path,
        **kwargs: Any,
    ) -> tuple[Path, Path]:
        """Generate both env.sh and env.ps1 scripts.

        Returns:
            Tuple of (env.sh path, env.ps1 path)
        """
        sh_path = project_dir / "env.sh"
        ps1_path = project_dir / "env.ps1"

        self.generate_env_script(project_name, sh_path, "sh", **kwargs)
        self.generate_env_script(project_name, ps1_path, "XXps1XX", **kwargs)

        return sh_path, ps1_path

    def xǁEnvScriptGeneratorǁgenerate_both_scripts__mutmut_26(
        self,
        project_name: str,
        project_dir: Path,
        **kwargs: Any,
    ) -> tuple[Path, Path]:
        """Generate both env.sh and env.ps1 scripts.

        Returns:
            Tuple of (env.sh path, env.ps1 path)
        """
        sh_path = project_dir / "env.sh"
        ps1_path = project_dir / "env.ps1"

        self.generate_env_script(project_name, sh_path, "sh", **kwargs)
        self.generate_env_script(project_name, ps1_path, "PS1", **kwargs)

        return sh_path, ps1_path
    
    xǁEnvScriptGeneratorǁgenerate_both_scripts__mutmut_mutants : ClassVar[MutantDict] = {
    'xǁEnvScriptGeneratorǁgenerate_both_scripts__mutmut_1': xǁEnvScriptGeneratorǁgenerate_both_scripts__mutmut_1, 
        'xǁEnvScriptGeneratorǁgenerate_both_scripts__mutmut_2': xǁEnvScriptGeneratorǁgenerate_both_scripts__mutmut_2, 
        'xǁEnvScriptGeneratorǁgenerate_both_scripts__mutmut_3': xǁEnvScriptGeneratorǁgenerate_both_scripts__mutmut_3, 
        'xǁEnvScriptGeneratorǁgenerate_both_scripts__mutmut_4': xǁEnvScriptGeneratorǁgenerate_both_scripts__mutmut_4, 
        'xǁEnvScriptGeneratorǁgenerate_both_scripts__mutmut_5': xǁEnvScriptGeneratorǁgenerate_both_scripts__mutmut_5, 
        'xǁEnvScriptGeneratorǁgenerate_both_scripts__mutmut_6': xǁEnvScriptGeneratorǁgenerate_both_scripts__mutmut_6, 
        'xǁEnvScriptGeneratorǁgenerate_both_scripts__mutmut_7': xǁEnvScriptGeneratorǁgenerate_both_scripts__mutmut_7, 
        'xǁEnvScriptGeneratorǁgenerate_both_scripts__mutmut_8': xǁEnvScriptGeneratorǁgenerate_both_scripts__mutmut_8, 
        'xǁEnvScriptGeneratorǁgenerate_both_scripts__mutmut_9': xǁEnvScriptGeneratorǁgenerate_both_scripts__mutmut_9, 
        'xǁEnvScriptGeneratorǁgenerate_both_scripts__mutmut_10': xǁEnvScriptGeneratorǁgenerate_both_scripts__mutmut_10, 
        'xǁEnvScriptGeneratorǁgenerate_both_scripts__mutmut_11': xǁEnvScriptGeneratorǁgenerate_both_scripts__mutmut_11, 
        'xǁEnvScriptGeneratorǁgenerate_both_scripts__mutmut_12': xǁEnvScriptGeneratorǁgenerate_both_scripts__mutmut_12, 
        'xǁEnvScriptGeneratorǁgenerate_both_scripts__mutmut_13': xǁEnvScriptGeneratorǁgenerate_both_scripts__mutmut_13, 
        'xǁEnvScriptGeneratorǁgenerate_both_scripts__mutmut_14': xǁEnvScriptGeneratorǁgenerate_both_scripts__mutmut_14, 
        'xǁEnvScriptGeneratorǁgenerate_both_scripts__mutmut_15': xǁEnvScriptGeneratorǁgenerate_both_scripts__mutmut_15, 
        'xǁEnvScriptGeneratorǁgenerate_both_scripts__mutmut_16': xǁEnvScriptGeneratorǁgenerate_both_scripts__mutmut_16, 
        'xǁEnvScriptGeneratorǁgenerate_both_scripts__mutmut_17': xǁEnvScriptGeneratorǁgenerate_both_scripts__mutmut_17, 
        'xǁEnvScriptGeneratorǁgenerate_both_scripts__mutmut_18': xǁEnvScriptGeneratorǁgenerate_both_scripts__mutmut_18, 
        'xǁEnvScriptGeneratorǁgenerate_both_scripts__mutmut_19': xǁEnvScriptGeneratorǁgenerate_both_scripts__mutmut_19, 
        'xǁEnvScriptGeneratorǁgenerate_both_scripts__mutmut_20': xǁEnvScriptGeneratorǁgenerate_both_scripts__mutmut_20, 
        'xǁEnvScriptGeneratorǁgenerate_both_scripts__mutmut_21': xǁEnvScriptGeneratorǁgenerate_both_scripts__mutmut_21, 
        'xǁEnvScriptGeneratorǁgenerate_both_scripts__mutmut_22': xǁEnvScriptGeneratorǁgenerate_both_scripts__mutmut_22, 
        'xǁEnvScriptGeneratorǁgenerate_both_scripts__mutmut_23': xǁEnvScriptGeneratorǁgenerate_both_scripts__mutmut_23, 
        'xǁEnvScriptGeneratorǁgenerate_both_scripts__mutmut_24': xǁEnvScriptGeneratorǁgenerate_both_scripts__mutmut_24, 
        'xǁEnvScriptGeneratorǁgenerate_both_scripts__mutmut_25': xǁEnvScriptGeneratorǁgenerate_both_scripts__mutmut_25, 
        'xǁEnvScriptGeneratorǁgenerate_both_scripts__mutmut_26': xǁEnvScriptGeneratorǁgenerate_both_scripts__mutmut_26
    }
    
    def generate_both_scripts(self, *args, **kwargs):
        result = _mutmut_trampoline(object.__getattribute__(self, "xǁEnvScriptGeneratorǁgenerate_both_scripts__mutmut_orig"), object.__getattribute__(self, "xǁEnvScriptGeneratorǁgenerate_both_scripts__mutmut_mutants"), args, kwargs, self)
        return result 
    
    generate_both_scripts.__signature__ = _mutmut_signature(xǁEnvScriptGeneratorǁgenerate_both_scripts__mutmut_orig)
    xǁEnvScriptGeneratorǁgenerate_both_scripts__mutmut_orig.__name__ = 'xǁEnvScriptGeneratorǁgenerate_both_scripts'


def x_create_project_env_scripts__mutmut_orig(project_dir: Path, workenv_name: str | None = None) -> tuple[Path, Path]:
    """Create environment scripts for a project based on its pyproject.toml.

    Args:
        project_dir: The project directory
        workenv_name: Optional workenv name override

    Returns:
        Tuple of (env.sh path, env.ps1 path)
    """
    pyproject_path = project_dir / "pyproject.toml"

    if not pyproject_path.exists():
        raise FileNotFoundError(f"No pyproject.toml found in {project_dir}")

    # Parse project name from pyproject.toml
    from provide.foundation.file.formats import read_toml

    pyproject = read_toml(pyproject_path)

    project_name = pyproject.get("project", {}).get("name", project_dir.name)
    python_requirement = pyproject.get("project", {}).get("requires-python")

    # Additional config for wrknv projects
    extra_config = {}
    if workenv_name:
        extra_config["workenv_name"] = workenv_name
        extra_config["venv_prefix"] = workenv_name

    # Add Python requirement if present
    if python_requirement:
        extra_config["python_requirement"] = python_requirement

    # Get wrknv configuration
    from wrknv.config import WorkenvConfig

    config = WorkenvConfig.load()
    env_config = config.get_env_config()

    # Use configuration from wrknv.toml if available
    if env_config:
        logger.info("Using env configuration from wrknv.toml", env_config=env_config)
        extra_config["include_tool_verification"] = env_config.get("include_tool_verification", True)

        # Handle new unified siblings format
        siblings_config = env_config.get("siblings", [])
        if siblings_config:
            extra_config["siblings"] = siblings_config
            # Keep backward compatibility
            extra_config["sibling_patterns"] = []
            extra_config["special_siblings"] = []
        else:
            # Backward compatibility with old format
            extra_config["sibling_patterns"] = env_config.get("sibling_patterns", [])
            extra_config["special_siblings"] = env_config.get("special_siblings", [])
            extra_config["siblings"] = []
    else:
        # Default configuration - empty by default
        logger.info("No env configuration found in wrknv.toml, using defaults")
        extra_config["include_tool_verification"] = True
        extra_config["sibling_patterns"] = []
        extra_config["special_siblings"] = []
        extra_config["siblings"] = []

    # Generate scripts
    generator = EnvScriptGenerator()
    sh_path, ps1_path = generator.generate_both_scripts(project_name, project_dir, **extra_config)

    print_success(f"Generated {sh_path}", Emoji.SUCCESS)
    print_success(f"Generated {ps1_path}", Emoji.SUCCESS)

    return sh_path, ps1_path


def x_create_project_env_scripts__mutmut_1(project_dir: Path, workenv_name: str | None = None) -> tuple[Path, Path]:
    """Create environment scripts for a project based on its pyproject.toml.

    Args:
        project_dir: The project directory
        workenv_name: Optional workenv name override

    Returns:
        Tuple of (env.sh path, env.ps1 path)
    """
    pyproject_path = None

    if not pyproject_path.exists():
        raise FileNotFoundError(f"No pyproject.toml found in {project_dir}")

    # Parse project name from pyproject.toml
    from provide.foundation.file.formats import read_toml

    pyproject = read_toml(pyproject_path)

    project_name = pyproject.get("project", {}).get("name", project_dir.name)
    python_requirement = pyproject.get("project", {}).get("requires-python")

    # Additional config for wrknv projects
    extra_config = {}
    if workenv_name:
        extra_config["workenv_name"] = workenv_name
        extra_config["venv_prefix"] = workenv_name

    # Add Python requirement if present
    if python_requirement:
        extra_config["python_requirement"] = python_requirement

    # Get wrknv configuration
    from wrknv.config import WorkenvConfig

    config = WorkenvConfig.load()
    env_config = config.get_env_config()

    # Use configuration from wrknv.toml if available
    if env_config:
        logger.info("Using env configuration from wrknv.toml", env_config=env_config)
        extra_config["include_tool_verification"] = env_config.get("include_tool_verification", True)

        # Handle new unified siblings format
        siblings_config = env_config.get("siblings", [])
        if siblings_config:
            extra_config["siblings"] = siblings_config
            # Keep backward compatibility
            extra_config["sibling_patterns"] = []
            extra_config["special_siblings"] = []
        else:
            # Backward compatibility with old format
            extra_config["sibling_patterns"] = env_config.get("sibling_patterns", [])
            extra_config["special_siblings"] = env_config.get("special_siblings", [])
            extra_config["siblings"] = []
    else:
        # Default configuration - empty by default
        logger.info("No env configuration found in wrknv.toml, using defaults")
        extra_config["include_tool_verification"] = True
        extra_config["sibling_patterns"] = []
        extra_config["special_siblings"] = []
        extra_config["siblings"] = []

    # Generate scripts
    generator = EnvScriptGenerator()
    sh_path, ps1_path = generator.generate_both_scripts(project_name, project_dir, **extra_config)

    print_success(f"Generated {sh_path}", Emoji.SUCCESS)
    print_success(f"Generated {ps1_path}", Emoji.SUCCESS)

    return sh_path, ps1_path


def x_create_project_env_scripts__mutmut_2(project_dir: Path, workenv_name: str | None = None) -> tuple[Path, Path]:
    """Create environment scripts for a project based on its pyproject.toml.

    Args:
        project_dir: The project directory
        workenv_name: Optional workenv name override

    Returns:
        Tuple of (env.sh path, env.ps1 path)
    """
    pyproject_path = project_dir * "pyproject.toml"

    if not pyproject_path.exists():
        raise FileNotFoundError(f"No pyproject.toml found in {project_dir}")

    # Parse project name from pyproject.toml
    from provide.foundation.file.formats import read_toml

    pyproject = read_toml(pyproject_path)

    project_name = pyproject.get("project", {}).get("name", project_dir.name)
    python_requirement = pyproject.get("project", {}).get("requires-python")

    # Additional config for wrknv projects
    extra_config = {}
    if workenv_name:
        extra_config["workenv_name"] = workenv_name
        extra_config["venv_prefix"] = workenv_name

    # Add Python requirement if present
    if python_requirement:
        extra_config["python_requirement"] = python_requirement

    # Get wrknv configuration
    from wrknv.config import WorkenvConfig

    config = WorkenvConfig.load()
    env_config = config.get_env_config()

    # Use configuration from wrknv.toml if available
    if env_config:
        logger.info("Using env configuration from wrknv.toml", env_config=env_config)
        extra_config["include_tool_verification"] = env_config.get("include_tool_verification", True)

        # Handle new unified siblings format
        siblings_config = env_config.get("siblings", [])
        if siblings_config:
            extra_config["siblings"] = siblings_config
            # Keep backward compatibility
            extra_config["sibling_patterns"] = []
            extra_config["special_siblings"] = []
        else:
            # Backward compatibility with old format
            extra_config["sibling_patterns"] = env_config.get("sibling_patterns", [])
            extra_config["special_siblings"] = env_config.get("special_siblings", [])
            extra_config["siblings"] = []
    else:
        # Default configuration - empty by default
        logger.info("No env configuration found in wrknv.toml, using defaults")
        extra_config["include_tool_verification"] = True
        extra_config["sibling_patterns"] = []
        extra_config["special_siblings"] = []
        extra_config["siblings"] = []

    # Generate scripts
    generator = EnvScriptGenerator()
    sh_path, ps1_path = generator.generate_both_scripts(project_name, project_dir, **extra_config)

    print_success(f"Generated {sh_path}", Emoji.SUCCESS)
    print_success(f"Generated {ps1_path}", Emoji.SUCCESS)

    return sh_path, ps1_path


def x_create_project_env_scripts__mutmut_3(project_dir: Path, workenv_name: str | None = None) -> tuple[Path, Path]:
    """Create environment scripts for a project based on its pyproject.toml.

    Args:
        project_dir: The project directory
        workenv_name: Optional workenv name override

    Returns:
        Tuple of (env.sh path, env.ps1 path)
    """
    pyproject_path = project_dir / "XXpyproject.tomlXX"

    if not pyproject_path.exists():
        raise FileNotFoundError(f"No pyproject.toml found in {project_dir}")

    # Parse project name from pyproject.toml
    from provide.foundation.file.formats import read_toml

    pyproject = read_toml(pyproject_path)

    project_name = pyproject.get("project", {}).get("name", project_dir.name)
    python_requirement = pyproject.get("project", {}).get("requires-python")

    # Additional config for wrknv projects
    extra_config = {}
    if workenv_name:
        extra_config["workenv_name"] = workenv_name
        extra_config["venv_prefix"] = workenv_name

    # Add Python requirement if present
    if python_requirement:
        extra_config["python_requirement"] = python_requirement

    # Get wrknv configuration
    from wrknv.config import WorkenvConfig

    config = WorkenvConfig.load()
    env_config = config.get_env_config()

    # Use configuration from wrknv.toml if available
    if env_config:
        logger.info("Using env configuration from wrknv.toml", env_config=env_config)
        extra_config["include_tool_verification"] = env_config.get("include_tool_verification", True)

        # Handle new unified siblings format
        siblings_config = env_config.get("siblings", [])
        if siblings_config:
            extra_config["siblings"] = siblings_config
            # Keep backward compatibility
            extra_config["sibling_patterns"] = []
            extra_config["special_siblings"] = []
        else:
            # Backward compatibility with old format
            extra_config["sibling_patterns"] = env_config.get("sibling_patterns", [])
            extra_config["special_siblings"] = env_config.get("special_siblings", [])
            extra_config["siblings"] = []
    else:
        # Default configuration - empty by default
        logger.info("No env configuration found in wrknv.toml, using defaults")
        extra_config["include_tool_verification"] = True
        extra_config["sibling_patterns"] = []
        extra_config["special_siblings"] = []
        extra_config["siblings"] = []

    # Generate scripts
    generator = EnvScriptGenerator()
    sh_path, ps1_path = generator.generate_both_scripts(project_name, project_dir, **extra_config)

    print_success(f"Generated {sh_path}", Emoji.SUCCESS)
    print_success(f"Generated {ps1_path}", Emoji.SUCCESS)

    return sh_path, ps1_path


def x_create_project_env_scripts__mutmut_4(project_dir: Path, workenv_name: str | None = None) -> tuple[Path, Path]:
    """Create environment scripts for a project based on its pyproject.toml.

    Args:
        project_dir: The project directory
        workenv_name: Optional workenv name override

    Returns:
        Tuple of (env.sh path, env.ps1 path)
    """
    pyproject_path = project_dir / "PYPROJECT.TOML"

    if not pyproject_path.exists():
        raise FileNotFoundError(f"No pyproject.toml found in {project_dir}")

    # Parse project name from pyproject.toml
    from provide.foundation.file.formats import read_toml

    pyproject = read_toml(pyproject_path)

    project_name = pyproject.get("project", {}).get("name", project_dir.name)
    python_requirement = pyproject.get("project", {}).get("requires-python")

    # Additional config for wrknv projects
    extra_config = {}
    if workenv_name:
        extra_config["workenv_name"] = workenv_name
        extra_config["venv_prefix"] = workenv_name

    # Add Python requirement if present
    if python_requirement:
        extra_config["python_requirement"] = python_requirement

    # Get wrknv configuration
    from wrknv.config import WorkenvConfig

    config = WorkenvConfig.load()
    env_config = config.get_env_config()

    # Use configuration from wrknv.toml if available
    if env_config:
        logger.info("Using env configuration from wrknv.toml", env_config=env_config)
        extra_config["include_tool_verification"] = env_config.get("include_tool_verification", True)

        # Handle new unified siblings format
        siblings_config = env_config.get("siblings", [])
        if siblings_config:
            extra_config["siblings"] = siblings_config
            # Keep backward compatibility
            extra_config["sibling_patterns"] = []
            extra_config["special_siblings"] = []
        else:
            # Backward compatibility with old format
            extra_config["sibling_patterns"] = env_config.get("sibling_patterns", [])
            extra_config["special_siblings"] = env_config.get("special_siblings", [])
            extra_config["siblings"] = []
    else:
        # Default configuration - empty by default
        logger.info("No env configuration found in wrknv.toml, using defaults")
        extra_config["include_tool_verification"] = True
        extra_config["sibling_patterns"] = []
        extra_config["special_siblings"] = []
        extra_config["siblings"] = []

    # Generate scripts
    generator = EnvScriptGenerator()
    sh_path, ps1_path = generator.generate_both_scripts(project_name, project_dir, **extra_config)

    print_success(f"Generated {sh_path}", Emoji.SUCCESS)
    print_success(f"Generated {ps1_path}", Emoji.SUCCESS)

    return sh_path, ps1_path


def x_create_project_env_scripts__mutmut_5(project_dir: Path, workenv_name: str | None = None) -> tuple[Path, Path]:
    """Create environment scripts for a project based on its pyproject.toml.

    Args:
        project_dir: The project directory
        workenv_name: Optional workenv name override

    Returns:
        Tuple of (env.sh path, env.ps1 path)
    """
    pyproject_path = project_dir / "pyproject.toml"

    if pyproject_path.exists():
        raise FileNotFoundError(f"No pyproject.toml found in {project_dir}")

    # Parse project name from pyproject.toml
    from provide.foundation.file.formats import read_toml

    pyproject = read_toml(pyproject_path)

    project_name = pyproject.get("project", {}).get("name", project_dir.name)
    python_requirement = pyproject.get("project", {}).get("requires-python")

    # Additional config for wrknv projects
    extra_config = {}
    if workenv_name:
        extra_config["workenv_name"] = workenv_name
        extra_config["venv_prefix"] = workenv_name

    # Add Python requirement if present
    if python_requirement:
        extra_config["python_requirement"] = python_requirement

    # Get wrknv configuration
    from wrknv.config import WorkenvConfig

    config = WorkenvConfig.load()
    env_config = config.get_env_config()

    # Use configuration from wrknv.toml if available
    if env_config:
        logger.info("Using env configuration from wrknv.toml", env_config=env_config)
        extra_config["include_tool_verification"] = env_config.get("include_tool_verification", True)

        # Handle new unified siblings format
        siblings_config = env_config.get("siblings", [])
        if siblings_config:
            extra_config["siblings"] = siblings_config
            # Keep backward compatibility
            extra_config["sibling_patterns"] = []
            extra_config["special_siblings"] = []
        else:
            # Backward compatibility with old format
            extra_config["sibling_patterns"] = env_config.get("sibling_patterns", [])
            extra_config["special_siblings"] = env_config.get("special_siblings", [])
            extra_config["siblings"] = []
    else:
        # Default configuration - empty by default
        logger.info("No env configuration found in wrknv.toml, using defaults")
        extra_config["include_tool_verification"] = True
        extra_config["sibling_patterns"] = []
        extra_config["special_siblings"] = []
        extra_config["siblings"] = []

    # Generate scripts
    generator = EnvScriptGenerator()
    sh_path, ps1_path = generator.generate_both_scripts(project_name, project_dir, **extra_config)

    print_success(f"Generated {sh_path}", Emoji.SUCCESS)
    print_success(f"Generated {ps1_path}", Emoji.SUCCESS)

    return sh_path, ps1_path


def x_create_project_env_scripts__mutmut_6(project_dir: Path, workenv_name: str | None = None) -> tuple[Path, Path]:
    """Create environment scripts for a project based on its pyproject.toml.

    Args:
        project_dir: The project directory
        workenv_name: Optional workenv name override

    Returns:
        Tuple of (env.sh path, env.ps1 path)
    """
    pyproject_path = project_dir / "pyproject.toml"

    if not pyproject_path.exists():
        raise FileNotFoundError(None)

    # Parse project name from pyproject.toml
    from provide.foundation.file.formats import read_toml

    pyproject = read_toml(pyproject_path)

    project_name = pyproject.get("project", {}).get("name", project_dir.name)
    python_requirement = pyproject.get("project", {}).get("requires-python")

    # Additional config for wrknv projects
    extra_config = {}
    if workenv_name:
        extra_config["workenv_name"] = workenv_name
        extra_config["venv_prefix"] = workenv_name

    # Add Python requirement if present
    if python_requirement:
        extra_config["python_requirement"] = python_requirement

    # Get wrknv configuration
    from wrknv.config import WorkenvConfig

    config = WorkenvConfig.load()
    env_config = config.get_env_config()

    # Use configuration from wrknv.toml if available
    if env_config:
        logger.info("Using env configuration from wrknv.toml", env_config=env_config)
        extra_config["include_tool_verification"] = env_config.get("include_tool_verification", True)

        # Handle new unified siblings format
        siblings_config = env_config.get("siblings", [])
        if siblings_config:
            extra_config["siblings"] = siblings_config
            # Keep backward compatibility
            extra_config["sibling_patterns"] = []
            extra_config["special_siblings"] = []
        else:
            # Backward compatibility with old format
            extra_config["sibling_patterns"] = env_config.get("sibling_patterns", [])
            extra_config["special_siblings"] = env_config.get("special_siblings", [])
            extra_config["siblings"] = []
    else:
        # Default configuration - empty by default
        logger.info("No env configuration found in wrknv.toml, using defaults")
        extra_config["include_tool_verification"] = True
        extra_config["sibling_patterns"] = []
        extra_config["special_siblings"] = []
        extra_config["siblings"] = []

    # Generate scripts
    generator = EnvScriptGenerator()
    sh_path, ps1_path = generator.generate_both_scripts(project_name, project_dir, **extra_config)

    print_success(f"Generated {sh_path}", Emoji.SUCCESS)
    print_success(f"Generated {ps1_path}", Emoji.SUCCESS)

    return sh_path, ps1_path


def x_create_project_env_scripts__mutmut_7(project_dir: Path, workenv_name: str | None = None) -> tuple[Path, Path]:
    """Create environment scripts for a project based on its pyproject.toml.

    Args:
        project_dir: The project directory
        workenv_name: Optional workenv name override

    Returns:
        Tuple of (env.sh path, env.ps1 path)
    """
    pyproject_path = project_dir / "pyproject.toml"

    if not pyproject_path.exists():
        raise FileNotFoundError(f"No pyproject.toml found in {project_dir}")

    # Parse project name from pyproject.toml
    from provide.foundation.file.formats import read_toml

    pyproject = None

    project_name = pyproject.get("project", {}).get("name", project_dir.name)
    python_requirement = pyproject.get("project", {}).get("requires-python")

    # Additional config for wrknv projects
    extra_config = {}
    if workenv_name:
        extra_config["workenv_name"] = workenv_name
        extra_config["venv_prefix"] = workenv_name

    # Add Python requirement if present
    if python_requirement:
        extra_config["python_requirement"] = python_requirement

    # Get wrknv configuration
    from wrknv.config import WorkenvConfig

    config = WorkenvConfig.load()
    env_config = config.get_env_config()

    # Use configuration from wrknv.toml if available
    if env_config:
        logger.info("Using env configuration from wrknv.toml", env_config=env_config)
        extra_config["include_tool_verification"] = env_config.get("include_tool_verification", True)

        # Handle new unified siblings format
        siblings_config = env_config.get("siblings", [])
        if siblings_config:
            extra_config["siblings"] = siblings_config
            # Keep backward compatibility
            extra_config["sibling_patterns"] = []
            extra_config["special_siblings"] = []
        else:
            # Backward compatibility with old format
            extra_config["sibling_patterns"] = env_config.get("sibling_patterns", [])
            extra_config["special_siblings"] = env_config.get("special_siblings", [])
            extra_config["siblings"] = []
    else:
        # Default configuration - empty by default
        logger.info("No env configuration found in wrknv.toml, using defaults")
        extra_config["include_tool_verification"] = True
        extra_config["sibling_patterns"] = []
        extra_config["special_siblings"] = []
        extra_config["siblings"] = []

    # Generate scripts
    generator = EnvScriptGenerator()
    sh_path, ps1_path = generator.generate_both_scripts(project_name, project_dir, **extra_config)

    print_success(f"Generated {sh_path}", Emoji.SUCCESS)
    print_success(f"Generated {ps1_path}", Emoji.SUCCESS)

    return sh_path, ps1_path


def x_create_project_env_scripts__mutmut_8(project_dir: Path, workenv_name: str | None = None) -> tuple[Path, Path]:
    """Create environment scripts for a project based on its pyproject.toml.

    Args:
        project_dir: The project directory
        workenv_name: Optional workenv name override

    Returns:
        Tuple of (env.sh path, env.ps1 path)
    """
    pyproject_path = project_dir / "pyproject.toml"

    if not pyproject_path.exists():
        raise FileNotFoundError(f"No pyproject.toml found in {project_dir}")

    # Parse project name from pyproject.toml
    from provide.foundation.file.formats import read_toml

    pyproject = read_toml(None)

    project_name = pyproject.get("project", {}).get("name", project_dir.name)
    python_requirement = pyproject.get("project", {}).get("requires-python")

    # Additional config for wrknv projects
    extra_config = {}
    if workenv_name:
        extra_config["workenv_name"] = workenv_name
        extra_config["venv_prefix"] = workenv_name

    # Add Python requirement if present
    if python_requirement:
        extra_config["python_requirement"] = python_requirement

    # Get wrknv configuration
    from wrknv.config import WorkenvConfig

    config = WorkenvConfig.load()
    env_config = config.get_env_config()

    # Use configuration from wrknv.toml if available
    if env_config:
        logger.info("Using env configuration from wrknv.toml", env_config=env_config)
        extra_config["include_tool_verification"] = env_config.get("include_tool_verification", True)

        # Handle new unified siblings format
        siblings_config = env_config.get("siblings", [])
        if siblings_config:
            extra_config["siblings"] = siblings_config
            # Keep backward compatibility
            extra_config["sibling_patterns"] = []
            extra_config["special_siblings"] = []
        else:
            # Backward compatibility with old format
            extra_config["sibling_patterns"] = env_config.get("sibling_patterns", [])
            extra_config["special_siblings"] = env_config.get("special_siblings", [])
            extra_config["siblings"] = []
    else:
        # Default configuration - empty by default
        logger.info("No env configuration found in wrknv.toml, using defaults")
        extra_config["include_tool_verification"] = True
        extra_config["sibling_patterns"] = []
        extra_config["special_siblings"] = []
        extra_config["siblings"] = []

    # Generate scripts
    generator = EnvScriptGenerator()
    sh_path, ps1_path = generator.generate_both_scripts(project_name, project_dir, **extra_config)

    print_success(f"Generated {sh_path}", Emoji.SUCCESS)
    print_success(f"Generated {ps1_path}", Emoji.SUCCESS)

    return sh_path, ps1_path


def x_create_project_env_scripts__mutmut_9(project_dir: Path, workenv_name: str | None = None) -> tuple[Path, Path]:
    """Create environment scripts for a project based on its pyproject.toml.

    Args:
        project_dir: The project directory
        workenv_name: Optional workenv name override

    Returns:
        Tuple of (env.sh path, env.ps1 path)
    """
    pyproject_path = project_dir / "pyproject.toml"

    if not pyproject_path.exists():
        raise FileNotFoundError(f"No pyproject.toml found in {project_dir}")

    # Parse project name from pyproject.toml
    from provide.foundation.file.formats import read_toml

    pyproject = read_toml(pyproject_path)

    project_name = None
    python_requirement = pyproject.get("project", {}).get("requires-python")

    # Additional config for wrknv projects
    extra_config = {}
    if workenv_name:
        extra_config["workenv_name"] = workenv_name
        extra_config["venv_prefix"] = workenv_name

    # Add Python requirement if present
    if python_requirement:
        extra_config["python_requirement"] = python_requirement

    # Get wrknv configuration
    from wrknv.config import WorkenvConfig

    config = WorkenvConfig.load()
    env_config = config.get_env_config()

    # Use configuration from wrknv.toml if available
    if env_config:
        logger.info("Using env configuration from wrknv.toml", env_config=env_config)
        extra_config["include_tool_verification"] = env_config.get("include_tool_verification", True)

        # Handle new unified siblings format
        siblings_config = env_config.get("siblings", [])
        if siblings_config:
            extra_config["siblings"] = siblings_config
            # Keep backward compatibility
            extra_config["sibling_patterns"] = []
            extra_config["special_siblings"] = []
        else:
            # Backward compatibility with old format
            extra_config["sibling_patterns"] = env_config.get("sibling_patterns", [])
            extra_config["special_siblings"] = env_config.get("special_siblings", [])
            extra_config["siblings"] = []
    else:
        # Default configuration - empty by default
        logger.info("No env configuration found in wrknv.toml, using defaults")
        extra_config["include_tool_verification"] = True
        extra_config["sibling_patterns"] = []
        extra_config["special_siblings"] = []
        extra_config["siblings"] = []

    # Generate scripts
    generator = EnvScriptGenerator()
    sh_path, ps1_path = generator.generate_both_scripts(project_name, project_dir, **extra_config)

    print_success(f"Generated {sh_path}", Emoji.SUCCESS)
    print_success(f"Generated {ps1_path}", Emoji.SUCCESS)

    return sh_path, ps1_path


def x_create_project_env_scripts__mutmut_10(project_dir: Path, workenv_name: str | None = None) -> tuple[Path, Path]:
    """Create environment scripts for a project based on its pyproject.toml.

    Args:
        project_dir: The project directory
        workenv_name: Optional workenv name override

    Returns:
        Tuple of (env.sh path, env.ps1 path)
    """
    pyproject_path = project_dir / "pyproject.toml"

    if not pyproject_path.exists():
        raise FileNotFoundError(f"No pyproject.toml found in {project_dir}")

    # Parse project name from pyproject.toml
    from provide.foundation.file.formats import read_toml

    pyproject = read_toml(pyproject_path)

    project_name = pyproject.get("project", {}).get(None, project_dir.name)
    python_requirement = pyproject.get("project", {}).get("requires-python")

    # Additional config for wrknv projects
    extra_config = {}
    if workenv_name:
        extra_config["workenv_name"] = workenv_name
        extra_config["venv_prefix"] = workenv_name

    # Add Python requirement if present
    if python_requirement:
        extra_config["python_requirement"] = python_requirement

    # Get wrknv configuration
    from wrknv.config import WorkenvConfig

    config = WorkenvConfig.load()
    env_config = config.get_env_config()

    # Use configuration from wrknv.toml if available
    if env_config:
        logger.info("Using env configuration from wrknv.toml", env_config=env_config)
        extra_config["include_tool_verification"] = env_config.get("include_tool_verification", True)

        # Handle new unified siblings format
        siblings_config = env_config.get("siblings", [])
        if siblings_config:
            extra_config["siblings"] = siblings_config
            # Keep backward compatibility
            extra_config["sibling_patterns"] = []
            extra_config["special_siblings"] = []
        else:
            # Backward compatibility with old format
            extra_config["sibling_patterns"] = env_config.get("sibling_patterns", [])
            extra_config["special_siblings"] = env_config.get("special_siblings", [])
            extra_config["siblings"] = []
    else:
        # Default configuration - empty by default
        logger.info("No env configuration found in wrknv.toml, using defaults")
        extra_config["include_tool_verification"] = True
        extra_config["sibling_patterns"] = []
        extra_config["special_siblings"] = []
        extra_config["siblings"] = []

    # Generate scripts
    generator = EnvScriptGenerator()
    sh_path, ps1_path = generator.generate_both_scripts(project_name, project_dir, **extra_config)

    print_success(f"Generated {sh_path}", Emoji.SUCCESS)
    print_success(f"Generated {ps1_path}", Emoji.SUCCESS)

    return sh_path, ps1_path


def x_create_project_env_scripts__mutmut_11(project_dir: Path, workenv_name: str | None = None) -> tuple[Path, Path]:
    """Create environment scripts for a project based on its pyproject.toml.

    Args:
        project_dir: The project directory
        workenv_name: Optional workenv name override

    Returns:
        Tuple of (env.sh path, env.ps1 path)
    """
    pyproject_path = project_dir / "pyproject.toml"

    if not pyproject_path.exists():
        raise FileNotFoundError(f"No pyproject.toml found in {project_dir}")

    # Parse project name from pyproject.toml
    from provide.foundation.file.formats import read_toml

    pyproject = read_toml(pyproject_path)

    project_name = pyproject.get("project", {}).get("name", None)
    python_requirement = pyproject.get("project", {}).get("requires-python")

    # Additional config for wrknv projects
    extra_config = {}
    if workenv_name:
        extra_config["workenv_name"] = workenv_name
        extra_config["venv_prefix"] = workenv_name

    # Add Python requirement if present
    if python_requirement:
        extra_config["python_requirement"] = python_requirement

    # Get wrknv configuration
    from wrknv.config import WorkenvConfig

    config = WorkenvConfig.load()
    env_config = config.get_env_config()

    # Use configuration from wrknv.toml if available
    if env_config:
        logger.info("Using env configuration from wrknv.toml", env_config=env_config)
        extra_config["include_tool_verification"] = env_config.get("include_tool_verification", True)

        # Handle new unified siblings format
        siblings_config = env_config.get("siblings", [])
        if siblings_config:
            extra_config["siblings"] = siblings_config
            # Keep backward compatibility
            extra_config["sibling_patterns"] = []
            extra_config["special_siblings"] = []
        else:
            # Backward compatibility with old format
            extra_config["sibling_patterns"] = env_config.get("sibling_patterns", [])
            extra_config["special_siblings"] = env_config.get("special_siblings", [])
            extra_config["siblings"] = []
    else:
        # Default configuration - empty by default
        logger.info("No env configuration found in wrknv.toml, using defaults")
        extra_config["include_tool_verification"] = True
        extra_config["sibling_patterns"] = []
        extra_config["special_siblings"] = []
        extra_config["siblings"] = []

    # Generate scripts
    generator = EnvScriptGenerator()
    sh_path, ps1_path = generator.generate_both_scripts(project_name, project_dir, **extra_config)

    print_success(f"Generated {sh_path}", Emoji.SUCCESS)
    print_success(f"Generated {ps1_path}", Emoji.SUCCESS)

    return sh_path, ps1_path


def x_create_project_env_scripts__mutmut_12(project_dir: Path, workenv_name: str | None = None) -> tuple[Path, Path]:
    """Create environment scripts for a project based on its pyproject.toml.

    Args:
        project_dir: The project directory
        workenv_name: Optional workenv name override

    Returns:
        Tuple of (env.sh path, env.ps1 path)
    """
    pyproject_path = project_dir / "pyproject.toml"

    if not pyproject_path.exists():
        raise FileNotFoundError(f"No pyproject.toml found in {project_dir}")

    # Parse project name from pyproject.toml
    from provide.foundation.file.formats import read_toml

    pyproject = read_toml(pyproject_path)

    project_name = pyproject.get("project", {}).get(project_dir.name)
    python_requirement = pyproject.get("project", {}).get("requires-python")

    # Additional config for wrknv projects
    extra_config = {}
    if workenv_name:
        extra_config["workenv_name"] = workenv_name
        extra_config["venv_prefix"] = workenv_name

    # Add Python requirement if present
    if python_requirement:
        extra_config["python_requirement"] = python_requirement

    # Get wrknv configuration
    from wrknv.config import WorkenvConfig

    config = WorkenvConfig.load()
    env_config = config.get_env_config()

    # Use configuration from wrknv.toml if available
    if env_config:
        logger.info("Using env configuration from wrknv.toml", env_config=env_config)
        extra_config["include_tool_verification"] = env_config.get("include_tool_verification", True)

        # Handle new unified siblings format
        siblings_config = env_config.get("siblings", [])
        if siblings_config:
            extra_config["siblings"] = siblings_config
            # Keep backward compatibility
            extra_config["sibling_patterns"] = []
            extra_config["special_siblings"] = []
        else:
            # Backward compatibility with old format
            extra_config["sibling_patterns"] = env_config.get("sibling_patterns", [])
            extra_config["special_siblings"] = env_config.get("special_siblings", [])
            extra_config["siblings"] = []
    else:
        # Default configuration - empty by default
        logger.info("No env configuration found in wrknv.toml, using defaults")
        extra_config["include_tool_verification"] = True
        extra_config["sibling_patterns"] = []
        extra_config["special_siblings"] = []
        extra_config["siblings"] = []

    # Generate scripts
    generator = EnvScriptGenerator()
    sh_path, ps1_path = generator.generate_both_scripts(project_name, project_dir, **extra_config)

    print_success(f"Generated {sh_path}", Emoji.SUCCESS)
    print_success(f"Generated {ps1_path}", Emoji.SUCCESS)

    return sh_path, ps1_path


def x_create_project_env_scripts__mutmut_13(project_dir: Path, workenv_name: str | None = None) -> tuple[Path, Path]:
    """Create environment scripts for a project based on its pyproject.toml.

    Args:
        project_dir: The project directory
        workenv_name: Optional workenv name override

    Returns:
        Tuple of (env.sh path, env.ps1 path)
    """
    pyproject_path = project_dir / "pyproject.toml"

    if not pyproject_path.exists():
        raise FileNotFoundError(f"No pyproject.toml found in {project_dir}")

    # Parse project name from pyproject.toml
    from provide.foundation.file.formats import read_toml

    pyproject = read_toml(pyproject_path)

    project_name = pyproject.get("project", {}).get("name", )
    python_requirement = pyproject.get("project", {}).get("requires-python")

    # Additional config for wrknv projects
    extra_config = {}
    if workenv_name:
        extra_config["workenv_name"] = workenv_name
        extra_config["venv_prefix"] = workenv_name

    # Add Python requirement if present
    if python_requirement:
        extra_config["python_requirement"] = python_requirement

    # Get wrknv configuration
    from wrknv.config import WorkenvConfig

    config = WorkenvConfig.load()
    env_config = config.get_env_config()

    # Use configuration from wrknv.toml if available
    if env_config:
        logger.info("Using env configuration from wrknv.toml", env_config=env_config)
        extra_config["include_tool_verification"] = env_config.get("include_tool_verification", True)

        # Handle new unified siblings format
        siblings_config = env_config.get("siblings", [])
        if siblings_config:
            extra_config["siblings"] = siblings_config
            # Keep backward compatibility
            extra_config["sibling_patterns"] = []
            extra_config["special_siblings"] = []
        else:
            # Backward compatibility with old format
            extra_config["sibling_patterns"] = env_config.get("sibling_patterns", [])
            extra_config["special_siblings"] = env_config.get("special_siblings", [])
            extra_config["siblings"] = []
    else:
        # Default configuration - empty by default
        logger.info("No env configuration found in wrknv.toml, using defaults")
        extra_config["include_tool_verification"] = True
        extra_config["sibling_patterns"] = []
        extra_config["special_siblings"] = []
        extra_config["siblings"] = []

    # Generate scripts
    generator = EnvScriptGenerator()
    sh_path, ps1_path = generator.generate_both_scripts(project_name, project_dir, **extra_config)

    print_success(f"Generated {sh_path}", Emoji.SUCCESS)
    print_success(f"Generated {ps1_path}", Emoji.SUCCESS)

    return sh_path, ps1_path


def x_create_project_env_scripts__mutmut_14(project_dir: Path, workenv_name: str | None = None) -> tuple[Path, Path]:
    """Create environment scripts for a project based on its pyproject.toml.

    Args:
        project_dir: The project directory
        workenv_name: Optional workenv name override

    Returns:
        Tuple of (env.sh path, env.ps1 path)
    """
    pyproject_path = project_dir / "pyproject.toml"

    if not pyproject_path.exists():
        raise FileNotFoundError(f"No pyproject.toml found in {project_dir}")

    # Parse project name from pyproject.toml
    from provide.foundation.file.formats import read_toml

    pyproject = read_toml(pyproject_path)

    project_name = pyproject.get(None, {}).get("name", project_dir.name)
    python_requirement = pyproject.get("project", {}).get("requires-python")

    # Additional config for wrknv projects
    extra_config = {}
    if workenv_name:
        extra_config["workenv_name"] = workenv_name
        extra_config["venv_prefix"] = workenv_name

    # Add Python requirement if present
    if python_requirement:
        extra_config["python_requirement"] = python_requirement

    # Get wrknv configuration
    from wrknv.config import WorkenvConfig

    config = WorkenvConfig.load()
    env_config = config.get_env_config()

    # Use configuration from wrknv.toml if available
    if env_config:
        logger.info("Using env configuration from wrknv.toml", env_config=env_config)
        extra_config["include_tool_verification"] = env_config.get("include_tool_verification", True)

        # Handle new unified siblings format
        siblings_config = env_config.get("siblings", [])
        if siblings_config:
            extra_config["siblings"] = siblings_config
            # Keep backward compatibility
            extra_config["sibling_patterns"] = []
            extra_config["special_siblings"] = []
        else:
            # Backward compatibility with old format
            extra_config["sibling_patterns"] = env_config.get("sibling_patterns", [])
            extra_config["special_siblings"] = env_config.get("special_siblings", [])
            extra_config["siblings"] = []
    else:
        # Default configuration - empty by default
        logger.info("No env configuration found in wrknv.toml, using defaults")
        extra_config["include_tool_verification"] = True
        extra_config["sibling_patterns"] = []
        extra_config["special_siblings"] = []
        extra_config["siblings"] = []

    # Generate scripts
    generator = EnvScriptGenerator()
    sh_path, ps1_path = generator.generate_both_scripts(project_name, project_dir, **extra_config)

    print_success(f"Generated {sh_path}", Emoji.SUCCESS)
    print_success(f"Generated {ps1_path}", Emoji.SUCCESS)

    return sh_path, ps1_path


def x_create_project_env_scripts__mutmut_15(project_dir: Path, workenv_name: str | None = None) -> tuple[Path, Path]:
    """Create environment scripts for a project based on its pyproject.toml.

    Args:
        project_dir: The project directory
        workenv_name: Optional workenv name override

    Returns:
        Tuple of (env.sh path, env.ps1 path)
    """
    pyproject_path = project_dir / "pyproject.toml"

    if not pyproject_path.exists():
        raise FileNotFoundError(f"No pyproject.toml found in {project_dir}")

    # Parse project name from pyproject.toml
    from provide.foundation.file.formats import read_toml

    pyproject = read_toml(pyproject_path)

    project_name = pyproject.get("project", None).get("name", project_dir.name)
    python_requirement = pyproject.get("project", {}).get("requires-python")

    # Additional config for wrknv projects
    extra_config = {}
    if workenv_name:
        extra_config["workenv_name"] = workenv_name
        extra_config["venv_prefix"] = workenv_name

    # Add Python requirement if present
    if python_requirement:
        extra_config["python_requirement"] = python_requirement

    # Get wrknv configuration
    from wrknv.config import WorkenvConfig

    config = WorkenvConfig.load()
    env_config = config.get_env_config()

    # Use configuration from wrknv.toml if available
    if env_config:
        logger.info("Using env configuration from wrknv.toml", env_config=env_config)
        extra_config["include_tool_verification"] = env_config.get("include_tool_verification", True)

        # Handle new unified siblings format
        siblings_config = env_config.get("siblings", [])
        if siblings_config:
            extra_config["siblings"] = siblings_config
            # Keep backward compatibility
            extra_config["sibling_patterns"] = []
            extra_config["special_siblings"] = []
        else:
            # Backward compatibility with old format
            extra_config["sibling_patterns"] = env_config.get("sibling_patterns", [])
            extra_config["special_siblings"] = env_config.get("special_siblings", [])
            extra_config["siblings"] = []
    else:
        # Default configuration - empty by default
        logger.info("No env configuration found in wrknv.toml, using defaults")
        extra_config["include_tool_verification"] = True
        extra_config["sibling_patterns"] = []
        extra_config["special_siblings"] = []
        extra_config["siblings"] = []

    # Generate scripts
    generator = EnvScriptGenerator()
    sh_path, ps1_path = generator.generate_both_scripts(project_name, project_dir, **extra_config)

    print_success(f"Generated {sh_path}", Emoji.SUCCESS)
    print_success(f"Generated {ps1_path}", Emoji.SUCCESS)

    return sh_path, ps1_path


def x_create_project_env_scripts__mutmut_16(project_dir: Path, workenv_name: str | None = None) -> tuple[Path, Path]:
    """Create environment scripts for a project based on its pyproject.toml.

    Args:
        project_dir: The project directory
        workenv_name: Optional workenv name override

    Returns:
        Tuple of (env.sh path, env.ps1 path)
    """
    pyproject_path = project_dir / "pyproject.toml"

    if not pyproject_path.exists():
        raise FileNotFoundError(f"No pyproject.toml found in {project_dir}")

    # Parse project name from pyproject.toml
    from provide.foundation.file.formats import read_toml

    pyproject = read_toml(pyproject_path)

    project_name = pyproject.get({}).get("name", project_dir.name)
    python_requirement = pyproject.get("project", {}).get("requires-python")

    # Additional config for wrknv projects
    extra_config = {}
    if workenv_name:
        extra_config["workenv_name"] = workenv_name
        extra_config["venv_prefix"] = workenv_name

    # Add Python requirement if present
    if python_requirement:
        extra_config["python_requirement"] = python_requirement

    # Get wrknv configuration
    from wrknv.config import WorkenvConfig

    config = WorkenvConfig.load()
    env_config = config.get_env_config()

    # Use configuration from wrknv.toml if available
    if env_config:
        logger.info("Using env configuration from wrknv.toml", env_config=env_config)
        extra_config["include_tool_verification"] = env_config.get("include_tool_verification", True)

        # Handle new unified siblings format
        siblings_config = env_config.get("siblings", [])
        if siblings_config:
            extra_config["siblings"] = siblings_config
            # Keep backward compatibility
            extra_config["sibling_patterns"] = []
            extra_config["special_siblings"] = []
        else:
            # Backward compatibility with old format
            extra_config["sibling_patterns"] = env_config.get("sibling_patterns", [])
            extra_config["special_siblings"] = env_config.get("special_siblings", [])
            extra_config["siblings"] = []
    else:
        # Default configuration - empty by default
        logger.info("No env configuration found in wrknv.toml, using defaults")
        extra_config["include_tool_verification"] = True
        extra_config["sibling_patterns"] = []
        extra_config["special_siblings"] = []
        extra_config["siblings"] = []

    # Generate scripts
    generator = EnvScriptGenerator()
    sh_path, ps1_path = generator.generate_both_scripts(project_name, project_dir, **extra_config)

    print_success(f"Generated {sh_path}", Emoji.SUCCESS)
    print_success(f"Generated {ps1_path}", Emoji.SUCCESS)

    return sh_path, ps1_path


def x_create_project_env_scripts__mutmut_17(project_dir: Path, workenv_name: str | None = None) -> tuple[Path, Path]:
    """Create environment scripts for a project based on its pyproject.toml.

    Args:
        project_dir: The project directory
        workenv_name: Optional workenv name override

    Returns:
        Tuple of (env.sh path, env.ps1 path)
    """
    pyproject_path = project_dir / "pyproject.toml"

    if not pyproject_path.exists():
        raise FileNotFoundError(f"No pyproject.toml found in {project_dir}")

    # Parse project name from pyproject.toml
    from provide.foundation.file.formats import read_toml

    pyproject = read_toml(pyproject_path)

    project_name = pyproject.get("project", ).get("name", project_dir.name)
    python_requirement = pyproject.get("project", {}).get("requires-python")

    # Additional config for wrknv projects
    extra_config = {}
    if workenv_name:
        extra_config["workenv_name"] = workenv_name
        extra_config["venv_prefix"] = workenv_name

    # Add Python requirement if present
    if python_requirement:
        extra_config["python_requirement"] = python_requirement

    # Get wrknv configuration
    from wrknv.config import WorkenvConfig

    config = WorkenvConfig.load()
    env_config = config.get_env_config()

    # Use configuration from wrknv.toml if available
    if env_config:
        logger.info("Using env configuration from wrknv.toml", env_config=env_config)
        extra_config["include_tool_verification"] = env_config.get("include_tool_verification", True)

        # Handle new unified siblings format
        siblings_config = env_config.get("siblings", [])
        if siblings_config:
            extra_config["siblings"] = siblings_config
            # Keep backward compatibility
            extra_config["sibling_patterns"] = []
            extra_config["special_siblings"] = []
        else:
            # Backward compatibility with old format
            extra_config["sibling_patterns"] = env_config.get("sibling_patterns", [])
            extra_config["special_siblings"] = env_config.get("special_siblings", [])
            extra_config["siblings"] = []
    else:
        # Default configuration - empty by default
        logger.info("No env configuration found in wrknv.toml, using defaults")
        extra_config["include_tool_verification"] = True
        extra_config["sibling_patterns"] = []
        extra_config["special_siblings"] = []
        extra_config["siblings"] = []

    # Generate scripts
    generator = EnvScriptGenerator()
    sh_path, ps1_path = generator.generate_both_scripts(project_name, project_dir, **extra_config)

    print_success(f"Generated {sh_path}", Emoji.SUCCESS)
    print_success(f"Generated {ps1_path}", Emoji.SUCCESS)

    return sh_path, ps1_path


def x_create_project_env_scripts__mutmut_18(project_dir: Path, workenv_name: str | None = None) -> tuple[Path, Path]:
    """Create environment scripts for a project based on its pyproject.toml.

    Args:
        project_dir: The project directory
        workenv_name: Optional workenv name override

    Returns:
        Tuple of (env.sh path, env.ps1 path)
    """
    pyproject_path = project_dir / "pyproject.toml"

    if not pyproject_path.exists():
        raise FileNotFoundError(f"No pyproject.toml found in {project_dir}")

    # Parse project name from pyproject.toml
    from provide.foundation.file.formats import read_toml

    pyproject = read_toml(pyproject_path)

    project_name = pyproject.get("XXprojectXX", {}).get("name", project_dir.name)
    python_requirement = pyproject.get("project", {}).get("requires-python")

    # Additional config for wrknv projects
    extra_config = {}
    if workenv_name:
        extra_config["workenv_name"] = workenv_name
        extra_config["venv_prefix"] = workenv_name

    # Add Python requirement if present
    if python_requirement:
        extra_config["python_requirement"] = python_requirement

    # Get wrknv configuration
    from wrknv.config import WorkenvConfig

    config = WorkenvConfig.load()
    env_config = config.get_env_config()

    # Use configuration from wrknv.toml if available
    if env_config:
        logger.info("Using env configuration from wrknv.toml", env_config=env_config)
        extra_config["include_tool_verification"] = env_config.get("include_tool_verification", True)

        # Handle new unified siblings format
        siblings_config = env_config.get("siblings", [])
        if siblings_config:
            extra_config["siblings"] = siblings_config
            # Keep backward compatibility
            extra_config["sibling_patterns"] = []
            extra_config["special_siblings"] = []
        else:
            # Backward compatibility with old format
            extra_config["sibling_patterns"] = env_config.get("sibling_patterns", [])
            extra_config["special_siblings"] = env_config.get("special_siblings", [])
            extra_config["siblings"] = []
    else:
        # Default configuration - empty by default
        logger.info("No env configuration found in wrknv.toml, using defaults")
        extra_config["include_tool_verification"] = True
        extra_config["sibling_patterns"] = []
        extra_config["special_siblings"] = []
        extra_config["siblings"] = []

    # Generate scripts
    generator = EnvScriptGenerator()
    sh_path, ps1_path = generator.generate_both_scripts(project_name, project_dir, **extra_config)

    print_success(f"Generated {sh_path}", Emoji.SUCCESS)
    print_success(f"Generated {ps1_path}", Emoji.SUCCESS)

    return sh_path, ps1_path


def x_create_project_env_scripts__mutmut_19(project_dir: Path, workenv_name: str | None = None) -> tuple[Path, Path]:
    """Create environment scripts for a project based on its pyproject.toml.

    Args:
        project_dir: The project directory
        workenv_name: Optional workenv name override

    Returns:
        Tuple of (env.sh path, env.ps1 path)
    """
    pyproject_path = project_dir / "pyproject.toml"

    if not pyproject_path.exists():
        raise FileNotFoundError(f"No pyproject.toml found in {project_dir}")

    # Parse project name from pyproject.toml
    from provide.foundation.file.formats import read_toml

    pyproject = read_toml(pyproject_path)

    project_name = pyproject.get("PROJECT", {}).get("name", project_dir.name)
    python_requirement = pyproject.get("project", {}).get("requires-python")

    # Additional config for wrknv projects
    extra_config = {}
    if workenv_name:
        extra_config["workenv_name"] = workenv_name
        extra_config["venv_prefix"] = workenv_name

    # Add Python requirement if present
    if python_requirement:
        extra_config["python_requirement"] = python_requirement

    # Get wrknv configuration
    from wrknv.config import WorkenvConfig

    config = WorkenvConfig.load()
    env_config = config.get_env_config()

    # Use configuration from wrknv.toml if available
    if env_config:
        logger.info("Using env configuration from wrknv.toml", env_config=env_config)
        extra_config["include_tool_verification"] = env_config.get("include_tool_verification", True)

        # Handle new unified siblings format
        siblings_config = env_config.get("siblings", [])
        if siblings_config:
            extra_config["siblings"] = siblings_config
            # Keep backward compatibility
            extra_config["sibling_patterns"] = []
            extra_config["special_siblings"] = []
        else:
            # Backward compatibility with old format
            extra_config["sibling_patterns"] = env_config.get("sibling_patterns", [])
            extra_config["special_siblings"] = env_config.get("special_siblings", [])
            extra_config["siblings"] = []
    else:
        # Default configuration - empty by default
        logger.info("No env configuration found in wrknv.toml, using defaults")
        extra_config["include_tool_verification"] = True
        extra_config["sibling_patterns"] = []
        extra_config["special_siblings"] = []
        extra_config["siblings"] = []

    # Generate scripts
    generator = EnvScriptGenerator()
    sh_path, ps1_path = generator.generate_both_scripts(project_name, project_dir, **extra_config)

    print_success(f"Generated {sh_path}", Emoji.SUCCESS)
    print_success(f"Generated {ps1_path}", Emoji.SUCCESS)

    return sh_path, ps1_path


def x_create_project_env_scripts__mutmut_20(project_dir: Path, workenv_name: str | None = None) -> tuple[Path, Path]:
    """Create environment scripts for a project based on its pyproject.toml.

    Args:
        project_dir: The project directory
        workenv_name: Optional workenv name override

    Returns:
        Tuple of (env.sh path, env.ps1 path)
    """
    pyproject_path = project_dir / "pyproject.toml"

    if not pyproject_path.exists():
        raise FileNotFoundError(f"No pyproject.toml found in {project_dir}")

    # Parse project name from pyproject.toml
    from provide.foundation.file.formats import read_toml

    pyproject = read_toml(pyproject_path)

    project_name = pyproject.get("project", {}).get("XXnameXX", project_dir.name)
    python_requirement = pyproject.get("project", {}).get("requires-python")

    # Additional config for wrknv projects
    extra_config = {}
    if workenv_name:
        extra_config["workenv_name"] = workenv_name
        extra_config["venv_prefix"] = workenv_name

    # Add Python requirement if present
    if python_requirement:
        extra_config["python_requirement"] = python_requirement

    # Get wrknv configuration
    from wrknv.config import WorkenvConfig

    config = WorkenvConfig.load()
    env_config = config.get_env_config()

    # Use configuration from wrknv.toml if available
    if env_config:
        logger.info("Using env configuration from wrknv.toml", env_config=env_config)
        extra_config["include_tool_verification"] = env_config.get("include_tool_verification", True)

        # Handle new unified siblings format
        siblings_config = env_config.get("siblings", [])
        if siblings_config:
            extra_config["siblings"] = siblings_config
            # Keep backward compatibility
            extra_config["sibling_patterns"] = []
            extra_config["special_siblings"] = []
        else:
            # Backward compatibility with old format
            extra_config["sibling_patterns"] = env_config.get("sibling_patterns", [])
            extra_config["special_siblings"] = env_config.get("special_siblings", [])
            extra_config["siblings"] = []
    else:
        # Default configuration - empty by default
        logger.info("No env configuration found in wrknv.toml, using defaults")
        extra_config["include_tool_verification"] = True
        extra_config["sibling_patterns"] = []
        extra_config["special_siblings"] = []
        extra_config["siblings"] = []

    # Generate scripts
    generator = EnvScriptGenerator()
    sh_path, ps1_path = generator.generate_both_scripts(project_name, project_dir, **extra_config)

    print_success(f"Generated {sh_path}", Emoji.SUCCESS)
    print_success(f"Generated {ps1_path}", Emoji.SUCCESS)

    return sh_path, ps1_path


def x_create_project_env_scripts__mutmut_21(project_dir: Path, workenv_name: str | None = None) -> tuple[Path, Path]:
    """Create environment scripts for a project based on its pyproject.toml.

    Args:
        project_dir: The project directory
        workenv_name: Optional workenv name override

    Returns:
        Tuple of (env.sh path, env.ps1 path)
    """
    pyproject_path = project_dir / "pyproject.toml"

    if not pyproject_path.exists():
        raise FileNotFoundError(f"No pyproject.toml found in {project_dir}")

    # Parse project name from pyproject.toml
    from provide.foundation.file.formats import read_toml

    pyproject = read_toml(pyproject_path)

    project_name = pyproject.get("project", {}).get("NAME", project_dir.name)
    python_requirement = pyproject.get("project", {}).get("requires-python")

    # Additional config for wrknv projects
    extra_config = {}
    if workenv_name:
        extra_config["workenv_name"] = workenv_name
        extra_config["venv_prefix"] = workenv_name

    # Add Python requirement if present
    if python_requirement:
        extra_config["python_requirement"] = python_requirement

    # Get wrknv configuration
    from wrknv.config import WorkenvConfig

    config = WorkenvConfig.load()
    env_config = config.get_env_config()

    # Use configuration from wrknv.toml if available
    if env_config:
        logger.info("Using env configuration from wrknv.toml", env_config=env_config)
        extra_config["include_tool_verification"] = env_config.get("include_tool_verification", True)

        # Handle new unified siblings format
        siblings_config = env_config.get("siblings", [])
        if siblings_config:
            extra_config["siblings"] = siblings_config
            # Keep backward compatibility
            extra_config["sibling_patterns"] = []
            extra_config["special_siblings"] = []
        else:
            # Backward compatibility with old format
            extra_config["sibling_patterns"] = env_config.get("sibling_patterns", [])
            extra_config["special_siblings"] = env_config.get("special_siblings", [])
            extra_config["siblings"] = []
    else:
        # Default configuration - empty by default
        logger.info("No env configuration found in wrknv.toml, using defaults")
        extra_config["include_tool_verification"] = True
        extra_config["sibling_patterns"] = []
        extra_config["special_siblings"] = []
        extra_config["siblings"] = []

    # Generate scripts
    generator = EnvScriptGenerator()
    sh_path, ps1_path = generator.generate_both_scripts(project_name, project_dir, **extra_config)

    print_success(f"Generated {sh_path}", Emoji.SUCCESS)
    print_success(f"Generated {ps1_path}", Emoji.SUCCESS)

    return sh_path, ps1_path


def x_create_project_env_scripts__mutmut_22(project_dir: Path, workenv_name: str | None = None) -> tuple[Path, Path]:
    """Create environment scripts for a project based on its pyproject.toml.

    Args:
        project_dir: The project directory
        workenv_name: Optional workenv name override

    Returns:
        Tuple of (env.sh path, env.ps1 path)
    """
    pyproject_path = project_dir / "pyproject.toml"

    if not pyproject_path.exists():
        raise FileNotFoundError(f"No pyproject.toml found in {project_dir}")

    # Parse project name from pyproject.toml
    from provide.foundation.file.formats import read_toml

    pyproject = read_toml(pyproject_path)

    project_name = pyproject.get("project", {}).get("name", project_dir.name)
    python_requirement = None

    # Additional config for wrknv projects
    extra_config = {}
    if workenv_name:
        extra_config["workenv_name"] = workenv_name
        extra_config["venv_prefix"] = workenv_name

    # Add Python requirement if present
    if python_requirement:
        extra_config["python_requirement"] = python_requirement

    # Get wrknv configuration
    from wrknv.config import WorkenvConfig

    config = WorkenvConfig.load()
    env_config = config.get_env_config()

    # Use configuration from wrknv.toml if available
    if env_config:
        logger.info("Using env configuration from wrknv.toml", env_config=env_config)
        extra_config["include_tool_verification"] = env_config.get("include_tool_verification", True)

        # Handle new unified siblings format
        siblings_config = env_config.get("siblings", [])
        if siblings_config:
            extra_config["siblings"] = siblings_config
            # Keep backward compatibility
            extra_config["sibling_patterns"] = []
            extra_config["special_siblings"] = []
        else:
            # Backward compatibility with old format
            extra_config["sibling_patterns"] = env_config.get("sibling_patterns", [])
            extra_config["special_siblings"] = env_config.get("special_siblings", [])
            extra_config["siblings"] = []
    else:
        # Default configuration - empty by default
        logger.info("No env configuration found in wrknv.toml, using defaults")
        extra_config["include_tool_verification"] = True
        extra_config["sibling_patterns"] = []
        extra_config["special_siblings"] = []
        extra_config["siblings"] = []

    # Generate scripts
    generator = EnvScriptGenerator()
    sh_path, ps1_path = generator.generate_both_scripts(project_name, project_dir, **extra_config)

    print_success(f"Generated {sh_path}", Emoji.SUCCESS)
    print_success(f"Generated {ps1_path}", Emoji.SUCCESS)

    return sh_path, ps1_path


def x_create_project_env_scripts__mutmut_23(project_dir: Path, workenv_name: str | None = None) -> tuple[Path, Path]:
    """Create environment scripts for a project based on its pyproject.toml.

    Args:
        project_dir: The project directory
        workenv_name: Optional workenv name override

    Returns:
        Tuple of (env.sh path, env.ps1 path)
    """
    pyproject_path = project_dir / "pyproject.toml"

    if not pyproject_path.exists():
        raise FileNotFoundError(f"No pyproject.toml found in {project_dir}")

    # Parse project name from pyproject.toml
    from provide.foundation.file.formats import read_toml

    pyproject = read_toml(pyproject_path)

    project_name = pyproject.get("project", {}).get("name", project_dir.name)
    python_requirement = pyproject.get("project", {}).get(None)

    # Additional config for wrknv projects
    extra_config = {}
    if workenv_name:
        extra_config["workenv_name"] = workenv_name
        extra_config["venv_prefix"] = workenv_name

    # Add Python requirement if present
    if python_requirement:
        extra_config["python_requirement"] = python_requirement

    # Get wrknv configuration
    from wrknv.config import WorkenvConfig

    config = WorkenvConfig.load()
    env_config = config.get_env_config()

    # Use configuration from wrknv.toml if available
    if env_config:
        logger.info("Using env configuration from wrknv.toml", env_config=env_config)
        extra_config["include_tool_verification"] = env_config.get("include_tool_verification", True)

        # Handle new unified siblings format
        siblings_config = env_config.get("siblings", [])
        if siblings_config:
            extra_config["siblings"] = siblings_config
            # Keep backward compatibility
            extra_config["sibling_patterns"] = []
            extra_config["special_siblings"] = []
        else:
            # Backward compatibility with old format
            extra_config["sibling_patterns"] = env_config.get("sibling_patterns", [])
            extra_config["special_siblings"] = env_config.get("special_siblings", [])
            extra_config["siblings"] = []
    else:
        # Default configuration - empty by default
        logger.info("No env configuration found in wrknv.toml, using defaults")
        extra_config["include_tool_verification"] = True
        extra_config["sibling_patterns"] = []
        extra_config["special_siblings"] = []
        extra_config["siblings"] = []

    # Generate scripts
    generator = EnvScriptGenerator()
    sh_path, ps1_path = generator.generate_both_scripts(project_name, project_dir, **extra_config)

    print_success(f"Generated {sh_path}", Emoji.SUCCESS)
    print_success(f"Generated {ps1_path}", Emoji.SUCCESS)

    return sh_path, ps1_path


def x_create_project_env_scripts__mutmut_24(project_dir: Path, workenv_name: str | None = None) -> tuple[Path, Path]:
    """Create environment scripts for a project based on its pyproject.toml.

    Args:
        project_dir: The project directory
        workenv_name: Optional workenv name override

    Returns:
        Tuple of (env.sh path, env.ps1 path)
    """
    pyproject_path = project_dir / "pyproject.toml"

    if not pyproject_path.exists():
        raise FileNotFoundError(f"No pyproject.toml found in {project_dir}")

    # Parse project name from pyproject.toml
    from provide.foundation.file.formats import read_toml

    pyproject = read_toml(pyproject_path)

    project_name = pyproject.get("project", {}).get("name", project_dir.name)
    python_requirement = pyproject.get(None, {}).get("requires-python")

    # Additional config for wrknv projects
    extra_config = {}
    if workenv_name:
        extra_config["workenv_name"] = workenv_name
        extra_config["venv_prefix"] = workenv_name

    # Add Python requirement if present
    if python_requirement:
        extra_config["python_requirement"] = python_requirement

    # Get wrknv configuration
    from wrknv.config import WorkenvConfig

    config = WorkenvConfig.load()
    env_config = config.get_env_config()

    # Use configuration from wrknv.toml if available
    if env_config:
        logger.info("Using env configuration from wrknv.toml", env_config=env_config)
        extra_config["include_tool_verification"] = env_config.get("include_tool_verification", True)

        # Handle new unified siblings format
        siblings_config = env_config.get("siblings", [])
        if siblings_config:
            extra_config["siblings"] = siblings_config
            # Keep backward compatibility
            extra_config["sibling_patterns"] = []
            extra_config["special_siblings"] = []
        else:
            # Backward compatibility with old format
            extra_config["sibling_patterns"] = env_config.get("sibling_patterns", [])
            extra_config["special_siblings"] = env_config.get("special_siblings", [])
            extra_config["siblings"] = []
    else:
        # Default configuration - empty by default
        logger.info("No env configuration found in wrknv.toml, using defaults")
        extra_config["include_tool_verification"] = True
        extra_config["sibling_patterns"] = []
        extra_config["special_siblings"] = []
        extra_config["siblings"] = []

    # Generate scripts
    generator = EnvScriptGenerator()
    sh_path, ps1_path = generator.generate_both_scripts(project_name, project_dir, **extra_config)

    print_success(f"Generated {sh_path}", Emoji.SUCCESS)
    print_success(f"Generated {ps1_path}", Emoji.SUCCESS)

    return sh_path, ps1_path


def x_create_project_env_scripts__mutmut_25(project_dir: Path, workenv_name: str | None = None) -> tuple[Path, Path]:
    """Create environment scripts for a project based on its pyproject.toml.

    Args:
        project_dir: The project directory
        workenv_name: Optional workenv name override

    Returns:
        Tuple of (env.sh path, env.ps1 path)
    """
    pyproject_path = project_dir / "pyproject.toml"

    if not pyproject_path.exists():
        raise FileNotFoundError(f"No pyproject.toml found in {project_dir}")

    # Parse project name from pyproject.toml
    from provide.foundation.file.formats import read_toml

    pyproject = read_toml(pyproject_path)

    project_name = pyproject.get("project", {}).get("name", project_dir.name)
    python_requirement = pyproject.get("project", None).get("requires-python")

    # Additional config for wrknv projects
    extra_config = {}
    if workenv_name:
        extra_config["workenv_name"] = workenv_name
        extra_config["venv_prefix"] = workenv_name

    # Add Python requirement if present
    if python_requirement:
        extra_config["python_requirement"] = python_requirement

    # Get wrknv configuration
    from wrknv.config import WorkenvConfig

    config = WorkenvConfig.load()
    env_config = config.get_env_config()

    # Use configuration from wrknv.toml if available
    if env_config:
        logger.info("Using env configuration from wrknv.toml", env_config=env_config)
        extra_config["include_tool_verification"] = env_config.get("include_tool_verification", True)

        # Handle new unified siblings format
        siblings_config = env_config.get("siblings", [])
        if siblings_config:
            extra_config["siblings"] = siblings_config
            # Keep backward compatibility
            extra_config["sibling_patterns"] = []
            extra_config["special_siblings"] = []
        else:
            # Backward compatibility with old format
            extra_config["sibling_patterns"] = env_config.get("sibling_patterns", [])
            extra_config["special_siblings"] = env_config.get("special_siblings", [])
            extra_config["siblings"] = []
    else:
        # Default configuration - empty by default
        logger.info("No env configuration found in wrknv.toml, using defaults")
        extra_config["include_tool_verification"] = True
        extra_config["sibling_patterns"] = []
        extra_config["special_siblings"] = []
        extra_config["siblings"] = []

    # Generate scripts
    generator = EnvScriptGenerator()
    sh_path, ps1_path = generator.generate_both_scripts(project_name, project_dir, **extra_config)

    print_success(f"Generated {sh_path}", Emoji.SUCCESS)
    print_success(f"Generated {ps1_path}", Emoji.SUCCESS)

    return sh_path, ps1_path


def x_create_project_env_scripts__mutmut_26(project_dir: Path, workenv_name: str | None = None) -> tuple[Path, Path]:
    """Create environment scripts for a project based on its pyproject.toml.

    Args:
        project_dir: The project directory
        workenv_name: Optional workenv name override

    Returns:
        Tuple of (env.sh path, env.ps1 path)
    """
    pyproject_path = project_dir / "pyproject.toml"

    if not pyproject_path.exists():
        raise FileNotFoundError(f"No pyproject.toml found in {project_dir}")

    # Parse project name from pyproject.toml
    from provide.foundation.file.formats import read_toml

    pyproject = read_toml(pyproject_path)

    project_name = pyproject.get("project", {}).get("name", project_dir.name)
    python_requirement = pyproject.get({}).get("requires-python")

    # Additional config for wrknv projects
    extra_config = {}
    if workenv_name:
        extra_config["workenv_name"] = workenv_name
        extra_config["venv_prefix"] = workenv_name

    # Add Python requirement if present
    if python_requirement:
        extra_config["python_requirement"] = python_requirement

    # Get wrknv configuration
    from wrknv.config import WorkenvConfig

    config = WorkenvConfig.load()
    env_config = config.get_env_config()

    # Use configuration from wrknv.toml if available
    if env_config:
        logger.info("Using env configuration from wrknv.toml", env_config=env_config)
        extra_config["include_tool_verification"] = env_config.get("include_tool_verification", True)

        # Handle new unified siblings format
        siblings_config = env_config.get("siblings", [])
        if siblings_config:
            extra_config["siblings"] = siblings_config
            # Keep backward compatibility
            extra_config["sibling_patterns"] = []
            extra_config["special_siblings"] = []
        else:
            # Backward compatibility with old format
            extra_config["sibling_patterns"] = env_config.get("sibling_patterns", [])
            extra_config["special_siblings"] = env_config.get("special_siblings", [])
            extra_config["siblings"] = []
    else:
        # Default configuration - empty by default
        logger.info("No env configuration found in wrknv.toml, using defaults")
        extra_config["include_tool_verification"] = True
        extra_config["sibling_patterns"] = []
        extra_config["special_siblings"] = []
        extra_config["siblings"] = []

    # Generate scripts
    generator = EnvScriptGenerator()
    sh_path, ps1_path = generator.generate_both_scripts(project_name, project_dir, **extra_config)

    print_success(f"Generated {sh_path}", Emoji.SUCCESS)
    print_success(f"Generated {ps1_path}", Emoji.SUCCESS)

    return sh_path, ps1_path


def x_create_project_env_scripts__mutmut_27(project_dir: Path, workenv_name: str | None = None) -> tuple[Path, Path]:
    """Create environment scripts for a project based on its pyproject.toml.

    Args:
        project_dir: The project directory
        workenv_name: Optional workenv name override

    Returns:
        Tuple of (env.sh path, env.ps1 path)
    """
    pyproject_path = project_dir / "pyproject.toml"

    if not pyproject_path.exists():
        raise FileNotFoundError(f"No pyproject.toml found in {project_dir}")

    # Parse project name from pyproject.toml
    from provide.foundation.file.formats import read_toml

    pyproject = read_toml(pyproject_path)

    project_name = pyproject.get("project", {}).get("name", project_dir.name)
    python_requirement = pyproject.get("project", ).get("requires-python")

    # Additional config for wrknv projects
    extra_config = {}
    if workenv_name:
        extra_config["workenv_name"] = workenv_name
        extra_config["venv_prefix"] = workenv_name

    # Add Python requirement if present
    if python_requirement:
        extra_config["python_requirement"] = python_requirement

    # Get wrknv configuration
    from wrknv.config import WorkenvConfig

    config = WorkenvConfig.load()
    env_config = config.get_env_config()

    # Use configuration from wrknv.toml if available
    if env_config:
        logger.info("Using env configuration from wrknv.toml", env_config=env_config)
        extra_config["include_tool_verification"] = env_config.get("include_tool_verification", True)

        # Handle new unified siblings format
        siblings_config = env_config.get("siblings", [])
        if siblings_config:
            extra_config["siblings"] = siblings_config
            # Keep backward compatibility
            extra_config["sibling_patterns"] = []
            extra_config["special_siblings"] = []
        else:
            # Backward compatibility with old format
            extra_config["sibling_patterns"] = env_config.get("sibling_patterns", [])
            extra_config["special_siblings"] = env_config.get("special_siblings", [])
            extra_config["siblings"] = []
    else:
        # Default configuration - empty by default
        logger.info("No env configuration found in wrknv.toml, using defaults")
        extra_config["include_tool_verification"] = True
        extra_config["sibling_patterns"] = []
        extra_config["special_siblings"] = []
        extra_config["siblings"] = []

    # Generate scripts
    generator = EnvScriptGenerator()
    sh_path, ps1_path = generator.generate_both_scripts(project_name, project_dir, **extra_config)

    print_success(f"Generated {sh_path}", Emoji.SUCCESS)
    print_success(f"Generated {ps1_path}", Emoji.SUCCESS)

    return sh_path, ps1_path


def x_create_project_env_scripts__mutmut_28(project_dir: Path, workenv_name: str | None = None) -> tuple[Path, Path]:
    """Create environment scripts for a project based on its pyproject.toml.

    Args:
        project_dir: The project directory
        workenv_name: Optional workenv name override

    Returns:
        Tuple of (env.sh path, env.ps1 path)
    """
    pyproject_path = project_dir / "pyproject.toml"

    if not pyproject_path.exists():
        raise FileNotFoundError(f"No pyproject.toml found in {project_dir}")

    # Parse project name from pyproject.toml
    from provide.foundation.file.formats import read_toml

    pyproject = read_toml(pyproject_path)

    project_name = pyproject.get("project", {}).get("name", project_dir.name)
    python_requirement = pyproject.get("XXprojectXX", {}).get("requires-python")

    # Additional config for wrknv projects
    extra_config = {}
    if workenv_name:
        extra_config["workenv_name"] = workenv_name
        extra_config["venv_prefix"] = workenv_name

    # Add Python requirement if present
    if python_requirement:
        extra_config["python_requirement"] = python_requirement

    # Get wrknv configuration
    from wrknv.config import WorkenvConfig

    config = WorkenvConfig.load()
    env_config = config.get_env_config()

    # Use configuration from wrknv.toml if available
    if env_config:
        logger.info("Using env configuration from wrknv.toml", env_config=env_config)
        extra_config["include_tool_verification"] = env_config.get("include_tool_verification", True)

        # Handle new unified siblings format
        siblings_config = env_config.get("siblings", [])
        if siblings_config:
            extra_config["siblings"] = siblings_config
            # Keep backward compatibility
            extra_config["sibling_patterns"] = []
            extra_config["special_siblings"] = []
        else:
            # Backward compatibility with old format
            extra_config["sibling_patterns"] = env_config.get("sibling_patterns", [])
            extra_config["special_siblings"] = env_config.get("special_siblings", [])
            extra_config["siblings"] = []
    else:
        # Default configuration - empty by default
        logger.info("No env configuration found in wrknv.toml, using defaults")
        extra_config["include_tool_verification"] = True
        extra_config["sibling_patterns"] = []
        extra_config["special_siblings"] = []
        extra_config["siblings"] = []

    # Generate scripts
    generator = EnvScriptGenerator()
    sh_path, ps1_path = generator.generate_both_scripts(project_name, project_dir, **extra_config)

    print_success(f"Generated {sh_path}", Emoji.SUCCESS)
    print_success(f"Generated {ps1_path}", Emoji.SUCCESS)

    return sh_path, ps1_path


def x_create_project_env_scripts__mutmut_29(project_dir: Path, workenv_name: str | None = None) -> tuple[Path, Path]:
    """Create environment scripts for a project based on its pyproject.toml.

    Args:
        project_dir: The project directory
        workenv_name: Optional workenv name override

    Returns:
        Tuple of (env.sh path, env.ps1 path)
    """
    pyproject_path = project_dir / "pyproject.toml"

    if not pyproject_path.exists():
        raise FileNotFoundError(f"No pyproject.toml found in {project_dir}")

    # Parse project name from pyproject.toml
    from provide.foundation.file.formats import read_toml

    pyproject = read_toml(pyproject_path)

    project_name = pyproject.get("project", {}).get("name", project_dir.name)
    python_requirement = pyproject.get("PROJECT", {}).get("requires-python")

    # Additional config for wrknv projects
    extra_config = {}
    if workenv_name:
        extra_config["workenv_name"] = workenv_name
        extra_config["venv_prefix"] = workenv_name

    # Add Python requirement if present
    if python_requirement:
        extra_config["python_requirement"] = python_requirement

    # Get wrknv configuration
    from wrknv.config import WorkenvConfig

    config = WorkenvConfig.load()
    env_config = config.get_env_config()

    # Use configuration from wrknv.toml if available
    if env_config:
        logger.info("Using env configuration from wrknv.toml", env_config=env_config)
        extra_config["include_tool_verification"] = env_config.get("include_tool_verification", True)

        # Handle new unified siblings format
        siblings_config = env_config.get("siblings", [])
        if siblings_config:
            extra_config["siblings"] = siblings_config
            # Keep backward compatibility
            extra_config["sibling_patterns"] = []
            extra_config["special_siblings"] = []
        else:
            # Backward compatibility with old format
            extra_config["sibling_patterns"] = env_config.get("sibling_patterns", [])
            extra_config["special_siblings"] = env_config.get("special_siblings", [])
            extra_config["siblings"] = []
    else:
        # Default configuration - empty by default
        logger.info("No env configuration found in wrknv.toml, using defaults")
        extra_config["include_tool_verification"] = True
        extra_config["sibling_patterns"] = []
        extra_config["special_siblings"] = []
        extra_config["siblings"] = []

    # Generate scripts
    generator = EnvScriptGenerator()
    sh_path, ps1_path = generator.generate_both_scripts(project_name, project_dir, **extra_config)

    print_success(f"Generated {sh_path}", Emoji.SUCCESS)
    print_success(f"Generated {ps1_path}", Emoji.SUCCESS)

    return sh_path, ps1_path


def x_create_project_env_scripts__mutmut_30(project_dir: Path, workenv_name: str | None = None) -> tuple[Path, Path]:
    """Create environment scripts for a project based on its pyproject.toml.

    Args:
        project_dir: The project directory
        workenv_name: Optional workenv name override

    Returns:
        Tuple of (env.sh path, env.ps1 path)
    """
    pyproject_path = project_dir / "pyproject.toml"

    if not pyproject_path.exists():
        raise FileNotFoundError(f"No pyproject.toml found in {project_dir}")

    # Parse project name from pyproject.toml
    from provide.foundation.file.formats import read_toml

    pyproject = read_toml(pyproject_path)

    project_name = pyproject.get("project", {}).get("name", project_dir.name)
    python_requirement = pyproject.get("project", {}).get("XXrequires-pythonXX")

    # Additional config for wrknv projects
    extra_config = {}
    if workenv_name:
        extra_config["workenv_name"] = workenv_name
        extra_config["venv_prefix"] = workenv_name

    # Add Python requirement if present
    if python_requirement:
        extra_config["python_requirement"] = python_requirement

    # Get wrknv configuration
    from wrknv.config import WorkenvConfig

    config = WorkenvConfig.load()
    env_config = config.get_env_config()

    # Use configuration from wrknv.toml if available
    if env_config:
        logger.info("Using env configuration from wrknv.toml", env_config=env_config)
        extra_config["include_tool_verification"] = env_config.get("include_tool_verification", True)

        # Handle new unified siblings format
        siblings_config = env_config.get("siblings", [])
        if siblings_config:
            extra_config["siblings"] = siblings_config
            # Keep backward compatibility
            extra_config["sibling_patterns"] = []
            extra_config["special_siblings"] = []
        else:
            # Backward compatibility with old format
            extra_config["sibling_patterns"] = env_config.get("sibling_patterns", [])
            extra_config["special_siblings"] = env_config.get("special_siblings", [])
            extra_config["siblings"] = []
    else:
        # Default configuration - empty by default
        logger.info("No env configuration found in wrknv.toml, using defaults")
        extra_config["include_tool_verification"] = True
        extra_config["sibling_patterns"] = []
        extra_config["special_siblings"] = []
        extra_config["siblings"] = []

    # Generate scripts
    generator = EnvScriptGenerator()
    sh_path, ps1_path = generator.generate_both_scripts(project_name, project_dir, **extra_config)

    print_success(f"Generated {sh_path}", Emoji.SUCCESS)
    print_success(f"Generated {ps1_path}", Emoji.SUCCESS)

    return sh_path, ps1_path


def x_create_project_env_scripts__mutmut_31(project_dir: Path, workenv_name: str | None = None) -> tuple[Path, Path]:
    """Create environment scripts for a project based on its pyproject.toml.

    Args:
        project_dir: The project directory
        workenv_name: Optional workenv name override

    Returns:
        Tuple of (env.sh path, env.ps1 path)
    """
    pyproject_path = project_dir / "pyproject.toml"

    if not pyproject_path.exists():
        raise FileNotFoundError(f"No pyproject.toml found in {project_dir}")

    # Parse project name from pyproject.toml
    from provide.foundation.file.formats import read_toml

    pyproject = read_toml(pyproject_path)

    project_name = pyproject.get("project", {}).get("name", project_dir.name)
    python_requirement = pyproject.get("project", {}).get("REQUIRES-PYTHON")

    # Additional config for wrknv projects
    extra_config = {}
    if workenv_name:
        extra_config["workenv_name"] = workenv_name
        extra_config["venv_prefix"] = workenv_name

    # Add Python requirement if present
    if python_requirement:
        extra_config["python_requirement"] = python_requirement

    # Get wrknv configuration
    from wrknv.config import WorkenvConfig

    config = WorkenvConfig.load()
    env_config = config.get_env_config()

    # Use configuration from wrknv.toml if available
    if env_config:
        logger.info("Using env configuration from wrknv.toml", env_config=env_config)
        extra_config["include_tool_verification"] = env_config.get("include_tool_verification", True)

        # Handle new unified siblings format
        siblings_config = env_config.get("siblings", [])
        if siblings_config:
            extra_config["siblings"] = siblings_config
            # Keep backward compatibility
            extra_config["sibling_patterns"] = []
            extra_config["special_siblings"] = []
        else:
            # Backward compatibility with old format
            extra_config["sibling_patterns"] = env_config.get("sibling_patterns", [])
            extra_config["special_siblings"] = env_config.get("special_siblings", [])
            extra_config["siblings"] = []
    else:
        # Default configuration - empty by default
        logger.info("No env configuration found in wrknv.toml, using defaults")
        extra_config["include_tool_verification"] = True
        extra_config["sibling_patterns"] = []
        extra_config["special_siblings"] = []
        extra_config["siblings"] = []

    # Generate scripts
    generator = EnvScriptGenerator()
    sh_path, ps1_path = generator.generate_both_scripts(project_name, project_dir, **extra_config)

    print_success(f"Generated {sh_path}", Emoji.SUCCESS)
    print_success(f"Generated {ps1_path}", Emoji.SUCCESS)

    return sh_path, ps1_path


def x_create_project_env_scripts__mutmut_32(project_dir: Path, workenv_name: str | None = None) -> tuple[Path, Path]:
    """Create environment scripts for a project based on its pyproject.toml.

    Args:
        project_dir: The project directory
        workenv_name: Optional workenv name override

    Returns:
        Tuple of (env.sh path, env.ps1 path)
    """
    pyproject_path = project_dir / "pyproject.toml"

    if not pyproject_path.exists():
        raise FileNotFoundError(f"No pyproject.toml found in {project_dir}")

    # Parse project name from pyproject.toml
    from provide.foundation.file.formats import read_toml

    pyproject = read_toml(pyproject_path)

    project_name = pyproject.get("project", {}).get("name", project_dir.name)
    python_requirement = pyproject.get("project", {}).get("requires-python")

    # Additional config for wrknv projects
    extra_config = None
    if workenv_name:
        extra_config["workenv_name"] = workenv_name
        extra_config["venv_prefix"] = workenv_name

    # Add Python requirement if present
    if python_requirement:
        extra_config["python_requirement"] = python_requirement

    # Get wrknv configuration
    from wrknv.config import WorkenvConfig

    config = WorkenvConfig.load()
    env_config = config.get_env_config()

    # Use configuration from wrknv.toml if available
    if env_config:
        logger.info("Using env configuration from wrknv.toml", env_config=env_config)
        extra_config["include_tool_verification"] = env_config.get("include_tool_verification", True)

        # Handle new unified siblings format
        siblings_config = env_config.get("siblings", [])
        if siblings_config:
            extra_config["siblings"] = siblings_config
            # Keep backward compatibility
            extra_config["sibling_patterns"] = []
            extra_config["special_siblings"] = []
        else:
            # Backward compatibility with old format
            extra_config["sibling_patterns"] = env_config.get("sibling_patterns", [])
            extra_config["special_siblings"] = env_config.get("special_siblings", [])
            extra_config["siblings"] = []
    else:
        # Default configuration - empty by default
        logger.info("No env configuration found in wrknv.toml, using defaults")
        extra_config["include_tool_verification"] = True
        extra_config["sibling_patterns"] = []
        extra_config["special_siblings"] = []
        extra_config["siblings"] = []

    # Generate scripts
    generator = EnvScriptGenerator()
    sh_path, ps1_path = generator.generate_both_scripts(project_name, project_dir, **extra_config)

    print_success(f"Generated {sh_path}", Emoji.SUCCESS)
    print_success(f"Generated {ps1_path}", Emoji.SUCCESS)

    return sh_path, ps1_path


def x_create_project_env_scripts__mutmut_33(project_dir: Path, workenv_name: str | None = None) -> tuple[Path, Path]:
    """Create environment scripts for a project based on its pyproject.toml.

    Args:
        project_dir: The project directory
        workenv_name: Optional workenv name override

    Returns:
        Tuple of (env.sh path, env.ps1 path)
    """
    pyproject_path = project_dir / "pyproject.toml"

    if not pyproject_path.exists():
        raise FileNotFoundError(f"No pyproject.toml found in {project_dir}")

    # Parse project name from pyproject.toml
    from provide.foundation.file.formats import read_toml

    pyproject = read_toml(pyproject_path)

    project_name = pyproject.get("project", {}).get("name", project_dir.name)
    python_requirement = pyproject.get("project", {}).get("requires-python")

    # Additional config for wrknv projects
    extra_config = {}
    if workenv_name:
        extra_config["workenv_name"] = None
        extra_config["venv_prefix"] = workenv_name

    # Add Python requirement if present
    if python_requirement:
        extra_config["python_requirement"] = python_requirement

    # Get wrknv configuration
    from wrknv.config import WorkenvConfig

    config = WorkenvConfig.load()
    env_config = config.get_env_config()

    # Use configuration from wrknv.toml if available
    if env_config:
        logger.info("Using env configuration from wrknv.toml", env_config=env_config)
        extra_config["include_tool_verification"] = env_config.get("include_tool_verification", True)

        # Handle new unified siblings format
        siblings_config = env_config.get("siblings", [])
        if siblings_config:
            extra_config["siblings"] = siblings_config
            # Keep backward compatibility
            extra_config["sibling_patterns"] = []
            extra_config["special_siblings"] = []
        else:
            # Backward compatibility with old format
            extra_config["sibling_patterns"] = env_config.get("sibling_patterns", [])
            extra_config["special_siblings"] = env_config.get("special_siblings", [])
            extra_config["siblings"] = []
    else:
        # Default configuration - empty by default
        logger.info("No env configuration found in wrknv.toml, using defaults")
        extra_config["include_tool_verification"] = True
        extra_config["sibling_patterns"] = []
        extra_config["special_siblings"] = []
        extra_config["siblings"] = []

    # Generate scripts
    generator = EnvScriptGenerator()
    sh_path, ps1_path = generator.generate_both_scripts(project_name, project_dir, **extra_config)

    print_success(f"Generated {sh_path}", Emoji.SUCCESS)
    print_success(f"Generated {ps1_path}", Emoji.SUCCESS)

    return sh_path, ps1_path


def x_create_project_env_scripts__mutmut_34(project_dir: Path, workenv_name: str | None = None) -> tuple[Path, Path]:
    """Create environment scripts for a project based on its pyproject.toml.

    Args:
        project_dir: The project directory
        workenv_name: Optional workenv name override

    Returns:
        Tuple of (env.sh path, env.ps1 path)
    """
    pyproject_path = project_dir / "pyproject.toml"

    if not pyproject_path.exists():
        raise FileNotFoundError(f"No pyproject.toml found in {project_dir}")

    # Parse project name from pyproject.toml
    from provide.foundation.file.formats import read_toml

    pyproject = read_toml(pyproject_path)

    project_name = pyproject.get("project", {}).get("name", project_dir.name)
    python_requirement = pyproject.get("project", {}).get("requires-python")

    # Additional config for wrknv projects
    extra_config = {}
    if workenv_name:
        extra_config["XXworkenv_nameXX"] = workenv_name
        extra_config["venv_prefix"] = workenv_name

    # Add Python requirement if present
    if python_requirement:
        extra_config["python_requirement"] = python_requirement

    # Get wrknv configuration
    from wrknv.config import WorkenvConfig

    config = WorkenvConfig.load()
    env_config = config.get_env_config()

    # Use configuration from wrknv.toml if available
    if env_config:
        logger.info("Using env configuration from wrknv.toml", env_config=env_config)
        extra_config["include_tool_verification"] = env_config.get("include_tool_verification", True)

        # Handle new unified siblings format
        siblings_config = env_config.get("siblings", [])
        if siblings_config:
            extra_config["siblings"] = siblings_config
            # Keep backward compatibility
            extra_config["sibling_patterns"] = []
            extra_config["special_siblings"] = []
        else:
            # Backward compatibility with old format
            extra_config["sibling_patterns"] = env_config.get("sibling_patterns", [])
            extra_config["special_siblings"] = env_config.get("special_siblings", [])
            extra_config["siblings"] = []
    else:
        # Default configuration - empty by default
        logger.info("No env configuration found in wrknv.toml, using defaults")
        extra_config["include_tool_verification"] = True
        extra_config["sibling_patterns"] = []
        extra_config["special_siblings"] = []
        extra_config["siblings"] = []

    # Generate scripts
    generator = EnvScriptGenerator()
    sh_path, ps1_path = generator.generate_both_scripts(project_name, project_dir, **extra_config)

    print_success(f"Generated {sh_path}", Emoji.SUCCESS)
    print_success(f"Generated {ps1_path}", Emoji.SUCCESS)

    return sh_path, ps1_path


def x_create_project_env_scripts__mutmut_35(project_dir: Path, workenv_name: str | None = None) -> tuple[Path, Path]:
    """Create environment scripts for a project based on its pyproject.toml.

    Args:
        project_dir: The project directory
        workenv_name: Optional workenv name override

    Returns:
        Tuple of (env.sh path, env.ps1 path)
    """
    pyproject_path = project_dir / "pyproject.toml"

    if not pyproject_path.exists():
        raise FileNotFoundError(f"No pyproject.toml found in {project_dir}")

    # Parse project name from pyproject.toml
    from provide.foundation.file.formats import read_toml

    pyproject = read_toml(pyproject_path)

    project_name = pyproject.get("project", {}).get("name", project_dir.name)
    python_requirement = pyproject.get("project", {}).get("requires-python")

    # Additional config for wrknv projects
    extra_config = {}
    if workenv_name:
        extra_config["WORKENV_NAME"] = workenv_name
        extra_config["venv_prefix"] = workenv_name

    # Add Python requirement if present
    if python_requirement:
        extra_config["python_requirement"] = python_requirement

    # Get wrknv configuration
    from wrknv.config import WorkenvConfig

    config = WorkenvConfig.load()
    env_config = config.get_env_config()

    # Use configuration from wrknv.toml if available
    if env_config:
        logger.info("Using env configuration from wrknv.toml", env_config=env_config)
        extra_config["include_tool_verification"] = env_config.get("include_tool_verification", True)

        # Handle new unified siblings format
        siblings_config = env_config.get("siblings", [])
        if siblings_config:
            extra_config["siblings"] = siblings_config
            # Keep backward compatibility
            extra_config["sibling_patterns"] = []
            extra_config["special_siblings"] = []
        else:
            # Backward compatibility with old format
            extra_config["sibling_patterns"] = env_config.get("sibling_patterns", [])
            extra_config["special_siblings"] = env_config.get("special_siblings", [])
            extra_config["siblings"] = []
    else:
        # Default configuration - empty by default
        logger.info("No env configuration found in wrknv.toml, using defaults")
        extra_config["include_tool_verification"] = True
        extra_config["sibling_patterns"] = []
        extra_config["special_siblings"] = []
        extra_config["siblings"] = []

    # Generate scripts
    generator = EnvScriptGenerator()
    sh_path, ps1_path = generator.generate_both_scripts(project_name, project_dir, **extra_config)

    print_success(f"Generated {sh_path}", Emoji.SUCCESS)
    print_success(f"Generated {ps1_path}", Emoji.SUCCESS)

    return sh_path, ps1_path


def x_create_project_env_scripts__mutmut_36(project_dir: Path, workenv_name: str | None = None) -> tuple[Path, Path]:
    """Create environment scripts for a project based on its pyproject.toml.

    Args:
        project_dir: The project directory
        workenv_name: Optional workenv name override

    Returns:
        Tuple of (env.sh path, env.ps1 path)
    """
    pyproject_path = project_dir / "pyproject.toml"

    if not pyproject_path.exists():
        raise FileNotFoundError(f"No pyproject.toml found in {project_dir}")

    # Parse project name from pyproject.toml
    from provide.foundation.file.formats import read_toml

    pyproject = read_toml(pyproject_path)

    project_name = pyproject.get("project", {}).get("name", project_dir.name)
    python_requirement = pyproject.get("project", {}).get("requires-python")

    # Additional config for wrknv projects
    extra_config = {}
    if workenv_name:
        extra_config["workenv_name"] = workenv_name
        extra_config["venv_prefix"] = None

    # Add Python requirement if present
    if python_requirement:
        extra_config["python_requirement"] = python_requirement

    # Get wrknv configuration
    from wrknv.config import WorkenvConfig

    config = WorkenvConfig.load()
    env_config = config.get_env_config()

    # Use configuration from wrknv.toml if available
    if env_config:
        logger.info("Using env configuration from wrknv.toml", env_config=env_config)
        extra_config["include_tool_verification"] = env_config.get("include_tool_verification", True)

        # Handle new unified siblings format
        siblings_config = env_config.get("siblings", [])
        if siblings_config:
            extra_config["siblings"] = siblings_config
            # Keep backward compatibility
            extra_config["sibling_patterns"] = []
            extra_config["special_siblings"] = []
        else:
            # Backward compatibility with old format
            extra_config["sibling_patterns"] = env_config.get("sibling_patterns", [])
            extra_config["special_siblings"] = env_config.get("special_siblings", [])
            extra_config["siblings"] = []
    else:
        # Default configuration - empty by default
        logger.info("No env configuration found in wrknv.toml, using defaults")
        extra_config["include_tool_verification"] = True
        extra_config["sibling_patterns"] = []
        extra_config["special_siblings"] = []
        extra_config["siblings"] = []

    # Generate scripts
    generator = EnvScriptGenerator()
    sh_path, ps1_path = generator.generate_both_scripts(project_name, project_dir, **extra_config)

    print_success(f"Generated {sh_path}", Emoji.SUCCESS)
    print_success(f"Generated {ps1_path}", Emoji.SUCCESS)

    return sh_path, ps1_path


def x_create_project_env_scripts__mutmut_37(project_dir: Path, workenv_name: str | None = None) -> tuple[Path, Path]:
    """Create environment scripts for a project based on its pyproject.toml.

    Args:
        project_dir: The project directory
        workenv_name: Optional workenv name override

    Returns:
        Tuple of (env.sh path, env.ps1 path)
    """
    pyproject_path = project_dir / "pyproject.toml"

    if not pyproject_path.exists():
        raise FileNotFoundError(f"No pyproject.toml found in {project_dir}")

    # Parse project name from pyproject.toml
    from provide.foundation.file.formats import read_toml

    pyproject = read_toml(pyproject_path)

    project_name = pyproject.get("project", {}).get("name", project_dir.name)
    python_requirement = pyproject.get("project", {}).get("requires-python")

    # Additional config for wrknv projects
    extra_config = {}
    if workenv_name:
        extra_config["workenv_name"] = workenv_name
        extra_config["XXvenv_prefixXX"] = workenv_name

    # Add Python requirement if present
    if python_requirement:
        extra_config["python_requirement"] = python_requirement

    # Get wrknv configuration
    from wrknv.config import WorkenvConfig

    config = WorkenvConfig.load()
    env_config = config.get_env_config()

    # Use configuration from wrknv.toml if available
    if env_config:
        logger.info("Using env configuration from wrknv.toml", env_config=env_config)
        extra_config["include_tool_verification"] = env_config.get("include_tool_verification", True)

        # Handle new unified siblings format
        siblings_config = env_config.get("siblings", [])
        if siblings_config:
            extra_config["siblings"] = siblings_config
            # Keep backward compatibility
            extra_config["sibling_patterns"] = []
            extra_config["special_siblings"] = []
        else:
            # Backward compatibility with old format
            extra_config["sibling_patterns"] = env_config.get("sibling_patterns", [])
            extra_config["special_siblings"] = env_config.get("special_siblings", [])
            extra_config["siblings"] = []
    else:
        # Default configuration - empty by default
        logger.info("No env configuration found in wrknv.toml, using defaults")
        extra_config["include_tool_verification"] = True
        extra_config["sibling_patterns"] = []
        extra_config["special_siblings"] = []
        extra_config["siblings"] = []

    # Generate scripts
    generator = EnvScriptGenerator()
    sh_path, ps1_path = generator.generate_both_scripts(project_name, project_dir, **extra_config)

    print_success(f"Generated {sh_path}", Emoji.SUCCESS)
    print_success(f"Generated {ps1_path}", Emoji.SUCCESS)

    return sh_path, ps1_path


def x_create_project_env_scripts__mutmut_38(project_dir: Path, workenv_name: str | None = None) -> tuple[Path, Path]:
    """Create environment scripts for a project based on its pyproject.toml.

    Args:
        project_dir: The project directory
        workenv_name: Optional workenv name override

    Returns:
        Tuple of (env.sh path, env.ps1 path)
    """
    pyproject_path = project_dir / "pyproject.toml"

    if not pyproject_path.exists():
        raise FileNotFoundError(f"No pyproject.toml found in {project_dir}")

    # Parse project name from pyproject.toml
    from provide.foundation.file.formats import read_toml

    pyproject = read_toml(pyproject_path)

    project_name = pyproject.get("project", {}).get("name", project_dir.name)
    python_requirement = pyproject.get("project", {}).get("requires-python")

    # Additional config for wrknv projects
    extra_config = {}
    if workenv_name:
        extra_config["workenv_name"] = workenv_name
        extra_config["VENV_PREFIX"] = workenv_name

    # Add Python requirement if present
    if python_requirement:
        extra_config["python_requirement"] = python_requirement

    # Get wrknv configuration
    from wrknv.config import WorkenvConfig

    config = WorkenvConfig.load()
    env_config = config.get_env_config()

    # Use configuration from wrknv.toml if available
    if env_config:
        logger.info("Using env configuration from wrknv.toml", env_config=env_config)
        extra_config["include_tool_verification"] = env_config.get("include_tool_verification", True)

        # Handle new unified siblings format
        siblings_config = env_config.get("siblings", [])
        if siblings_config:
            extra_config["siblings"] = siblings_config
            # Keep backward compatibility
            extra_config["sibling_patterns"] = []
            extra_config["special_siblings"] = []
        else:
            # Backward compatibility with old format
            extra_config["sibling_patterns"] = env_config.get("sibling_patterns", [])
            extra_config["special_siblings"] = env_config.get("special_siblings", [])
            extra_config["siblings"] = []
    else:
        # Default configuration - empty by default
        logger.info("No env configuration found in wrknv.toml, using defaults")
        extra_config["include_tool_verification"] = True
        extra_config["sibling_patterns"] = []
        extra_config["special_siblings"] = []
        extra_config["siblings"] = []

    # Generate scripts
    generator = EnvScriptGenerator()
    sh_path, ps1_path = generator.generate_both_scripts(project_name, project_dir, **extra_config)

    print_success(f"Generated {sh_path}", Emoji.SUCCESS)
    print_success(f"Generated {ps1_path}", Emoji.SUCCESS)

    return sh_path, ps1_path


def x_create_project_env_scripts__mutmut_39(project_dir: Path, workenv_name: str | None = None) -> tuple[Path, Path]:
    """Create environment scripts for a project based on its pyproject.toml.

    Args:
        project_dir: The project directory
        workenv_name: Optional workenv name override

    Returns:
        Tuple of (env.sh path, env.ps1 path)
    """
    pyproject_path = project_dir / "pyproject.toml"

    if not pyproject_path.exists():
        raise FileNotFoundError(f"No pyproject.toml found in {project_dir}")

    # Parse project name from pyproject.toml
    from provide.foundation.file.formats import read_toml

    pyproject = read_toml(pyproject_path)

    project_name = pyproject.get("project", {}).get("name", project_dir.name)
    python_requirement = pyproject.get("project", {}).get("requires-python")

    # Additional config for wrknv projects
    extra_config = {}
    if workenv_name:
        extra_config["workenv_name"] = workenv_name
        extra_config["venv_prefix"] = workenv_name

    # Add Python requirement if present
    if python_requirement:
        extra_config["python_requirement"] = None

    # Get wrknv configuration
    from wrknv.config import WorkenvConfig

    config = WorkenvConfig.load()
    env_config = config.get_env_config()

    # Use configuration from wrknv.toml if available
    if env_config:
        logger.info("Using env configuration from wrknv.toml", env_config=env_config)
        extra_config["include_tool_verification"] = env_config.get("include_tool_verification", True)

        # Handle new unified siblings format
        siblings_config = env_config.get("siblings", [])
        if siblings_config:
            extra_config["siblings"] = siblings_config
            # Keep backward compatibility
            extra_config["sibling_patterns"] = []
            extra_config["special_siblings"] = []
        else:
            # Backward compatibility with old format
            extra_config["sibling_patterns"] = env_config.get("sibling_patterns", [])
            extra_config["special_siblings"] = env_config.get("special_siblings", [])
            extra_config["siblings"] = []
    else:
        # Default configuration - empty by default
        logger.info("No env configuration found in wrknv.toml, using defaults")
        extra_config["include_tool_verification"] = True
        extra_config["sibling_patterns"] = []
        extra_config["special_siblings"] = []
        extra_config["siblings"] = []

    # Generate scripts
    generator = EnvScriptGenerator()
    sh_path, ps1_path = generator.generate_both_scripts(project_name, project_dir, **extra_config)

    print_success(f"Generated {sh_path}", Emoji.SUCCESS)
    print_success(f"Generated {ps1_path}", Emoji.SUCCESS)

    return sh_path, ps1_path


def x_create_project_env_scripts__mutmut_40(project_dir: Path, workenv_name: str | None = None) -> tuple[Path, Path]:
    """Create environment scripts for a project based on its pyproject.toml.

    Args:
        project_dir: The project directory
        workenv_name: Optional workenv name override

    Returns:
        Tuple of (env.sh path, env.ps1 path)
    """
    pyproject_path = project_dir / "pyproject.toml"

    if not pyproject_path.exists():
        raise FileNotFoundError(f"No pyproject.toml found in {project_dir}")

    # Parse project name from pyproject.toml
    from provide.foundation.file.formats import read_toml

    pyproject = read_toml(pyproject_path)

    project_name = pyproject.get("project", {}).get("name", project_dir.name)
    python_requirement = pyproject.get("project", {}).get("requires-python")

    # Additional config for wrknv projects
    extra_config = {}
    if workenv_name:
        extra_config["workenv_name"] = workenv_name
        extra_config["venv_prefix"] = workenv_name

    # Add Python requirement if present
    if python_requirement:
        extra_config["XXpython_requirementXX"] = python_requirement

    # Get wrknv configuration
    from wrknv.config import WorkenvConfig

    config = WorkenvConfig.load()
    env_config = config.get_env_config()

    # Use configuration from wrknv.toml if available
    if env_config:
        logger.info("Using env configuration from wrknv.toml", env_config=env_config)
        extra_config["include_tool_verification"] = env_config.get("include_tool_verification", True)

        # Handle new unified siblings format
        siblings_config = env_config.get("siblings", [])
        if siblings_config:
            extra_config["siblings"] = siblings_config
            # Keep backward compatibility
            extra_config["sibling_patterns"] = []
            extra_config["special_siblings"] = []
        else:
            # Backward compatibility with old format
            extra_config["sibling_patterns"] = env_config.get("sibling_patterns", [])
            extra_config["special_siblings"] = env_config.get("special_siblings", [])
            extra_config["siblings"] = []
    else:
        # Default configuration - empty by default
        logger.info("No env configuration found in wrknv.toml, using defaults")
        extra_config["include_tool_verification"] = True
        extra_config["sibling_patterns"] = []
        extra_config["special_siblings"] = []
        extra_config["siblings"] = []

    # Generate scripts
    generator = EnvScriptGenerator()
    sh_path, ps1_path = generator.generate_both_scripts(project_name, project_dir, **extra_config)

    print_success(f"Generated {sh_path}", Emoji.SUCCESS)
    print_success(f"Generated {ps1_path}", Emoji.SUCCESS)

    return sh_path, ps1_path


def x_create_project_env_scripts__mutmut_41(project_dir: Path, workenv_name: str | None = None) -> tuple[Path, Path]:
    """Create environment scripts for a project based on its pyproject.toml.

    Args:
        project_dir: The project directory
        workenv_name: Optional workenv name override

    Returns:
        Tuple of (env.sh path, env.ps1 path)
    """
    pyproject_path = project_dir / "pyproject.toml"

    if not pyproject_path.exists():
        raise FileNotFoundError(f"No pyproject.toml found in {project_dir}")

    # Parse project name from pyproject.toml
    from provide.foundation.file.formats import read_toml

    pyproject = read_toml(pyproject_path)

    project_name = pyproject.get("project", {}).get("name", project_dir.name)
    python_requirement = pyproject.get("project", {}).get("requires-python")

    # Additional config for wrknv projects
    extra_config = {}
    if workenv_name:
        extra_config["workenv_name"] = workenv_name
        extra_config["venv_prefix"] = workenv_name

    # Add Python requirement if present
    if python_requirement:
        extra_config["PYTHON_REQUIREMENT"] = python_requirement

    # Get wrknv configuration
    from wrknv.config import WorkenvConfig

    config = WorkenvConfig.load()
    env_config = config.get_env_config()

    # Use configuration from wrknv.toml if available
    if env_config:
        logger.info("Using env configuration from wrknv.toml", env_config=env_config)
        extra_config["include_tool_verification"] = env_config.get("include_tool_verification", True)

        # Handle new unified siblings format
        siblings_config = env_config.get("siblings", [])
        if siblings_config:
            extra_config["siblings"] = siblings_config
            # Keep backward compatibility
            extra_config["sibling_patterns"] = []
            extra_config["special_siblings"] = []
        else:
            # Backward compatibility with old format
            extra_config["sibling_patterns"] = env_config.get("sibling_patterns", [])
            extra_config["special_siblings"] = env_config.get("special_siblings", [])
            extra_config["siblings"] = []
    else:
        # Default configuration - empty by default
        logger.info("No env configuration found in wrknv.toml, using defaults")
        extra_config["include_tool_verification"] = True
        extra_config["sibling_patterns"] = []
        extra_config["special_siblings"] = []
        extra_config["siblings"] = []

    # Generate scripts
    generator = EnvScriptGenerator()
    sh_path, ps1_path = generator.generate_both_scripts(project_name, project_dir, **extra_config)

    print_success(f"Generated {sh_path}", Emoji.SUCCESS)
    print_success(f"Generated {ps1_path}", Emoji.SUCCESS)

    return sh_path, ps1_path


def x_create_project_env_scripts__mutmut_42(project_dir: Path, workenv_name: str | None = None) -> tuple[Path, Path]:
    """Create environment scripts for a project based on its pyproject.toml.

    Args:
        project_dir: The project directory
        workenv_name: Optional workenv name override

    Returns:
        Tuple of (env.sh path, env.ps1 path)
    """
    pyproject_path = project_dir / "pyproject.toml"

    if not pyproject_path.exists():
        raise FileNotFoundError(f"No pyproject.toml found in {project_dir}")

    # Parse project name from pyproject.toml
    from provide.foundation.file.formats import read_toml

    pyproject = read_toml(pyproject_path)

    project_name = pyproject.get("project", {}).get("name", project_dir.name)
    python_requirement = pyproject.get("project", {}).get("requires-python")

    # Additional config for wrknv projects
    extra_config = {}
    if workenv_name:
        extra_config["workenv_name"] = workenv_name
        extra_config["venv_prefix"] = workenv_name

    # Add Python requirement if present
    if python_requirement:
        extra_config["python_requirement"] = python_requirement

    # Get wrknv configuration
    from wrknv.config import WorkenvConfig

    config = None
    env_config = config.get_env_config()

    # Use configuration from wrknv.toml if available
    if env_config:
        logger.info("Using env configuration from wrknv.toml", env_config=env_config)
        extra_config["include_tool_verification"] = env_config.get("include_tool_verification", True)

        # Handle new unified siblings format
        siblings_config = env_config.get("siblings", [])
        if siblings_config:
            extra_config["siblings"] = siblings_config
            # Keep backward compatibility
            extra_config["sibling_patterns"] = []
            extra_config["special_siblings"] = []
        else:
            # Backward compatibility with old format
            extra_config["sibling_patterns"] = env_config.get("sibling_patterns", [])
            extra_config["special_siblings"] = env_config.get("special_siblings", [])
            extra_config["siblings"] = []
    else:
        # Default configuration - empty by default
        logger.info("No env configuration found in wrknv.toml, using defaults")
        extra_config["include_tool_verification"] = True
        extra_config["sibling_patterns"] = []
        extra_config["special_siblings"] = []
        extra_config["siblings"] = []

    # Generate scripts
    generator = EnvScriptGenerator()
    sh_path, ps1_path = generator.generate_both_scripts(project_name, project_dir, **extra_config)

    print_success(f"Generated {sh_path}", Emoji.SUCCESS)
    print_success(f"Generated {ps1_path}", Emoji.SUCCESS)

    return sh_path, ps1_path


def x_create_project_env_scripts__mutmut_43(project_dir: Path, workenv_name: str | None = None) -> tuple[Path, Path]:
    """Create environment scripts for a project based on its pyproject.toml.

    Args:
        project_dir: The project directory
        workenv_name: Optional workenv name override

    Returns:
        Tuple of (env.sh path, env.ps1 path)
    """
    pyproject_path = project_dir / "pyproject.toml"

    if not pyproject_path.exists():
        raise FileNotFoundError(f"No pyproject.toml found in {project_dir}")

    # Parse project name from pyproject.toml
    from provide.foundation.file.formats import read_toml

    pyproject = read_toml(pyproject_path)

    project_name = pyproject.get("project", {}).get("name", project_dir.name)
    python_requirement = pyproject.get("project", {}).get("requires-python")

    # Additional config for wrknv projects
    extra_config = {}
    if workenv_name:
        extra_config["workenv_name"] = workenv_name
        extra_config["venv_prefix"] = workenv_name

    # Add Python requirement if present
    if python_requirement:
        extra_config["python_requirement"] = python_requirement

    # Get wrknv configuration
    from wrknv.config import WorkenvConfig

    config = WorkenvConfig.load()
    env_config = None

    # Use configuration from wrknv.toml if available
    if env_config:
        logger.info("Using env configuration from wrknv.toml", env_config=env_config)
        extra_config["include_tool_verification"] = env_config.get("include_tool_verification", True)

        # Handle new unified siblings format
        siblings_config = env_config.get("siblings", [])
        if siblings_config:
            extra_config["siblings"] = siblings_config
            # Keep backward compatibility
            extra_config["sibling_patterns"] = []
            extra_config["special_siblings"] = []
        else:
            # Backward compatibility with old format
            extra_config["sibling_patterns"] = env_config.get("sibling_patterns", [])
            extra_config["special_siblings"] = env_config.get("special_siblings", [])
            extra_config["siblings"] = []
    else:
        # Default configuration - empty by default
        logger.info("No env configuration found in wrknv.toml, using defaults")
        extra_config["include_tool_verification"] = True
        extra_config["sibling_patterns"] = []
        extra_config["special_siblings"] = []
        extra_config["siblings"] = []

    # Generate scripts
    generator = EnvScriptGenerator()
    sh_path, ps1_path = generator.generate_both_scripts(project_name, project_dir, **extra_config)

    print_success(f"Generated {sh_path}", Emoji.SUCCESS)
    print_success(f"Generated {ps1_path}", Emoji.SUCCESS)

    return sh_path, ps1_path


def x_create_project_env_scripts__mutmut_44(project_dir: Path, workenv_name: str | None = None) -> tuple[Path, Path]:
    """Create environment scripts for a project based on its pyproject.toml.

    Args:
        project_dir: The project directory
        workenv_name: Optional workenv name override

    Returns:
        Tuple of (env.sh path, env.ps1 path)
    """
    pyproject_path = project_dir / "pyproject.toml"

    if not pyproject_path.exists():
        raise FileNotFoundError(f"No pyproject.toml found in {project_dir}")

    # Parse project name from pyproject.toml
    from provide.foundation.file.formats import read_toml

    pyproject = read_toml(pyproject_path)

    project_name = pyproject.get("project", {}).get("name", project_dir.name)
    python_requirement = pyproject.get("project", {}).get("requires-python")

    # Additional config for wrknv projects
    extra_config = {}
    if workenv_name:
        extra_config["workenv_name"] = workenv_name
        extra_config["venv_prefix"] = workenv_name

    # Add Python requirement if present
    if python_requirement:
        extra_config["python_requirement"] = python_requirement

    # Get wrknv configuration
    from wrknv.config import WorkenvConfig

    config = WorkenvConfig.load()
    env_config = config.get_env_config()

    # Use configuration from wrknv.toml if available
    if env_config:
        logger.info(None, env_config=env_config)
        extra_config["include_tool_verification"] = env_config.get("include_tool_verification", True)

        # Handle new unified siblings format
        siblings_config = env_config.get("siblings", [])
        if siblings_config:
            extra_config["siblings"] = siblings_config
            # Keep backward compatibility
            extra_config["sibling_patterns"] = []
            extra_config["special_siblings"] = []
        else:
            # Backward compatibility with old format
            extra_config["sibling_patterns"] = env_config.get("sibling_patterns", [])
            extra_config["special_siblings"] = env_config.get("special_siblings", [])
            extra_config["siblings"] = []
    else:
        # Default configuration - empty by default
        logger.info("No env configuration found in wrknv.toml, using defaults")
        extra_config["include_tool_verification"] = True
        extra_config["sibling_patterns"] = []
        extra_config["special_siblings"] = []
        extra_config["siblings"] = []

    # Generate scripts
    generator = EnvScriptGenerator()
    sh_path, ps1_path = generator.generate_both_scripts(project_name, project_dir, **extra_config)

    print_success(f"Generated {sh_path}", Emoji.SUCCESS)
    print_success(f"Generated {ps1_path}", Emoji.SUCCESS)

    return sh_path, ps1_path


def x_create_project_env_scripts__mutmut_45(project_dir: Path, workenv_name: str | None = None) -> tuple[Path, Path]:
    """Create environment scripts for a project based on its pyproject.toml.

    Args:
        project_dir: The project directory
        workenv_name: Optional workenv name override

    Returns:
        Tuple of (env.sh path, env.ps1 path)
    """
    pyproject_path = project_dir / "pyproject.toml"

    if not pyproject_path.exists():
        raise FileNotFoundError(f"No pyproject.toml found in {project_dir}")

    # Parse project name from pyproject.toml
    from provide.foundation.file.formats import read_toml

    pyproject = read_toml(pyproject_path)

    project_name = pyproject.get("project", {}).get("name", project_dir.name)
    python_requirement = pyproject.get("project", {}).get("requires-python")

    # Additional config for wrknv projects
    extra_config = {}
    if workenv_name:
        extra_config["workenv_name"] = workenv_name
        extra_config["venv_prefix"] = workenv_name

    # Add Python requirement if present
    if python_requirement:
        extra_config["python_requirement"] = python_requirement

    # Get wrknv configuration
    from wrknv.config import WorkenvConfig

    config = WorkenvConfig.load()
    env_config = config.get_env_config()

    # Use configuration from wrknv.toml if available
    if env_config:
        logger.info("Using env configuration from wrknv.toml", env_config=None)
        extra_config["include_tool_verification"] = env_config.get("include_tool_verification", True)

        # Handle new unified siblings format
        siblings_config = env_config.get("siblings", [])
        if siblings_config:
            extra_config["siblings"] = siblings_config
            # Keep backward compatibility
            extra_config["sibling_patterns"] = []
            extra_config["special_siblings"] = []
        else:
            # Backward compatibility with old format
            extra_config["sibling_patterns"] = env_config.get("sibling_patterns", [])
            extra_config["special_siblings"] = env_config.get("special_siblings", [])
            extra_config["siblings"] = []
    else:
        # Default configuration - empty by default
        logger.info("No env configuration found in wrknv.toml, using defaults")
        extra_config["include_tool_verification"] = True
        extra_config["sibling_patterns"] = []
        extra_config["special_siblings"] = []
        extra_config["siblings"] = []

    # Generate scripts
    generator = EnvScriptGenerator()
    sh_path, ps1_path = generator.generate_both_scripts(project_name, project_dir, **extra_config)

    print_success(f"Generated {sh_path}", Emoji.SUCCESS)
    print_success(f"Generated {ps1_path}", Emoji.SUCCESS)

    return sh_path, ps1_path


def x_create_project_env_scripts__mutmut_46(project_dir: Path, workenv_name: str | None = None) -> tuple[Path, Path]:
    """Create environment scripts for a project based on its pyproject.toml.

    Args:
        project_dir: The project directory
        workenv_name: Optional workenv name override

    Returns:
        Tuple of (env.sh path, env.ps1 path)
    """
    pyproject_path = project_dir / "pyproject.toml"

    if not pyproject_path.exists():
        raise FileNotFoundError(f"No pyproject.toml found in {project_dir}")

    # Parse project name from pyproject.toml
    from provide.foundation.file.formats import read_toml

    pyproject = read_toml(pyproject_path)

    project_name = pyproject.get("project", {}).get("name", project_dir.name)
    python_requirement = pyproject.get("project", {}).get("requires-python")

    # Additional config for wrknv projects
    extra_config = {}
    if workenv_name:
        extra_config["workenv_name"] = workenv_name
        extra_config["venv_prefix"] = workenv_name

    # Add Python requirement if present
    if python_requirement:
        extra_config["python_requirement"] = python_requirement

    # Get wrknv configuration
    from wrknv.config import WorkenvConfig

    config = WorkenvConfig.load()
    env_config = config.get_env_config()

    # Use configuration from wrknv.toml if available
    if env_config:
        logger.info(env_config=env_config)
        extra_config["include_tool_verification"] = env_config.get("include_tool_verification", True)

        # Handle new unified siblings format
        siblings_config = env_config.get("siblings", [])
        if siblings_config:
            extra_config["siblings"] = siblings_config
            # Keep backward compatibility
            extra_config["sibling_patterns"] = []
            extra_config["special_siblings"] = []
        else:
            # Backward compatibility with old format
            extra_config["sibling_patterns"] = env_config.get("sibling_patterns", [])
            extra_config["special_siblings"] = env_config.get("special_siblings", [])
            extra_config["siblings"] = []
    else:
        # Default configuration - empty by default
        logger.info("No env configuration found in wrknv.toml, using defaults")
        extra_config["include_tool_verification"] = True
        extra_config["sibling_patterns"] = []
        extra_config["special_siblings"] = []
        extra_config["siblings"] = []

    # Generate scripts
    generator = EnvScriptGenerator()
    sh_path, ps1_path = generator.generate_both_scripts(project_name, project_dir, **extra_config)

    print_success(f"Generated {sh_path}", Emoji.SUCCESS)
    print_success(f"Generated {ps1_path}", Emoji.SUCCESS)

    return sh_path, ps1_path


def x_create_project_env_scripts__mutmut_47(project_dir: Path, workenv_name: str | None = None) -> tuple[Path, Path]:
    """Create environment scripts for a project based on its pyproject.toml.

    Args:
        project_dir: The project directory
        workenv_name: Optional workenv name override

    Returns:
        Tuple of (env.sh path, env.ps1 path)
    """
    pyproject_path = project_dir / "pyproject.toml"

    if not pyproject_path.exists():
        raise FileNotFoundError(f"No pyproject.toml found in {project_dir}")

    # Parse project name from pyproject.toml
    from provide.foundation.file.formats import read_toml

    pyproject = read_toml(pyproject_path)

    project_name = pyproject.get("project", {}).get("name", project_dir.name)
    python_requirement = pyproject.get("project", {}).get("requires-python")

    # Additional config for wrknv projects
    extra_config = {}
    if workenv_name:
        extra_config["workenv_name"] = workenv_name
        extra_config["venv_prefix"] = workenv_name

    # Add Python requirement if present
    if python_requirement:
        extra_config["python_requirement"] = python_requirement

    # Get wrknv configuration
    from wrknv.config import WorkenvConfig

    config = WorkenvConfig.load()
    env_config = config.get_env_config()

    # Use configuration from wrknv.toml if available
    if env_config:
        logger.info("Using env configuration from wrknv.toml", )
        extra_config["include_tool_verification"] = env_config.get("include_tool_verification", True)

        # Handle new unified siblings format
        siblings_config = env_config.get("siblings", [])
        if siblings_config:
            extra_config["siblings"] = siblings_config
            # Keep backward compatibility
            extra_config["sibling_patterns"] = []
            extra_config["special_siblings"] = []
        else:
            # Backward compatibility with old format
            extra_config["sibling_patterns"] = env_config.get("sibling_patterns", [])
            extra_config["special_siblings"] = env_config.get("special_siblings", [])
            extra_config["siblings"] = []
    else:
        # Default configuration - empty by default
        logger.info("No env configuration found in wrknv.toml, using defaults")
        extra_config["include_tool_verification"] = True
        extra_config["sibling_patterns"] = []
        extra_config["special_siblings"] = []
        extra_config["siblings"] = []

    # Generate scripts
    generator = EnvScriptGenerator()
    sh_path, ps1_path = generator.generate_both_scripts(project_name, project_dir, **extra_config)

    print_success(f"Generated {sh_path}", Emoji.SUCCESS)
    print_success(f"Generated {ps1_path}", Emoji.SUCCESS)

    return sh_path, ps1_path


def x_create_project_env_scripts__mutmut_48(project_dir: Path, workenv_name: str | None = None) -> tuple[Path, Path]:
    """Create environment scripts for a project based on its pyproject.toml.

    Args:
        project_dir: The project directory
        workenv_name: Optional workenv name override

    Returns:
        Tuple of (env.sh path, env.ps1 path)
    """
    pyproject_path = project_dir / "pyproject.toml"

    if not pyproject_path.exists():
        raise FileNotFoundError(f"No pyproject.toml found in {project_dir}")

    # Parse project name from pyproject.toml
    from provide.foundation.file.formats import read_toml

    pyproject = read_toml(pyproject_path)

    project_name = pyproject.get("project", {}).get("name", project_dir.name)
    python_requirement = pyproject.get("project", {}).get("requires-python")

    # Additional config for wrknv projects
    extra_config = {}
    if workenv_name:
        extra_config["workenv_name"] = workenv_name
        extra_config["venv_prefix"] = workenv_name

    # Add Python requirement if present
    if python_requirement:
        extra_config["python_requirement"] = python_requirement

    # Get wrknv configuration
    from wrknv.config import WorkenvConfig

    config = WorkenvConfig.load()
    env_config = config.get_env_config()

    # Use configuration from wrknv.toml if available
    if env_config:
        logger.info("XXUsing env configuration from wrknv.tomlXX", env_config=env_config)
        extra_config["include_tool_verification"] = env_config.get("include_tool_verification", True)

        # Handle new unified siblings format
        siblings_config = env_config.get("siblings", [])
        if siblings_config:
            extra_config["siblings"] = siblings_config
            # Keep backward compatibility
            extra_config["sibling_patterns"] = []
            extra_config["special_siblings"] = []
        else:
            # Backward compatibility with old format
            extra_config["sibling_patterns"] = env_config.get("sibling_patterns", [])
            extra_config["special_siblings"] = env_config.get("special_siblings", [])
            extra_config["siblings"] = []
    else:
        # Default configuration - empty by default
        logger.info("No env configuration found in wrknv.toml, using defaults")
        extra_config["include_tool_verification"] = True
        extra_config["sibling_patterns"] = []
        extra_config["special_siblings"] = []
        extra_config["siblings"] = []

    # Generate scripts
    generator = EnvScriptGenerator()
    sh_path, ps1_path = generator.generate_both_scripts(project_name, project_dir, **extra_config)

    print_success(f"Generated {sh_path}", Emoji.SUCCESS)
    print_success(f"Generated {ps1_path}", Emoji.SUCCESS)

    return sh_path, ps1_path


def x_create_project_env_scripts__mutmut_49(project_dir: Path, workenv_name: str | None = None) -> tuple[Path, Path]:
    """Create environment scripts for a project based on its pyproject.toml.

    Args:
        project_dir: The project directory
        workenv_name: Optional workenv name override

    Returns:
        Tuple of (env.sh path, env.ps1 path)
    """
    pyproject_path = project_dir / "pyproject.toml"

    if not pyproject_path.exists():
        raise FileNotFoundError(f"No pyproject.toml found in {project_dir}")

    # Parse project name from pyproject.toml
    from provide.foundation.file.formats import read_toml

    pyproject = read_toml(pyproject_path)

    project_name = pyproject.get("project", {}).get("name", project_dir.name)
    python_requirement = pyproject.get("project", {}).get("requires-python")

    # Additional config for wrknv projects
    extra_config = {}
    if workenv_name:
        extra_config["workenv_name"] = workenv_name
        extra_config["venv_prefix"] = workenv_name

    # Add Python requirement if present
    if python_requirement:
        extra_config["python_requirement"] = python_requirement

    # Get wrknv configuration
    from wrknv.config import WorkenvConfig

    config = WorkenvConfig.load()
    env_config = config.get_env_config()

    # Use configuration from wrknv.toml if available
    if env_config:
        logger.info("using env configuration from wrknv.toml", env_config=env_config)
        extra_config["include_tool_verification"] = env_config.get("include_tool_verification", True)

        # Handle new unified siblings format
        siblings_config = env_config.get("siblings", [])
        if siblings_config:
            extra_config["siblings"] = siblings_config
            # Keep backward compatibility
            extra_config["sibling_patterns"] = []
            extra_config["special_siblings"] = []
        else:
            # Backward compatibility with old format
            extra_config["sibling_patterns"] = env_config.get("sibling_patterns", [])
            extra_config["special_siblings"] = env_config.get("special_siblings", [])
            extra_config["siblings"] = []
    else:
        # Default configuration - empty by default
        logger.info("No env configuration found in wrknv.toml, using defaults")
        extra_config["include_tool_verification"] = True
        extra_config["sibling_patterns"] = []
        extra_config["special_siblings"] = []
        extra_config["siblings"] = []

    # Generate scripts
    generator = EnvScriptGenerator()
    sh_path, ps1_path = generator.generate_both_scripts(project_name, project_dir, **extra_config)

    print_success(f"Generated {sh_path}", Emoji.SUCCESS)
    print_success(f"Generated {ps1_path}", Emoji.SUCCESS)

    return sh_path, ps1_path


def x_create_project_env_scripts__mutmut_50(project_dir: Path, workenv_name: str | None = None) -> tuple[Path, Path]:
    """Create environment scripts for a project based on its pyproject.toml.

    Args:
        project_dir: The project directory
        workenv_name: Optional workenv name override

    Returns:
        Tuple of (env.sh path, env.ps1 path)
    """
    pyproject_path = project_dir / "pyproject.toml"

    if not pyproject_path.exists():
        raise FileNotFoundError(f"No pyproject.toml found in {project_dir}")

    # Parse project name from pyproject.toml
    from provide.foundation.file.formats import read_toml

    pyproject = read_toml(pyproject_path)

    project_name = pyproject.get("project", {}).get("name", project_dir.name)
    python_requirement = pyproject.get("project", {}).get("requires-python")

    # Additional config for wrknv projects
    extra_config = {}
    if workenv_name:
        extra_config["workenv_name"] = workenv_name
        extra_config["venv_prefix"] = workenv_name

    # Add Python requirement if present
    if python_requirement:
        extra_config["python_requirement"] = python_requirement

    # Get wrknv configuration
    from wrknv.config import WorkenvConfig

    config = WorkenvConfig.load()
    env_config = config.get_env_config()

    # Use configuration from wrknv.toml if available
    if env_config:
        logger.info("USING ENV CONFIGURATION FROM WRKNV.TOML", env_config=env_config)
        extra_config["include_tool_verification"] = env_config.get("include_tool_verification", True)

        # Handle new unified siblings format
        siblings_config = env_config.get("siblings", [])
        if siblings_config:
            extra_config["siblings"] = siblings_config
            # Keep backward compatibility
            extra_config["sibling_patterns"] = []
            extra_config["special_siblings"] = []
        else:
            # Backward compatibility with old format
            extra_config["sibling_patterns"] = env_config.get("sibling_patterns", [])
            extra_config["special_siblings"] = env_config.get("special_siblings", [])
            extra_config["siblings"] = []
    else:
        # Default configuration - empty by default
        logger.info("No env configuration found in wrknv.toml, using defaults")
        extra_config["include_tool_verification"] = True
        extra_config["sibling_patterns"] = []
        extra_config["special_siblings"] = []
        extra_config["siblings"] = []

    # Generate scripts
    generator = EnvScriptGenerator()
    sh_path, ps1_path = generator.generate_both_scripts(project_name, project_dir, **extra_config)

    print_success(f"Generated {sh_path}", Emoji.SUCCESS)
    print_success(f"Generated {ps1_path}", Emoji.SUCCESS)

    return sh_path, ps1_path


def x_create_project_env_scripts__mutmut_51(project_dir: Path, workenv_name: str | None = None) -> tuple[Path, Path]:
    """Create environment scripts for a project based on its pyproject.toml.

    Args:
        project_dir: The project directory
        workenv_name: Optional workenv name override

    Returns:
        Tuple of (env.sh path, env.ps1 path)
    """
    pyproject_path = project_dir / "pyproject.toml"

    if not pyproject_path.exists():
        raise FileNotFoundError(f"No pyproject.toml found in {project_dir}")

    # Parse project name from pyproject.toml
    from provide.foundation.file.formats import read_toml

    pyproject = read_toml(pyproject_path)

    project_name = pyproject.get("project", {}).get("name", project_dir.name)
    python_requirement = pyproject.get("project", {}).get("requires-python")

    # Additional config for wrknv projects
    extra_config = {}
    if workenv_name:
        extra_config["workenv_name"] = workenv_name
        extra_config["venv_prefix"] = workenv_name

    # Add Python requirement if present
    if python_requirement:
        extra_config["python_requirement"] = python_requirement

    # Get wrknv configuration
    from wrknv.config import WorkenvConfig

    config = WorkenvConfig.load()
    env_config = config.get_env_config()

    # Use configuration from wrknv.toml if available
    if env_config:
        logger.info("Using env configuration from wrknv.toml", env_config=env_config)
        extra_config["include_tool_verification"] = None

        # Handle new unified siblings format
        siblings_config = env_config.get("siblings", [])
        if siblings_config:
            extra_config["siblings"] = siblings_config
            # Keep backward compatibility
            extra_config["sibling_patterns"] = []
            extra_config["special_siblings"] = []
        else:
            # Backward compatibility with old format
            extra_config["sibling_patterns"] = env_config.get("sibling_patterns", [])
            extra_config["special_siblings"] = env_config.get("special_siblings", [])
            extra_config["siblings"] = []
    else:
        # Default configuration - empty by default
        logger.info("No env configuration found in wrknv.toml, using defaults")
        extra_config["include_tool_verification"] = True
        extra_config["sibling_patterns"] = []
        extra_config["special_siblings"] = []
        extra_config["siblings"] = []

    # Generate scripts
    generator = EnvScriptGenerator()
    sh_path, ps1_path = generator.generate_both_scripts(project_name, project_dir, **extra_config)

    print_success(f"Generated {sh_path}", Emoji.SUCCESS)
    print_success(f"Generated {ps1_path}", Emoji.SUCCESS)

    return sh_path, ps1_path


def x_create_project_env_scripts__mutmut_52(project_dir: Path, workenv_name: str | None = None) -> tuple[Path, Path]:
    """Create environment scripts for a project based on its pyproject.toml.

    Args:
        project_dir: The project directory
        workenv_name: Optional workenv name override

    Returns:
        Tuple of (env.sh path, env.ps1 path)
    """
    pyproject_path = project_dir / "pyproject.toml"

    if not pyproject_path.exists():
        raise FileNotFoundError(f"No pyproject.toml found in {project_dir}")

    # Parse project name from pyproject.toml
    from provide.foundation.file.formats import read_toml

    pyproject = read_toml(pyproject_path)

    project_name = pyproject.get("project", {}).get("name", project_dir.name)
    python_requirement = pyproject.get("project", {}).get("requires-python")

    # Additional config for wrknv projects
    extra_config = {}
    if workenv_name:
        extra_config["workenv_name"] = workenv_name
        extra_config["venv_prefix"] = workenv_name

    # Add Python requirement if present
    if python_requirement:
        extra_config["python_requirement"] = python_requirement

    # Get wrknv configuration
    from wrknv.config import WorkenvConfig

    config = WorkenvConfig.load()
    env_config = config.get_env_config()

    # Use configuration from wrknv.toml if available
    if env_config:
        logger.info("Using env configuration from wrknv.toml", env_config=env_config)
        extra_config["XXinclude_tool_verificationXX"] = env_config.get("include_tool_verification", True)

        # Handle new unified siblings format
        siblings_config = env_config.get("siblings", [])
        if siblings_config:
            extra_config["siblings"] = siblings_config
            # Keep backward compatibility
            extra_config["sibling_patterns"] = []
            extra_config["special_siblings"] = []
        else:
            # Backward compatibility with old format
            extra_config["sibling_patterns"] = env_config.get("sibling_patterns", [])
            extra_config["special_siblings"] = env_config.get("special_siblings", [])
            extra_config["siblings"] = []
    else:
        # Default configuration - empty by default
        logger.info("No env configuration found in wrknv.toml, using defaults")
        extra_config["include_tool_verification"] = True
        extra_config["sibling_patterns"] = []
        extra_config["special_siblings"] = []
        extra_config["siblings"] = []

    # Generate scripts
    generator = EnvScriptGenerator()
    sh_path, ps1_path = generator.generate_both_scripts(project_name, project_dir, **extra_config)

    print_success(f"Generated {sh_path}", Emoji.SUCCESS)
    print_success(f"Generated {ps1_path}", Emoji.SUCCESS)

    return sh_path, ps1_path


def x_create_project_env_scripts__mutmut_53(project_dir: Path, workenv_name: str | None = None) -> tuple[Path, Path]:
    """Create environment scripts for a project based on its pyproject.toml.

    Args:
        project_dir: The project directory
        workenv_name: Optional workenv name override

    Returns:
        Tuple of (env.sh path, env.ps1 path)
    """
    pyproject_path = project_dir / "pyproject.toml"

    if not pyproject_path.exists():
        raise FileNotFoundError(f"No pyproject.toml found in {project_dir}")

    # Parse project name from pyproject.toml
    from provide.foundation.file.formats import read_toml

    pyproject = read_toml(pyproject_path)

    project_name = pyproject.get("project", {}).get("name", project_dir.name)
    python_requirement = pyproject.get("project", {}).get("requires-python")

    # Additional config for wrknv projects
    extra_config = {}
    if workenv_name:
        extra_config["workenv_name"] = workenv_name
        extra_config["venv_prefix"] = workenv_name

    # Add Python requirement if present
    if python_requirement:
        extra_config["python_requirement"] = python_requirement

    # Get wrknv configuration
    from wrknv.config import WorkenvConfig

    config = WorkenvConfig.load()
    env_config = config.get_env_config()

    # Use configuration from wrknv.toml if available
    if env_config:
        logger.info("Using env configuration from wrknv.toml", env_config=env_config)
        extra_config["INCLUDE_TOOL_VERIFICATION"] = env_config.get("include_tool_verification", True)

        # Handle new unified siblings format
        siblings_config = env_config.get("siblings", [])
        if siblings_config:
            extra_config["siblings"] = siblings_config
            # Keep backward compatibility
            extra_config["sibling_patterns"] = []
            extra_config["special_siblings"] = []
        else:
            # Backward compatibility with old format
            extra_config["sibling_patterns"] = env_config.get("sibling_patterns", [])
            extra_config["special_siblings"] = env_config.get("special_siblings", [])
            extra_config["siblings"] = []
    else:
        # Default configuration - empty by default
        logger.info("No env configuration found in wrknv.toml, using defaults")
        extra_config["include_tool_verification"] = True
        extra_config["sibling_patterns"] = []
        extra_config["special_siblings"] = []
        extra_config["siblings"] = []

    # Generate scripts
    generator = EnvScriptGenerator()
    sh_path, ps1_path = generator.generate_both_scripts(project_name, project_dir, **extra_config)

    print_success(f"Generated {sh_path}", Emoji.SUCCESS)
    print_success(f"Generated {ps1_path}", Emoji.SUCCESS)

    return sh_path, ps1_path


def x_create_project_env_scripts__mutmut_54(project_dir: Path, workenv_name: str | None = None) -> tuple[Path, Path]:
    """Create environment scripts for a project based on its pyproject.toml.

    Args:
        project_dir: The project directory
        workenv_name: Optional workenv name override

    Returns:
        Tuple of (env.sh path, env.ps1 path)
    """
    pyproject_path = project_dir / "pyproject.toml"

    if not pyproject_path.exists():
        raise FileNotFoundError(f"No pyproject.toml found in {project_dir}")

    # Parse project name from pyproject.toml
    from provide.foundation.file.formats import read_toml

    pyproject = read_toml(pyproject_path)

    project_name = pyproject.get("project", {}).get("name", project_dir.name)
    python_requirement = pyproject.get("project", {}).get("requires-python")

    # Additional config for wrknv projects
    extra_config = {}
    if workenv_name:
        extra_config["workenv_name"] = workenv_name
        extra_config["venv_prefix"] = workenv_name

    # Add Python requirement if present
    if python_requirement:
        extra_config["python_requirement"] = python_requirement

    # Get wrknv configuration
    from wrknv.config import WorkenvConfig

    config = WorkenvConfig.load()
    env_config = config.get_env_config()

    # Use configuration from wrknv.toml if available
    if env_config:
        logger.info("Using env configuration from wrknv.toml", env_config=env_config)
        extra_config["include_tool_verification"] = env_config.get(None, True)

        # Handle new unified siblings format
        siblings_config = env_config.get("siblings", [])
        if siblings_config:
            extra_config["siblings"] = siblings_config
            # Keep backward compatibility
            extra_config["sibling_patterns"] = []
            extra_config["special_siblings"] = []
        else:
            # Backward compatibility with old format
            extra_config["sibling_patterns"] = env_config.get("sibling_patterns", [])
            extra_config["special_siblings"] = env_config.get("special_siblings", [])
            extra_config["siblings"] = []
    else:
        # Default configuration - empty by default
        logger.info("No env configuration found in wrknv.toml, using defaults")
        extra_config["include_tool_verification"] = True
        extra_config["sibling_patterns"] = []
        extra_config["special_siblings"] = []
        extra_config["siblings"] = []

    # Generate scripts
    generator = EnvScriptGenerator()
    sh_path, ps1_path = generator.generate_both_scripts(project_name, project_dir, **extra_config)

    print_success(f"Generated {sh_path}", Emoji.SUCCESS)
    print_success(f"Generated {ps1_path}", Emoji.SUCCESS)

    return sh_path, ps1_path


def x_create_project_env_scripts__mutmut_55(project_dir: Path, workenv_name: str | None = None) -> tuple[Path, Path]:
    """Create environment scripts for a project based on its pyproject.toml.

    Args:
        project_dir: The project directory
        workenv_name: Optional workenv name override

    Returns:
        Tuple of (env.sh path, env.ps1 path)
    """
    pyproject_path = project_dir / "pyproject.toml"

    if not pyproject_path.exists():
        raise FileNotFoundError(f"No pyproject.toml found in {project_dir}")

    # Parse project name from pyproject.toml
    from provide.foundation.file.formats import read_toml

    pyproject = read_toml(pyproject_path)

    project_name = pyproject.get("project", {}).get("name", project_dir.name)
    python_requirement = pyproject.get("project", {}).get("requires-python")

    # Additional config for wrknv projects
    extra_config = {}
    if workenv_name:
        extra_config["workenv_name"] = workenv_name
        extra_config["venv_prefix"] = workenv_name

    # Add Python requirement if present
    if python_requirement:
        extra_config["python_requirement"] = python_requirement

    # Get wrknv configuration
    from wrknv.config import WorkenvConfig

    config = WorkenvConfig.load()
    env_config = config.get_env_config()

    # Use configuration from wrknv.toml if available
    if env_config:
        logger.info("Using env configuration from wrknv.toml", env_config=env_config)
        extra_config["include_tool_verification"] = env_config.get("include_tool_verification", None)

        # Handle new unified siblings format
        siblings_config = env_config.get("siblings", [])
        if siblings_config:
            extra_config["siblings"] = siblings_config
            # Keep backward compatibility
            extra_config["sibling_patterns"] = []
            extra_config["special_siblings"] = []
        else:
            # Backward compatibility with old format
            extra_config["sibling_patterns"] = env_config.get("sibling_patterns", [])
            extra_config["special_siblings"] = env_config.get("special_siblings", [])
            extra_config["siblings"] = []
    else:
        # Default configuration - empty by default
        logger.info("No env configuration found in wrknv.toml, using defaults")
        extra_config["include_tool_verification"] = True
        extra_config["sibling_patterns"] = []
        extra_config["special_siblings"] = []
        extra_config["siblings"] = []

    # Generate scripts
    generator = EnvScriptGenerator()
    sh_path, ps1_path = generator.generate_both_scripts(project_name, project_dir, **extra_config)

    print_success(f"Generated {sh_path}", Emoji.SUCCESS)
    print_success(f"Generated {ps1_path}", Emoji.SUCCESS)

    return sh_path, ps1_path


def x_create_project_env_scripts__mutmut_56(project_dir: Path, workenv_name: str | None = None) -> tuple[Path, Path]:
    """Create environment scripts for a project based on its pyproject.toml.

    Args:
        project_dir: The project directory
        workenv_name: Optional workenv name override

    Returns:
        Tuple of (env.sh path, env.ps1 path)
    """
    pyproject_path = project_dir / "pyproject.toml"

    if not pyproject_path.exists():
        raise FileNotFoundError(f"No pyproject.toml found in {project_dir}")

    # Parse project name from pyproject.toml
    from provide.foundation.file.formats import read_toml

    pyproject = read_toml(pyproject_path)

    project_name = pyproject.get("project", {}).get("name", project_dir.name)
    python_requirement = pyproject.get("project", {}).get("requires-python")

    # Additional config for wrknv projects
    extra_config = {}
    if workenv_name:
        extra_config["workenv_name"] = workenv_name
        extra_config["venv_prefix"] = workenv_name

    # Add Python requirement if present
    if python_requirement:
        extra_config["python_requirement"] = python_requirement

    # Get wrknv configuration
    from wrknv.config import WorkenvConfig

    config = WorkenvConfig.load()
    env_config = config.get_env_config()

    # Use configuration from wrknv.toml if available
    if env_config:
        logger.info("Using env configuration from wrknv.toml", env_config=env_config)
        extra_config["include_tool_verification"] = env_config.get(True)

        # Handle new unified siblings format
        siblings_config = env_config.get("siblings", [])
        if siblings_config:
            extra_config["siblings"] = siblings_config
            # Keep backward compatibility
            extra_config["sibling_patterns"] = []
            extra_config["special_siblings"] = []
        else:
            # Backward compatibility with old format
            extra_config["sibling_patterns"] = env_config.get("sibling_patterns", [])
            extra_config["special_siblings"] = env_config.get("special_siblings", [])
            extra_config["siblings"] = []
    else:
        # Default configuration - empty by default
        logger.info("No env configuration found in wrknv.toml, using defaults")
        extra_config["include_tool_verification"] = True
        extra_config["sibling_patterns"] = []
        extra_config["special_siblings"] = []
        extra_config["siblings"] = []

    # Generate scripts
    generator = EnvScriptGenerator()
    sh_path, ps1_path = generator.generate_both_scripts(project_name, project_dir, **extra_config)

    print_success(f"Generated {sh_path}", Emoji.SUCCESS)
    print_success(f"Generated {ps1_path}", Emoji.SUCCESS)

    return sh_path, ps1_path


def x_create_project_env_scripts__mutmut_57(project_dir: Path, workenv_name: str | None = None) -> tuple[Path, Path]:
    """Create environment scripts for a project based on its pyproject.toml.

    Args:
        project_dir: The project directory
        workenv_name: Optional workenv name override

    Returns:
        Tuple of (env.sh path, env.ps1 path)
    """
    pyproject_path = project_dir / "pyproject.toml"

    if not pyproject_path.exists():
        raise FileNotFoundError(f"No pyproject.toml found in {project_dir}")

    # Parse project name from pyproject.toml
    from provide.foundation.file.formats import read_toml

    pyproject = read_toml(pyproject_path)

    project_name = pyproject.get("project", {}).get("name", project_dir.name)
    python_requirement = pyproject.get("project", {}).get("requires-python")

    # Additional config for wrknv projects
    extra_config = {}
    if workenv_name:
        extra_config["workenv_name"] = workenv_name
        extra_config["venv_prefix"] = workenv_name

    # Add Python requirement if present
    if python_requirement:
        extra_config["python_requirement"] = python_requirement

    # Get wrknv configuration
    from wrknv.config import WorkenvConfig

    config = WorkenvConfig.load()
    env_config = config.get_env_config()

    # Use configuration from wrknv.toml if available
    if env_config:
        logger.info("Using env configuration from wrknv.toml", env_config=env_config)
        extra_config["include_tool_verification"] = env_config.get("include_tool_verification", )

        # Handle new unified siblings format
        siblings_config = env_config.get("siblings", [])
        if siblings_config:
            extra_config["siblings"] = siblings_config
            # Keep backward compatibility
            extra_config["sibling_patterns"] = []
            extra_config["special_siblings"] = []
        else:
            # Backward compatibility with old format
            extra_config["sibling_patterns"] = env_config.get("sibling_patterns", [])
            extra_config["special_siblings"] = env_config.get("special_siblings", [])
            extra_config["siblings"] = []
    else:
        # Default configuration - empty by default
        logger.info("No env configuration found in wrknv.toml, using defaults")
        extra_config["include_tool_verification"] = True
        extra_config["sibling_patterns"] = []
        extra_config["special_siblings"] = []
        extra_config["siblings"] = []

    # Generate scripts
    generator = EnvScriptGenerator()
    sh_path, ps1_path = generator.generate_both_scripts(project_name, project_dir, **extra_config)

    print_success(f"Generated {sh_path}", Emoji.SUCCESS)
    print_success(f"Generated {ps1_path}", Emoji.SUCCESS)

    return sh_path, ps1_path


def x_create_project_env_scripts__mutmut_58(project_dir: Path, workenv_name: str | None = None) -> tuple[Path, Path]:
    """Create environment scripts for a project based on its pyproject.toml.

    Args:
        project_dir: The project directory
        workenv_name: Optional workenv name override

    Returns:
        Tuple of (env.sh path, env.ps1 path)
    """
    pyproject_path = project_dir / "pyproject.toml"

    if not pyproject_path.exists():
        raise FileNotFoundError(f"No pyproject.toml found in {project_dir}")

    # Parse project name from pyproject.toml
    from provide.foundation.file.formats import read_toml

    pyproject = read_toml(pyproject_path)

    project_name = pyproject.get("project", {}).get("name", project_dir.name)
    python_requirement = pyproject.get("project", {}).get("requires-python")

    # Additional config for wrknv projects
    extra_config = {}
    if workenv_name:
        extra_config["workenv_name"] = workenv_name
        extra_config["venv_prefix"] = workenv_name

    # Add Python requirement if present
    if python_requirement:
        extra_config["python_requirement"] = python_requirement

    # Get wrknv configuration
    from wrknv.config import WorkenvConfig

    config = WorkenvConfig.load()
    env_config = config.get_env_config()

    # Use configuration from wrknv.toml if available
    if env_config:
        logger.info("Using env configuration from wrknv.toml", env_config=env_config)
        extra_config["include_tool_verification"] = env_config.get("XXinclude_tool_verificationXX", True)

        # Handle new unified siblings format
        siblings_config = env_config.get("siblings", [])
        if siblings_config:
            extra_config["siblings"] = siblings_config
            # Keep backward compatibility
            extra_config["sibling_patterns"] = []
            extra_config["special_siblings"] = []
        else:
            # Backward compatibility with old format
            extra_config["sibling_patterns"] = env_config.get("sibling_patterns", [])
            extra_config["special_siblings"] = env_config.get("special_siblings", [])
            extra_config["siblings"] = []
    else:
        # Default configuration - empty by default
        logger.info("No env configuration found in wrknv.toml, using defaults")
        extra_config["include_tool_verification"] = True
        extra_config["sibling_patterns"] = []
        extra_config["special_siblings"] = []
        extra_config["siblings"] = []

    # Generate scripts
    generator = EnvScriptGenerator()
    sh_path, ps1_path = generator.generate_both_scripts(project_name, project_dir, **extra_config)

    print_success(f"Generated {sh_path}", Emoji.SUCCESS)
    print_success(f"Generated {ps1_path}", Emoji.SUCCESS)

    return sh_path, ps1_path


def x_create_project_env_scripts__mutmut_59(project_dir: Path, workenv_name: str | None = None) -> tuple[Path, Path]:
    """Create environment scripts for a project based on its pyproject.toml.

    Args:
        project_dir: The project directory
        workenv_name: Optional workenv name override

    Returns:
        Tuple of (env.sh path, env.ps1 path)
    """
    pyproject_path = project_dir / "pyproject.toml"

    if not pyproject_path.exists():
        raise FileNotFoundError(f"No pyproject.toml found in {project_dir}")

    # Parse project name from pyproject.toml
    from provide.foundation.file.formats import read_toml

    pyproject = read_toml(pyproject_path)

    project_name = pyproject.get("project", {}).get("name", project_dir.name)
    python_requirement = pyproject.get("project", {}).get("requires-python")

    # Additional config for wrknv projects
    extra_config = {}
    if workenv_name:
        extra_config["workenv_name"] = workenv_name
        extra_config["venv_prefix"] = workenv_name

    # Add Python requirement if present
    if python_requirement:
        extra_config["python_requirement"] = python_requirement

    # Get wrknv configuration
    from wrknv.config import WorkenvConfig

    config = WorkenvConfig.load()
    env_config = config.get_env_config()

    # Use configuration from wrknv.toml if available
    if env_config:
        logger.info("Using env configuration from wrknv.toml", env_config=env_config)
        extra_config["include_tool_verification"] = env_config.get("INCLUDE_TOOL_VERIFICATION", True)

        # Handle new unified siblings format
        siblings_config = env_config.get("siblings", [])
        if siblings_config:
            extra_config["siblings"] = siblings_config
            # Keep backward compatibility
            extra_config["sibling_patterns"] = []
            extra_config["special_siblings"] = []
        else:
            # Backward compatibility with old format
            extra_config["sibling_patterns"] = env_config.get("sibling_patterns", [])
            extra_config["special_siblings"] = env_config.get("special_siblings", [])
            extra_config["siblings"] = []
    else:
        # Default configuration - empty by default
        logger.info("No env configuration found in wrknv.toml, using defaults")
        extra_config["include_tool_verification"] = True
        extra_config["sibling_patterns"] = []
        extra_config["special_siblings"] = []
        extra_config["siblings"] = []

    # Generate scripts
    generator = EnvScriptGenerator()
    sh_path, ps1_path = generator.generate_both_scripts(project_name, project_dir, **extra_config)

    print_success(f"Generated {sh_path}", Emoji.SUCCESS)
    print_success(f"Generated {ps1_path}", Emoji.SUCCESS)

    return sh_path, ps1_path


def x_create_project_env_scripts__mutmut_60(project_dir: Path, workenv_name: str | None = None) -> tuple[Path, Path]:
    """Create environment scripts for a project based on its pyproject.toml.

    Args:
        project_dir: The project directory
        workenv_name: Optional workenv name override

    Returns:
        Tuple of (env.sh path, env.ps1 path)
    """
    pyproject_path = project_dir / "pyproject.toml"

    if not pyproject_path.exists():
        raise FileNotFoundError(f"No pyproject.toml found in {project_dir}")

    # Parse project name from pyproject.toml
    from provide.foundation.file.formats import read_toml

    pyproject = read_toml(pyproject_path)

    project_name = pyproject.get("project", {}).get("name", project_dir.name)
    python_requirement = pyproject.get("project", {}).get("requires-python")

    # Additional config for wrknv projects
    extra_config = {}
    if workenv_name:
        extra_config["workenv_name"] = workenv_name
        extra_config["venv_prefix"] = workenv_name

    # Add Python requirement if present
    if python_requirement:
        extra_config["python_requirement"] = python_requirement

    # Get wrknv configuration
    from wrknv.config import WorkenvConfig

    config = WorkenvConfig.load()
    env_config = config.get_env_config()

    # Use configuration from wrknv.toml if available
    if env_config:
        logger.info("Using env configuration from wrknv.toml", env_config=env_config)
        extra_config["include_tool_verification"] = env_config.get("include_tool_verification", False)

        # Handle new unified siblings format
        siblings_config = env_config.get("siblings", [])
        if siblings_config:
            extra_config["siblings"] = siblings_config
            # Keep backward compatibility
            extra_config["sibling_patterns"] = []
            extra_config["special_siblings"] = []
        else:
            # Backward compatibility with old format
            extra_config["sibling_patterns"] = env_config.get("sibling_patterns", [])
            extra_config["special_siblings"] = env_config.get("special_siblings", [])
            extra_config["siblings"] = []
    else:
        # Default configuration - empty by default
        logger.info("No env configuration found in wrknv.toml, using defaults")
        extra_config["include_tool_verification"] = True
        extra_config["sibling_patterns"] = []
        extra_config["special_siblings"] = []
        extra_config["siblings"] = []

    # Generate scripts
    generator = EnvScriptGenerator()
    sh_path, ps1_path = generator.generate_both_scripts(project_name, project_dir, **extra_config)

    print_success(f"Generated {sh_path}", Emoji.SUCCESS)
    print_success(f"Generated {ps1_path}", Emoji.SUCCESS)

    return sh_path, ps1_path


def x_create_project_env_scripts__mutmut_61(project_dir: Path, workenv_name: str | None = None) -> tuple[Path, Path]:
    """Create environment scripts for a project based on its pyproject.toml.

    Args:
        project_dir: The project directory
        workenv_name: Optional workenv name override

    Returns:
        Tuple of (env.sh path, env.ps1 path)
    """
    pyproject_path = project_dir / "pyproject.toml"

    if not pyproject_path.exists():
        raise FileNotFoundError(f"No pyproject.toml found in {project_dir}")

    # Parse project name from pyproject.toml
    from provide.foundation.file.formats import read_toml

    pyproject = read_toml(pyproject_path)

    project_name = pyproject.get("project", {}).get("name", project_dir.name)
    python_requirement = pyproject.get("project", {}).get("requires-python")

    # Additional config for wrknv projects
    extra_config = {}
    if workenv_name:
        extra_config["workenv_name"] = workenv_name
        extra_config["venv_prefix"] = workenv_name

    # Add Python requirement if present
    if python_requirement:
        extra_config["python_requirement"] = python_requirement

    # Get wrknv configuration
    from wrknv.config import WorkenvConfig

    config = WorkenvConfig.load()
    env_config = config.get_env_config()

    # Use configuration from wrknv.toml if available
    if env_config:
        logger.info("Using env configuration from wrknv.toml", env_config=env_config)
        extra_config["include_tool_verification"] = env_config.get("include_tool_verification", True)

        # Handle new unified siblings format
        siblings_config = None
        if siblings_config:
            extra_config["siblings"] = siblings_config
            # Keep backward compatibility
            extra_config["sibling_patterns"] = []
            extra_config["special_siblings"] = []
        else:
            # Backward compatibility with old format
            extra_config["sibling_patterns"] = env_config.get("sibling_patterns", [])
            extra_config["special_siblings"] = env_config.get("special_siblings", [])
            extra_config["siblings"] = []
    else:
        # Default configuration - empty by default
        logger.info("No env configuration found in wrknv.toml, using defaults")
        extra_config["include_tool_verification"] = True
        extra_config["sibling_patterns"] = []
        extra_config["special_siblings"] = []
        extra_config["siblings"] = []

    # Generate scripts
    generator = EnvScriptGenerator()
    sh_path, ps1_path = generator.generate_both_scripts(project_name, project_dir, **extra_config)

    print_success(f"Generated {sh_path}", Emoji.SUCCESS)
    print_success(f"Generated {ps1_path}", Emoji.SUCCESS)

    return sh_path, ps1_path


def x_create_project_env_scripts__mutmut_62(project_dir: Path, workenv_name: str | None = None) -> tuple[Path, Path]:
    """Create environment scripts for a project based on its pyproject.toml.

    Args:
        project_dir: The project directory
        workenv_name: Optional workenv name override

    Returns:
        Tuple of (env.sh path, env.ps1 path)
    """
    pyproject_path = project_dir / "pyproject.toml"

    if not pyproject_path.exists():
        raise FileNotFoundError(f"No pyproject.toml found in {project_dir}")

    # Parse project name from pyproject.toml
    from provide.foundation.file.formats import read_toml

    pyproject = read_toml(pyproject_path)

    project_name = pyproject.get("project", {}).get("name", project_dir.name)
    python_requirement = pyproject.get("project", {}).get("requires-python")

    # Additional config for wrknv projects
    extra_config = {}
    if workenv_name:
        extra_config["workenv_name"] = workenv_name
        extra_config["venv_prefix"] = workenv_name

    # Add Python requirement if present
    if python_requirement:
        extra_config["python_requirement"] = python_requirement

    # Get wrknv configuration
    from wrknv.config import WorkenvConfig

    config = WorkenvConfig.load()
    env_config = config.get_env_config()

    # Use configuration from wrknv.toml if available
    if env_config:
        logger.info("Using env configuration from wrknv.toml", env_config=env_config)
        extra_config["include_tool_verification"] = env_config.get("include_tool_verification", True)

        # Handle new unified siblings format
        siblings_config = env_config.get(None, [])
        if siblings_config:
            extra_config["siblings"] = siblings_config
            # Keep backward compatibility
            extra_config["sibling_patterns"] = []
            extra_config["special_siblings"] = []
        else:
            # Backward compatibility with old format
            extra_config["sibling_patterns"] = env_config.get("sibling_patterns", [])
            extra_config["special_siblings"] = env_config.get("special_siblings", [])
            extra_config["siblings"] = []
    else:
        # Default configuration - empty by default
        logger.info("No env configuration found in wrknv.toml, using defaults")
        extra_config["include_tool_verification"] = True
        extra_config["sibling_patterns"] = []
        extra_config["special_siblings"] = []
        extra_config["siblings"] = []

    # Generate scripts
    generator = EnvScriptGenerator()
    sh_path, ps1_path = generator.generate_both_scripts(project_name, project_dir, **extra_config)

    print_success(f"Generated {sh_path}", Emoji.SUCCESS)
    print_success(f"Generated {ps1_path}", Emoji.SUCCESS)

    return sh_path, ps1_path


def x_create_project_env_scripts__mutmut_63(project_dir: Path, workenv_name: str | None = None) -> tuple[Path, Path]:
    """Create environment scripts for a project based on its pyproject.toml.

    Args:
        project_dir: The project directory
        workenv_name: Optional workenv name override

    Returns:
        Tuple of (env.sh path, env.ps1 path)
    """
    pyproject_path = project_dir / "pyproject.toml"

    if not pyproject_path.exists():
        raise FileNotFoundError(f"No pyproject.toml found in {project_dir}")

    # Parse project name from pyproject.toml
    from provide.foundation.file.formats import read_toml

    pyproject = read_toml(pyproject_path)

    project_name = pyproject.get("project", {}).get("name", project_dir.name)
    python_requirement = pyproject.get("project", {}).get("requires-python")

    # Additional config for wrknv projects
    extra_config = {}
    if workenv_name:
        extra_config["workenv_name"] = workenv_name
        extra_config["venv_prefix"] = workenv_name

    # Add Python requirement if present
    if python_requirement:
        extra_config["python_requirement"] = python_requirement

    # Get wrknv configuration
    from wrknv.config import WorkenvConfig

    config = WorkenvConfig.load()
    env_config = config.get_env_config()

    # Use configuration from wrknv.toml if available
    if env_config:
        logger.info("Using env configuration from wrknv.toml", env_config=env_config)
        extra_config["include_tool_verification"] = env_config.get("include_tool_verification", True)

        # Handle new unified siblings format
        siblings_config = env_config.get("siblings", None)
        if siblings_config:
            extra_config["siblings"] = siblings_config
            # Keep backward compatibility
            extra_config["sibling_patterns"] = []
            extra_config["special_siblings"] = []
        else:
            # Backward compatibility with old format
            extra_config["sibling_patterns"] = env_config.get("sibling_patterns", [])
            extra_config["special_siblings"] = env_config.get("special_siblings", [])
            extra_config["siblings"] = []
    else:
        # Default configuration - empty by default
        logger.info("No env configuration found in wrknv.toml, using defaults")
        extra_config["include_tool_verification"] = True
        extra_config["sibling_patterns"] = []
        extra_config["special_siblings"] = []
        extra_config["siblings"] = []

    # Generate scripts
    generator = EnvScriptGenerator()
    sh_path, ps1_path = generator.generate_both_scripts(project_name, project_dir, **extra_config)

    print_success(f"Generated {sh_path}", Emoji.SUCCESS)
    print_success(f"Generated {ps1_path}", Emoji.SUCCESS)

    return sh_path, ps1_path


def x_create_project_env_scripts__mutmut_64(project_dir: Path, workenv_name: str | None = None) -> tuple[Path, Path]:
    """Create environment scripts for a project based on its pyproject.toml.

    Args:
        project_dir: The project directory
        workenv_name: Optional workenv name override

    Returns:
        Tuple of (env.sh path, env.ps1 path)
    """
    pyproject_path = project_dir / "pyproject.toml"

    if not pyproject_path.exists():
        raise FileNotFoundError(f"No pyproject.toml found in {project_dir}")

    # Parse project name from pyproject.toml
    from provide.foundation.file.formats import read_toml

    pyproject = read_toml(pyproject_path)

    project_name = pyproject.get("project", {}).get("name", project_dir.name)
    python_requirement = pyproject.get("project", {}).get("requires-python")

    # Additional config for wrknv projects
    extra_config = {}
    if workenv_name:
        extra_config["workenv_name"] = workenv_name
        extra_config["venv_prefix"] = workenv_name

    # Add Python requirement if present
    if python_requirement:
        extra_config["python_requirement"] = python_requirement

    # Get wrknv configuration
    from wrknv.config import WorkenvConfig

    config = WorkenvConfig.load()
    env_config = config.get_env_config()

    # Use configuration from wrknv.toml if available
    if env_config:
        logger.info("Using env configuration from wrknv.toml", env_config=env_config)
        extra_config["include_tool_verification"] = env_config.get("include_tool_verification", True)

        # Handle new unified siblings format
        siblings_config = env_config.get([])
        if siblings_config:
            extra_config["siblings"] = siblings_config
            # Keep backward compatibility
            extra_config["sibling_patterns"] = []
            extra_config["special_siblings"] = []
        else:
            # Backward compatibility with old format
            extra_config["sibling_patterns"] = env_config.get("sibling_patterns", [])
            extra_config["special_siblings"] = env_config.get("special_siblings", [])
            extra_config["siblings"] = []
    else:
        # Default configuration - empty by default
        logger.info("No env configuration found in wrknv.toml, using defaults")
        extra_config["include_tool_verification"] = True
        extra_config["sibling_patterns"] = []
        extra_config["special_siblings"] = []
        extra_config["siblings"] = []

    # Generate scripts
    generator = EnvScriptGenerator()
    sh_path, ps1_path = generator.generate_both_scripts(project_name, project_dir, **extra_config)

    print_success(f"Generated {sh_path}", Emoji.SUCCESS)
    print_success(f"Generated {ps1_path}", Emoji.SUCCESS)

    return sh_path, ps1_path


def x_create_project_env_scripts__mutmut_65(project_dir: Path, workenv_name: str | None = None) -> tuple[Path, Path]:
    """Create environment scripts for a project based on its pyproject.toml.

    Args:
        project_dir: The project directory
        workenv_name: Optional workenv name override

    Returns:
        Tuple of (env.sh path, env.ps1 path)
    """
    pyproject_path = project_dir / "pyproject.toml"

    if not pyproject_path.exists():
        raise FileNotFoundError(f"No pyproject.toml found in {project_dir}")

    # Parse project name from pyproject.toml
    from provide.foundation.file.formats import read_toml

    pyproject = read_toml(pyproject_path)

    project_name = pyproject.get("project", {}).get("name", project_dir.name)
    python_requirement = pyproject.get("project", {}).get("requires-python")

    # Additional config for wrknv projects
    extra_config = {}
    if workenv_name:
        extra_config["workenv_name"] = workenv_name
        extra_config["venv_prefix"] = workenv_name

    # Add Python requirement if present
    if python_requirement:
        extra_config["python_requirement"] = python_requirement

    # Get wrknv configuration
    from wrknv.config import WorkenvConfig

    config = WorkenvConfig.load()
    env_config = config.get_env_config()

    # Use configuration from wrknv.toml if available
    if env_config:
        logger.info("Using env configuration from wrknv.toml", env_config=env_config)
        extra_config["include_tool_verification"] = env_config.get("include_tool_verification", True)

        # Handle new unified siblings format
        siblings_config = env_config.get("siblings", )
        if siblings_config:
            extra_config["siblings"] = siblings_config
            # Keep backward compatibility
            extra_config["sibling_patterns"] = []
            extra_config["special_siblings"] = []
        else:
            # Backward compatibility with old format
            extra_config["sibling_patterns"] = env_config.get("sibling_patterns", [])
            extra_config["special_siblings"] = env_config.get("special_siblings", [])
            extra_config["siblings"] = []
    else:
        # Default configuration - empty by default
        logger.info("No env configuration found in wrknv.toml, using defaults")
        extra_config["include_tool_verification"] = True
        extra_config["sibling_patterns"] = []
        extra_config["special_siblings"] = []
        extra_config["siblings"] = []

    # Generate scripts
    generator = EnvScriptGenerator()
    sh_path, ps1_path = generator.generate_both_scripts(project_name, project_dir, **extra_config)

    print_success(f"Generated {sh_path}", Emoji.SUCCESS)
    print_success(f"Generated {ps1_path}", Emoji.SUCCESS)

    return sh_path, ps1_path


def x_create_project_env_scripts__mutmut_66(project_dir: Path, workenv_name: str | None = None) -> tuple[Path, Path]:
    """Create environment scripts for a project based on its pyproject.toml.

    Args:
        project_dir: The project directory
        workenv_name: Optional workenv name override

    Returns:
        Tuple of (env.sh path, env.ps1 path)
    """
    pyproject_path = project_dir / "pyproject.toml"

    if not pyproject_path.exists():
        raise FileNotFoundError(f"No pyproject.toml found in {project_dir}")

    # Parse project name from pyproject.toml
    from provide.foundation.file.formats import read_toml

    pyproject = read_toml(pyproject_path)

    project_name = pyproject.get("project", {}).get("name", project_dir.name)
    python_requirement = pyproject.get("project", {}).get("requires-python")

    # Additional config for wrknv projects
    extra_config = {}
    if workenv_name:
        extra_config["workenv_name"] = workenv_name
        extra_config["venv_prefix"] = workenv_name

    # Add Python requirement if present
    if python_requirement:
        extra_config["python_requirement"] = python_requirement

    # Get wrknv configuration
    from wrknv.config import WorkenvConfig

    config = WorkenvConfig.load()
    env_config = config.get_env_config()

    # Use configuration from wrknv.toml if available
    if env_config:
        logger.info("Using env configuration from wrknv.toml", env_config=env_config)
        extra_config["include_tool_verification"] = env_config.get("include_tool_verification", True)

        # Handle new unified siblings format
        siblings_config = env_config.get("XXsiblingsXX", [])
        if siblings_config:
            extra_config["siblings"] = siblings_config
            # Keep backward compatibility
            extra_config["sibling_patterns"] = []
            extra_config["special_siblings"] = []
        else:
            # Backward compatibility with old format
            extra_config["sibling_patterns"] = env_config.get("sibling_patterns", [])
            extra_config["special_siblings"] = env_config.get("special_siblings", [])
            extra_config["siblings"] = []
    else:
        # Default configuration - empty by default
        logger.info("No env configuration found in wrknv.toml, using defaults")
        extra_config["include_tool_verification"] = True
        extra_config["sibling_patterns"] = []
        extra_config["special_siblings"] = []
        extra_config["siblings"] = []

    # Generate scripts
    generator = EnvScriptGenerator()
    sh_path, ps1_path = generator.generate_both_scripts(project_name, project_dir, **extra_config)

    print_success(f"Generated {sh_path}", Emoji.SUCCESS)
    print_success(f"Generated {ps1_path}", Emoji.SUCCESS)

    return sh_path, ps1_path


def x_create_project_env_scripts__mutmut_67(project_dir: Path, workenv_name: str | None = None) -> tuple[Path, Path]:
    """Create environment scripts for a project based on its pyproject.toml.

    Args:
        project_dir: The project directory
        workenv_name: Optional workenv name override

    Returns:
        Tuple of (env.sh path, env.ps1 path)
    """
    pyproject_path = project_dir / "pyproject.toml"

    if not pyproject_path.exists():
        raise FileNotFoundError(f"No pyproject.toml found in {project_dir}")

    # Parse project name from pyproject.toml
    from provide.foundation.file.formats import read_toml

    pyproject = read_toml(pyproject_path)

    project_name = pyproject.get("project", {}).get("name", project_dir.name)
    python_requirement = pyproject.get("project", {}).get("requires-python")

    # Additional config for wrknv projects
    extra_config = {}
    if workenv_name:
        extra_config["workenv_name"] = workenv_name
        extra_config["venv_prefix"] = workenv_name

    # Add Python requirement if present
    if python_requirement:
        extra_config["python_requirement"] = python_requirement

    # Get wrknv configuration
    from wrknv.config import WorkenvConfig

    config = WorkenvConfig.load()
    env_config = config.get_env_config()

    # Use configuration from wrknv.toml if available
    if env_config:
        logger.info("Using env configuration from wrknv.toml", env_config=env_config)
        extra_config["include_tool_verification"] = env_config.get("include_tool_verification", True)

        # Handle new unified siblings format
        siblings_config = env_config.get("SIBLINGS", [])
        if siblings_config:
            extra_config["siblings"] = siblings_config
            # Keep backward compatibility
            extra_config["sibling_patterns"] = []
            extra_config["special_siblings"] = []
        else:
            # Backward compatibility with old format
            extra_config["sibling_patterns"] = env_config.get("sibling_patterns", [])
            extra_config["special_siblings"] = env_config.get("special_siblings", [])
            extra_config["siblings"] = []
    else:
        # Default configuration - empty by default
        logger.info("No env configuration found in wrknv.toml, using defaults")
        extra_config["include_tool_verification"] = True
        extra_config["sibling_patterns"] = []
        extra_config["special_siblings"] = []
        extra_config["siblings"] = []

    # Generate scripts
    generator = EnvScriptGenerator()
    sh_path, ps1_path = generator.generate_both_scripts(project_name, project_dir, **extra_config)

    print_success(f"Generated {sh_path}", Emoji.SUCCESS)
    print_success(f"Generated {ps1_path}", Emoji.SUCCESS)

    return sh_path, ps1_path


def x_create_project_env_scripts__mutmut_68(project_dir: Path, workenv_name: str | None = None) -> tuple[Path, Path]:
    """Create environment scripts for a project based on its pyproject.toml.

    Args:
        project_dir: The project directory
        workenv_name: Optional workenv name override

    Returns:
        Tuple of (env.sh path, env.ps1 path)
    """
    pyproject_path = project_dir / "pyproject.toml"

    if not pyproject_path.exists():
        raise FileNotFoundError(f"No pyproject.toml found in {project_dir}")

    # Parse project name from pyproject.toml
    from provide.foundation.file.formats import read_toml

    pyproject = read_toml(pyproject_path)

    project_name = pyproject.get("project", {}).get("name", project_dir.name)
    python_requirement = pyproject.get("project", {}).get("requires-python")

    # Additional config for wrknv projects
    extra_config = {}
    if workenv_name:
        extra_config["workenv_name"] = workenv_name
        extra_config["venv_prefix"] = workenv_name

    # Add Python requirement if present
    if python_requirement:
        extra_config["python_requirement"] = python_requirement

    # Get wrknv configuration
    from wrknv.config import WorkenvConfig

    config = WorkenvConfig.load()
    env_config = config.get_env_config()

    # Use configuration from wrknv.toml if available
    if env_config:
        logger.info("Using env configuration from wrknv.toml", env_config=env_config)
        extra_config["include_tool_verification"] = env_config.get("include_tool_verification", True)

        # Handle new unified siblings format
        siblings_config = env_config.get("siblings", [])
        if siblings_config:
            extra_config["siblings"] = None
            # Keep backward compatibility
            extra_config["sibling_patterns"] = []
            extra_config["special_siblings"] = []
        else:
            # Backward compatibility with old format
            extra_config["sibling_patterns"] = env_config.get("sibling_patterns", [])
            extra_config["special_siblings"] = env_config.get("special_siblings", [])
            extra_config["siblings"] = []
    else:
        # Default configuration - empty by default
        logger.info("No env configuration found in wrknv.toml, using defaults")
        extra_config["include_tool_verification"] = True
        extra_config["sibling_patterns"] = []
        extra_config["special_siblings"] = []
        extra_config["siblings"] = []

    # Generate scripts
    generator = EnvScriptGenerator()
    sh_path, ps1_path = generator.generate_both_scripts(project_name, project_dir, **extra_config)

    print_success(f"Generated {sh_path}", Emoji.SUCCESS)
    print_success(f"Generated {ps1_path}", Emoji.SUCCESS)

    return sh_path, ps1_path


def x_create_project_env_scripts__mutmut_69(project_dir: Path, workenv_name: str | None = None) -> tuple[Path, Path]:
    """Create environment scripts for a project based on its pyproject.toml.

    Args:
        project_dir: The project directory
        workenv_name: Optional workenv name override

    Returns:
        Tuple of (env.sh path, env.ps1 path)
    """
    pyproject_path = project_dir / "pyproject.toml"

    if not pyproject_path.exists():
        raise FileNotFoundError(f"No pyproject.toml found in {project_dir}")

    # Parse project name from pyproject.toml
    from provide.foundation.file.formats import read_toml

    pyproject = read_toml(pyproject_path)

    project_name = pyproject.get("project", {}).get("name", project_dir.name)
    python_requirement = pyproject.get("project", {}).get("requires-python")

    # Additional config for wrknv projects
    extra_config = {}
    if workenv_name:
        extra_config["workenv_name"] = workenv_name
        extra_config["venv_prefix"] = workenv_name

    # Add Python requirement if present
    if python_requirement:
        extra_config["python_requirement"] = python_requirement

    # Get wrknv configuration
    from wrknv.config import WorkenvConfig

    config = WorkenvConfig.load()
    env_config = config.get_env_config()

    # Use configuration from wrknv.toml if available
    if env_config:
        logger.info("Using env configuration from wrknv.toml", env_config=env_config)
        extra_config["include_tool_verification"] = env_config.get("include_tool_verification", True)

        # Handle new unified siblings format
        siblings_config = env_config.get("siblings", [])
        if siblings_config:
            extra_config["XXsiblingsXX"] = siblings_config
            # Keep backward compatibility
            extra_config["sibling_patterns"] = []
            extra_config["special_siblings"] = []
        else:
            # Backward compatibility with old format
            extra_config["sibling_patterns"] = env_config.get("sibling_patterns", [])
            extra_config["special_siblings"] = env_config.get("special_siblings", [])
            extra_config["siblings"] = []
    else:
        # Default configuration - empty by default
        logger.info("No env configuration found in wrknv.toml, using defaults")
        extra_config["include_tool_verification"] = True
        extra_config["sibling_patterns"] = []
        extra_config["special_siblings"] = []
        extra_config["siblings"] = []

    # Generate scripts
    generator = EnvScriptGenerator()
    sh_path, ps1_path = generator.generate_both_scripts(project_name, project_dir, **extra_config)

    print_success(f"Generated {sh_path}", Emoji.SUCCESS)
    print_success(f"Generated {ps1_path}", Emoji.SUCCESS)

    return sh_path, ps1_path


def x_create_project_env_scripts__mutmut_70(project_dir: Path, workenv_name: str | None = None) -> tuple[Path, Path]:
    """Create environment scripts for a project based on its pyproject.toml.

    Args:
        project_dir: The project directory
        workenv_name: Optional workenv name override

    Returns:
        Tuple of (env.sh path, env.ps1 path)
    """
    pyproject_path = project_dir / "pyproject.toml"

    if not pyproject_path.exists():
        raise FileNotFoundError(f"No pyproject.toml found in {project_dir}")

    # Parse project name from pyproject.toml
    from provide.foundation.file.formats import read_toml

    pyproject = read_toml(pyproject_path)

    project_name = pyproject.get("project", {}).get("name", project_dir.name)
    python_requirement = pyproject.get("project", {}).get("requires-python")

    # Additional config for wrknv projects
    extra_config = {}
    if workenv_name:
        extra_config["workenv_name"] = workenv_name
        extra_config["venv_prefix"] = workenv_name

    # Add Python requirement if present
    if python_requirement:
        extra_config["python_requirement"] = python_requirement

    # Get wrknv configuration
    from wrknv.config import WorkenvConfig

    config = WorkenvConfig.load()
    env_config = config.get_env_config()

    # Use configuration from wrknv.toml if available
    if env_config:
        logger.info("Using env configuration from wrknv.toml", env_config=env_config)
        extra_config["include_tool_verification"] = env_config.get("include_tool_verification", True)

        # Handle new unified siblings format
        siblings_config = env_config.get("siblings", [])
        if siblings_config:
            extra_config["SIBLINGS"] = siblings_config
            # Keep backward compatibility
            extra_config["sibling_patterns"] = []
            extra_config["special_siblings"] = []
        else:
            # Backward compatibility with old format
            extra_config["sibling_patterns"] = env_config.get("sibling_patterns", [])
            extra_config["special_siblings"] = env_config.get("special_siblings", [])
            extra_config["siblings"] = []
    else:
        # Default configuration - empty by default
        logger.info("No env configuration found in wrknv.toml, using defaults")
        extra_config["include_tool_verification"] = True
        extra_config["sibling_patterns"] = []
        extra_config["special_siblings"] = []
        extra_config["siblings"] = []

    # Generate scripts
    generator = EnvScriptGenerator()
    sh_path, ps1_path = generator.generate_both_scripts(project_name, project_dir, **extra_config)

    print_success(f"Generated {sh_path}", Emoji.SUCCESS)
    print_success(f"Generated {ps1_path}", Emoji.SUCCESS)

    return sh_path, ps1_path


def x_create_project_env_scripts__mutmut_71(project_dir: Path, workenv_name: str | None = None) -> tuple[Path, Path]:
    """Create environment scripts for a project based on its pyproject.toml.

    Args:
        project_dir: The project directory
        workenv_name: Optional workenv name override

    Returns:
        Tuple of (env.sh path, env.ps1 path)
    """
    pyproject_path = project_dir / "pyproject.toml"

    if not pyproject_path.exists():
        raise FileNotFoundError(f"No pyproject.toml found in {project_dir}")

    # Parse project name from pyproject.toml
    from provide.foundation.file.formats import read_toml

    pyproject = read_toml(pyproject_path)

    project_name = pyproject.get("project", {}).get("name", project_dir.name)
    python_requirement = pyproject.get("project", {}).get("requires-python")

    # Additional config for wrknv projects
    extra_config = {}
    if workenv_name:
        extra_config["workenv_name"] = workenv_name
        extra_config["venv_prefix"] = workenv_name

    # Add Python requirement if present
    if python_requirement:
        extra_config["python_requirement"] = python_requirement

    # Get wrknv configuration
    from wrknv.config import WorkenvConfig

    config = WorkenvConfig.load()
    env_config = config.get_env_config()

    # Use configuration from wrknv.toml if available
    if env_config:
        logger.info("Using env configuration from wrknv.toml", env_config=env_config)
        extra_config["include_tool_verification"] = env_config.get("include_tool_verification", True)

        # Handle new unified siblings format
        siblings_config = env_config.get("siblings", [])
        if siblings_config:
            extra_config["siblings"] = siblings_config
            # Keep backward compatibility
            extra_config["sibling_patterns"] = None
            extra_config["special_siblings"] = []
        else:
            # Backward compatibility with old format
            extra_config["sibling_patterns"] = env_config.get("sibling_patterns", [])
            extra_config["special_siblings"] = env_config.get("special_siblings", [])
            extra_config["siblings"] = []
    else:
        # Default configuration - empty by default
        logger.info("No env configuration found in wrknv.toml, using defaults")
        extra_config["include_tool_verification"] = True
        extra_config["sibling_patterns"] = []
        extra_config["special_siblings"] = []
        extra_config["siblings"] = []

    # Generate scripts
    generator = EnvScriptGenerator()
    sh_path, ps1_path = generator.generate_both_scripts(project_name, project_dir, **extra_config)

    print_success(f"Generated {sh_path}", Emoji.SUCCESS)
    print_success(f"Generated {ps1_path}", Emoji.SUCCESS)

    return sh_path, ps1_path


def x_create_project_env_scripts__mutmut_72(project_dir: Path, workenv_name: str | None = None) -> tuple[Path, Path]:
    """Create environment scripts for a project based on its pyproject.toml.

    Args:
        project_dir: The project directory
        workenv_name: Optional workenv name override

    Returns:
        Tuple of (env.sh path, env.ps1 path)
    """
    pyproject_path = project_dir / "pyproject.toml"

    if not pyproject_path.exists():
        raise FileNotFoundError(f"No pyproject.toml found in {project_dir}")

    # Parse project name from pyproject.toml
    from provide.foundation.file.formats import read_toml

    pyproject = read_toml(pyproject_path)

    project_name = pyproject.get("project", {}).get("name", project_dir.name)
    python_requirement = pyproject.get("project", {}).get("requires-python")

    # Additional config for wrknv projects
    extra_config = {}
    if workenv_name:
        extra_config["workenv_name"] = workenv_name
        extra_config["venv_prefix"] = workenv_name

    # Add Python requirement if present
    if python_requirement:
        extra_config["python_requirement"] = python_requirement

    # Get wrknv configuration
    from wrknv.config import WorkenvConfig

    config = WorkenvConfig.load()
    env_config = config.get_env_config()

    # Use configuration from wrknv.toml if available
    if env_config:
        logger.info("Using env configuration from wrknv.toml", env_config=env_config)
        extra_config["include_tool_verification"] = env_config.get("include_tool_verification", True)

        # Handle new unified siblings format
        siblings_config = env_config.get("siblings", [])
        if siblings_config:
            extra_config["siblings"] = siblings_config
            # Keep backward compatibility
            extra_config["XXsibling_patternsXX"] = []
            extra_config["special_siblings"] = []
        else:
            # Backward compatibility with old format
            extra_config["sibling_patterns"] = env_config.get("sibling_patterns", [])
            extra_config["special_siblings"] = env_config.get("special_siblings", [])
            extra_config["siblings"] = []
    else:
        # Default configuration - empty by default
        logger.info("No env configuration found in wrknv.toml, using defaults")
        extra_config["include_tool_verification"] = True
        extra_config["sibling_patterns"] = []
        extra_config["special_siblings"] = []
        extra_config["siblings"] = []

    # Generate scripts
    generator = EnvScriptGenerator()
    sh_path, ps1_path = generator.generate_both_scripts(project_name, project_dir, **extra_config)

    print_success(f"Generated {sh_path}", Emoji.SUCCESS)
    print_success(f"Generated {ps1_path}", Emoji.SUCCESS)

    return sh_path, ps1_path


def x_create_project_env_scripts__mutmut_73(project_dir: Path, workenv_name: str | None = None) -> tuple[Path, Path]:
    """Create environment scripts for a project based on its pyproject.toml.

    Args:
        project_dir: The project directory
        workenv_name: Optional workenv name override

    Returns:
        Tuple of (env.sh path, env.ps1 path)
    """
    pyproject_path = project_dir / "pyproject.toml"

    if not pyproject_path.exists():
        raise FileNotFoundError(f"No pyproject.toml found in {project_dir}")

    # Parse project name from pyproject.toml
    from provide.foundation.file.formats import read_toml

    pyproject = read_toml(pyproject_path)

    project_name = pyproject.get("project", {}).get("name", project_dir.name)
    python_requirement = pyproject.get("project", {}).get("requires-python")

    # Additional config for wrknv projects
    extra_config = {}
    if workenv_name:
        extra_config["workenv_name"] = workenv_name
        extra_config["venv_prefix"] = workenv_name

    # Add Python requirement if present
    if python_requirement:
        extra_config["python_requirement"] = python_requirement

    # Get wrknv configuration
    from wrknv.config import WorkenvConfig

    config = WorkenvConfig.load()
    env_config = config.get_env_config()

    # Use configuration from wrknv.toml if available
    if env_config:
        logger.info("Using env configuration from wrknv.toml", env_config=env_config)
        extra_config["include_tool_verification"] = env_config.get("include_tool_verification", True)

        # Handle new unified siblings format
        siblings_config = env_config.get("siblings", [])
        if siblings_config:
            extra_config["siblings"] = siblings_config
            # Keep backward compatibility
            extra_config["SIBLING_PATTERNS"] = []
            extra_config["special_siblings"] = []
        else:
            # Backward compatibility with old format
            extra_config["sibling_patterns"] = env_config.get("sibling_patterns", [])
            extra_config["special_siblings"] = env_config.get("special_siblings", [])
            extra_config["siblings"] = []
    else:
        # Default configuration - empty by default
        logger.info("No env configuration found in wrknv.toml, using defaults")
        extra_config["include_tool_verification"] = True
        extra_config["sibling_patterns"] = []
        extra_config["special_siblings"] = []
        extra_config["siblings"] = []

    # Generate scripts
    generator = EnvScriptGenerator()
    sh_path, ps1_path = generator.generate_both_scripts(project_name, project_dir, **extra_config)

    print_success(f"Generated {sh_path}", Emoji.SUCCESS)
    print_success(f"Generated {ps1_path}", Emoji.SUCCESS)

    return sh_path, ps1_path


def x_create_project_env_scripts__mutmut_74(project_dir: Path, workenv_name: str | None = None) -> tuple[Path, Path]:
    """Create environment scripts for a project based on its pyproject.toml.

    Args:
        project_dir: The project directory
        workenv_name: Optional workenv name override

    Returns:
        Tuple of (env.sh path, env.ps1 path)
    """
    pyproject_path = project_dir / "pyproject.toml"

    if not pyproject_path.exists():
        raise FileNotFoundError(f"No pyproject.toml found in {project_dir}")

    # Parse project name from pyproject.toml
    from provide.foundation.file.formats import read_toml

    pyproject = read_toml(pyproject_path)

    project_name = pyproject.get("project", {}).get("name", project_dir.name)
    python_requirement = pyproject.get("project", {}).get("requires-python")

    # Additional config for wrknv projects
    extra_config = {}
    if workenv_name:
        extra_config["workenv_name"] = workenv_name
        extra_config["venv_prefix"] = workenv_name

    # Add Python requirement if present
    if python_requirement:
        extra_config["python_requirement"] = python_requirement

    # Get wrknv configuration
    from wrknv.config import WorkenvConfig

    config = WorkenvConfig.load()
    env_config = config.get_env_config()

    # Use configuration from wrknv.toml if available
    if env_config:
        logger.info("Using env configuration from wrknv.toml", env_config=env_config)
        extra_config["include_tool_verification"] = env_config.get("include_tool_verification", True)

        # Handle new unified siblings format
        siblings_config = env_config.get("siblings", [])
        if siblings_config:
            extra_config["siblings"] = siblings_config
            # Keep backward compatibility
            extra_config["sibling_patterns"] = []
            extra_config["special_siblings"] = None
        else:
            # Backward compatibility with old format
            extra_config["sibling_patterns"] = env_config.get("sibling_patterns", [])
            extra_config["special_siblings"] = env_config.get("special_siblings", [])
            extra_config["siblings"] = []
    else:
        # Default configuration - empty by default
        logger.info("No env configuration found in wrknv.toml, using defaults")
        extra_config["include_tool_verification"] = True
        extra_config["sibling_patterns"] = []
        extra_config["special_siblings"] = []
        extra_config["siblings"] = []

    # Generate scripts
    generator = EnvScriptGenerator()
    sh_path, ps1_path = generator.generate_both_scripts(project_name, project_dir, **extra_config)

    print_success(f"Generated {sh_path}", Emoji.SUCCESS)
    print_success(f"Generated {ps1_path}", Emoji.SUCCESS)

    return sh_path, ps1_path


def x_create_project_env_scripts__mutmut_75(project_dir: Path, workenv_name: str | None = None) -> tuple[Path, Path]:
    """Create environment scripts for a project based on its pyproject.toml.

    Args:
        project_dir: The project directory
        workenv_name: Optional workenv name override

    Returns:
        Tuple of (env.sh path, env.ps1 path)
    """
    pyproject_path = project_dir / "pyproject.toml"

    if not pyproject_path.exists():
        raise FileNotFoundError(f"No pyproject.toml found in {project_dir}")

    # Parse project name from pyproject.toml
    from provide.foundation.file.formats import read_toml

    pyproject = read_toml(pyproject_path)

    project_name = pyproject.get("project", {}).get("name", project_dir.name)
    python_requirement = pyproject.get("project", {}).get("requires-python")

    # Additional config for wrknv projects
    extra_config = {}
    if workenv_name:
        extra_config["workenv_name"] = workenv_name
        extra_config["venv_prefix"] = workenv_name

    # Add Python requirement if present
    if python_requirement:
        extra_config["python_requirement"] = python_requirement

    # Get wrknv configuration
    from wrknv.config import WorkenvConfig

    config = WorkenvConfig.load()
    env_config = config.get_env_config()

    # Use configuration from wrknv.toml if available
    if env_config:
        logger.info("Using env configuration from wrknv.toml", env_config=env_config)
        extra_config["include_tool_verification"] = env_config.get("include_tool_verification", True)

        # Handle new unified siblings format
        siblings_config = env_config.get("siblings", [])
        if siblings_config:
            extra_config["siblings"] = siblings_config
            # Keep backward compatibility
            extra_config["sibling_patterns"] = []
            extra_config["XXspecial_siblingsXX"] = []
        else:
            # Backward compatibility with old format
            extra_config["sibling_patterns"] = env_config.get("sibling_patterns", [])
            extra_config["special_siblings"] = env_config.get("special_siblings", [])
            extra_config["siblings"] = []
    else:
        # Default configuration - empty by default
        logger.info("No env configuration found in wrknv.toml, using defaults")
        extra_config["include_tool_verification"] = True
        extra_config["sibling_patterns"] = []
        extra_config["special_siblings"] = []
        extra_config["siblings"] = []

    # Generate scripts
    generator = EnvScriptGenerator()
    sh_path, ps1_path = generator.generate_both_scripts(project_name, project_dir, **extra_config)

    print_success(f"Generated {sh_path}", Emoji.SUCCESS)
    print_success(f"Generated {ps1_path}", Emoji.SUCCESS)

    return sh_path, ps1_path


def x_create_project_env_scripts__mutmut_76(project_dir: Path, workenv_name: str | None = None) -> tuple[Path, Path]:
    """Create environment scripts for a project based on its pyproject.toml.

    Args:
        project_dir: The project directory
        workenv_name: Optional workenv name override

    Returns:
        Tuple of (env.sh path, env.ps1 path)
    """
    pyproject_path = project_dir / "pyproject.toml"

    if not pyproject_path.exists():
        raise FileNotFoundError(f"No pyproject.toml found in {project_dir}")

    # Parse project name from pyproject.toml
    from provide.foundation.file.formats import read_toml

    pyproject = read_toml(pyproject_path)

    project_name = pyproject.get("project", {}).get("name", project_dir.name)
    python_requirement = pyproject.get("project", {}).get("requires-python")

    # Additional config for wrknv projects
    extra_config = {}
    if workenv_name:
        extra_config["workenv_name"] = workenv_name
        extra_config["venv_prefix"] = workenv_name

    # Add Python requirement if present
    if python_requirement:
        extra_config["python_requirement"] = python_requirement

    # Get wrknv configuration
    from wrknv.config import WorkenvConfig

    config = WorkenvConfig.load()
    env_config = config.get_env_config()

    # Use configuration from wrknv.toml if available
    if env_config:
        logger.info("Using env configuration from wrknv.toml", env_config=env_config)
        extra_config["include_tool_verification"] = env_config.get("include_tool_verification", True)

        # Handle new unified siblings format
        siblings_config = env_config.get("siblings", [])
        if siblings_config:
            extra_config["siblings"] = siblings_config
            # Keep backward compatibility
            extra_config["sibling_patterns"] = []
            extra_config["SPECIAL_SIBLINGS"] = []
        else:
            # Backward compatibility with old format
            extra_config["sibling_patterns"] = env_config.get("sibling_patterns", [])
            extra_config["special_siblings"] = env_config.get("special_siblings", [])
            extra_config["siblings"] = []
    else:
        # Default configuration - empty by default
        logger.info("No env configuration found in wrknv.toml, using defaults")
        extra_config["include_tool_verification"] = True
        extra_config["sibling_patterns"] = []
        extra_config["special_siblings"] = []
        extra_config["siblings"] = []

    # Generate scripts
    generator = EnvScriptGenerator()
    sh_path, ps1_path = generator.generate_both_scripts(project_name, project_dir, **extra_config)

    print_success(f"Generated {sh_path}", Emoji.SUCCESS)
    print_success(f"Generated {ps1_path}", Emoji.SUCCESS)

    return sh_path, ps1_path


def x_create_project_env_scripts__mutmut_77(project_dir: Path, workenv_name: str | None = None) -> tuple[Path, Path]:
    """Create environment scripts for a project based on its pyproject.toml.

    Args:
        project_dir: The project directory
        workenv_name: Optional workenv name override

    Returns:
        Tuple of (env.sh path, env.ps1 path)
    """
    pyproject_path = project_dir / "pyproject.toml"

    if not pyproject_path.exists():
        raise FileNotFoundError(f"No pyproject.toml found in {project_dir}")

    # Parse project name from pyproject.toml
    from provide.foundation.file.formats import read_toml

    pyproject = read_toml(pyproject_path)

    project_name = pyproject.get("project", {}).get("name", project_dir.name)
    python_requirement = pyproject.get("project", {}).get("requires-python")

    # Additional config for wrknv projects
    extra_config = {}
    if workenv_name:
        extra_config["workenv_name"] = workenv_name
        extra_config["venv_prefix"] = workenv_name

    # Add Python requirement if present
    if python_requirement:
        extra_config["python_requirement"] = python_requirement

    # Get wrknv configuration
    from wrknv.config import WorkenvConfig

    config = WorkenvConfig.load()
    env_config = config.get_env_config()

    # Use configuration from wrknv.toml if available
    if env_config:
        logger.info("Using env configuration from wrknv.toml", env_config=env_config)
        extra_config["include_tool_verification"] = env_config.get("include_tool_verification", True)

        # Handle new unified siblings format
        siblings_config = env_config.get("siblings", [])
        if siblings_config:
            extra_config["siblings"] = siblings_config
            # Keep backward compatibility
            extra_config["sibling_patterns"] = []
            extra_config["special_siblings"] = []
        else:
            # Backward compatibility with old format
            extra_config["sibling_patterns"] = None
            extra_config["special_siblings"] = env_config.get("special_siblings", [])
            extra_config["siblings"] = []
    else:
        # Default configuration - empty by default
        logger.info("No env configuration found in wrknv.toml, using defaults")
        extra_config["include_tool_verification"] = True
        extra_config["sibling_patterns"] = []
        extra_config["special_siblings"] = []
        extra_config["siblings"] = []

    # Generate scripts
    generator = EnvScriptGenerator()
    sh_path, ps1_path = generator.generate_both_scripts(project_name, project_dir, **extra_config)

    print_success(f"Generated {sh_path}", Emoji.SUCCESS)
    print_success(f"Generated {ps1_path}", Emoji.SUCCESS)

    return sh_path, ps1_path


def x_create_project_env_scripts__mutmut_78(project_dir: Path, workenv_name: str | None = None) -> tuple[Path, Path]:
    """Create environment scripts for a project based on its pyproject.toml.

    Args:
        project_dir: The project directory
        workenv_name: Optional workenv name override

    Returns:
        Tuple of (env.sh path, env.ps1 path)
    """
    pyproject_path = project_dir / "pyproject.toml"

    if not pyproject_path.exists():
        raise FileNotFoundError(f"No pyproject.toml found in {project_dir}")

    # Parse project name from pyproject.toml
    from provide.foundation.file.formats import read_toml

    pyproject = read_toml(pyproject_path)

    project_name = pyproject.get("project", {}).get("name", project_dir.name)
    python_requirement = pyproject.get("project", {}).get("requires-python")

    # Additional config for wrknv projects
    extra_config = {}
    if workenv_name:
        extra_config["workenv_name"] = workenv_name
        extra_config["venv_prefix"] = workenv_name

    # Add Python requirement if present
    if python_requirement:
        extra_config["python_requirement"] = python_requirement

    # Get wrknv configuration
    from wrknv.config import WorkenvConfig

    config = WorkenvConfig.load()
    env_config = config.get_env_config()

    # Use configuration from wrknv.toml if available
    if env_config:
        logger.info("Using env configuration from wrknv.toml", env_config=env_config)
        extra_config["include_tool_verification"] = env_config.get("include_tool_verification", True)

        # Handle new unified siblings format
        siblings_config = env_config.get("siblings", [])
        if siblings_config:
            extra_config["siblings"] = siblings_config
            # Keep backward compatibility
            extra_config["sibling_patterns"] = []
            extra_config["special_siblings"] = []
        else:
            # Backward compatibility with old format
            extra_config["XXsibling_patternsXX"] = env_config.get("sibling_patterns", [])
            extra_config["special_siblings"] = env_config.get("special_siblings", [])
            extra_config["siblings"] = []
    else:
        # Default configuration - empty by default
        logger.info("No env configuration found in wrknv.toml, using defaults")
        extra_config["include_tool_verification"] = True
        extra_config["sibling_patterns"] = []
        extra_config["special_siblings"] = []
        extra_config["siblings"] = []

    # Generate scripts
    generator = EnvScriptGenerator()
    sh_path, ps1_path = generator.generate_both_scripts(project_name, project_dir, **extra_config)

    print_success(f"Generated {sh_path}", Emoji.SUCCESS)
    print_success(f"Generated {ps1_path}", Emoji.SUCCESS)

    return sh_path, ps1_path


def x_create_project_env_scripts__mutmut_79(project_dir: Path, workenv_name: str | None = None) -> tuple[Path, Path]:
    """Create environment scripts for a project based on its pyproject.toml.

    Args:
        project_dir: The project directory
        workenv_name: Optional workenv name override

    Returns:
        Tuple of (env.sh path, env.ps1 path)
    """
    pyproject_path = project_dir / "pyproject.toml"

    if not pyproject_path.exists():
        raise FileNotFoundError(f"No pyproject.toml found in {project_dir}")

    # Parse project name from pyproject.toml
    from provide.foundation.file.formats import read_toml

    pyproject = read_toml(pyproject_path)

    project_name = pyproject.get("project", {}).get("name", project_dir.name)
    python_requirement = pyproject.get("project", {}).get("requires-python")

    # Additional config for wrknv projects
    extra_config = {}
    if workenv_name:
        extra_config["workenv_name"] = workenv_name
        extra_config["venv_prefix"] = workenv_name

    # Add Python requirement if present
    if python_requirement:
        extra_config["python_requirement"] = python_requirement

    # Get wrknv configuration
    from wrknv.config import WorkenvConfig

    config = WorkenvConfig.load()
    env_config = config.get_env_config()

    # Use configuration from wrknv.toml if available
    if env_config:
        logger.info("Using env configuration from wrknv.toml", env_config=env_config)
        extra_config["include_tool_verification"] = env_config.get("include_tool_verification", True)

        # Handle new unified siblings format
        siblings_config = env_config.get("siblings", [])
        if siblings_config:
            extra_config["siblings"] = siblings_config
            # Keep backward compatibility
            extra_config["sibling_patterns"] = []
            extra_config["special_siblings"] = []
        else:
            # Backward compatibility with old format
            extra_config["SIBLING_PATTERNS"] = env_config.get("sibling_patterns", [])
            extra_config["special_siblings"] = env_config.get("special_siblings", [])
            extra_config["siblings"] = []
    else:
        # Default configuration - empty by default
        logger.info("No env configuration found in wrknv.toml, using defaults")
        extra_config["include_tool_verification"] = True
        extra_config["sibling_patterns"] = []
        extra_config["special_siblings"] = []
        extra_config["siblings"] = []

    # Generate scripts
    generator = EnvScriptGenerator()
    sh_path, ps1_path = generator.generate_both_scripts(project_name, project_dir, **extra_config)

    print_success(f"Generated {sh_path}", Emoji.SUCCESS)
    print_success(f"Generated {ps1_path}", Emoji.SUCCESS)

    return sh_path, ps1_path


def x_create_project_env_scripts__mutmut_80(project_dir: Path, workenv_name: str | None = None) -> tuple[Path, Path]:
    """Create environment scripts for a project based on its pyproject.toml.

    Args:
        project_dir: The project directory
        workenv_name: Optional workenv name override

    Returns:
        Tuple of (env.sh path, env.ps1 path)
    """
    pyproject_path = project_dir / "pyproject.toml"

    if not pyproject_path.exists():
        raise FileNotFoundError(f"No pyproject.toml found in {project_dir}")

    # Parse project name from pyproject.toml
    from provide.foundation.file.formats import read_toml

    pyproject = read_toml(pyproject_path)

    project_name = pyproject.get("project", {}).get("name", project_dir.name)
    python_requirement = pyproject.get("project", {}).get("requires-python")

    # Additional config for wrknv projects
    extra_config = {}
    if workenv_name:
        extra_config["workenv_name"] = workenv_name
        extra_config["venv_prefix"] = workenv_name

    # Add Python requirement if present
    if python_requirement:
        extra_config["python_requirement"] = python_requirement

    # Get wrknv configuration
    from wrknv.config import WorkenvConfig

    config = WorkenvConfig.load()
    env_config = config.get_env_config()

    # Use configuration from wrknv.toml if available
    if env_config:
        logger.info("Using env configuration from wrknv.toml", env_config=env_config)
        extra_config["include_tool_verification"] = env_config.get("include_tool_verification", True)

        # Handle new unified siblings format
        siblings_config = env_config.get("siblings", [])
        if siblings_config:
            extra_config["siblings"] = siblings_config
            # Keep backward compatibility
            extra_config["sibling_patterns"] = []
            extra_config["special_siblings"] = []
        else:
            # Backward compatibility with old format
            extra_config["sibling_patterns"] = env_config.get(None, [])
            extra_config["special_siblings"] = env_config.get("special_siblings", [])
            extra_config["siblings"] = []
    else:
        # Default configuration - empty by default
        logger.info("No env configuration found in wrknv.toml, using defaults")
        extra_config["include_tool_verification"] = True
        extra_config["sibling_patterns"] = []
        extra_config["special_siblings"] = []
        extra_config["siblings"] = []

    # Generate scripts
    generator = EnvScriptGenerator()
    sh_path, ps1_path = generator.generate_both_scripts(project_name, project_dir, **extra_config)

    print_success(f"Generated {sh_path}", Emoji.SUCCESS)
    print_success(f"Generated {ps1_path}", Emoji.SUCCESS)

    return sh_path, ps1_path


def x_create_project_env_scripts__mutmut_81(project_dir: Path, workenv_name: str | None = None) -> tuple[Path, Path]:
    """Create environment scripts for a project based on its pyproject.toml.

    Args:
        project_dir: The project directory
        workenv_name: Optional workenv name override

    Returns:
        Tuple of (env.sh path, env.ps1 path)
    """
    pyproject_path = project_dir / "pyproject.toml"

    if not pyproject_path.exists():
        raise FileNotFoundError(f"No pyproject.toml found in {project_dir}")

    # Parse project name from pyproject.toml
    from provide.foundation.file.formats import read_toml

    pyproject = read_toml(pyproject_path)

    project_name = pyproject.get("project", {}).get("name", project_dir.name)
    python_requirement = pyproject.get("project", {}).get("requires-python")

    # Additional config for wrknv projects
    extra_config = {}
    if workenv_name:
        extra_config["workenv_name"] = workenv_name
        extra_config["venv_prefix"] = workenv_name

    # Add Python requirement if present
    if python_requirement:
        extra_config["python_requirement"] = python_requirement

    # Get wrknv configuration
    from wrknv.config import WorkenvConfig

    config = WorkenvConfig.load()
    env_config = config.get_env_config()

    # Use configuration from wrknv.toml if available
    if env_config:
        logger.info("Using env configuration from wrknv.toml", env_config=env_config)
        extra_config["include_tool_verification"] = env_config.get("include_tool_verification", True)

        # Handle new unified siblings format
        siblings_config = env_config.get("siblings", [])
        if siblings_config:
            extra_config["siblings"] = siblings_config
            # Keep backward compatibility
            extra_config["sibling_patterns"] = []
            extra_config["special_siblings"] = []
        else:
            # Backward compatibility with old format
            extra_config["sibling_patterns"] = env_config.get("sibling_patterns", None)
            extra_config["special_siblings"] = env_config.get("special_siblings", [])
            extra_config["siblings"] = []
    else:
        # Default configuration - empty by default
        logger.info("No env configuration found in wrknv.toml, using defaults")
        extra_config["include_tool_verification"] = True
        extra_config["sibling_patterns"] = []
        extra_config["special_siblings"] = []
        extra_config["siblings"] = []

    # Generate scripts
    generator = EnvScriptGenerator()
    sh_path, ps1_path = generator.generate_both_scripts(project_name, project_dir, **extra_config)

    print_success(f"Generated {sh_path}", Emoji.SUCCESS)
    print_success(f"Generated {ps1_path}", Emoji.SUCCESS)

    return sh_path, ps1_path


def x_create_project_env_scripts__mutmut_82(project_dir: Path, workenv_name: str | None = None) -> tuple[Path, Path]:
    """Create environment scripts for a project based on its pyproject.toml.

    Args:
        project_dir: The project directory
        workenv_name: Optional workenv name override

    Returns:
        Tuple of (env.sh path, env.ps1 path)
    """
    pyproject_path = project_dir / "pyproject.toml"

    if not pyproject_path.exists():
        raise FileNotFoundError(f"No pyproject.toml found in {project_dir}")

    # Parse project name from pyproject.toml
    from provide.foundation.file.formats import read_toml

    pyproject = read_toml(pyproject_path)

    project_name = pyproject.get("project", {}).get("name", project_dir.name)
    python_requirement = pyproject.get("project", {}).get("requires-python")

    # Additional config for wrknv projects
    extra_config = {}
    if workenv_name:
        extra_config["workenv_name"] = workenv_name
        extra_config["venv_prefix"] = workenv_name

    # Add Python requirement if present
    if python_requirement:
        extra_config["python_requirement"] = python_requirement

    # Get wrknv configuration
    from wrknv.config import WorkenvConfig

    config = WorkenvConfig.load()
    env_config = config.get_env_config()

    # Use configuration from wrknv.toml if available
    if env_config:
        logger.info("Using env configuration from wrknv.toml", env_config=env_config)
        extra_config["include_tool_verification"] = env_config.get("include_tool_verification", True)

        # Handle new unified siblings format
        siblings_config = env_config.get("siblings", [])
        if siblings_config:
            extra_config["siblings"] = siblings_config
            # Keep backward compatibility
            extra_config["sibling_patterns"] = []
            extra_config["special_siblings"] = []
        else:
            # Backward compatibility with old format
            extra_config["sibling_patterns"] = env_config.get([])
            extra_config["special_siblings"] = env_config.get("special_siblings", [])
            extra_config["siblings"] = []
    else:
        # Default configuration - empty by default
        logger.info("No env configuration found in wrknv.toml, using defaults")
        extra_config["include_tool_verification"] = True
        extra_config["sibling_patterns"] = []
        extra_config["special_siblings"] = []
        extra_config["siblings"] = []

    # Generate scripts
    generator = EnvScriptGenerator()
    sh_path, ps1_path = generator.generate_both_scripts(project_name, project_dir, **extra_config)

    print_success(f"Generated {sh_path}", Emoji.SUCCESS)
    print_success(f"Generated {ps1_path}", Emoji.SUCCESS)

    return sh_path, ps1_path


def x_create_project_env_scripts__mutmut_83(project_dir: Path, workenv_name: str | None = None) -> tuple[Path, Path]:
    """Create environment scripts for a project based on its pyproject.toml.

    Args:
        project_dir: The project directory
        workenv_name: Optional workenv name override

    Returns:
        Tuple of (env.sh path, env.ps1 path)
    """
    pyproject_path = project_dir / "pyproject.toml"

    if not pyproject_path.exists():
        raise FileNotFoundError(f"No pyproject.toml found in {project_dir}")

    # Parse project name from pyproject.toml
    from provide.foundation.file.formats import read_toml

    pyproject = read_toml(pyproject_path)

    project_name = pyproject.get("project", {}).get("name", project_dir.name)
    python_requirement = pyproject.get("project", {}).get("requires-python")

    # Additional config for wrknv projects
    extra_config = {}
    if workenv_name:
        extra_config["workenv_name"] = workenv_name
        extra_config["venv_prefix"] = workenv_name

    # Add Python requirement if present
    if python_requirement:
        extra_config["python_requirement"] = python_requirement

    # Get wrknv configuration
    from wrknv.config import WorkenvConfig

    config = WorkenvConfig.load()
    env_config = config.get_env_config()

    # Use configuration from wrknv.toml if available
    if env_config:
        logger.info("Using env configuration from wrknv.toml", env_config=env_config)
        extra_config["include_tool_verification"] = env_config.get("include_tool_verification", True)

        # Handle new unified siblings format
        siblings_config = env_config.get("siblings", [])
        if siblings_config:
            extra_config["siblings"] = siblings_config
            # Keep backward compatibility
            extra_config["sibling_patterns"] = []
            extra_config["special_siblings"] = []
        else:
            # Backward compatibility with old format
            extra_config["sibling_patterns"] = env_config.get("sibling_patterns", )
            extra_config["special_siblings"] = env_config.get("special_siblings", [])
            extra_config["siblings"] = []
    else:
        # Default configuration - empty by default
        logger.info("No env configuration found in wrknv.toml, using defaults")
        extra_config["include_tool_verification"] = True
        extra_config["sibling_patterns"] = []
        extra_config["special_siblings"] = []
        extra_config["siblings"] = []

    # Generate scripts
    generator = EnvScriptGenerator()
    sh_path, ps1_path = generator.generate_both_scripts(project_name, project_dir, **extra_config)

    print_success(f"Generated {sh_path}", Emoji.SUCCESS)
    print_success(f"Generated {ps1_path}", Emoji.SUCCESS)

    return sh_path, ps1_path


def x_create_project_env_scripts__mutmut_84(project_dir: Path, workenv_name: str | None = None) -> tuple[Path, Path]:
    """Create environment scripts for a project based on its pyproject.toml.

    Args:
        project_dir: The project directory
        workenv_name: Optional workenv name override

    Returns:
        Tuple of (env.sh path, env.ps1 path)
    """
    pyproject_path = project_dir / "pyproject.toml"

    if not pyproject_path.exists():
        raise FileNotFoundError(f"No pyproject.toml found in {project_dir}")

    # Parse project name from pyproject.toml
    from provide.foundation.file.formats import read_toml

    pyproject = read_toml(pyproject_path)

    project_name = pyproject.get("project", {}).get("name", project_dir.name)
    python_requirement = pyproject.get("project", {}).get("requires-python")

    # Additional config for wrknv projects
    extra_config = {}
    if workenv_name:
        extra_config["workenv_name"] = workenv_name
        extra_config["venv_prefix"] = workenv_name

    # Add Python requirement if present
    if python_requirement:
        extra_config["python_requirement"] = python_requirement

    # Get wrknv configuration
    from wrknv.config import WorkenvConfig

    config = WorkenvConfig.load()
    env_config = config.get_env_config()

    # Use configuration from wrknv.toml if available
    if env_config:
        logger.info("Using env configuration from wrknv.toml", env_config=env_config)
        extra_config["include_tool_verification"] = env_config.get("include_tool_verification", True)

        # Handle new unified siblings format
        siblings_config = env_config.get("siblings", [])
        if siblings_config:
            extra_config["siblings"] = siblings_config
            # Keep backward compatibility
            extra_config["sibling_patterns"] = []
            extra_config["special_siblings"] = []
        else:
            # Backward compatibility with old format
            extra_config["sibling_patterns"] = env_config.get("XXsibling_patternsXX", [])
            extra_config["special_siblings"] = env_config.get("special_siblings", [])
            extra_config["siblings"] = []
    else:
        # Default configuration - empty by default
        logger.info("No env configuration found in wrknv.toml, using defaults")
        extra_config["include_tool_verification"] = True
        extra_config["sibling_patterns"] = []
        extra_config["special_siblings"] = []
        extra_config["siblings"] = []

    # Generate scripts
    generator = EnvScriptGenerator()
    sh_path, ps1_path = generator.generate_both_scripts(project_name, project_dir, **extra_config)

    print_success(f"Generated {sh_path}", Emoji.SUCCESS)
    print_success(f"Generated {ps1_path}", Emoji.SUCCESS)

    return sh_path, ps1_path


def x_create_project_env_scripts__mutmut_85(project_dir: Path, workenv_name: str | None = None) -> tuple[Path, Path]:
    """Create environment scripts for a project based on its pyproject.toml.

    Args:
        project_dir: The project directory
        workenv_name: Optional workenv name override

    Returns:
        Tuple of (env.sh path, env.ps1 path)
    """
    pyproject_path = project_dir / "pyproject.toml"

    if not pyproject_path.exists():
        raise FileNotFoundError(f"No pyproject.toml found in {project_dir}")

    # Parse project name from pyproject.toml
    from provide.foundation.file.formats import read_toml

    pyproject = read_toml(pyproject_path)

    project_name = pyproject.get("project", {}).get("name", project_dir.name)
    python_requirement = pyproject.get("project", {}).get("requires-python")

    # Additional config for wrknv projects
    extra_config = {}
    if workenv_name:
        extra_config["workenv_name"] = workenv_name
        extra_config["venv_prefix"] = workenv_name

    # Add Python requirement if present
    if python_requirement:
        extra_config["python_requirement"] = python_requirement

    # Get wrknv configuration
    from wrknv.config import WorkenvConfig

    config = WorkenvConfig.load()
    env_config = config.get_env_config()

    # Use configuration from wrknv.toml if available
    if env_config:
        logger.info("Using env configuration from wrknv.toml", env_config=env_config)
        extra_config["include_tool_verification"] = env_config.get("include_tool_verification", True)

        # Handle new unified siblings format
        siblings_config = env_config.get("siblings", [])
        if siblings_config:
            extra_config["siblings"] = siblings_config
            # Keep backward compatibility
            extra_config["sibling_patterns"] = []
            extra_config["special_siblings"] = []
        else:
            # Backward compatibility with old format
            extra_config["sibling_patterns"] = env_config.get("SIBLING_PATTERNS", [])
            extra_config["special_siblings"] = env_config.get("special_siblings", [])
            extra_config["siblings"] = []
    else:
        # Default configuration - empty by default
        logger.info("No env configuration found in wrknv.toml, using defaults")
        extra_config["include_tool_verification"] = True
        extra_config["sibling_patterns"] = []
        extra_config["special_siblings"] = []
        extra_config["siblings"] = []

    # Generate scripts
    generator = EnvScriptGenerator()
    sh_path, ps1_path = generator.generate_both_scripts(project_name, project_dir, **extra_config)

    print_success(f"Generated {sh_path}", Emoji.SUCCESS)
    print_success(f"Generated {ps1_path}", Emoji.SUCCESS)

    return sh_path, ps1_path


def x_create_project_env_scripts__mutmut_86(project_dir: Path, workenv_name: str | None = None) -> tuple[Path, Path]:
    """Create environment scripts for a project based on its pyproject.toml.

    Args:
        project_dir: The project directory
        workenv_name: Optional workenv name override

    Returns:
        Tuple of (env.sh path, env.ps1 path)
    """
    pyproject_path = project_dir / "pyproject.toml"

    if not pyproject_path.exists():
        raise FileNotFoundError(f"No pyproject.toml found in {project_dir}")

    # Parse project name from pyproject.toml
    from provide.foundation.file.formats import read_toml

    pyproject = read_toml(pyproject_path)

    project_name = pyproject.get("project", {}).get("name", project_dir.name)
    python_requirement = pyproject.get("project", {}).get("requires-python")

    # Additional config for wrknv projects
    extra_config = {}
    if workenv_name:
        extra_config["workenv_name"] = workenv_name
        extra_config["venv_prefix"] = workenv_name

    # Add Python requirement if present
    if python_requirement:
        extra_config["python_requirement"] = python_requirement

    # Get wrknv configuration
    from wrknv.config import WorkenvConfig

    config = WorkenvConfig.load()
    env_config = config.get_env_config()

    # Use configuration from wrknv.toml if available
    if env_config:
        logger.info("Using env configuration from wrknv.toml", env_config=env_config)
        extra_config["include_tool_verification"] = env_config.get("include_tool_verification", True)

        # Handle new unified siblings format
        siblings_config = env_config.get("siblings", [])
        if siblings_config:
            extra_config["siblings"] = siblings_config
            # Keep backward compatibility
            extra_config["sibling_patterns"] = []
            extra_config["special_siblings"] = []
        else:
            # Backward compatibility with old format
            extra_config["sibling_patterns"] = env_config.get("sibling_patterns", [])
            extra_config["special_siblings"] = None
            extra_config["siblings"] = []
    else:
        # Default configuration - empty by default
        logger.info("No env configuration found in wrknv.toml, using defaults")
        extra_config["include_tool_verification"] = True
        extra_config["sibling_patterns"] = []
        extra_config["special_siblings"] = []
        extra_config["siblings"] = []

    # Generate scripts
    generator = EnvScriptGenerator()
    sh_path, ps1_path = generator.generate_both_scripts(project_name, project_dir, **extra_config)

    print_success(f"Generated {sh_path}", Emoji.SUCCESS)
    print_success(f"Generated {ps1_path}", Emoji.SUCCESS)

    return sh_path, ps1_path


def x_create_project_env_scripts__mutmut_87(project_dir: Path, workenv_name: str | None = None) -> tuple[Path, Path]:
    """Create environment scripts for a project based on its pyproject.toml.

    Args:
        project_dir: The project directory
        workenv_name: Optional workenv name override

    Returns:
        Tuple of (env.sh path, env.ps1 path)
    """
    pyproject_path = project_dir / "pyproject.toml"

    if not pyproject_path.exists():
        raise FileNotFoundError(f"No pyproject.toml found in {project_dir}")

    # Parse project name from pyproject.toml
    from provide.foundation.file.formats import read_toml

    pyproject = read_toml(pyproject_path)

    project_name = pyproject.get("project", {}).get("name", project_dir.name)
    python_requirement = pyproject.get("project", {}).get("requires-python")

    # Additional config for wrknv projects
    extra_config = {}
    if workenv_name:
        extra_config["workenv_name"] = workenv_name
        extra_config["venv_prefix"] = workenv_name

    # Add Python requirement if present
    if python_requirement:
        extra_config["python_requirement"] = python_requirement

    # Get wrknv configuration
    from wrknv.config import WorkenvConfig

    config = WorkenvConfig.load()
    env_config = config.get_env_config()

    # Use configuration from wrknv.toml if available
    if env_config:
        logger.info("Using env configuration from wrknv.toml", env_config=env_config)
        extra_config["include_tool_verification"] = env_config.get("include_tool_verification", True)

        # Handle new unified siblings format
        siblings_config = env_config.get("siblings", [])
        if siblings_config:
            extra_config["siblings"] = siblings_config
            # Keep backward compatibility
            extra_config["sibling_patterns"] = []
            extra_config["special_siblings"] = []
        else:
            # Backward compatibility with old format
            extra_config["sibling_patterns"] = env_config.get("sibling_patterns", [])
            extra_config["XXspecial_siblingsXX"] = env_config.get("special_siblings", [])
            extra_config["siblings"] = []
    else:
        # Default configuration - empty by default
        logger.info("No env configuration found in wrknv.toml, using defaults")
        extra_config["include_tool_verification"] = True
        extra_config["sibling_patterns"] = []
        extra_config["special_siblings"] = []
        extra_config["siblings"] = []

    # Generate scripts
    generator = EnvScriptGenerator()
    sh_path, ps1_path = generator.generate_both_scripts(project_name, project_dir, **extra_config)

    print_success(f"Generated {sh_path}", Emoji.SUCCESS)
    print_success(f"Generated {ps1_path}", Emoji.SUCCESS)

    return sh_path, ps1_path


def x_create_project_env_scripts__mutmut_88(project_dir: Path, workenv_name: str | None = None) -> tuple[Path, Path]:
    """Create environment scripts for a project based on its pyproject.toml.

    Args:
        project_dir: The project directory
        workenv_name: Optional workenv name override

    Returns:
        Tuple of (env.sh path, env.ps1 path)
    """
    pyproject_path = project_dir / "pyproject.toml"

    if not pyproject_path.exists():
        raise FileNotFoundError(f"No pyproject.toml found in {project_dir}")

    # Parse project name from pyproject.toml
    from provide.foundation.file.formats import read_toml

    pyproject = read_toml(pyproject_path)

    project_name = pyproject.get("project", {}).get("name", project_dir.name)
    python_requirement = pyproject.get("project", {}).get("requires-python")

    # Additional config for wrknv projects
    extra_config = {}
    if workenv_name:
        extra_config["workenv_name"] = workenv_name
        extra_config["venv_prefix"] = workenv_name

    # Add Python requirement if present
    if python_requirement:
        extra_config["python_requirement"] = python_requirement

    # Get wrknv configuration
    from wrknv.config import WorkenvConfig

    config = WorkenvConfig.load()
    env_config = config.get_env_config()

    # Use configuration from wrknv.toml if available
    if env_config:
        logger.info("Using env configuration from wrknv.toml", env_config=env_config)
        extra_config["include_tool_verification"] = env_config.get("include_tool_verification", True)

        # Handle new unified siblings format
        siblings_config = env_config.get("siblings", [])
        if siblings_config:
            extra_config["siblings"] = siblings_config
            # Keep backward compatibility
            extra_config["sibling_patterns"] = []
            extra_config["special_siblings"] = []
        else:
            # Backward compatibility with old format
            extra_config["sibling_patterns"] = env_config.get("sibling_patterns", [])
            extra_config["SPECIAL_SIBLINGS"] = env_config.get("special_siblings", [])
            extra_config["siblings"] = []
    else:
        # Default configuration - empty by default
        logger.info("No env configuration found in wrknv.toml, using defaults")
        extra_config["include_tool_verification"] = True
        extra_config["sibling_patterns"] = []
        extra_config["special_siblings"] = []
        extra_config["siblings"] = []

    # Generate scripts
    generator = EnvScriptGenerator()
    sh_path, ps1_path = generator.generate_both_scripts(project_name, project_dir, **extra_config)

    print_success(f"Generated {sh_path}", Emoji.SUCCESS)
    print_success(f"Generated {ps1_path}", Emoji.SUCCESS)

    return sh_path, ps1_path


def x_create_project_env_scripts__mutmut_89(project_dir: Path, workenv_name: str | None = None) -> tuple[Path, Path]:
    """Create environment scripts for a project based on its pyproject.toml.

    Args:
        project_dir: The project directory
        workenv_name: Optional workenv name override

    Returns:
        Tuple of (env.sh path, env.ps1 path)
    """
    pyproject_path = project_dir / "pyproject.toml"

    if not pyproject_path.exists():
        raise FileNotFoundError(f"No pyproject.toml found in {project_dir}")

    # Parse project name from pyproject.toml
    from provide.foundation.file.formats import read_toml

    pyproject = read_toml(pyproject_path)

    project_name = pyproject.get("project", {}).get("name", project_dir.name)
    python_requirement = pyproject.get("project", {}).get("requires-python")

    # Additional config for wrknv projects
    extra_config = {}
    if workenv_name:
        extra_config["workenv_name"] = workenv_name
        extra_config["venv_prefix"] = workenv_name

    # Add Python requirement if present
    if python_requirement:
        extra_config["python_requirement"] = python_requirement

    # Get wrknv configuration
    from wrknv.config import WorkenvConfig

    config = WorkenvConfig.load()
    env_config = config.get_env_config()

    # Use configuration from wrknv.toml if available
    if env_config:
        logger.info("Using env configuration from wrknv.toml", env_config=env_config)
        extra_config["include_tool_verification"] = env_config.get("include_tool_verification", True)

        # Handle new unified siblings format
        siblings_config = env_config.get("siblings", [])
        if siblings_config:
            extra_config["siblings"] = siblings_config
            # Keep backward compatibility
            extra_config["sibling_patterns"] = []
            extra_config["special_siblings"] = []
        else:
            # Backward compatibility with old format
            extra_config["sibling_patterns"] = env_config.get("sibling_patterns", [])
            extra_config["special_siblings"] = env_config.get(None, [])
            extra_config["siblings"] = []
    else:
        # Default configuration - empty by default
        logger.info("No env configuration found in wrknv.toml, using defaults")
        extra_config["include_tool_verification"] = True
        extra_config["sibling_patterns"] = []
        extra_config["special_siblings"] = []
        extra_config["siblings"] = []

    # Generate scripts
    generator = EnvScriptGenerator()
    sh_path, ps1_path = generator.generate_both_scripts(project_name, project_dir, **extra_config)

    print_success(f"Generated {sh_path}", Emoji.SUCCESS)
    print_success(f"Generated {ps1_path}", Emoji.SUCCESS)

    return sh_path, ps1_path


def x_create_project_env_scripts__mutmut_90(project_dir: Path, workenv_name: str | None = None) -> tuple[Path, Path]:
    """Create environment scripts for a project based on its pyproject.toml.

    Args:
        project_dir: The project directory
        workenv_name: Optional workenv name override

    Returns:
        Tuple of (env.sh path, env.ps1 path)
    """
    pyproject_path = project_dir / "pyproject.toml"

    if not pyproject_path.exists():
        raise FileNotFoundError(f"No pyproject.toml found in {project_dir}")

    # Parse project name from pyproject.toml
    from provide.foundation.file.formats import read_toml

    pyproject = read_toml(pyproject_path)

    project_name = pyproject.get("project", {}).get("name", project_dir.name)
    python_requirement = pyproject.get("project", {}).get("requires-python")

    # Additional config for wrknv projects
    extra_config = {}
    if workenv_name:
        extra_config["workenv_name"] = workenv_name
        extra_config["venv_prefix"] = workenv_name

    # Add Python requirement if present
    if python_requirement:
        extra_config["python_requirement"] = python_requirement

    # Get wrknv configuration
    from wrknv.config import WorkenvConfig

    config = WorkenvConfig.load()
    env_config = config.get_env_config()

    # Use configuration from wrknv.toml if available
    if env_config:
        logger.info("Using env configuration from wrknv.toml", env_config=env_config)
        extra_config["include_tool_verification"] = env_config.get("include_tool_verification", True)

        # Handle new unified siblings format
        siblings_config = env_config.get("siblings", [])
        if siblings_config:
            extra_config["siblings"] = siblings_config
            # Keep backward compatibility
            extra_config["sibling_patterns"] = []
            extra_config["special_siblings"] = []
        else:
            # Backward compatibility with old format
            extra_config["sibling_patterns"] = env_config.get("sibling_patterns", [])
            extra_config["special_siblings"] = env_config.get("special_siblings", None)
            extra_config["siblings"] = []
    else:
        # Default configuration - empty by default
        logger.info("No env configuration found in wrknv.toml, using defaults")
        extra_config["include_tool_verification"] = True
        extra_config["sibling_patterns"] = []
        extra_config["special_siblings"] = []
        extra_config["siblings"] = []

    # Generate scripts
    generator = EnvScriptGenerator()
    sh_path, ps1_path = generator.generate_both_scripts(project_name, project_dir, **extra_config)

    print_success(f"Generated {sh_path}", Emoji.SUCCESS)
    print_success(f"Generated {ps1_path}", Emoji.SUCCESS)

    return sh_path, ps1_path


def x_create_project_env_scripts__mutmut_91(project_dir: Path, workenv_name: str | None = None) -> tuple[Path, Path]:
    """Create environment scripts for a project based on its pyproject.toml.

    Args:
        project_dir: The project directory
        workenv_name: Optional workenv name override

    Returns:
        Tuple of (env.sh path, env.ps1 path)
    """
    pyproject_path = project_dir / "pyproject.toml"

    if not pyproject_path.exists():
        raise FileNotFoundError(f"No pyproject.toml found in {project_dir}")

    # Parse project name from pyproject.toml
    from provide.foundation.file.formats import read_toml

    pyproject = read_toml(pyproject_path)

    project_name = pyproject.get("project", {}).get("name", project_dir.name)
    python_requirement = pyproject.get("project", {}).get("requires-python")

    # Additional config for wrknv projects
    extra_config = {}
    if workenv_name:
        extra_config["workenv_name"] = workenv_name
        extra_config["venv_prefix"] = workenv_name

    # Add Python requirement if present
    if python_requirement:
        extra_config["python_requirement"] = python_requirement

    # Get wrknv configuration
    from wrknv.config import WorkenvConfig

    config = WorkenvConfig.load()
    env_config = config.get_env_config()

    # Use configuration from wrknv.toml if available
    if env_config:
        logger.info("Using env configuration from wrknv.toml", env_config=env_config)
        extra_config["include_tool_verification"] = env_config.get("include_tool_verification", True)

        # Handle new unified siblings format
        siblings_config = env_config.get("siblings", [])
        if siblings_config:
            extra_config["siblings"] = siblings_config
            # Keep backward compatibility
            extra_config["sibling_patterns"] = []
            extra_config["special_siblings"] = []
        else:
            # Backward compatibility with old format
            extra_config["sibling_patterns"] = env_config.get("sibling_patterns", [])
            extra_config["special_siblings"] = env_config.get([])
            extra_config["siblings"] = []
    else:
        # Default configuration - empty by default
        logger.info("No env configuration found in wrknv.toml, using defaults")
        extra_config["include_tool_verification"] = True
        extra_config["sibling_patterns"] = []
        extra_config["special_siblings"] = []
        extra_config["siblings"] = []

    # Generate scripts
    generator = EnvScriptGenerator()
    sh_path, ps1_path = generator.generate_both_scripts(project_name, project_dir, **extra_config)

    print_success(f"Generated {sh_path}", Emoji.SUCCESS)
    print_success(f"Generated {ps1_path}", Emoji.SUCCESS)

    return sh_path, ps1_path


def x_create_project_env_scripts__mutmut_92(project_dir: Path, workenv_name: str | None = None) -> tuple[Path, Path]:
    """Create environment scripts for a project based on its pyproject.toml.

    Args:
        project_dir: The project directory
        workenv_name: Optional workenv name override

    Returns:
        Tuple of (env.sh path, env.ps1 path)
    """
    pyproject_path = project_dir / "pyproject.toml"

    if not pyproject_path.exists():
        raise FileNotFoundError(f"No pyproject.toml found in {project_dir}")

    # Parse project name from pyproject.toml
    from provide.foundation.file.formats import read_toml

    pyproject = read_toml(pyproject_path)

    project_name = pyproject.get("project", {}).get("name", project_dir.name)
    python_requirement = pyproject.get("project", {}).get("requires-python")

    # Additional config for wrknv projects
    extra_config = {}
    if workenv_name:
        extra_config["workenv_name"] = workenv_name
        extra_config["venv_prefix"] = workenv_name

    # Add Python requirement if present
    if python_requirement:
        extra_config["python_requirement"] = python_requirement

    # Get wrknv configuration
    from wrknv.config import WorkenvConfig

    config = WorkenvConfig.load()
    env_config = config.get_env_config()

    # Use configuration from wrknv.toml if available
    if env_config:
        logger.info("Using env configuration from wrknv.toml", env_config=env_config)
        extra_config["include_tool_verification"] = env_config.get("include_tool_verification", True)

        # Handle new unified siblings format
        siblings_config = env_config.get("siblings", [])
        if siblings_config:
            extra_config["siblings"] = siblings_config
            # Keep backward compatibility
            extra_config["sibling_patterns"] = []
            extra_config["special_siblings"] = []
        else:
            # Backward compatibility with old format
            extra_config["sibling_patterns"] = env_config.get("sibling_patterns", [])
            extra_config["special_siblings"] = env_config.get("special_siblings", )
            extra_config["siblings"] = []
    else:
        # Default configuration - empty by default
        logger.info("No env configuration found in wrknv.toml, using defaults")
        extra_config["include_tool_verification"] = True
        extra_config["sibling_patterns"] = []
        extra_config["special_siblings"] = []
        extra_config["siblings"] = []

    # Generate scripts
    generator = EnvScriptGenerator()
    sh_path, ps1_path = generator.generate_both_scripts(project_name, project_dir, **extra_config)

    print_success(f"Generated {sh_path}", Emoji.SUCCESS)
    print_success(f"Generated {ps1_path}", Emoji.SUCCESS)

    return sh_path, ps1_path


def x_create_project_env_scripts__mutmut_93(project_dir: Path, workenv_name: str | None = None) -> tuple[Path, Path]:
    """Create environment scripts for a project based on its pyproject.toml.

    Args:
        project_dir: The project directory
        workenv_name: Optional workenv name override

    Returns:
        Tuple of (env.sh path, env.ps1 path)
    """
    pyproject_path = project_dir / "pyproject.toml"

    if not pyproject_path.exists():
        raise FileNotFoundError(f"No pyproject.toml found in {project_dir}")

    # Parse project name from pyproject.toml
    from provide.foundation.file.formats import read_toml

    pyproject = read_toml(pyproject_path)

    project_name = pyproject.get("project", {}).get("name", project_dir.name)
    python_requirement = pyproject.get("project", {}).get("requires-python")

    # Additional config for wrknv projects
    extra_config = {}
    if workenv_name:
        extra_config["workenv_name"] = workenv_name
        extra_config["venv_prefix"] = workenv_name

    # Add Python requirement if present
    if python_requirement:
        extra_config["python_requirement"] = python_requirement

    # Get wrknv configuration
    from wrknv.config import WorkenvConfig

    config = WorkenvConfig.load()
    env_config = config.get_env_config()

    # Use configuration from wrknv.toml if available
    if env_config:
        logger.info("Using env configuration from wrknv.toml", env_config=env_config)
        extra_config["include_tool_verification"] = env_config.get("include_tool_verification", True)

        # Handle new unified siblings format
        siblings_config = env_config.get("siblings", [])
        if siblings_config:
            extra_config["siblings"] = siblings_config
            # Keep backward compatibility
            extra_config["sibling_patterns"] = []
            extra_config["special_siblings"] = []
        else:
            # Backward compatibility with old format
            extra_config["sibling_patterns"] = env_config.get("sibling_patterns", [])
            extra_config["special_siblings"] = env_config.get("XXspecial_siblingsXX", [])
            extra_config["siblings"] = []
    else:
        # Default configuration - empty by default
        logger.info("No env configuration found in wrknv.toml, using defaults")
        extra_config["include_tool_verification"] = True
        extra_config["sibling_patterns"] = []
        extra_config["special_siblings"] = []
        extra_config["siblings"] = []

    # Generate scripts
    generator = EnvScriptGenerator()
    sh_path, ps1_path = generator.generate_both_scripts(project_name, project_dir, **extra_config)

    print_success(f"Generated {sh_path}", Emoji.SUCCESS)
    print_success(f"Generated {ps1_path}", Emoji.SUCCESS)

    return sh_path, ps1_path


def x_create_project_env_scripts__mutmut_94(project_dir: Path, workenv_name: str | None = None) -> tuple[Path, Path]:
    """Create environment scripts for a project based on its pyproject.toml.

    Args:
        project_dir: The project directory
        workenv_name: Optional workenv name override

    Returns:
        Tuple of (env.sh path, env.ps1 path)
    """
    pyproject_path = project_dir / "pyproject.toml"

    if not pyproject_path.exists():
        raise FileNotFoundError(f"No pyproject.toml found in {project_dir}")

    # Parse project name from pyproject.toml
    from provide.foundation.file.formats import read_toml

    pyproject = read_toml(pyproject_path)

    project_name = pyproject.get("project", {}).get("name", project_dir.name)
    python_requirement = pyproject.get("project", {}).get("requires-python")

    # Additional config for wrknv projects
    extra_config = {}
    if workenv_name:
        extra_config["workenv_name"] = workenv_name
        extra_config["venv_prefix"] = workenv_name

    # Add Python requirement if present
    if python_requirement:
        extra_config["python_requirement"] = python_requirement

    # Get wrknv configuration
    from wrknv.config import WorkenvConfig

    config = WorkenvConfig.load()
    env_config = config.get_env_config()

    # Use configuration from wrknv.toml if available
    if env_config:
        logger.info("Using env configuration from wrknv.toml", env_config=env_config)
        extra_config["include_tool_verification"] = env_config.get("include_tool_verification", True)

        # Handle new unified siblings format
        siblings_config = env_config.get("siblings", [])
        if siblings_config:
            extra_config["siblings"] = siblings_config
            # Keep backward compatibility
            extra_config["sibling_patterns"] = []
            extra_config["special_siblings"] = []
        else:
            # Backward compatibility with old format
            extra_config["sibling_patterns"] = env_config.get("sibling_patterns", [])
            extra_config["special_siblings"] = env_config.get("SPECIAL_SIBLINGS", [])
            extra_config["siblings"] = []
    else:
        # Default configuration - empty by default
        logger.info("No env configuration found in wrknv.toml, using defaults")
        extra_config["include_tool_verification"] = True
        extra_config["sibling_patterns"] = []
        extra_config["special_siblings"] = []
        extra_config["siblings"] = []

    # Generate scripts
    generator = EnvScriptGenerator()
    sh_path, ps1_path = generator.generate_both_scripts(project_name, project_dir, **extra_config)

    print_success(f"Generated {sh_path}", Emoji.SUCCESS)
    print_success(f"Generated {ps1_path}", Emoji.SUCCESS)

    return sh_path, ps1_path


def x_create_project_env_scripts__mutmut_95(project_dir: Path, workenv_name: str | None = None) -> tuple[Path, Path]:
    """Create environment scripts for a project based on its pyproject.toml.

    Args:
        project_dir: The project directory
        workenv_name: Optional workenv name override

    Returns:
        Tuple of (env.sh path, env.ps1 path)
    """
    pyproject_path = project_dir / "pyproject.toml"

    if not pyproject_path.exists():
        raise FileNotFoundError(f"No pyproject.toml found in {project_dir}")

    # Parse project name from pyproject.toml
    from provide.foundation.file.formats import read_toml

    pyproject = read_toml(pyproject_path)

    project_name = pyproject.get("project", {}).get("name", project_dir.name)
    python_requirement = pyproject.get("project", {}).get("requires-python")

    # Additional config for wrknv projects
    extra_config = {}
    if workenv_name:
        extra_config["workenv_name"] = workenv_name
        extra_config["venv_prefix"] = workenv_name

    # Add Python requirement if present
    if python_requirement:
        extra_config["python_requirement"] = python_requirement

    # Get wrknv configuration
    from wrknv.config import WorkenvConfig

    config = WorkenvConfig.load()
    env_config = config.get_env_config()

    # Use configuration from wrknv.toml if available
    if env_config:
        logger.info("Using env configuration from wrknv.toml", env_config=env_config)
        extra_config["include_tool_verification"] = env_config.get("include_tool_verification", True)

        # Handle new unified siblings format
        siblings_config = env_config.get("siblings", [])
        if siblings_config:
            extra_config["siblings"] = siblings_config
            # Keep backward compatibility
            extra_config["sibling_patterns"] = []
            extra_config["special_siblings"] = []
        else:
            # Backward compatibility with old format
            extra_config["sibling_patterns"] = env_config.get("sibling_patterns", [])
            extra_config["special_siblings"] = env_config.get("special_siblings", [])
            extra_config["siblings"] = None
    else:
        # Default configuration - empty by default
        logger.info("No env configuration found in wrknv.toml, using defaults")
        extra_config["include_tool_verification"] = True
        extra_config["sibling_patterns"] = []
        extra_config["special_siblings"] = []
        extra_config["siblings"] = []

    # Generate scripts
    generator = EnvScriptGenerator()
    sh_path, ps1_path = generator.generate_both_scripts(project_name, project_dir, **extra_config)

    print_success(f"Generated {sh_path}", Emoji.SUCCESS)
    print_success(f"Generated {ps1_path}", Emoji.SUCCESS)

    return sh_path, ps1_path


def x_create_project_env_scripts__mutmut_96(project_dir: Path, workenv_name: str | None = None) -> tuple[Path, Path]:
    """Create environment scripts for a project based on its pyproject.toml.

    Args:
        project_dir: The project directory
        workenv_name: Optional workenv name override

    Returns:
        Tuple of (env.sh path, env.ps1 path)
    """
    pyproject_path = project_dir / "pyproject.toml"

    if not pyproject_path.exists():
        raise FileNotFoundError(f"No pyproject.toml found in {project_dir}")

    # Parse project name from pyproject.toml
    from provide.foundation.file.formats import read_toml

    pyproject = read_toml(pyproject_path)

    project_name = pyproject.get("project", {}).get("name", project_dir.name)
    python_requirement = pyproject.get("project", {}).get("requires-python")

    # Additional config for wrknv projects
    extra_config = {}
    if workenv_name:
        extra_config["workenv_name"] = workenv_name
        extra_config["venv_prefix"] = workenv_name

    # Add Python requirement if present
    if python_requirement:
        extra_config["python_requirement"] = python_requirement

    # Get wrknv configuration
    from wrknv.config import WorkenvConfig

    config = WorkenvConfig.load()
    env_config = config.get_env_config()

    # Use configuration from wrknv.toml if available
    if env_config:
        logger.info("Using env configuration from wrknv.toml", env_config=env_config)
        extra_config["include_tool_verification"] = env_config.get("include_tool_verification", True)

        # Handle new unified siblings format
        siblings_config = env_config.get("siblings", [])
        if siblings_config:
            extra_config["siblings"] = siblings_config
            # Keep backward compatibility
            extra_config["sibling_patterns"] = []
            extra_config["special_siblings"] = []
        else:
            # Backward compatibility with old format
            extra_config["sibling_patterns"] = env_config.get("sibling_patterns", [])
            extra_config["special_siblings"] = env_config.get("special_siblings", [])
            extra_config["XXsiblingsXX"] = []
    else:
        # Default configuration - empty by default
        logger.info("No env configuration found in wrknv.toml, using defaults")
        extra_config["include_tool_verification"] = True
        extra_config["sibling_patterns"] = []
        extra_config["special_siblings"] = []
        extra_config["siblings"] = []

    # Generate scripts
    generator = EnvScriptGenerator()
    sh_path, ps1_path = generator.generate_both_scripts(project_name, project_dir, **extra_config)

    print_success(f"Generated {sh_path}", Emoji.SUCCESS)
    print_success(f"Generated {ps1_path}", Emoji.SUCCESS)

    return sh_path, ps1_path


def x_create_project_env_scripts__mutmut_97(project_dir: Path, workenv_name: str | None = None) -> tuple[Path, Path]:
    """Create environment scripts for a project based on its pyproject.toml.

    Args:
        project_dir: The project directory
        workenv_name: Optional workenv name override

    Returns:
        Tuple of (env.sh path, env.ps1 path)
    """
    pyproject_path = project_dir / "pyproject.toml"

    if not pyproject_path.exists():
        raise FileNotFoundError(f"No pyproject.toml found in {project_dir}")

    # Parse project name from pyproject.toml
    from provide.foundation.file.formats import read_toml

    pyproject = read_toml(pyproject_path)

    project_name = pyproject.get("project", {}).get("name", project_dir.name)
    python_requirement = pyproject.get("project", {}).get("requires-python")

    # Additional config for wrknv projects
    extra_config = {}
    if workenv_name:
        extra_config["workenv_name"] = workenv_name
        extra_config["venv_prefix"] = workenv_name

    # Add Python requirement if present
    if python_requirement:
        extra_config["python_requirement"] = python_requirement

    # Get wrknv configuration
    from wrknv.config import WorkenvConfig

    config = WorkenvConfig.load()
    env_config = config.get_env_config()

    # Use configuration from wrknv.toml if available
    if env_config:
        logger.info("Using env configuration from wrknv.toml", env_config=env_config)
        extra_config["include_tool_verification"] = env_config.get("include_tool_verification", True)

        # Handle new unified siblings format
        siblings_config = env_config.get("siblings", [])
        if siblings_config:
            extra_config["siblings"] = siblings_config
            # Keep backward compatibility
            extra_config["sibling_patterns"] = []
            extra_config["special_siblings"] = []
        else:
            # Backward compatibility with old format
            extra_config["sibling_patterns"] = env_config.get("sibling_patterns", [])
            extra_config["special_siblings"] = env_config.get("special_siblings", [])
            extra_config["SIBLINGS"] = []
    else:
        # Default configuration - empty by default
        logger.info("No env configuration found in wrknv.toml, using defaults")
        extra_config["include_tool_verification"] = True
        extra_config["sibling_patterns"] = []
        extra_config["special_siblings"] = []
        extra_config["siblings"] = []

    # Generate scripts
    generator = EnvScriptGenerator()
    sh_path, ps1_path = generator.generate_both_scripts(project_name, project_dir, **extra_config)

    print_success(f"Generated {sh_path}", Emoji.SUCCESS)
    print_success(f"Generated {ps1_path}", Emoji.SUCCESS)

    return sh_path, ps1_path


def x_create_project_env_scripts__mutmut_98(project_dir: Path, workenv_name: str | None = None) -> tuple[Path, Path]:
    """Create environment scripts for a project based on its pyproject.toml.

    Args:
        project_dir: The project directory
        workenv_name: Optional workenv name override

    Returns:
        Tuple of (env.sh path, env.ps1 path)
    """
    pyproject_path = project_dir / "pyproject.toml"

    if not pyproject_path.exists():
        raise FileNotFoundError(f"No pyproject.toml found in {project_dir}")

    # Parse project name from pyproject.toml
    from provide.foundation.file.formats import read_toml

    pyproject = read_toml(pyproject_path)

    project_name = pyproject.get("project", {}).get("name", project_dir.name)
    python_requirement = pyproject.get("project", {}).get("requires-python")

    # Additional config for wrknv projects
    extra_config = {}
    if workenv_name:
        extra_config["workenv_name"] = workenv_name
        extra_config["venv_prefix"] = workenv_name

    # Add Python requirement if present
    if python_requirement:
        extra_config["python_requirement"] = python_requirement

    # Get wrknv configuration
    from wrknv.config import WorkenvConfig

    config = WorkenvConfig.load()
    env_config = config.get_env_config()

    # Use configuration from wrknv.toml if available
    if env_config:
        logger.info("Using env configuration from wrknv.toml", env_config=env_config)
        extra_config["include_tool_verification"] = env_config.get("include_tool_verification", True)

        # Handle new unified siblings format
        siblings_config = env_config.get("siblings", [])
        if siblings_config:
            extra_config["siblings"] = siblings_config
            # Keep backward compatibility
            extra_config["sibling_patterns"] = []
            extra_config["special_siblings"] = []
        else:
            # Backward compatibility with old format
            extra_config["sibling_patterns"] = env_config.get("sibling_patterns", [])
            extra_config["special_siblings"] = env_config.get("special_siblings", [])
            extra_config["siblings"] = []
    else:
        # Default configuration - empty by default
        logger.info(None)
        extra_config["include_tool_verification"] = True
        extra_config["sibling_patterns"] = []
        extra_config["special_siblings"] = []
        extra_config["siblings"] = []

    # Generate scripts
    generator = EnvScriptGenerator()
    sh_path, ps1_path = generator.generate_both_scripts(project_name, project_dir, **extra_config)

    print_success(f"Generated {sh_path}", Emoji.SUCCESS)
    print_success(f"Generated {ps1_path}", Emoji.SUCCESS)

    return sh_path, ps1_path


def x_create_project_env_scripts__mutmut_99(project_dir: Path, workenv_name: str | None = None) -> tuple[Path, Path]:
    """Create environment scripts for a project based on its pyproject.toml.

    Args:
        project_dir: The project directory
        workenv_name: Optional workenv name override

    Returns:
        Tuple of (env.sh path, env.ps1 path)
    """
    pyproject_path = project_dir / "pyproject.toml"

    if not pyproject_path.exists():
        raise FileNotFoundError(f"No pyproject.toml found in {project_dir}")

    # Parse project name from pyproject.toml
    from provide.foundation.file.formats import read_toml

    pyproject = read_toml(pyproject_path)

    project_name = pyproject.get("project", {}).get("name", project_dir.name)
    python_requirement = pyproject.get("project", {}).get("requires-python")

    # Additional config for wrknv projects
    extra_config = {}
    if workenv_name:
        extra_config["workenv_name"] = workenv_name
        extra_config["venv_prefix"] = workenv_name

    # Add Python requirement if present
    if python_requirement:
        extra_config["python_requirement"] = python_requirement

    # Get wrknv configuration
    from wrknv.config import WorkenvConfig

    config = WorkenvConfig.load()
    env_config = config.get_env_config()

    # Use configuration from wrknv.toml if available
    if env_config:
        logger.info("Using env configuration from wrknv.toml", env_config=env_config)
        extra_config["include_tool_verification"] = env_config.get("include_tool_verification", True)

        # Handle new unified siblings format
        siblings_config = env_config.get("siblings", [])
        if siblings_config:
            extra_config["siblings"] = siblings_config
            # Keep backward compatibility
            extra_config["sibling_patterns"] = []
            extra_config["special_siblings"] = []
        else:
            # Backward compatibility with old format
            extra_config["sibling_patterns"] = env_config.get("sibling_patterns", [])
            extra_config["special_siblings"] = env_config.get("special_siblings", [])
            extra_config["siblings"] = []
    else:
        # Default configuration - empty by default
        logger.info("XXNo env configuration found in wrknv.toml, using defaultsXX")
        extra_config["include_tool_verification"] = True
        extra_config["sibling_patterns"] = []
        extra_config["special_siblings"] = []
        extra_config["siblings"] = []

    # Generate scripts
    generator = EnvScriptGenerator()
    sh_path, ps1_path = generator.generate_both_scripts(project_name, project_dir, **extra_config)

    print_success(f"Generated {sh_path}", Emoji.SUCCESS)
    print_success(f"Generated {ps1_path}", Emoji.SUCCESS)

    return sh_path, ps1_path


def x_create_project_env_scripts__mutmut_100(project_dir: Path, workenv_name: str | None = None) -> tuple[Path, Path]:
    """Create environment scripts for a project based on its pyproject.toml.

    Args:
        project_dir: The project directory
        workenv_name: Optional workenv name override

    Returns:
        Tuple of (env.sh path, env.ps1 path)
    """
    pyproject_path = project_dir / "pyproject.toml"

    if not pyproject_path.exists():
        raise FileNotFoundError(f"No pyproject.toml found in {project_dir}")

    # Parse project name from pyproject.toml
    from provide.foundation.file.formats import read_toml

    pyproject = read_toml(pyproject_path)

    project_name = pyproject.get("project", {}).get("name", project_dir.name)
    python_requirement = pyproject.get("project", {}).get("requires-python")

    # Additional config for wrknv projects
    extra_config = {}
    if workenv_name:
        extra_config["workenv_name"] = workenv_name
        extra_config["venv_prefix"] = workenv_name

    # Add Python requirement if present
    if python_requirement:
        extra_config["python_requirement"] = python_requirement

    # Get wrknv configuration
    from wrknv.config import WorkenvConfig

    config = WorkenvConfig.load()
    env_config = config.get_env_config()

    # Use configuration from wrknv.toml if available
    if env_config:
        logger.info("Using env configuration from wrknv.toml", env_config=env_config)
        extra_config["include_tool_verification"] = env_config.get("include_tool_verification", True)

        # Handle new unified siblings format
        siblings_config = env_config.get("siblings", [])
        if siblings_config:
            extra_config["siblings"] = siblings_config
            # Keep backward compatibility
            extra_config["sibling_patterns"] = []
            extra_config["special_siblings"] = []
        else:
            # Backward compatibility with old format
            extra_config["sibling_patterns"] = env_config.get("sibling_patterns", [])
            extra_config["special_siblings"] = env_config.get("special_siblings", [])
            extra_config["siblings"] = []
    else:
        # Default configuration - empty by default
        logger.info("no env configuration found in wrknv.toml, using defaults")
        extra_config["include_tool_verification"] = True
        extra_config["sibling_patterns"] = []
        extra_config["special_siblings"] = []
        extra_config["siblings"] = []

    # Generate scripts
    generator = EnvScriptGenerator()
    sh_path, ps1_path = generator.generate_both_scripts(project_name, project_dir, **extra_config)

    print_success(f"Generated {sh_path}", Emoji.SUCCESS)
    print_success(f"Generated {ps1_path}", Emoji.SUCCESS)

    return sh_path, ps1_path


def x_create_project_env_scripts__mutmut_101(project_dir: Path, workenv_name: str | None = None) -> tuple[Path, Path]:
    """Create environment scripts for a project based on its pyproject.toml.

    Args:
        project_dir: The project directory
        workenv_name: Optional workenv name override

    Returns:
        Tuple of (env.sh path, env.ps1 path)
    """
    pyproject_path = project_dir / "pyproject.toml"

    if not pyproject_path.exists():
        raise FileNotFoundError(f"No pyproject.toml found in {project_dir}")

    # Parse project name from pyproject.toml
    from provide.foundation.file.formats import read_toml

    pyproject = read_toml(pyproject_path)

    project_name = pyproject.get("project", {}).get("name", project_dir.name)
    python_requirement = pyproject.get("project", {}).get("requires-python")

    # Additional config for wrknv projects
    extra_config = {}
    if workenv_name:
        extra_config["workenv_name"] = workenv_name
        extra_config["venv_prefix"] = workenv_name

    # Add Python requirement if present
    if python_requirement:
        extra_config["python_requirement"] = python_requirement

    # Get wrknv configuration
    from wrknv.config import WorkenvConfig

    config = WorkenvConfig.load()
    env_config = config.get_env_config()

    # Use configuration from wrknv.toml if available
    if env_config:
        logger.info("Using env configuration from wrknv.toml", env_config=env_config)
        extra_config["include_tool_verification"] = env_config.get("include_tool_verification", True)

        # Handle new unified siblings format
        siblings_config = env_config.get("siblings", [])
        if siblings_config:
            extra_config["siblings"] = siblings_config
            # Keep backward compatibility
            extra_config["sibling_patterns"] = []
            extra_config["special_siblings"] = []
        else:
            # Backward compatibility with old format
            extra_config["sibling_patterns"] = env_config.get("sibling_patterns", [])
            extra_config["special_siblings"] = env_config.get("special_siblings", [])
            extra_config["siblings"] = []
    else:
        # Default configuration - empty by default
        logger.info("NO ENV CONFIGURATION FOUND IN WRKNV.TOML, USING DEFAULTS")
        extra_config["include_tool_verification"] = True
        extra_config["sibling_patterns"] = []
        extra_config["special_siblings"] = []
        extra_config["siblings"] = []

    # Generate scripts
    generator = EnvScriptGenerator()
    sh_path, ps1_path = generator.generate_both_scripts(project_name, project_dir, **extra_config)

    print_success(f"Generated {sh_path}", Emoji.SUCCESS)
    print_success(f"Generated {ps1_path}", Emoji.SUCCESS)

    return sh_path, ps1_path


def x_create_project_env_scripts__mutmut_102(project_dir: Path, workenv_name: str | None = None) -> tuple[Path, Path]:
    """Create environment scripts for a project based on its pyproject.toml.

    Args:
        project_dir: The project directory
        workenv_name: Optional workenv name override

    Returns:
        Tuple of (env.sh path, env.ps1 path)
    """
    pyproject_path = project_dir / "pyproject.toml"

    if not pyproject_path.exists():
        raise FileNotFoundError(f"No pyproject.toml found in {project_dir}")

    # Parse project name from pyproject.toml
    from provide.foundation.file.formats import read_toml

    pyproject = read_toml(pyproject_path)

    project_name = pyproject.get("project", {}).get("name", project_dir.name)
    python_requirement = pyproject.get("project", {}).get("requires-python")

    # Additional config for wrknv projects
    extra_config = {}
    if workenv_name:
        extra_config["workenv_name"] = workenv_name
        extra_config["venv_prefix"] = workenv_name

    # Add Python requirement if present
    if python_requirement:
        extra_config["python_requirement"] = python_requirement

    # Get wrknv configuration
    from wrknv.config import WorkenvConfig

    config = WorkenvConfig.load()
    env_config = config.get_env_config()

    # Use configuration from wrknv.toml if available
    if env_config:
        logger.info("Using env configuration from wrknv.toml", env_config=env_config)
        extra_config["include_tool_verification"] = env_config.get("include_tool_verification", True)

        # Handle new unified siblings format
        siblings_config = env_config.get("siblings", [])
        if siblings_config:
            extra_config["siblings"] = siblings_config
            # Keep backward compatibility
            extra_config["sibling_patterns"] = []
            extra_config["special_siblings"] = []
        else:
            # Backward compatibility with old format
            extra_config["sibling_patterns"] = env_config.get("sibling_patterns", [])
            extra_config["special_siblings"] = env_config.get("special_siblings", [])
            extra_config["siblings"] = []
    else:
        # Default configuration - empty by default
        logger.info("No env configuration found in wrknv.toml, using defaults")
        extra_config["include_tool_verification"] = None
        extra_config["sibling_patterns"] = []
        extra_config["special_siblings"] = []
        extra_config["siblings"] = []

    # Generate scripts
    generator = EnvScriptGenerator()
    sh_path, ps1_path = generator.generate_both_scripts(project_name, project_dir, **extra_config)

    print_success(f"Generated {sh_path}", Emoji.SUCCESS)
    print_success(f"Generated {ps1_path}", Emoji.SUCCESS)

    return sh_path, ps1_path


def x_create_project_env_scripts__mutmut_103(project_dir: Path, workenv_name: str | None = None) -> tuple[Path, Path]:
    """Create environment scripts for a project based on its pyproject.toml.

    Args:
        project_dir: The project directory
        workenv_name: Optional workenv name override

    Returns:
        Tuple of (env.sh path, env.ps1 path)
    """
    pyproject_path = project_dir / "pyproject.toml"

    if not pyproject_path.exists():
        raise FileNotFoundError(f"No pyproject.toml found in {project_dir}")

    # Parse project name from pyproject.toml
    from provide.foundation.file.formats import read_toml

    pyproject = read_toml(pyproject_path)

    project_name = pyproject.get("project", {}).get("name", project_dir.name)
    python_requirement = pyproject.get("project", {}).get("requires-python")

    # Additional config for wrknv projects
    extra_config = {}
    if workenv_name:
        extra_config["workenv_name"] = workenv_name
        extra_config["venv_prefix"] = workenv_name

    # Add Python requirement if present
    if python_requirement:
        extra_config["python_requirement"] = python_requirement

    # Get wrknv configuration
    from wrknv.config import WorkenvConfig

    config = WorkenvConfig.load()
    env_config = config.get_env_config()

    # Use configuration from wrknv.toml if available
    if env_config:
        logger.info("Using env configuration from wrknv.toml", env_config=env_config)
        extra_config["include_tool_verification"] = env_config.get("include_tool_verification", True)

        # Handle new unified siblings format
        siblings_config = env_config.get("siblings", [])
        if siblings_config:
            extra_config["siblings"] = siblings_config
            # Keep backward compatibility
            extra_config["sibling_patterns"] = []
            extra_config["special_siblings"] = []
        else:
            # Backward compatibility with old format
            extra_config["sibling_patterns"] = env_config.get("sibling_patterns", [])
            extra_config["special_siblings"] = env_config.get("special_siblings", [])
            extra_config["siblings"] = []
    else:
        # Default configuration - empty by default
        logger.info("No env configuration found in wrknv.toml, using defaults")
        extra_config["XXinclude_tool_verificationXX"] = True
        extra_config["sibling_patterns"] = []
        extra_config["special_siblings"] = []
        extra_config["siblings"] = []

    # Generate scripts
    generator = EnvScriptGenerator()
    sh_path, ps1_path = generator.generate_both_scripts(project_name, project_dir, **extra_config)

    print_success(f"Generated {sh_path}", Emoji.SUCCESS)
    print_success(f"Generated {ps1_path}", Emoji.SUCCESS)

    return sh_path, ps1_path


def x_create_project_env_scripts__mutmut_104(project_dir: Path, workenv_name: str | None = None) -> tuple[Path, Path]:
    """Create environment scripts for a project based on its pyproject.toml.

    Args:
        project_dir: The project directory
        workenv_name: Optional workenv name override

    Returns:
        Tuple of (env.sh path, env.ps1 path)
    """
    pyproject_path = project_dir / "pyproject.toml"

    if not pyproject_path.exists():
        raise FileNotFoundError(f"No pyproject.toml found in {project_dir}")

    # Parse project name from pyproject.toml
    from provide.foundation.file.formats import read_toml

    pyproject = read_toml(pyproject_path)

    project_name = pyproject.get("project", {}).get("name", project_dir.name)
    python_requirement = pyproject.get("project", {}).get("requires-python")

    # Additional config for wrknv projects
    extra_config = {}
    if workenv_name:
        extra_config["workenv_name"] = workenv_name
        extra_config["venv_prefix"] = workenv_name

    # Add Python requirement if present
    if python_requirement:
        extra_config["python_requirement"] = python_requirement

    # Get wrknv configuration
    from wrknv.config import WorkenvConfig

    config = WorkenvConfig.load()
    env_config = config.get_env_config()

    # Use configuration from wrknv.toml if available
    if env_config:
        logger.info("Using env configuration from wrknv.toml", env_config=env_config)
        extra_config["include_tool_verification"] = env_config.get("include_tool_verification", True)

        # Handle new unified siblings format
        siblings_config = env_config.get("siblings", [])
        if siblings_config:
            extra_config["siblings"] = siblings_config
            # Keep backward compatibility
            extra_config["sibling_patterns"] = []
            extra_config["special_siblings"] = []
        else:
            # Backward compatibility with old format
            extra_config["sibling_patterns"] = env_config.get("sibling_patterns", [])
            extra_config["special_siblings"] = env_config.get("special_siblings", [])
            extra_config["siblings"] = []
    else:
        # Default configuration - empty by default
        logger.info("No env configuration found in wrknv.toml, using defaults")
        extra_config["INCLUDE_TOOL_VERIFICATION"] = True
        extra_config["sibling_patterns"] = []
        extra_config["special_siblings"] = []
        extra_config["siblings"] = []

    # Generate scripts
    generator = EnvScriptGenerator()
    sh_path, ps1_path = generator.generate_both_scripts(project_name, project_dir, **extra_config)

    print_success(f"Generated {sh_path}", Emoji.SUCCESS)
    print_success(f"Generated {ps1_path}", Emoji.SUCCESS)

    return sh_path, ps1_path


def x_create_project_env_scripts__mutmut_105(project_dir: Path, workenv_name: str | None = None) -> tuple[Path, Path]:
    """Create environment scripts for a project based on its pyproject.toml.

    Args:
        project_dir: The project directory
        workenv_name: Optional workenv name override

    Returns:
        Tuple of (env.sh path, env.ps1 path)
    """
    pyproject_path = project_dir / "pyproject.toml"

    if not pyproject_path.exists():
        raise FileNotFoundError(f"No pyproject.toml found in {project_dir}")

    # Parse project name from pyproject.toml
    from provide.foundation.file.formats import read_toml

    pyproject = read_toml(pyproject_path)

    project_name = pyproject.get("project", {}).get("name", project_dir.name)
    python_requirement = pyproject.get("project", {}).get("requires-python")

    # Additional config for wrknv projects
    extra_config = {}
    if workenv_name:
        extra_config["workenv_name"] = workenv_name
        extra_config["venv_prefix"] = workenv_name

    # Add Python requirement if present
    if python_requirement:
        extra_config["python_requirement"] = python_requirement

    # Get wrknv configuration
    from wrknv.config import WorkenvConfig

    config = WorkenvConfig.load()
    env_config = config.get_env_config()

    # Use configuration from wrknv.toml if available
    if env_config:
        logger.info("Using env configuration from wrknv.toml", env_config=env_config)
        extra_config["include_tool_verification"] = env_config.get("include_tool_verification", True)

        # Handle new unified siblings format
        siblings_config = env_config.get("siblings", [])
        if siblings_config:
            extra_config["siblings"] = siblings_config
            # Keep backward compatibility
            extra_config["sibling_patterns"] = []
            extra_config["special_siblings"] = []
        else:
            # Backward compatibility with old format
            extra_config["sibling_patterns"] = env_config.get("sibling_patterns", [])
            extra_config["special_siblings"] = env_config.get("special_siblings", [])
            extra_config["siblings"] = []
    else:
        # Default configuration - empty by default
        logger.info("No env configuration found in wrknv.toml, using defaults")
        extra_config["include_tool_verification"] = False
        extra_config["sibling_patterns"] = []
        extra_config["special_siblings"] = []
        extra_config["siblings"] = []

    # Generate scripts
    generator = EnvScriptGenerator()
    sh_path, ps1_path = generator.generate_both_scripts(project_name, project_dir, **extra_config)

    print_success(f"Generated {sh_path}", Emoji.SUCCESS)
    print_success(f"Generated {ps1_path}", Emoji.SUCCESS)

    return sh_path, ps1_path


def x_create_project_env_scripts__mutmut_106(project_dir: Path, workenv_name: str | None = None) -> tuple[Path, Path]:
    """Create environment scripts for a project based on its pyproject.toml.

    Args:
        project_dir: The project directory
        workenv_name: Optional workenv name override

    Returns:
        Tuple of (env.sh path, env.ps1 path)
    """
    pyproject_path = project_dir / "pyproject.toml"

    if not pyproject_path.exists():
        raise FileNotFoundError(f"No pyproject.toml found in {project_dir}")

    # Parse project name from pyproject.toml
    from provide.foundation.file.formats import read_toml

    pyproject = read_toml(pyproject_path)

    project_name = pyproject.get("project", {}).get("name", project_dir.name)
    python_requirement = pyproject.get("project", {}).get("requires-python")

    # Additional config for wrknv projects
    extra_config = {}
    if workenv_name:
        extra_config["workenv_name"] = workenv_name
        extra_config["venv_prefix"] = workenv_name

    # Add Python requirement if present
    if python_requirement:
        extra_config["python_requirement"] = python_requirement

    # Get wrknv configuration
    from wrknv.config import WorkenvConfig

    config = WorkenvConfig.load()
    env_config = config.get_env_config()

    # Use configuration from wrknv.toml if available
    if env_config:
        logger.info("Using env configuration from wrknv.toml", env_config=env_config)
        extra_config["include_tool_verification"] = env_config.get("include_tool_verification", True)

        # Handle new unified siblings format
        siblings_config = env_config.get("siblings", [])
        if siblings_config:
            extra_config["siblings"] = siblings_config
            # Keep backward compatibility
            extra_config["sibling_patterns"] = []
            extra_config["special_siblings"] = []
        else:
            # Backward compatibility with old format
            extra_config["sibling_patterns"] = env_config.get("sibling_patterns", [])
            extra_config["special_siblings"] = env_config.get("special_siblings", [])
            extra_config["siblings"] = []
    else:
        # Default configuration - empty by default
        logger.info("No env configuration found in wrknv.toml, using defaults")
        extra_config["include_tool_verification"] = True
        extra_config["sibling_patterns"] = None
        extra_config["special_siblings"] = []
        extra_config["siblings"] = []

    # Generate scripts
    generator = EnvScriptGenerator()
    sh_path, ps1_path = generator.generate_both_scripts(project_name, project_dir, **extra_config)

    print_success(f"Generated {sh_path}", Emoji.SUCCESS)
    print_success(f"Generated {ps1_path}", Emoji.SUCCESS)

    return sh_path, ps1_path


def x_create_project_env_scripts__mutmut_107(project_dir: Path, workenv_name: str | None = None) -> tuple[Path, Path]:
    """Create environment scripts for a project based on its pyproject.toml.

    Args:
        project_dir: The project directory
        workenv_name: Optional workenv name override

    Returns:
        Tuple of (env.sh path, env.ps1 path)
    """
    pyproject_path = project_dir / "pyproject.toml"

    if not pyproject_path.exists():
        raise FileNotFoundError(f"No pyproject.toml found in {project_dir}")

    # Parse project name from pyproject.toml
    from provide.foundation.file.formats import read_toml

    pyproject = read_toml(pyproject_path)

    project_name = pyproject.get("project", {}).get("name", project_dir.name)
    python_requirement = pyproject.get("project", {}).get("requires-python")

    # Additional config for wrknv projects
    extra_config = {}
    if workenv_name:
        extra_config["workenv_name"] = workenv_name
        extra_config["venv_prefix"] = workenv_name

    # Add Python requirement if present
    if python_requirement:
        extra_config["python_requirement"] = python_requirement

    # Get wrknv configuration
    from wrknv.config import WorkenvConfig

    config = WorkenvConfig.load()
    env_config = config.get_env_config()

    # Use configuration from wrknv.toml if available
    if env_config:
        logger.info("Using env configuration from wrknv.toml", env_config=env_config)
        extra_config["include_tool_verification"] = env_config.get("include_tool_verification", True)

        # Handle new unified siblings format
        siblings_config = env_config.get("siblings", [])
        if siblings_config:
            extra_config["siblings"] = siblings_config
            # Keep backward compatibility
            extra_config["sibling_patterns"] = []
            extra_config["special_siblings"] = []
        else:
            # Backward compatibility with old format
            extra_config["sibling_patterns"] = env_config.get("sibling_patterns", [])
            extra_config["special_siblings"] = env_config.get("special_siblings", [])
            extra_config["siblings"] = []
    else:
        # Default configuration - empty by default
        logger.info("No env configuration found in wrknv.toml, using defaults")
        extra_config["include_tool_verification"] = True
        extra_config["XXsibling_patternsXX"] = []
        extra_config["special_siblings"] = []
        extra_config["siblings"] = []

    # Generate scripts
    generator = EnvScriptGenerator()
    sh_path, ps1_path = generator.generate_both_scripts(project_name, project_dir, **extra_config)

    print_success(f"Generated {sh_path}", Emoji.SUCCESS)
    print_success(f"Generated {ps1_path}", Emoji.SUCCESS)

    return sh_path, ps1_path


def x_create_project_env_scripts__mutmut_108(project_dir: Path, workenv_name: str | None = None) -> tuple[Path, Path]:
    """Create environment scripts for a project based on its pyproject.toml.

    Args:
        project_dir: The project directory
        workenv_name: Optional workenv name override

    Returns:
        Tuple of (env.sh path, env.ps1 path)
    """
    pyproject_path = project_dir / "pyproject.toml"

    if not pyproject_path.exists():
        raise FileNotFoundError(f"No pyproject.toml found in {project_dir}")

    # Parse project name from pyproject.toml
    from provide.foundation.file.formats import read_toml

    pyproject = read_toml(pyproject_path)

    project_name = pyproject.get("project", {}).get("name", project_dir.name)
    python_requirement = pyproject.get("project", {}).get("requires-python")

    # Additional config for wrknv projects
    extra_config = {}
    if workenv_name:
        extra_config["workenv_name"] = workenv_name
        extra_config["venv_prefix"] = workenv_name

    # Add Python requirement if present
    if python_requirement:
        extra_config["python_requirement"] = python_requirement

    # Get wrknv configuration
    from wrknv.config import WorkenvConfig

    config = WorkenvConfig.load()
    env_config = config.get_env_config()

    # Use configuration from wrknv.toml if available
    if env_config:
        logger.info("Using env configuration from wrknv.toml", env_config=env_config)
        extra_config["include_tool_verification"] = env_config.get("include_tool_verification", True)

        # Handle new unified siblings format
        siblings_config = env_config.get("siblings", [])
        if siblings_config:
            extra_config["siblings"] = siblings_config
            # Keep backward compatibility
            extra_config["sibling_patterns"] = []
            extra_config["special_siblings"] = []
        else:
            # Backward compatibility with old format
            extra_config["sibling_patterns"] = env_config.get("sibling_patterns", [])
            extra_config["special_siblings"] = env_config.get("special_siblings", [])
            extra_config["siblings"] = []
    else:
        # Default configuration - empty by default
        logger.info("No env configuration found in wrknv.toml, using defaults")
        extra_config["include_tool_verification"] = True
        extra_config["SIBLING_PATTERNS"] = []
        extra_config["special_siblings"] = []
        extra_config["siblings"] = []

    # Generate scripts
    generator = EnvScriptGenerator()
    sh_path, ps1_path = generator.generate_both_scripts(project_name, project_dir, **extra_config)

    print_success(f"Generated {sh_path}", Emoji.SUCCESS)
    print_success(f"Generated {ps1_path}", Emoji.SUCCESS)

    return sh_path, ps1_path


def x_create_project_env_scripts__mutmut_109(project_dir: Path, workenv_name: str | None = None) -> tuple[Path, Path]:
    """Create environment scripts for a project based on its pyproject.toml.

    Args:
        project_dir: The project directory
        workenv_name: Optional workenv name override

    Returns:
        Tuple of (env.sh path, env.ps1 path)
    """
    pyproject_path = project_dir / "pyproject.toml"

    if not pyproject_path.exists():
        raise FileNotFoundError(f"No pyproject.toml found in {project_dir}")

    # Parse project name from pyproject.toml
    from provide.foundation.file.formats import read_toml

    pyproject = read_toml(pyproject_path)

    project_name = pyproject.get("project", {}).get("name", project_dir.name)
    python_requirement = pyproject.get("project", {}).get("requires-python")

    # Additional config for wrknv projects
    extra_config = {}
    if workenv_name:
        extra_config["workenv_name"] = workenv_name
        extra_config["venv_prefix"] = workenv_name

    # Add Python requirement if present
    if python_requirement:
        extra_config["python_requirement"] = python_requirement

    # Get wrknv configuration
    from wrknv.config import WorkenvConfig

    config = WorkenvConfig.load()
    env_config = config.get_env_config()

    # Use configuration from wrknv.toml if available
    if env_config:
        logger.info("Using env configuration from wrknv.toml", env_config=env_config)
        extra_config["include_tool_verification"] = env_config.get("include_tool_verification", True)

        # Handle new unified siblings format
        siblings_config = env_config.get("siblings", [])
        if siblings_config:
            extra_config["siblings"] = siblings_config
            # Keep backward compatibility
            extra_config["sibling_patterns"] = []
            extra_config["special_siblings"] = []
        else:
            # Backward compatibility with old format
            extra_config["sibling_patterns"] = env_config.get("sibling_patterns", [])
            extra_config["special_siblings"] = env_config.get("special_siblings", [])
            extra_config["siblings"] = []
    else:
        # Default configuration - empty by default
        logger.info("No env configuration found in wrknv.toml, using defaults")
        extra_config["include_tool_verification"] = True
        extra_config["sibling_patterns"] = []
        extra_config["special_siblings"] = None
        extra_config["siblings"] = []

    # Generate scripts
    generator = EnvScriptGenerator()
    sh_path, ps1_path = generator.generate_both_scripts(project_name, project_dir, **extra_config)

    print_success(f"Generated {sh_path}", Emoji.SUCCESS)
    print_success(f"Generated {ps1_path}", Emoji.SUCCESS)

    return sh_path, ps1_path


def x_create_project_env_scripts__mutmut_110(project_dir: Path, workenv_name: str | None = None) -> tuple[Path, Path]:
    """Create environment scripts for a project based on its pyproject.toml.

    Args:
        project_dir: The project directory
        workenv_name: Optional workenv name override

    Returns:
        Tuple of (env.sh path, env.ps1 path)
    """
    pyproject_path = project_dir / "pyproject.toml"

    if not pyproject_path.exists():
        raise FileNotFoundError(f"No pyproject.toml found in {project_dir}")

    # Parse project name from pyproject.toml
    from provide.foundation.file.formats import read_toml

    pyproject = read_toml(pyproject_path)

    project_name = pyproject.get("project", {}).get("name", project_dir.name)
    python_requirement = pyproject.get("project", {}).get("requires-python")

    # Additional config for wrknv projects
    extra_config = {}
    if workenv_name:
        extra_config["workenv_name"] = workenv_name
        extra_config["venv_prefix"] = workenv_name

    # Add Python requirement if present
    if python_requirement:
        extra_config["python_requirement"] = python_requirement

    # Get wrknv configuration
    from wrknv.config import WorkenvConfig

    config = WorkenvConfig.load()
    env_config = config.get_env_config()

    # Use configuration from wrknv.toml if available
    if env_config:
        logger.info("Using env configuration from wrknv.toml", env_config=env_config)
        extra_config["include_tool_verification"] = env_config.get("include_tool_verification", True)

        # Handle new unified siblings format
        siblings_config = env_config.get("siblings", [])
        if siblings_config:
            extra_config["siblings"] = siblings_config
            # Keep backward compatibility
            extra_config["sibling_patterns"] = []
            extra_config["special_siblings"] = []
        else:
            # Backward compatibility with old format
            extra_config["sibling_patterns"] = env_config.get("sibling_patterns", [])
            extra_config["special_siblings"] = env_config.get("special_siblings", [])
            extra_config["siblings"] = []
    else:
        # Default configuration - empty by default
        logger.info("No env configuration found in wrknv.toml, using defaults")
        extra_config["include_tool_verification"] = True
        extra_config["sibling_patterns"] = []
        extra_config["XXspecial_siblingsXX"] = []
        extra_config["siblings"] = []

    # Generate scripts
    generator = EnvScriptGenerator()
    sh_path, ps1_path = generator.generate_both_scripts(project_name, project_dir, **extra_config)

    print_success(f"Generated {sh_path}", Emoji.SUCCESS)
    print_success(f"Generated {ps1_path}", Emoji.SUCCESS)

    return sh_path, ps1_path


def x_create_project_env_scripts__mutmut_111(project_dir: Path, workenv_name: str | None = None) -> tuple[Path, Path]:
    """Create environment scripts for a project based on its pyproject.toml.

    Args:
        project_dir: The project directory
        workenv_name: Optional workenv name override

    Returns:
        Tuple of (env.sh path, env.ps1 path)
    """
    pyproject_path = project_dir / "pyproject.toml"

    if not pyproject_path.exists():
        raise FileNotFoundError(f"No pyproject.toml found in {project_dir}")

    # Parse project name from pyproject.toml
    from provide.foundation.file.formats import read_toml

    pyproject = read_toml(pyproject_path)

    project_name = pyproject.get("project", {}).get("name", project_dir.name)
    python_requirement = pyproject.get("project", {}).get("requires-python")

    # Additional config for wrknv projects
    extra_config = {}
    if workenv_name:
        extra_config["workenv_name"] = workenv_name
        extra_config["venv_prefix"] = workenv_name

    # Add Python requirement if present
    if python_requirement:
        extra_config["python_requirement"] = python_requirement

    # Get wrknv configuration
    from wrknv.config import WorkenvConfig

    config = WorkenvConfig.load()
    env_config = config.get_env_config()

    # Use configuration from wrknv.toml if available
    if env_config:
        logger.info("Using env configuration from wrknv.toml", env_config=env_config)
        extra_config["include_tool_verification"] = env_config.get("include_tool_verification", True)

        # Handle new unified siblings format
        siblings_config = env_config.get("siblings", [])
        if siblings_config:
            extra_config["siblings"] = siblings_config
            # Keep backward compatibility
            extra_config["sibling_patterns"] = []
            extra_config["special_siblings"] = []
        else:
            # Backward compatibility with old format
            extra_config["sibling_patterns"] = env_config.get("sibling_patterns", [])
            extra_config["special_siblings"] = env_config.get("special_siblings", [])
            extra_config["siblings"] = []
    else:
        # Default configuration - empty by default
        logger.info("No env configuration found in wrknv.toml, using defaults")
        extra_config["include_tool_verification"] = True
        extra_config["sibling_patterns"] = []
        extra_config["SPECIAL_SIBLINGS"] = []
        extra_config["siblings"] = []

    # Generate scripts
    generator = EnvScriptGenerator()
    sh_path, ps1_path = generator.generate_both_scripts(project_name, project_dir, **extra_config)

    print_success(f"Generated {sh_path}", Emoji.SUCCESS)
    print_success(f"Generated {ps1_path}", Emoji.SUCCESS)

    return sh_path, ps1_path


def x_create_project_env_scripts__mutmut_112(project_dir: Path, workenv_name: str | None = None) -> tuple[Path, Path]:
    """Create environment scripts for a project based on its pyproject.toml.

    Args:
        project_dir: The project directory
        workenv_name: Optional workenv name override

    Returns:
        Tuple of (env.sh path, env.ps1 path)
    """
    pyproject_path = project_dir / "pyproject.toml"

    if not pyproject_path.exists():
        raise FileNotFoundError(f"No pyproject.toml found in {project_dir}")

    # Parse project name from pyproject.toml
    from provide.foundation.file.formats import read_toml

    pyproject = read_toml(pyproject_path)

    project_name = pyproject.get("project", {}).get("name", project_dir.name)
    python_requirement = pyproject.get("project", {}).get("requires-python")

    # Additional config for wrknv projects
    extra_config = {}
    if workenv_name:
        extra_config["workenv_name"] = workenv_name
        extra_config["venv_prefix"] = workenv_name

    # Add Python requirement if present
    if python_requirement:
        extra_config["python_requirement"] = python_requirement

    # Get wrknv configuration
    from wrknv.config import WorkenvConfig

    config = WorkenvConfig.load()
    env_config = config.get_env_config()

    # Use configuration from wrknv.toml if available
    if env_config:
        logger.info("Using env configuration from wrknv.toml", env_config=env_config)
        extra_config["include_tool_verification"] = env_config.get("include_tool_verification", True)

        # Handle new unified siblings format
        siblings_config = env_config.get("siblings", [])
        if siblings_config:
            extra_config["siblings"] = siblings_config
            # Keep backward compatibility
            extra_config["sibling_patterns"] = []
            extra_config["special_siblings"] = []
        else:
            # Backward compatibility with old format
            extra_config["sibling_patterns"] = env_config.get("sibling_patterns", [])
            extra_config["special_siblings"] = env_config.get("special_siblings", [])
            extra_config["siblings"] = []
    else:
        # Default configuration - empty by default
        logger.info("No env configuration found in wrknv.toml, using defaults")
        extra_config["include_tool_verification"] = True
        extra_config["sibling_patterns"] = []
        extra_config["special_siblings"] = []
        extra_config["siblings"] = None

    # Generate scripts
    generator = EnvScriptGenerator()
    sh_path, ps1_path = generator.generate_both_scripts(project_name, project_dir, **extra_config)

    print_success(f"Generated {sh_path}", Emoji.SUCCESS)
    print_success(f"Generated {ps1_path}", Emoji.SUCCESS)

    return sh_path, ps1_path


def x_create_project_env_scripts__mutmut_113(project_dir: Path, workenv_name: str | None = None) -> tuple[Path, Path]:
    """Create environment scripts for a project based on its pyproject.toml.

    Args:
        project_dir: The project directory
        workenv_name: Optional workenv name override

    Returns:
        Tuple of (env.sh path, env.ps1 path)
    """
    pyproject_path = project_dir / "pyproject.toml"

    if not pyproject_path.exists():
        raise FileNotFoundError(f"No pyproject.toml found in {project_dir}")

    # Parse project name from pyproject.toml
    from provide.foundation.file.formats import read_toml

    pyproject = read_toml(pyproject_path)

    project_name = pyproject.get("project", {}).get("name", project_dir.name)
    python_requirement = pyproject.get("project", {}).get("requires-python")

    # Additional config for wrknv projects
    extra_config = {}
    if workenv_name:
        extra_config["workenv_name"] = workenv_name
        extra_config["venv_prefix"] = workenv_name

    # Add Python requirement if present
    if python_requirement:
        extra_config["python_requirement"] = python_requirement

    # Get wrknv configuration
    from wrknv.config import WorkenvConfig

    config = WorkenvConfig.load()
    env_config = config.get_env_config()

    # Use configuration from wrknv.toml if available
    if env_config:
        logger.info("Using env configuration from wrknv.toml", env_config=env_config)
        extra_config["include_tool_verification"] = env_config.get("include_tool_verification", True)

        # Handle new unified siblings format
        siblings_config = env_config.get("siblings", [])
        if siblings_config:
            extra_config["siblings"] = siblings_config
            # Keep backward compatibility
            extra_config["sibling_patterns"] = []
            extra_config["special_siblings"] = []
        else:
            # Backward compatibility with old format
            extra_config["sibling_patterns"] = env_config.get("sibling_patterns", [])
            extra_config["special_siblings"] = env_config.get("special_siblings", [])
            extra_config["siblings"] = []
    else:
        # Default configuration - empty by default
        logger.info("No env configuration found in wrknv.toml, using defaults")
        extra_config["include_tool_verification"] = True
        extra_config["sibling_patterns"] = []
        extra_config["special_siblings"] = []
        extra_config["XXsiblingsXX"] = []

    # Generate scripts
    generator = EnvScriptGenerator()
    sh_path, ps1_path = generator.generate_both_scripts(project_name, project_dir, **extra_config)

    print_success(f"Generated {sh_path}", Emoji.SUCCESS)
    print_success(f"Generated {ps1_path}", Emoji.SUCCESS)

    return sh_path, ps1_path


def x_create_project_env_scripts__mutmut_114(project_dir: Path, workenv_name: str | None = None) -> tuple[Path, Path]:
    """Create environment scripts for a project based on its pyproject.toml.

    Args:
        project_dir: The project directory
        workenv_name: Optional workenv name override

    Returns:
        Tuple of (env.sh path, env.ps1 path)
    """
    pyproject_path = project_dir / "pyproject.toml"

    if not pyproject_path.exists():
        raise FileNotFoundError(f"No pyproject.toml found in {project_dir}")

    # Parse project name from pyproject.toml
    from provide.foundation.file.formats import read_toml

    pyproject = read_toml(pyproject_path)

    project_name = pyproject.get("project", {}).get("name", project_dir.name)
    python_requirement = pyproject.get("project", {}).get("requires-python")

    # Additional config for wrknv projects
    extra_config = {}
    if workenv_name:
        extra_config["workenv_name"] = workenv_name
        extra_config["venv_prefix"] = workenv_name

    # Add Python requirement if present
    if python_requirement:
        extra_config["python_requirement"] = python_requirement

    # Get wrknv configuration
    from wrknv.config import WorkenvConfig

    config = WorkenvConfig.load()
    env_config = config.get_env_config()

    # Use configuration from wrknv.toml if available
    if env_config:
        logger.info("Using env configuration from wrknv.toml", env_config=env_config)
        extra_config["include_tool_verification"] = env_config.get("include_tool_verification", True)

        # Handle new unified siblings format
        siblings_config = env_config.get("siblings", [])
        if siblings_config:
            extra_config["siblings"] = siblings_config
            # Keep backward compatibility
            extra_config["sibling_patterns"] = []
            extra_config["special_siblings"] = []
        else:
            # Backward compatibility with old format
            extra_config["sibling_patterns"] = env_config.get("sibling_patterns", [])
            extra_config["special_siblings"] = env_config.get("special_siblings", [])
            extra_config["siblings"] = []
    else:
        # Default configuration - empty by default
        logger.info("No env configuration found in wrknv.toml, using defaults")
        extra_config["include_tool_verification"] = True
        extra_config["sibling_patterns"] = []
        extra_config["special_siblings"] = []
        extra_config["SIBLINGS"] = []

    # Generate scripts
    generator = EnvScriptGenerator()
    sh_path, ps1_path = generator.generate_both_scripts(project_name, project_dir, **extra_config)

    print_success(f"Generated {sh_path}", Emoji.SUCCESS)
    print_success(f"Generated {ps1_path}", Emoji.SUCCESS)

    return sh_path, ps1_path


def x_create_project_env_scripts__mutmut_115(project_dir: Path, workenv_name: str | None = None) -> tuple[Path, Path]:
    """Create environment scripts for a project based on its pyproject.toml.

    Args:
        project_dir: The project directory
        workenv_name: Optional workenv name override

    Returns:
        Tuple of (env.sh path, env.ps1 path)
    """
    pyproject_path = project_dir / "pyproject.toml"

    if not pyproject_path.exists():
        raise FileNotFoundError(f"No pyproject.toml found in {project_dir}")

    # Parse project name from pyproject.toml
    from provide.foundation.file.formats import read_toml

    pyproject = read_toml(pyproject_path)

    project_name = pyproject.get("project", {}).get("name", project_dir.name)
    python_requirement = pyproject.get("project", {}).get("requires-python")

    # Additional config for wrknv projects
    extra_config = {}
    if workenv_name:
        extra_config["workenv_name"] = workenv_name
        extra_config["venv_prefix"] = workenv_name

    # Add Python requirement if present
    if python_requirement:
        extra_config["python_requirement"] = python_requirement

    # Get wrknv configuration
    from wrknv.config import WorkenvConfig

    config = WorkenvConfig.load()
    env_config = config.get_env_config()

    # Use configuration from wrknv.toml if available
    if env_config:
        logger.info("Using env configuration from wrknv.toml", env_config=env_config)
        extra_config["include_tool_verification"] = env_config.get("include_tool_verification", True)

        # Handle new unified siblings format
        siblings_config = env_config.get("siblings", [])
        if siblings_config:
            extra_config["siblings"] = siblings_config
            # Keep backward compatibility
            extra_config["sibling_patterns"] = []
            extra_config["special_siblings"] = []
        else:
            # Backward compatibility with old format
            extra_config["sibling_patterns"] = env_config.get("sibling_patterns", [])
            extra_config["special_siblings"] = env_config.get("special_siblings", [])
            extra_config["siblings"] = []
    else:
        # Default configuration - empty by default
        logger.info("No env configuration found in wrknv.toml, using defaults")
        extra_config["include_tool_verification"] = True
        extra_config["sibling_patterns"] = []
        extra_config["special_siblings"] = []
        extra_config["siblings"] = []

    # Generate scripts
    generator = None
    sh_path, ps1_path = generator.generate_both_scripts(project_name, project_dir, **extra_config)

    print_success(f"Generated {sh_path}", Emoji.SUCCESS)
    print_success(f"Generated {ps1_path}", Emoji.SUCCESS)

    return sh_path, ps1_path


def x_create_project_env_scripts__mutmut_116(project_dir: Path, workenv_name: str | None = None) -> tuple[Path, Path]:
    """Create environment scripts for a project based on its pyproject.toml.

    Args:
        project_dir: The project directory
        workenv_name: Optional workenv name override

    Returns:
        Tuple of (env.sh path, env.ps1 path)
    """
    pyproject_path = project_dir / "pyproject.toml"

    if not pyproject_path.exists():
        raise FileNotFoundError(f"No pyproject.toml found in {project_dir}")

    # Parse project name from pyproject.toml
    from provide.foundation.file.formats import read_toml

    pyproject = read_toml(pyproject_path)

    project_name = pyproject.get("project", {}).get("name", project_dir.name)
    python_requirement = pyproject.get("project", {}).get("requires-python")

    # Additional config for wrknv projects
    extra_config = {}
    if workenv_name:
        extra_config["workenv_name"] = workenv_name
        extra_config["venv_prefix"] = workenv_name

    # Add Python requirement if present
    if python_requirement:
        extra_config["python_requirement"] = python_requirement

    # Get wrknv configuration
    from wrknv.config import WorkenvConfig

    config = WorkenvConfig.load()
    env_config = config.get_env_config()

    # Use configuration from wrknv.toml if available
    if env_config:
        logger.info("Using env configuration from wrknv.toml", env_config=env_config)
        extra_config["include_tool_verification"] = env_config.get("include_tool_verification", True)

        # Handle new unified siblings format
        siblings_config = env_config.get("siblings", [])
        if siblings_config:
            extra_config["siblings"] = siblings_config
            # Keep backward compatibility
            extra_config["sibling_patterns"] = []
            extra_config["special_siblings"] = []
        else:
            # Backward compatibility with old format
            extra_config["sibling_patterns"] = env_config.get("sibling_patterns", [])
            extra_config["special_siblings"] = env_config.get("special_siblings", [])
            extra_config["siblings"] = []
    else:
        # Default configuration - empty by default
        logger.info("No env configuration found in wrknv.toml, using defaults")
        extra_config["include_tool_verification"] = True
        extra_config["sibling_patterns"] = []
        extra_config["special_siblings"] = []
        extra_config["siblings"] = []

    # Generate scripts
    generator = EnvScriptGenerator()
    sh_path, ps1_path = None

    print_success(f"Generated {sh_path}", Emoji.SUCCESS)
    print_success(f"Generated {ps1_path}", Emoji.SUCCESS)

    return sh_path, ps1_path


def x_create_project_env_scripts__mutmut_117(project_dir: Path, workenv_name: str | None = None) -> tuple[Path, Path]:
    """Create environment scripts for a project based on its pyproject.toml.

    Args:
        project_dir: The project directory
        workenv_name: Optional workenv name override

    Returns:
        Tuple of (env.sh path, env.ps1 path)
    """
    pyproject_path = project_dir / "pyproject.toml"

    if not pyproject_path.exists():
        raise FileNotFoundError(f"No pyproject.toml found in {project_dir}")

    # Parse project name from pyproject.toml
    from provide.foundation.file.formats import read_toml

    pyproject = read_toml(pyproject_path)

    project_name = pyproject.get("project", {}).get("name", project_dir.name)
    python_requirement = pyproject.get("project", {}).get("requires-python")

    # Additional config for wrknv projects
    extra_config = {}
    if workenv_name:
        extra_config["workenv_name"] = workenv_name
        extra_config["venv_prefix"] = workenv_name

    # Add Python requirement if present
    if python_requirement:
        extra_config["python_requirement"] = python_requirement

    # Get wrknv configuration
    from wrknv.config import WorkenvConfig

    config = WorkenvConfig.load()
    env_config = config.get_env_config()

    # Use configuration from wrknv.toml if available
    if env_config:
        logger.info("Using env configuration from wrknv.toml", env_config=env_config)
        extra_config["include_tool_verification"] = env_config.get("include_tool_verification", True)

        # Handle new unified siblings format
        siblings_config = env_config.get("siblings", [])
        if siblings_config:
            extra_config["siblings"] = siblings_config
            # Keep backward compatibility
            extra_config["sibling_patterns"] = []
            extra_config["special_siblings"] = []
        else:
            # Backward compatibility with old format
            extra_config["sibling_patterns"] = env_config.get("sibling_patterns", [])
            extra_config["special_siblings"] = env_config.get("special_siblings", [])
            extra_config["siblings"] = []
    else:
        # Default configuration - empty by default
        logger.info("No env configuration found in wrknv.toml, using defaults")
        extra_config["include_tool_verification"] = True
        extra_config["sibling_patterns"] = []
        extra_config["special_siblings"] = []
        extra_config["siblings"] = []

    # Generate scripts
    generator = EnvScriptGenerator()
    sh_path, ps1_path = generator.generate_both_scripts(None, project_dir, **extra_config)

    print_success(f"Generated {sh_path}", Emoji.SUCCESS)
    print_success(f"Generated {ps1_path}", Emoji.SUCCESS)

    return sh_path, ps1_path


def x_create_project_env_scripts__mutmut_118(project_dir: Path, workenv_name: str | None = None) -> tuple[Path, Path]:
    """Create environment scripts for a project based on its pyproject.toml.

    Args:
        project_dir: The project directory
        workenv_name: Optional workenv name override

    Returns:
        Tuple of (env.sh path, env.ps1 path)
    """
    pyproject_path = project_dir / "pyproject.toml"

    if not pyproject_path.exists():
        raise FileNotFoundError(f"No pyproject.toml found in {project_dir}")

    # Parse project name from pyproject.toml
    from provide.foundation.file.formats import read_toml

    pyproject = read_toml(pyproject_path)

    project_name = pyproject.get("project", {}).get("name", project_dir.name)
    python_requirement = pyproject.get("project", {}).get("requires-python")

    # Additional config for wrknv projects
    extra_config = {}
    if workenv_name:
        extra_config["workenv_name"] = workenv_name
        extra_config["venv_prefix"] = workenv_name

    # Add Python requirement if present
    if python_requirement:
        extra_config["python_requirement"] = python_requirement

    # Get wrknv configuration
    from wrknv.config import WorkenvConfig

    config = WorkenvConfig.load()
    env_config = config.get_env_config()

    # Use configuration from wrknv.toml if available
    if env_config:
        logger.info("Using env configuration from wrknv.toml", env_config=env_config)
        extra_config["include_tool_verification"] = env_config.get("include_tool_verification", True)

        # Handle new unified siblings format
        siblings_config = env_config.get("siblings", [])
        if siblings_config:
            extra_config["siblings"] = siblings_config
            # Keep backward compatibility
            extra_config["sibling_patterns"] = []
            extra_config["special_siblings"] = []
        else:
            # Backward compatibility with old format
            extra_config["sibling_patterns"] = env_config.get("sibling_patterns", [])
            extra_config["special_siblings"] = env_config.get("special_siblings", [])
            extra_config["siblings"] = []
    else:
        # Default configuration - empty by default
        logger.info("No env configuration found in wrknv.toml, using defaults")
        extra_config["include_tool_verification"] = True
        extra_config["sibling_patterns"] = []
        extra_config["special_siblings"] = []
        extra_config["siblings"] = []

    # Generate scripts
    generator = EnvScriptGenerator()
    sh_path, ps1_path = generator.generate_both_scripts(project_name, None, **extra_config)

    print_success(f"Generated {sh_path}", Emoji.SUCCESS)
    print_success(f"Generated {ps1_path}", Emoji.SUCCESS)

    return sh_path, ps1_path


def x_create_project_env_scripts__mutmut_119(project_dir: Path, workenv_name: str | None = None) -> tuple[Path, Path]:
    """Create environment scripts for a project based on its pyproject.toml.

    Args:
        project_dir: The project directory
        workenv_name: Optional workenv name override

    Returns:
        Tuple of (env.sh path, env.ps1 path)
    """
    pyproject_path = project_dir / "pyproject.toml"

    if not pyproject_path.exists():
        raise FileNotFoundError(f"No pyproject.toml found in {project_dir}")

    # Parse project name from pyproject.toml
    from provide.foundation.file.formats import read_toml

    pyproject = read_toml(pyproject_path)

    project_name = pyproject.get("project", {}).get("name", project_dir.name)
    python_requirement = pyproject.get("project", {}).get("requires-python")

    # Additional config for wrknv projects
    extra_config = {}
    if workenv_name:
        extra_config["workenv_name"] = workenv_name
        extra_config["venv_prefix"] = workenv_name

    # Add Python requirement if present
    if python_requirement:
        extra_config["python_requirement"] = python_requirement

    # Get wrknv configuration
    from wrknv.config import WorkenvConfig

    config = WorkenvConfig.load()
    env_config = config.get_env_config()

    # Use configuration from wrknv.toml if available
    if env_config:
        logger.info("Using env configuration from wrknv.toml", env_config=env_config)
        extra_config["include_tool_verification"] = env_config.get("include_tool_verification", True)

        # Handle new unified siblings format
        siblings_config = env_config.get("siblings", [])
        if siblings_config:
            extra_config["siblings"] = siblings_config
            # Keep backward compatibility
            extra_config["sibling_patterns"] = []
            extra_config["special_siblings"] = []
        else:
            # Backward compatibility with old format
            extra_config["sibling_patterns"] = env_config.get("sibling_patterns", [])
            extra_config["special_siblings"] = env_config.get("special_siblings", [])
            extra_config["siblings"] = []
    else:
        # Default configuration - empty by default
        logger.info("No env configuration found in wrknv.toml, using defaults")
        extra_config["include_tool_verification"] = True
        extra_config["sibling_patterns"] = []
        extra_config["special_siblings"] = []
        extra_config["siblings"] = []

    # Generate scripts
    generator = EnvScriptGenerator()
    sh_path, ps1_path = generator.generate_both_scripts(project_dir, **extra_config)

    print_success(f"Generated {sh_path}", Emoji.SUCCESS)
    print_success(f"Generated {ps1_path}", Emoji.SUCCESS)

    return sh_path, ps1_path


def x_create_project_env_scripts__mutmut_120(project_dir: Path, workenv_name: str | None = None) -> tuple[Path, Path]:
    """Create environment scripts for a project based on its pyproject.toml.

    Args:
        project_dir: The project directory
        workenv_name: Optional workenv name override

    Returns:
        Tuple of (env.sh path, env.ps1 path)
    """
    pyproject_path = project_dir / "pyproject.toml"

    if not pyproject_path.exists():
        raise FileNotFoundError(f"No pyproject.toml found in {project_dir}")

    # Parse project name from pyproject.toml
    from provide.foundation.file.formats import read_toml

    pyproject = read_toml(pyproject_path)

    project_name = pyproject.get("project", {}).get("name", project_dir.name)
    python_requirement = pyproject.get("project", {}).get("requires-python")

    # Additional config for wrknv projects
    extra_config = {}
    if workenv_name:
        extra_config["workenv_name"] = workenv_name
        extra_config["venv_prefix"] = workenv_name

    # Add Python requirement if present
    if python_requirement:
        extra_config["python_requirement"] = python_requirement

    # Get wrknv configuration
    from wrknv.config import WorkenvConfig

    config = WorkenvConfig.load()
    env_config = config.get_env_config()

    # Use configuration from wrknv.toml if available
    if env_config:
        logger.info("Using env configuration from wrknv.toml", env_config=env_config)
        extra_config["include_tool_verification"] = env_config.get("include_tool_verification", True)

        # Handle new unified siblings format
        siblings_config = env_config.get("siblings", [])
        if siblings_config:
            extra_config["siblings"] = siblings_config
            # Keep backward compatibility
            extra_config["sibling_patterns"] = []
            extra_config["special_siblings"] = []
        else:
            # Backward compatibility with old format
            extra_config["sibling_patterns"] = env_config.get("sibling_patterns", [])
            extra_config["special_siblings"] = env_config.get("special_siblings", [])
            extra_config["siblings"] = []
    else:
        # Default configuration - empty by default
        logger.info("No env configuration found in wrknv.toml, using defaults")
        extra_config["include_tool_verification"] = True
        extra_config["sibling_patterns"] = []
        extra_config["special_siblings"] = []
        extra_config["siblings"] = []

    # Generate scripts
    generator = EnvScriptGenerator()
    sh_path, ps1_path = generator.generate_both_scripts(project_name, **extra_config)

    print_success(f"Generated {sh_path}", Emoji.SUCCESS)
    print_success(f"Generated {ps1_path}", Emoji.SUCCESS)

    return sh_path, ps1_path


def x_create_project_env_scripts__mutmut_121(project_dir: Path, workenv_name: str | None = None) -> tuple[Path, Path]:
    """Create environment scripts for a project based on its pyproject.toml.

    Args:
        project_dir: The project directory
        workenv_name: Optional workenv name override

    Returns:
        Tuple of (env.sh path, env.ps1 path)
    """
    pyproject_path = project_dir / "pyproject.toml"

    if not pyproject_path.exists():
        raise FileNotFoundError(f"No pyproject.toml found in {project_dir}")

    # Parse project name from pyproject.toml
    from provide.foundation.file.formats import read_toml

    pyproject = read_toml(pyproject_path)

    project_name = pyproject.get("project", {}).get("name", project_dir.name)
    python_requirement = pyproject.get("project", {}).get("requires-python")

    # Additional config for wrknv projects
    extra_config = {}
    if workenv_name:
        extra_config["workenv_name"] = workenv_name
        extra_config["venv_prefix"] = workenv_name

    # Add Python requirement if present
    if python_requirement:
        extra_config["python_requirement"] = python_requirement

    # Get wrknv configuration
    from wrknv.config import WorkenvConfig

    config = WorkenvConfig.load()
    env_config = config.get_env_config()

    # Use configuration from wrknv.toml if available
    if env_config:
        logger.info("Using env configuration from wrknv.toml", env_config=env_config)
        extra_config["include_tool_verification"] = env_config.get("include_tool_verification", True)

        # Handle new unified siblings format
        siblings_config = env_config.get("siblings", [])
        if siblings_config:
            extra_config["siblings"] = siblings_config
            # Keep backward compatibility
            extra_config["sibling_patterns"] = []
            extra_config["special_siblings"] = []
        else:
            # Backward compatibility with old format
            extra_config["sibling_patterns"] = env_config.get("sibling_patterns", [])
            extra_config["special_siblings"] = env_config.get("special_siblings", [])
            extra_config["siblings"] = []
    else:
        # Default configuration - empty by default
        logger.info("No env configuration found in wrknv.toml, using defaults")
        extra_config["include_tool_verification"] = True
        extra_config["sibling_patterns"] = []
        extra_config["special_siblings"] = []
        extra_config["siblings"] = []

    # Generate scripts
    generator = EnvScriptGenerator()
    sh_path, ps1_path = generator.generate_both_scripts(project_name, project_dir, )

    print_success(f"Generated {sh_path}", Emoji.SUCCESS)
    print_success(f"Generated {ps1_path}", Emoji.SUCCESS)

    return sh_path, ps1_path


def x_create_project_env_scripts__mutmut_122(project_dir: Path, workenv_name: str | None = None) -> tuple[Path, Path]:
    """Create environment scripts for a project based on its pyproject.toml.

    Args:
        project_dir: The project directory
        workenv_name: Optional workenv name override

    Returns:
        Tuple of (env.sh path, env.ps1 path)
    """
    pyproject_path = project_dir / "pyproject.toml"

    if not pyproject_path.exists():
        raise FileNotFoundError(f"No pyproject.toml found in {project_dir}")

    # Parse project name from pyproject.toml
    from provide.foundation.file.formats import read_toml

    pyproject = read_toml(pyproject_path)

    project_name = pyproject.get("project", {}).get("name", project_dir.name)
    python_requirement = pyproject.get("project", {}).get("requires-python")

    # Additional config for wrknv projects
    extra_config = {}
    if workenv_name:
        extra_config["workenv_name"] = workenv_name
        extra_config["venv_prefix"] = workenv_name

    # Add Python requirement if present
    if python_requirement:
        extra_config["python_requirement"] = python_requirement

    # Get wrknv configuration
    from wrknv.config import WorkenvConfig

    config = WorkenvConfig.load()
    env_config = config.get_env_config()

    # Use configuration from wrknv.toml if available
    if env_config:
        logger.info("Using env configuration from wrknv.toml", env_config=env_config)
        extra_config["include_tool_verification"] = env_config.get("include_tool_verification", True)

        # Handle new unified siblings format
        siblings_config = env_config.get("siblings", [])
        if siblings_config:
            extra_config["siblings"] = siblings_config
            # Keep backward compatibility
            extra_config["sibling_patterns"] = []
            extra_config["special_siblings"] = []
        else:
            # Backward compatibility with old format
            extra_config["sibling_patterns"] = env_config.get("sibling_patterns", [])
            extra_config["special_siblings"] = env_config.get("special_siblings", [])
            extra_config["siblings"] = []
    else:
        # Default configuration - empty by default
        logger.info("No env configuration found in wrknv.toml, using defaults")
        extra_config["include_tool_verification"] = True
        extra_config["sibling_patterns"] = []
        extra_config["special_siblings"] = []
        extra_config["siblings"] = []

    # Generate scripts
    generator = EnvScriptGenerator()
    sh_path, ps1_path = generator.generate_both_scripts(project_name, project_dir, **extra_config)

    print_success(None, Emoji.SUCCESS)
    print_success(f"Generated {ps1_path}", Emoji.SUCCESS)

    return sh_path, ps1_path


def x_create_project_env_scripts__mutmut_123(project_dir: Path, workenv_name: str | None = None) -> tuple[Path, Path]:
    """Create environment scripts for a project based on its pyproject.toml.

    Args:
        project_dir: The project directory
        workenv_name: Optional workenv name override

    Returns:
        Tuple of (env.sh path, env.ps1 path)
    """
    pyproject_path = project_dir / "pyproject.toml"

    if not pyproject_path.exists():
        raise FileNotFoundError(f"No pyproject.toml found in {project_dir}")

    # Parse project name from pyproject.toml
    from provide.foundation.file.formats import read_toml

    pyproject = read_toml(pyproject_path)

    project_name = pyproject.get("project", {}).get("name", project_dir.name)
    python_requirement = pyproject.get("project", {}).get("requires-python")

    # Additional config for wrknv projects
    extra_config = {}
    if workenv_name:
        extra_config["workenv_name"] = workenv_name
        extra_config["venv_prefix"] = workenv_name

    # Add Python requirement if present
    if python_requirement:
        extra_config["python_requirement"] = python_requirement

    # Get wrknv configuration
    from wrknv.config import WorkenvConfig

    config = WorkenvConfig.load()
    env_config = config.get_env_config()

    # Use configuration from wrknv.toml if available
    if env_config:
        logger.info("Using env configuration from wrknv.toml", env_config=env_config)
        extra_config["include_tool_verification"] = env_config.get("include_tool_verification", True)

        # Handle new unified siblings format
        siblings_config = env_config.get("siblings", [])
        if siblings_config:
            extra_config["siblings"] = siblings_config
            # Keep backward compatibility
            extra_config["sibling_patterns"] = []
            extra_config["special_siblings"] = []
        else:
            # Backward compatibility with old format
            extra_config["sibling_patterns"] = env_config.get("sibling_patterns", [])
            extra_config["special_siblings"] = env_config.get("special_siblings", [])
            extra_config["siblings"] = []
    else:
        # Default configuration - empty by default
        logger.info("No env configuration found in wrknv.toml, using defaults")
        extra_config["include_tool_verification"] = True
        extra_config["sibling_patterns"] = []
        extra_config["special_siblings"] = []
        extra_config["siblings"] = []

    # Generate scripts
    generator = EnvScriptGenerator()
    sh_path, ps1_path = generator.generate_both_scripts(project_name, project_dir, **extra_config)

    print_success(f"Generated {sh_path}", None)
    print_success(f"Generated {ps1_path}", Emoji.SUCCESS)

    return sh_path, ps1_path


def x_create_project_env_scripts__mutmut_124(project_dir: Path, workenv_name: str | None = None) -> tuple[Path, Path]:
    """Create environment scripts for a project based on its pyproject.toml.

    Args:
        project_dir: The project directory
        workenv_name: Optional workenv name override

    Returns:
        Tuple of (env.sh path, env.ps1 path)
    """
    pyproject_path = project_dir / "pyproject.toml"

    if not pyproject_path.exists():
        raise FileNotFoundError(f"No pyproject.toml found in {project_dir}")

    # Parse project name from pyproject.toml
    from provide.foundation.file.formats import read_toml

    pyproject = read_toml(pyproject_path)

    project_name = pyproject.get("project", {}).get("name", project_dir.name)
    python_requirement = pyproject.get("project", {}).get("requires-python")

    # Additional config for wrknv projects
    extra_config = {}
    if workenv_name:
        extra_config["workenv_name"] = workenv_name
        extra_config["venv_prefix"] = workenv_name

    # Add Python requirement if present
    if python_requirement:
        extra_config["python_requirement"] = python_requirement

    # Get wrknv configuration
    from wrknv.config import WorkenvConfig

    config = WorkenvConfig.load()
    env_config = config.get_env_config()

    # Use configuration from wrknv.toml if available
    if env_config:
        logger.info("Using env configuration from wrknv.toml", env_config=env_config)
        extra_config["include_tool_verification"] = env_config.get("include_tool_verification", True)

        # Handle new unified siblings format
        siblings_config = env_config.get("siblings", [])
        if siblings_config:
            extra_config["siblings"] = siblings_config
            # Keep backward compatibility
            extra_config["sibling_patterns"] = []
            extra_config["special_siblings"] = []
        else:
            # Backward compatibility with old format
            extra_config["sibling_patterns"] = env_config.get("sibling_patterns", [])
            extra_config["special_siblings"] = env_config.get("special_siblings", [])
            extra_config["siblings"] = []
    else:
        # Default configuration - empty by default
        logger.info("No env configuration found in wrknv.toml, using defaults")
        extra_config["include_tool_verification"] = True
        extra_config["sibling_patterns"] = []
        extra_config["special_siblings"] = []
        extra_config["siblings"] = []

    # Generate scripts
    generator = EnvScriptGenerator()
    sh_path, ps1_path = generator.generate_both_scripts(project_name, project_dir, **extra_config)

    print_success(Emoji.SUCCESS)
    print_success(f"Generated {ps1_path}", Emoji.SUCCESS)

    return sh_path, ps1_path


def x_create_project_env_scripts__mutmut_125(project_dir: Path, workenv_name: str | None = None) -> tuple[Path, Path]:
    """Create environment scripts for a project based on its pyproject.toml.

    Args:
        project_dir: The project directory
        workenv_name: Optional workenv name override

    Returns:
        Tuple of (env.sh path, env.ps1 path)
    """
    pyproject_path = project_dir / "pyproject.toml"

    if not pyproject_path.exists():
        raise FileNotFoundError(f"No pyproject.toml found in {project_dir}")

    # Parse project name from pyproject.toml
    from provide.foundation.file.formats import read_toml

    pyproject = read_toml(pyproject_path)

    project_name = pyproject.get("project", {}).get("name", project_dir.name)
    python_requirement = pyproject.get("project", {}).get("requires-python")

    # Additional config for wrknv projects
    extra_config = {}
    if workenv_name:
        extra_config["workenv_name"] = workenv_name
        extra_config["venv_prefix"] = workenv_name

    # Add Python requirement if present
    if python_requirement:
        extra_config["python_requirement"] = python_requirement

    # Get wrknv configuration
    from wrknv.config import WorkenvConfig

    config = WorkenvConfig.load()
    env_config = config.get_env_config()

    # Use configuration from wrknv.toml if available
    if env_config:
        logger.info("Using env configuration from wrknv.toml", env_config=env_config)
        extra_config["include_tool_verification"] = env_config.get("include_tool_verification", True)

        # Handle new unified siblings format
        siblings_config = env_config.get("siblings", [])
        if siblings_config:
            extra_config["siblings"] = siblings_config
            # Keep backward compatibility
            extra_config["sibling_patterns"] = []
            extra_config["special_siblings"] = []
        else:
            # Backward compatibility with old format
            extra_config["sibling_patterns"] = env_config.get("sibling_patterns", [])
            extra_config["special_siblings"] = env_config.get("special_siblings", [])
            extra_config["siblings"] = []
    else:
        # Default configuration - empty by default
        logger.info("No env configuration found in wrknv.toml, using defaults")
        extra_config["include_tool_verification"] = True
        extra_config["sibling_patterns"] = []
        extra_config["special_siblings"] = []
        extra_config["siblings"] = []

    # Generate scripts
    generator = EnvScriptGenerator()
    sh_path, ps1_path = generator.generate_both_scripts(project_name, project_dir, **extra_config)

    print_success(f"Generated {sh_path}", )
    print_success(f"Generated {ps1_path}", Emoji.SUCCESS)

    return sh_path, ps1_path


def x_create_project_env_scripts__mutmut_126(project_dir: Path, workenv_name: str | None = None) -> tuple[Path, Path]:
    """Create environment scripts for a project based on its pyproject.toml.

    Args:
        project_dir: The project directory
        workenv_name: Optional workenv name override

    Returns:
        Tuple of (env.sh path, env.ps1 path)
    """
    pyproject_path = project_dir / "pyproject.toml"

    if not pyproject_path.exists():
        raise FileNotFoundError(f"No pyproject.toml found in {project_dir}")

    # Parse project name from pyproject.toml
    from provide.foundation.file.formats import read_toml

    pyproject = read_toml(pyproject_path)

    project_name = pyproject.get("project", {}).get("name", project_dir.name)
    python_requirement = pyproject.get("project", {}).get("requires-python")

    # Additional config for wrknv projects
    extra_config = {}
    if workenv_name:
        extra_config["workenv_name"] = workenv_name
        extra_config["venv_prefix"] = workenv_name

    # Add Python requirement if present
    if python_requirement:
        extra_config["python_requirement"] = python_requirement

    # Get wrknv configuration
    from wrknv.config import WorkenvConfig

    config = WorkenvConfig.load()
    env_config = config.get_env_config()

    # Use configuration from wrknv.toml if available
    if env_config:
        logger.info("Using env configuration from wrknv.toml", env_config=env_config)
        extra_config["include_tool_verification"] = env_config.get("include_tool_verification", True)

        # Handle new unified siblings format
        siblings_config = env_config.get("siblings", [])
        if siblings_config:
            extra_config["siblings"] = siblings_config
            # Keep backward compatibility
            extra_config["sibling_patterns"] = []
            extra_config["special_siblings"] = []
        else:
            # Backward compatibility with old format
            extra_config["sibling_patterns"] = env_config.get("sibling_patterns", [])
            extra_config["special_siblings"] = env_config.get("special_siblings", [])
            extra_config["siblings"] = []
    else:
        # Default configuration - empty by default
        logger.info("No env configuration found in wrknv.toml, using defaults")
        extra_config["include_tool_verification"] = True
        extra_config["sibling_patterns"] = []
        extra_config["special_siblings"] = []
        extra_config["siblings"] = []

    # Generate scripts
    generator = EnvScriptGenerator()
    sh_path, ps1_path = generator.generate_both_scripts(project_name, project_dir, **extra_config)

    print_success(f"Generated {sh_path}", Emoji.SUCCESS)
    print_success(None, Emoji.SUCCESS)

    return sh_path, ps1_path


def x_create_project_env_scripts__mutmut_127(project_dir: Path, workenv_name: str | None = None) -> tuple[Path, Path]:
    """Create environment scripts for a project based on its pyproject.toml.

    Args:
        project_dir: The project directory
        workenv_name: Optional workenv name override

    Returns:
        Tuple of (env.sh path, env.ps1 path)
    """
    pyproject_path = project_dir / "pyproject.toml"

    if not pyproject_path.exists():
        raise FileNotFoundError(f"No pyproject.toml found in {project_dir}")

    # Parse project name from pyproject.toml
    from provide.foundation.file.formats import read_toml

    pyproject = read_toml(pyproject_path)

    project_name = pyproject.get("project", {}).get("name", project_dir.name)
    python_requirement = pyproject.get("project", {}).get("requires-python")

    # Additional config for wrknv projects
    extra_config = {}
    if workenv_name:
        extra_config["workenv_name"] = workenv_name
        extra_config["venv_prefix"] = workenv_name

    # Add Python requirement if present
    if python_requirement:
        extra_config["python_requirement"] = python_requirement

    # Get wrknv configuration
    from wrknv.config import WorkenvConfig

    config = WorkenvConfig.load()
    env_config = config.get_env_config()

    # Use configuration from wrknv.toml if available
    if env_config:
        logger.info("Using env configuration from wrknv.toml", env_config=env_config)
        extra_config["include_tool_verification"] = env_config.get("include_tool_verification", True)

        # Handle new unified siblings format
        siblings_config = env_config.get("siblings", [])
        if siblings_config:
            extra_config["siblings"] = siblings_config
            # Keep backward compatibility
            extra_config["sibling_patterns"] = []
            extra_config["special_siblings"] = []
        else:
            # Backward compatibility with old format
            extra_config["sibling_patterns"] = env_config.get("sibling_patterns", [])
            extra_config["special_siblings"] = env_config.get("special_siblings", [])
            extra_config["siblings"] = []
    else:
        # Default configuration - empty by default
        logger.info("No env configuration found in wrknv.toml, using defaults")
        extra_config["include_tool_verification"] = True
        extra_config["sibling_patterns"] = []
        extra_config["special_siblings"] = []
        extra_config["siblings"] = []

    # Generate scripts
    generator = EnvScriptGenerator()
    sh_path, ps1_path = generator.generate_both_scripts(project_name, project_dir, **extra_config)

    print_success(f"Generated {sh_path}", Emoji.SUCCESS)
    print_success(f"Generated {ps1_path}", None)

    return sh_path, ps1_path


def x_create_project_env_scripts__mutmut_128(project_dir: Path, workenv_name: str | None = None) -> tuple[Path, Path]:
    """Create environment scripts for a project based on its pyproject.toml.

    Args:
        project_dir: The project directory
        workenv_name: Optional workenv name override

    Returns:
        Tuple of (env.sh path, env.ps1 path)
    """
    pyproject_path = project_dir / "pyproject.toml"

    if not pyproject_path.exists():
        raise FileNotFoundError(f"No pyproject.toml found in {project_dir}")

    # Parse project name from pyproject.toml
    from provide.foundation.file.formats import read_toml

    pyproject = read_toml(pyproject_path)

    project_name = pyproject.get("project", {}).get("name", project_dir.name)
    python_requirement = pyproject.get("project", {}).get("requires-python")

    # Additional config for wrknv projects
    extra_config = {}
    if workenv_name:
        extra_config["workenv_name"] = workenv_name
        extra_config["venv_prefix"] = workenv_name

    # Add Python requirement if present
    if python_requirement:
        extra_config["python_requirement"] = python_requirement

    # Get wrknv configuration
    from wrknv.config import WorkenvConfig

    config = WorkenvConfig.load()
    env_config = config.get_env_config()

    # Use configuration from wrknv.toml if available
    if env_config:
        logger.info("Using env configuration from wrknv.toml", env_config=env_config)
        extra_config["include_tool_verification"] = env_config.get("include_tool_verification", True)

        # Handle new unified siblings format
        siblings_config = env_config.get("siblings", [])
        if siblings_config:
            extra_config["siblings"] = siblings_config
            # Keep backward compatibility
            extra_config["sibling_patterns"] = []
            extra_config["special_siblings"] = []
        else:
            # Backward compatibility with old format
            extra_config["sibling_patterns"] = env_config.get("sibling_patterns", [])
            extra_config["special_siblings"] = env_config.get("special_siblings", [])
            extra_config["siblings"] = []
    else:
        # Default configuration - empty by default
        logger.info("No env configuration found in wrknv.toml, using defaults")
        extra_config["include_tool_verification"] = True
        extra_config["sibling_patterns"] = []
        extra_config["special_siblings"] = []
        extra_config["siblings"] = []

    # Generate scripts
    generator = EnvScriptGenerator()
    sh_path, ps1_path = generator.generate_both_scripts(project_name, project_dir, **extra_config)

    print_success(f"Generated {sh_path}", Emoji.SUCCESS)
    print_success(Emoji.SUCCESS)

    return sh_path, ps1_path


def x_create_project_env_scripts__mutmut_129(project_dir: Path, workenv_name: str | None = None) -> tuple[Path, Path]:
    """Create environment scripts for a project based on its pyproject.toml.

    Args:
        project_dir: The project directory
        workenv_name: Optional workenv name override

    Returns:
        Tuple of (env.sh path, env.ps1 path)
    """
    pyproject_path = project_dir / "pyproject.toml"

    if not pyproject_path.exists():
        raise FileNotFoundError(f"No pyproject.toml found in {project_dir}")

    # Parse project name from pyproject.toml
    from provide.foundation.file.formats import read_toml

    pyproject = read_toml(pyproject_path)

    project_name = pyproject.get("project", {}).get("name", project_dir.name)
    python_requirement = pyproject.get("project", {}).get("requires-python")

    # Additional config for wrknv projects
    extra_config = {}
    if workenv_name:
        extra_config["workenv_name"] = workenv_name
        extra_config["venv_prefix"] = workenv_name

    # Add Python requirement if present
    if python_requirement:
        extra_config["python_requirement"] = python_requirement

    # Get wrknv configuration
    from wrknv.config import WorkenvConfig

    config = WorkenvConfig.load()
    env_config = config.get_env_config()

    # Use configuration from wrknv.toml if available
    if env_config:
        logger.info("Using env configuration from wrknv.toml", env_config=env_config)
        extra_config["include_tool_verification"] = env_config.get("include_tool_verification", True)

        # Handle new unified siblings format
        siblings_config = env_config.get("siblings", [])
        if siblings_config:
            extra_config["siblings"] = siblings_config
            # Keep backward compatibility
            extra_config["sibling_patterns"] = []
            extra_config["special_siblings"] = []
        else:
            # Backward compatibility with old format
            extra_config["sibling_patterns"] = env_config.get("sibling_patterns", [])
            extra_config["special_siblings"] = env_config.get("special_siblings", [])
            extra_config["siblings"] = []
    else:
        # Default configuration - empty by default
        logger.info("No env configuration found in wrknv.toml, using defaults")
        extra_config["include_tool_verification"] = True
        extra_config["sibling_patterns"] = []
        extra_config["special_siblings"] = []
        extra_config["siblings"] = []

    # Generate scripts
    generator = EnvScriptGenerator()
    sh_path, ps1_path = generator.generate_both_scripts(project_name, project_dir, **extra_config)

    print_success(f"Generated {sh_path}", Emoji.SUCCESS)
    print_success(f"Generated {ps1_path}", )

    return sh_path, ps1_path

x_create_project_env_scripts__mutmut_mutants : ClassVar[MutantDict] = {
'x_create_project_env_scripts__mutmut_1': x_create_project_env_scripts__mutmut_1, 
    'x_create_project_env_scripts__mutmut_2': x_create_project_env_scripts__mutmut_2, 
    'x_create_project_env_scripts__mutmut_3': x_create_project_env_scripts__mutmut_3, 
    'x_create_project_env_scripts__mutmut_4': x_create_project_env_scripts__mutmut_4, 
    'x_create_project_env_scripts__mutmut_5': x_create_project_env_scripts__mutmut_5, 
    'x_create_project_env_scripts__mutmut_6': x_create_project_env_scripts__mutmut_6, 
    'x_create_project_env_scripts__mutmut_7': x_create_project_env_scripts__mutmut_7, 
    'x_create_project_env_scripts__mutmut_8': x_create_project_env_scripts__mutmut_8, 
    'x_create_project_env_scripts__mutmut_9': x_create_project_env_scripts__mutmut_9, 
    'x_create_project_env_scripts__mutmut_10': x_create_project_env_scripts__mutmut_10, 
    'x_create_project_env_scripts__mutmut_11': x_create_project_env_scripts__mutmut_11, 
    'x_create_project_env_scripts__mutmut_12': x_create_project_env_scripts__mutmut_12, 
    'x_create_project_env_scripts__mutmut_13': x_create_project_env_scripts__mutmut_13, 
    'x_create_project_env_scripts__mutmut_14': x_create_project_env_scripts__mutmut_14, 
    'x_create_project_env_scripts__mutmut_15': x_create_project_env_scripts__mutmut_15, 
    'x_create_project_env_scripts__mutmut_16': x_create_project_env_scripts__mutmut_16, 
    'x_create_project_env_scripts__mutmut_17': x_create_project_env_scripts__mutmut_17, 
    'x_create_project_env_scripts__mutmut_18': x_create_project_env_scripts__mutmut_18, 
    'x_create_project_env_scripts__mutmut_19': x_create_project_env_scripts__mutmut_19, 
    'x_create_project_env_scripts__mutmut_20': x_create_project_env_scripts__mutmut_20, 
    'x_create_project_env_scripts__mutmut_21': x_create_project_env_scripts__mutmut_21, 
    'x_create_project_env_scripts__mutmut_22': x_create_project_env_scripts__mutmut_22, 
    'x_create_project_env_scripts__mutmut_23': x_create_project_env_scripts__mutmut_23, 
    'x_create_project_env_scripts__mutmut_24': x_create_project_env_scripts__mutmut_24, 
    'x_create_project_env_scripts__mutmut_25': x_create_project_env_scripts__mutmut_25, 
    'x_create_project_env_scripts__mutmut_26': x_create_project_env_scripts__mutmut_26, 
    'x_create_project_env_scripts__mutmut_27': x_create_project_env_scripts__mutmut_27, 
    'x_create_project_env_scripts__mutmut_28': x_create_project_env_scripts__mutmut_28, 
    'x_create_project_env_scripts__mutmut_29': x_create_project_env_scripts__mutmut_29, 
    'x_create_project_env_scripts__mutmut_30': x_create_project_env_scripts__mutmut_30, 
    'x_create_project_env_scripts__mutmut_31': x_create_project_env_scripts__mutmut_31, 
    'x_create_project_env_scripts__mutmut_32': x_create_project_env_scripts__mutmut_32, 
    'x_create_project_env_scripts__mutmut_33': x_create_project_env_scripts__mutmut_33, 
    'x_create_project_env_scripts__mutmut_34': x_create_project_env_scripts__mutmut_34, 
    'x_create_project_env_scripts__mutmut_35': x_create_project_env_scripts__mutmut_35, 
    'x_create_project_env_scripts__mutmut_36': x_create_project_env_scripts__mutmut_36, 
    'x_create_project_env_scripts__mutmut_37': x_create_project_env_scripts__mutmut_37, 
    'x_create_project_env_scripts__mutmut_38': x_create_project_env_scripts__mutmut_38, 
    'x_create_project_env_scripts__mutmut_39': x_create_project_env_scripts__mutmut_39, 
    'x_create_project_env_scripts__mutmut_40': x_create_project_env_scripts__mutmut_40, 
    'x_create_project_env_scripts__mutmut_41': x_create_project_env_scripts__mutmut_41, 
    'x_create_project_env_scripts__mutmut_42': x_create_project_env_scripts__mutmut_42, 
    'x_create_project_env_scripts__mutmut_43': x_create_project_env_scripts__mutmut_43, 
    'x_create_project_env_scripts__mutmut_44': x_create_project_env_scripts__mutmut_44, 
    'x_create_project_env_scripts__mutmut_45': x_create_project_env_scripts__mutmut_45, 
    'x_create_project_env_scripts__mutmut_46': x_create_project_env_scripts__mutmut_46, 
    'x_create_project_env_scripts__mutmut_47': x_create_project_env_scripts__mutmut_47, 
    'x_create_project_env_scripts__mutmut_48': x_create_project_env_scripts__mutmut_48, 
    'x_create_project_env_scripts__mutmut_49': x_create_project_env_scripts__mutmut_49, 
    'x_create_project_env_scripts__mutmut_50': x_create_project_env_scripts__mutmut_50, 
    'x_create_project_env_scripts__mutmut_51': x_create_project_env_scripts__mutmut_51, 
    'x_create_project_env_scripts__mutmut_52': x_create_project_env_scripts__mutmut_52, 
    'x_create_project_env_scripts__mutmut_53': x_create_project_env_scripts__mutmut_53, 
    'x_create_project_env_scripts__mutmut_54': x_create_project_env_scripts__mutmut_54, 
    'x_create_project_env_scripts__mutmut_55': x_create_project_env_scripts__mutmut_55, 
    'x_create_project_env_scripts__mutmut_56': x_create_project_env_scripts__mutmut_56, 
    'x_create_project_env_scripts__mutmut_57': x_create_project_env_scripts__mutmut_57, 
    'x_create_project_env_scripts__mutmut_58': x_create_project_env_scripts__mutmut_58, 
    'x_create_project_env_scripts__mutmut_59': x_create_project_env_scripts__mutmut_59, 
    'x_create_project_env_scripts__mutmut_60': x_create_project_env_scripts__mutmut_60, 
    'x_create_project_env_scripts__mutmut_61': x_create_project_env_scripts__mutmut_61, 
    'x_create_project_env_scripts__mutmut_62': x_create_project_env_scripts__mutmut_62, 
    'x_create_project_env_scripts__mutmut_63': x_create_project_env_scripts__mutmut_63, 
    'x_create_project_env_scripts__mutmut_64': x_create_project_env_scripts__mutmut_64, 
    'x_create_project_env_scripts__mutmut_65': x_create_project_env_scripts__mutmut_65, 
    'x_create_project_env_scripts__mutmut_66': x_create_project_env_scripts__mutmut_66, 
    'x_create_project_env_scripts__mutmut_67': x_create_project_env_scripts__mutmut_67, 
    'x_create_project_env_scripts__mutmut_68': x_create_project_env_scripts__mutmut_68, 
    'x_create_project_env_scripts__mutmut_69': x_create_project_env_scripts__mutmut_69, 
    'x_create_project_env_scripts__mutmut_70': x_create_project_env_scripts__mutmut_70, 
    'x_create_project_env_scripts__mutmut_71': x_create_project_env_scripts__mutmut_71, 
    'x_create_project_env_scripts__mutmut_72': x_create_project_env_scripts__mutmut_72, 
    'x_create_project_env_scripts__mutmut_73': x_create_project_env_scripts__mutmut_73, 
    'x_create_project_env_scripts__mutmut_74': x_create_project_env_scripts__mutmut_74, 
    'x_create_project_env_scripts__mutmut_75': x_create_project_env_scripts__mutmut_75, 
    'x_create_project_env_scripts__mutmut_76': x_create_project_env_scripts__mutmut_76, 
    'x_create_project_env_scripts__mutmut_77': x_create_project_env_scripts__mutmut_77, 
    'x_create_project_env_scripts__mutmut_78': x_create_project_env_scripts__mutmut_78, 
    'x_create_project_env_scripts__mutmut_79': x_create_project_env_scripts__mutmut_79, 
    'x_create_project_env_scripts__mutmut_80': x_create_project_env_scripts__mutmut_80, 
    'x_create_project_env_scripts__mutmut_81': x_create_project_env_scripts__mutmut_81, 
    'x_create_project_env_scripts__mutmut_82': x_create_project_env_scripts__mutmut_82, 
    'x_create_project_env_scripts__mutmut_83': x_create_project_env_scripts__mutmut_83, 
    'x_create_project_env_scripts__mutmut_84': x_create_project_env_scripts__mutmut_84, 
    'x_create_project_env_scripts__mutmut_85': x_create_project_env_scripts__mutmut_85, 
    'x_create_project_env_scripts__mutmut_86': x_create_project_env_scripts__mutmut_86, 
    'x_create_project_env_scripts__mutmut_87': x_create_project_env_scripts__mutmut_87, 
    'x_create_project_env_scripts__mutmut_88': x_create_project_env_scripts__mutmut_88, 
    'x_create_project_env_scripts__mutmut_89': x_create_project_env_scripts__mutmut_89, 
    'x_create_project_env_scripts__mutmut_90': x_create_project_env_scripts__mutmut_90, 
    'x_create_project_env_scripts__mutmut_91': x_create_project_env_scripts__mutmut_91, 
    'x_create_project_env_scripts__mutmut_92': x_create_project_env_scripts__mutmut_92, 
    'x_create_project_env_scripts__mutmut_93': x_create_project_env_scripts__mutmut_93, 
    'x_create_project_env_scripts__mutmut_94': x_create_project_env_scripts__mutmut_94, 
    'x_create_project_env_scripts__mutmut_95': x_create_project_env_scripts__mutmut_95, 
    'x_create_project_env_scripts__mutmut_96': x_create_project_env_scripts__mutmut_96, 
    'x_create_project_env_scripts__mutmut_97': x_create_project_env_scripts__mutmut_97, 
    'x_create_project_env_scripts__mutmut_98': x_create_project_env_scripts__mutmut_98, 
    'x_create_project_env_scripts__mutmut_99': x_create_project_env_scripts__mutmut_99, 
    'x_create_project_env_scripts__mutmut_100': x_create_project_env_scripts__mutmut_100, 
    'x_create_project_env_scripts__mutmut_101': x_create_project_env_scripts__mutmut_101, 
    'x_create_project_env_scripts__mutmut_102': x_create_project_env_scripts__mutmut_102, 
    'x_create_project_env_scripts__mutmut_103': x_create_project_env_scripts__mutmut_103, 
    'x_create_project_env_scripts__mutmut_104': x_create_project_env_scripts__mutmut_104, 
    'x_create_project_env_scripts__mutmut_105': x_create_project_env_scripts__mutmut_105, 
    'x_create_project_env_scripts__mutmut_106': x_create_project_env_scripts__mutmut_106, 
    'x_create_project_env_scripts__mutmut_107': x_create_project_env_scripts__mutmut_107, 
    'x_create_project_env_scripts__mutmut_108': x_create_project_env_scripts__mutmut_108, 
    'x_create_project_env_scripts__mutmut_109': x_create_project_env_scripts__mutmut_109, 
    'x_create_project_env_scripts__mutmut_110': x_create_project_env_scripts__mutmut_110, 
    'x_create_project_env_scripts__mutmut_111': x_create_project_env_scripts__mutmut_111, 
    'x_create_project_env_scripts__mutmut_112': x_create_project_env_scripts__mutmut_112, 
    'x_create_project_env_scripts__mutmut_113': x_create_project_env_scripts__mutmut_113, 
    'x_create_project_env_scripts__mutmut_114': x_create_project_env_scripts__mutmut_114, 
    'x_create_project_env_scripts__mutmut_115': x_create_project_env_scripts__mutmut_115, 
    'x_create_project_env_scripts__mutmut_116': x_create_project_env_scripts__mutmut_116, 
    'x_create_project_env_scripts__mutmut_117': x_create_project_env_scripts__mutmut_117, 
    'x_create_project_env_scripts__mutmut_118': x_create_project_env_scripts__mutmut_118, 
    'x_create_project_env_scripts__mutmut_119': x_create_project_env_scripts__mutmut_119, 
    'x_create_project_env_scripts__mutmut_120': x_create_project_env_scripts__mutmut_120, 
    'x_create_project_env_scripts__mutmut_121': x_create_project_env_scripts__mutmut_121, 
    'x_create_project_env_scripts__mutmut_122': x_create_project_env_scripts__mutmut_122, 
    'x_create_project_env_scripts__mutmut_123': x_create_project_env_scripts__mutmut_123, 
    'x_create_project_env_scripts__mutmut_124': x_create_project_env_scripts__mutmut_124, 
    'x_create_project_env_scripts__mutmut_125': x_create_project_env_scripts__mutmut_125, 
    'x_create_project_env_scripts__mutmut_126': x_create_project_env_scripts__mutmut_126, 
    'x_create_project_env_scripts__mutmut_127': x_create_project_env_scripts__mutmut_127, 
    'x_create_project_env_scripts__mutmut_128': x_create_project_env_scripts__mutmut_128, 
    'x_create_project_env_scripts__mutmut_129': x_create_project_env_scripts__mutmut_129
}

def create_project_env_scripts(*args, **kwargs):
    result = _mutmut_trampoline(x_create_project_env_scripts__mutmut_orig, x_create_project_env_scripts__mutmut_mutants, args, kwargs)
    return result 

create_project_env_scripts.__signature__ = _mutmut_signature(x_create_project_env_scripts__mutmut_orig)
x_create_project_env_scripts__mutmut_orig.__name__ = 'x_create_project_env_scripts'


# 🧰🌍🔚
